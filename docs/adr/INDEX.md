# ADR Index — QuantMaster Pro

@responsibility ADR 번호 발급 SSOT — 충돌·누락 추적, 다음 발급 번호 단일 출처.

본 인덱스는 `docs/adr/*.md` 의 단일 발급 통로. 신규 ADR 작성 시 본 파일의 *다음 발급 번호* 만 사용하고, 작성 직후 본 파일에 한 줄 추가.

## 발급 룰

1. **다음 발급 번호** — 본 인덱스 §"다음 발급" 섹션의 정수값. 신규 ADR 의 파일명 prefix 는 정확히 그 값.
2. **번호 충돌 금지** — 동일 번호 ADR 파일이 ≥2건이면 발급 결함. 신규 ADR 작성 전 반드시 본 인덱스 갱신 (충돌 회피).
3. **건너뛰기 금지** — 누락된 번호는 §"누락 (Gap)" 에 기록만, 재사용 금지 (git history 추적성 보존).
4. **PR 머지 후** — CLAUDE.md "변경 이력" 추가 + 본 인덱스 §"전체 인덱스" 한 줄 추가 동시 의무.
5. **ADR 번호 = 1회 발급** — 머지된 ADR 의 번호 변경 금지 (외부 참조·git diff 무결성).

## 다음 발급

**다음 ADR 번호: `0630`**

(2026-06-18 기준, 마지막 발급 0629 — intraday-mover-candidate-pool-wiring. **Status: Proposed (Phase 0 — 경계·타입·ADR·flag/상수 계약 pin. flag default OFF byte-identical. 구현 engine-dev.)** ADR-0629 — ADR-0628 이 screener.json 을 당일 intraday 로 신선화했으나 **메인 스캔 candidate pool 이 그 우물을 안 읽는** 갭의 배선. 검증된 원인: buildCandidatePool 호출(persistScanResults:385~397) 입력은 (a) loadWatchlist 파생 existingWatchlist + (b) 직전 스캔 carry(previousDayTopRanked/openShadow 가 둘 다 _lastScanSummary.candidatePool.candidateSnapshots 자기복제 — "전일" 오명명·풀 고정)뿐. getScreenerCache(stockScreener:101) 를 읽는 곳은 intradayScanner.discoverIntradayCandidates(:248) 하나뿐이고 거기서도 intradayReady===true 15분 관문(candidateSelect:20)에 막혀 같은 스캔 미반영. TR(FHPST01700000 등락률·FHPST01710000 거래량 kisRankingClient:122~154)은 이미 당일 intraday — 신규 KIS 호출 불요·캐시 read 만(ADR-0561). 처방: buildCandidatePool 의 **이미 plumbing 된 intradayMovers 슬롯**(candidatePoolBuilder:269, addCandidates INTRADAY_MOVER 소비 :849, 태그 :11)에 신선 screener 당일 상위를 INTRADAY_MOVER 소스로 주입 → 오늘 리더(삼화콘덴서 +28%)가 intradayReady 15분 관문 우회·평가 풀 직접 진입, ADR-0628 D5 약세 회전이 낙오주 밀어냄. 복잡도 회피(필수): persistScanResults 1487줄/한계 13줄·candidatePoolBuilder 1131줄 여유 적음 → 신규 모듈 server/trading/signalScanner/scanDiagnostics/intradayMoverCandidateSource.ts(매핑·flag·상한 격리)·persistScanResults import1+호출 intradayMovers 프로퍼티1(순증≤4줄)·candidatePoolBuilder 0줄. 신규 flag INTRADAY_MOVER_CANDIDATE_SOURCE_ENABLED default OFF(===true ADR-0157·SSOT isIntradayMoverCandidateSourceEnabled)·상한 INTRADAY_MOVER_MAX_INJECT 기본20(0/NaN→20·상한 clamp50, 풀 폭주·과열 방지)·OFF면 collectIntradayMoverCandidates()→[] candidate pool byte-identical. 매핑 ScreenedStock→CandidatePoolInputCandidate(code/name/currentPrice/volume·changeRate 내림차순 상위N·source INTRADAY_MOVER addCandidates 자동태깅·featureScores 미매핑 buildCandidatePool missing 채점 위임). intradayReady 우회 안전성: candidate pool 직접 주입(채점 우회 아님 — Gate0~3+requiredScore70+overheatPenalty 여전 적용)·SHADOW_ONLY 무해·flag OFF opt-in·MAX_INJECT 상한. ⚠️향후 LIVE risk: 당일 급등주가 LIVE-eligible 풀 진입 가능(현 SHADOW 무해) — flag/상한/채점이 통제, LIVE 전환 시 별도 ADR 과열컷. 채점 불변: requiredScore=70/Gate0~3/STRONG/condition weight 0줄·유니버스 신선화지 채점 아님(ADR-0612 입력측 수리 정합). 타입 src/types 무수정(server-local·intradayMovers 슬롯 재사용). 신규 fetch 0(getScreenerCache 캐시 read). 9대 불변식 #1(catch [] fallback 무중단)·#2(shadow 표본 강화)·#3/#9(SourceSnapshot 무접촉·발굴 레이어 캐시 read·Gate provider 직접조회 0 — screener.json 은 ADR-0628 이 kisClient 단일통로로 채운 파일)·#6(빈캐시 [] 흡수 stale≠bearish)·#7(KIS L1)·#8(SHADOW live NONE) 보존. Patch Scope Guard(ADR-530): targetDomain signalScanner(1)·allowedFiles(intradayMoverCandidateSource 신규·persistScanResults import+호출 ≤4줄·.env.example 2 flag·*.test.ts)·forbiddenFiles(SourceSnapshot·autoTradeEngine·Gate0~3 채점·requiredScore SSOT·kisClient 본체·realDataKisGet·candidatePoolBuilder 본체·candidateSelect·preScreenStocks·src/**·aiUniverseService). rollback ENV 1줄(INTRADAY_MOVER_CANDIDATE_SOURCE_ENABLED=false). engine-dev 인계(신규 모듈 3함수·persistScanResults intradayMovers 1줄·회귀 7케이스 OFF[]byte-identical/ON 상위N/MAX_INJECT 상한·clamp/빈캐시 graceful/매핑 정합/통합). Alternatives: (a)candidateSelect 관문 완화 기각(별 책임·우회로 충분) (b)persistScanResults 인라인 기각(1500 한계) (c)candidatePoolBuilder read 신설 기각(SRP·슬롯 기존) (d)신규 슬롯/태그 신설 기각(intradayMovers 기plumbing) (e)오명명 재명명 분리(후속 patch) (f)intradayScanner 직접 push 기각(이중 주입) (g)신규 KIS fetch 기각(캐시 재사용 ADR-0561) (h)default ON 기각(opt-in) (i)전량 주입 기각(상한 통제). 계보 0628/0612/0617/0618/0561/0157/0146/0530. INDEX 0629→0630 갱신.)

(2026-06-18 기준, 마지막 발급 0628 — intraday-leader-universe-freshness. **Status: Proposed (Phase 0 — 경계·타입·ADR·flag/throttle 계약 pin. 양 flag default OFF byte-identical. 구현 engine-dev.)** ADR-0628 — 강세장(코스피 +1.45%)인데 13:37 장중 스캔 후보 0개의 근본수리. 검증된 원인=signalScanner 평가 풀(49)이 아침 스냅샷 고정, 두 겹: 원인①(구조적) intradayScanner.discoverIntradayCandidates(:214) 발굴 풀 2소스가 모두 아침 고정 — getExpandedUniverse(주간/장전) + getScreenerCache(:248, screener.json 은 preScreenStocks(:118 KIS 상승률·거래량·신고가·외인 4 TR)가 쓰는데 OPENING_AUCTION 08:45 1회뿐 tradingOrchestrator:526)·장중 KIS 순위 TR 재호출 코드 부재→09:00 이후 신규 리더(삼화콘덴서 +28%)는 풀에 코드 자체 부재. INTRADAY tick(:598)은 scanAndUpdateIntradayWatchlist 매분 호출하나 screener.json read-only. 원인②(설정) 발굴→평가풀 유일 다리 LeadershipBridge default OFF(isLeadershipBridgeEnabled leadershipBridge:30)·발굴돼도 intradayReady:false 슬롯만(:311)·selectCandidates(candidateSelect:20)는 watchlist+intradayReady===true 만 읽음→평가 풀 미도달. 둘 다 ON 이어야 신규 리더가 49 평가 풀 도달(D3 표). 운영자 확정 수리=screener.json 장중 갱신+bridge 활성(둘 다). 처방: D1 INTRADAY tick scanAndUpdateIntradayWatchlist 직전 flag-gated 쓰로틀 preScreenStocks 재호출 — 신규 flag INTRADAY_SCREENER_REFRESH_ENABLED default OFF(=== 'true' ADR-0157·SSOT isIntradayScreenerRefreshEnabled stockScreener co-locate)·쓰로틀 INTRADAY_SCREENER_REFRESH_MIN_INTERVAL_MIN 기본 12분(권장 10~15·하한 5분 clamp·getIntradayScreenerRefreshIntervalMs)·모듈 스코프 lastIntradayRefreshAt·wrapper maybeRefreshScreenerIntraday 캡슐화(미충족 KIS 호출 0·충족 시 preScreen+영속)·intradayScanner getScreenerCache(:248) 코드 0줄(screener.json 신선화로 자동 신선·intradayScanner+autoPopulateWatchlist 양쪽 한 수정). D2 LeadershipBridge 기존 flag LEADERSHIP_BRIDGE_ENABLED ON 권고(코드 0줄·ADR-0551 배선 기구현·.env.example 문서화). D4 채점 불변: requiredScore=70/Gate0~3 채점식/STRONG 승격식/condition weight/preScreen 4 TR 파라미터/qualifiesAsLeader 임계(gate4.5·mtas6·RS≥KOSPI) 0줄 무접촉 — 유니버스 신선화지 채점 변경 아님. 타입 src/types 무수정(server-local flag/throttle·additive only). 신규 fetch 설계 0(preScreen 재사용·두 번째 발굴 산식 0). executionImpact 현 SHADOW_ONLY 라 live 매수 NONE/OFF byte-identical/ON execution-adjacent(풀 확대·채점 0줄). providerImpact KIS quota — 12분 쓰로틀당 4 TR·일 상한 ≈128 추가 호출·하한 5분 clamp·KRX/Yahoo 0(ADR-0561 rate 관리). 9대 불변식 #1(catch 격리 무중단)·#2(shadow 표본 강화)·#3/#9(SourceSnapshot 무접촉·발굴 레이어만·Gate provider 직접조회 0·kisClient 단일통로 realDataKisGet)·#6(preScreen 실패 allSettled+emitProviderWarn 흡수·stale≠bearish)·#7(KIS L1·L4 0)·#8(SHADOW_ONLY live NONE) 보존. Patch Scope Guard(ADR-530): targetDomain screener+orchestrator(2)·allowedFiles(stockScreener SSOT+throttle+maybeRefreshScreenerIntraday·tradingOrchestrator INTRADAY 1 호출·.env.example 3 flag·*.test.ts)·forbiddenFiles(SourceSnapshot·autoTradeEngine·Gate0~3 채점·requiredScore SSOT·kisClient 본체·realDataKisGet 시그니처·bridgeLeadersToMomentum/qualifiesAsLeader 임계·candidateSelect 채점·src/**·aiUniverseService). rollback ENV 1줄(INTRADAY_SCREENER_REFRESH_ENABLED=false). engine-dev 인계(stockScreener SSOT 2함수+wrapper+lastIntradayRefreshAt·tradingOrchestrator INTRADAY 삽입·회귀 5케이스 OFF byte-identical/throttle 미충족 0/충족 1회+갱신/clamp/throw 무중단·LEADERSHIP_BRIDGE_ENABLED ON 권고+.env.example). Alternatives: (a)screener 갱신만 기각(intradayReady 전환 대기 지연) (b)bridge ON 만 기각(신선 후보 부재) (c)신규 랭킹 fetch 모듈 기각(preScreen 재사용·ADR-0561/0617) (d)intradayScanner KIS 직접 호출 기각(불변식 #9) (e)default ON 기각(opt-in) (f)쓰로틀 없는 매 tick 기각(quota 폭주) (g)requiredScore 완화 기각(신선화지 채점 아님·0627 계열). 계보 0551/0617/0618/0561/0157/0043/0445/0146. INDEX 0628→0629 갱신.)

(2026-06-18 기준, 마지막 발급 0627 — gate1-rs-percentile-continuous-and-breakout-ohlcv-field-fix. **Status: Proposed (Phase 0 — 경계·타입·ADR. flag default OFF byte-identical. 구현 engine-dev.)** ADR-0627 — Gate1 positive starvation(finalScoreAvg≈55<70·INSUFFICIENT_POSITIVE_SCORE 81%)의 진짜 wiring 갭 2종 교정. Audit 결론: `SCORE_CEILING_TOO_LOW` 은 오진(configuredPositiveMax 116≫70 천장 충분·observedPositiveMax 16.6 점화율 문제). ADR-0613 은 LIVE 배선 완료(minimumSignalScoreTrace:172/179/501)됐으나 입력 resolver 가 잘못된 필드를 봐 실효 0. GAP-A(RS step 양자화): decompositionBuilder:263~266 가 연속 percentile rsRankPct(0~100)를 5-bucket step(≥90→10·≥80→8·≥60→5·≥50→2·else 0)으로 붕괴→p<50 전부 0·RS avg +2.5/20 의 직접 원인=연속 정보 손실 버그. GAP-B(breakout OHLCV 필드명 불일치): gate1PositiveCeilingWiringAdr0613:97~99 resolveBreakoutOhlcvInput 가 high20/high60 만 보나 trace 카논 필드는 high20d(d접미·decompositionBuilder:217)→hasOhlcv=false 상시→flag ON 에서도 no-op·breakout avg +0.3/7 의 직접 원인. 부수정정: 0613 applyRsPercentileWiring 입력 crossSectionalPercentile 은 production write 0(죽은 필드·실 percentile 은 rsRankPct). 처방: D1 신규 flag GATE1_RS_PERCENTILE_CONTINUOUS_ENABLED default OFF(isGate1RsPercentileContinuousEnabled gateConfig SSOT·===true ADR-0157)→ON 시 decompositionBuilder relativeStrengthScore=clamp(rsRankPct/10,0,10) 연속·OFF→step byte-identical·maxScore 10/weight 1 무변경. D2 기존 flag GATE1_POSITIVE_CEILING_WIRING_ENABLED 재사용·resolveBreakoutOhlcvInput 키에 high20d/quote.high20d/quoteFeatures.high20d(high60·high120 동형) 추가·OFF byte-identical·ADR-0475 resolveBreakoutStructureSource 재사용(두 번째 공식 0). D3 dry-run 상시 관측(0613 breakout hypothetical §2 실효화+신규 rsContinuousPromotionDelta/rsContinuousHypotheticalActualScore/rsContinuousHypotheticalPassed force-ON·관측 전용·try/catch 격리 불변식#1). 채택 안 함: applyPositiveMaxNormalization(116분모 축소 역효과·required 충돌) default OFF 유지·ceiling 인상·required 70 완화·weight 인상·STRONG_BUY/threshold 변경 전부 금지. 절대제약: requiredScore=70 무접촉(validator check_gate1_required_score_ssot FORBIDDEN 무위반)·condition weight 불변·CONDITION_PASS_THRESHOLD=5 불변·thresholdAutoChanged=false. ADR-0471 freezeRule 정합(step→연속=손실 복원·임계 완화 아님·weight 인상 아님·p=100 동일 10 천장 무변). 신규 fetch 0. executionImpact OFF=NONE/ON=Gate1 점수 변화(현 SHADOW_ONLY 안전). 타입 additive optional(hypothetical 3필드·src/types 무수정 server-local). 계보 0467/0468/0471/0475/0613/0546/0157. INDEX 0627→0628 갱신.)

(2026-06-18 기준, 마지막 발급 0626 — shakeout-initial-loss-twobar-shadow. **Status: Proposed (Phase 0 — 경계·타입·ADR. ENV default OFF, LIVE byte-equivalent. 구현 engine-dev.)** ADR-0626(A) — 진입 직후 손실 초기 손절(INITIAL/REGIME)에 wick 즉시청산 대신 2-bar 종가확인 확대(진단 옵션 A·근본원인 #4). 기존 2-bar(ADR-0085)는 `isBepProtection===(stopLossExitType==='PROFIT_PROTECTION')` 일 때만 작동→손실초기 사각지대. seam=`twoBarBepGate.applyTwoBarBepGate` 결정트리 재구조화(1.BEP_DISABLED SKIP·2.isBepProtection 기존 BEP 경로·3.isBepProtection=false∧손실초기 확대게이트 a∧b∧c∧d 충족→evaluateTwoBarConfirmation({isBepProtection:true}) 재사용·4.그외 SKIP byte-equivalent). 게이트: (a)ENV SHAKEOUT_INITIAL_LOSS_TWOBAR_SHADOW_ENABLED==='true' default OFF·(b)getTradingMode()!=='LIVE'[LIVE 절대 우회 금지·불변식 #8·byte-identical]·(c)stopLossExitType∈{INITIAL,REGIME,INITIAL_AND_REGIME}·(d)returnPct>SHAKEOUT_INITIAL_LOSS_DEEP_DROP_BYPASS_PCT(-12.0)[깊은하락 우회·즉시손절 유지]. hardStopLoss.ts 호출부 stopLossExitType·returnPct 2인자 전달만(본체 불변). twoBarConfirmation.ts 0줄(정책함수 재사용·두 번째 산식 0). bepGlideTouchAt 재사용·신규 영속 필드 0. 타입 additive optional(TwoBarBepGateInput stopLossExitType?/returnPct?·src/types 무수정). LIVE byte-equivalent(OFF→SHADOW·LIVE 즉시손절·ON+LIVE→조건b=false SKIP byte-identical·ENV 1줄 롤백). 9대 불변식 #1/#2/#8 보존. 계보 0085/0157/0079/0112/0146. INDEX 0626→0627 갱신.)

(2026-06-18 기준, 마지막 발급 0625 — shakeout-stoploss-forward-outcome-labeler. **Status: Proposed (Phase 0 — 경계·타입·ADR. ENV default OFF byte-equivalent. 구현 engine-dev.)** ADR-0625(D) — 실행 포지션 HIT_STOP 청산 후 종목 급등(=셰이크아웃) 정량 학습 부재(진단 Q4·근본원인 #6). 기존 futureReturnResolver(ADR-0175)는 shadow-learning-only signal 대상이라 실행 청산 포지션 미도달. 신규 관측 모듈 `server/learning/shakeoutStopForwardLabeler.ts`(대상=loadShadowTrades HIT_STOP·exitTime·exitOutcome≠BE·exitPrice positive)·horizon 1/3/5/10영업일·청산가 대비 maxRecoveryPct≥SHAKEOUT_RECOVERY_THRESHOLD_PCT(5.0%·모듈 export SSOT)→isShakeout 확정(RESOLVED). 신규 repo `shakeoutStopOutcomeRepo.ts`(atomic tmp→rename·손상 JSON []·upsertLabel tradeId 키 RESOLVED 불변성 skip·loadLabels)·paths.ts SHAKEOUT_STOP_OUTCOME_LEDGER_FILE 1줄(shadow-trades.json 물리 분리 ADR-0445). priceFetcher 주입 seam=fetchHistoricalClosePrice(KIS 일봉 primary·ADR-0561)·미전달 전건 errors++·warn 1회(quota 0). cron shakeout_stop_forward_labeling KST16:50(`50 7 * * 1-5`·TRADING_DAY_ONLY·ENV 단락). ENV SHAKEOUT_STOP_FORWARD_LABELER_ENABLED==='true' default OFF(ADR-0157). 안전 invariant 7종(LIVE 본체 0줄·주문 import 0·KIS read-only·ENV OFF·호출자 1건·원본 불변·providerIssue≠marketSignal). executionImpact NONE(관측 전용·OFF byte-equivalent). src/types 무수정(server-local). 계보 0175/0445/0561/0157/0112/0045/0146. INDEX 0625→0626 갱신.)
(2026-06-18 기준, 마지막 발급 0624 — shadow-always-on-by-default-operator-decision-removal. **Status: Proposed (Phase 0 — 정책·경계 ADR. 설계 전용·코드 0줄. 구현은 승인 후 engine-dev/quality-guard.)** ADR-0624 — 운영자 지침(2026-06-18: ① 운영자 결정 의존 금지 ② always-on trading 최우선 ③ 기능 과잉=혼란). 진단: shadow 매수 0·학습 정체·게이트 빡빡의 원인=로직 부재 아니라 LIVE-안전 기능까지 `default OFF` 로 잠가 운영자 flag flip 대기(always-on 불변식 #1·#2 진입경로 위반). 오늘 Patch-LearningRegime-CanonicalSource-v2 노트도 "shadow 진입 개방은 ADR-0608/0619 flag ON 별도 필요" 명시(flag OFF 라 regime 고쳐도 안 열림). 처방: LIVE-안전 shadow 레버를 opt-in(default OFF)→opt-out(default ON) 반전 + 운영자 결정점을 SHADOW→LIVE 단일 경계로 축소 + flag 표면 축소(새 임계식·새 불변식·캠페인 레이어 0). D1 shadow 진입 자유화 default ON(ADR-0619 D2+0608 ENV 기본값 반전·live `isShadow=false` byte-identical·비상 kill `SHADOW_LIBERALIZATION_KILL` 1개=opt-in 2개→비상 OFF 1개 knob 축소). D2 shadow 학습 자동 적용(ADR-0581 shadow provider 자동 등록·실돈 무관 #8 이라 승인 불요·`PENDING_REVIEW` 정체 shadow 측 제거·운영자 승인은 SHADOW→LIVE 만). D3 flag 표면 축소(opt-in 2→비상 kill 1·캠페인/단계게이트 미추가). D4 유일 운영자 결정점=SHADOW→LIVE(돈 경계·default OFF 유지·data-triggered 1회 확인·`NON_LIVE_SOURCE`/clamp/canary 보존 #7). D5 본 ADR byte-equivalent(문서·코드 0·runtime 0). 이전 ADR-0624 초안(intelligence-activation-campaign 캠페인 프레이밍)을 운영자 지침 ①③ 충돌로 대체·폐기. 9대 불변식 텍스트 0줄(#1 always-on 강화·#2 default ON 실질보장·#7 LIVE 단일게이트·#8 분리 복원·#3/#4/#5/#6/#9 보존). ADR-0619 Alt(h)/0608/0157 default OFF 관성을 LIVE-safe shadow 한정 명시 override(LIVE 영향 변경은 default OFF 유지). Patch Scope Guard: targetDomain process/policy(0 코드)·allowedFiles(본 ADR·INDEX 다음발급+인덱스 한 줄·10-patch-history 한 줄·_workspace plan)·forbiddenFiles(server/**·src/**·.env*·인용 ADR 본문·00-project-charter 불변식)·executionImpact 문서 NONE/구현 D1·D2 shadow(live byte-identical)·D4 LIVE(default OFF 단일게이트)·rollback 문서 revert+kill-switch/ENV 1줄. 동반 `_workspace/2026-06-18_intelligence-activation/PLAN.md`. 계보 0619/0608/0581/0546/0173/0157/0146. INDEX 0624→0625 갱신.)
(2026-06-18 기준, 마지막 발급 0623 — price-correction-stage2-shadow-activation. **Status: Accepted (Stage 2a 구현 — ENV `PRICE_CORRECTION_SHADOW_ENABLED` default OFF byte-equivalent + Seam A diagnostics 집계 채움). Stage 2b(shadow corrected 치환)는 후속 PR.)** ADR-0623 — ADR-0414 Stage 1 Read-Only(dead-but-tested) 의 직속 후속. Stage 2a 가 채우는 것=ScanSummary `priceIntegrity`/`priceCorrection` diagnostics 집계(buyListLoop Seam A 가 kisIntradayCorrectionStep 직후 이미 보유한 reCheckQuote/currentPrice 로 evaluatePriceIntegrity→evaluatePriceCorrection→accumulatePriceIntegrityCorrection·외부 fetch 0·corrected→LIVE/Shadow 의사결정 미사용·try/catch 격리 불변식 #1). 신규 ENV SSOT isPriceCorrectionShadowEnabled()(priceCorrectionEngine 거주·=== 'true' 정확비교 ADR-0157·PRICE_CORRECTION_DISABLED 우선 게이팅). ScanCounters additive 옵셔널 priceIntegritySamples?/priceCorrectionSamples?(server-local·src/types 무수정·미초기화 시 byte-equivalent). persistScanResults reduce(statusCounts/correctionTypeCounts/averageConfidence/dropGapCalculationCount/shadowOnlySuggestedCount/topAffected). LIVE 무접촉(entryEngine/exitEngine/order executor/kisClient/autoTradeEngine 0줄·정적 grep 가드). Stage 2b(Seam B kisIntradayCorrection 시그니처 변경+corrected 복제 합성)는 본 PR 미구현. 9대 불변식 #1/#4/#8/#9·ADR-0414 invariant 10종 계승+Stage2 4종 보존. 롤백 ENV 1줄. 계보 0414/0412/0436/0608/0619/0157/0185~0189. INDEX 0623→0624 갱신.)

(2026-06-17 기준, 마지막 발급 0622 — universe-discovery-aggressiveness-topn-rs-percentile. **Status: Proposed (Phase 0 — 경계·타입·ADR. 양 flag default OFF byte-identical. 구현은 engine-dev 인계.)** ADR-0622 — 발굴 후보 21~22개 전부 KOSPI laggard(avgRelativeReturn20d≈−10~−13%)·STRONG/shadow 진입 0의 발굴 적극성 강화(3축). Audit 결론: ① 품질=신규 코드 0(ADR-0617 LEADER_UNIVERSE_INJECTION carry+union 보존 universeScanner:399~458 배선완료 + ADR-0618 LEADER_DAILY_REFRESH runLeaderUniverseDailyRefresh 실구현+cron leader_universe_daily_refresh KST08:10 배선완료가 외인·기관·시총 주도주 발굴 완비·두 flag default OFF·.env.example 미기재 → flag ON 권고+.env.example 4종 문서화=patch·ADR 발급 0) ② 수량=진짜 갭(universeScanner top-N 컷 2곳 60 하드코딩: kisRows.slice(0,60):356 + applyLeaderPreservation(.,60,.):441·scanUniverse 는 이미 krxFullMaster~2700 전수라 확대 불요) ③ 방향=진짜 갭(percentile 정렬 부재: calcStage1Score RS 0-floor 보너스는 절대 가점·risk-on분기만·랭크 변별 약함, quantitativeCandidateGenerator RS 필터는 MOMENTUM 전용·universeScanner 미적용, ADR-0616 leader/laggard 관측은 미배선). ②③ 같은 모듈·강결합 → 단일 ADR(targetDomain server/screener 1개). 처방: 신규 순수 모듈 universeDiscoveryAggressivenessAdr0622.ts(universeScanner 1210줄 보호). D1 ② top-N 상향 flag UNIVERSE_STAGE1_TOPN_EXPANSION_ENABLED default OFF·STAGE1_TOPN_BASE=60/EXPANDED=90·resolveStage1TopN()·:356/:441 wiring·OFF→60 byte-identical·quota KIS quote+30/스캔(budget lazy ADR-0558 컷 후 fetch 유지·eager 금지·KRX/Yahoo 0)·품질 희석 리스크 D2 병행 권고. D2 ③ RS percentile flag UNIVERSE_RS_PERCENTILE_RANK_ENABLED default OFF·RS=quote.return20d−kospi20dReturn(둘다%·ADR-0616 정의 재사용·두 번째 RS 공식 0·UNIVERSE_RS_LEADER_THRESHOLD=0 재사용)·cross-sectional percentile(결손 미부여·디모트 안함 결손≠laggard 불변식#6)·rsPriorityComparator(positive-RS 우선편입·laggard stage1Score 동률 후순위 디모트·RS 결손 중립)·OFF→stage1Score 단독 byte-identical·ADR-0617 주도주 union 보존 직교·rsScoreFromExcess구간/AXIS_WEIGHTS/requiredScore=70/STRONG 승격식 0줄(발굴 레이어만·Gate2 ADR-0621 직교). D3 dry-run 상시 관측(flag 무관 ADR-0616/0599 선례·wouldExpandTopNAddedCount/wouldRsPromoteCount/wouldLaggardDemoteCount/currentLaggardShare·ledger ADR-0614/0616 atomic tmp→rename+FIFO60+손상fallback+scanDateKey upsert·paths.ts UNIVERSE_DISCOVERY_AGGRESSIVENESS_LEDGER_FILE 1줄 ADR-0445·flag ON만 append·산출 flag 무관·top-N 컷 직후 stamp·try/catch 격리 불변식#1·0616 RS 산출식 import 재사용). D4 양 flag OFF byte-identical·독립(②만/③만/둘다)·ENV 1줄 롤백. D5 타입 additive only(UniverseDiscoveryAggressivenessObservation+ledger row·기존 CandidateStock/rsScoreFromExcess구간/AXIS_WEIGHTS/requiredScore/RS 정의 불변). executionImpact OFF=NONE byte-identical(quota 0)/② ON=execution-adjacent(후보 풀 확대+KIS fetch+30)/③ ON=execution-adjacent(정렬 변경·fetch 0)·현 SHADOW_ONLY 안전. 9대 불변식 #1/#3(SourceSnapshot 우회 0·macro carry 재사용)/#6(RS/benchmark 결손 정렬 무변경)/#7(주도주 source 매매 직접 0·품질 필터 유지)/#9(kisClient 단일통로·신규 raw 0) 보존. complexity universeScanner 1210(+~40·1500 여유). Patch Scope Guard: targetDomain server/screener(1)·allowedFiles(universeDiscoveryAggressivenessAdr0622 신규·universeScanner :356/:441 컷+정렬+stamp·paths.ts 1줄·.env.example ②2flag+①4flag·*.test.ts)·forbiddenFiles(autoTradeEngine·kisClient raw·buyPipeline 실주문·SourceSnapshot·calcStage1Score 점수식·gate2ConfluenceScore·rsScoreFromExcess구간·AXIS_WEIGHTS·GATE2_PASS_*_MIN_SCORE·STRONG 승격식·Gate1 requiredScore·두 번째 RS 공식·leaderUniverseInjectionAdr0617 본문). rollback ENV 1줄. engine-dev 인계(① .env.example 4종+flag ON 권고[코드0]·② resolveStage1TopN+:356/:441 wiring·③ computeRsPercentiles+rsPriorityComparator+:441 비교자·공통 ledger·회귀 OFF byte-identical/② ON 90+fetch+30/③ ON positive-RS 우선·laggard 디모트·결손 중립·benchmark 결손 무변경·주도주 union 직교/dry-run flag 무관/ledger atomic·FIFO·upsert·손상). Alternatives: (a)calcStage1Score RS_BONUS_CAP 인상 기각(절대 가점 변별 약함) (b)두 번째 RS 공식 기각(0616/0612 재사용) (c)scanUniverse 확대 기각(이미 전수) (d)②③ 별도 ADR 기각(같은 모듈 강결합) (e)① 신규 ADR 기각(0617/0618 완비·patch) (f)default ON 기각(opt-in) (g)Gate2/STRONG 손대기 기각(발굴 레이어만) (h)eager 전수 fetch 기각(budget lazy) (i)laggard hard-filter 기각(graceful 약화·soft 우선 정렬 후보 0 위험 없음 불변식#6). 계보 0616/0617/0618/0612/0550/0614/0599/0561/0558/0157/0445/0146. INDEX 0622→0623 갱신.)

(2026-06-16 기준, 마지막 발급 0621 — gate2-rs-benchmark-dualization-kosdaq. **Status: Proposed (Phase 0 — 경계·타입·ADR. flag default OFF byte-equivalent. 구현은 engine-dev 인계.)** ADR-0621 — 코스피 강세(+1.55%)/코스닥 약세(−1.60%) 디버전스 장에서 Gate2 STRONG 0건. 근본원인=`gate2ConfluenceScore.buildRsAxis`(:315~395 RETURN20D_MINUS_INDEX 분기 :360~380)가 RS excess 를 KOSPI 20일 수익률 단일 벤치마크(`kospi20dReturn`)로만 계산→코스닥 종목을 강한 KOSPI 대비 재면 초과수익 구조적 압축→BULLISH RS(score≥85·excess≥+8%p) 도달 불가→`bullishAxisCount` 미달→STRONG 승격(coverageAdjustedScore≥80 AND bullishAxisCount≥requiredConfluenceAxisCount, :787) 차단. 인프라 이미 존재: `benchmarkReturnNormalizer.ts`(selectBenchmarkForSymbol→KOSDAQ 종목 benchmarkKey:'KOSDAQ'·normalizeBenchmarkReturnForGate2→시장 맞는 벤치마크로 relativeReturn 계산·KOSDAQ 결손시 KOSPI fallback+진단노트)가 gate2Diagnostics 관측 경로에서만 가동·LIVE buildRsAxis 미배선(코드 갭 주석 CURRENT_GATE2_FORMULA_STILL_USES_CTX_KOSPI20D_RETURN). 신규 발견 갭 2건: (1)carry — candidate trace(decompositionBuilder.ts→buildGate2ConfluenceSummary)가 kospi20dReturn 만 carry·kosdaq20dReturn/market 미carry, LIVE caller(universeScanner·stockScreener :700·709) 도 kospi20dReturn 만 전달 (2)source — kosdaq20dReturn(코스닥 지수 20d 수익률)이 MacroState(core.ts:109 kospi20dReturn 만)·indexMacroSections 어디에도 미산출. 처방=두 번째 RS 공식 신설 0·새 임계 0: D1 buildRsAxis 의 RETURN20D_MINUS_INDEX 분기를 flag ON 시 normalizeBenchmarkReturnForGate2 relativeReturn 으로 배선(rsScoreFromExcess 산식·구간 무변경·올바른 excess 주입만·source 라벨 RETURN20D_MINUS_KOSDAQ) D2 flag GATE2_RS_KOSDAQ_BENCHMARK_ENABLED==='true' default OFF(ADR-0157)·SSOT isGate2RsKosdaqBenchmarkEnabled(gate2ConfluenceScore 거주·기존 flag 동거) D3 dry-run 상시 관측(flag 무관·ADR-0599 wouldPass*Proportional 동형) rsKosdaqBenchmarkDryRun(Gate2AxisScore RS축+Gate2EvaluationResult)+Gate2ConfluenceSummary.wouldStrongIfKosdaqBenchmark — KOSDAQ 벤치마크였다면 RS status/score 산출 D4 kosdaq20dReturn source 신규 fetch 0(fetchKoreanIndexDaily('KOSDAQ') KIS-first·sectorEnergyProvider:878 이미 fetch 재사용·nDayReturn(closes,20) KOSPI 동형·KIS Primary ADR-0561·결손시 KOSPI fallback 불변식 #6) D5 carry(market 출처=getStockByCode(code).market/quote.market/.KQ·.KS suffix=selectBenchmarkForSymbol 기존 처리·classifySymbol 은 KOSPI/KOSDAQ 미구분이라 사용불가·kosdaq20dReturn MacroState→trace decompositionBuilder:186 동형 1줄) D6 byte-equivalent(OFF→KOSPI 단일 벤치마크 100% 보존). 타입 additive optional 만(Gate2RsKosdaqBenchmarkDryRun·기존 필드/rsScoreFromExcess구간/AXIS_WEIGHTS/GATE2_PASS_*_MIN_SCORE/Gate1 requiredScore=70 불변·benchmarkKey enum 재사용). executionImpact OFF=NONE byte-equivalent(quota 0)/ON=gate2-scoring-adjacent(RS축 재계산만·주문/autoTradeEngine/kisClient/SourceSnapshot 0줄·현 SHADOW_ONLY 안전). 9대 불변식 #3(SourceSnapshot 우회 0·macro source 영속후 carry)·#6(KOSDAQ 결손 KOSPI fallback)·#7(L1 KIS 지수·L4 미사용)·#9(Gate 내부 provider 직접조회 0·kisClient 단일통로) 보존. Patch Scope Guard: targetDomain quant+marketDataRefresh+signalScanner(3 한계)·allowedFiles(gate2ConfluenceScore buildRsAxis flag분기+SSOT+dry-run+타입·indexMacroSections kosdaq20dReturn source·macroStateRepo+core.ts MacroState additive·decompositionBuilder/symbolFeatures/types carry·universeScanner/stockScreener caller carry·.env.example 1줄·*.test.ts)·forbiddenFiles(autoTradeEngine·kisClient raw·buyPipeline 실주문·SourceSnapshot·rsScoreFromExcess구간·AXIS_WEIGHTS·GATE2_PASS_*_MIN_SCORE·STRONG 승격식 :787·Gate1 requiredScore·두 번째 RS 공식). rollback ENV 1줄. engine-dev 인계(buildRsAxis flag 분기·kosdaq20dReturn source 재사용·carry 배선·dry-run 산출·회귀 9케이스 OFF byte-equivalent/ON KOSDAQ BULLISH/결손 fallback/STRONG 정합). Alternatives: (a)두 번째 RS 공식 신설 기각(ADR-0616·normalizer 재사용) (b)default ON 기각(opt-in) (c)sector benchmark 확대 기각(범위 초과·SECTOR_RETURN_FALLBACK 별도) (d)classifySymbol 시장구분 기각(KOSPI/KOSDAQ 미구분) (e)Gate 내부 직접 fetch 기각(불변식 #9) (f)Yahoo ^KQ11 primary 기각(ADR-0561) (g)patch type 기각(신규 flag/SSOT/타입/정책=ADR 의무). 계보 0612/0616/0617/0618/0599/0600/0561/0157/0526/0146. INDEX 0621→0622 갱신.)

(2026-06-16 기준, 마지막 발급 0620 — provisional-shadow-entry-price-measurement-wiring. **Status: Proposed (Phase 0 — 경계·타입·ADR. 측정 정합 정정. 구현은 engine-dev 인계.)** ADR-0620 — `/shadow_provisional` 실데이터 50건 전부 INSUFFICIENT_DATA(observed 0)의 근본 수리. 원인=신호/정책 결함 아닌 **측정 기준가 누락**: ProvisionalShadowLedgerEntry 에 entryPrice 필드 부재→buildEntry() 가 기준가 미기록→성과 리포트가 `(entry as {entryPrice?:number}).entryPrice` 로 읽으나 항상 undefined→전 horizon DATA_UNAVAILABLE→summary INSUFFICIENT_DATA. 기록 호출부(perSymbol/steps/provisionalShadowLane.ts→buyListLoop.ts:446) 시점 currentPrice(loopInit KIS 게이트 가격·실진입 revalidation 동일 소스, ADR-0561 정합)가 스코프 존재·신규 fetch 0. ADR vs patch 판정=ADR 발급(3개 영속·타입 경계 additive 필드 신설=SSOT 타입 계약 변경+ADR-0426/0427/0428/0431 provisional 계보 추적성 보존). 타입: entryPrice?:number + entryPriceSource?:'KIS_CURRENT'|'SCAN_SNAPSHOT'(ADR-0431 source 라벨 동형). 로직: derive positive-finite carry(0/NaN/음수/Infinity 거부)→buildEntry additive stamp→provisionalShadowLaneDerive(currentPrice 인자)→buyListLoop 전달(entryPriceSource:'KIS_CURRENT'). 하위호환: 기존 50건 미설정→INSUFFICIENT 유지(소급 backfill 0). 불변식: liveAllowed:false 불변·executionImpact=NONE(주문/fetch 0줄)·shadowLearningImpact=측정 정상화(#2 표본 강화)·9대 불변식 0줄·provisional/counterfactual/일반 shadow ledger 물리 분리 유지·#6(결손=측정 보류≠bearish)·#7(L1 측정 기준가·매매 직접 미사용). 계보 0426/0427/0428/0431/0561/0146. INDEX 0620→0621 갱신.)

(2026-06-16 기준, 마지막 발급 0619 — shadow-only-entry-liberalization-gate1-gate3. **Status: Proposed (Phase 0 — 경계·타입·ADR. flag default OFF byte-identical. 구현은 engine-dev 인계.)** ADR-0619 — 코스피 상승장인데 shadow paper-fill 0. 근본원인=shadow 진입이 live 와 **이중 게이트(Gate1 점수+Gate3 타이밍)** 공유, 불변식 #8(실거래 차단↔shadow 차단 분리) 의도 무력화. 차단 단일 지점 entryEngine.ts:370 `if(quoteSignalType==='SKIP' || (quoteGateScore??minGate)<minGate)` 가 두 독립 차단을 OR. **뒤 절(Gate1 점수)**=ADR-0608 seam(resolveEntryMinGateScore+GATE1_REGIME_AWARE_SHADOW_ENTRY_ENABLED+REGIME_AWARE_SHADOW floor-5 우회) 이미 머지·default OFF 라 비활성. **앞 절(Gate3 타이밍)**=`signalType==='SKIP'` shadow 우회 분기 전무(미해결 핵심). signalType='SKIP' 발생원 quantFilter.ts:990-1038 4종: (i)regime band 점수미달·(ii)MTAS≤3 구조약세=**완화 대상**(타이밍/점수대기 학습가치), (iii)VIX보수모드·(iv)isTradingHeld 연속손절홀드=**유지 차단**(리스크 게이트), +DATA_HOLD(ADR-0117 sanity)·SKIPPED_POLICY_BLOCK(liveEntryAllowed=false)=**절대 완화 금지**. 처방: D1 Gate1 완화=ADR-0608 flag **활성만 처방**(코드 변경 0·두 번째 임계식 신설 금지·engine-dev 는 Phase2 floor 우회 실적용 회귀 검증만). D2 Gate3 완화(신규 핵심)=:370 앞 절 `(signalType==='SKIP' && !shadowTimingBypass)` 재구조화, shadowTimingBypass=신규 ENV SHADOW_PREBREAKOUT_ENTRY_ENABLED==='true'(ADR-0157 default OFF) && isShadow===true && skipCause∈{BAND_MISS,MTAS_WEAK} 셋다 참일 때만(VIX/연속손절/DATA_HOLD/POLICY_BLOCK bypass 불가·skipCause UNKNOWN/미전달→차단 유지 불변식 #6). SKIP 사유 식별 seam=quantFilter signalType 산출 시점에 cause enum 동시 반환(details 문자열 파싱 금지 fragile)→entryRevalidationStep→evaluateEntryRevalidation carry. **불변식 #8 핵심**: bypass 후에도 뒤 절 `score<minGate`·DATA_HOLD·dropFromOpen/openGap/volume sanity 전부 그대로 평가(두 완화 직교·타이밍만 면제). live byte-identical=isShadow false∥ENV OFF→shadowTimingBypass 항상 false→`(SKIP&&!false)=SKIP` 연산 동치. D3 flag default OFF=둘 다 OFF→:370 현행 byte-identical, 두 flag 독립(Gate1만/Gate3만/둘다 조합·각 ENV 1줄 롤백). D4 레인/라벨 격리=entryThresholdMode 유니언 확장 'PREBREAKOUT_SHADOW'(additive·ADR-0608 'REGIME_AWARE_SHADOW' 와 별개·둘다 완화 시 우선순위 PREBREAKOUT_SHADOW 보수 층화)·학습 표본 분리 전용(청산/사이징/LIVE 판정 0·불변식 #8)·counterfactual(0426/0427)/provisional lane(0430)=진입가미도달 관측과 구분(본 라벨=실제 paper-fill). D5 관측 ledger(ADR-0614 패턴 atomic tmp→rename+rolling FIFO+손상 fallback+scanDateKey upsert)=스캔당 1행 liberalizedEntryCount/gate1LiberalizedCount/gate3LiberalizedCount/skipCauseDist(bandMiss·mtasWeak·vixBlocked·tradingHeld·unknown)·paths.ts SHADOW_ENTRY_LIBERALIZATION_LEDGER_FILE 1줄(0445 물리분리)·buyListLoop 스캔종료 직후 aggregate 1회 try/catch 격리(불변식 #1)·flag 무관 산출(OFF baseline 0). executionImpact: 양 flag OFF=NONE byte-identical(KIS/KRX quota 0)/ON=shadow paper-fill 확대(의도·autoTradeEngine/kisClient/buyPipeline 실주문/SourceSnapshot/live minGate 본문 0줄). shadowLearningImpact=확대(의도)+라벨 격리. 9대 불변식 #1/#2/#4/#7/**#8(분리 복원)** 보존. Patch Scope Guard: targetDomain entry/shadow(2 도메인)·allowedFiles(entryEngine :370 앞 절 bypass+skipCause·entryRevalidationStep carry+라벨·entryRevalidationGate 배선·quantFilter skipCause enum 동시반환[점수산식 무변경]·신규 shadowEntryLiberalizationLedgerAdr0619·gate1ShadowEntryThreshold 유니언확장+carry 우선순위·paths 1줄·shadowTradeRepo/types 유니언·src/types/autoTradeClient 미러·.env.example 1줄·*.test.ts)·forbiddenFiles(autoTradeEngine·kisClient raw·buyPipeline 실주문·SourceSnapshot·getMinGateScore live 분기·getEffectiveGateThreshold·Gate score 산식). engine-dev 인계(a)Gate1 완화 ADR-0608 flag 활성+floor 우회 회귀 검증 (b)Gate3 :370 앞 절 shadow bypass 재구조화+skipCause enum 정규화+완화 범위(band/MTAS만) (c)양 flag OFF·live byte-identical 연산 동치 (d)PREBREAKOUT_SHADOW 라벨 격리+우선순위 (e)관측 ledger atomic/FIFO/upsert (f)회귀 8케이스(OFF byte-identical·ON shadow band/MTAS 진입·VIX/홀드/DATA_HOLD 유지차단·live 유지·뒤 절 직교·UNKNOWN 보수·라벨·ledger). complexity entryEngine 572(+~25·1500 여유)/entryRevalidationStep 157/gate1ShadowEntryThreshold 81. Alternatives: (a):370 신규 임계식 기각(0608 seam 재사용) (b)전 SKIP 일괄완화 기각(VIX/홀드/DATA_HOLD 리스크·무결성 게이트) (c)details 파싱 기각(fragile·cause enum 동시반환) (d)단일 통합 flag 기각(Gate1/Gate3 독립 점진·롤백) (e)bypass 후 score 면제 기각(두 완화 직교·#8) (f)counterfactual lane 재사용 기각(미도달 관측≠paper-fill) (g)patch type 기각(신규 flag/라벨/ledger/진입 정책) (h)default ON 기각(opt-in). 계보 0608/0449/0426/0427/0430/0614/0117/0157/0146. INDEX 0619→0620 갱신.)

(2026-06-16 기준, 마지막 발급 0618 — leader-universe-daily-refresh. **Status: Proposed (Phase 0 — 경계·타입·ADR. flag default OFF byte-identical. 구현은 engine-dev 인계.)** ADR-0618 — ADR-0617 이 보존하는 주도주(LEADER_SOURCES={FOREIGN_NET_BUY,INST_NET_BUY,MARKET_CAP})는 dynamicUniverseExpander.getExpandedUniverse()/getExpandedUniverseSourceMap() 캐시(dynamic-universe.json)에서 오나, 그 캐시 수집 주기·TTL 이 주도주 신선도와 불일치: runDynamicUniverseExpansion cron=주 1회(토 09:00 KST·screenerJobs.ts:55 `0 0 * * 6` UTC·WEEKEND_MAINTENANCE) + 동적 엔트리 TTL=14일(EXPIRY_DAYS) → ADR-0617 이 **최대 2주 묵은 주도주**를 Stage1 보존 가능(외인·기관 수급 매일 회전→stale leader 주입 위험). 재사용 seam(핵심): expandOnEmpty(ttlDays=3)(dynamicUniverseExpander.ts:426-502)이 **이미 랭킹 기반 신선 발굴**(getShadowSafeRanking(k,{limit:30})×RANKING_KEYS 병렬 allSettled·5분 캐시·시장별 부분실패 허용→toDynamic(e,source) 매핑→단축 TTL 영속)→신규 fetch 설계 불요, 이 경로를 LEADER_SOURCES 매핑분에 한해 매 거래일 돌리면 됨. KIS 랭킹 키 가용(kisRankingClient RankingType): volume/fluctuation/market-cap/institutional-net-buy/large-volume/short-balance — **외국인 순매수 전용 TR 부재**(현행 expandOnEmpty 가 volume=거래량 상위 수급 유입 근사를 FOREIGN_NET_BUY 로 매핑, 본 ADR 도 그 근사 재사용·두 번째 매핑식 0·정밀 외인 TR 후속 ADR). 처방: **LEADER_SOURCES 3종만** 매 거래일 장전 신선 갱신해 ADR-0617 이 "오늘의 주도주" 보존. (1) 신규 함수 runLeaderUniverseDailyRefresh()(dynamicUniverseExpander additive): expandOnEmpty 랭킹 발굴 블록 LEADER 매핑분만(market-cap→MARKET_CAP·institutional-net-buy→INST_NET_BUY[value>0 필터]·volume→FOREIGN_NET_BUY 근사·**fluctuation/large-volume/short-balance 키 미호출**=MID_RISER/LARGE_VOLUME/SHORT_HEAVY 주간 유지·범위 밖), 단축 TTL LEADER_REFRESH_TTL_DAYS=3(per-entry expiresAt·stale 자동 만료), **공통 헬퍼 추출 채택**(toDynamic·getShadowSafeRanking allSettled·dedup 병합 루프를 collectRankingDynamicStocks 류 내부 헬퍼로 — expandOnEmpty 외부 동작 byte-identical 보존 필수·깨면 호출+중복 허용 후퇴), Telegram 무음. (2) 신규 cron(screenerJobs.ts+scheduleCatalog.ts): 매 거래일 장전 권장 **KST 08:10**(`10 23 * * 0-4` UTC·stage2_3 08:35 이전·kis_token_refresh 08:20/data_completeness_reset 08:00 stagger)·**TRADING_DAY_ONLY**·Telegram 무음(silentWhen)·flag-gated(콜백 진입 시 isLeaderUniverseDailyRefreshEnabled 단락·등록은 항상·본체 flag 단락 ENV 1줄 롤백). (3) flag LEADER_DAILY_REFRESH_ENABLED === 'true' default OFF(ADR-0157)·SSOT isLeaderUniverseDailyRefreshEnabled(dynamicUniverseExpander 거주·두 번째 enum 0)·**OFF→신규 cron 콜백 진입 직후 단락(랭킹 fetch 0·json 미변경)→주간 expansion 만→getExpandedUniverse* 현행 byte-identical**. (4) TTL/중복 정합(주간 14일↔일일 3일 공존): purgeExpired per-entry expiresAt(혼재 안전·무변경), 같은 code 충돌(일일 LEADER↔주간) → **expiresAt 일일 TTL upsert(연장)+source LEADER 재기입**(신선도 목적·carry 최신 반영), 주간 비주도 엔트리(MID_RISER 등)는 일일이 LEADER 키만 수집→미발견→기존 expiresAt·source 보존, **expandOnEmpty 의 dedup=skip(연장 안 함)과 의도적 분기**(refreshExisting 플래그·expandOnEmpty=false byte-identical/일일=true). (5) quota/KIS Primary(ADR-0561): getShadowSafeRanking 5분 캐시·KIS 단일 통로(신규 raw 0 불변식 #9)·일일 LEADER 3키×시장2=시장별 6 TR/거래일·주간 대비 증가분 **주당 +15 랭킹 호출**(5분 캐시 흡수·장전 1회 미미·KRX/Yahoo quota 0). executionImpact OFF=NONE byte-identical / ON=universe 신선도 변경(ADR-0617 도 ON 이면 보존 대상이 오늘 주도주로 바뀜 execution-adjacent·단 둘 다 default OFF·현 SHADOW_ONLY 출하 안전·본 ADR 단독 ON[0617 OFF]=carry/보존 미수행 실행 무영향 캐시만 신선). 9대 불변식 #1(cron 실패 격리·Trading Engine 무중단)·#3/#4(SourceSnapshot 미변경·동적 유니버스 영속만)·#9(KIS 단일 통로·신규 raw 0)·#7(L4 랭킹 매매 직접 결정 0) 보존. complexityGuard dynamicUniverseExpander 546줄(1,500 여유·순증 ~40-60줄). Patch Scope Guard: targetDomain server/screener+server/scheduler(2 도메인)·allowedFiles(dynamicUniverseExpander runLeaderUniverseDailyRefresh+헬퍼 추출+flag SSOT+TTL 상수·screenerJobs cron 1건·scheduleCatalog 1행·*.test.ts)·forbiddenFiles(autoTradeEngine·kisClient raw·SourceSnapshot·calcStage1Score·Gate score·runDynamicUniverseExpansion 주간 본문 미변경·leaderUniverseInjectionAdr0617 applyLeaderPreservation/ledger 본문 미변경). engine-dev 인계(runLeaderUniverseDailyRefresh 설계·헬퍼 추출 byte-identical·3일 TTL upsert·cron 등록 시각/가드/flag·OFF byte-identical·TTL/중복 정합·quota 산정·회귀 OFF byte-identical/ON LEADER 갱신/expandOnEmpty 동치/주간 무영향). Alternatives: (a)주간 expansion 통째 일일화 기각(범위 초과·quota·안정 소스 회전) (b)EXPIRY_DAYS 전역 단축 기각(비주도 조기 만료·유니버스 축소·per-entry TTL 정확) (c)신규 실시간 외인 TR 기각(kisClient 단일 통로·quota·후속) (d)헬퍼 추출 없이 expandOnEmpty 통째 호출 기각(5키·MID_RISER 범위 초과·중복 fetch·단 byte-identical 깨지면 후퇴) (e)cron 을 스캔 이후 배치 기각(스캔 전 신선화 필수·08:10 채택) (f)dedup skip 유지 기각(신선도 목적 upsert 연장 필요·의도적 분기) (g)default ON 기각(opt-in·0617 OFF 시 단독 효과 0) (h)patch type 기각(신규 cron·flag·함수·신선도 정책=ADR 의무). 계보 0617/0561/0157/0043/0445. INDEX 0618→0619 갱신.)

(2026-06-16 기준, 마지막 발급 0617 — leader-universe-stage1-preservation. **Status: Proposed (Phase 0 — 경계·타입·ADR. flag default OFF byte-identical. 구현은 engine-dev 인계.)** ADR-0617 — audit 재발견: "외인·기관 순매수 대형 주도주 발굴"은 이미 완전 구축·가동 중(dynamicUniverseExpander DynamicStock.source FOREIGN_NET_BUY/INST_NET_BUY/MARKET_CAP + runDynamicUniverseExpansion cron 수집·영속 + getExpandedUniverse 로드)이나 universeScanner stage1QuantFilter 가 3중 무력화: ① :388-390 scanUniverse=krxFullMaster(~2700)>expanded(~100)?full:expanded → 항상 full master → expandedUniverse 주도주 set 폐기 ② :405-412 CandidateStock 생성 시 source 태그 미탑재 → 주도주 식별 소실 ③ :424-427 sort(stage1Score).slice(0,60) → calcStage1Score 평시 눌림목 편향(소형주)이라 대형 주도주 60위 밖 탈락(ADR-0616 정량화 편향의 근본 원인 중 하나). 처방: expandedUniverse 주도주 source 태그를 Stage1 carry + 소형주 편향 top-60 점수컷에서 주도주 강제 보존(union)해 Stage2(수급 confluence·RS) 평가 도달, **신규 fetch 0**(cron 수집분 재사용)·flag default OFF byte-identical. 신규 순수 모듈 server/screener/leaderUniverseInjectionAdr0617.ts(로직 집약·universeScanner ~1175 보호). (a) carry: CandidateStock.source? additive optional(DynamicStock['source'] union 재사용·두 번째 enum 신설 금지)·carrySourceTags(expandedUniverse 종목만 태그·full master undefined·OFF→전건 undefined 기존 동치)·code→source Map 은 dynamicUniverseExpander getExpandedUniverseSourceMap additive getter 신설(기존 getExpandedUniverse 무파괴). (b) 강제 보존 **설계 (a) 60컷 후 union(중복제거) 채택**(vs 예약 슬롯 — union 이 top-N 비주도 후보 무손실·preservedCount=드롭됐을 수 직결): 주도주=LEADER_SOURCES{FOREIGN_NET_BUY,INST_NET_BUY,MARKET_CAP}만(52W_HIGH/MID_RISER/LARGE_VOLUME 수급 귀속 불명확·SHORT_HEAVY 반대 신호 보존 절대 금지·확대는 후속 ADR), **Stage1 기본 관문(price>0·tradable·관리종목·OVERHEAT·OVEREXTENDED) 통과 필수 — 점수-랭크 컷만 면제·품질 필터 유지**(입력은 이미 evaluateStage1FilterTracked 통과분 → +9% 과열 주도주는 OVERHEAT 에서 이미 탈락·보존 미도달·불변식 #7 정합), result=[...topN,...컷밖 주도주](dedup code 기준·stage1Score 비교자 재사용·두 번째 정렬식 0). (c) flag LEADER_UNIVERSE_INJECTION_ENABLED === 'true' default OFF(ADR-0157)·SSOT isLeaderUniverseInjectionEnabled()·OFF 시 :388-390 scanUniverse 선택·:424-427 top-60 컷 byte-identical·ENV 1줄 롤백. (d) 관측 ledger(ADR-0614/0616 패턴 atomic tmp→rename+rolling FIFO 60스캔+손상 fallback+scanDateKey upsert): 스캔당 1행 leadersInPoolCount/leadersInTopNCount/preservedCount(=드롭됐을 수)+source 태그 분포(leadersInPoolDist/preservedDist), paths.ts LEADER_UNIVERSE_INJECTION_LEDGER_FILE 1줄(0614/0616 물리 분리 ADR-0445), stamp=top-N 컷 직후 1회 aggregate·flag 무관 산출(OFF preservedCount=0 관측)·append flag ON만·try/catch 격리(불변식 #1). (e) 신규 fetch 0(getExpandedUniverse cron 캐시+이미 fetch 된 후보 quote read·KIS/KRX/Yahoo quota 0·신선도 개선은 후속 ADR). executionImpact OFF=NONE byte-identical / ON=EXECUTION_ADJACENT(유니버스 변경→LIVE 실매수 대상 변동·현 SHADOW_ONLY·default OFF 출하 안전·observationOnly/executionImpact 는 preservedCount>0 결정). 9대 불변식 #3(SourceSnapshot 우회 0·source 식별 전용)/#7(L4 태그 매매 결정 직접 금지·품질 필터 유지)/#9(kisClient 단일 통로·신규 fetch 0) 보존. Patch Scope Guard: targetDomain server/screener/*(3 이내)·allowedFiles(leaderUniverseInjectionAdr0617 신규·universeScanner stage1QuantFilter wiring·pipelineHelpers source? 1필드·dynamicUniverseExpander getExpandedUniverseSourceMap getter·paths.ts 1줄·*.test.ts)·forbiddenFiles(autoTradeEngine·kisClient raw fetch·SourceSnapshot·calcStage1Score 점수식 본문·evaluateStage1Filter 품질 필터 본문·Gate score). engine-dev 인계(carry·union 보존·관문 정책·flag OFF byte-identical·ledger·source Map getter·회귀 OFF byte-identical/ON 보존+ledger). Alternatives: (a)calcStage1Score 점수식 직접 교정 기각(평시 전략 충돌·미검증 즉시 영향) (b)예약 슬롯 기각(비주도 후보 손실) (c)품질 필터까지 면제 기각(추격 매수·불변식 #7) (d)52W_HIGH/LARGE_VOLUME 포함 기각(귀속 불명확·SHORT_HEAVY 반대) (e)신규 실시간 fetch 기각(quota·후속) (f)두 번째 source enum 기각(union 재사용) (g)default ON 기각(opt-in) (h)patch type 기각(신규 정책·경계·flag·ledger=ADR 의무). 계보 0616/0612/0550/0614/0561/0157/0445. INDEX 0617→0618 갱신.)

(2026-06-16 기준, 마지막 발급 0616 — universe-composition-bias-observation. **Status: Proposed (Phase 0 — 경계·타입·ADR. default OFF byte-equivalent. 구현은 engine-dev 인계.)** ADR-0616 — 유니버스 발굴이 코스닥 소형주/낙폭 반등주에 구조적 편향(디버전스 장 2026-06-16 코스피+1.55% vs 코스닥−1.60%에서 코스피 대형 주도주를 후보에 못 넣음·후보 RS −8.97% 코스피 9%p 언더퍼폼, ADR-0612 진단 연장). 운영자 결정: **편향을 바꾸기 전에 fetch-0·OFF-safe 관측 하네스로 정량화**(스캔별 KOSPI/KOSDAQ 비중 + leader/laggard 분포 누적 → 디버전스 장 편향 추세를 운영자가 직접 관측 후 별도 ADR 로 발굴 편향 교정). per-candidate stamp 아닌 **per-scan aggregate**: 후보 집합 전체에 대해 ① 시장 구성(getStockByCode(c.code).market 로컬 KRX 마스터 → KOSPI/KOSDAQ/기타[KONEX·OTHER·미매칭] 카운트, fetch 0) ② leader/laggard(RS=candidate.quote.return20d − benchmarkReturn20d[macroState.kospi20dReturn], **둘 다 % 동일 단위 직접 차감** — calcStage1Score UNIVERSE_RS_GATE 와 동일 RS 정의·두 번째 공식 0; RS>=0 leader / <0 laggard + rsAvg/Median[rsComputed 한정·0건 null] + 벤치마크 null→RS 전건 skip 결손≠0 불변식 #6) ③ 시총 tier(가능 시 listedShares×price → LARGE>=3조/MID>=1조/SMALL, **listedShares 결손/price<=0 종목은 skippedCount 만 증가·가짜 시총 0 불변식 #6**, 전원 불가 시 marketCapTierDist=null SKIP) ④ topLaggardCodes(RS 최하위 N 예시, 진단 표시). 신규 순수 모듈 server/screener/universeCompositionBiasObservationAdr0616.ts(getStockByCode 만 경유·신규 KIS/KRX/Yahoo 호출 0, RS 는 기존 trace 필드 재사용·두 번째 RS 공식 신설 금지, 로직 집약 → universeScanner ≤1500 보호). 타입 UniverseCompositionBiasObservation{scanDateKey, totalCandidates, kospi/kosdaq/otherCount, rsComputedCount/leaderCount/laggardCount/rsAvg/rsMedian/benchmarkReturn20d, marketCapTierDist?{large/mid/small/computed/skippedCount}|null, topLaggardCodes[]{code,rs}, executionImpact:NONE, observationOnly:true}. ledger 채택(ADR-0614 패턴: atomic write tmp→rename + rolling FIFO 60스캔 + 손상 JSON fallback + scanDateKey upsert last-write-wins) — **단일 배열 ledger**(per-stock Record 아닌 per-scan row), 운영자가 디버전스 장 편향 추세를 보려면 시계열 누적 필요(ADR-0615 ledger 불필요와 분기: 본 산출물은 영속 소스에 derived 미존재). paths.ts UNIVERSE_COMPOSITION_BIAS_LEDGER_FILE 1줄(consecutive-netbuy[0614]·gate2-sox[0605]·투자자흐름 cache 와 물리 분리 ADR-0445). stamp 지점 universeScanner.stage3AIScreenAndRegister Stage3 컨플루언스 루프 **이후 1회**(per-candidate 아님 — aggregate, ADR-0614 append per-stock 루프 종료 직후)·benchmark=macroState.kospi20dReturn 동 스코프 재사용·flag OFF→진입 skip byte-identical·try/catch 격리(불변식 #1·관측 실패 시 scan 본체 보호). ENV UNIVERSE_COMPOSITION_BIAS_OBSERVATION_ENABLED === 'true' default OFF(ADR-0157, opt-in 영속 I/O)·SSOT isUniverseCompositionBiasObservationEnabled(). **관측 전용**: universe 발굴/Stage1 필터/정렬/Gate score/주문/autoTradeEngine/kisClient/SourceSnapshot 0줄·ledger append + (선택) ScanSummary additive 집계만. L1(market·listedShares) source 이나 본 단계 매매 결정 직접 사용 아님(불변식 #7). executionImpact OFF=NONE byte-equivalent(KIS/KRX/Yahoo quota 0) / ON=per-scan aggregate 산출+ledger append+순수 산출(발굴·Gate 무배선). providerImpact 신규 fetch 0(getStockByCode 로컬 마스터 + 이미 fetch 된 후보 quote read only). 9대 불변식 #1/#6/#7 보존. engine-dev 인계(타입·순수 산출식·RS/시총 임계·now·getStockByCode 주입·ledger atomic/FIFO/upsert·stamp wiring·ENV·회귀 OFF byte-identical/ON append). Alternatives: (a)발굴 필터/정렬 직접 교정 기각(미검증 즉시 발굴 영향·운영자 "관측 먼저" 결정 위반) (b)per-candidate stamp 기각(구성 편향은 집합 속성·aggregate 가 정직) (c)두 번째 RS 공식 신설 기각(calcStage1Score RS 정의 재사용) (d)listedShares 결손 시 가짜 시총/0 대체 기각(불변식 #6) (e)ledger 없이 ScanSummary stamp 만 기각(디버전스 추세=시계열 누적 필요) (f)patch type 기각(신규 관측 ledger+개념=ADR 의무) (g)default ON 기각(opt-in). 계보 0612/0614/0615/0561/0157/0445/0476/0416/0421. INDEX 0616→0617 갱신.)

(2026-06-16 기준, 마지막 발급 0615 — news-lag-entry-window-observation. **Status: Proposed (Phase 0 — 경계·타입·ADR. default OFF byte-equivalent. 구현은 engine-dev 인계.)** ADR-0615 — newsLagBayesian 이 (newsType×sector) lag posterior(getOptimalEntryWindow meanLagDays/ci95Low·High/sampleSize, n<3→null)를 학습·영속하나 **dead**(/news_patterns 텔레그램 표시 1곳만 소비, 종목선정/진입 타이밍 호출 0건) — 학습 알파가 의사결정에 미도달. 이를 후보 선정 시점에 관측 전용으로 surface: 각 후보 sector 에 대해 (a)활성 뉴스(미완료 NewsSupplyRecord) × (b)학습 posterior 조인해 진입 윈도우 PRE/IN/PAST 분류·stamp. 핵심 조인 blocker(newsSupplyLogger 는 종목 아닌 (newsType×sector) 단위 기록 → 후보엔 per-stock newsType 없음)를 **sector 단위 조인으로 정직하게 우회**(candidate.sector↔record.sector, computeEtfSectorBoost c.sector 선례 동형); per-stock newsType 귀속은 후속 ADR. 신규 순수 모듈 server/learning/newsLagEntryWindowObservationAdr0615.ts(loadNewsSupplyRecords·getOptimalEntryWindow 기존 export 만 경유 — 내부 store 직접 접근 금지, 로직 집약 → universeScanner ≤1500 보호). 타입 NewsLagEntryWindowObservation{sector, matchedRecords[]{newsType,detectedAt,elapsedDays,meanLagDays,ci95Low/High,sampleSize,windowStatus:PRE|IN|PAST}, bestStatus(IN>PRE>PAST>null), executionImpact:NONE, observationOnly:true}, 후보 stamp=CandidateStock.newsLagEntryWindow? additive optional. 분류 임계 **ci95 채택**(getOptimalEntryWindow 가 이미 노출 — μ±σ 대비 두 번째 공식·재노출 0): elapsed<ci95Low→PRE / ci95Low≤elapsed≤ci95High→IN / >ci95High→PAST. elapsed=businessDaysElapsed(detectedAt, now) now 주입(테스트 결정성, newsSupplyLogger private 정의 복제·drift 테스트 고정). graceful skip(불변식 #6): posterior null(n<3)·손상 detectedAt·sector 미매칭 → 매칭 제외. stamp 지점 universeScanner.ts:696-719 Stage3 컨플루언스 루프(ADR-0614 append 인접) — 루프 진입 전 computeNewsLagEntryWindowBySector 1회(loadNewsSupplyRecords 1회 read, fetch 0)→루프 내 Map.get(c.sector) stamp, try/catch 격리(불변식 #1), flag OFF→null→no-op byte-identical. ScanSummary additive optional 1필드(sectorsMatched/inWindowCount/preWindowCount/pastWindowCount/executionImpact:NONE) Gate 미소비. **ledger 불필요(Phase 0)**: 입력이 이미 두 영속 소스(news-supply-records/news-lag-posterior)에 존재·산출물 derived/transient → 신규 paths.ts·ledger 파일 0(추세 관측 필요 시 후속 ADR ADR-0614 패턴). ENV NEWS_LAG_ENTRY_WINDOW_OBSERVATION_ENABLED === 'true' default OFF(ADR-0157, opt-in 영속 read)·SSOT isNewsLagEntryWindowObservationEnabled(). executionImpact: OFF=NONE byte-equivalent(KIS/KRX/Yahoo quota 0)/ON=후보 stamp+ScanSummary 집계+순수 산출만(Gate score/주문/autoTradeEngine/kisClient/SourceSnapshot/entryTimingSignal/computeEtfSectorBoost 본문/newsLag·newsSupply 내부 store 0줄). providerImpact 신규 fetch 0(로컬 영속 read only). 9대 불변식 #1/#6/#7 보존. 후속 승격(별도 ADR): per-stock newsType 귀속·IN_WINDOW forward outcome 성숙 후 Gate1 score/SHADOW 진입 레버. engine-dev 인계(타입·순수 산출식·분류 임계·now 주입·stamp wiring·ScanSummary 집계·회귀 OFF byte-identical/ON stamp). Alternatives: (a)entryTimingSignal/Gate 직접 배선 기각(미검증 즉시 실행 영향) (b)per-stock newsType 본 ADR 해결 기각(record 단위 불일치·가짜 매칭) (c)μ±σ 임계 기각(ci95 이미 노출·두 번째 공식 0) (d)신규 ledger 기각(입력 영속 존재·후속 ADR) (e)patch type 기각(신규 관측 신호·경계·stamp=ADR 의무) (f)default ON 기각(opt-in). 계보 0007/0614/0561/0157/0416/0421. INDEX 0615→0616 갱신.)

(2026-06-16 기준, 마지막 발급 0614 — consecutive-netbuy-observation-ledger. **Status: Proposed (Phase 0 — 경계·타입·ADR. default OFF byte-equivalent. 구현은 engine-dev 인계.)** ADR-0614 — 외국인/기관 순매수가 L1(KRX/KIS) 수집되나 supply_confluence(evaluators.ts:676-737)가 당일 동반 순매수만 판정해 연속(N영업일)/누적/시총대비 신호는 0건(매 스캔 단일 일자 스냅샷만 보고 버림). 이를 신규 관측 전용 시계열 하네스로 만든다(실측 누적 후 별도 ADR로 Gate score 승격). 핵심 제약 신규 fetch 0: 스캔 시점 이미 fetch 완료된 후보별 c.kisFlow 에 piggyback(universeScanner.ts:689-699 Stage3 컨플루언스 재평가 루프, runConfluenceEngine 호출 직후/인접)→ledger append, KIS/KRX quota 0. 신규 모듈 server/trading/signalScanner/consecutiveNetBuyLedgerAdr0614.ts(provider/now/fetch 0 순수 산출 + 영속) + paths.ts CONSECUTIVE_NETBUY_LEDGER_FILE 1줄(기존 investor-flow-cache/krx-parser-diag/gate1-dry-run ledger 와 물리 분리 ADR-0445). ledger row={dateKey,stockCode,foreignNetBuy,institutionalNetBuy,provider,marketCap?(가용 시만 null),fetchedAt,semanticStatus,executionImpact:NONE,observationOnly:true}, 캡=per-stock rolling window(기본 10영업일 FIFO, investorFlowCacheRepo slice 패턴) + 글로벌 종목 캡(기본 600). 신호 타입 ConsecutiveNetBuySignal(consecutiveForeign/Institution/BothDays·cumulative*·netBuyVsMarketCapPct(단위 정합 검증 전 null·가짜 비율 금지)·bothConsecutive·asOf). single 통로: investorFlowSemanticAvailability 경유 available(OK)일 때만 기록, 부재/DEGRADED/FIELD_MISSING 미기록(결손≠신호 불변식 #6). dedup=dateKey×stockCode upsert(last-write-wins, 장중 누적 증가 더 정확)·atomic write·재스캔 안전. ENV CONSECUTIVE_NETBUY_OBSERVATION_ENABLED === 'true' default OFF(ADR-0157, opt-in 영속 I/O)·SSOT isConsecutiveNetBuyObservationEnabled(). append try/catch 격리(불변식 #1). 관측 전용: Gate score/주문/autoTradeEngine/kisClient 0줄·ScanSummary additive optional 1필드 stamp만. L1 source이나 본 단계 매매 결정 직접 사용 아님(불변식 #7). executionImpact OFF=NONE byte-equivalent / ON=ledger append+stamp+순수 산출(Gate 무배선). engine-dev 인계(ledger 스키마·캡·신호 산출식·append wiring·dedup·ENV·회귀 OFF byte-identical/ON append+stamp). Alternatives: (a)evaluator 본문 직접 추가 기각(순수성·책임 혼재·즉시 score 영향) (b)별도 N일 fetch 기각(quota) (c)cache 누적 기각(물리 분리 위반) (d)patch type 기각(신규 ledger+신호 개념=ADR 의무) (e)default ON 기각(opt-in). 계보 0477/0502/0445/0476/0561/0157/0416/0421. INDEX 0614→0615 갱신.)

(2026-06-15 기준, 마지막 발급 0613 — gate1-positive-ceiling-wiring-off-by-default-promotion. **Status: Proposed (Phase 0 — 경계·타입·ADR. default OFF byte-equivalent. 구현은 engine-dev 인계.)** ADR-0613 — 운영자 진단 `/scan_blockers` 강세장(R3_EARLY)인데 Gate1 진입 0(finalScoreAvg 56.7 < required 70 + positive 천장 저활용 configuredPositiveMax 116 vs observedPositiveMax 49.2·utilizationByMax 42.4%·SCORE_CEILING_TOO_LOW, nextAction WIRE_RS_PERCENTILE_INPUT/WIRE_BREAKOUT_STRUCTURE/NORMALIZE_POSITIVE_MAX_TO_100)의 근본 원인=천장을 채울 입력(횡단면 RS percentile·BREAKOUT OHLCV 구조점수)·정규화가 dry-run 모듈에만 존재하고 LIVE scorer(buildMinimumSignalScoreTrace)에 미배선. 운영자 결정 "ENV 플래그 default OFF 로 wiring 머지 + 관측 ledger 활성" — 천장 배선을 LIVE 경로에 연결하되 flag OFF=byte-identical, 운영자 forward-outcome 성숙 후 직접 flip. **목적은 진입 증대 아님**(천장 정규화 단독 POSITIVE_REPAIR_ONLY 는 survivors 0+netAvg 30.7→5.5 하락 — 진입 레버는 RISK_SPLIT ADR-0470)이라 배선 안전 연결+관측 가능화가 목적. 처방: 신규 순수 SSOT server/trading/signalScanner/gate1PositiveCeilingWiringAdr0613.ts(provider/store/now/fetch 0)에 flag-gated transform 3종 집약 + buildMinimumSignalScoreTrace component 조립부에서만 분기, 기존 dry-run 함수(resolveBreakoutStructureSource/resolveRelativeStrengthSource ADR-0475·NORMALIZE_TO_100 ADR-0468)재사용(두 번째 점수 공식 0). ENV GATE1_POSITIVE_CEILING_WIRING_ENABLED === 'true' default OFF·SSOT isGate1PositiveCeilingWiringEnabled() gateConfig.ts 거주(ADR-0546 isGate1RegimeAwareRequiredEnabled 동거). 3 배선=(a)RS percentile 입력(ADR-0597 crossSectionalPercentile RELATIVE_STRENGTH 추가 우선순위·ADR-0469 dedup 정합·maxScore10 무변경) (b)BREAKOUT_STRUCTURE(trace OHLCV 시 resolveBreakoutStructureSource 결과 추가 소스·maxScore10 무변경) (c)positive max→100(ADR-0468 NORMALIZE_TO_100). requiredScore=70·computedScore=ΣweightedScore·passed 판정 무변경, flag OFF 셋 다 0효과. 관측 ledger=flag 무관 항상 hypothetical delta(ceilingWiringRsPercentileDelta/BreakoutDelta/NormalizeDelta/HypotheticalActualScore/HypotheticalPassed) ADR-0476 행 stamp(try/catch 격리 불변식 #1). ADR-0471 freeze 정합(곡선 변경 아닌 OFF-by-default 승격 경로 추가, ADR-0520 동일 철학). executionImpact: flag OFF NONE(byte-equivalent·KIS/KRX quota 0)/ON gate1-scoring-adjacent(천장 회계만·autoTradeEngine/kisClient/order 0줄·requiredScore 무변경). engine-dev 인계(SSOT 모듈·scorer 분기·ledger stamp·회귀 OFF byte-identical/ON delta). 계보 0467/0468/0471/0475/0476/0520/0546/0597/0611/0146. INDEX 0613→0614 갱신.)

(2026-06-15 기준, 마지막 발급 0612 — universe-relative-strength-gate. **Status: Accepted (구현 — ENV flag default OFF byte-equivalent, 운영자 활성화 대기).** ADR-0612 — 운영자 진단 "강세장(KOSPI 20일 +7.5%)인데 스캔 후보가 주도주가 아니라 낙폭과대 반등주(avgRelativeReturn20d=−9%·median −12%, avgReturn5d +13% vs 20d −1.4%)로 채워짐"의 근본 수리. 원인=유니버스 발굴(quantitativeCandidateGenerator MOMENTUM: 절대 momentum20d+거래대금)·Stage1 점수(calcStage1Score: 절대 changePercent/return5d/high20d, 평시 눌림목 보상)가 전부 절대수익만 보고 시장상대강도(RS)를 안 봄 — RS는 Gate1 채점 컴포넌트로만 존재하고 선정 단계엔 없어 주도주가 후보 풀에 못 들어옴. 핵심 수학 함정: RS=종목20d−시장20d에서 시장값은 스캔 내 상수라 단순 재정렬은 no-op(전 종목 동일 차감) → 필터 또는 0-floor 클램프 비선형으로만 효과. 처방(UNIVERSE_RS_GATE_ENABLED === 'true' default OFF, SSOT isUniverseRelativeStrengthGateEnabled generator 소유): A 발굴=generator MOMENTUM에 momentum20d≥시장수익률 필터(graceful fallback 생존<max(5,maxCandidates) 시 미적용·후보 0 방지)+relativeStrength20d 진단 metric, B Stage1=calcStage1Score risk-on 분기에 clamp(return20d−benchmark,0,3) 0-floor 보너스(시장 못 이김=0 대칭차감 아님→변별). 단위 정합 분리: A momentum20d 소수 vs kospi20dReturn 퍼센트→/100 정규화, B return20d·kospi20dReturn 둘 다 퍼센트→직접차감. 벤치마크=macroState.kospi20dReturn 로컬 read(신규 fetch 0), aiUniverseService.tryTier3Quant·universeScanner.stage1QuantFilter 2곳 wiring. requiredScore=70·Gate·autoTradeEngine·kisClient 0줄, flag OFF byte-equivalent. 신규 테스트 14케이스(generator RS 필터 5 + Stage1 0-floor 9), typecheck EXIT 0·신규+인접 30 PASS·complexity/responsibility/sds OK. 롤백 ENV 1줄. executionImpact: flag OFF NONE(ON 시 유니버스 입력 교정 — Gate 산식 무변경). 계보 0611/0550/0578. INDEX 0612→0613 갱신.)

(2026-06-15 기준, 마지막 발급 0611 — gate1-sector-rs-component-reactivation. **Status: Accepted (구현 — 라이브 점수 동작 변경이나 ENV flag default OFF byte-equivalent, 운영자 활성화 대기).** ADR-0611 — 운영자 진단 "Gate1 점수 천장 묶임"(거래일 avg 55·pass 12/50, forensic observedPositiveMax=55 vs configuredPositiveMax=116·scaleMismatch=true)의 근본 수리. 라이브 스코어러 활성 maxScore 합(108)과 requiredScore=70 이 calibrate 된 configuredPositiveMax(116)의 8점 차 = SECTOR_RELATIVE_STRENGTH 가 ADR-0467 에서 advisory-only(maxScore:0)로 주차된 capacity → 상위 8점 영구 봉인(ADR-0467 의도치 않은 side effect). resolveSectorRsComponentScore 신규 순수 함수: flag(GATE1_SECTOR_RS_COMPONENT_ENABLED === 'true' default OFF) OFF=byte-equivalent(maxScore0·weighted0), ON=8pt 복원·섹터상대수익(stock−sector 20d)만 소비(RS 컴포넌트 시장상대/rsRankPct 와 이중계상 회피 ADR-0469 정합)·((pct+10)/20)×100 정규화·입력 부재 graceful 0(결손≠페널티 불변식 #6). requiredScore=70 무변경(절대 보존)·신규 fetch 0(기존 trace 입력 소비). 현실 lift 점진적: 섹터상대 입력 sparse(gate2 sectorCycle 대부분 SHADOW_ONLY)라 ADR-0600/0601 hydration 성숙 따라 증가 — 천장은 열되 데이터가 채움. 활성화 기준=ADR-0471 freeze 정합 D5 표본 성숙 후 70+ 밴드 winRate 우위 확인. GHOST_SIGNAL_STRENGTH 는 입력 부재(28/28)+학습→라이브 피드백이라 별도 ADR 대상으로 제외. 신규 테스트 6케이스(flag off byte-equiv·on capacity·약/음수 비례·부재 graceful·이중계상 회피·actualScore lift), minimumSignal 회귀 61 PASS. 롤백 ENV 1줄. executionImpact: flag OFF NONE(ON 시 라이브 Gate1 점수 enrich, 의도된 효과). 계보 0467/0469/0471/0546/0578. INDEX 0611→0612 갱신.)

(2026-06-14 기준, 마지막 발급 0610 — counterfactual-outcome-board-format-extraction. **Status: Accepted (구현 동반 — byte-equivalent 분해, executionImpact=NONE).** ADR-0610 — `server/learning/counterfactualOutcomeBoard.ts` 1,499~1,500줄 ACMA 한계 고착(R1 net-0 으로 겨우 통과·이전 패치 "무접촉" 회피, 다음 변경 영구 차단) 선제 분해. build(집계)+format(표시) 혼재 SRP 위반. format(표시) 함수·표시 전용 헬퍼를 신규 `counterfactualOutcomeBoardFormat.ts`(244줄)로 **순수 이동**(함수 본문 0변경) — `formatCounterfactual*`(SafetyChecks/BoardSummary/Gate1/Gate2/Gate3/Missed/Today/Review/Debug/CommandReply 10종) + 표시 전용 5헬퍼(`pct`/`num`/`safetyLine`/`distributionLine`/`formatBandRows`, grep 전수 확인 build 미사용·format 전용) + `CounterfactualCommandMode`/`resolveCounterfactualCommandMode`. 본체(1,500→1,279줄)는 build 함수(`buildCounterfactualOutcomeBoard`/`buildBandOutcome`/`buildDebugSummary`/`rowFrom*`/`splitCounterfactualRows`/`exclusionReasonFor`/`gate1BandFor` 등)+타입 정의 잔류 + format 심볼 `export { … } from './counterfactualOutcomeBoardFormat.js'` **re-export**(+`export type CounterfactualCommandMode`) → 호출처 import 경로 무변경(byte-equivalent 핵심). 순환 회피: format.ts → board.ts 는 `import type`(ESM 컴파일 시 제거, 런타임 엣지 0), board.ts → format.ts 값 re-export 1방향 → 런타임 순환 0(typecheck clean). 공유 값 헬퍼 0(grep 확정)이라 제3 util 파일 불요. 소비처 3곳(`counterfactualOutcomeBoard.test.ts`·`counterfactualGate1BandPipeline.test.ts`·텔레그램 `counterfactual.cmd.ts`) 무수정 green. LIVE 매매 무관(learning board 표시 레이어)·9대 불변식 #1/#2 보존·R1(legacyScale70Plus/excludedByReason) 라인 byte 그대로 이동·ADR-0609 stamp 무영향. Alternatives: (a)분할 안함→다음 변경 영구 차단 기각 (b)build 추출 기각(타입 SSOT 가 build 입출력 귀속·양방향 import 유발, format 더 작고 단방향) (c)소비처 직접 import 경로 변경 기각(byte-equivalent 깨짐) (d)공유 헬퍼 제3 파일 기각(공유 0). 검증 lint(client+server tsc) EXIT=0·complexity 두 파일 ≤1500·responsibility 신규 @태그·sds swallow 증가 0·테스트 3파일 11 green. 계보 ADR-0596/0524/0521/0523/0609. INDEX 0610→0611 갱신.)

(2026-06-12 기준, 마지막 발급 0609 — gate1-eligibility-shadow-score. **Status: Accepted (구현 동반 — Phase 0 관측 전용, executionImpact=NONE, LIVE 점수 0줄).** ADR-0609 — Track B Phase 0: Gate1 상수/맥락 블록 32점(WATCHLIST_PRIORITY 8 상수·INVESTOR_FLOW 8 가용성·MARKET_REGIME 6 시장전체동일 = 순수 상수 22점, 종목 변별력 0)을 "점수"가 아닌 "적격성(eligibility) 게이트"로 재해석한 shadow 판정을 관측 전용으로 산출·기록. 절대 보존 충돌 핵심: minimumSignalScoreTrace.ts:436-493 computedScore=Σ weightedScore 가 상수 포함 + :493 passed=actualScore>=requiredScore(70) → 상수 32 제거 시 actualScore ~32 하락·70 임계로 통과 0 → requiredScore 동시 재보정 없이 분리 불가 → shadow 병렬 판정만 절대 보존(70 byte-equivalent)과 양립. 신규 순수 SSOT server/trading/signalScanner/gate1EligibilityShadowScoreAdr0609.ts(buildGate1EligibilityShadowReportAdr0609/judgeGate1EligibilityAdr0609, provider/store/now 0) — 입력 ADR-0597 산출물(marketBlockScore·totalPercentile) + 상수 컴포넌트 weightedScore, 산출 ① eligible(WATCHLIST>0 && MARKET_REGIME>=0 비emergency && INVESTOR_FLOW>=0 비bearish, **불변식 #6 결손/UNKNOWN→판정 보류=보수 통과(eligibilityDegraded 라벨), bearish 변환·탈락 절대 금지**) ② marketOnlyPassed(marketBlockScore>=42 잠정, null→null 미평가 결손≠탈락) ③ percentilePassed(totalPercentile>=70 잠정 상위~30%). 잠정 임계 GATE1_ELIGIBILITY_SHADOW_THRESHOLDS 단일 SSOT(Phase 1 dry-run 보정 대상). 배선 ADR-0597 동형(신규 ledger 0): persistScanResults ADR-0597 호출부 인근에서 0609 판정 산출→(a)ScanSummary.gate1EligibilityShadowAdr0609 additive 집계(eligibleCount/marketOnlyPassCount/percentilePassCount + executionImpact:NONE) (b)minSignalScoreBySymbol map 경유 관측 ledger 행에 eligibilityShadowEligible/MarketOnlyPassed/PercentilePassed stamp(try/catch 격리·불변식 #1). 상수 컴포넌트(ADR-0597 InputRow positiveComponents=AUDITED_POSITIVE_FEATURES 한정 미포함)는 canonical minSignal.components 에서 직접 보충(fetch 0). LIVE byte-equivalent: minimumSignalScoreTrace D1-D4(computedScore/actualScore/scoreGap/passed) 0줄·componentScorers/gateConfig(LEGACY_GATE1_REQUIRED_SCORE=70)/entryEngine/autoTradeEngine/quantFilter/perSymbolEvaluation buyList 정렬부/kisClient 0줄·0fetch. 소비처 0(Phase 0 DoD): 산출물은 ScanSummary 집계·ledger stamp·테스트 외 buyList/Gate hard-block/entry/Kelly 어디서도 미소비(3중 분리 — 파일/소비처/임계, requiredScore 70 미독). Alternatives: (a)requiredScore 동시 하향+상수 직접 제거 기각(절대 보존 위반·미검증 완화) (b)가중치 축소 기각(여전히 passed 변경) (c)shadow 병렬 채택. 신규 테스트 17케이스(적격성 진리표·결손→보류 불변식 #6·42/70 경계·null 미평가·집계 마커). 후속 Phase 1(counterfactual 대조·임계 보정)·Phase 2(별도 ADR·governance·requiredScore 재보정) 로드맵. 계보 ADR-0597/0581/0541/0476/0546. INDEX 0609→0610 갱신.)

(2026-06-12 기준, 마지막 발급 0608 — shadow-only-regime-aware-gate1-entry-threshold. **Status: Proposed (Phase 0 — 경계·타입·ADR, default OFF byte-equivalent).** ADR-0608 — SHADOW(paper) 진입 게이트에만 regime-aware Gate1 임계 적용해 [regime,legacy) 구간(R3_EARLY ×1 [4,7)/×10 [40,70)) 후보를 paper fill→익절/손절/forward 누적·검증, LIVE 진입은 legacy 임계 byte-equivalent 보존. 진입 게이트 추적 확정: 점수 임계가 매수/paper-fill 을 차단하는 단일 지점은 evaluateEntryRevalidation(entryEngine.ts:313, line370 `<minGate`)뿐이고 minGate 은 호출자 주입(`input.minGateScore ?? ENTRY_MIN_GATE_SCORE=5`) — 본체 임계 하드코딩 0 = seam. 임계 결정 site=entryRevalidationStep.ts:67(`getMinGateScore(regime)`=×1 getEffectiveGateThreshold). 체인 buyListLoop.ts:446 handleEntryRevalidationGate→entryRevalidationStep→evaluateEntryRevalidation, !proceed→'SKIP'→buyListLoop:457 continue→하류 buildBuyTrade(:716) 도달 불가 = SHADOW paper fill 도 LIVE 와 동일 임계 공유(LIVE/SHADOW 분기는 stockShadowMode=buildBuyTrade mode 뿐). gate1Passed(quantFilter.ts:788)=조건 status 집계(unavailable/degraded/thresholdNotMet=0 && fired>0)≠점수 임계, isGate1Survivor=survivor 자격일 뿐. 비진입 resolveGate1RequiredScore(×10) 3곳(gateThresholdRecommendation:65·gate1RegimeAwareWindowAdr0546·gate1ScoreAccounting) 무변경. provisionalShadowLane/counterfactual/gateEligibilitySplit=관측·ledger 전용(라이프사이클 아님, line400/429/435<446 이미 minGate 무관)·preBreakoutEntry buildBuyTrade(shadowMode:true,:205/419)=entryPrice 근접 트리거라 임계 직교 — 전부 무접촉. 처방: 신규 순수 SSOT gate1ShadowEntryThreshold.ts(resolveEntryMinGateScore({regime,isShadow})=ENV ON&&isShadow→getEffectiveGateThreshold(regime), 그 외→getMinGateScore byte-equivalent + isGate1RegimeAwareShadowEntryEnabled 정확비교), ENV GATE1_REGIME_AWARE_SHADOW_ENTRY_ENABLED default OFF. 타입 ServerShadowTrade(server)+클라 미러 additive optional entryThresholdMode?:'LEGACY'|'REGIME_AWARE_SHADOW'(buildBuyTrade 스탬프, 학습 표본 격리 — 청산/사이징/LIVE 판정 영향 0). LIVE byte-equivalent: evaluateEntryRevalidation/getMinGateScore/getEffectiveGateThreshold 본체 0줄·LIVE 분기=getMinGateScore 1:1·dryRunScanner:269 무변경·autoTradeEngine/KIS 무접촉. 9대 불변식 #1(SHADOW 확대만·순수함수 실패시 LIVE fallback)·#7(KIS L1 점수·L4 0)·#8(LIVE legacy vs SHADOW regime-aware 명시 분리·표본 라벨 격리)·#6 보존. executionImpact OFF=NONE/ON=execution-adjacent(SHADOW only·LIVE 본체 0줄·KIS quota 0). engine-dev Phase 1 인계(헬퍼→entryRevalidationStep isShadow 배선→라벨 스탬프→회귀 OFF byte-equivalent/ON shadow 확대/LIVE 무변경/표본 라벨). 계보 ADR-0546/0436/0426/0427/0393/0146/0607. INDEX 0608→0609 갱신.)

(2026-06-12 기준, 마지막 발급 0607 — live-shadow-channel-quiet-isolation. **Status: Accepted (구현 동반, default OFF byte-equivalent).** ADR-0607 — LIVE 전환 사전작업 A1: LIVE 모드(`getExecutionMode()==='LIVE'`)일 때 SHADOW 신호/메시지를 CH4(SYSTEM/JOURNAL)로 격리해 CH1(EXECUTION)/CH2(SIGNAL)을 LIVE 체결·신호 전용으로 비운다. 신규 순수 SSOT `server/alerts/shadowQuietRouting.ts`(provider/store 0): `resolveShadowChannelRoute(intendedRoute,isShadow)` = 3중 AND(`isShadow && LIVE_SHADOW_QUIET_ENABLED==='true' && getExecutionMode()==='LIVE'`)→JOURNAL, 그 외 intendedRoute 그대로(byte-equivalent, OFF 시 getExecutionMode 단락 미호출) + `isLiveShadowQuietEnabled()`(정확 비교). 적용 2곳 — `routeTelegramEvent` SIGNAL 분기(`resolveShadowChannelRoute(SIGNAL, SHADOW_SIGNAL_EVENTS.has(type))`, taxonomy SSOT·방어적, emit 프로덕션 호출자 0건) + `channelBuySignalEmitted`(mode-aware 실효 CH2 chokepoint, `p.mode==='SHADOW'`). scope 제외(follow-up): `channelSellSignal`(mode 파라미터 부재·CH1 EXECUTION·호출자 9개 exit-engine 도메인 mode-agnostic shadow ledger → Telegram 단일 도메인+최소 골격 위반) + 개인 DM SHADOW 격리. 순환 의존 없음(alerts→state→persistence 단방향·channelStatsRepo 는 alertCategories leaf 만 → 정적 import 채택). ENV `LIVE_SHADOW_QUIET_ENABLED` default OFF=현재 SHADOW(OFF/PAPER) 운영 영향 0·1줄 롤백. 채널 경계(ADR-0032 §3) 정합 — route(채널 의미)만 변경·channel ID 무접촉(validate:channelBoundary 통과). 9대 불변식 #6(SHADOW→CH4 는 정책 상태이지 장애 아님·격리 경로 "장애" 표현 0)·#8(Shadow 학습 무영향·발송 채널만) 보존. executionImpact: OFF=NONE / ON+LIVE=채널 의미 조정(LIVE 매매 본체 0줄·KIS 0). 신규 테스트 18케이스(헬퍼 진리표 7 + routeTelegramEvent 통합 7 + channelBuySignalEmitted dispatch 4). 계보 ADR-0393/0395/0032/0466/0146. INDEX 0607→0608 갱신.)

(2026-06-12 기준, 마지막 발급 0606 — runner-mode-trend-following-exit-and-slot-exemption. **Status: Proposed (Phase 0 — 경계·타입·ADR, default OFF byte-equivalent).** ADR-0606 — 익절 엔진 "러너(Runner) 모드" 도입. 단일 개념 "러너 = 트레일링 활성 종목 = 슬롯 카운트 제외"(사용자 확정 2결정 통합). 문제: 트레일링이 모든 LIMIT 트랜치 소화 후에만 활성(trancheTakeProfitLimit.ts:82) + R4_NEUTRAL/R5_CAUTION 트레일링 트랜치 부재(전량 LIMIT) → 큰 러너 구조적 조기 청산. 정책 ① 러너 승격 = 마지막 LIMIT 트랜치 도달 OR returnPct ≥ RUNNER_PROMOTE_RETURN_PCT(가드 [10,40] 기본 18) ② 승격 후 잔량 전량(ratio≥0.999) 트랜치만 스킵(초기 소량 LIMIT 유지) + 넓은 트레일링 resolveRunnerTrailPct=clamp(max(진입trailPct+bonus,floor),진입,0.30)(bonus 0.05/floor 0.15 기본, 항상 진입 이상·타이트화 금지) ③ 슬롯 제외 = slotAccounting.computeSlotConsumption eligible 필터에서 isRunner 제외(단일 SSOT → loopInitializer/preflight/slotSizing 3소비처 자연 전파, 배선 0) + runnerCount additive 진단 필드, regime maxPositions 분모 무변경 ④ ENV 2게이트 독립(RUNNER_MODE_ENABLED·RUNNER_SLOT_EXEMPTION_ENABLED, 각 1줄 롤백) ⑤ SHADOW 전용·LIVE 본체 0줄·byte-equivalent(OFF). 신규 순수 SSOT server/trading/exit/policies/runnerPolicy.ts(evaluateRunnerPromotion/resolveRunnerTrailPct/shouldSkipTrancheForRunner + ENV 헬퍼, provider/store/now 0 — riskOnFastUpgrade.ts 분리 선례). 타입 ServerShadowTrade(server) + 클라이언트 미러(autoTradeClient.ts) 옵셔널 additive 5필드(isRunner/runnerActivatedAt/runnerTrailPct/runnerPromotedReturnPct/runnerPromotionReason). 9대 불변식 #1 liveness(SHADOW 평가 try/catch 격리)·#6(결손→미승격 보수)·#7(KIS L1 returnPct, L4 0)·#8(SHADOW 장부 한정, LIVE 무접촉) 보존. executionImpact: OFF=NONE / 게이트 ON=execution-adjacent(SHADOW only). engine-dev 인계(Phase 1 승격·트랜치 스킵 wiring → Phase 2 슬롯 제외 → Phase 4 운영자 검증). 계보 ADR-0028/0080/0085/0593/0561/0146. INDEX 0606→0607 갱신.)

(2026-06-11 기준, 마지막 발급 0605 — us-leading-index-residual-wiring. **Status: Accepted (구현 — 행동 변경 경로 전부 flag default OFF, 수집·표시 즉시 가동).** ADR-0605 — ADR-0604 "미구현 (후속 ADR 후보)" 3건 일괄 이행. D1 fast-upgrade 보조 AND: ADR-0593 3중 AND 에 ④ SPX 야간 비급락 확인(REGIME_RISK_ON_FAST_UPGRADE_US_OVERNIGHT_AND_ENABLED === 'true' default OFF, MIN_PCT 가드 [-5,5] default -0.5) — 보조 ON+SPX 부재 시 보수 미발동(예외 가속 경로 한정, 결손→bearish 변환 아님), resolveRiskOnFastUpgradeInputs 가 macroState.spxDayReturn 주입+로그 표기. D2 SOX→Gate2 반도체 proxy 축: 수집(즉시) resolveKisOverseasSoxIscd(ENV 정정 가능 default 'SOX')→refreshSpxSection 에서 macroState.soxDayReturn/sox20dReturn(~1콜/일) + proxy(GATE2_SOX_SECTOR_AXIS_ENABLED === 'true' default OFF) 신규 gate2SoxSemiAxisAdr0605.ts — ADR-0601 hydration 이후 잔존 결손 반도체 후보만 stockVsSectorReturn20d 단독 주입으로 buildSectorAxis 최대 62 자연 캡(currentLeader 민팅 구조적 불가, ADR-0600 동일 보수), 본 모듈 KIS fetch 0. D3 NDX 밴드 분리(표시 전용 즉시): /us_overnight 가 SPX+NDX 5밴드 분리 출력+pairedRows spx/ndx. 신규 테스트 12케이스(보조 AND 진리표·SOX flag/결손/캡·NDX 밴드). 같은 PR 에서 ADR-0602 Phase 1 구현(shadow 교체 집행 TRADE_REPLACEMENT_SHADOW_EXECUTE_ENABLED default OFF·일일 상한 2·LIVE 무접촉·SECTOR_REPLACEMENT_EXIT 태그 — ADR-0602 Status 갱신, 신규 ADR 미발급)과 /us_overnight 자동완성 등재, 선존 flake 3건(walkForward 2·FssVars 1 — 최초 dynamic import transform>5s timeout) warm-up 수리 동반. 계보 0603/0604/0593/0600/0601. INDEX 0605→0606 갱신.)

(2026-06-11 기준, 마지막 발급 0604 — us-overnight-stratify-and-defense. **Status: Accepted (2단계 구현·가동 — 3단계 구현·default OFF).** ADR-0604 — ADR-0603 후속 2·3단계. 2단계(즉시 가동): usOvernightStratifyLedger — KST 일자당 1행 {spx/ndx 야간, kospiDayReturn} (캡 400, 야간=first-write-wins 로 미국 장중 오염 방지·kospi=last-write-wins 종가 수렴), refreshSpxSection 말미 기록(실패 격리), 신규 /us_overnight 명령 — SPX 야간 5밴드별 KOSPI 평균·승률 실측(게이트 미소비 — 활성화 판단 유일 근거, 상관 가정 금지). 3단계(default OFF, US_OVERNIGHT_DEFENSE_ENABLED === 'true'): SPX 야간 < 임계(-2.5 기본, -10~-0.5 가드) 시 ①resolveRecoveryBiasScore derived -12(usOvernightBoost +10 하방 대칭) ②resolveR6RecoveryDecayFloor 0(급락 밤 회복 latch 가속 차단) — flag OFF 양 경로 byte-equivalent(R6 recovery 회귀 green). 활성화 기준 명문화: <-2% 밴드 n>=10+KOSPI 평균 음수 실측. 미구현 후속: fast-upgrade 보조 AND·SOXX 섹터축·NDX 밴드 분리. FOMC/VIX 중복 계상 0(게이트형)·lookahead 0·결손 미발동(불변식 #6). 신규 테스트 4케이스(upsert 의미론·밴드 집계·빈 표본·ENV 가드). 계보 0603/0592/0593. INDEX 0604→0605 갱신.)

(2026-06-11 기준, 마지막 발급 0603 — kis-overseas-index-promotion. **Status: Accepted (1단계 구현, default ON `!== 'false'`).** ADR-0603 — 가능성 검토(미국 선행지수↔한국 연결)의 1단계: 레짐이 이미 소비 중인 SPX(usOvernightBoost MHS+10·합성식 ×2·분류 조건 3곳)의 소스를 Yahoo(L3)→KIS 공식 해외지수 일봉(FHKST03030100, N: 해외지수 — 다우30/나스닥100/S&P500 공식 지원 확인)으로 승격. 신규 kisClient/query/overseasIndex.ts(단일통로 realDataKisGet·지수×KST일자 메모 캐시 ~1콜/일·실패 당일 억제·null→호출측 Yahoo fallback 보존, ISCD ENV 정정 가능) + refreshSpxSection KIS-first(소비식 0줄 변경·source 로그 표기) + NDX 관측 수집(macroState.ndxDayReturn/ndx20dReturn additive — 레짐 미소비, 한국 성장주 상관 실증용). 2/3단계(usOvernight stratify→야간 급락 개장 전 보수 강등·fast-upgrade 보조 AND·SOXX 섹터축)는 로드맵 명시(FOMC/VIX 중복 계상 주의·lookahead 금지). 신규 테스트 3케이스(수익률 산식·결손 제외·ENV 가드). 계보 검토 20260611/ADR-0561/0592/0593. INDEX 0603→0604 갱신.)

(2026-06-11 기준, 마지막 발급 0602 — same-sector-replacement-observation. **Status: Accepted (Phase 0 구현 — 관측·표시 전용, default ON `!== 'false'`).** ADR-0602 — 운영자 토론(섹터 리미트 해제?)의 코드 추적에서 이중 갭 확정: tradeReplacement(Phase 4-⑦ 교체 엔진)는 운영 호출 0건 미배선 dead 모듈 + proposeReplacement 조건(iii) heldSector!==candSector 로 동일 섹터 내 강자 교체 설계상 불가. 처방=한도 해제 대신 '같은 슬롯 내 약자→강자 교체' 경로의 phased 도입 — Phase 0(구현): 신규 순수 함수 proposeSameSectorReplacement(gate 우위>=1.5 필수, 정체보유 momentumSlowing/수익률<=0 은 score 가중 +5 — 발동 빈도 관측 목적), sectorConcentrationGate 차단 분기에서 평가 후 로그/텔레그램에 '교체 관측(Phase0·실집행 없음)' 1줄 표기(평가 실패 격리·결손 필드 보수 기본값 불변식 #6), flag TRADE_REPLACEMENT_OBSERVE_ENABLED default ON(표시 전용·1줄 롤백). Phase 1(설계만): shadow 교체 집행 flag OFF + 쿨다운·일일 상한, 직전 patch 의 SECTOR_CONCENTRATION_LIMIT counterfactual 라벨 분포로 발동 조건 확정. Phase 2(설계만): live 교체 — shadow 표본 N>=30+교체 우위 통계+운영자 승인+autoTradeEngine 단일통로 2주문 설계 선행(후속 ADR). MAX_SECTOR_CONCENTRATION 무변경·기존 proposeReplacement 의미 무변경. 신규 테스트 4케이스(약자 선택·우위 부족·힌트 표기·flag off byte-equivalent). 계보 토론 20260611/Patch-SectorLimit-Blocked-Counterfactual/ADR-0030/0594/0598. INDEX 0602→0603 갱신.)

(2026-06-11 기준, 마지막 발급 0601 — gate2-kis-native-investor-flow-hydration. **Status: Accepted (D3 구현, default ON `!== 'false'`).** ADR-0601 — ADR-0600 D3 실행: 공식 API 인벤토리로 신규 TR 불필요 확정(kisClient.fetchKisInvestorTradeByStockDaily+kisInvestorFlowEvidence 기존 단일통로) — 갭은 호출 범위(ADR-0477 라우터가 스캔당 대표 1종목만). 신규 scanDiagnostics/gate2InvestorFlowHydrationAdr0601.ts 가 persistScanResults 의 decomposition 직전에 kisInvestorFlow 결손 후보(Gate1 통과 우선)를 보충: quota 3중 관리(일중 캐시 symbol×KST일자 재스캔 fetch 0 + 스캔당 신규 조회 상한 GATE2_INVESTOR_FLOW_HYDRATION_MAX 0~50 default 16 + 당일 실패 재시도 억제), snapshot.gate2ExternalDataCoverage.kisInvestorFlow 주입(hydratedBy 표기)→buildSupplyAxis 기존 경로 소비(gate2ConfluenceScore 0줄), ScanSummary 집계+1줄 로그, 실패 전면 격리(불변식 #1/#2). 우선순위 L1 네이티브>ADR-0600 시맨틱 fallback>missing (ADR-0561 KIS Primary). D4(KOSDAQ 마스터 업종코드→Sector 네이티브)는 Phase 2 — 마스터 고정폭 파서·일캐시·sectorEnergy 통합 별도 단위, 그때까지 동종군 fallback 유지. 신규 테스트 6케이스(ENV 가드·결손만 주입·일중 캐시·상한+gate1 우선·실패 격리·flag off). 계보 0600/0561/0477. INDEX 0601→0602 갱신.)

(2026-06-11 기준, 마지막 발급 0600 — gate2-supply-sector-axis-coverage-fallback. **Status: Accepted (D1/D2 구현, default ON `!== 'false'`).** ADR-0600 — Gate2 진단 차선 Supply 28/43·Sector 21/43 결손 축 보전. D1 Supply: KIS 투자자행 결손 시 Gate1 시맨틱 수급(supplyConfluenceState, ADR-0477~0488 라우터 산출 — Gate2 미소비 배선 갭)을 BULLISH 78(ACCUMULATING 캡·BULLISH 민팅 금지)/NEUTRAL 50/BEARISH 30 DEGRADED·ADVISORY 로 소비, UNKNOWN/부재→missing 유지(불변식 #6). D2 Sector: 업종지수 결손(코스닥 매핑 부재) 시 summary 1회 산출 동종군 컨텍스트(섹터 n>=3·return20d 중앙값)로 stockVsPeer20d ≥+3→62(상한)/≤−5→35/그외 50, n<3→missing, fetch 0. default ON 근거: 진단/View 차선 한정(소비처 전수 scanDiagnostics 확인·live 0줄)+보수 캡(STRONG BULLISH 요건 기여 불가)+ADR-0578 선례+1줄 롤백. D3/D4 후속(ADR-0601 예정, ADR-0561 KIS Primary): 공식 API 인벤토리 확정 — inquire-investor(국내주식-012, frgn/orgn_ntby_qty) kisClient 단일통로+Gate1 생존자 한정 hydration+일캐시, KOSDAQ 마스터(kis_kosdaq_code_mst) 지수업종 대/중/소분류 코드로 코스닥 업종 매핑 해소 — 도입 시 fallback 은 최후 보조 강등. 신규 테스트 3케이스(시맨틱 78/UNKNOWN·flag off/동종군 62·35·n<3·BULLISH 불가). 계보 0599/0477~0488/0578/0561/0416. INDEX 0600→0601 갱신.)

(2026-06-11 기준, 마지막 발급 0599 — gate2-coverage-proportional-confluence-requirement. **Status: Accepted (구현 동반, default OFF byte-equivalent).** ADR-0599 — Gate2 STRONG 사상 0건의 산술 원인 봉인: 결손 축이 점수에선 분모 제외(ADR-0416 준수)되나 STRONG/WEAK 의 bullish>=3 이 절대 개수라 가용 5축=3/5(60%) vs 가용 3축=3/3(100%)으로 결손이 요구를 강화(커버리지 Supply 27/43·Sector 21/43·Fund 27/43 + BULLISH 컷 85 결합 시 도달 불가). 처방=proportionalRequiredAxisCount(usable)=clamp(ceil(usable×0.6),1,3) — flag GATE2_PROPORTIONAL_BULLISH_ENABLED ON 시 요구 개수만 교체(점수 임계 80/65/50·BULLISH 85·<3축 DATA_INCOMPLETE·AI_ESTIMATED 분모 제외 불변식 #7 전부 불변), OFF(default) 시 기존 3 고정 + dry-run(wouldPassStrong/WeakProportional·requiredConfluenceAxisCount·summary would*Proportional·compact proportionalDryRun 1줄) 상시 관측. phased Phase0 관측→Phase1 counterfactual 대조→Phase2 ENV ON. 커버리지 결손 1차 원인(KIS 투자자행 NO_ROW_FOUND·코스닥 업종지수 매핑 부재·DART 미수신)은 provider 개선 별도 과제 명시. 계보 Gate2 추적 20260611/ADR-0416/0519/0598. INDEX 0599→0600 갱신.)

(2026-06-11 기준, 마지막 발급 0598 — entry-chasing-guard-gap-hardening. **Status: Accepted (구현 동반, default OFF byte-equivalent).** ADR-0598 — 유진테크(084370) shadow 매수 추적에서 확정된 과열 추격 가드 갭 2건 봉인. G1: FOMO 쿨다운(5d+15%→48h)이 워치리스트 등록 시점 1회만 평가(universeScanner.ts:780)·진입 시점 cooldownGate 는 기존 cooldownUntil 필드만 확인 → 급등 전 등록 종목은 진입 순간 +20%여도 미발동. 처방=cooldownGate 에 진입 시점 symbolFeatures.return5d(ADR-0578, %단위) 재평가 — flag REGRET_ENTRY_REEVALUATION_ENABLED ON 시 쿨다운 신규 stamp+차단(기존 의미론), OFF(default) 시 [REGRET_ENTRY_REEVAL_OBSERVE] would-cooldown 로그만, 결손→skip(불변식 #6). G2: gate3PriceConfirmation 이 ma20 결손 시 이격(≥12%)·3ATR 체크 단락 + high20d 만으로 BREAKOUT_CONFIRMED→priceReady→live 허용(결손=통과 역설). 처방=flag GATE3_EXTENSION_GUARD_STRICT_ENABLED ON 시 NOT_CONFIRMED(WAIT, status union 신규값 0) 확정 보류, OFF(default) 시 EXTENSION_UNVERIFIABLE_MA20_MISSING_OBSERVE note+missingFields 진단만. shadow 무영향(불변식 #8 — gate3LastTrigger 전 분기 shadowObservableAllowed=true 보존). phased(ADR-0592/0593/0594 선례)·ENV 각 1줄 독립 롤백·fetch 0. 신규 테스트 cooldownGate G1 4케이스+gate3PriceConfirmation G2 4케이스. 계보 유진테크 추적 20260611/ADR-0030/0578/0146. INDEX 0598→0599 갱신.)

(2026-06-11 기준, 마지막 발급 0597 — gate1-cross-sectional-percentile-shadow-score. **Status: Accepted (구현 동반, 관측 전용).** ADR-0597 — Gate1 절대점수의 레짐 의존(약세장 전 종목 동조 하락, 횡단면 요소 RS 10/108 뿐 — 리뷰 2026-06-11 §1.3) 검증용 횡단면 percentile shadow 보조점수. 신규 순수 SSOT server/trading/signalScanner/gate1CrossSectionalShadowScoreAdr0597.ts — canonical 점수(ADR-0541 starvation traces, fetch 0·quota 0)에서 스캔 내 midrank 백분위(동률 0.5 가중·n=1→50)와 시장신호 블록 합(PM/TECH/VOL/RS/WUS/BREAKOUT 6종, 상수/맥락 블록 제외, 컴포넌트 부재 시 null=결손≠0 불변식 #6) 산출. 기록 2경로 — ScanSummary.gate1CrossSectionalShadowAdr0597(additive) + ADR-0476 관측 ledger 행 crossSectionalPercentile/marketBlockScore/marketBlockPercentile stamp(점수 스케일 정합 patch 의 minSignalScoreBySymbol map 경유, 신규 배선 0) → "절대 점수 밴드 × percentile 밴드" 교차 forward 분석 가능. Gate 판정·requiredScore·가중치·정렬 미소비(관측·기록만), ENV 불요 항상-on 진단, 실패 try/catch 격리+warn. 후속(별도 ADR): percentile 보조 임계 live 반영·상수블록 32점 적격성 게이트 분리. 계보 리뷰 20260611/ADR-0541/0546/0476/0467. INDEX 0597→0598 갱신.)

(2026-06-10 기준, 마지막 발급 0596 — minimumsignalscoretrace-component-decomposition. ADR-0596 — `signalScanner/minimumSignalScoreTrace.ts` 1,499~1,500줄 ACMA 한계(1,500) 1줄 차 재도달(ADR-0524 분해 후 ADR-0467/0505/0594 누적 재상승) 선제 분해. 점수 컴포넌트 계산층(`priceMomentumScore`/`breakoutScore`/`volumeLiquidityScore`/`technicalTrendScore`/`normalizedRelativeStrength` ~415줄)·trace 경로 해석 유틸(`resolveNumericTracePath`/`breakoutSignalState` 등 ~178줄)·집계 리포트(`buildMinSignalScoreDecompositionReport`/`buildSignalScoreCalibrationResults` ~233줄)를 `minimumSignalScoreTrace/` 하위 신규 3모듈(traceFieldResolver ~205 / componentScorers ~455 / decompositionReport ~265)로 **순수 이동** — 본체는 trace/audit/softFail/riskPenalty 조립 4종만 잔류 ~690줄. public API re-export byte-equivalent(외부 importer 10+ 무수정), 가중치·페널티·confidence 정책 0변경, advisory-only executionImpact=NONE. 정적 가드(operatorActionRouterAdr0480 L180 `requiredScore` 단언)는 본체 잔류 설계로 갱신 0건 — green 재확인 의무. 9대 불변식 #2/#6/#7/#9 보존(scorer 는 trace 입력만, provider 직접 조회 0). 계보 ADR-0524/0466/0467/0594/0444. INDEX 0596→0597 갱신.)

(2026-06-10 기준, 발급 0595 — marketdatarefresh-section-module-decomposition. ADR-0595 — `trading/marketDataRefresh.ts` 1,496~1,497줄 ACMA 한계 4줄 차 재도달(ADR-0580/0589/0592 3차 분해 후 30일 9회 수정 최다 핫스팟) 선제 분해. refresh*Section 도메인 그룹(지수거시 KOSPI/VKOSPI/USD-KRW/SPX/DXY/FRED/MHS · 수급신용 FSS/공매도/신용잔고/FSS 11분류 · 프로그램매매 · 섹터에너지)과 공유 observability(emitMarketDataProviderWarn 등)를 `marketDataRefresh/` 하위 신규 5모듈(refreshObservability ~165 / indexMacroSections ~370 / supplyCreditSections ~380 / programMarketSection ~200 / sectorEnergySection ~170)로 **순수 이동** — 본체는 `refreshMarketRegimeVars`(섹션 호출 순서)+`buildUpdatedMacroState`(MERGE 영속)+re-export 오케스트레이터 ~375줄 잔류. 호출 그래프·순서·merge 불변, public API re-export byte-equivalent(외부 importer 14 무수정), executionImpact=NONE. 정적 grep 가드 9파일(marketDataRefresh*.test 6 + sectorEnergy Adr0399/0423/0454) ADR-0444 concat 패턴(기존 HELPERS_PATH 선례)으로 갱신 — 단언 정규식 무수정, read 경로만 확장. 9대 불변식 #3/#6/#7/#9 보존(providerIssue≠marketSignal 문구 byte 보존·L4 캡 텍스트 그대로·Gate 노출 0). 계보 ADR-0580/0589/0592/0444/0561. INDEX 0595→0596 갱신.)

(2026-06-09 기준, 마지막 발급 0594 — gate1-regime-aware-reversal-momentum-credit. **Status: Implemented (PR #1380, default OFF — shadow 활성화는 운영자 결정, docs/ops/reversal-recognition-activation-playbook.md).** ADR-0594 — ADR-0593(레짐 risk-on)의 Gate1 종목-점수판. priceMomentumScore(minimumSignalScoreTrace.ts:477)가 return5d/return20d 다일집계 전용 → 폭락이 5일창 오염 → KOSPI +7.45% 강반등 당일에도 PRICE_MOMENTUM(maxScore 20) 점수 바닥(평균 기여 +8.2), changePercent/prdy_ctrt/intraday grep 0회=오늘 단일일 강세 입력 부재 확정. BREAKOUT_STRUCTURE 는 turtle-high 요구로 폭락저점 반등에 0(46/49 TURTLE_HIGH_NOT_MET). 결과 configuredPositiveMax 116 vs observed 50.6, 반등 리더 미차별화 → ADR-0593 이 레짐 올려도 종목점수 단절. **처방=regime-aware reversal momentum credit** — 신규 flag GATE1_REVERSAL_MOMENTUM_ENABLED(default OFF) ON + risk-on 국면(ADR-0593 RISK_ON_REGIMES 게이트 재사용 후보)에서만 quote.changePercent(기존 trace 필드 types.ts:409, quota 0)를 PRICE_MOMENTUM 에 bounded 양수 보너스로 가산(T_min~T_cap 선형·MAX_BONUS 상한·maxScore 20 천장 불변·hard-block/감점 무변경). 난제=buy-the-top 경계→3중 방어(regime-gate 평시 0·bounded T_cap 과급등 cap/감쇠·ADR-0593 breadth/VKOSPI 게이트 정합). 신규 순수 SSOT server/trading/signalScanner/reversalMomentumCredit.ts(evaluateReversalMomentumCredit/isGate1ReversalMomentumEnabled, ADR-0593 riskOnFastUpgrade.ts 분리 선례). regime 이미 buildMinimumSignalScoreTrace input(line 655) threaded → 배선 0. 단계적(Phase 0 데이터→1 shadow counterfactual N영업일→2 검증→3 live, ADR-0592/0593/0581 phased). ADR-0546(임계 완화)와 상보·독립(0594=점수 올림·0546=바 낮춤, 0594 더 근본=반전 리더만 차별 보상). ADR-0467 양수 컴포넌트 회계 정합. 9대 불변식 #6(changePercent 부재→가산 0 보수)·#7(KIS L1 파생) 보존. ADR-0146 byte-equivalent(flag OFF 점수 100% 보존·ENV 1줄 롤백·executionImpact NONE·entryEngine/autoTradeEngine/Gate hard-block/Kelly 0줄). 미해결(운영자/engine-dev): 가산 크기(MAX_BONUS 6~8·T_min +3%)·과급등 캡 T_cap(+12~15% cap vs 감쇠)·ADR-0593 게이트 재사용 여부(canonical RISK_ON_REGIMES vs shouldFastUpgradeToR3Early)·shadow N영업일. 계보 ADR-0593/0550/0592/0546/0467/0146/0581/0572. INDEX 0594→0595 갱신.)

(2026-06-09 기준, 마지막 발급 0593 — regime-risk-on-fast-upgrade-symmetric-r6-rebound. **Status: Implemented (PR #1378, default OFF — shadow 활성화는 운영자 결정, docs/ops/reversal-recognition-activation-playbook.md).** ADR-0593 — 폭락 다음날 강반등(KOSPI +7.45%)에도 R4_NEUTRAL 고착으로 ADR-0550 risk-on 주도주 포착이 잠드는 상승/하락 비대칭 + 후행 집계 결함 수리. classifyRegime(regimeEngine.ts:186)의 kospiDayReturn 이 R6 하락(<-5)에만 소비되고 상방 미사용 + R3 정상경로(5/20일 후행)·R3 강제승급(MA20·외국인지속)이 폭락 다음날 둘 다 실패 → fall-through R4. RISK_ON_REGIMES(pipelineHelpers.ts:643 R1/R2/R3_EARLY)에 R4 미포함이 병목. **처방=R6 -5% 블랙스완 하락 트리거의 상방 대칭** — 신규 flag REGIME_RISK_ON_FAST_UPGRADE_ENABLED(default OFF) ON + 3중 AND(오늘 kospiDayReturn≥T(+3~4%, 운영자확정)·VKOSPI 진정·시장 breadth 우위) 동시 충족 시 classifyRegime fall-through 직전 분기에서 **provisional R3_EARLY 승급(캡 — R2/R1 직행 금지)**. 신규 순수 SSOT server/trading/regime/riskOnFastUpgrade.ts(evaluateRiskOnFastUpgrade/isRiskOnFastUpgradeEnabled, ADR-0592 kospiTriggerFreshness.ts 분리 선례) + ADR-0592 resolveRecoveryKospiDayReturn freshness/TTL SSOT 재사용(falling-knife 방지) + kospiIntradayReturn MacroState 필드(ADR-0592 기존) 재사용. RegimeVariables +2 optional(kospiDayReturnToday/marketBreadthAdvanceRatio, additive 후방호환). 단계적 활성화(Phase 0 데이터→1 shadow 관측 N영업일→2 counterfactual 검증→3 live, ADR-0581/0592 phased). whipsaw 가드=breadth+VKOSPI 동시 AND + default OFF + R3 캡. 가드레일 ADR-0550 상속(universe 후보 유입만 확장·No Gate/Kelly/STRONG_BUY/Shadow/order change). 9대 불변식 #6(breadth 결손→보수적 미승급, bullish 변환 0)·#7(breadth/intraday KIS L1, Yahoo 금지 ADR-0561) 보존. ADR-0146 byte-equivalent(flag OFF baseline 100% 보존·ENV 1줄 롤백·executionImpact NONE·KIS/KRX quota 0). 미해결(운영자/engine-dev): 임계 T 확정·breadth 소스(현 코드 시장 breadth 부재 recommendationTypes.ts:18)·VKOSPI 가드 형태·shadow N영업일. 계보 ADR-0550/0592/0146/0581/0561/0531/0136. INDEX 0593→0594 갱신.)

(2026-06-09 기준, 마지막 발급 0592 — r6-trigger-freshness-tradedate-intraday-rebound. ADR-0592 — R6_DEFENSE trigger freshness 를 봉 거래일(KRX) 기준으로 평가(결함 A: 어제 폭락 봉이 오늘 'FRESH intraday trigger' 로 오인되는 false-latch 차단) + 오늘 intraday KOSPI 수익률(KIS 종합지수 0001 L1) 을 recovery 평가에 공급(결함 B: confirmations 정상 누적). 신규 SSOT kospiTriggerFreshness.ts(순수 함수·resolveKospiTriggerFreshness/isTradeDateFreshnessEnabled, isKrxTradingDay/toKstDateKey ADR-0559 재사용) + marketDataRefresh/kospiIntradayRefresh.ts(거래일 영속 + KIS intraday quote fetch + carry-forward 경고). MacroState +4 옵셔널 필드(kospiTriggerSourceTradeDate/kospiIntradayReturn/kospiIntradaySourceTradeDate/kospiIntradayFetchedAt) · R6TriggerBreakdown +3(kospiTriggerSourceTradeDate/tradeDateIsToday/intradayReturnUsed). flag 3종 전부 default OFF=baseline byte-equivalent(R6_TRIGGER_TRADEDATE_FRESHNESS_ENABLED·R6_KOSPI_INTRADAY_QUOTE_ENABLED·R6_INTRADAY_REBOUND_RELEASE_ENABLED). 임계값(-5/+30/±3/+3%) 무변경. 9대 불변식 #1/#2/#6 보존·KIS 단일 통로(raw KIS REST 0)·Yahoo 금지(ADR-0561 KIS Primary). 롤백=각 flag 1줄 OFF. 계보 ADR-0559/0561/0563/0567/0584. INDEX 0592→0593 갱신.)

(2026-06-09 기준, 마지막 발급 0591 — market-truth-layer-trading-date-ssot. ADR-0591 — market truth layer trading date ssot. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-09 기준, 마지막 발급 0590 — routebuilder-investor-flow-router-cyclomatic-complexity-reduction. ADR-0590 — routeBuilder investor flow router cyclomatic complexity reduction. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-09 기준, 마지막 발급 0589 — summarybuilder-and-marketdatarefresh-cyclomatic-complexity-reduction. ADR-0589 — summaryBuilder and marketDataRefresh cyclomatic complexity reduction. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-09 기준, 마지막 발급 0588 — persistscanresults-mid-scan-diagnostic-blocks-extraction. ADR-0588 — persistScanResults mid-scan diagnostic blocks extraction. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-09 기준, 마지막 발급 0587 — kissectorenergyprovider-type-extraction-decomposition. ADR-0587 — kisSectorEnergyProvider type-extraction decomposition. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-07 기준, 마지막 발급 0586 — vkospi-flatduringmove-heuristic-reverted-real-crash-value. ADR-0586 — VKOSPI flat-during-move 휴리스틱 전면 revert(ADR-0584 Phase B 가드 + ADR-0585 교정 + /regime ⚠️STALE 마커 + flag 2종 + vkospiFreshness.ts + freshness 테스트 2건 제거). `/vkospi_index_dump`(KRX 306행 덤프) 결과 73.4 가 **진짜 KRX-공식 VKOSPI(코스피200 변동성지수)**임이 드러남: 이름매칭 정확·~16 대체행 없음·같은 덤프에 실제 대폭락 확인(KRX300 레버리지 −11.88%·SK하이닉스 선물레버 −26.70%) → 폭락장 VKOSPI 73 현실적(COVID ~69)·VIX 16(미 평온)은 한국발 국지폭락 디커플링 정상. flat-during-move 는 진짜 spike 는 안 막지만 **진짜 plateau(높고 변화작음)는 오격리** = ADR-0530 이 G2 로 피하려던 폭락 마스킹 재도입. **revert**. **유지**: ADR-0584 Phase A 사실 가시화(vkospiBaseDate/vkospiFetchedAt 영속·VKOSPI_ROW_AMBIGUOUS/VKOSPI_CARRY_FORWARD 경고·/regime baseDate 표시)·/vkospi_index_dump·ADR-0530 classic 가드 원복. **부수 정밀화**: bare '변동성지수' 필터가 '최소변동성지수'(주식지수 14694) 동음이의 매칭 fragility → '코스피 200 변동성지수' 한정(현 선택 byte-equivalent·순서변동 robustness). prod 효과=/regime 진짜 VKOSPI 에 잘못된 ⚠️STALE 미표기(가드/교정 flag 는 기본 OFF라 매매 0). 교훈=단일 지표 implausibility 판정 전 시장맥락(동반 카니지) 우선·더 나은 stale 판별=baseDate 신선도(후속). 미해결: chg%=0.00 원인·73.44 외부 교차확인·baseDate 캘린더 stale-gate. lint0·ADR-0530 테스트·validate:all EXIT=0. 계보 ADR-0530/0584/0585. INDEX 0586→0587 갱신.)
(2026-06-07 기준, 마지막 발급 0585 — vkospi-fetch-correction-frozen-reject-yahoo-fallback. ADR-0585 — VKOSPI fetch *교정*(ADR-0584 후속 #1 안전 구현, flag `VKOSPI_FETCH_CORRECTION_ENABLED` default OFF byte-identical, 격리 flag와 독립). ADR-0584는 stale 값을 격리/표시만 하고 교정 안 함(73.4 그대로). 공식 KRX VKOSPI 인덱스코드는 외부확인 없이 하드코딩 위험 → 대신 기존 Yahoo `^VKOSPI` fallback을 frozen 시에도 활용해 코드검증 없이 실제값 교정. (D1) marketDataRefresh: flag ON ∧ detectVkospiFlatDuringMove(ADR-0584 SSOT, |KOSPI|≥3 ∧ |VKOSPI changePct|<0.5) → KRX 거부(VKOSPI_KRX_REJECTED_FROZEN 경고)+throw → Yahoo fallback 진입. (D2) Yahoo 대체값도 frozen 검증 → frozen이면 미채택(VKOSPI_YAHOO_REJECTED_FROZEN), 정상이면 채택=교정값. (D3) 둘 다 거부/부재 → carry-forward(ADR-0584 경고)·잘못된 값 신규기록 안 함. 안전 판별자=flat-during-move만(VIX-괴리 단독거부는 진짜 spike 마스킹 위험 배제). flag OFF=fetch 경로 byte-identical. executionImpact: OFF=NONE / ON=execution-adjacent(served vkospi→risk axis/R6)→관측 권고. 불변식 #6(provider 신선도≠market signal·0치환0)·ADR-0561 KIS-primary(KRX L1 우선·신규 Yahoo-first 0). 잔존 후속: 공식 인덱스코드 검증·risk axis trustState 소비·baseDate 캘린더 stale-gate. 테스트 flag 독립성+detector 재사용(0584 33건). 롤백 ENV 제거 byte-equivalent. 계보 ADR-0530/0584. INDEX 0585→0586 갱신.)
(2026-06-07 기준, 마지막 발급 0584 — vkospi-stale-freshness-flatduringmove-guard. ADR-0584 — VKOSPI stale 가시화(Phase A always-on·executionImpact=NONE) + flag-gated flat-during-move 격리(Phase B `VKOSPI_STALE_GUARD_ENABLED` default OFF·byte-identical). 배경=라이브 VKOSPI 73.4(prevClose 73.44·dayChange 0.00·VIX 16.1·KOSPI −5.54%) stale 확정(3-에이전트 진단): 이름 substring fetch(공식코드 검증 0)·신선도추적 부재(BAS_DD 폐기·*FetchedAt 없음)·silent carry-forward(warn 0)·ADR-0530 가드 G2(KOSPI 비스트레스)가 폭락일엔 false라 stale-high 통과. 신규 SSOT vkospiFreshness.ts(detectVkospiFlatDuringMove=|KOSPI수익률|≥3 ∧ |VKOSPI변화|<0.5 → frozen, 캘린더 의존0·진짜 spike는 큰 변화라 미마스킹 / isVkospiStaleGuardEnabled / classifyVkospiFreshness). **Phase A**: marketDataRefresh가 vkospiBaseDate(BAS_DD)+vkospiFetchedAt 영속·다중매칭 VKOSPI_ROW_AMBIGUOUS 경고·KRX+Yahoo 실패 시 VKOSPI_CARRY_FORWARD 경고·/regime ⚠️STALE 마커+baseDate/fetchedAt 노출. **Phase B**: vkospiSanityGuard G4 추가(flag ON ∧ flat-during-move → UNTRUSTED_IMPLAUSIBLE, G2 무관 → 폭락일 갭 차단), guardTriggered=!disabled&&(classic||g4), flag OFF=byte-identical, 기존 IMPLAUSIBLE level gate 재사용(신규 배선 0). 불변식 #6 보존(providerIssue·marketSignal=false·null≠0). 한계(후속): risk axis(scoreRisk)는 원값 직독이라 격리 미반영(confluence=discovery-only)·공식 인덱스코드 검증·baseDate 캘린더 stale-gate. 테스트 33·lint0·complexity OK·responsibility0. 롤백=ENV 제거 byte-equivalent. 계보 ADR-0530. INDEX 0584→0585 갱신.)
(2026-06-07 기준, 마지막 발급 0583 — mhs-source-degrade-visibility-and-confluence-guard. ADR-0583 — MHS 소스 저하(FRED/ECOS 결손) end-to-end 가시화(Phase A) + flag-gated confluence 가드(Phase B). 배경 = 배포지 FRED hard egress 차단(DNS resolve OK·TCP TIMEOUT, IPv4-only host)으로 fred=false 인데 MHS 는 75(GREEN)로 정상처럼 표시되던 silent degradation(computeMacroIndex 가 sourcesOk 반환하나 영속·노출·소비 0). MHS 는 execution-adjacent(confluence calcMacroScore MHS≥70 +8/55~69 +3 매크로축 부스트 + fomcCalendar 완화 임계). 신규 SSOT server/engines/mhsDegrade.ts(deriveMhsDegrade: FULL/PARTIAL/FALLBACK confidence + degraded + isMhsDegradeGuardEnabled flag). **Phase A 항상-on executionImpact=NONE**: marketDataRefresh 가 매 사이클 idx.sourcesOk→deriveMhsDegrade 도출해 MacroState 영속(mhsSourcesOk/mhsConfidence/mhsDegraded, computeMacroIndex 성공 시에만·실패 시 이전값 보존) + MHS 로그 confidence 태그 + /regime formatMhsConfidenceLine 1줄(어느 소스 살아있는지+신뢰도). **Phase B flag MHS_DEGRADE_GUARD_ENABLED default OFF byte-identical**: ON+mhsDegraded 시에만 calcMacroScore MHS 보정 비대칭 억제 — ≥70 +8→+3(factor GREEN_DEGRADED)·55~69 +3→0(낙관 부스트 생략)·<40 -15 보존(비관 페널티=안전 방향 유지). flag OFF/mhsDegraded≠true=기존 분기 byte-identical. executionImpact: Phase A=NONE / Phase B OFF=NONE·ON=execution-adjacent(confluence signal 영향→shadow A/B 권고). 불변식 #6 정합(FRED 결손=confidence 강등이지 약세 신호 변환 아님)·#9(MacroState read/write only·신규 fetch 0)·VERBATIM 0줄. 테스트 30(진리표·flag·/regime 라인·가드 진리표). 롤백=ENV 제거 byte-equivalent. INDEX 0583→0584 갱신.)
(2026-06-07 기준, 마지막 발급 0582 — condition-verifiability-3tier-technical-computed-promotion. ADR-0582 — 종목 27조건을 검증가능(24)/본질-AI(3: 촉매·심리·엘리엇)로 정적 분리하고, 본질-AI 를 weightedScore 분모에서 제외(점수=검증가능 항목 중 검증율). 기술 6조건(일목·골든크로스·거래량·터틀·피보나치·다이버전스)을 enrichment main path 에서 OHLCV 결정적 계산으로 checklist 덮어쓰기→COMPUTED 검증 승격, marginAcceleration 은 DART 영업이익률 당기>전기로 API 승격. 검증가능 13→20, 최종 점수 상한 ~74→~92. 표시/진단 전용(서버 autoTradeEngine/SourceSnapshot/Gate/Provider/Telegram/Shadow 무접촉, executionImpact=NONE, 9대 불변식 VERBATIM 0줄). ConditionView.verifiability 필드·canon evaluableCount/intrinsicAiCount/intrinsicAiMetCount 신설, UI 3-구분(검증/AI 검증가능 미수집/정성-AI 분모제외) 노출. 회귀 finalScore 59→60. ※ 최초 0581 발급됐으나 main ADR-0581(shadow→live 가중치 승격)과 번호 충돌 → merge 시 0582 재번호. 후속(상대강도·모멘텀순위 벤치마크 fetch)=별도 ADR. INDEX 0582→0583 갱신.)
(2026-06-07 기준, 마지막 발급 0581 — shadow-to-live-weight-promotion-pipeline. ADR-0581 — shadow/candidate 검증 조건가중치 → live 승격 파이프라인(phased, flag-gated). **Phase 0~2 구현(전부 byte-equivalent·executionImpact=NONE·live 미도달)**: P0 `LEARNING_WEIGHT_PROMOTION_ENABLED` flag(default OFF) / P1 candidate 레인 read `loadCandidateConditionWeights`+`conditionWeightsCandidateFile`(write-only 갭 해소) / P2 `registerLearnedConditionWeightProvider` seam(미등록=파일직독 byte-identical, gateConfig provider 패턴 동형). **Phase 3~4(candidate→core promoter·governance 영속·rollout·snapshot 복원)는 follow-up, live 전환 전 마무리 예정**. paper→live 신뢰경계 보존(shadow 단독 자동 core write 불가, guardrail NON_LIVE_SOURCE). 70 anchor 무관. seam 테스트 8/8·lint0·complexity OK·responsibility0. INDEX 0580→0581 갱신.)
(2026-06-07 기준, 마지막 발급 0580 — marketdatarefresh-acma-type-helper-extraction. ADR-0580 — marketDataRefresh.ts(1499→1327, ACMA 1500 한계 1줄 차 선제 분해). 타입 8종→`marketDataRefresh/types.ts` + 순수 leaf 헬퍼 11→`helpers.ts` 추출 + `export *` 재노출(외부 importer 0건). execution-relevant 파일이나 타입+순수함수 재배치라 런타임 byte-equivalent·executionImpact=NONE. 거대함수 refreshMarketRegimeVars(739L) 무접촉. lint0·complexity OK·테스트 9파일/130 통과. INDEX 0579→0580 갱신.)
(2026-06-06 기준, 마지막 발급 0579 — acma-imminent-files-type-extraction-decomposition. ADR-0579 — ACMA 1,500 한계 임박 2파일 선제 type-only 분해: gate1DryRunObservationLedgerAdr0476.ts(1500→1242, SHADOW_ONLY executionImpact=NONE) + sectorEnergyProvider.ts(1499→1343, type-only byte-equivalent). type/interface 선언을 각 `<name>/types.ts` 서브모듈로 추출 + `export *` 재노출(외부 importer 경로 0건). 정적 grep-guard 2건 본문+types 동시 read 로 갱신(ADR-0444). 01/09 복잡도 표·INDEX 푸터 실측 동기화 동반. lint0·complexity OK·responsibility0·gate1 25/25·sector 71/71. INDEX 0578→0579 갱신.)
(2026-06-05 기준, 마지막 발급 0578 — gate1-technical-indicator-injection. ADR-0578 — Gate1 기술지표 주입 배선(존재-but-미배선). injectPerSymbolPriceContext 가 일봉 bars 를 fetch 해 return/volume 만 주입하고 ma20/ma60/rsi14/atr 미계산 → 스코어러 TECHNICAL_TREND(14)+VOLUME_LIQUIDITY 항상 0 → Gate1 49/70 고착(RS 48/48 vs tech 22/48 비대칭: RS 는 featurePack fallback 생존·기술은 quote 단독). 산식은 kisQuoteAdapter 에 이미 존재. (D1) _indicators 에 calcSMA·calcATR 추가(kisQuoteAdapter 동일 산식, RSI 는 calcRSI14 재사용). (D2) flag GATE1_TECHNICAL_INDICATOR_INJECTION_ENABLED ON 시 동일 bars(역순 변환)로 지표 계산→candidate.symbolFeatures 주입(기존값 보존). (D3) KIS lookback 35→90 역일(ma60용, 호출수 불변 rows만). (D4) technicalInjected 로깅으로 bar-fetch 커버리지 실측. flag OFF default=byte-equivalent. executionImpact OFF=NONE/ON=execution-adjacent(requiredScore 70·×1 게이트 0변경, score 상승으로 Gate1 통과 증가 가능→shadow 검증 권고). providerImpact 호출수 불변. 불변식 #6/#7 보존·스코어러 본체·70 무접촉·VERBATIM 0줄. 롤백 ENV 제거 byte-equivalent. 테스트 +4. INDEX 0578→0579 갱신.)
(2026-06-05 기준, 마지막 발급 0577 — sector-energy-canonical-percandidate-leadership-unification. ADR-0577 — 섹터에너지 canonical 일원화: per-candidate Gate2 SECTOR_LEADERSHIP 축 데이터원을 레거시 basket(KIS_SECTOR_ISCD_MAP 2xxx·DIAGNOSTIC_ONLY)→canonical verified official(OFFICIAL_SECTOR_ENERGY_BASE_VERIFY_TARGETS L1·11/11 VERIFIED)로 단일화. normalizeSectorThemeCycleForGate2 우선순위 raw??canonical(L1·ADR-0570 sectorIndexCycleProvider 룩업·신규 KIS콜 0)??basket fallback. flag default OFF byte-identical, executionImpact=execution-adjacent(sectorReturn20d→sectorScoreBoost +2/-1→STRONG_BUY)→shadow A/B 필수. 데이터위계 canonical=L1·basket=diagnostic(ADR-0561 정합). 9대 불변식 VERBATIM 0줄. ※ 최초 ADR-0572 로 발급됐으나 main ADR-0572(ATR 손절밴드)와 번호 충돌 → merge 시 0577 재번호. INDEX 0576→0577 갱신.)

(2026-06-05 기준, 마지막 발급 0576 — shadow-p0-operational-warn-escalation-exclusion. ADR-0576 — SHADOW(실거래 아님·학습) P0 operational warn 을 T1 ack 폐루프(재발송+60분 CRITICAL 에스컬레이션)에서 제외(불변식 #8). 배경 = "[T1 경보 60분 미확인 — 에스컬레이션]" 이 P0 EXECUTION(symbol=011070 mode=SHADOW)으로 반복 발생(사용자 보고), SHADOW 는 실주문/자금 0(executionImpact=SHADOW_EXECUTION_DEGRADED, LIVE_ORDER_BLOCKED 아님)인데 LIVE 와 동일 urgency 60분 CRITICAL 에스컬레이션 = 과도 노이즈. (D1) emitTelegramCriticalAlert 의 SHADOW/SHADOW_ONLY P0 발송에 requireAck:false → T1 ack 미등록 → 재발송·에스컬레이션 0, 단 1회 발송으로 가시성 보존(불변식 #2). (D2) LIVE/모드미상 P0 는 기존 ack 폐루프+에스컬레이션 유지. (D3) ENV SHADOW_P0_ACK_LOOP_ENABLED=true 1줄 롤백(=== 'true', default 미설정=제외). executionImpact=NONE(알림 폐루프 진입 여부만·실주문/SourceSnapshot/Gate/사이징/Shadow 무접촉, #8 정합). 계보 ADR-0042/532/0573(ack family). 9대 불변식 VERBATIM 0줄. 테스트 3(SHADOW requireAck:false/LIVE 유지/ENV 복원)+ackFamily 계승. 롤백=ENV byte-equivalent. INDEX 0576→0577 갱신.)

(2026-06-05 기준, 마지막 발급 0574 — kis-sector-name-canonical-normalization. ADR-0574 — kis sector name canonical normalization. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-05 기준, 마지막 발급 0573 — ack-family-supersession. ADR-0573 — T1 ack 패밀리 흡수. stopApproachAlert Stage2("손절 임박")·Stage3("손절 집행 임박")는 priority CRITICAL→tier T1_ALARM→ackTracker T1 ack 등록, 미확인 시 sweepPendingAcks 30분 재발송+60분 에스컬레이션. 두 단계 dedupeKey 가 stop_approach_2:CODE / stop_approach_3:CODE 로 달라 기존 dedupeKey 흡수 미적용 → 둘 다 pending → 한 종목에서 재발송/에스컬레이션 단계 수만큼 배가("[재발송 — 미확인 T1 경보]" 이중 발생, 사용자 보고). ADR-0572(ATR 밴드)가 발생 *빈도* 를 줄였으나 다단계 폐루프 중복은 잔존. (D1) TelegramAlertOptions.ackFamilyKey? 신설·T1AckEntry.familyKey 영속·registerPendingAck 가 같은 familyKey 이전 pending ack 흡수(새 ack 가 이전 단계 대체) → 종목당 활성 [확인] 1건. (D2) stopApproachAlert 3단계 ackFamilyKey: stop_approach:{종목코드} 공유 → Stage3 이 Stage2 ack 흡수 → 재발송/에스컬레이션 종목당 3→1. (D3) 기존 dedupeKey 흡수 유지(additive)·ackFamilyKey 미지정 모든 호출자 byte-equivalent·RESEND_AFTER_MS 30분/ESCALATE_AFTER_MS 60분 간격 무변경(흡수만). (D4) ack 안전망(미확인 재발송·에스컬레이션) 보존(종목당 최신 단계 1건). executionImpact=NONE(알림 폐루프 엔트리 수만 축소·실주문/SourceSnapshot/Gate/사이징/Shadow 무접촉). 계보 ADR-0042(개인 DM 손절 카운트다운)·ADR-532(telegram noise reduction)·ADR-0572(ATR 밴드, 상보). 9대 불변식 VERBATIM 0줄(#1/#2/#6/#8 보존). 롤백=stopApproachAlert ackFamilyKey 부여 제거 byte-equivalent(별도 ENV 불필요, 호출자 opt 제거가 곧 롤백). 테스트 engine-dev 5케이스(familyKey 흡수/다른 키 유지/미지정 회귀/dedupeKey 회귀/stopApproachAlert opt 전파). INDEX 0573→0574 갱신.)
(2026-06-05 기준, 마지막 발급 0572 — atr-dynamic-stop-approach-bands. ADR-0572 — 손절 접근 3단계 경보(stopApproachAlert)의 distToStop 발동 임계를 고정 5/3/1% → 종목 ATR14 동적 밴드 min(배수×atrPct, 고정 cap)로 전환. 배경 = KB금융 등 저변동주에서 손절선이 진입가에 가까워 소폭 하락에도 "손절 임박/집행 임박" 개인 DM 과발생(사용자 "너무 민감"), 근본원인=ATR(변동성) 무관 고정 밴드. (D1) atrPct=entryATR14/currentPrice×100, band(stage)=min(MULTIPLIER×atrPct, LEGACY): Stage1 2.0×ATR%/cap5 · Stage2 1.0×ATR%/cap3 · Stage3 0.4×ATR%/cap1. (D2) cap 단방향 — ATR 스케일링은 밴드를 *좁히기만* 함 → 고변동주는 cap 적용 byte-equivalent(현상 유지), 저변동주만 둔감화, 어떤 입력도 레거시보다 더 민감해지지 않음 → 회귀 위험 0. (D3) Fallback = entryATR14 부재/비유한/≤0, currentPrice 비유한/≤0 → 고정 5/3/1% 보수적 복귀. (D4) ENV STOP_APPROACH_ATR_BANDS_DISABLED default OFF=기능 ON(저변동주 과민 해소), true 1줄=고정 밴드 전면 복원 즉시 롤백. executionImpact=NONE — 개인 DM(sendPrivateAlert) 알림 *발동 임계* 만 조정, 실주문/SourceSnapshot/Gate score/사이징/Shadow/hardStopLoss 가격 무접촉, 실제 청산은 channelSellSignal 사후 보고(ADR-0042 계승). 계보 ADR-0028a(exitEngine 분해)·0042(개인 DM·countdown)·0079(ATR BEP glide, entryATR14 흐름 재사용). 9대 불변식 VERBATIM 0줄(#1/#2/#6/#8 보존). 테스트 = 고변동 cap 보존·저변동 둔감화·entryATR14/currentPrice fallback·ENV 롤백·dedupe/source 무영향·cap 단방향. INDEX 0572→0573 갱신.)

(2026-06-04 기준, 마지막 발급 0571 — candidate-sector-gate2-themecycle-wiring. ADR-0571 — 후보 sector→Gate2 sectorThemeCycle.sector 합성 배선(ADR-0423/0568/0570 완결편). 배경 = 0568(threading)·0570(데이터) flag 둘 다 ON 인데도 /scan_blockers(2026-06-04 15:18 KST) "Sector 0/25 | sector=UNKNOWN" 지속 — 근본원인 repo 전체 sectorThemeCycle producer 0건이라 후보 sector 항상 null → getSectorEnergyScore 매칭 실패 → sectorReturn20d=null → SECTOR_THEME_CYCLE_MISSING. (D1) sectorThemeCycleProducer 신설 = canonicalizeSectorName(stockSector) ?? canonicalizeSectorName(getSectorByCode(symbol)) 로 세분화 라벨(반도체소재·2차전지·헬스케어)→12 canonical KRX 섹터명 귀속(신규 섹터 0, KIS콜 0, sectorMap SSOT 재사용). (D2) externalCoverage 가 flag ON 시 producer 호출 → sectorThemeCycle ?? produced → normalizer canonical sector 매칭 → OK_WITH_DATA. (D3) 매칭 실패/미분류→undefined→quote/stockMaster.sector fallback graceful(byte-equal). (D4) ADR-0568 동일 flag SECTOR_ENERGY_GATE2_WIRING_ENABLED 재사용(신규 flag 0) default OFF=byte-equal. executionImpact=(b) execution-adjacent(sectorReturn20d→sectorScoreBoost +2/-1→STRONG_BUY)→shadow 검증 필수. ADR-0448 hardBlock 신설 0·불변식 #6 marketSignal:false·VERBATIM 0줄. 테스트 producer 7 + gate2Diagnostics 45(fixture canonical 화). 롤백=ENV OFF byte-equivalent. INDEX 0571→0572 갱신.)

(2026-06-04 기준, 마지막 발급 0568 — sector-energy-gate2-confluence-wiring. ADR-0568 — sector energy gate2 confluence wiring. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)
(2026-06-04 기준, 마지막 발급 0570 — sector-index-20d-cycle-wiring. ADR-0570 — 공식 섹터 index 20d/5d return 을 SectorEnergyInput.sectorReturn20d(+5d)에 배선해 Gate2 sector cycle "Sector 0/25"(SECTOR_THEME_CYCLE_MISSING) 해소(ADR-0423 후속). (D1) fetch 재사용 = fetchKisSectorIndexDaily(realDataKisGet SSOT 경유, raw KIS 0), 섹터→iscd 매핑 = OFFICIAL_SECTOR_ENERGY_BASE_VERIFY_TARGETS(SectorEnergyCanonicalResolver verifiedMapping, 반도체→4003·자동차→4002·화학→0008…) 재사용·신규매핑 0. (D2) sectorIndexCycleProvider 가 섹터당 1콜(~11)·refresh 당 1회·6h+ TTL 캐시(휴장 인지)·후보당 재fetch 0, ADR-0561 KIS-primary 정합. (D3) buildInputsFromDeltas(KRX_CODE·STOCK_DAILY·indexName fallback 단일 chokepoint)가 sectorCycleInputFields spread → evaluateSectorEnergy hasKisSectorMetrics 점화 → scores[].sectorReturn20d 살아남 → normalizeSectorThemeCycleForGate2 OK_WITH_DATA. (D4) flag SECTOR_INDEX_CYCLE_WIRING_ENABLED default OFF=byte-equal(빈 Map 미배선·KIS 추가콜 0), throw 격리 graceful 미배선 복귀. (D5) executionImpact=(b) execution-adjacent — hasKisSectorMetrics 가 sectorReturn20d finite 시 scoring formula 분기 변경 → energyScore → tier(LEADING/LAGGING) → applySectorScoreBoost +2/-1 → STRONG_BUY 게이팅 영향 → default OFF + shadow 검증 필수(diagnostic-only 아님). (D6) sector=UNKNOWN 종목은 normalizer FIELD_MISSING 으로 잔존(분류 SSOT 별건, ADR-0534 verifiedMapping 무접촉). 불변식 #1/ADR-0448(hardBlock 신설 0)·#6(marketSignal:false)·§2.2-2(raw KIS 0) 보존. 9대 불변식 VERBATIM 0줄. 롤백=ENV 제거 byte-equivalent. 테스트 13 pass(return 계산·flag OFF·캐시·graceful·MISSING 해소·marketSignal·UNKNOWN 잔존). 외부 ADR-0568(sectorEnergyResult→Gate2 confluence thread)과 상보 통합 — 본 0570 는 sectorReturn20d *데이터* 정확화, 0568 은 결과 *threading*. INDEX 0570→0571 갱신.)

(2026-06-04 기준, 마지막 발급 0568 — sector-energy-gate2-confluence-wiring. ADR-0568 — sector energy gate2 confluence wiring. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)
(2026-06-04 기준, 마지막 발급 0567 — canonicalsession-wallclock-stale-correction. ADR-0567 — canonicalSession stale-CLOSED wall-clock 교정(flag-gated 진단 정합). 계승 ADR-0559(휴장일 SSOT). 배경 = /scan_blockers 장중(2026-06-04 11:03 KST 거래일) 덤프에서 marketSessionState=CLOSED·canonicalSession=CLOSED 관측(같은 덤프 entryFilter=NORMAL 불일치). 근본원인 = resolveScanMarketSessionView 의 wall-clock 보정이 canonical∈{UNKNOWN,REGULAR_OPEN}일 때만 적용 → 상류 macroGateState stale CLOSED 를 장중 wall-clock(REGULAR_OPEN)으로 못 고침 → live 전환 시 장중 false-차단. (D1) holiday-aware 교정 분기 추가: canonical∈{CLOSED,POST_CLOSE}+intraday wall-clock(ISO T-anchored 파서)=REGULAR_OPEN 시 충돌 진단로그(silent 금지)+flag ON&KRX 거래일에만 canonical='REGULAR_OPEN' 교정. (D2) 거래일 판정 isKrxTradingDay(krxHolidays SSOT, ADR-0559) 위임 — 신규 휴일셋 0, 공휴일(2026-06-03 지방선거)·주말·장외 false-open 차단. (D3) flag SCAN_SESSION_WALLCLOCK_CORRECTION_ENABLED default OFF=byte-equal(OFF 경로 canonical 무변경·진단로그만, 세션 산출 동일). 부수발견 kstMinuteFromLabel loose 정규식(ISO mm:ss 오인)은 교정 분기 한정 T-anchored 파서로 회피·legacy 무변경(별도 patch 권장). 9대 불변식 VERBATIM 0줄·#4#5 정합(세션은 policy/permission만, SourceSnapshot 불변)·실주문 본체 0줄. executionImpact flag OFF NONE/flag ON live-gating permissive 교정. 롤백=ENV 제거 byte-equivalent. 테스트 19 pass(진리표 전수). INDEX 0567→0568 갱신.)

(2026-06-03 기준, 마지막 발급 0566 — newssupplylogger-domestic-kis-split. ADR-0566 — newsSupplyLogger 국내 종가 KIS(L1) 분리(잔존 burn-down #3, 혼합 callsite 심볼 분리). flag-gated: OFF default=byte-equal, ON=국내 KIS. 계승 ADR-0561/0562(B4 EWY)/0563/0564/0565. 배경 = fetchNDayChange(symbol,n) 의 fetchCloses(Yahoo)를 EWY(글로벌 ETF, B4 KIS 미커버, L157/166)와 국내 code(.KS/.KQ, L177)가 공유 → 국내만 KIS 분리하려면 callsite 분리 필요(비실행 학습 귀인). (D1) fetchNDayChangeDomestic(yahooSymbol,n) 신규 — .KS/.KQ→6자리 code(/^\d{6}$/)→KIS 일봉 종가 동일 idx 산식(calDays·idx=len-1-n), 비국내/부족→fetchNDayChange 위임. T+5 국내루프(L177)만 교체, EWY(L157/166)는 fetchNDayChange 유지(B4 분리 안 함). (D2) flag KIS_OHLCV_PRIMARY_ENABLED 재사용: OFF=즉시 fetchNDayChange 위임 byte-equal+fetchKisDailyCandles lazy await import(import 그래프 byte-equal), ON=국내 KIS 1차→부족 시 Yahoo. (D3) 잔존 fetchCloses(var)=EWY 글로벌(B4)+국내 fallback 전용 → 가드 GRANDFATHER→WHITELIST 승격(globalScanAgent 동형), grandfather 8→7. (D4) shadow A/B 미적용(비실행), flag-OFF byte-equiv 구조적(guarded if+lazy). 9대 불변식 VERBATIM 0줄·ADR-0562 B4(EWY 영구 Yahoo) 계승·실행경로/Shadow 무접촉·executionImpact NONE(flag OFF)·providerImpact=flag ON T+5 국내 종목당 KIS 1콜(주간 bound). 롤백=L177 복원+함수 제거+whitelist→grandfather byte-equivalent. INDEX 0566→0567 갱신.)

(2026-06-03 기준, 마지막 발급 0565 — latewinevaluator-kis-ohlcv-insertion. ADR-0565 — lateWinEvaluator KIS(L1) 일봉 OHLCV 1차 삽입(잔존 burn-down #2, ADR-0563 §D4 "OHLCV" shape). flag-gated: flag OFF default=byte-equal, flag ON=KIS 1차. 계승 ADR-0561/0563/0564. 배경 = lateWinEvaluator.fetchOHLCV 가 raw query2/query1.finance.yahoo.com/v8/chart(.KS/.KQ) 직접 URL 로 OHLCVDay[](date/high/low/close, 과거→최신) 조회, reEvaluateExpired 가 EXPIRED 추천 60/90일 재평가에 사용(learningOrchestrator 소비, 비실행). (D1) fetchOHLCV 진입부 fetchOHLCVFromKis(code,from,to) 1차 삽입 — fetchKisChartData(code,'D',formatYmdCompact(from),formatYmdCompact(to)) 캔들→OHLCVDay 매핑(date 'YYYYMMDD'→'YYYY-MM-DD' 변환, Date.parse 보존, close≤0/비유한 제외), 빈→[]→Yahoo graceful(불변식6). 동일 from/to→KIS·Yahoo 동일 거래일 인덱스 정합(ohlcv[59]/[89] 의미 보존, ~65거래일<100봉 단일콜). (D2) flag KIS_OHLCV_PRIMARY_ENABLED 재사용: OFF=skip+fetchKisChartData lazy await import(import 그래프 byte-equal), ON=KIS 1차→빈 시 Yahoo. (D3) Yahoo fallback 강등 → 가드 GRANDFATHER→WHITELIST 승격, grandfather 12→10. (D4) shadow A/B·전용 테스트 미적용 — 비실행+fetchOHLCV private+무테스트 모듈, flag-OFF byte-equiv 구조적 보장(guarded if+lazy import), 회귀=가드 EXIT0+lint+ADR-0564 선례. 9대 불변식 VERBATIM 0줄·실행경로 무접촉·executionImpact NONE(flag OFF)·providerImpact=flag ON 재평가 KIS 1콜/레코드(주간 cron·150ms bound). 롤백=삽입 revert+whitelist→grandfather byte-equivalent. INDEX 0565→0566 갱신.)

(2026-06-03 기준, 마지막 발급 0564 — koreanquotebridge-kis-second-insertion. ADR-0564 — koreanQuoteBridge KIS(L1) 2차 삽입(잔존 burn-down #1, ADR-0563 §D4 첫 실행). flag-gated 런타임: flag OFF default=byte-equal, flag ON=KIS 2차. 계승 ADR-0561/0562/0563. 배경 = koreanQuoteBridge.fetchKoreanDailyQuote 가 KRX(L1)-first 실패 시 곧바로 Yahoo(L3)로 강등(절대불변식 위반), 소비처는 krxRouter REST 단일(비실행). KIS 일봉 fetchKisDailyCandles(FHKST03010100 full OHLCV)가 대체자산. (D1) KRX·Yahoo 사이 fetchFromKis(code) 삽입 — fetchKisDailyCandles(code,12) 최신 캔들(과거→최신, closeSeriesProvider 동형)로 KoreanDailyQuote 구성(close/open/high/low/volume=최신·prevClose=직전·change/changePct=safePctChange·source 'kis'·name=''), 0개/close≤0→null. (D2) flag KIS_OHLCV_PRIMARY_ENABLED 재사용(R1/closeSeriesProvider 동일 도메인·신규 ENV 0): OFF=블록 skip=byte-equal(krxRouter 응답·source·호출순서 불변), ON=KRX 실패 시 KIS→실패 시에만 Yahoo. (D3) Yahoo 최후 fallback 강등 → 가드 GRANDFATHER→WHITELIST 승격(KRX+KIS primary+Yahoo 최후 fallback 브릿지, closeSeriesProvider 동형), grandfather 13→12. (D4) shadow A/B 미적용 — ADR-0563 §D4 shadow A/B 는 실행경로(Gate score 괴리) 전제, koreanQuoteBridge 비실행→괴리 0, flag-OFF byte-equiv+KIS 단위테스트로 충분. 9대 불변식 VERBATIM 0줄·KIS 장애→null→Yahoo graceful(불변식#6)·실행경로 무접촉·executionImpact NONE(flag OFF)·providerImpact=flag ON KRX 실패경로 KIS 1콜(krxRouter 빈도 bound). 롤백=fetchFromKis 삽입 revert+가드 whitelist→grandfather 복원 byte-equivalent. INDEX 0564→0565 갱신.)

(2026-06-03 기준, 마지막 발급 0563 — execution-path-yahoo-burndown-complete-residual-freeze. ADR-0563 — 실행경로 Yahoo burn-down 완료 선언 + 잔존 비실행 동결(문서/ADR/CLAUDE.md/가드 사유문자열 전용·런타임 .ts 동작 0줄·ENV 0·quota 0·탐지 로직 무변경). 계승 ADR-0561(KIS Primary 절대불변식)·0562(경계 잠금+가드 전수 탐지). 배경 = ADR-0562 가 잔존 Yahoo-first 를 GRANDFATHER 7파일/13 hit 으로 전수 가시화한 뒤 잔존을 전수 재조사하니 closeSeriesProvider 드롭인 0개 + 단 하나도 live 매수·매도·Gate score 에 유입 안 됨. (D1) 실행경로 Yahoo burn-down 완료 선언(CLOSED) — Gate quote C1 grandfather 0(R1 technicalQuoteRouter KIS FHKST03010100 primary) + LIVE 청산 종가 closeSeriesProvider(commit ee7d6c1). 사용자 절대불변식 실행경로 충족. (D2) 잔존 13 hit/7파일 비실행 동결(FROZEN, bounded backlog) — 학습 귀인 3(newsSupplyLogger:85 국내+EWY 혼재·lateWinEvaluator:68 OHLCV raw URL·historicalClosePrice:65 이미 KIS-first 단일시점)·텔레그램 표시 3(reportGenerator:896/905 ^KS11+KRW=X·marketDataRefresh:855 KRW=X 이미 ECOS 교차검증 ADR-0071·sectorSources:272/277 assetProfile 섹터)·KRX-fallback quote 1(koreanQuoteBridge:142/154 KRX(L1)-first→Yahoo fallback L1 primary 충족). 두더지잡기 종료 — 일괄·즉흥 마이그레이션 금지. (D3) 가드 사유 재분류 MIGRATION_PENDING_ADR0561 → FROZEN_NON_EXECUTION_ADR0563(check_kis_primary_invariant.js GRANDFATHER 7항목+OK 메시지+가이던스+헤더 주석+test (g) 사유 assert) — 탐지 로직·매칭·EXIT 무변경, 신규 Yahoo-first 여전히 EXIT 1 차단(drift 봉쇄 유지). (D4) 재활성=shape 별 deliberate ADR only(OHLCV/지수/ECOS-FX/섹터/KIS-quote 어댑터 라우터 서로 다름+shadow A/B+회귀), 동결이 기본값. 9대 불변식 VERBATIM 0줄·ADR-0561/0562 D1~D4 계승(무효화0)·executionImpact=NONE(런타임 동작 0·정적 텍스트/사유문자열·매매경로 무접촉·quota 0). 롤백=사유 문자열 MIGRATION_PENDING_ADR0561 revert byte-equivalent+문서 revert. INDEX 0563→0564 갱신.)

(2026-06-03 기준, 마지막 발급 0562 — yahoo-dependency-boundary-lock. ADR-0562 — Yahoo(L3) Dependency Boundary Lock: 영구 차용 5건 잠금 + 가드 전수 탐지 확장(문서/ADR/CLAUDE.md/charter/가드사양 전용·런타임 .ts 0줄·ENV 0·가드 확장 구현은 후속 engine-dev). 분류 SSOT=docs/audits/2026-06-03-yahoo-dependency-catalog.md. 문제 = ADR-0561 가드(check_kis_primary_invariant.js)가 fetchYahooQuote 2종만 탐지 → 직접 URL(query1/query2.finance.yahoo.com)·.KS/.KQ raw concat·fetchCloses('^KS11'/'KRW=X') 국내심볼·Yahoo client 직접호출(yahooConsensusClient/dxyIntradayClient/fetchYahooSector/fetchYahooBundle) 사각지대 → 두더지잡기 미종료(lateWinEvaluator:68·historicalClosePrice:65·koreanQuoteBridge:142·sectorSources:272·reportGenerator:896/905·marketDataRefresh:855·exitEngine/helpers priceHistory:29/ma60:42 가 현 가드 EXIT 0 통과하지만 실제 Yahoo-first). (D1) (B)4+(D)1=5건 영구 정당 차용 잠금(재논의 종료): B1 컨센서스(yahooConsensusClient, KIS endpoint 0)·B2 미국/일본/중국지수+VIX(globalScanAgent/preMarketSignal/macroSectorSync/marketOverviewIndicators, KIS overseas N=다우/나스닥/S&P만·VIX/Nikkei/CSI300 미커버)·B3 DXY(dxyIntradayClient/dxyMonitor, DX-Y.NYB ICE 합성지수 부재)·B4 글로벌ETF+WTI(EWY/SOXX/XLE/WOOD/CL=F, S=금선물만·WTI 미커버)·D1 AI추천Tier3(quantitativeCandidateGenerator/aiUniverseService, ADR-0011 자동매매 분리). (D2) (C)5 사용처 burn-down 목표0: C1 국내quote/OHLCV(FHKST03010100, burn-down 완료→WHITELIST 흡수)·C2 국내historical/exit종가(lateWinEvaluator/historicalClosePrice/exitEngine priceHistory·ma60, catalog 미포착 exit경로 추가발견)·C3 KOSPI/KOSDAQ지수(FHKUP03500100)·C4 USD/KRW(ECOS-primary 승격 ADR-0071)·C5 섹터(KRX 업종분류). (D3) 가드 확장=통합(신규 분리 기각, KIS-primary 단일 가드 SSOT): DETECTORS D-URL/D-SUFFIX/D-CLOSES-DOMESTIC(국내심볼 ^KS11/^KQ11/KRW=X만 위반·글로벌심볼 ^GSPC/^VIX/DX-Y/=F/EWY는 B whitelist)/D-YAHOO-CLIENT, WHITELIST(B4+D1+라우터/resolver/adapter/policy 사유주석), GRANDFATHER_ALLOWLIST(C 8 file:line MIGRATION_PENDING_ADR0561 파일단위 burn-down), 그외 신규=EXIT 1, FP 최소화(주석/import/정의/문자열/whitelist/grandfather 제외·fetchCloses 변수인자 callsite 파일분류). (D4) Yahoo 최소화 최종상태=5건 고정. (C)마이그레이션 byte-equal 불가→각 shadow A/B+회귀 별도 실행 ADR. 9대 불변식 VERBATIM 0줄·ADR-0561 D1~D4 계승(무효화0)·executionImpact=NONE(가드 구현단계도 NONE·정적 텍스트·매매경로 무접촉·quota 0). 롤백=확장분 revert byte-equivalent. INDEX 0562→0563 갱신.)

(2026-06-03 기준, 마지막 발급 0561 — kis-primary-absolute-invariant. ADR-0561 — KIS(L1) Primary 절대불변식: KIS-capable 레이어에서 Yahoo(L3)-first 금지. 사용자 절대불변식 선언("KIS 공급 가능 레이어에서 Yahoo primary 불가, Yahoo 는 KIS 대체불가 시에만 fallback")을 §2.3 데이터 신뢰 위계의 엄격·절대 형태로 코드화(문서/ADR/CLAUDE.md/charter/가드사양 전용·런타임 .ts 0줄·ENV 0). 위반 산증거 = 메인 Gate quote(stockScreener.ts:577 등 ~10곳)가 fetchYahooQuoteByCode 로 L3-first → MA/RSI/MACD/ATR/BB/MTAS 시계열 파생지표가 매수·매도 결정에 L3 primary 유입(§2.3 위반). KIS 공급 가능(ADR-0547 자산 완비: buildExtendedFromKisDaily/fetchTechnicalQuote/enrichQuoteWithKisMTAS/_indicators SSOT, FHKST03010100 D/W/M 1콜=100건이 전 파생지표 OHLCV 전량 공급, Yahoo 고유 계산불가=0, 갭=per 1개). (D1) 절대불변식 채택 — KIS-capable 레이어 Yahoo primary 금지, Yahoo=KIS 불가 시 최후 fallback only. (D2) quota≠Yahoo-first 회피 사유 — 해법은 엔지니어링(KIS 일봉 6h+휴장 인지 TTL 캐시·배치·rate 관리·DISCOVERY 버킷·cooldown); **ADR-0547 §8(스캔 단계 Yahoo 우선 유지) 폐기·개정**(스캔도 불변식 적용·quota 는 캐시/배치로 해소), ADR-0547 본체(KIS-first 빌더/라우터/ENV/100건 분석)는 계승·강화(무효화 아님). (D3) Yahoo 허용=진짜 대체불가만(ADR-0558 정당경계 패턴) — 현 후보 trailingPE(per) 1개 잠정허용(KIS finance 보강 재검토 후 burn-down), A-1 시계열 파생지표 전부 Yahoo-first 금지(마이그레이션 대상), raw 시세 이미 KIS primary. (D4) 위반 탐지 가드 사양(후속 engine-dev check_kis_primary_invariant.js) — fetchYahooQuoteByCode/fetchYahooQuote Gate/매매 primary 직접호출 텍스트 grep, router fallback·resolver·per 화이트리스트, 현존 ~10곳 GRANDFATHER_ALLOWLIST(MIGRATION_PENDING_ADR0561) 통과·마이그레이션 PR마다 burn-down, 신규 Yahoo-first=EXIT 1, check_ssot_drift_registry/single_funnel 패턴 재사용·validate:all+precommit 등재. Gate quote 마이그레이션 의무화(byte-equal 불가=수정주가 괴리·per 강등 → shadow A/B 허용오차 확정 후 별도 실행 ADR/패치), 9대 불변식 VERBATIM 0줄(§2.3 데이터 신뢰 절대규칙으로 등재), 현재가 라우터 SSOT 직교. executionImpact=NONE(본 ADR 문서, 후속 마이그레이션 구현은 shadow A/B 후 별도). INDEX 0561→0562 갱신.)

(2026-06-03 기준, 마지막 발급 0560 — ssot-drift-prevention-registry. ADR-0560 — SSOT Drift-Prevention Registry + 정적 가드(동일개념 다중구현 사전 차단, 문서/레지스트리/가드사양 전용·런타임 .ts 0줄·가드 구현은 후속 engine-dev). 문제 = SSOT Single-Funnel 프로그램(ADR-0555~0559)이 동일개념 다중구현 drift 를 *사후·운(運)* 으로만 발견(calendar isKrxHoliday 2027 LIVE 구멍이 산 증거 — golden master 가 우연히 전수비교했기에 발견). 신규 PR 의 3번째 구현을 커밋타임 차단하는 장치 부재(check_ssot_single_funnel 은 provider import 만 보고 심볼 중복 미검사). (D1) docs/SSOT_REGISTRY.md(개념→owner file:export→guarded 심볼→LEGITIMATE 예외→근거 ADR 5컬럼) 를 drift-prevention 데이터 SSOT 채택 — KIS endpoint registry 텍스트검증 모델(check_kis_official_endpoint_registry.js) 일반화, 새 SSOT 신설 0(ADR-0555 §0 계승, owner 모듈 기록만). (D2) 정적가드 check_ssot_drift_registry.js(후속 engine-dev) 가 guarded 심볼 집합에 대해 server/+src/ 2개+ 모듈 export/정의 검사, 등재 LEGITIMATE 쌍 통과·그외 신규 EXIT 1(check_ssot_single_funnel allowlist grandfather 계승). (D3) guarded 심볼별 정의/export 모듈 집합 ⊆ {owner}∪allowed 통과·외 모듈 신규 위반 보고(export 선언+정의만, 주석/import type/call site 제외). (D4) ADR-0558 원칙 계승 — 무조건 단일화 아님, 진짜 drift 만 차단·정당 분리(krxTradingCalendar 위임 re-export·addWeekdaysApprox 별도개념·ssotPipeline projection 동명이인·get/loadOpenPositions 필터강도 분리)는 LEGITIMATE 명시 허용(burn-down 제외·신규 유사 복제는 차단). (D5) grandfather(현존 LEGITIMATE 쌍 allowed 등재→통과)+burn-down(addBusinessDaysFromKstDate 인라인 catalog #2 수렴 후 allowed 빈채 유지→자동 강화). (D6) 가드 입력 계약 = GUARDED_SYMBOLS 8종(isKrxHoliday/addBusinessDaysFromKstDate/addWeekdaysApprox/collectUnifiedSnapshot/UnifiedSourceSnapshot/getOpenPositions/loadOpenPositions/MarketSession) + LEGITIMATE_PAIRS(owner+allowed[]), 메타등재 #5 providerIssue↔marketSignal 는 불변식 #6 격리로 guarded 제외. (D7) 텍스트 정규식 탐지(AST 미사용, TypeScript 컴파일·런타임 import 0)·동명이의는 레지스트리 컨셉 매핑 분리·에러메시지 필수포맷. (D8) validate:all 끝 + precommit check_ssot_single_funnel 인접 등재(engine-dev package.json) + checkFile/GUARDED_SYMBOLS/LEGITIMATE_PAIRS export→*.test.ts 잠금. 본 ADR executionImpact=NONE(런타임 0줄), 가드 구현단계도 NONE(정적 텍스트 검사·매매경로 무접촉·KIS/KRX quota 0·ENV 0·신규위반=커밋차단 마찰만). 롤백=package.json 가드 1줄 제거 byte-equivalent. INDEX 0560→0561 갱신.)

(2026-06-03 기준, 마지막 발급 0559 — calendar-ssot-krxholidays-delegation. ADR-0559 — Calendar(휴장일) SSOT 통합: 휴장일 데이터 SSOT 를 krxHolidays.ts(`KRX_HOLIDAYS` set, 2027 커버·대체공휴일 로직·ADR-0548 KIS chk-holiday L1 sync)로 단일화, krxTradingCalendar.ts 는 자체 하드코딩 휴일셋(2026만, 2027 키 0)을 버리고 위임 헬퍼 레이어로 강등(문서/ARCHITECTURE.md/타입 핀 전용, 위임 교체 런타임 .ts 0줄·구현은 후속 engine-dev). 문제 = 휴장일 판정 원장 2개가 상호 독립이고 **LIVE 게이트(preflight.ts:43,513 isKrxTradingDay→entryPolicySemantics.resolveMarketSessionState→liveOrderAllowed)가 불완전 krxTradingCalendar 의존** → 2027 평일 공휴일 8건 + 2026-12-31 구멍(golden master calendarSsot.characterization.test.ts 커밋 3643835 가 실효 불일치 9건 캡처). (D1) 데이터 SSOT=krxHolidays. (D2) krxTradingCalendar 공개 API(isKrxHoliday/isKrxTradingDay/previousKrxTradingDay/isAcceptableKrxDailyBase/isAcceptableKrxBusinessDayBase/recommendedDailyStaleWindowDays/toKstDateKey) **시그니처 보존**·내부 휴일 판정만 krxHolidays 위임 → **18개 소스 소비처 코드 무변경**(전수 확인: 모두 walk/판정 함수만 import, 휴일 set 미직접 import), 두 isKrxHoliday 동일 데이터 → divergence 구조적 소멸. (D3) krxHolidays STATIC_HOLIDAYS 에 '2026-12-31' 추가(연말 폐장 일반 규칙 인코딩 권장, 단순 1줄도 허용·과잉설계 금지). (D4) 유일 허용 행동 변경=**안전 방향만** — 통합 후 LIVE 게이트가 2027 8건+12/31 을 휴장일로 인식=실제 휴장일 주문 차단(구멍 폐쇄), **거래일을 휴장일로 잘못 막는 변경 0건**(golden master 9건 전부 truth=HOLIDAY 확인), 그 외 byte-equivalent. (D5) walk 헬퍼는 krxHolidays.isKrxBusinessDay 위 재구성·출력 동일성 유지. (D6) 책임 경계 명문화: krxHolidays=휴장일 데이터 SSOT(+KIS sync), krxTradingCalendar=거래일 walk/staleness 헬퍼(데이터 위임)·중복 isKrxHoliday 는 re-export/위임. 안전성 표 9 불일치 전부 SAFE(8 차단정정+1 비대칭폐쇄·TRADING→HOLIDAY 오재분류 0·AGREEMENT 0 regression). executionImpact: 문서단계 NONE / 구현단계 MEDIUM(LIVE 게이트 휴장일 인식 확대, 단 안전방향·byte-equivalent except 휴장일 추가·돈손실 방향 위험 0). 롤백=데이터 위임 1줄 복원. INDEX 0559→0560 갱신.)

(2026-06-03 기준, 마지막 발급 0558 — universescanner-lazy-budget-legitimate-non-funnel-boundary. ADR-0558 — universeScanner Lazy/Budget Fetch = Legitimate Non-Funnel Boundary (**SSOT Single-Funnel 프로그램 ADR-0555 헌법의 마무리 ADR**, 문서/ARCHITECTURE.md/가드주석 전용·런타임 .ts 0줄). 근거 = engine-dev 묶음5 0단계 타당성 평가 중단 보고(`bundle5-stage2-feasibility-halt.md`). 문제 = universeScanner Stage2 의 provider 직접 fetch 를 factory 정본 입력화(V1 burn-down)하려면 factory 유일 진입점 eager 전수수집 `collectUnifiedSnapshot`(`symbolDataCollector.ts:579`)을 budget gate 이전에 호출 → budget(max25, `universeScanner.ts:187`·통과후보만 `:509`·음수 changePercent SKIP 제외) 우회로 **quota 순증 + SKIP-제외 dedup 경계 파괴(불변식 #9 위반)** = 헌법 합격조건(golden 0변경·quota 순증 0) 동시 위반. (D1) universeScanner lazy/budget fetch = **quota 절약 정당 설계 분기**, factory eager 모델로 **통합하지 않는다**(비-통합이 헌법 정합). (D2) supply(`fetchKisInvestorTradeByStockDaily`, `kisClient/query.ts:901`)는 Stage2·factory **이미 동일 단일함수 공유 = 단일 소유 충족**(Registry 명문화); quote 경로 이원(`enrichQuoteWithKisMTAS`/`fetchKisStockFullQuote`)은 eager 회피로 통합 보류(정당). (D3) `scripts/check_ssot_single_funnel.js` V1/V1-ext allowlist 사유 `P3~P5 burn-down 예정`→**`LEGITIMATE_BUDGET_LAZY (영구 허용)`** 재작성(V5 `LEGITIMATE_DIAGNOSTIC` 선례 패턴, burn-down 제외, 신규 *유사* 복제는 여전히 차단). (D4) 로드맵 종결 — 묶음5(부분)·묶음6(Stage1 ranking) = **'정당한 경계 — 비추진'**, 묶음6 별도 ADR·shadow A/B 불요화; `USE_UNIFIED_SOURCE_SNAPSHOT` 전역 활성화는 marketProgram 범위·별도 shadow A/B 잔존. (D5) dead carry 방지(ADR-0557 D3) 대칭 — factory 가 universeScanner 용 quote/supply 를 eager 생산 안 하는 것이 정합. 미래 dev 오수정 방지: "왜 universeScanner 는 factory 미사용?" 을 버그로 오인 입력화 금지(budget 우회·#9 회귀). 남은 burn-down = V3(marketProgramFlowProvider, 별건). executionImpact=NONE(코드 0줄, 가드 주석 사유 재분류만, guard EXIT=0 불변). INDEX 0558→0559 갱신.)

(2026-06-03 기준, 마지막 발급 0557 — unified-sourcesnapshot-consumer-threading. ADR-0557 — Unified SourceSnapshot Consumer Threading (정본 소비 배포 계약, ADR-0556 factory 위 묶음3·5·6 재개 선행, 문서/타입 핀 전용·코드 0줄). 문제 = 묶음4 가 factory 출력에 `supply.marketProgram` 을 부착했으나 **소비처 0건(dead carry)** — 생산 경로(`signalScanner/index.ts:381/390` 지역 변수 `unifiedSnapshot`)와 소비 경로(`normalSupplyPreviewRunner.ts:127` 가 `resolveMarketProgramFlow()` 직접 재호출하는 telegram 별도 call tree)가 물리적 분리. (D1) 채택 = **C(소비자 optional `snapshot?`/`marketProgram?` 인자 + 호출자 주입) + B(비-스캔 경로 진입부 `collectUnifiedSnapshot()` 1회 호출)**; **A(retention store `getLatestUnifiedSnapshot` 신설) 기각** — 세 번째 SourceSnapshot 아티팩트=두 번째 SSOT(ADR-0555 §0·불변식 #3 위반), B+C 는 store 0(정본은 호출 시점 생산·인자 흐름뿐). (D2) 표준 threading 경로 = 스캔 경로(지역 변수 thread)·비-스캔 경로(진입부 factory 호출) 두 입구 → 하나의 주입 형태. (D3) **Consumer Contract(dead carry 방지)** = (a) 정본 present→read(정본 일치 발현)/(b) absent→기존 fallback(flag OFF byte-equivalent), **factory 부착 필드는 반드시 소비 계약 동반**. (D4) 첫 적용 묶음3 normalSupplyPreview = B+C+(a/b), 타입 = 기존 `MarketProgramFlowResult` 재사용(신규 타입 0). (D5) ARCHITECTURE.md threading 경계 등재. flag ON 시 호출수 byte-equivalent(factory marketProgram = runner 가 직접 호출하던 동일 `resolveMarketProgramFlow` 1회), 신규 store/fetch 0, throw 금지(불변식 #1)·providerIssue≠marketSignal(불변식 #6)·L4 미포함(불변식 #7). Roadmap 묶음3(NONE)→5(간접·스캔 지역 변수 thread)→6(Stage1 ranking executionImpact 직접→별도 ADR+shadow A/B, 본 ADR 대체 안 함). executionImpact=NONE. INDEX 0557→0558 갱신.)

(2026-06-03 기준, 마지막 발급 0556 — sourcesnapshot-factory-boundary-and-contract. ADR-0556 — SourceSnapshot Factory Boundary & Contract (ADR-0555 로드맵 P2 선행 계약, 문서/계약 전용·코드 0줄). (D1) factory 배치 = **기존 ADR-0519 `server/trading/symbolDataCollector.ts` 확장**, 신규 `server/snapshot/sourceSnapshotFactory.ts` 신설 *기각*(A=collector·B=`ssotPipeline.ts` projection 에 이은 세 번째 아티팩트=두 번째 SSOT, ADR-0555 §0 위반). B(`ssotPipeline.ts` `UnifiedSourceSnapshot`/`FeatureSnapshot`)는 동명이인 — factory 아닌 perSymbol→gate/policy 순수 projection 레이어로 책임 재확인(병합·이동 0). (D2) freshness = **기존 enum 재사용**(`DataHealth` `shadowCase.ts:29` 공통어휘 + 도메인별 `providerHealth`/`SymbolDataQuality`/`SymbolDartCadence` 유지·매핑), 신규 enum 신설 금지. (D3) ADR-0011 경로분리 = factory 는 자동매매/서버스캔(L1/L2) 전용, `aiUniverseService`(L3/L4) import 금지·데이터 미혼합(불변식7). (D4) provider 장애 throw 금지 — 필드만 `freshness=STALE|MISSING+providerIssue` 격리·marketSignal 비변환(불변식1·2·4·5·6); `quote` 만 필수(부재→`SymbolDataQuality=MISSING`) 나머지 optional. (D5) **신규 fetch 0·per-scan KIS/KRX quota 순증 0 불변 고정**(불변식9). 입출력 11필드 계약표 + Roadmap(구현=engine-dev 묶음0 skeleton~5 byte-equiv/NONE-간접; 묶음6 universeScanner Stage1 ranking=executionImpact 직접→별도 ADR+shadow A/B 분리). 신규 타입 0(기존 A/B 책임 재확인만). executionImpact=NONE. INDEX 0556→0557 갱신.)

(2026-06-03 기준, 마지막 발급 0555 — ssot-single-funnel-enforcement-constitution. ADR-0555 — SSOT Single-Funnel Enforcement Constitution. 사용자 요청 "헌법을 세워 각 정보가 한 곳으로 모이게 하고 순서대로 패치" 의 거버넌스 기반. 두 번째 SSOT 신설 금지 — 기존 9대 불변식·7대 단일통로·ADR-0519/0525/0526/0527/0528 Canonical 체인을 *확장·강제*만. (a) Information Ownership Registry 헌법 채택(docs/ai/03 §, 13 정보타입 → 단일 소유모듈 코드근거 지도) (b) 순서대로 로드맵 P0(헌법)→P1(정적가드+baseline allowlist)→P2(UnifiedSourceSnapshot factory 구현·배선, 별도 ADR)→P3(universeScanner/marketProgramFlowProvider 우회 제거)→P4(dartProviderSignalSplit mixed 제거)→P5(ShadowPositionRegistry 영속화) — 가드(byte-equiv, 회귀0)가 factory 보다 선행 (c) KIS 공식 api 를 kisClient TR_ID·엔드포인트 스펙 SSOT 로 바인딩(docs/reference/kis-open-trading-api/, TR_ID 교차대조 17건 중 일치13/LEGACY4[order TR_ID 0801/0802/0803/8001 → 공식 0011/0012/0013/0081]) (d) enforcement=정적 가드 매 커밋 강제 + baseline grandfather→burn-down. 문서/ADR 전용 — 런타임 소스 0줄, behavior 0, executionImpact=NONE, ENV 0건.)

(2026-06-03 기준, 마지막 발급 0554 — counterfacture-gate-self-tuning-threshold-pipeline. ADR-0554 — counterfacture-gate self-tuning threshold pipeline. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-02 기준, 마지막 발급 0553 — vts-paper-trading-order-symmetry-and-realmoney-guard. ADR-0553 — VTS(모의) 주문 대칭 게이트 + 실계좌 시동 안전 가드. 2축 정책(실행 ON/OFF × 계좌 real/모의): resolveFacadeOrderExecutionAllowed(orders.ts, flag VTS_PAPER_TRADING_ENABLED default OFF → kisIsReal&&LIVE byte-identical / ON → mode==='LIVE' 면 계좌는 KIS_IS_REAL 라우팅 = 모의 매도 대칭, facade 5경로[매수·매도·손절OCO·익절OCO·취소] 단일 게이트) + resolveAutoTradeStartupGuard(startupExecutionContext, index.ts 인라인 throw 격상 — LIVE+실계좌는 KIS_REAL_MONEY_ACK 필수 하드가드 / LIVE+모의는 VTS_PAPER_TRADING_ENABLED 필수[기존 거부 byte-equivalent 트리거]) + 시동 배너 paperExecutionActive/accountType(REAL_MONEY/PAPER_VTS/NONE) real·모의 구분(LIVE+모의 "주문 OFF" 오표시 정정). 상류 스캔/게이트/유니버스 무관, shadow 학습 ledger always-on 보존(불변식 #2), 매수 shadowMode 디스패처 무수정. 롤백=VTS_PAPER_TRADING_ENABLED/KIS_REAL_MONEY_ACK env 해제 → byte-equivalent. executionImpact: flag OFF NONE / flag ON HIGH[단 KIS_IS_REAL=false 모의계좌라 실제 돈 위험 0].)

(2026-06-02 기준, 마지막 발급 0552 — remove-phantom-krx-lunch-break-session-window. ADR-0552 — remove phantom KRX lunch break session window. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-02 기준, 마지막 발급 0551 — leadershipbridge-intraday-leader-injection-wiring. ADR-0551 — leadershipBridge intraday leader injection wiring. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-02 기준, 마지막 발급 0550 — stage1-risk-on-regime-leader-capture. ADR-0550 — stage1 risk-on regime leader capture. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-06-01 기준, 마지막 발급 0549 — market-rally-search-lens. ADR-0549 — 자동매매 트리(signalScanner/gates/buyPipeline)와 물리적으로 분리된 추천/관측 전용 read-model 레이어 server/trading/recommendation/ 신설. PR-1(Foundation): MarketRallyLens/RecommendationScore 타입 SSOT(recommendationTypes.ts, GateScore 와 별도 타입·별도 척도·역류 금지) + buildMarketRallyLens(marketRallyLens.ts, ENV MARKET_RALLY_LENS_ENABLED default OFF → DISABLED, 조건1[KOSPI 당일+1.5%]·4[외국인5일순매수]·6[20일추세+당일반등] 만 활성, 조건2/3/5/7 입력부재 RALLY_INPUT_NOT_AVAILABLE degrade, providerIssue→RALLY_UNKNOWN_PROVIDER_ISSUE 불변식#6 bullish/bearish 비변환, 새 provider 호출 0건) + UnifiedMacroContext.kospiDayReturn? read-only carry(symbolDataCollector buildMacroContext 1줄, 불변식#3 강화 — 기존 macroState 값 시점고정 복사) + 정적가드 check_rally_lens_isolation.js(recommendation/** 의 buyPipeline/entryEngine/tranche/autoTradeEngine/kisClient/exitEngine/paperEntry/provisionalShadow/counterfactual import fail, validate:all·precommit 등재). live 매매 본체 0줄, ENV 1줄 byte-equivalent 롤백, executionImpact=NONE. PR-2(score/universe)·PR-3(surfacing/telegram)·PR-4(UI)는 후속 patch type.)
(2026-05-31 기준, 마지막 발급 0548 — kis-chk-holiday-opnd-yn-authority. ADR-0548 — KIS 국내휴장일조회 chk-holiday(TR CTCA0903R)의 opnd_yn(개장일여부)을 L1 권위 휴장 출처로 도입. 하드코딩 휴일 테이블 2벌(krxHolidays STATIC + krxTradingCalendar) 의존을 KIS 권위로 격상. 하이브리드 3층 fallback(L1 KIS opnd_yn=N→krxHolidayRepo patch addedBy:'sync' / L3 STATIC 안전망 / 최후 주말). 신규 repo·타입 0(krxHolidayRepo sync 슬롯 예약 재사용, MarketDataMode HOLIDAY_CACHE 기존 존재). 동기 시그니처 보존 + KIS fetch 는 maintenanceJobs cron(연 1~2회, 이미-등록 skip+idempotent)으로 분리 — 공식 "1일 1회 이하" 제약 강제. marketClock classifyMarketDataMode 평일 휴장→HOLIDAY_CACHE 즉시 배선(ADR-0547 일봉 TTL 무수정 정밀화). 순환참조는 경량 leaf(query/holidayCalendar) 동적 import 로 차단. ENV KIS_HOLIDAY_CALENDAR_ENABLED default OFF(sync 잡만 게이트). KIS 실패 시 STATIC fallback(불변식 #6), realDataKisGet 단일 통로, 불변식 #4 보존, executionImpact=NONE. krxTradingCalendar orphan 수렴은 Phase 2.)
(2026-05-31 기준, 마지막 발급 0547 — technical-ohlcv-kis-primary. ADR-0547 — 기술지표(RSI/MACD/볼린저/VCP/MTAS) OHLCV 시계열 1차 출처를 KIS 일봉(L1, FHKST03010100)으로 승격하고 Yahoo chart(L3)를 fallback 강등. 신규 계산 0 — 기존 빌더(fetchKisQuoteFallback/buildExtendedFromKisDaily) 호출 우선순위만 전환(선택지 C, 신규 얇은 라우터 server/screener/adapters/technicalQuoteRouter.ts). ENV KIS_OHLCV_PRIMARY_ENABLED default OFF → byte-equivalent 롤백. Phase 1 = prefetchedContext(정밀/AI 경로)만 적용, 풀스캔(stockScreener/universeScanner)은 Yahoo 우선 유지(Phase 2). 일봉 6h + 휴장 인지 TTL 캐시로 KIS quota 보호. quote(현재가) fallback 라우터 SSOT(KIS_QUOTE>KIS_CACHED>KRX>YAHOO) 와 직교(현재가=FHKST01010100, 시계열=FHKST03010100). 불변식 #4/#6 보존, executionImpact=NONE. 휴장 취약점의 권위 해법 chk-holiday(CTCA0903R)는 도메인 분리로 ADR-0548 후보.)
(2026-05-30 기준, 마지막 발급 0546 — gate1-required-score-ssot. ADR-0546 — Gate1 required score 를 레짐 SSOT(server/trading/gateConfig.ts resolveGate1RequiredScore/getRegimeAwareGate1RequiredScore/LEGACY_GATE1_REQUIRED_SCORE/GATE1_SCORE_SCALE/isGate1RegimeAwareRequiredEnabled)로 단일화. Phase 1 동작 보존 — ENV GATE1_REGIME_AWARE_REQUIRED default OFF → 모든 레짐 70 반환(회귀 0). 8개 포렌식/관측/dry-run 파일(ADR-0476/0481/0482/0484~0488/0491) 하드코딩 70(~20곳, 타입 포함) → LEGACY_GATE1_REQUIRED_SCORE byte-equivalent 치환 + scaleMismatch 판정식 isGate1ScaleMismatch 로 SSOT 추출(중복 제거) + Gate1ScoreAccountingReport legacyRequired/regimeAwareRequired/regimeAwareGap/applied/regimeAwareActive 섀도 병행 로깅(Phase2 forward-outcome 관측용) + 정적 가드 check_gate1_required_score_ssot.js(validate:all·precommit 등재). live 경로(entryEngine/exitEngine/kisClient/autoTradeEngine) 0줄, R3 Sanity Guard 입력 legacy 70 유지, thresholdAutoChanged=false 불변, KIS/KRX quota 0, executionImpact=NONE. true 전환·R3 Sanity 승격·regime 라벨 정합은 Phase 2(PENDING_WIRING 등재).)
(2026-05-30 기준, 마지막 발급 0545 — sectorenergy-last-known-verified-snapshot. ADR-0545 — ADR-0544 후속(PR-2): 휴일/세션닫힘에 직전 verified sector snapshot 을 영속·표시. 신규 persistent repo sectorEnergyVerifiedSnapshotRepo(기존 *Repo.ts atomic tmp+rename 패턴 재사용, 레코드 snapshotId/asOf/tradeDate/verifiedOfficialSectorCount/promotionCoverage/selectedSourceTier) + 어댑터 deriveSectorEnergyCanonicalState write(verified>0 & 세션open 일 때만)·read(세션닫힘 시 sameTradeDate→prevEOD→이전 EOD 우선순위) + resolver 입력 lastKnownSnapshot/canonical state lastKnown 표시필드(sectorIndexVerifyMode=LAST_KNOWN_VALID, lastKnownUsableForLivePromotion=false 타입고정, lastKnownUsableForShadowEvidence=true) 부재 시 reason=SESSION_CLOSED_NO_LAST_KNOWN_SECTOR_SNAPSHOT; ENV SECTOR_ENERGY_LAST_KNOWN_SNAPSHOT_ENABLED default OFF 1줄 byte-equivalent 롤백(미설정 시 write·read 미수행=PR-1 동작), promotionCoveragePass 산식·공식 11섹터 정책·promotion/sectorBoost/strongBuy 게이팅 0줄(당일 verified=0 → false 유지, last-known 표시+shadowEvidence 전용 live promotion 절대 비활성), KIS/KRX quota 0, 불변식 #4/#6/#7/#8 보존; executionImpact=NONE.)
(2026-05-30 기준, 마지막 발급 0544 — sectorenergy-session-not-verifiable-display-isolation. ADR-0544 — 휴일/비장중 KIS index verify-skip 을 소스 결함(MISSING/OFFICIAL_SECTOR_SOURCE_MISSING) 과 표시상 분리: resolver 입력 옵셔널 sessionVerifiability + 어댑터(deriveSectorEnergyCanonicalState) master.reasonCodes(SECTOR_INDEX_MARKET_CLOSED/HOLIDAY_NO_SESSION_OBSERVE_ONLY)→입력 매핑, sessionClosed&&verifySkipped&&verified=0 일 때만 dataQuality=SESSION_NOT_VERIFIABLE/reason=SECTOR_INDEX_VERIFY_SKIPPED_SESSION_CLOSED/status=OBSERVE_ONLY_SESSION_CLOSED/sectorIndexVerifyMode 재분류(장중 verify-0 은 MISSING 유지) + KisIndexQuoteClientStatus session-skip vs AUTH_NOT_READY 분리(휴일 providerIssue=false) + operatorAction REPAIR_SECTOR_INDEX_MASTER P1→P3 Observe(장중 P1 유지) + Bottleneck Truth SectorEnergy Health 블록; ENV SECTOR_ENERGY_SESSION_ISOLATION_ADR0544_DISABLED 1줄 byte-equivalent 롤백, promotion/sectorBoost/strongBuy false→true 0(promotionCoveragePass 산식·공식 11섹터 정책 무접촉), 불변식 #4/#6/#7/#8 보존; executionImpact=NONE.)
(2026-05-29 기준, 마지막 발급 0543 — macro-shortselling-kis-l1-proxy-fallback. ADR-0543 — KRX OTP 구조적 차단으로 정체된 shortSelling 신선도를 KIS 공식 daily-short-sale(FHPST04830000) 시장-프록시 ETF(069500, 미반환 시 005930 재시도) 비중(ssts_tr_pbmn_rlim, L1)으로 보강: fetchKrxShortSelling 폴백 체인 KRX_OTP 뒤·KIS_ESTIMATE 앞에 KIS_PROXY 삽입(kisClient 단일통로 fetchKisDailyShortSale 재사용, 수정 0줄) + ShortSellingSource union 에 KIS_PROXY/CACHE 정식 추가(shortBackfill as any 해소) + 소비측 L4 격리(KIS_ESTIMATE/CACHE 는 R5 8% 임계 평가 제외, ratio 캡); ENV SHORT_SELLING_KIS_PROXY_FALLBACK default OFF 1줄 byte-equivalent·즉시 롤백, regimeBridge.base.ts ?? 5 무변경, KRX quota 0·KIS quota +1콜/일, 불변식 #1/#2/#6/#7/#9 보존; executionImpact=NONE(SHADOW_ONLY, live 전환 시 R5 임계 입력 변경 미실현).)
(2026-05-29 기준, 마지막 발급 0542 — investor-flow-supply-router-kis-output-bucket-realign. ADR-0542 — investorFlowProviderRouterAdr0477 KIS coverage 2/11 정정: KIS investor-trade-by-stock-daily output1/output2 버킷 합성(synthesizeInvestorFlowAcrossBuckets, 외국인/기관이 버킷에 갈라진 종목 복구, 기대 7~10/11) + registry outputBuckets 드리프트 정정 + priority KIS-first 명시 재정렬(byte-equivalent) + KIS_ONLY_TRACE 진단; ENV KIS_INVESTOR_OUTPUT_BUCKET_FIX default ON 1줄 revert, KIS quota 0, L1 한정(L4 estimate 는 ADR-0543 분리), 불변식 #6/#7 보존; executionImpact=NONE(SHADOW_ONLY).)
(2026-05-29 기준, 마지막 발급 0541 — positive-score-starvation-audit-relocation. ADR-0541 — ADR-0467 starvation audit 누적을 per-symbol intraday re-check(kisIntradayCorrection.ts:125)에서 persistScanResults(entryFilterDecomposition 직후)로 이전, canonical minSignalScoreTrace.components 공급으로 CORE_SIGNAL(PRICE_MOMENTUM 등) 오귀속·OTHER_POSITIVE leak 정정; 모집단 buyList subset→candidateSet 전수, report 빌드 순서 재배치; 소비 로직(gate1PositiveScoreStarvation.ts) 무변경, 신규 ENV 0건, 9대 불변식 무위반; executionImpact=NONE(진단 전용).)
(2026-05-29 기준, 마지막 발급 0540 — macro-shortselling-trading-day-aware-freshness. ADR-0540 — macroDataHealthRouter shortSelling freshness 36h flat → 거래일-aware(ADR-0190/0483 정합, tradingDayDistance 거리≤2 FRESH·3+ outage STALE 유지), 주말/T+1 정상지연 STALE 오판 제거, 나머지 7소스 byte-equivalent, 신규 ENV 0건, 불변식 #6 보존(confidence/ExecutionPermission 만 영향·stale→tradable 승격 아님); executionImpact=NONE(SHADOW_ONLY).)
(2026-05-29 기준, 마지막 발급 0539 — r6-recovery-regime-linked-cooldown-fasttrack. ADR-0539 — R6 Recovery Regime-Linked Cooldown Fast-Track (내부설정 TradingSettings.r6RecoveryFastTrack.enabled 기본 ON — Railway ENV 아님, 설정 API/UI 토글; evidenceComplete+confirmations 유지 cooldown 시간벽만 우회: rawR3_EARLY↑ + not-stale + shock-latch 비활성, 불변식 #6 정합, FSS/engine 차단 별도); 의도적 활성화(NOT byte-equivalent), 롤백=설정 enabled=false.)

(2026-05-27 기준, 마지막 발급 0538 — scanblockers-message-section-decomposition. ADR-0538 — formatScanBlockersMessage 섹션 빌더 분해 (cc 131→≤25, scanBlockersMessageSections.ts 신규, display-only byte-equivalent, 테스트 335-pass 게이트); executionImpact=NONE.)
(2026-05-27 기준, 마지막 발급 0537 — kisclient-query-decomposition. ADR-0537 — kisClient/query.ts 분해 (sectorIndex + 순수 helpers leaf 모듈 추출, byte-equivalent, baseline 해소); executionImpact=NONE.)
(2026-05-27 기준, 마지막 발급 0536 — yahoo-quotesummary-per-eps-opportunistic-enrichment. ADR-0536 — Yahoo quoteSummary 기반 PER/EPS Opportunistic Enrichment (PENDING_WIRING C18, Proposed/설계 only, Naver→Yahoo quoteSummary fallback + perSource 출처 + ENV YAHOO_QUOTE_SUMMARY_PER_ENABLED default OFF; executionImpact=NONE).)
(2026-05-27 기준, 마지막 발급 0535 — regime-authority-hierarchy-decision-context-ssot. ADR-0535 — regime-authority-hierarchy-decision-context-ssot. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-26 기준, 마지막 발급 0534 — sector-energy-canonical-baseline-lock. ADR-0534 — SectorEnergy Canonical Baseline Lock(SectorEnergyCanonicalResolver + 공식 11개 coverage 단일 SSOT, KIS basket/grouped/old coverage 는 diagnosticOnly, 조선/방산/원자력/이차전지 는 themeTag; executionImpact=NONE).)

(2026-05-26 기준, 마지막 발급 0533 — unified-forward-outcome-labeling-bus. ADR-0533 — Unified Forward Outcome Labeling Bus for Gate3/Gate1/Near-Miss/Counterfactual evidence; executionImpact=NONE.)

(2026-05-26 기준, 마지막 발급 0532 — kis-finance-fundamentals-migration. ADR-0532 — KIS Finance Fundamentals Migration. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-26 기준, 마지막 발급 0531 — gate0-regime-ssot-single-source. ADR-0531 — Gate0 Regime SSOT 단일화(canonical=resolveRegimeSnapshot, legacy getLiveRegime/transitionState는 deprecated 라벨; decision 소비처 마이그레이션은 단계적 rollout).)
(2026-05-26 기준, 마지막 발급 0530 — vkospi-stale-implausible-trust-guard. ADR-0530 — VKOSPI Stale/Implausible Trust Guard (불변식 #6, R6-recovery vkospiOk 격리). 0531(gate0-regime-ssot-single-source)은 후속 PR 예약.)
(2026-05-26 기준, 마지막 발급 0529 — dart-financials-canonical-inclusion-into-sourcesnapshot. ADR-0529 — DART Financials Canonical Inclusion into SourceSnapshot. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-26 기준, 마지막 발급 0528 — railway-decision-log-correlation-standard. ADR-0528 — railway decision log correlation standard. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-25 기준, 마지막 발급 0527 — executionpermissionresolution-unification-and-positionpolicydecision-ssot. ADR-0527 — ExecutionPermissionResolution Unification and PositionPolicyDecision SSOT. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-25 기준, 마지막 발급 0526 — candidategateevaluationview-ssot. ADR-0526 — CandidateGateEvaluationView SSOT. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-25 기준, 마지막 발급 0525 — gate-debug-raw-canonical-summary-rebinding. ADR-0525 — Gate Debug Raw Canonical Summary Rebinding. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-25 기준, 마지막 발급 0524 — minimumsignalscoretrace-types-and-scoring-decomposition. ADR-0524 — minimumSignalScoreTrace types and scoring decomposition. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-25 기준, 마지막 발급 0523 — gate2externaldataprovider-types-and-helpers-decomposition. ADR-0523 — gate2ExternalDataProvider types and helpers decomposition. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-25 기준, 마지막 발급 0522 — regimelearningbank-analytics-helper-decomposition. ADR-0522 — regimeLearningBank analytics helper decomposition. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)

(2026-05-25 기준, 마지막 발급 0521 — sectorenergymastersupplyunknownpolicyadr0488-decomposition. ADR-0521 — sectorEnergyMasterSupplyUnknownPolicyAdr0488 decomposition. Generated by scripts/create_adr.js; fill Context/Decision/Consequences before merge.)


마지막 발급: 0520 — gate1-scoring-alignment-dry-run-gating (2026-05-25).

> 본 섹션은 *다음 발급 번호 + 마지막 발급 한 줄* 만 유지한다. ADR 본문 요약·근거는 각 ADR 파일(`docs/adr/NNNN-*.md`)이 SSOT — 여기에 누적 금지 (토큰 효율, §발급 룰 4 "한 줄").

## 알려진 충돌 (Known Conflicts)

머지된 ADR 들의 번호 중복. 향후 신규 발급 시 본 인덱스 검증으로 영구 차단. 기존 ADR 번호 변경은 git diff·외부 참조 무결성 보호를 위해 *금지* — 본 표가 단일 진실 출처.

**별칭 정책 (ADR-0159, 2026-05-02 도입)** — 충돌 ADR 인용 시 *별칭 사용 권장* (예: `ADR-0028a` exitEngine 분해 / `ADR-0028b` rejection-universe-tracker / `ADR-0028c` ui-redesign-p0). 비충돌 ADR 은 기존 형식 그대로 (예: `ADR-0085`). 별칭 부여 기준 — 동일 그룹 내 PR 머지 시점 오름차순 (a → b → c). 강제 검증 default OFF (회귀 위험 격리, 6개월 운영 후 활성화 검토).

| 번호 | 별칭 | 파일 | 의도 도메인 | PR / 머지 시점 | 비고 |
|------|------|------|-------------|----------------|------|
| 0028 | **0028a** | `0028-exitEngine-decomposition.md` | exitEngine 분해 (P2) | PR-53 (2026-04-26) | 절대 규칙 #6 (1500줄) |
| 0028 | **0028b** | `0028-rejection-universe-tracker.md` | 거절 종목 사후 추적 | PR-L (2026-04-26) | 자기학습 시리즈 |
| 0028 | **0028c** | `0028-ui-redesign-p0-banners-badges-cards.md` | UI P0-A 배너/배지/카드 | PR-A (2026-04-26) | UI 재설계 |
| 0029 | **0029a** | `0029-condition-source-tier-and-recommendation-history.md` | UI P0-B 조건 출처 + 이력 | PR-B (2026-04-26) | UI 재설계 |
| 0029 | **0029b** | `0029-counterfactual-twin-portfolio.md` | Twin Portfolio Ranking | PR-M (2026-04-26) | 자기학습 시리즈 |
| 0029 | **0029c** | `0029-stockScreener-decomposition.md` | stockScreener 분해 (P1) | PR-55 (2026-04-26) | 절대 규칙 #6 |
| 0030 | **0030a** | `0030-latent-signal-scorer.md` | VCP + Catalyst 사전 신호 | PR-N (2026-04-26) | 자기학습 시리즈 |
| 0030 | **0030b** | `0030-price-alert-watcher.md` | UI P0-C Web Notification | PR-C (2026-04-26) | UI 재설계 |
| 0030 | **0030c** | `0030-signalScanner-entry-gates-phase-b.md` | EntryGate Chain Phase B | PR-57 (2026-04-26) | signalScanner 분해 |
| 0031 | **0031a** | `0031-last-trigger-enemy-tranche-cards.md` | Last Trigger + Enemy Card | PR-D (2026-04-26) | UI 재설계 |
| 0031 | **0031b** | `0031-order-type-optimizer.md` | 슬리피지 학습 + 주문 타입 | PR-O (2026-04-26) | 자기학습 시리즈 |
| 0031 | **0031c** | `0031-signalScanner-revalidation-and-sizing-patterns.md` | RevalidationStep 패턴 | PR-59 (2026-04-26) | signalScanner 분해 |
| 0032 | **0032a** | `0032-sector-rotation-heatmap.md` | UI P1-E 섹터 히트맵 | PR-E (2026-04-26) | UI 재설계 |
| 0032 | **0032b** | `0032-self-learning-series-overview.md` | 자기학습 시리즈 통합 SSOT | PR-P (2026-04-26) | 자기학습 시리즈 |
| 0067 | **0067a** | `0067-multi-timeframe-confluence-gate.md` | MTF Confluence Gate | PR-Q (2026-04-26) | EntryGate Phase B |
| 0067 | **0067b** | `0067-marketoverview-boundary-guard.md` | MarketOverview boundary lint | PR-α 후속 (2026-04-27) | 데이터 안정성 |
| 0068 | **0068a** | `0068-macrostate-stale-block.md` | macroState stale 차단 | PR-α 후속 (2026-04-27) | 데이터 안정성 |
| 0068 | **0068b** | `0068-shadow-learning-hooks-wiring.md` | Rejection + Twin wiring | PR-R (2026-04-28) | 자기학습 시리즈 |
| 0124 | **0124a** | `0124-regime-coverage-suggest-false-positive.md` | regimeCoverage 표본 임계 | (날짜 추가 필요) | 학습 시그널 정합 |
| 0124 | **0124b** | `0124-telegram-reports-be-visibility.md` | 텔레그램 리포트 BE 표기 | PR-ADR-0124 (2026-04-30) | BE 정합 |
| 0146 | **0146a** | `0146-pr-pace-audit-rule.md` | 10-PR audit 룰 + 자가 review 체크리스트 | PR-Governance-3 (2026-05-01) | 거버넌스 |
| 0146 | **0146b** | `0146-telegram-sanity-unlock-cmd.md` | `/sanity` 텔레그램 명령 R3 Block 즉시 해제 | (2026-05-03) | 운영 명령어 |
| 0147 | **0147a** | `0147-kis-token-disk-persistence.md` | KIS 토큰 디스크 영속 (재부팅 OAuth2 차단) | (2026-05-01) | 인프라 |
| 0147 | **0147b** | `0147-signalScanner-orchestration-migration.md` | signalScanner Phase 3 6단계 오케스트레이터 승격 | (2026-05-03) | 리팩토링 |
| 0168 | **0168a** | `0168-kelly-clamp-ssot.md` | Kelly clamp 수치 정책 SSOT | PR-Kelly-Clamp-SSOT (2026-05-02) | 사이징 |
| 0168 | **0168b** | `0168-emergency-data-quality-circuit-breaker.md` | Emergency Data Quality Circuit Breaker | (2026-05-03~05-05) | 데이터 안전 |
| 0502 | **0502a** | `0502-kis-official-investor-flow-promotion.md` | KIS 공식 투자자 흐름 evidence 라우터 wiring | 6867ed3 (2026-05-11 23:05) | 수급 데이터 |
| 0502 | **0502b** | `0502-kis-official-global-fallback-until-krx-recovery.md` | KIS 공식 가격 fresh 시 Yahoo 진단 강등 | 70c409b (2026-05-11 23:39) | 데이터 안정성 |
| 0502 | **0502c** | `0502-krxclient-decomposition.md` | krxClient 분해 (ACMA 1500줄 한계 해소) | PR-Refactor-krxClient (2026-05-12) | 절대 규칙 #6 (1500줄) |

**충돌 그룹 12개 / 충돌 ADR 29개 (별칭 29건 부여, ADR-0159)**. 향후 신규 발급은 §"다음 발급" 번호 사용 → 충돌 0건 유지.

## 누락 (Gap)

미사용 번호 — 발급 실수·rebase 충돌 회피 누락. *재사용 금지* (git history 추적성 보호).

| 번호 | 사유 (추정) |
|------|-------------|
| 0062 | rebase 충돌 회피 (main #380 ADR-0061 vs 본 시리즈 0064~0066 그룹 재할당) |
| 0063 | 동일 |
| 0089 | 누락 원인 불명 (후속 audit 가능) |
| 0105 | UI Phase 시리즈 stack rebase 시 누락 |
| 0106 | 동일 |
| 0143 | KIS GitHub 검증 PR rebase 시 누락 (0144/0145 그룹) |
| 0196~0210 | 세션 2026-05-06 fast iteration — commit message 가 0211 부터 시작, INDEX.md 등재 안 됨 |
| 0212~0220 | 동일 (0211 다음 0221 점프) |
| 0222~0230 | 동일 (0221 다음 0231 점프) |
| 0232~0233 | 동일 (0231 다음 0234) |
| 0236 | 동일 (0235 다음 0237) |
| 0238~0240 | 동일 (0237 다음 0241) |
| 0243~0244 | 동일 (0242 다음 0245) |
| 0246~0247 | 동일 (0245 다음 0248) |
| 0253 | **포기 결정** — Yahoo→KIS 시계열 합성 (회귀 위험 큼, DECIDED_NOT_WIRING) |
| 0254 | **포기 결정** — 점심 회로 절전 (ADR-0237 와 책임 중복, DECIDED_NOT_WIRING) |
| 0257 | 세션 fast iteration — commit message 가 0258 점프 |

총 누락 47개 (기존 6 + 세션 2026-05-06 41). 번호 재사용 금지 (git history 추적성 보호).

## 전체 인덱스

| 번호 | 제목 | 도메인 |
| 0620 | Provisional Shadow 측정 기준가(entryPrice) 배선 — `/shadow_provisional` 50건 전부 INSUFFICIENT_DATA(observed 0)의 근본 수리(신호 결함 아닌 측정 기준가 누락). ProvisionalShadowLedgerEntry entryPrice 필드 부재→buildEntry 미기록→성과 리포트가 (entry as {entryPrice?}).entryPrice undefined→전 horizon DATA_UNAVAILABLE. 기록 호출부(provisionalShadowLane.ts→buyListLoop.ts:446)에 currentPrice(KIS 게이트 가격·실진입 동일 소스 ADR-0561 정합) 스코프 존재·신규 fetch 0. 타입 pin: entryPrice?:number + entryPriceSource?:'KIS_CURRENT'|'SCAN_SNAPSHOT'(ADR-0431 source 라벨 동형) 3경계 additive(ledger·candidate·derive input). 로직: derive positive-finite carry→buildEntry stamp→provisionalShadowLaneDerive(currentPrice 인자)→buyListLoop 전달. 하위호환 기존 50건 미설정→INSUFFICIENT 유지(소급 0). liveAllowed:false 불변·executionImpact NONE·9대 불변식 0줄·provisional/counterfactual/일반 shadow ledger 물리 분리 유지·#6(결손=측정 보류≠bearish)·#7(L1 측정 기준가·매매 직접 미사용) | learning / persistence+signalScanner |
| 0618 | 주도주 소스 일일 신선화 — ADR-0617 이 보존하는 주도주(LEADER_SOURCES=FOREIGN/INST/MARKET_CAP)가 dynamicUniverseExpander 캐시(runDynamicUniverseExpansion 주 1회 토 09:00 + 14일 TTL)에서 와 **최대 2주 묵은 주도주** 주입 위험(외인·기관 수급 매일 회전→stale leader). 재사용 seam expandOnEmpty(랭킹 발굴 getShadowSafeRanking×RANKING_KEYS·5분 캐시·allSettled)를 LEADER 매핑분만 매 거래일 돌려 신선화(신규 fetch 경로 0). 신규 runLeaderUniverseDailyRefresh(market-cap/institutional-net-buy/volume[외인 근사] 3키만·fluctuation/large-volume/short-balance 미호출=주간 유지)·단축 TTL 3일(stale 자동 만료)·공통 헬퍼 추출(expandOnEmpty byte-identical 보존)·Telegram 무음. 신규 cron KST 08:10(스캔 전·TRADING_DAY_ONLY·flag-gated 단락). ENV LEADER_DAILY_REFRESH_ENABLED default OFF byte-identical(OFF→cron no-op·주간만). TTL/중복 정합: per-entry expiresAt 혼재 안전·같은 code 일일 upsert(연장)+source 재기입·주간 비주도 무영향·expandOnEmpty skip 과 의도적 분기(refreshExisting). quota 일일 LEADER 3키(주당 +15·5분 캐시 흡수·KIS Primary 0561·KRX/Yahoo 0). executionImpact OFF=NONE / ON=신선도 변경(0617 도 ON 이면 보존 대상 갱신·둘 다 default OFF·SHADOW_ONLY 출하 안전). 불변식 #1/#3/#4/#7/#9 보존(정밀 외인 TR·일중 재갱신 후속 ADR) | screener / scheduler |
| 0617 | 주도주 Stage1 보존 — "외인·기관 순매수 대형 주도주 발굴"은 이미 가동 중(dynamicUniverseExpander source FOREIGN/INST/MARKET_CAP)이나 universeScanner stage1QuantFilter 3중 무력화(① scanUniverse 항상 full master → expanded 주도주 폐기 ② source 태그 미탑재 ③ top-60 점수컷 소형주 편향 탈락). 처방=source 태그 Stage1 carry + top-60 점수컷에서 주도주 강제 보존(union·중복제거). 주도주=FOREIGN_NET_BUY/INST_NET_BUY/MARKET_CAP만(SHORT_HEAVY 반대 신호 보존 금지)·**Stage1 품질 관문(price>0·tradable·관리종목·OVERHEAT·OVEREXTENDED) 통과 필수·점수-랭크 컷만 면제**(불변식 #7). 신규 모듈 leaderUniverseInjectionAdr0617.ts·CandidateStock.source? additive(DynamicStock['source'] 재사용·두 번째 enum 0)·dynamicUniverseExpander getExpandedUniverseSourceMap getter·ledger(ADR-0614 패턴·preservedCount=드롭됐을 수+source 분포)·paths.ts 1줄. ENV LEADER_UNIVERSE_INJECTION_ENABLED default OFF byte-identical(:388-390/:424-427 현행 동치)·신규 fetch 0(cron 캐시 재사용). executionImpact OFF=NONE / ON=EXECUTION_ADJACENT(유니버스 변경·SHADOW_ONLY 출하 안전). 불변식 #3/#7/#9 보존(후속 ADR 발굴 신선도·보존 확대·Gate 승격) | policy / screener |
| 0616 | 유니버스 구성 편향 per-scan 관측 전용 ledger — 발굴 코스닥 소형주/낙폭반등 편향(디버전스 장 코스피 대형 주도주 누락·후보 RS −8.97%)을 편향 교정 전 fetch-0·OFF-safe 로 정량화. per-scan aggregate: ① 시장 구성(getStockByCode market KOSPI/KOSDAQ/기타·로컬 마스터 fetch 0) ② leader/laggard(RS=quote.return20d−kospi20dReturn 둘 다 %·calcStage1Score RS 정의 재사용·두 번째 공식 0·RS>=0 leader/<0 laggard·avg/median·벤치마크 null→skip) ③ 시총 tier(listedShares×price LARGE 3조/MID 1조/SMALL·결손→skip 가짜 시총 0 불변식 #6·전원 불가 null SKIP) ④ topLaggardCodes N. 신규 모듈 universeCompositionBiasObservationAdr0616.ts(getStockByCode 만 경유·신규 fetch 0)·ledger(ADR-0614 패턴 atomic+FIFO 60스캔+손상 fallback+scanDateKey upsert·단일 배열)·paths.ts UNIVERSE_COMPOSITION_BIAS_LEDGER_FILE 물리 분리. stamp universeScanner Stage3 루프 이후 1회(aggregate)·try/catch 격리. ENV UNIVERSE_COMPOSITION_BIAS_OBSERVATION_ENABLED default OFF byte-equivalent. 발굴/Stage1 필터/정렬/Gate score/주문/autoTradeEngine/kisClient/SourceSnapshot 0줄(관측 전용·후속 ADR 발굴 편향 교정) | policy / screener |
| 0615 | 뉴스시차 진입윈도우 관측 전용 stamp — newsLagBayesian getOptimalEntryWindow(dead, /news_patterns 표시만)를 후보 선정 시점에 surface. 각 후보 sector 의 활성 NewsSupplyRecord(미완료)×lag posterior 조인→진입 윈도우 PRE/IN/PAST 분류·stamp. sector 단위 조인으로 per-stock newsType 부재 정직 우회(candidate.sector↔record.sector, computeEtfSectorBoost 선례)·per-stock 귀속 후속 ADR. 신규 모듈 newsLagEntryWindowObservationAdr0615.ts(loadNewsSupplyRecords·getOptimalEntryWindow 기존 export 만 경유·내부 store 0). 분류 ci95 채택(이미 노출·μ±σ 대비 두 번째 공식 0)·now 주입(테스트 결정성)·graceful skip(n<3/손상 detectedAt/미매칭, 불변식 #6). stamp universeScanner.ts:696-719(ADR-0614 인접·try/catch 격리)·CandidateStock.newsLagEntryWindow? additive·ScanSummary 집계 1필드. ledger 불필요(입력 영속 존재·후속 ADR). ENV NEWS_LAG_ENTRY_WINDOW_OBSERVATION_ENABLED default OFF byte-equivalent. Gate score/주문/autoTradeEngine/kisClient/SourceSnapshot/entryTimingSignal 0줄·신규 fetch 0(관측 전용·후속 ADR Gate 승격) | policy / learning+screener |
| 0614 | 외국인/기관 연속 순매수 관측 전용 시계열 ledger — supply_confluence 가 당일 동반 순매수만 보고 버리는 연속(N영업일)/누적 신호를 신규 ledger 로 누적. c.kisFlow piggyback(신규 fetch 0·KIS/KRX quota 0)·per-stock rolling window(10d FIFO)+글로벌 캡 600·investorFlowSemanticAvailability 단일통로(OK만 기록·결손≠신호 불변식 #6)·dateKey×stockCode upsert last-write-wins·atomic write. ENV CONSECUTIVE_NETBUY_OBSERVATION_ENABLED default OFF byte-equivalent. Gate score/주문/autoTradeEngine/kisClient/SourceSnapshot 0줄(관측 전용·후속 ADR Gate 승격) | policy / signalScanner |
| 0613 | Gate1 천장 배선 OFF-by-default 승격 — RS percentile 입력·BREAKOUT_STRUCTURE OHLCV·positive max→100 정규화 3종을 LIVE minimum-signal scorer 에 연결, GATE1_POSITIVE_CEILING_WIRING_ENABLED default OFF byte-equivalent + 관측 ledger hypothetical delta stamp. dry-run 함수 재사용(두 번째 공식 0)·requiredScore=70 무변경·ADR-0471 freeze 정합 | policy / signalScanner |
| 0612 | 유니버스 선정 RS 게이트 — 발굴 MOMENTUM momentum20d≥시장 필터(graceful fallback) + Stage1 0-floor RS 보너스(clamp(return20d−KOSPI20d,0,3)). 강세장 laggard 유입 차단, no-op 함정 회피(필터·비선형), default OFF·신규 fetch 0·requiredScore 무변경 | policy / services+screener |
| 0611 | Gate1 SECTOR_RELATIVE_STRENGTH 재활성 — ADR-0467 advisory 주차로 봉인된 8점 capacity(108 vs configured 116) 복원, 섹터상대수익만 소비(RS 이중계상 회피), default OFF·requiredScore=70 무변경 | policy / signalScanner |
| 0610 | counterfactualOutcomeBoard format-layer extraction — 1,499~1,500줄 ACMA 한계 고착(R1 net-0·다음 변경 영구 차단) 선제 분해. format(표시) 함수 10종(formatCounterfactual* SafetyChecks/BoardSummary/Gate1/Gate2/Gate3/Missed/Today/Review/Debug/CommandReply)+표시 전용 5헬퍼(pct/num/safetyLine/distributionLine/formatBandRows, grep 확인 build 미사용)+CounterfactualCommandMode/resolveCounterfactualCommandMode 를 신규 counterfactualOutcomeBoardFormat.ts(244줄)로 순수 이동, 본체(1,500→1,279줄)는 build 집계 함수+타입 잔류 + format 심볼 re-export(호출처 import 경로 무변경 byte-equivalent). 순환 회피: format→board 는 import type(런타임 엣지 0)·board→format 값 re-export 1방향. 소비처 3곳 무수정 green. LIVE 무관(learning board 표시층)·executionImpact=NONE·9대 불변식 #1/#2 보존·R1/ADR-0609 무영향. 계보 ADR-0596/0524/0521/0523 | refactor / learning / complexity |
| 0609 | Gate1 상수블록 eligibility shadow 판정(Phase 0 관측 전용·LIVE 점수 0줄) — 신규 순수 SSOT gate1EligibilityShadowScoreAdr0609.ts buildGate1EligibilityShadowReportAdr0609/judgeGate1EligibilityAdr0609. 입력 ADR-0597 산출물(marketBlockScore·totalPercentile)+상수 컴포넌트, 산출 eligible(WATCHLIST>0&&REGIME>=0&&INVESTOR>=0, 불변식 #6 결손→보류 보수 통과·bearish 변환 0)·marketOnlyPassed(>=42 null→null)·percentilePassed(>=70). 잠정 임계 GATE1_ELIGIBILITY_SHADOW_THRESHOLDS SSOT(Phase 1 보정). 배선 ADR-0597 동형 ScanSummary 집계+ledger stamp(try/catch 격리). 소비처 0(buyList/Gate/entry/Kelly 미소비, 3중 분리). LIVE byte-equivalent minimumSignalScoreTrace D1-D4·gateConfig 70·0fetch | gate1 / scoring-shadow / observation-only |
| 0608 | SHADOW 전용 regime-aware Gate1 진입 임계 — 신규 순수 SSOT gate1ShadowEntryThreshold.ts resolveEntryMinGateScore({regime,isShadow}) = ENV ON&&isShadow→getEffectiveGateThreshold(regime) / 그 외→getMinGateScore byte-equivalent. 진입 차단 단일 지점 evaluateEntryRevalidation(entryEngine.ts:370, minGate 호출자 주입) seam=entryRevalidationStep.ts:67. SHADOW paper fill 도 LIVE 와 동일 임계 공유 확인(분기=stockShadowMode). 타입 ServerShadowTrade+클라 미러 additive entryThresholdMode?:'LEGACY'\|'REGIME_AWARE_SHADOW'. ENV GATE1_REGIME_AWARE_SHADOW_ENTRY_ENABLED default OFF=byte-equivalent, LIVE 본체 0줄·KIS 0 | trading / entry-threshold / flag-gated |
| 0607 | LIVE 모드 SHADOW 메시지 CH4(JOURNAL) 격리 — 신규 순수 SSOT shadowQuietRouting.ts resolveShadowChannelRoute 3중 AND(LIVE_SHADOW_QUIET_ENABLED·getExecutionMode LIVE·isShadow)→JOURNAL. 적용 routeTelegramEvent SIGNAL 분기 + channelBuySignalEmitted(mode-aware). channelSellSignal/개인DM 은 scope 제외(follow-up). ENV default OFF=byte-equivalent, 현 SHADOW 운영 영향 0·LIVE 본체 0줄·KIS 0 | telegram / channel-routing / flag-gated |
| 0606 | 러너(Runner) 모드 — 추세추종 익절(전량 트랜치 스킵 + 넓은 트레일링 resolveRunnerTrailPct floor 0.15) + 슬롯 카운트 제외(computeSlotConsumption 단일 SSOT, runnerCount additive). 승격=마지막 LIMIT 트랜치 OR +18%(ENV). 신규 순수 SSOT runnerPolicy.ts + ServerShadowTrade 옵셔널 5필드(클라 미러 동기). ENV 2게이트 독립 default OFF=byte-equivalent, SHADOW 전용·LIVE 본체 0줄 | trading / exit-engine+slot / flag-gated |
| 0605 | 미국 선행지수 연결 잔여 이행 — fast-upgrade 보조 AND(④ SPX 야간 비급락, default OFF) + SOX 수집·Gate2 반도체 proxy 축(stockVsSector 단독 주입 62 캡, default OFF) + /us_overnight NDX 밴드 분리(표시 전용) | policy / regime+gate2+learning |
| 0604 | 미국 야간↔KOSPI stratify 관측(2단계, /us_overnight 5밴드 실측·게이트 미소비) + 야간 급락 개장 전 보수 강등(3단계 — bias -12·R6 회복 가속 차단, default OFF, usOvernightBoost 하방 대칭). 활성화 기준=밴드 실측 n>=10 | policy / learning+regime |
| 0603 | 미국 지수 KIS-primary 승격 — SPX Yahoo→KIS 해외지수 일봉(FHKST03030100, 일캐시·fallback 보존·소비식 0줄) + NDX 관측 수집. 미국 선행지수↔한국 레짐 연결 1단계, 2/3단계(야간 stratify→게이트 반영)는 로드맵 | policy / kisClient+marketDataRefresh |
| 0602 | 동일 섹터 강자 교체 phased 도입 — Phase 0 관측(proposeSameSectorReplacement+가드 차단 시 교체 후보 1줄 표기, 실집행 0, default ON). 교체 엔진 미배선+동일섹터 금지 이중 갭의 출구를 한도 해제 대신 데이터 검증으로. Phase 1 shadow 집행/Phase 2 live 는 설계만 | policy / trading+entryGates |
| 0601 | Gate2 Supply 축 KIS 네이티브 hydration — 기존 단일통로 investor-flow evidence 를 결손 후보 전체로 확장 주입(일중 캐시·스캔당 상한 16·실패 격리, default ON). L1>시맨틱 fallback>missing 우선순위. D4 KOSDAQ 마스터 업종코드는 Phase 2 | policy / signalScanner+supply |
| 0600 | Gate2 Supply/Sector 결손 축 보수 fallback — Gate1 시맨틱 수급(78캡)·스캔 동종군 상대수익(62상한) DEGRADED/ADVISORY 소비, BULLISH 민팅 금지·fetch 0·default ON(진단 차선 한정). KIS 네이티브(inquire-investor·KOSDAQ 마스터 업종코드) 1순위 후속 ADR-0601 로드맵 명시 | policy / quant |
| 0599 | Gate2 confluence 가용 축 비례 요구 — 결손 축이 STRONG/WEAK 절대 개수 조건(bullish>=3)을 역설적으로 강화하는 갭 보정(ceil 60%·<=3), flag default OFF + dry-run 상시 관측(proportionalDryRun). 점수 임계·BULLISH 컷 불변, ADR-0416 정합 완성. 계보 Gate2 추적 20260611/0416/0519 | policy / quant |
| 0598 | 과열 추격 가드 갭 봉인 — G1 FOMO 쿨다운 진입 시점 재평가(REGRET_ENTRY_REEVALUATION_ENABLED) + G2 Gate3 ma20 결손 시 BREAKOUT_CONFIRMED 보수화(GATE3_EXTENSION_GUARD_STRICT_ENABLED). 둘 다 default OFF byte-equivalent·observe 로그, live 만 보수화·shadow 무영향(불변식 #8). 계보 유진테크 추적 20260611/0030/0578 | policy / signalScanner+quant |
| 0597 | Gate1 횡단면 percentile shadow 보조점수 — canonical 점수(ADR-0541)에서 스캔 내 midrank 백분위 + 시장신호 블록 합(상수블록 제외) 산출, ScanSummary+ADR-0476 ledger 행 stamp (관측 전용, Gate 판정 미소비, fetch 0). 절대 vs 횡단면 임계 논쟁의 forward 증거 수집 개시. 계보 리뷰 20260611/0541/0546/0476 | policy / signalScanner |
| 0596 | minimumSignalScoreTrace component decomposition — 점수 컴포넌트 계산(scorer 5종)·trace 경로 해석·집계 리포트를 minimumSignalScoreTrace/ 하위 3모듈(traceFieldResolver/componentScorers/decompositionReport)로 순수 이동, 본체 1,500→~690줄(조립 4종 잔류). re-export byte-equivalent·정책 0변경·advisory-only. 가드 갱신 0건(requiredScore 단언 본체 잔류). 계보 ADR-0524/0466/0594 | refactor / signalScanner |
| 0595 | marketDataRefresh section-module decomposition — refresh*Section 도메인 그룹(지수거시/수급신용/프로그램매매/섹터에너지)+공유 observability 를 marketDataRefresh/ 하위 5모듈로 순수 이동, 본체 1,497→~375줄(오케스트레이터+MERGE 영속+re-export 잔류). 호출 그래프·순서 불변, 정적 가드 9파일 ADR-0444 concat 패턴 갱신(단언 무수정). 계보 ADR-0580/0589/0592 | refactor / trading |
| 0594 | Gate1 regime-aware reversal momentum credit — risk-on 국면(RISK_ON_REGIMES) 한정 quote.changePercent 를 PRICE_MOMENTUM 에 bounded 양수 가산(T_min~T_cap 선형·MAX_BONUS 상한·maxScore 20 천장 불변·hard-block/감점 무변경). 신규 SSOT signalScanner/reversalMomentumCredit.ts. flag GATE1_REVERSAL_MOMENTUM_ENABLED default OFF=byte-equivalent. 계보 ADR-0593/0592/0550/0546 | trading / gate1 / flag-gated |
| 0593 | regime risk-on fast-upgrade — R6 -5% 블랙스완 하락 트리거의 상방 대칭. 3중 AND(KOSPI 당일 강반등≥T·VKOSPI 진정·breadth 우위) 동시 충족 시 classifyRegime fall-through 직전 provisional R3_EARLY 승급(캡 — R2/R1 직행 금지). 신규 SSOT regime/riskOnFastUpgrade.ts. flag REGIME_RISK_ON_FAST_UPGRADE_ENABLED default OFF=byte-equivalent. 계보 ADR-0550/0592/0581/0561 | trading / regime / flag-gated |
| 0592 | R6_DEFENSE trigger freshness by trade-date + intraday KOSPI rebound recognition (shadow-gated). 봉 거래일(KRX) 기준 freshness 강등(결함 A false-latch 차단) + 오늘 intraday KOSPI 수익률(KIS 종합지수 0001 L1) recovery 공급(결함 B). 신규 SSOT kospiTriggerFreshness.ts(순수) + marketDataRefresh/kospiIntradayRefresh.ts. MacroState +4 / R6TriggerBreakdown +3 옵셔널. flag 3종 default OFF=byte-equivalent. 임계값 무변경, KIS 단일 통로, 9대 불변식 #1/#2/#6. 계보 ADR-0559/0561/0567/0584 | trading / regime / flag-gated |
| 0591 | market truth layer trading date ssot | trading |
| 0590 | routeBuilder investor flow router cyclomatic complexity reduction | refactor |
| 0589 | summaryBuilder and marketDataRefresh cyclomatic complexity reduction | refactor |
| 0588 | persistScanResults mid-scan diagnostic blocks extraction | refactor |
| 0587 | kisSectorEnergyProvider type-extraction decomposition | refactor |
| 0586 | VKOSPI flat-during-move 휴리스틱 revert — /vkospi_index_dump 결과 73.4 가 진짜 KRX-공식 VKOSPI(실제 대폭락: KRX300 레버리지 −11.88%·SK하이닉스 선물레버 −26.70%, VIX 16 은 한국발 국지폭락 디커플링). 휴리스틱이 진짜 plateau 를 오격리 → ADR-0584 Phase B 가드+ADR-0585 교정+⚠️STALE 마커+flag 2종+vkospiFreshness.ts 제거, ADR-0530 classic 가드 원복. 유지: Phase A 사실 가시화(baseDate/fetchedAt/경고)+/vkospi_index_dump. 부수: VKOSPI 이름필터 정밀화('최소변동성지수' 동음이의 배제, byte-equivalent). 교훈=단일지표 implausibility 판정 전 시장맥락 우선. 계보 ADR-0530/0584/0585 | regime / data-trust |
| 0585 | VKOSPI fetch 교정(ADR-0584 후속, flag VKOSPI_FETCH_CORRECTION_ENABLED default OFF byte-identical, 격리 flag와 독립): frozen(flat-during-move) KRX 값 거부→Yahoo `^VKOSPI` 검증값 채택→둘 다 frozen이면 carry-forward. 격리에 그치지 않고 실제값 교정. 안전 판별자=flat-during-move만(진짜 spike 미마스킹). KIS-primary(KRX L1 우선)·불변식 #6 보존. 잔존: 공식 인덱스코드 검증·risk axis 소비. 계보 ADR-0530/0584 | regime / data-trust / flag-gated |
| 0584 | VKOSPI stale 가시화(Phase A always-on·executionImpact=NONE: baseDate(BAS_DD)/fetchedAt 영속·carry-forward/ambiguous 경고·/regime ⚠️STALE) + flag-gated flat-during-move 격리(Phase B VKOSPI_STALE_GUARD_ENABLED default OFF byte-identical: KOSPI 급변 중 VKOSPI flat=frozen → UNTRUSTED_IMPLAUSIBLE, ADR-0530 G2 폭락일 갭 차단, 진짜 spike 미마스킹). 신규 SSOT vkospiFreshness.ts. 불변식 #6 보존. 한계(후속): risk-axis 격리·공식 인덱스코드 검증·baseDate 캘린더 stale-gate. 계보 ADR-0530 | regime / data-trust / flag-gated |
| 0583 | MHS 소스 저하(FRED/ECOS 결손) end-to-end 가시화(Phase A always-on·executionImpact=NONE) + flag-gated confluence 가드(Phase B MHS_DEGRADE_GUARD_ENABLED default OFF·byte-identical). 신규 SSOT mhsDegrade.ts(deriveMhsDegrade FULL/PARTIAL/FALLBACK·isMhsDegradeGuardEnabled), marketDataRefresh→MacroState(mhsSourcesOk/mhsConfidence/mhsDegraded) 영속+/regime 신뢰도 라인, Phase B ON+degraded 시 calcMacroScore MHS 낙관부스트 비대칭 억제(≥70 +8→+3·55~69 +3→0·<40 -15 보존). 불변식 #6/#9·VERBATIM 0줄, 테스트 30 | macro / confluence / flag-gated |
| 0582 | 조건 검증가능성 3-tier 분리 + 기술지표 계산 검증 승격 — VERIFIABLE(24)/AI_INTRINSIC(3:촉매·심리·엘리엇) 분리해 본질-AI 를 weightedScore 분모에서 제외(점수=검증가능 항목 중 검증율), 기술 6조건 OHLCV 결정적 계산으로 checklist 덮어쓰기→COMPUTED 승격 + marginAcceleration DART 영업이익률 추세→API 승격, 검증가능 13→20·상한 ~74→~92, 표시/진단 전용·executionImpact=NONE·9대 불변식 VERBATIM 0줄, UI 3-구분 노출, 회귀 finalScore 59→60 (최초 0581→main 충돌 0582 재번호) | dashboard / scoring / data-trust |
| 0581 | shadow→live 조건가중치 승격 파이프라인 (phased, flag-gated). Phase 0~2 토대 구현(flag·candidate reader·provider seam, byte-equivalent·executionImpact=NONE); Phase 3~4(promoter·rollout) follow-up·pre-live | learning / promotion / flag-gated |
| 0580 | marketDataRefresh ACMA 한계 임박 선제 분해 (1499→1327, 타입 8종+순수헬퍼 11→types.ts/helpers.ts, byte-equivalent·executionImpact=NONE, refreshMarketRegimeVars 무접촉) | refactor / complexity |
| 0579 | ACMA 1500 한계 임박 파일 선제 type-extraction 분해 (gate1DryRunObservationLedgerAdr0476 1500→1242 · sectorEnergyProvider 1499→1343, types.ts 추출·byte-equivalent) | refactor / complexity |
| 0578 | gate1 technical indicator injection (ma/rsi/atr 미배선 해소, flag-gated) | engine / signalScanner |
| 0577 | 섹터에너지 canonical 일원화 — per-candidate Gate2 SECTOR_LEADERSHIP 축 데이터원을 레거시 basket(KIS_SECTOR_ISCD_MAP 2xxx·DIAGNOSTIC_ONLY)에서 canonical verified official(OFFICIAL_SECTOR_ENERGY_BASE_VERIFY_TARGETS L1·11/11 VERIFIED)로 단일화. normalizeSectorThemeCycleForGate2 우선순위 raw??canonical(L1)??basket fallback, 기존 2-flag 재사용 default OFF byte-identical, executionImpact=execution-adjacent(sectorReturn20d→sectorScoreBoost +2/-1→STRONG_BUY)→shadow A/B 필수, 데이터위계 canonical=L1·basket=diagnostic(ADR-0561 정합), /sector_energy_diag 모순 collapse(DISPLAY_ONLY). ※ 최초 0572 발급→main 0572(ATR)와 충돌→merge 시 0577 재번호 | sector-energy / gate2 / kis-primary / flag-gated / execution-adjacent |
| 0576 | SHADOW P0 operational warn 에스컬레이션 제외 — SHADOW/SHADOW_ONLY P0 는 requireAck:false(재발송·60분 CRITICAL 에스컬레이션 0, 1회 발송 가시성 보존), LIVE 는 ack 루프 유지, ENV SHADOW_P0_ACK_LOOP_ENABLED 롤백, executionImpact NONE, 불변식 #8 정합 | telegram / alert-noise / ack-escalation / shadow |
| 0575 | defensive entry lane for r6 crash | trading |
| 0574 | kis sector name canonical normalization | quant |
| 0569 | take profit reentry guard | trading |
| 0568 | sector energy gate2 confluence wiring | quant |
| 0570 | Sector index 20d/5d return cycle 배선 — 공식 섹터 index daily(fetchKisSectorIndexDaily, realDataKisGet SSOT)에서 sectorReturn20d/5d 계산 → SectorEnergyInput 배선 → Gate2 sector cycle "Sector 0/25"(SECTOR_THEME_CYCLE_MISSING) 데이터 정확화(ADR-0568 confluence threading 과 상보), 섹터→iscd verifiedMapping 재사용·신규매핑 0, 6h+캐시·섹터당 1콜·후보당 재fetch 0, flag SECTOR_INDEX_CYCLE_WIRING_ENABLED default OFF=byte-equal(KIS콜 0), executionImpact=(b)execution-adjacent(hasKisSectorMetrics 점화→scoring formula→tier→applySectorScoreBoost +2/-1→STRONG_BUY 영향)→shadow 검증 필수, hardBlock/marketSignal 무회귀, ADR-0423 후속 | sector-energy / gate2 / kis-primary / flag-gated / execution-adjacent |
| 0568 | sector energy gate2 confluence wiring — sectorEnergyResult 를 Gate2 confluence external coverage 입력에 thread(SECTOR_ENERGY_GATE2_WIRING_ENABLED default OFF), SECTOR_LEADERSHIP MISSING 해소, ADR-0423 후속 | sector-energy / gate2 / flag-gated |
| 0573 | T1 ack 패밀리 흡수 — 같은 ackFamilyKey 새 ack 가 이전 pending 흡수, 손절 접근 다단계 재발송/에스컬레이션 종목당 3→1 축소, ackFamilyKey 미지정 byte-equivalent, executionImpact NONE, ADR-0572 상보 | telegram / alert-noise / ack-escalation / exit-engine |
| 0572 | 손절 접근 밴드 ATR 동적화 — 고정 5/3/1% → min(배수×ATR%, 고정) cap, 저변동주 과민 해소, ENV STOP_APPROACH_ATR_BANDS_DISABLED 롤백, default ON, 알림전용 executionImpact NONE | exit-engine / telegram / alert-noise / flag-gated |
| 0571 | 후보 sector→Gate2 sectorThemeCycle.sector 합성 배선 — repo 전체 sectorThemeCycle producer 0건이라 후보 sector=UNKNOWN→"Sector 0/25" 잔존(0568 threading·0570 데이터 켜도). sectorThemeCycleProducer 신설(canonicalizeSectorName: 세분화 라벨→12 canonical KRX 섹터명 귀속, getSectorByCode fallback, KIS콜 0·sectorMap SSOT 재사용), externalCoverage flag ON 시 호출→normalizer 매칭→OK_WITH_DATA, 미분류→undefined graceful(byte-equal), ADR-0568 동일 flag 재사용(신규 0) default OFF=byte-equal, executionImpact=(b)execution-adjacent(sectorScoreBoost→STRONG_BUY)→shadow 필수, ADR-0448 hardBlock 무신설·marketSignal:false, ADR-0423/0568/0570 완결편 | sector-energy / gate2 / flag-gated / execution-adjacent |
| 0567 | canonicalSession stale-CLOSED wall-clock 교정 — 장중 stale macro CLOSED 를 거래일+wall-clock=REGULAR_OPEN 시 교정(flag SCAN_SESSION_WALLCLOCK_CORRECTION_ENABLED default OFF=byte-equal), 휴장일 SSOT(isKrxTradingDay, ADR-0559) 위임으로 공휴일/주말 false-open 차단, flag ON=live-gating permissive 교정, 진단 정합 | scanner / session / execution-gating / flag-gated / calendar-ssot |
| 0566 | newsSupplyLogger 국내 종가 KIS(L1) 분리 — 국내 code 만 fetchNDayChangeDomestic(KIS-first flag-gated), EWY 글로벌(B4)은 Yahoo 유지, 잔존 fetchCloses=EWY 전용 → grandfather 8→7(WHITELIST 승격), flag OFF byte-equal(import 그래프 포함 lazy), 비실행(학습 귀인)·shadow A/B 미적용, 잔존 burn-down #3(혼합 callsite 심볼 분리) | learning / data-trust / kis-primary / yahoo-boundary / flag-gated |
| 0565 | lateWinEvaluator KIS(L1) 일봉 OHLCV 1차 삽입 — KIS→Yahoo(flag-gated, KIS_OHLCV_PRIMARY_ENABLED 재사용), Yahoo fallback 강등, grandfather 12→10(WHITELIST 승격), flag OFF byte-equal(import 그래프 포함 lazy), 비실행(learningOrchestrator)·shadow A/B 미적용, 잔존 burn-down #2(ADR-0563 §D4 OHLCV shape) | learning / data-trust / kis-primary / yahoo-boundary / flag-gated |
| 0564 | koreanQuoteBridge KIS(L1) 2차 삽입 — KRX→KIS→Yahoo 3단(flag-gated, KIS_OHLCV_PRIMARY_ENABLED 재사용), Yahoo 최후 fallback 강등, grandfather 13→12(WHITELIST 승격), flag OFF byte-equal, 비실행(krxRouter REST)·shadow A/B 미적용, 잔존 burn-down #1(ADR-0563 §D4) | provider / data-trust / kis-primary / yahoo-boundary / flag-gated |
| 0563 | 실행경로 Yahoo burn-down 완료 선언 + 잔존 비실행 동결 — Gate quote(C1)·LIVE 청산 종가 grandfather 0, 잔존 13 hit/7파일(학습 귀인·텔레그램 표시·KRX-fallback) FROZEN_NON_EXECUTION_ADR0563 동결(추적 유지·신규 Yahoo-first 차단 유지, 재활성=shape별 deliberate ADR), 두더지잡기 종료, 런타임 동작 0줄 | provider / data-trust / kis-primary / yahoo-boundary / static-guard / docs-only |
| 0562 | Yahoo(L3) Dependency Boundary Lock — 영구 차용 5건(B4+D1) 잠금 + 가드 전수 탐지 확장(직접 URL·.KS/.KQ concat·fetchCloses 국내심볼·Yahoo client 직접호출, C5 grandfather burn-down, 통합 단일가드, 런타임 0줄·구현 후속 engine-dev) | provider / data-trust / kis-primary / yahoo-boundary / static-guard / docs-only |
| 0561 | KIS(L1) Primary 절대불변식 — KIS-capable 레이어 Yahoo(L3)-first 금지 (§2.3 엄격·절대화·ADR-0547 §8 스캔 Yahoo-first 폐기·quota=엔지니어링 해결·진짜 대체불가 per 1개만 허용·grandfather→burn-down·가드 사양 후속 engine-dev·런타임 0줄) | provider / data-trust / kis-primary / invariant / static-guard / docs-only |
| 0560 | SSOT Drift-Prevention Registry + 정적 가드 (동일개념 다중구현 신규 재발 커밋타임 차단·docs/SSOT_REGISTRY.md 데이터 SSOT·grandfather/burn-down·진짜 drift 만 차단·가드 구현 선행 계약) | governance / ssot / drift-prevention / static-guard / docs-only |
| 0559 | Calendar(휴장일) SSOT 통합 — krxHolidays 데이터 SSOT / krxTradingCalendar 위임 헬퍼 (LIVE 게이트 2027+12/31 구멍 폐쇄·소비처 무변경·안전방향 only·구현 선행 계약) | trading-engine / calendar / live-gating / ssot / docs-only |
| 0558 | universeScanner Lazy/Budget = Legitimate Non-Funnel Boundary (SSOT Single-Funnel 프로그램 **마무리 ADR**·factory eager 통합 금지·supply 단일함수 공유 충족·V1 allowlist LEGITIMATE_BUDGET_LAZY 영구허용·묶음5/6 비추진 종결) | governance / source-snapshot / candidate-universe / non-funnel-boundary / ssot / docs-only |
| 0557 | Unified SourceSnapshot Consumer Threading (정본 소비 배포 계약·C+B 채택·A retention store 기각·Consumer Contract dead carry 방지·묶음3/5/6 재개 roadmap) | governance / source-snapshot / consumer-threading / ssot / docs-only |
| 0556 | SourceSnapshot Factory Boundary & Contract (ADR-0519 collector 확장·신규 factory 신설 기각, 입출력 11필드 계약, freshness 기존 enum 재사용, ADR-0011 경로분리, quota 순증 0) | governance / source-snapshot / factory-boundary / ssot / docs-only |
| 0555 | SSOT Single-Funnel Enforcement Constitution (Information Ownership Registry + 순서대로 로드맵 + KIS 레퍼런스 바인딩) | governance / ssot / source-snapshot / enforcement / docs-only |
| 0554 | counterfacture-gate self-tuning threshold pipeline | learning |
| 0553 | VTS paper-trading order symmetry + real-money startup guard | trading |
| 0552 | remove phantom KRX lunch break session window | trading |
| 0551 | leadershipBridge intraday leader injection wiring | screener |
| 0550 | stage1 risk-on regime leader capture | screener |
| 0549 | Market Rally Search Lens (추천/관측 전용 read-model, 자동매매 분리, ENV OFF byte-equivalent) | recommendation / read-model / source-snapshot-carry / isolation-guard / shadow-only |
| 0548 | KIS chk-holiday(CTCA0903R) opnd_yn 을 L1 권위 휴장 출처로 도입 (하이브리드 fallback, HOLIDAY_CACHE 배선) | provider / calendar / market-clock / kis-quota |
| 0547 | 기술지표 OHLCV 시계열 1차 출처 KIS 일봉(L1) 승격, Yahoo(L3) fallback 강등 (Phase 1 정밀경로) | provider / screener-adapter / technical-quote / kis-quota |
| 0546 | Gate1 Required-Score SSOT 통합 (Phase 1, 동작 보존) | gate-system / diagnostics / scan-blockers / config-ssot / shadow-only |
| 0545 | SectorEnergy Last-Known Verified Snapshot (Display + Shadow-Only) | sector-energy / display-classification / last-known-snapshot / persistence / shadow-only |
| 0544 | SectorEnergy Session-Not-Verifiable Display Isolation | sector-energy / display-classification / session-isolation / provider-policy / shadow-only |
| 0543 | Macro shortSelling KIS L1 Proxy Fallback (KRX OTP outage remediation) | provider / macro-health / regime / shadow-only / data-source-policy |
| 0542 | Investor Flow Supply Router KIS output1/output2 Bucket Realign + KIS-first Reprioritization | provider / supply-routing / kis-client / shadow-only |
| 0541 | Positive Score Starvation Audit Relocation to persistScanResults | gate-system / diagnostics / scan-blockers / display-only |
| 0540 | Macro shortSelling Trading-Day-Aware Freshness | regime / provider / macro-health / live-gating |
| 0539 | R6 Recovery Regime-Linked Cooldown Fast-Track | trading-engine / regime / r6-cooldown / live-gating |
| 0538 | scanBlockers Message Section Decomposition | telegram / scan-blockers / refactor / godfunction |
| 0537 | kisClient/query.ts Decomposition | kis-client / refactor / complexity-baseline |
| 0536 | yahoo-quotesummary-per-eps-opportunistic-enrichment | discovery / enrichment / data-source |
| 0535 | regime-authority-hierarchy-decision-context-ssot | governance |
| 0534 | sector-energy-canonical-baseline-lock | domain / trading / telegram / ssot |
| 0533 | Unified Forward Outcome Labeling Bus | learning / gate3 / gate1 / near-miss / counterfactual |
| 0532 | KIS Finance Fundamentals Migration | gate2 |
| 0531 | gate0-regime-ssot-single-source | trading / regime / gate0 / ssot / migration |
| 0530 | vkospi-stale-implausible-trust-guard | trading / regime / provider-sanity / invariant-6 |
| 0529 | DART Financials Canonical Inclusion into SourceSnapshot | trading |
| 0528 | railway decision log correlation standard | observability / decision-log / correlation |
| 0527 | ExecutionPermissionResolution Unification and PositionPolicyDecision SSOT | trading / execution-permission / position-policy-ssot |
| 0526 | CandidateGateEvaluationView SSOT | trading / gate-evaluation / candidate-view-ssot |
| 0525 | Gate Debug Raw Canonical Summary Rebinding | telegram / debug-raw / canonical-projection |
| 0524 | minimumSignalScoreTrace types and scoring decomposition | refactor |
| 0523 | gate2ExternalDataProvider types and helpers decomposition | refactor |
| 0522 | regimeLearningBank analytics helper decomposition | refactor |
| 0521 | sectorEnergyMasterSupplyUnknownPolicyAdr0488 decomposition | refactor |
|------|------|--------|
| 0520 | gate1-scoring-alignment-dry-run-gating | trading / gate1 / dry-run / observation / feature-flag |
| 0519 | unified-source-snapshot | trading / data-collection / source-snapshot / feature-flag |
| 0518 | corporate-action-guard-dailybar-continuity-verification | trading / corporate-action / watchlist / dailybar-verify |
| 0517 | kis-investor-flow-supply-health-bridge | kis / supply / forensic / wiring / patch |
| 0516 | watchlist-tier-based-kis-rest-usage | trading / watchlist / kis-rest / tier-policy |
| 0515 | volume-clock-lunch-window-adr0192-alignment | trading / volume-clock / sell-only / time-window |
| 0514 | sell-only-gate-flat-row-carry-restore | supply / sell-only / gate-semantic |
| 0513 | supply-gate-semantic-unwrap-wiring | supply / gate-semantic / diagnostics |
| 0511 | kis-sector-index-daily-fetcher | kis / sector-energy / proxy-fallback / callable-only |
| 0510 | krx-first-universe-and-sector-energy-pipeline-design | design / krx-first / sector-energy / pipeline |
| 0509 | unified-gate-score-kernel-audit | telegram / unified-gate-kernel / display-only |
| 0508 | sigterm-graceful-inflight-approval-drain | telegram / shadow-approval / graceful-shutdown / runtime |
| 0507 | gate1-forensic-collector-wiring-and-gate-compact-split | telegram / scan-blockers / forensic-collector / display-only |
| 0506 | scan-blockers-compact-and-adr0505-emission-verification | telegram / scan-blockers / display-only |
| 0488 | sector-energy-master-and-supply-unknown-policy | diagnostics / sector-energy / supply-unknown |
| 0487 | fresh-data-supply-layer-foundation | diagnostics / fresh-data / supply-foundation |
| 0486 | supply-recovery-runtime-mount-verification | diagnostics / supply / runtime-mount |
| 0485 | supply-advisory-promotion-readiness-audit | diagnostics / supply / readiness-audit |
| 0484 | supply-coverage-recovery-observation | diagnostics / supply / recovery-observation |
| 0483 | supply-source-freshness-dual-clock-refresh-job | diagnostics / freshness / shadow-refresh |
| 0482 | semantic-net-buy-normalizer-implementation | diagnostics / investor-flow / semantic-normalizer |
| 0481 | naver-investor-trend-collector-wiring | diagnostics / investor-flow / shadow-only |
| 0480 | operator-action-router-remediation-queue | diagnostics / operator-action / scan-blockers |
| 0479 | scan-blockers-detail-trace-registry | scan-blockers / detail-trace / read-only |
| 0478 | scan-blockers-compact-output-policy | scan-blockers / compact-output / priority-registry |
| 0477 | investor-flow-provider-router-wiring | signalScanner / supply / provider-router |
| 0476 | gate1-near-miss-dry-run-observation-ledger | signalScanner / gate1 / observation |
| 0475 | gate1-positive-source-wiring | signalScanner / gate1 / dry-run |
| 0474 | sector-energy-indexcode-coverage-recovery | clients / sectorEnergy / dry-run |
| 0473 | supply-provider-warmup-semantic-netbuy-cache-policy | supply / signalScanner / dry-run |
| 0462 | sector-energy-ssot-fallback-contamination-gate-router | sectorEnergy / gate-router / liveness |
| 0469 | gate1-penalty-deduplication-dry-run | signalScanner / gate1 / dry-run |
| 0470 | gate1-risk-double-count-dry-run | signalScanner / risk / dry-run |
| 0472 | gate1-scoring-alignment-and-dry-run-policy-promotion | signalScanner / gate1 / dry-run |
| 0456 | dart-name-disambiguation | governance / persistence / dart / disambiguation |
| 0455 | krx-master-db-enrichment-automation | governance / persistence / krx-master |
| 0454 | sector-energy-inputs-writer-wiring | governance / silent-degradation / sectorEnergy |
| 0453 | adr-index-baseline-retrofit (commit-label only, fast-iteration) | governance / infra |
| 0452 | shadow-near-breakout-entry-liveness | signalScanner / shadow / liveness |
| 0451 | empty-scan-liveness-policy | signalScanner / liveness / empty-scan |
| 0450 | kis-ws-pre-breakout-priority-routing | signalScanner / kis-ws / priority |
| 0449 | pre-breakout-wait-liveness-policy | signalScanner / liveness / wait |
| 0448 | trading-engine-liveness-first-auxiliary-no-hard-block | sectorEnergy / liveness / r3 |
| 0447 | sector-energy-alias-registry-expansion-and-aggregate-row-filtering | sectorEnergy / observability |
| 0446 | sector-energy-recovery-phase2-and-sanity-decomposition | sectorEnergy / observability |
| 0445 | krx-investor-flow-parser-empty-rows-hardening | supply / observability |
| 0444 | static-grep-guards-hardening | infrastructure / static-checks |
| 0443 | yahoo-symbol-resolver-ssot-migration | infrastructure / adapters |
| 0442 | kis-ws-subscription-diagnostics-exposure | infrastructure / clients |
| 0441 | kis-stream-bulk-apply-priority-wiring | infrastructure / kis-stream |
| 0440 | symbol-normalizer-direct-import-migration | infrastructure / utils |
| 0439 | provisional-shadow-price-provider-cache-lookup-hardening | learning / persistence |
| 0438 | symbol-resolver-invalid-krx-code-normalization | infrastructure / utils |
| 0437 | kis-websocket-subscription-priority-queue | infrastructure / clients |
| 0436 | gate-eligibility-split | learning / trading / orchestration |
| 0435 | investor-flow-provider-recovery | data / learning / telegram |
| 0434 | counterfactual-price-provider-cache-wiring | learning / telegram |
| 0001 | signalScanner-decomposition | refactor |
| 0002 | test-colocation | infra |
| 0003 | legacy-warn-backlog | infra |
| 0004 | yahoo-adr-deprecation | data |
| 0005 | strong-buy-and-telegram-trim | trading |
| 0006 | attribution-composite-key | learning |
| 0007 | learning-feedback-loop-policy | learning |
| 0008 | kelly-time-decay-wiring | trading |
| 0009 | external-data-call-budget | data |
| 0010 | external-call-budget-hardening | data |
| 0011 | ai-recommendation-source-split | data |
| 0012 | search-page-market-context-consolidation | ui |
| 0013 | multi-source-stock-master | data |
| 0014 | kis-retry-safety-policy | trading |
| 0015 | reconciliation-source-priority | trading |
| 0016 | weekend-ai-universe-fallback | data |
| 0017 | telegram-meta-commands-stage1 | telegram |
| 0018 | self-learning-data-integrity | learning |
| 0019 | recommendation-snapshot-lifecycle | learning |
| 0020 | source-weighted-learning | learning |
| 0021 | loss-reason-tagging | learning |
| 0022 | loss-reason-weighted-learning | learning |
| 0023 | condition-profit-factor-edge-score | learning |
| 0024 | regime-memory-bank | learning |
| 0025 | loss-reason-manual-override | learning |
| 0026 | attribution-classifier | learning |
| 0027 | learning-shadow-model | learning |
| 0028 | exitEngine-decomposition | conflict ×3 |
| 0029 | condition-source-tier | conflict ×3 |
| 0030 | latent-signal-scorer | conflict ×3 |
| 0031 | last-trigger-enemy-tranche | conflict ×3 |
| 0032 | sector-rotation-heatmap | conflict ×2 |
| 0033 | candidate-pipeline-visualization | ui |
| 0034 | macro-intelligence-and-history-extensions | ui |
| 0035 | attribution-and-correlation | learning |
| 0036 | budget-policy-extraction | trading |
| 0037 | alert-router-vibration-policy | telegram |
| 0038 | private-vs-channel-separation | telegram |
| 0039 | telegram-callsite-migration | telegram |
| 0040 | macro-digest-cron | telegram |
| 0041 | weekly-self-critique-report | telegram |
| 0042 | channel-test-and-stop-countdown | telegram |
| 0043 | market-day-classifier-and-schedule-guard | infra |
| 0044 | holiday-resume-policy | trading |
| 0045 | krx-holiday-annual-audit | infra |
| 0046 | f2w-drift-detector | learning |
| 0047 | reflection-module-halflife | learning |
| 0048 | learning-coverage-heatmap | learning |
| 0049 | context-adaptive-auto-trade-layout | ui |
| 0050 | account-survival-gauge | ui |
| 0051 | invalidation-meter | ui |
| 0052 | one-decision-resolver | ui |
| 0053 | nightly-reflection-card | ui |
| 0054 | concordance-matrix | ui |
| 0055 | gate-mini-indicator | ui |
| 0056 | yahoo-probe-resilience | data |
| 0057 | fomc-policy-v4 | trading |
| 0058 | egressguard-intent-tags | data |
| 0059 | safe-pct-change-helper | infra |
| 0060 | shadow-trade-sector-persistence | trading |
| 0061 | fomc-day-liquidation | trading |
| **0062** | *(누락)* | — |
| **0063** | *(누락)* | — |
| 0064 | market-overview-prefill-overlay | data |
| 0065 | market-indicators-snapshot-resilience | data |
| 0066 | market-overview-swr-cache | data |
| 0067 | marketoverview-boundary-guard | conflict ×2 |
| 0068 | macrostate-stale-block | conflict ×2 |
| 0069 | x-field-stale-ui-badge | ui |
| 0070 | market-data-health-score | data |
| 0071 | cross-source-data-validation | data |
| 0072 | entry-circuit-breaker | trading |
| 0073 | regime-holding-period-shortening | trading |
| 0074 | regime-message-live-regime-line | telegram |
| 0075 | sector-score-boost | trading |
| 0076 | fomc-regime-kelly-precedence | trading |
| 0077 | trade-signal-status-state-machine | trading |
| 0078 | enemy-auto-block | trading |
| 0079 | atr-buffered-bep-glide | trading |
| 0080 | capital-weighted-slot-accounting | trading |
| 0081 | preorder-guard-final-gate-ssot | trading |
| 0082 | yahoo-range-restriction-policy | data |
| 0083 | walk-forward-framework-extension | learning |
| 0084 | condition-lifecycle-status-policy | learning |
| 0085 | two-bar-confirmation-and-slot-sizing | trading |
| 0086 | shadow-walk-forward-framework | learning |
| 0087 | shadow-condition-attribution | learning |
| 0088 | shadow-learning-dashboard | ui |
| **0089** | *(누락)* | — |
| 0090 | cache-coherence-auditor | infra |
| 0091 | yahoo-stale-base-fallback | data |
| 0092 | channel-sell-cumulative-remaining-qty | telegram |
| 0093 | gating-alert-dedupe | telegram |
| 0094 | ui-language-ssot | ui |
| 0095 | data-quality-5tier-auto-ladder | ui |
| 0096 | idontknow-and-data-quality-ribbon | ui |
| 0097 | verdict-card-and-time-band | ui |
| 0098 | confluence-meter-and-axis-reason | ui |
| 0099 | ui-verbosity-toggle | ui |
| 0100 | ui-verbosity-toggle-embed | ui |
| 0101 | verdict-card-verbosity-wiring | ui |
| 0102 | confluence-meter-verbosity-wiring | ui |
| 0103 | ribbon-idontknow-verbosity-wiring | ui |
| 0104 | gating-alert-window-and-fomc-liquidation-label | telegram |
| **0105** | *(누락)* | — |
| **0106** | *(누락)* | — |
| 0107 | mhs-axis-and-gate-zero-context | trading |
| 0108 | operator-noise-reduction | telegram |
| 0109 | data-quality-badge-verbosity-wiring | ui |
| 0110 | gate-status-card-verbosity-wiring | ui |
| 0111 | circuit-breaker-baseline-reset | trading |
| 0112 | breakeven-classification-and-circuit-isolation | trading |
| 0113 | yahoo-drift-tiered-sanity-and-corporate-action-detector | data |
| 0114 | data-trust-layer-policy | data |
| 0115 | entry-price-immutable-and-execution-relaxation | trading |
| 0116 | raw-adjusted-price-and-gate3-relaxation-wiring | trading |
| 0117 | sanity-trade-block-gate | data |
| 0118 | scan-blockers-diagnostic-infrastructure | trading |
| 0119 | empty-scan-reason-classification | trading |
| 0120 | gate-pass-counters-and-r3-sanity | trading |
| 0121 | price-source-policy | data |
| 0122 | sector-energy-krx-matching-and-data-quality | trading |
| 0123 | be-isolation-in-learning-and-reports | learning |
| 0124 | regime-coverage-suggest-false-positive | conflict ×2 |
| 0125 | sector-energy-data-quality-wiring | trading |
| 0126 | price-source-policy-execution-wiring | trading |
| 0127 | scan-summary-sector-energy-quality | trading |
| 0128 | data-verification-timing-and-data-hold-state-machine | data |
| 0129 | mdd-ssot-be-narrative-macro-gate-wiring | learning |
| 0130 | cumulative-reflection-context | learning |
| 0131 | self-health-check-loop | infra |
| 0132 | edge-trigger-scheduler-logging-and-holiday-enter-alert | infra |
| 0133 | file-complexity-gate-integrity | infra |
| 0134 | persymbol-evaluation-decomposition | refactor |
| 0135 | kisclient-decomposition | refactor |
| 0136 | fss-records-age-diagnostic-and-passive-active-null | data |
| 0137 | kis-stock-program-trade-today | data |
| 0138 | kis-market-program-trade | data |
| 0139 | ecos-margin-balance-5d-change | data |
| 0140 | naver-foreigner-ratio-trend | data |
| 0141 | fss-investor-detail-fetcher | data |
| 0142 | fss-passive-active-mapping | data |
| **0143** | *(누락)* | — |
| 0144 | kis-program-trade-endpoint-correction | data |
| 0145 | diagnose-short-macro-state | telegram |
| 0146 | pr-pace-audit-rule | governance |
| 0146 | telegram-sanity-unlock-cmd | telegram |
| 0147 | kis-token-disk-persistence | infra |
| 0147 | signalScanner-orchestration-migration | refactor |
| 0148 | governance-followup-static-checks | governance |
| 0149 | condition-mapping-fix | learning |
| 0150 | phase1-dart-finalize | learning |
| 0151 | phase2-kis-supply-audit | learning |
| 0152 | naver-foreigner-trend-endpoint | learning |
| 0153 | phase3-globalintel-synthesis | learning |
| 0154 | phase4-closeout-and-residual-policy | learning |
| 0155 | krx-investor-trend-endpoint | learning |
| 0156 | yahoo-consensus-endpoint | learning |
| 0157 | feedback-loop-now-injection | learning |
| 0158 | wiring-sla-auto-expiry | governance |
| 0159 | adr-alias-diaspora | governance |
| 0160 | reflection-signal-routing | learning |
| 0161 | position-sizing-engine-tier-based | trading |
| 0162 | position-sizing-engine-shadow-apply | trading |
| 0163 | position-sizing-engine-extension-3-paths | trading |
| 0164 | peak-equity-tracking | trading |
| 0165 | position-sizing-engine-live-activation | trading |
| 0166 | regime-exposure-budget | trading |
| 0167 | current-equity-exposure-accurate | trading |
| 0168 | kelly-clamp-ssot | trading |
| 0168 | emergency-data-quality-circuit-breaker | data |
| 0169 | tranche-executor-exposure-budget-wiring | trading |
| 0170 | exposure-regime-auto-mapping | trading |
| 0171 | sizing-exposure-budget-verbose-log | trading |
| 0172 | sizing-engine-liquidity-sector-real-data | trading |
| 0173 | shadow-learning-on-blocked-days | trading |
| 0174 | safety-gate-attribution-and-shadow-live-delta | learning |
| 0175 | future-return-resolver-cron | learning |
| 0176 | missed-learning-queue-cron-wiring | learning |
| 0177 | learning-sanity-dashboard-endpoint | learning |
| 0178 | learning-sanity-dashboard-ui | learning |
| 0179 | missed-learning-queue-stats-card | learning |
| 0180 | rejected-winners-card | learning |
| 0181 | stale-reflections-card | learning |
| 0182 | unresolved-counterfactuals-card | learning |
| 0183 | shadow-learning-blocked-day-wiring | trading |
| 0184 | emergency-data-quality-guards-wiring-phase-a | dataQuality |
| 0185 | emergency-data-quality-guards-wiring-phase-b | dataQuality |
| 0186 | order-type-optimizer-wiring-phase-1 | trading |
| 0187 | macro-state-dead-read-wiring | persistence |
| 0188 | lint-baseline-cleanup | quality |
| 0189 | premarket-gap-probe-krx-calendar | trading |
| 0190 | safe-pct-change-krx-calendar | trading |
| 0191 | position-truth-ssot-and-shadow-mode-header | persistence |
| 0192 | trade-window-policy-update | trading |
| 0193 | block-new-buy-manage-only-symmetric-coupling | trading |
| 0194 | telegram-block-guard-commands | telegram |
| 0195 | r3-sanity-block-telegram-unblock | telegram |
| 0211 | gate-evaluation-fallback (commit-label only, fast-iteration) | trading |
| 0221 | kis-prevclose-priority (commit-label only, fast-iteration) | trading |
| 0231 | krx-master-symbol-resolver-ssot (commit-label only, fast-iteration) | trading |
| 0234 | yahoo-meta-symbol-validation (commit-label only, fast-iteration) | trading |
| 0235 | yahoo-close-timestamps-stale (commit-label only, fast-iteration) | trading |
| 0237 | volumeclock-blocked-empty-scan (commit-label only, fast-iteration) | orchestrator |
| 0241 | yahoo-sanity-aware-fallback (commit-label only, fast-iteration) | trading |
| 0242 | stockmaster-auto-enrichment (commit-label only, fast-iteration) | data |
| 0245 | stockmaster-integrity-check (commit-label only, fast-iteration) | data |
| 0248 | watchlist-diversity-monitor (commit-label only, fast-iteration) | learning |
| 0249 | global-yahoo-symbol-ssot-api (commit-label only, fast-iteration) | trading |
| 0250 | user-diagnostic-hints-ledger (commit-label only, fast-iteration) | learning |
| 0251 | krx-off-hours-counter-isolation (commit-label only, fast-iteration) | clients |
| 0252 | krx-business-day-grace-extension (commit-label only, fast-iteration) | utils |
| 0255 | yahoo-freshness-ledger (commit-label only, fast-iteration) | persistence |
| 0256 | krx-time-window-gating (commit-label only, fast-iteration) | clients |
| 0258 | health-full-diagnostic-command (commit-label only, fast-iteration) | telegram |
| 0259 | krx-cooldown-probe-mode (commit-label only, fast-iteration) | clients |
| 0260 | defect-evolution-ledger (commit-label only, fast-iteration) | learning |
| 0323 | gate-pass-rate-sla | learning |
| 0324 | gate-contribution-analyzer | learning |
| 0325 | gate-threshold-auto-tuning | learning |
| 0329 | persona-balance-ledger | learning |
| 0341 | krx-trading-calendar-wiring | clients |
| 0342 | krx-empty-response-auto-retry | clients |
| 0343 | sector-energy-cache-fallback | clients |
| 0364 | sector-energy-yahoo-etf-fallback | clients |
| 0370 | sector-energy-hardening-phase-1 | clients |
| 0387 | condition-eval-status-data-unavailable | quant/conditions |
| 0388 | condition-eval-status-error-isolation | quant/conditions |
| 0389 | evaluator-status-migration-and-gate-audit-extension | quant/conditions |
| 0390 | five-evaluator-status-migration | quant/conditions |
| 0391 | p0a-mode-observability | telegram |
| 0392 | p0b-trading-mode-ssot | trading |
| 0393 | p1-execution-mode-and-shadow-ledger | state |
| 0394 | p1.5-execution-terminology-ssot (commit-label only, fast-iteration) | types |
| 0395 | p2-persistent-execution-mode-override | persistence |
| 0396 | sector-energy-dataquality-decomposition | clients |
| 0397 | sector-energy-yahoo-etf-fallback-wiring | clients |
| 0398 | sector-energy-strong-buy-confidence-gate | trading |
| 0399 | sector-energy-source-restoration | clients |
| 0400 | wire-sector-energy-strong-buy-gate | trading |
| 0401 | r3-sanity-violation-state-machine | trading |
| 0411 | evaluator-provider-degraded-and-kis-recovery | trading |
| 0412 | frozen-quote-detector-and-holiday-r3-streak-guard | trading |
| 0413 | stock-master-evening-cron | scheduler |
| 0415 | sector-energy-stale-partial-volume-strong-buy-gate | trading |
| 0414 | price-integrity-correction-overlay-readonly | trading |
| 0416 | evaluator-status-supply-earnings-data-unavailable | quant/conditions |
| 0417 | postmortem-action-taxonomy-split | orchestrator |
| 0418 | evaluator-data-availability-metadata-automation | quant/conditions |
| 0419 | r3-sanity-streak-excludes-sell-only-volume-clock | trading |

| 0420 | fresh-scan-blocker-attribution | trading |

| 0421 | investor-flow-semantic-availability | supply / quant/conditions |

| 0433 | counterfactual-universe-learning-preflight | learning / telegram |
| 0432 | shadow-learning-promotion-recommendations | learning / telegram |

| 0431 | counterfactual-shadow-performance-report | learning / telegram |

| 0430 | counterfactual-shadow-learning-under-sell-only | trading / persistence |

| 0429 | wire-provisional-shadow-price-provider | learning / telegram |

| 0428 | provisional-shadow-performance-report | learning / telegram |

| 0427 | wire-r3-provisional-shadow-ledger | persistence / trading |

| 0426 | r3-provisional-shadow-lane | trading |

| 0425 | gate-decision-router | trading |

| 0424 | sector-indexcode-provider-repair | clients |

| 0423 | sector-energy-data-truth-repair | clients / trading |

| 0422 | gate2-leadership-attribution | trading |

| 0489 | adr-merge-conflict-resolution-guard | governance |
| 0490 | program-trading-data-line | trading |
| 0491 | supply-snapshot-store-replay | trading |
| 0492 | adr-merge-conflict-resolution-branch | governance |
| 0493 | data-promotion-readiness-audit | trading |
| 0494 | fresh-data-promotion-audit-wiring | trading |
| 0495 | sector-index-master-seed-mapping-repair | trading |
| 0496 | investor-flow-sample-seed-semantic-netbuy-normalization | trading |
| 0497 | diagnostic-taxonomy-gate-failure-attribution-ssot | trading |
| 0498 | fresh-data-status-view-model-wiring | trading |
| 0499 | provider-health-market-signal-classifier-migration | trading |
| 0500 | empty-scan-root-cause-dashboard | diagnostics |
| 0501 | weekend-replay-gate-failure-cause | diagnostics |
| 0502 | krxclient-decomposition | refactor |
| 0503 | kis-official-supply-pack-until-krx-recovery | supply |
| 0504 | position-card-source-validation | telegram |
| 0505 | gate1-minimum-signal-forensic-audit | diagnostics |
| 0619 | shadow-only-entry-liberalization-gate1-gate3 | trading / learning |
| 0620 | provisional-shadow-entry-price-measurement-wiring | learning / persistence |
| 0621 | gate2-rs-benchmark-dualization-kosdaq | quant / gate2 |
| 0622 | universe-discovery-aggressiveness-topn-rs-percentile | policy / screener |
| 0623 | price-correction-stage2-shadow-activation | trading / diagnostics |
| 0624 | shadow-always-on-by-default-operator-decision-removal | policy / shadow / learning |
| 0625 | shakeout-stoploss-forward-outcome-labeler | learning / persistence |
| 0626 | shakeout-initial-loss-twobar-shadow | trading / exit |
| 0627 | gate1-rs-percentile-continuous-and-breakout-ohlcv-field-fix | trading / gate1-scoring |
| 0628 | intraday-leader-universe-freshness | screener / orchestrator |
| 0629 | intraday-mover-candidate-pool-wiring | signalScanner / candidate-pool |

**최대 발급 0629 · 다음 발급 0630** — `node scripts/check_adr_index.js --json` 기준 (2026-06-18 실측·validate:adrIndex). 카운트 SSOT = `validate:adrIndex`, 충돌·누락 분류는 위 §"알려진 충돌"·§"누락".

## 후속 PR — 자동 충돌 검사 정적 스크립트

본 인덱스 갱신 누락 차단을 위해 후속 PR 에서 `scripts/check_adr_index.js` 신규 — `docs/adr/*.md` 파일 시스템 vs INDEX.md §"전체 인덱스" 표 정합 검증 + `validate:all` 통합. 본 PR 은 인프라 수동 작성만, 자동 검사는 별도 PR 분리 (회귀 위험 격리).
