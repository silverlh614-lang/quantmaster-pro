# QuantMaster Pro

## 프로젝트 개요

AI 기반 한국 주식 퀀트 트레이딩 시스템. 27개 조건 + 4단계 Gate 필터를 통과한
종목에만 신호를 출력하며, KIS API로 실제 주문을 집행한다.

핵심 참조 문서:
- 요구사항·도메인: `README.md`
- 모듈 경계: `ARCHITECTURE.md`
- 운영·인시던트: `docs/incident-playbook.md`
- 환경/비밀 분리: `.env.example`
- 품질 게이트: `npm run validate:all`, `npm run precommit`

디렉토리 구조 요지:
- `src/` — 프론트엔드 + 공유 타입·서비스 (Vite + React 19 + Zustand + TanStack Query)
- `server/` — Express 기반 백엔드 (KIS 클라이언트, 트레이딩 엔진, 스크리너, 텔레그램)
- `scripts/` — 자체 검증 파이프라인 (complexity/responsibility/exposure/sds/gemini)
- `docs/` — 인시던트 플레이북, ADR

## 하네스: QuantMaster Harness

**트리거:** 매매 엔진 / 퀀트 필터(Gate 0~3) / 대시보드 / 변곡점 모듈(THS/VDA/FSS/IPS) /
서버 리팩토링 관련 작업 요청 시 `.claude/skills/quantmaster-orchestrator` 스킬을 사용하라.

추가 전용 스킬:
- `.claude/skills/server-refactor-orchestrator` — 1,000줄 이상 서버 파일 분해 전용
- `.claude/skills/incident-responder` — Telegram/로그 인시던트 진단 전용

**단순 질문은 직접 응답 가능.** (예: "이 함수가 뭐야?", "타입 오류 한 줄 수정")
**복잡 작업은 하네스 필수.** (예: "새 Gate 조건 추가", "signalScanner 분해", "webhookHandler 재설계")

## 에이전트 팀 (4인)

| 역할 | 담당 영역 | DoD |
|------|-----------|------|
| `architect` | `ARCHITECTURE.md` 경계 설계, `src/types/`, ADR 작성 | `npm run validate:responsibility` 통과 |
| `engine-dev` | `server/trading/*`, `server/clients/kisClient.ts`, `server/quant*`, `src/services/quant*` | `npm run lint` + 해당 `*.test.ts` 통과 |
| `dashboard-dev` | `src/pages/*`, `src/components/*`, `src/hooks/*`, Zustand 스토어 | `npm run validate:complexity` 통과 |
| `quality-guard` | QA + 보안 + 경계면 교차 비교 | `npm run validate:all` 전체 통과 |

보안/이상감지는 `scripts/scan_exposure.js`, `scripts/silent_degradation_sentinel.js`가 이미
기계 에이전트로 동작 중이므로 AI 에이전트는 조율·해석·수정 위임에 집중한다.

## 절대 규칙

1. **@responsibility 태그 의무**: 모든 새 파일은 상단 20줄 내 25단어 이내 책임 명시
   (`scripts/check_responsibility.js`로 강제).
2. **kisClient 단일 통로**: KIS API 호출은 `server/clients/kisClient.ts` 경유만 허용.
   다른 모듈은 raw KIS REST 호출 금지.
3. **stockService 단일 통로 (자동매매·서버 스크리너용)**: 자동매매와 서버 스크리너의
   외부 데이터(Yahoo/DART/Gemini/KIS 프록시/KRX) 페칭은 `src/services/stockService.ts`
   에서만 시작한다.
   **AI 추천 단일 통로**: AI 종목 추천(MOMENTUM/QUANT_SCREEN/BEAR_SCREEN/EARLY_DETECT)
   universe 발굴·enrichment 는 `server/services/aiUniverseService.ts` 단일 통로만
   사용한다 (KIS/KRX 직접 호출 금지, ADR-0011). 자동매매 경로는 본 모듈 import 금지.
4. **autoTradeEngine 단일 통로**: `AUTO_TRADE_ENABLED=true` 상태에서 실주문은
   서버 측 `autoTradeEngine`만 집행한다. 클라이언트는 실주문 금지.
5. **ARCHITECTURE.md 경계 준수**: 수정 전 해당 모듈의 Single Responsibility 재확인.
6. **복잡도 한계**: 파일당 1,500줄, 함수당 한계는 `scripts/check_complexity.js` 기준.
   초과 시 즉시 분할.
7. **커밋 전**: `npm run precommit` 필수 통과. 훅 우회(`--no-verify`) 금지.

## 기존 복잡도 위반 (리팩토링 우선순위)

하네스 도입 시점 기준 1,000줄 초과 서버 파일:

| 파일 | 줄 수 | 우선순위 |
|------|------:|----------|
| `server/trading/signalScanner.ts` | 1,820 | P0 — 변동성 최대 지점 |

> ✅ `server/telegram/webhookHandler.ts` 1,858 → 155 줄 (-91.7%) — PR-44/45/46/47 (ADR-0017 §Stage 2 A+B1+B2+B3) 로 commands/* 8 디렉토리 51 cmd 분리 완료 (2026-04-25). 본 표에서 정식 제거.
> ✅ `server/trading/exitEngine.ts` 1,358 → 18 줄 (-98.7%, barrel) — PR-53 (ADR-0028) 로 `exitEngine/{index,types,helpers/×6,rules/×16}` 분해 완료 (2026-04-26). 본 표에서 정식 제거.
> ✅ `server/screener/stockScreener.ts` 1,573 → 542 줄 (-65.5%) — PR-55 (ADR-0029) 로 `screener/{stockUniverse,rejectionLog,watchlistRejectionReport,adapters/{yahoo,kis,krx,_indicators}}` 분리 + barrel 완료 (2026-04-26). 본 표에서 정식 제거.

분해 설계는 `docs/adr/` 에 ADR로 선행 기록 후 `server-refactor-orchestrator` 스킬로 진행한다.

## 검증 파이프라인 요약

| 스크립트 | 검사 항목 |
|----------|-----------|
| `npm run validate:gemini` | Gemini 호출 규약 위반 탐지 |
| `npm run validate:complexity` | 파일·함수 복잡도 임계 초과 탐지 |
| `npm run validate:sds` | Silent Degradation(조용한 성능 저하) 패턴 탐지 |
| `npm run validate:exposure` | 비밀·토큰 노출 스캔 |
| `npm run validate:responsibility` | `@responsibility` 태그 존재·길이 검사 |
| `npm run lint` | `tsc --noEmit` (클라 + 서버 tsconfig 각각) |
| `npm run precommit` | 배포창 + 노출(증분) + 복잡도 + 책임(변경분) + Gemini + lint |

## 하네스 사용 워크플로 (요약)

1. 사용자 요청 → 본 문서의 "트리거" 판정
2. 해당 스킬 호출 → `_workspace/{YYYY-MM-DD}_{task}/` 생성
3. `architect` → (`engine-dev` ∥ `dashboard-dev`) → `quality-guard` 순서로 위임
4. 통합 검증(Phase 4): `npm run lint` → `npm run validate:all` → 해당 테스트 → 교차 비교 → `npm run precommit`
5. Phase 5: CLAUDE.md 하단 "변경 이력"에 한 줄 추가 → 의미 있는 커밋 메시지 작성

상세는 `.claude/skills/quantmaster-orchestrator/SKILL.md` 참조.

## 변경 이력

| 날짜 | 변경 내용 | 대상 | 사유 |
|------|----------|------|------|
| 2026-04-26 | PR-Q Multi-Timeframe Confluence Gate (페어 B #2, ADR-0067, 사용자 5개 결합 아이디어 잔여 PR-Q): 사용자 요청 "옵션 1" — 페어 B 시작. PR-A~P 자기학습 시리즈 머지(#393) 후속 — 사용자 10 결합 아이디어 페어 B (#2 + #7 + #4) 의 첫 PR. **#2 Multi-Timeframe Signal Confluence** 신규 entryGates 모듈. ① **신규 게이트** `server/trading/signalScanner/entryGates/timeframeConfluenceGate.ts` (~80 LoC) — 30분/일/주봉 일목+MA20 정렬 boolean 카운트 → 0/3 차단 / 1/3 PROBING / 2~3/3 CONVICTION. ② **EntryGateContext 확장** — `timeframeConfluence?: TimeframeAlignment` 옵셔널 필드 (daily/weekly/intraday30m boolean). 데이터 부재 시 게이트 자동 PASS skip. ③ **ENTRY_GATES_PHASE_B 8 게이트로 확장** — 기존 7 (cooldown/blacklist/addBuyBlock/rrr/sectorConcentration/sectorPreGuard/portfolioRisk) + timeframeConfluence (마지막). ④ **상수 SSOT 3종** — CONFLUENCE_BLOCK_THRESHOLD=0 / PROBING=1 / STANDARD=2 + countTimeframeAlignment 순수 함수 export (테스트 가능). ⑤ **환경변수 롤백** — `ENTRY_TIMEFRAME_CONFLUENCE_DISABLED=true` → 항상 PASS. ⑥ **회귀 테스트 18 케이스 신규** — countTimeframeAlignment 5 (undefined / 빈 / 3/3 / 1/3 / 부분) + 게이트 9 분기 (skip / 0/3 / 1/3 / 2/3 / 3/3 / 1/1 / 0/1 / ENV / 빈 객체) + ENTRY_GATES_PHASE_B 통합 3 (포함 / 마지막 / 8개) + 상수 1. 인접 게이트 회귀 39 무회귀 (총 57 entryGates pass). ⑦ **wiring 본 PR scope 밖** — `perSymbolEvaluation` 의 ctx 빌더가 timeframeConfluence 주입하는 wiring + stockScreener 어댑터 KIS 30분/주봉 fetch 후속 PR. 본 PR 은 게이트 인프라만. ⑧ **검증** — vitest **316 files / 3426 tests pass** (신규 18 + 기존 3408 무회귀) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종) + precommit 전체 통과 (`ALLOW_DEPLOY_WINDOW=1`). KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — 게이트 layer 추가 + 데이터 부재 시 자동 skip. | `docs/adr/0067-multi-timeframe-confluence-gate.md` (신규), `server/trading/signalScanner/entryGates/timeframeConfluenceGate.ts` (신규), `server/trading/signalScanner/entryGates/types.ts` (TimeframeAlignment + EntryGateContext.timeframeConfluence 옵셔널 추가), `server/trading/signalScanner/entryGates/index.ts` (배열 + barrel), `server/trading/signalScanner/entryGates/__tests__/timeframeConfluenceGate.test.ts` (신규 18 케이스), `server/trading/signalScanner/entryGates/__tests__/_testHelpers.ts` (timeframeConfluence propagate) | 사용자 "엔트리게이트에 추가 안 됨" 지적 해소. 페어 B 의 첫 PR — wiring (데이터 주입) 은 후속 PR 분리해 회귀 위험 격리. PR-R (#7 Dynamic RRR) / PR-S (#4 Sector Rotation Differential) 가 페어 B 잔여, PR-T (#5 Pyramid) / PR-U (#3 Re-entry) 가 페어 C 후속. |
| 2026-04-23 | 하네스 신규 구축 (CLAUDE.md + agents/skills) | `.claude/`, `CLAUDE.md` | AI 조율 레이어 도입 |
| 2026-04-23 | ADR 0001~0003 + ARCHITECTURE.md 분해 예고 | `docs/adr/`, `ARCHITECTURE.md` | 대형 파일 분해 설계·테스트 co-location·WARN 백로그 정책 |
| 2026-04-23 | learningJobs.ts @responsibility 27→24단어 축약 | `server/scheduler/learningJobs.ts` | ADR-0003 P0 즉시 해소 |
| 2026-04-24 | PR-1 자금·장부 안전 가드 (#7 동시호가 Full 가드, #10 Shadow 집계 SSOT + [SHADOW] 뱃지, #4 Yahoo ADR 비활성 + ADR-0004, #5 KIS 전일종가 기반 preMarketGapProbe) | `server/orchestrator/tradingOrchestrator.ts`, `server/persistence/shadowTradeRepo.ts`, `server/telegram/webhookHandler.ts`, `server/alerts/adrGapCalculator.ts` (stub), `server/clients/kisClient.ts` (+fetchKisPrevClose), `server/trading/preMarketGapProbe.ts` (신규), `docs/adr/0004-yahoo-adr-deprecation.md` (신규) | 동시호가 Full 오발주·Shadow 0건 버그·Yahoo OTC 이론시가 괴리 해소 |
| 2026-04-24 | PR-2 관심종목 서버 동기화 + UI API 회복력 (#1 user-watchlist CRUD, #2 TanStack retry/onError + ScreenerPage useQuery 전환) | `server/persistence/userWatchlistRepo.ts` (신규), `server/routes/userWatchlistRouter.ts` (신규), `src/hooks/useWatchlistSync.ts` (신규), `src/components/common/QueryProvider.tsx`, `src/api/autoTradeClient.ts` | 기기 간 관심종목 일치 + API 실패 시 UI 깨짐 방지 |
| 2026-04-24 | PR-3 운영·효율 (#8 섹션 하드캡 trim, #9 /reconcile push + 16:05 KST 드라이런 브로드캐스트, #6 Gemini 재시도 2→3·백오프 상향, #3 canonical 캐시키) | `server/persistence/watchlistRepo.ts`, `server/scheduler/maintenanceJobs.ts`, `server/telegram/webhookHandler.ts`, `server/clients/geminiClient.ts`, `server/persistence/aiCacheRepo.ts` | watchlist 91개 폭증·수동 /reconcile 누락·Gemini fallback 반복·캐시 미스 해소 |
| 2026-04-24 | PR-4 경계 위생 (A emergency.ts raw fetch 제거 → kisGet/kisPost, B regime-change dedupeKey up/down 분리, C Yahoo KOSPI 호출 재분류 및 ADR-0004 scope 명시) | `server/emergency.ts`, `server/trading/regimeBridge.ts`, `docs/adr/0004-yahoo-adr-deprecation.md` | kisClient 단일 통로 규칙 준수, up/down 알림 덮어쓰기 방지, 후속 과제 경계 명문화 |
| 2026-04-24 | PR-5 Shadow 계좌 독립 원장 분리 (#11 signalScanner · preMarketOrderPrep 가 SHADOW 모드에서는 computeShadowAccount 사용, LIVE 모드에서만 fetchAccountBalance) | `server/trading/signalScanner.ts`, `server/orchestrator/tradingOrchestrator.ts`, `server/persistence/shadowAccountRepo.test.ts` (신규) | SHADOW 로그·사이징·자산표기가 실계좌 잔고와 섞이던 문제 해소 |
| 2026-04-24 | PR-6 exitEngine 동시 실행 가드 (#12 orchestratorJobs vs shadowResolverJob 5분 overlap 시 `updateShadowResults` 중복 진입 차단 mutex) | `server/trading/exitEngine.ts`, `server/trading/exitEngineMutex.test.ts` (신규) | L3 분할 익절·원금보호 알림이 텔레그램에 2번 송출되며 "잔여: 60주" 가 두 번 보이던 문제 해소 |
| 2026-04-24 | PR-7 SHADOW BUY fill 기록 (#13 PENDING→ACTIVE 전환 시 `appendFill(BUY)`; 기존 trade 부팅·tick 시작부 idempotent 백필) | `server/persistence/shadowTradeRepo.ts` (+`backfillShadowBuyFills`), `server/trading/exitEngine.ts`, `server/index.ts`, `server/persistence/shadowBuyFillBackfill.test.ts` (신규) | SHADOW 부분 매도 후 잔량이 갱신되지 않던 고질 원인(BUY fill 부재로 인한 `syncPositionCache` no-op) 해소 |
| 2026-04-24 | PR-8 /pnl realized+unrealized 분리 표시 (부분매도 누적 실현손익 + 잔량 평가손익 + 총 수익률 동시 표시) | `server/telegram/webhookHandler.ts` | 기존 /pnl 이 unrealized 만 표시해 부분매도 누적 수익이 안 보이던 혼란 해소 |
| 2026-04-24 | PR-9 텔레그램 거래 요약 정상화 (① 리포트 P&L `returnPct→getWeightedPnlPct` fills SSOT 전환 + KST 일자 필터, ② 12:30 정오 점검 섹션 헤더 하드코딩 시간 제거 → unifiedBriefing 단일 타임스탬프, ③ `TELEGRAM_CHANNEL_ID→TELEGRAM_CHAT_ID` 단일 변수 통합 + 브로드캐스트 중복 송신 제거) | `server/alerts/reportGenerator.ts`, `server/alerts/telegramClient.ts`, `server/alerts/alertRouter.ts`, `server/alerts/channelPipeline.ts`, `server/alerts/scanReviewReport.ts`, `server/telegram/webhookHandler.ts` | 일일/정오/장마감 리포트가 신호·결산·P&L 0 으로 표시되던 버그, 한 메시지에 11:30/12:00 시간대 중복 표기, 단일 chat 환경에서 TELEGRAM_CHANNEL_ID 미설정으로 채널 알림 스킵되던 문제 해소 |
| 2026-04-24 | PR-10 DxyIntraday Yahoo 실패 해소 (① `fetchYahooIntradayBars` DXY 호출 range `1d→5d` 로 US 비장중/주말 빈 봉 방어, ② `fetchAvSyntheticDxy` Promise.all→순차 12초 간격 + `ALPHA_VANTAGE_API_KEY` 미설정 시 네트워크 호출 없이 null, ③ `runDxyIntradayMonitor` "모두 실패" 로그를 AV 미설정 시 warn→info 로 다운그레이드, ④ cron 시간대 의도 주석 정리) | `server/alerts/dxyIntradayClient.ts`, `server/alerts/dxyMonitor.ts`, `server/scheduler/alertJobs.ts` | KST 점심(UTC 00~05) cron 에서 Yahoo 1일봉 부족 + AV 동시 6콜 rate-limit 겹쳐 매 5분마다 빨간 "데이터 소스 모두 실패" WARN 이 찍히던 문제 해소 |
| 2026-04-24 | PR-11 장중 매수 경로 반응속도 개선 (① `requestImmediateRescan()` 훅 신설 + `exitEngine` HIT_TARGET/HIT_STOP 전이 시 호출 → 청산 즉시 다음 tick 재스캔, ② `decideScan` 의 maxPositions 를 `signalScanner` 와 동일한 `min(MAX_CONVICTION_POSITIONS, regimeConfig.maxPositions)` 로 정합 + INTRADAY·PRE_BREAKOUT 제외 카운트 일치, ③ `positionAdj` 양방향 보상: ≤50% 점유 시 -1분 가속, ④ 13:00 점심 차단 해제 감지 후 1회 강제 스캔) | `server/orchestrator/adaptiveScanScheduler.ts`, `server/trading/exitEngine.ts` | 익절·손절 직후 빈 슬롯이 최대 10분 방치되던 지연, 스케줄러와 스캐너의 슬롯 한도 불일치, 슬롯 여유 구간에서 재스캔 주기가 관성 유지, 13:00 재개 직후 5분 대기 문제 해소 |
| 2026-04-24 | PR-12 AI 추천·SHADOW STRONG_BUY·Telegram trim 정비 (ADR-0005: ① `generatePreMortem` 응답 sanitize 추가 — Gemini 페르소나 서문 제거 + 번호항목 3개 추출 + 600자 상한, ② `stopLossTransparencyReport` `extractPreMortemLines` 로 번호/하이픈 필터 적용, ③ `CHANNEL_SEPARATOR` 20→16 자 축소로 모바일 wrap 해소, ④ `computeShadowMonthlyStats` 에 STRONG_BUY fallback — profileType A/B + RRR≥3 + R1/R2/R3 강세 레짐 조건으로 레거시 trade 복원, ⑤ `momentumRecommendations` 프롬프트의 STRONG_BUY 기준을 서버 Gate Score≥9/RRR≥3/강세 레짐으로 명시 정렬) | `server/alerts/channelFormatter.ts` + 26개 separator 일괄, `server/alerts/stopLossTransparencyReport.ts`, `server/trading/entryEngine.ts`, `server/trading/sanitizePreMortem.test.ts` (신규), `server/persistence/shadowTradeRepo.ts` (+`isStrongBuyTrade`), `server/persistence/shadowTradeRepo.test.ts` (+ADR-0005 케이스), `src/services/stock/momentumRecommendations.ts`, `docs/adr/0005-strong-buy-and-telegram-trim.md` (신규) | 손절 투명 리포트 Pre-Mortem 본문 "Gate 2" 뒤 잘림·구분선 모바일 2줄 wrap·[SHADOW] STRONG_BUY WIN 0.0% 고착·AI 추천 경로가 자동매매 Gate 기준과 동기화 안 되던 문제 해소 |
| 2026-04-24 | PR-13 QUANT_SCREEN · BEAR_SCREEN Gate 정렬 (ADR-0005 §3 확장: ① `quantScreenRecommendations` 프롬프트에 STRONG_BUY 6축 조건 — combinedScore 상위 30% + ROE≥15% + 부채비율<100% + 기관·외인 5일 순매수 + accumulationPhase='ACCUMULATION' + RRR≥3 + 섹터 대비 저평가, BUY 는 상위 70% + RRR≥2, ② `bearScreenerRecommendations` 카테고리 조건 4→5·배당 4%→5%·ROE 20%→25% 상향 + 기관 5일 순매수·공매도 잔고 감소·RRR≥3 추가 + R6_DEFENSE 레짐에서 일반 STRONG_BUY 부여 금지 — regimePlaybook CONFIRMED_STRONG_BUY 정책 정렬) | `src/services/stock/quantScreenRecommendations.ts`, `src/services/stock/bearScreenerRecommendations.ts`, `docs/adr/0005-strong-buy-and-telegram-trim.md` (§3 업데이트) | AI 추천 모든 경로(MOMENTUM/QUANT_SCREEN/BEAR_SCREEN) 가 서버 자동매매 Gate 기준 및 레짐 Kelly 캡 정책과 정렬되어 AI 추천 결과와 SHADOW STRONG_BUY 분류 기준 일관성 확보 |
| 2026-04-24 | PR-14 스케줄러 카탈로그 재정비 + 조건부 무음 가시화 (① `scheduleCatalog.ts` 에 Telegram 송출 작업 일괄 반영 — 06:15 섹터 ETF 모멘텀·07:30 외인 수급 선행·08:40 DXY·10:45 pre-market HK·16:05 shadow qty dryrun + 주간/월간 리포트 8건 추가, ② `ScheduleEntry.silentWhen` 필드 도입 — 08:30 pre-market card(|Bias|<40 무음), 15:35 INFO digest(버퍼 0건 무음), 16:05 high_52w_scan(편입 0건 무음) 등 "cron 은 돌지만 조건부 무음" 경로를 단일 SSOT 로 명시, ③ `formatSchedulerSummary` 에 🔕 마커 + `formatSchedulerDetail` 에 조건부 무음 사유 표시) | `server/scheduler/scheduleCatalog.ts` | 사용자 "08:30 텔레그램 안 옴" 문의 — 실제로는 `runPreMarketSignal` 의 |Bias Score|<40 NEUTRAL 일 무음 동작. "15:45 안 옴" 문의 — 해당 시각에 등록된 작업 자체가 없음. 운영자가 /scheduler detail 에서 바로 확인 가능 |
| 2026-04-24 | PR-15 일일 리포트 실현 집계 fill SSOT 전환 — 부분매도 이익 누락 버그 해소 (① `collectTodayRealizations` + `summarizeTodayRealizations` 헬퍼 도입 — fill.timestamp 기준 오늘(KST) CONFIRMED/PROVISIONAL SELL 을 전부 모아 부분매도·전량청산 구분 + 가중 pnlPct + 원화 합계 제공, ② `generateDailyReport` / `sendIntradayMarketReport` / `sendPostMarketReport` 가 기존 `signalTime === today` + `HIT_TARGET/STOP` 필터를 버리고 새 SSOT 사용 — ACTIVE 포지션 부분매도 익절이 집계에 포함됨, ③ Gemini 프롬프트에 "실현 상세 부호를 그대로 따라 서술, 부분매도 익절이 있으면 손실만 있었다고 단정 짓지 마라" 지시 추가, ④ 신규 테스트 `todayRealizations.test.ts` — REVERTED 제외, PROVISIONAL 포함, 이월 청산 포함, 사용자 재현 시나리오(현대제철 전량손절 + 포스코인터 부분익절) 6 케이스 전부 통과) | `server/alerts/reportGenerator.ts`, `server/alerts/todayRealizations.test.ts` (신규) | 사용자 "오늘 일부 이익분이 있었음에도 기록은 손실만 보고 있음" 문의 — 어제 진입→오늘 전량 손절된 현대제철만 잡히고 오늘 발생한 부분매도 익절(ACTIVE 유지)이 `closed` 필터에서 통째로 배제되던 버그 해소 |
| 2026-04-24 | PR-16 학습 엔진도 부분매도 실현 반영 (PR-15 연장) — nightlyReflection 에 동일 SSOT 도입 (① `ReflectionInputs.partialRealizationsToday` 필드 신설 — ACTIVE 상태에서 오늘 CONFIRMED SELL fill 이 있는 포지션을 수집, ② `summarizeTodaysRealizationsForLearning` 헬퍼 — fill 단위 승/손, 가중 P&L, 실현 원화, 라벨 제공, ③ `mainReflection.formatNarrativeInput` 재작성 — 전량/부분 실현을 구분 표시 + 가중 P&L + 원화 합계 narrative 주입, SCHEMA_HINT 에 "부분매도 있으면 손실만 단정 금지" 지시 명문화 + dailyVerdict 판정을 fill 기반 승/손 비율로 재정의, ④ Persona Round-Table 이 전량 청산 부재 시 부분매도 포지션을 1순위로 승격, ⑤ Experiment Proposal 의 `lossRatio` 를 fill 단위 (`lossFills / (winFills + lossFills)`) 로 재계산 — 부분익절이 있는 날 잘못된 실험 제안 억제, ⑥ 회귀 테스트 `reflectionPartialRealization.test.ts` 4 케이스 pass) | `server/learning/nightlyReflectionEngine.ts`, `server/learning/reflectionModules/mainReflection.ts`, `server/learning/reflectionPartialRealization.test.ts` (신규) | 자기학습 Gemini narrative 가 오늘 실현된 부분 익절을 보지 못하고 "손실만 있었다" 서술을 반복하던 편향 차단, 편향 heatmap·실험 제안의 lossRatio 왜곡 교정 |
| 2026-04-24 | PR-17 부분매도·트랜치 체결 누락 전수 수정 + BUY side SSOT 도입 (① `collectTodayBuyEvents`·`summarizeTodayBuyEvents` 신설 — 오늘 KST CONFIRMED/PROVISIONAL BUY fill 을 fill.timestamp 기준으로 집계해 신규 진입 vs 트랜치 체결 구분, ② `scanReviewReport` — "매수 N개" 를 scanTrace 카운터에서 실제 fill 기반으로 교체 + "결산 N건" 을 fill SSOT 기반 "실현 N건 (부분매도/전량청산)" 로 확장, "내일 진입 제외" 도 fill 기반 오늘 신규 진입으로 확정, ③ `qualityScorecard` — Trade Yield 의 분모/분자 (todayTradesTotal/Closed/Won/Lost) 전부 fill SSOT 전환, ④ `engineRouter /auto-trade/engine/status` — API 응답의 todayBuys/todayExits 를 fill SSOT 로 교체 → 대시보드가 어제 signaled/오늘 tranche 체결 및 부분매도 둘 다 정확 반영, ⑤ `biasHeatmap` Loss Aversion·Overconfidence 점수 — `partialRealizationsToday` 입력 추가 후 fill 단위 승/손 차감 방식으로 재계산, 부분익절 있는 날 과도한 손실회피 경보 차단, ⑥ 회귀 테스트 `todayBuyEvents.test.ts` 7 케이스 pass — REVERTED 제외/PROVISIONAL 포함/트랜치 분리/비용 합계 등) | `server/alerts/reportGenerator.ts` (+buy SSOT), `server/alerts/scanReviewReport.ts`, `server/alerts/qualityScorecard.ts`, `server/alerts/todayBuyEvents.test.ts` (신규), `server/learning/reflectionModules/biasHeatmap.ts`, `server/learning/nightlyReflectionEngine.ts`, `server/routes/autoTrade/engineRouter.ts` | 사용자 이미지 증거 "스캔 108개 → 매수 5개 / 탈락 103개 · 결산 1건 (승 0 / 패 1)" 에서 오늘 발생한 부분익절 누락 버그와 트랜치 체결 미계상 버그를 전 리포트·대시보드·학습 편향 heatmap 에 걸쳐 일괄 해소 |
| 2026-04-24 | PR-18 주간/전체 리포트·추천 트래커 fill SSOT 확장 (① `shadowTradeRepo.aggregateFillStats(trades, range?)` 신설 — 기간/PROVISIONAL 게이팅 가능한 fill-level 집계 (winFills/lossFills/uniqueTradeCount/fullClosedCount/partialOnlyCount/weightedReturnPct/totalRealizedKrw), ② `weeklyIntegrityReport` 에 "실현 이벤트 (fill 단위)" 섹션 신설 + `WeeklyIntegrityStats` 에 fillWins/fillLosses/fillWeightedReturnPct/fillRealizedKrw/partialOnlyCount 확장 — 이전 주 signaled / 이번 주 부분매도된 건도 주간 리포트에 포함, ③ `shadowProgressBriefing` 에 "🎯 실현 fill" 라인 추가 + `ShadowProgress` 타입 확장 — trade-level 졸업 샘플(30건)과 fill-level 실현 이벤트를 병행 표시, ④ `recommendationTracker` 의 `winRate` 를 `getWeightedPnlPct > 0` 기준 (fill 가중 > 0) 으로 재계산 — 형식적 HIT_STOP 이지만 도중 부분익절로 경제적 순이익인 trade 를 정확히 승으로 반영, ⑤ 회귀 테스트 `aggregateFillStats.test.ts` 5 케이스 — range 필터링·REVERTED 제외·PROVISIONAL 게이팅·부분매도/전량청산 구분) | `server/persistence/shadowTradeRepo.ts` (+aggregateFillStats), `server/persistence/aggregateFillStats.test.ts` (신규), `server/alerts/weeklyIntegrityReport.ts`, `server/alerts/shadowProgressBriefing.ts`, `server/learning/recommendationTracker.ts` | PR-15~17 로 일일 리포트·학습 엔진·대시보드 API 가 fill SSOT 로 정렬됐으나 주간 리포트·SHADOW 졸업 브리핑·월간 winRate 가 여전히 trade status 기준이라 부분익절 기여도가 장기 통계에서 사라지던 문제 해소 |
| 2026-04-24 | PR-19 AttributionRepo 복합키 `(tradeId, fillId)` 전환 — 부분매도별 조건 가중치 학습 스키마 확립 (ADR-0006: ① `ServerAttributionRecord` 에 `fillId?` / `attributionType?: 'FULL_CLOSE' \| 'PARTIAL'` / `qtyRatio?` 3개 옵셔널 필드 추가, ② `CURRENT_ATTRIBUTION_SCHEMA_VERSION` 1→2 + v1 레코드에 `attributionType='FULL_CLOSE'` / `qtyRatio=1.0` 자동 주입하는 마이그레이션 확장, ③ `appendAttributionRecord` dedup 을 `isSameKey` 로 분기 — FULL_CLOSE 는 tradeId 단독, PARTIAL 은 `(tradeId, fillId)` 복합키 → 동일 trade 의 FULL_CLOSE 1건 + PARTIAL N건 병존 가능, ④ `computeAttributionStats` 를 qtyRatio 가중 평균·가중 승률로 재작성 — 50% 부분매도는 weight 0.5 로 기여하여 동일 trade 합이 최대 1.0 을 초과하지 않도록 보장, ⑤ `emitPartialAttribution` 옵셔널 헬퍼 — 기존 FULL_CLOSE baseline 에서 conditionScores 승계 또는 override 로 자립 가능, baseline 없으면 null 반환해 학습 오염 차단, ⑥ 회귀 테스트 `attributionRepoPartial.test.ts` 8 케이스 pass) | `server/persistence/attributionRepo.ts`, `server/persistence/attributionRepoPartial.test.ts` (신규), `docs/adr/0006-attribution-composite-key.md` (신규) | 조건별 가중치 학습 경로가 `tradeId` 유니크 키 제약으로 부분매도별 기여도를 수용할 수 없던 구조적 한계 해소. 후속 PR 로 exitEngine 의 partial SELL fill commit 시점 wiring + `analyzeAttribution` 가중화 진행 예정 |
| 2026-04-24 | PR-20 스캔 회고 "내일 진입 대기" Gate 임계값 + Gemini 응답 페르소나 서문 자동 제거 (① `scanReviewReport.pickTomorrowCandidates` 재작성 — 단순 상위 N개에서 Gate ≥ 9(STRONG_BUY 근접) 우선 + Gate ≥ 7(BUY 근접) 보충 2단 필터로 전환, Gate 3.0 같은 저점수 종목 노이즈 자동 제외, 메시지에 🔥/✅ 뱃지 + "Gate ≥ 7 만 표시" 헤더 + "해당 없음" 안내, ② `geminiClient.stripPersonaPreamble()` 신설 — 응답 상단의 "QuantMaster 시스템 아키텍트로서…/제시한다/분석한다" 메타 서문 자동 제거, 구분선(---/━━━) 스킵, JSON(`{`,`[`)·코드블록·번호리스트·이모지·볼드는 본문 신호로 보호, ③ `GeminiTextOptions.stripPreamble` (기본 true) 도입 — mainReflection 처럼 JSON 을 원본 그대로 처리해야 하는 곳은 false 로 opt-out, ④ 회귀 테스트 `stripPersonaPreamble.test.ts` 8 + `tomorrowCandidates.test.ts` 2 = 10 케이스 pass) | `server/alerts/scanReviewReport.ts`, `server/alerts/tomorrowCandidates.test.ts` (신규), `server/clients/geminiClient.ts` (+stripPersonaPreamble), `server/clients/stripPersonaPreamble.test.ts` (신규) | 사용자 지적: 내일 진입 대기에 Gate 3.0 짜리 종목이 의미 없이 들어가던 현상 제거 + "QuantMaster 시스템 아키텍트로서 …제시한다" 같은 영양가 없는 페르소나 메타 서문이 주간 퀀트 인사이트·기타 리포트 텍스트를 차지하던 문제 일괄 해소 |
| 2026-04-24 | PR-21 KIS 회로차단기 404 완화 — 하드/소프트 이원화 (Railway 로그 증거: 16:30 `[KIS-RealData] API 오류 404 (FHPST01710000)` 직후 회로가 닫혀 실계좌 조회 차단됨. 기존 정책은 404 를 5xx/403 과 동일하게 3회 임계·10분 쿨다운 처리 — KIS 일시 장애·종목 미지원 같은 transient 404 에서도 정상 조회까지 10분간 죽던 현상. ① `CircuitState` 를 `hardFailures` (5xx/403) + `softFailures` (404) 로 이원화, ② 하드 3회→10분 / 소프트 10회→2분 으로 분리 임계값·쿨다운, ③ 두 카운터는 독립 — 5xx 2건 + 404 5건 이 있어도 어느 쪽도 임계 미달이면 미차단, ④ 성공 응답 시 두 카운터 모두 리셋, ⑤ `KIS_LENIENT_404=true` env 스위치 — 404 를 회로 카운팅에서 완전히 제외하고 경고만, ⑥ `getCircuitBreakerStats` 반환에 hardFailures/softFailures/lastBlockedBy 확장 + `consecutiveFailures` 합 별칭 유지, ⑦ 회귀 테스트 `kisCircuit404.test.ts` 8 케이스 pass) | `server/clients/kisClient.ts`, `server/clients/kisCircuit404.test.ts` (신규) | 사용자 보고 "KIS 404 로 주식 정보 조회가 어렵다 — 제약을 풀어야 한다" 에 대응. transient 404 에도 10분 차단되던 과도한 안전장치를 정상 운영 흐름 보호 쪽으로 완화, 하드 장애(5xx/403) 에는 기존 보호 유지 |
| 2026-04-24 | PR-23 UI 디테일 + 외부 호출 예산 감축 (ADR-0009: ① `server/utils/marketClock.ts` 신설 — 서버 SSOT (`isMarketOpen` / `isMarketDataPublished` / `isPostClosePendingPublish` + `DATA_FETCH_FORCE_{MARKET,OFF}` env 오버라이드), ② `kisRankingClient.getRanking` 장외 게이트 — 주말/평일 09:00 이전·15:30 이후는 캐시 hit 이면 TTL 무시 반환, miss 면 `[]` 반환(1분 throttle 로그), `bypassCache=true` 관리자 진단 경로 유지, ③ `krxClient.fetch{InvestorTrading,PerPbr,ShortBalance}` — `resolveTradeDate()` + `previousBusinessDayYYYYMMDD()` 로 `isMarketDataPublished=false` 시 자동 직전 영업일 후퇴(주말/평일 18:00 이전 KRX 400 해소) + bld 연속 5회 실패 → 1시간 soft cooldown, ④ `marketDataRouter /historical-data` in-process LRU(max 500, `symbol:range:interval` 키) — `1d`=1h / 분봉=5m / 그 외 15m, 장외 ×3, ⑤ `reflectionGemini` JSON 파싱 실패 근본원인 교정 — `prependPersona:false` + `stripPreamble:false` + `maxOutputTokens 2048→4096` 으로 페르소나 prelude 제거·JSON 잘림 방지 (aiProvider `TextOnlyOptions` 에 두 옵션 pass-through 추가), ⑥ UI 회귀: `useRecommendationStore.toggleWatchlist` 가 `watchedPrice` fallback (`currentPrice→entryPrice→peakPrice`, 0/NaN → undefined) + `watchedAt` ISO 통일, `useWatchlistSync.fromUserWatchlistItem` 서버 prime 시 비어있는 `watchedPrice` 를 `currentPrice` 로 백필 + legacy `watchedAt` 보정, `WatchlistHeader` Market Sentiment 4개 카드(환율/국채 10년물/미 국채 10년물/달러 인덱스) 조건 `value>0` 통일 — 0 오표시 차단, ⑦ 장외 폴링 완화: `useStockSync` 가 `getCycleMs()` 로 장중 5분 / 장외 15분 분기, ⑧ 회귀 테스트 29 케이스(marketClock 14 + kisRanking gate 2 + krx gate 5 + proxyCache 4 + reflectionGemini 2 + dashboard lint/complexity 무회귀) pass) | `server/utils/marketClock.ts` (신규), `server/utils/marketClock.test.ts` (신규), `server/clients/kisRankingClient.ts`, `server/clients/kisRankingClient.marketClockGate.test.ts` (신규), `server/clients/krxClient.ts`, `server/clients/krxClient.marketClockGate.test.ts` (신규), `server/routes/marketDataRouter.ts`, `server/routes/marketDataRouter.proxyCache.test.ts` (신규), `server/ai/aiProvider.ts`, `server/learning/reflectionIntegrity.ts`, `server/learning/reflectionModules/reflectionGemini.ts`, `server/learning/reflectionModules/reflectionGemini.test.ts` (신규), `src/stores/useRecommendationStore.ts`, `src/hooks/useWatchlistSync.ts`, `src/components/watchlist/WatchlistHeader.tsx`, `src/hooks/useStockSync.ts`, `docs/adr/0009-external-data-call-budget.md` (신규) | 사용자 보고: (1) 관심종목 추가일/추가 금액/변동률 "-" 공란 — `watchedPrice` 추가 시점 0 유입·서버 prime 빈 필드 누적 백필 부재, (2) AI 추천 MARKET SENTIMENT 환율/DXY "0" 표시 — Gemini 응답 JSON 이 preFilledBlock 결측 시 템플릿 0 그대로 복제하던 구조, UI 에서 `value>0` 가드로 "—" 로 치환(근본 해소는 후속 PR 의 ECOS fallback 강화 + 서버 DXY 스냅샷 브릿지), (3) Railway 로그 KIS `FHPST01710000` 404 매 분 누적 — 장외 랭킹 TR 호출 자체 차단으로 근절, (4) KRX `MDCSTAT02203`/`MDCSTAT03501` HTTP 400 — 통계 미확정 시각에 오늘 날짜 쿼리하던 문제 해소, (5) Yahoo 프록시 과호출 (IP 블랙리스트 누적 우려) — `/historical-data` LRU 캐시로 동일 URL 중복 호출 coalescing, (6) 매일 "자기반성 또 실패, Gemini 응답 실패 — 템플릿 fallback" 반복 — 페르소나 prelude 주입·2048 token 잘림이 JSON 파싱을 매번 실패시키던 근본원인 교정 |
| 2026-04-24 | PR-22 자기루프 피드백 정리 (ADR-0007 학습 모듈 폐쇄루프 정책 · ADR-0008 kellyHalfLife 실 사이징 연결: (A1) `HalfLifeSnapshot.effectiveKelly→decayedKelly` rename + `applyHalfLifeDecay` 유틸 신설 + `computeRiskAdjustedSize` 에 옵셔널 `timeDecayInput` 추가 — daysHeld=0 이면 weight=1 이라 신규 진입 등가성 유지, `KELLY_TIME_DECAY_ENABLED=false` 단락 스위치, (A2) `probingBandit.armStatsFromHistory` `RecommendationRecord.profileType` 부재 확인 후 legacy fallback 정책 유지 + signalType:X 가 아닌 armKey 진입 시 프로세스당 1회 warn, `__resetLegacyArmKeyWarningForTests`, (B) `suggestNotifier` 24h dedupe + `LEARNING_SUGGEST_ENABLED` 게이팅 + `suggestThresholds` SSOT + counterfactual/ledger/kellySurface/regimeCoverage 각각 `evaluateXxxSuggestion` + `learningJobs.ts` 가 16:00 counterfactual / 16:15 ledger resolve 직후 suggest 평가 호출, (C) `syntheticReplay.ts` (153줄, import 0건) 삭제 — `newsLagBayesian.ts` 는 `newsSupplyLogger` + `webhookHandler` 에서 실제 import 되어 있음이 확인되어 scope 에서 제외) | `server/trading/kellyHalfLife.ts`, `server/trading/accountRiskBudget.ts`, `server/trading/accountRiskBudgetTimeDecay.test.ts` (신규), `server/learning/probingBandit.ts`, `server/learning/probingBanditArmKey.test.ts` (신규), `server/learning/suggestNotifier.ts` (신규), `server/learning/suggestThresholds.ts` (신규), `server/learning/suggestNotifier.test.ts` (신규), `server/learning/counterfactualShadow.ts`, `server/learning/counterfactualSuggest.test.ts` (신규), `server/learning/ledgerSimulator.ts`, `server/learning/ledgerSuggest.test.ts` (신규), `server/learning/kellySurfaceMap.ts`, `server/learning/kellySurfaceSuggest.test.ts` (신규), `server/learning/regimeBalancedSampler.ts`, `server/learning/regimeCoverageSuggest.test.ts` (신규), `server/scheduler/learningJobs.ts`, `docs/adr/0007-learning-feedback-loop-policy.md` (신규), `docs/adr/0008-kelly-time-decay-wiring.md` (신규), `server/learning/syntheticReplay.ts` (삭제), `.env.example` | 사용자 자기루프 감사 결과 (1) kellyHalfLife 시간감쇠가 실제 사이징 경로에 미연결 + `effectiveKelly` 변수명 동명이인, (2) probingBandit armKey 가 `signalType:profileType` 생성이지만 매칭은 signalType 만 하여 A/B/C arm 해상도 손실, (3) counterfactual/ledger/kellySurface/regimeCoverage 4개 학습 모듈이 통계만 돌고 결과가 Gate/Kelly/레짐에 feedback 안 되던 "조회 전용" 상태, (4) syntheticReplay 완전 고립 153줄 정리 — ADR 로 폐쇄루프 정책 명문화 + 하이브리드 (임계 충족 시 텔레그램 suggest 알림, 실제 반영은 Phase 2 수동 승인) 로 해소 |
| 2026-04-24 | PR-24 외부 호출 예산 강화 3종 (ADR-0010: ① `marketDataRouter` 주말 KR 게이트 미들웨어 — `/historical-data` 진입 직후 KR 심볼(`.KS$/.KQ$/^\d{6}$`) + KST 주말(토/일) 분기, 캐시 hit → `X-Cache: STALE-WEEKEND` 로 stale 서빙, miss → 204 No Content 로 새 outbound 차단, 평일·US 심볼은 `next()` 통과, 판정 로직은 `evaluateWeekendGate(symbol, range, interval, now)` 순수 함수로 분리해 단위 테스트 가능, ② `marketDataRouter` Yahoo 프록시 in-flight coalescing — `_yahooInflight: Map<cacheKey, Promise>` 도입, 캐시 set 이전 윈도우의 동시 요청은 진행 중인 Promise 에 편승 → `X-Cache: COALESCED` 헤더, finally 에서 Map 청소, `fetchYahooHistorical(...)` 헬퍼로 outbound 본체 분리, ③ KIS 404 엔드포인트 24h 영속 블랙리스트 — `kisEndpointBlacklistRepo.ts` 신설 (30분 슬라이딩 윈도우 / 10회 임계 / 24h 차단, 메모리 + Volume JSON 이중 저장 + debounce flush), `kisClient._isCircuitOpen` 가 블랙리스트 우선 체크 → SOFT 쿨다운 2분 만료 후에도 24h 동안 호출 차단 유지, `_recordCircuitFailure(trId, 404)` 가 `recordEndpoint404` 동시 호출, `_recordCircuitSuccess` 가 윈도우 카운터 리셋, `resetKisCircuits` 가 블랙리스트도 함께 청소, `KIS_DISABLE_404_BLACKLIST=true` env 탈출구, ④ 부팅 시 `loadKisEndpointBlacklist()` 호출로 만료 entry 자동 청소 후 활성 entry 재로드, ⑤ 회귀 테스트 28 케이스 pass — weekendGate 8 + coalescing 2 + blacklistRepo 11 + circuit↔blacklist wiring 7) | `server/routes/marketDataRouter.ts`, `server/routes/marketDataRouter.weekendGate.test.ts` (신규), `server/routes/marketDataRouter.coalescing.test.ts` (신규), `server/persistence/kisEndpointBlacklistRepo.ts` (신규), `server/persistence/kisEndpointBlacklistRepo.test.ts` (신규), `server/persistence/paths.ts` (+`KIS_ENDPOINT_BLACKLIST_FILE`), `server/clients/kisClient.ts`, `server/clients/kisCircuitBlacklist.test.ts` (신규), `server/index.ts`, `docs/adr/0010-external-call-budget-hardening.md` (신규) | Railway 로그 분석: (1) ADR-0009 (PR-23) 가 장외 LRU TTL ×3 연장만 적용했으나 주말 KR 심볼 새 폴링은 여전히 outbound, (2) `useStockSync` 가 동일 cacheKey 를 짧은 윈도우에 N회 쿼리하면 outbound 도 N회 (캐시 set 이전 race), (3) PR-21 SOFT 회로 2분 쿨다운 만료 후 같은 죽은 엔드포인트(예: 장외 랭킹 TR FHPST01710000) 를 다시 두드려 카운터 누적 무한 반복 + 재배포 시 메모리 카운터 리셋되어 처음부터 재시작 — 영속 블랙리스트로 24h 단위 차단하여 근절 |
| 2026-04-24 | PR-25-A AI 추천 경로 KIS/KRX 분리 인프라 단계 (ADR-0011, B+C 하이브리드 권장안: ① `server/services/aiUniverseService.ts` 신규 — AI 추천 universe 발굴 + enrichment 단일 통로, KIS/KRX 직접 호출 금지, MOMENTUM/QUANT_SCREEN/BEAR_SCREEN/EARLY_DETECT 4 mode 별 Google 쿼리 매핑 + 출처 다양성 정렬 + Naver Finance enrichment 병렬 4건, ② `server/clients/googleSearchClient.ts` 신규 — Google Custom Search JSON API 단일 통로, `KOREAN_FINANCE_WHITELIST` (m.stock.naver.com / finance.naver.com / hankyung.com / mk.co.kr / sedaily.com / infostock.co.kr) 6개 도메인 site: OR 절 강제, hl=ko gl=kr, 8s timeout, NOT_CONFIGURED/BUDGET_EXCEEDED/ERROR/GOOGLE_CSE 4 source 분기, ③ `server/clients/naverFinanceClient.ts` 신규 — m.stock.naver.com/api/stock/{code}/integration 모바일 endpoint, 현재가/PER/PBR/EPS/BPS/시총/외인비율/배당률 추출, 6s timeout, iPhone Safari UA, ④ `server/persistence/krxStockMasterRepo.ts` 신규 — 부팅·24h TTL 만료 시 1회 KRX MDCSTAT01901 다운로드 → data/krx-master.json 영속, longest-match 종목명 추출(매치 영역 placeholder 마스킹), AI 추천·자동매매 공유 (절대 규칙 #2 무위반 — KRX 공개 통계 endpoint), ⑤ `server/persistence/aiCallBudgetRepo.ts` 신규 — bucket 별(google_search 80 / naver_finance 1000 / krx_master_refresh 5) 일일 호출 카운터, KST 자정 자동 리셋, AI_DAILY_CALL_BUDGET env override (0 도 유효), debounce flush, ⑥ `server/persistence/paths.ts` (+`KRX_STOCK_MASTER_FILE`, `AI_CALL_BUDGET_FILE`), ⑦ `.env.example` (+ GOOGLE_SEARCH_API_KEY / GOOGLE_SEARCH_CX / AI_DAILY_CALL_BUDGET 3 변수), ⑧ ARCHITECTURE.md (+ aiUniverseService boundary, googleSearchClient/naverFinanceClient/krxStockMasterRepo 모듈 등록), ⑨ CLAUDE.md 절대 규칙 #3 분기 — `stockService` (자동매매·서버 스크리너) vs `aiUniverseService` (AI 추천), ⑩ 회귀 테스트 31 케이스 pass — krxStockMasterRepo 8 + aiCallBudgetRepo 9 + googleSearchClient 7 + aiUniverseService 7. PR-25-B 에서 momentum/quantScreen/bearScreen 추천 경로 wiring + enrichment.ts KRX valuation 호출 대체 예정.) | `server/services/aiUniverseService.ts` (신규), `server/services/aiUniverseService.test.ts` (신규), `server/clients/googleSearchClient.ts` (신규), `server/clients/googleSearchClient.test.ts` (신규), `server/clients/naverFinanceClient.ts` (신규), `server/persistence/krxStockMasterRepo.ts` (신규), `server/persistence/krxStockMasterRepo.test.ts` (신규), `server/persistence/aiCallBudgetRepo.ts` (신규), `server/persistence/aiCallBudgetRepo.test.ts` (신규), `server/persistence/paths.ts`, `.env.example`, `ARCHITECTURE.md`, `CLAUDE.md`, `docs/adr/0011-ai-recommendation-source-split.md` (신규) | 사용자 요청: "AI 종목 추천은 KIS·KRX 안 쓰고 Google searching 으로 찾을 것 — KIS/KRX 는 자동매매 전용. 비용 최소화." 옵션 A(Gemini grounding)는 수치 hallucination + grounding fee + 응답 지연으로 부적합 판정 → B(Custom Search 화이트리스트) + C(Naver Finance 모바일) 하이브리드 채택. 자동매매(signalScanner/entryEngine/autoTradeEngine) 코드 무변경 — quota 침범 차단. PR-25-B 에서 추천 경로 wiring 까지 완료해야 사용자 효과 가시화 |
| 2026-04-24 | PR-25-B AI 추천 경로 wiring (ADR-0011 §6 Phase B): ① `server/routes/aiUniverseRouter.ts` 신규 — `/api/ai-universe/discover?mode=...` (universe 발굴) + `/api/ai-universe/snapshot?code=...` (단일 enrichment, 기존 `/api/krx/valuation` drop-in 대체) + `/api/ai-universe/snapshots?codes=A,B,C` (다중 prefetch) + `/api/ai-universe/budget` (운영 모니터) + `/api/ai-universe/enriched?code=...` 5 endpoints. 시총 한국식 단위 표기 helper `formatMarketCapKr` 분리, ② `server/index.ts` 에 `/api/ai-universe` 마운트, ③ `src/api/aiUniverseClient.ts` 신규 — `discoverAiUniverse` / `fetchAiUniverseSnapshot` / `fetchAiUniverseSnapshots` 3 함수 + 타입 export, ④ `src/services/stock/enrichment.ts` `fetchKrxValuation` 본체 — `/api/krx/valuation` fetch 제거, `fetchAiUniverseSnapshot` dynamic import 로 교체. 캐시·schema 호환 유지로 `enrichStockWithRealData` / `enrichLeadingStocksMarketCap` 호출자 변경 0. 결과적으로 `quantScreenRecommendations` / `bearScreenerRecommendations` 도 자동 wiring (enrichment 만 의존), ⑤ `src/services/stock/momentumRecommendations.ts` 본체 재작성 — `fetchKisRanking` 호출 3건 (volume/fluctuation/market-cap) 제거 + `prefetchValuations` 헬퍼 제거, `discoverAiUniverse(MOMENTUM|EARLY_DETECT, {maxCandidates:20, enrich:true})` 1회 호출로 통합. mode 매핑 `toUniverseMode`, candidate 평탄화 `flattenCandidates(AiUniverseDiscoverResult|null) → MomentumCandidate[]` 분리해 단위 테스트 가능. SMALL_MID_CAP 은 KOSDAQ 우선 정렬로 단순화 (시총 정밀 필터는 프롬프트 위임), 예산 초과 시 candidateBlock 에 명시적 안내 메시지, ⑥ 회귀 테스트 11 케이스 신규 — `aiUniverseRouter.formatMarketCapKr` 5 + `momentumRecommendations` flatten/mode 6. 누적 PR-25-A+B 42 케이스 pass. 자동매매(signalScanner/entryEngine/autoTradeEngine/exitEngine) 코드 무변경 — quota 침범 차단 효과 즉시 가시화. PR-25-C 잔여 범위: `enrichment.fetchKisSupply` / `fetchKisShortSelling` 분리 + dead `fetchKisRanking` 정리 + Gemini grounding narrative 보조 옵션. | `server/routes/aiUniverseRouter.ts` (신규), `server/routes/aiUniverseRouter.test.ts` (신규), `server/index.ts`, `src/api/aiUniverseClient.ts` (신규), `src/services/stock/enrichment.ts`, `src/services/stock/momentumRecommendations.ts`, `src/services/stock/momentumRecommendations.wiring.test.ts` (신규) | 사용자 요청 후속: PR-25-A 인프라가 추천 경로에 실제 연결되어 사용자 효과 가시화 — AI 추천 호출이 KIS/KRX quota 를 더 이상 소비하지 않음. 기존 호출 비용: 추천 1회당 KIS rank 3 + KRX valuation 30 = 33 회 → 신규: google_search 2 + naver_finance 12 = 14 회 (분리 통로, KIS 미사용) |
| 2026-04-24 | PR-25-C AI 추천 KIS 의존 완전 제거 + 비용 텔레메트리 (ADR-0011 §6 Phase C): ① `src/services/stock/enrichment.ts` — `fetchKisSupply`/`fetchKisShortSelling` 호출 제거, `fetchAiUniverseSnapshot` 1회 호출로 대체 + `buildSnapshotSupplyStub` 헬퍼 신설 (Naver `foreignerOwnRatio` 만 실제값, 일별 순매수·공매도 잔고는 0/빈배열로 AI 자체 판단 위임 — PR-13 프롬프트 정렬 유지), ② `src/services/stock/quantScreener.ts` `detectSilentAccumulation` — KIS 호출 2건 제거, `fetchAiUniverseSnapshot` N건으로 교체, kisDataBlocks 재작성 (외인 지분율/PER/PBR/시총 4항목 + "일별 순매수·공매도 추세는 AI 자체 판단 영역" 명시), ③ `src/services/stock/kisDataFetcher.ts` 전체 삭제 (103 LoC dead code 제거) + `src/services/stockService.ts` 의 `export * from ./stock/kisDataFetcher` 제거 — AI 추천 경로 KIS 호출자 0건 확정, ④ `server/persistence/aiCallBudgetRepo.ts` 임계 경보 — `ALERT_THRESHOLDS_PCT = [80, 95, 100]` + `DailyBudgetState.alertedThresholds` 필드로 bucket/임계 별 dedupe + `BudgetAlertHook` 타입 + `setBudgetAlertHook` setter + `tryConsume`/`recordCall` 에 `checkThresholds` wiring (한도 0 예외, hook 예외 안전 처리), ⑤ `server/index.ts` 부팅 시 `setBudgetAlertHook` 등록 — 80/95/100% 별 아이콘(⚠️/🚨/🛑) + bucket/used/limit 표시 텔레그램 알림, ⑥ 회귀 테스트 13 케이스 신규 — threshold 9 + snapshotStub 4. 누적 PR-25-A+B+C 55 케이스 pass. 자동매매(server/clients/kisClient.ts + signalScanner/entryEngine/autoTradeEngine/stockScreener) 코드 무변경 — KIS/KRX 는 자동매매 전용으로 100% 격리 완료. | `src/services/stock/enrichment.ts`, `src/services/stock/enrichment.snapshotStub.test.ts` (신규), `src/services/stock/quantScreener.ts`, `src/services/stock/kisDataFetcher.ts` (삭제), `src/services/stockService.ts`, `server/persistence/aiCallBudgetRepo.ts`, `server/persistence/aiCallBudgetRepo.threshold.test.ts` (신규), `server/index.ts` | 사용자 요청 최종: "AI 종목 추천은 KIS·KRX 안 쓸 것 — 자동매매에만." ADR-0011 3 단계(A 인프라 → B wiring → C KIS 완전 제거) 완주. AI 추천 1회 호출 비용: KIS rank 3 + KIS supply N + KIS short N + KRX valuation N = ~40회 → google_search 2 + naver_finance N (≤12) = ~14회 (모두 분리 통로). 80/95/100% 예산 도달 시 텔레그램 1회 알림으로 쿼터 폭주 조기 감지. |

| 2026-04-25 | PR-30 KrxStockMaster CSV 0건 진단+교정 (① 원인 가설: `refreshKrxStockMaster` 의 fileDn 엔드포인트가 `krxPost` 와 달리 `Content-Type` 헤더만 붙이고 있어 KRX 가 bot 판정·빈 응답 반환 가능성. ② 헤더 보강 — `krxPost` 와 동일한 `Accept` / `User-Agent` / `Referer` / `Origin` 4종 추가, ③ URL 상수화 — 하드코딩 `http://data.krx.co.kr` 을 `KRX_PUBLIC_API_BASE` env 오버라이드 지원 상수로 교체, ④ OTP `.trim()` + 빈 문자열 조기 반환 — 빈 OTP 로 download_csv 를 때려 빈 CSV 받던 흐름 차단, ⑤ 진단 로그 2종 — `[KrxMaster:diag] OTP status=... otp.len=... otp.head="..."` + `CSV status=... bytes=... head="..."` 로 다음 평일 갱신 시 원인 즉시 식별, ⑥ 0건 warn 메시지에 `bytes=` 포함 — HTML 오류 페이지(큰 bytes) vs 빈 응답(0 bytes) 구분 가능) | `server/persistence/krxStockMasterRepo.ts` | 사용자 지적: PR-28 의 "주말 단락" 이 평일에 재현될 수 있는 실제 버그를 가릴 가능성. 옵션 A(진단 로그) + B(헤더 교정) 동시 채택 — 다음 평일 09:00~18:00 창에서 `[KrxMaster:diag]` 로그 1회 관찰 후 원인 확정. OTP 빈 응답이면 추가 세션 쿠키/multi-step 인증 필요, CSV HTML 페이지면 referer/UA 검증 실패 확정 |
| 2026-04-25 | PR-29 Egress Guard 도입 — outbound HTTP 최종 관문 (⑥ 핵심 구조 변경: Express 미들웨어는 HTTP 진입점만 커버하지만, 서버 내부 11개 경로가 `fetch()` 로 Yahoo 를 직접 호출해 시장 게이트를 우회하던 구조 해소. ① `server/utils/egressGuard.ts` 신설 — `evaluateEgress(url, now?)` 순수 함수 + `guardedFetch(input, init?)` wrapper. URL.host 를 HOST_RULES 에 매칭해 Yahoo chart(`/v[78]/finance/chart/{symbol}`) + quoteSummary(`/v\d+/finance/quoteSummary/{symbol}`) 에서 심볼 추출 → `classifySymbol()` + `isMarketOpenFor()` 판정 → 닫혀있으면 503 synthetic Response 반환(`X-Egress-Guard: market-closed` 헤더 + body `{gated:true, symbol, market, reason}`). 미등록 host(KIS/KRX/ECOS/FRED) 는 pass-through, ② `EGRESS_GUARD_DISABLED=true` 긴급 롤백 스위치, ③ 심볼별 60s throttle debug 로그 — `[EgressGuard] skip NYSE:^VIX — NYSE closed`, ④ 11개 파일 wiring — `marketDataRouter.fetchYahooHistorical` + `/market-indicators` inline + `alerts/dxyIntradayClient` + `alerts/sectorEtfMomentum` + `learning/backtestEngine` + `learning/lateWinEvaluator` + `screener/sectorSources` + `screener/stockScreener` + `telegram/webhookHandler(^KS11 probe)` + `trading/marketDataRefresh` + `trading/pipelineDiagnosis` + `clients/koreanQuoteBridge`. 각 파일은 `fetch()` → `guardedFetch()` 이름만 변경(RequestInit signature 동일). 기존 `if (!res.ok) return null` 패턴이 503 을 자연스럽게 null 폴백으로 흡수, ⑤ 테스트 15 케이스 — URL 파싱(query1/query2/encoded %5EKQ11/quoteSummary) + 주말/평일 장외/장중 판정 + 미등록 host pass + synthetic 503 body + EGRESS_GUARD_DISABLED env 탈출. 누적 99 케이스 pass) | `server/utils/egressGuard.ts` (신규), `server/utils/egressGuard.test.ts` (신규), `server/routes/marketDataRouter.ts`, `server/alerts/dxyIntradayClient.ts`, `server/alerts/sectorEtfMomentum.ts`, `server/learning/backtestEngine.ts`, `server/learning/lateWinEvaluator.ts`, `server/screener/sectorSources.ts`, `server/screener/stockScreener.ts`, `server/telegram/webhookHandler.ts`, `server/trading/marketDataRefresh.ts`, `server/trading/pipelineDiagnosis.ts`, `server/clients/koreanQuoteBridge.ts` | 사용자 이미지(Apr 25 KST 토요일 ^KQ11 Yahoo 프록시 발생) 가 시사한 구조적 한계 — marketDataRouter 미들웨어가 `/api/historical-data` 만 커버하지만 서버 스케줄러(dxyMonitor/sectorEtfMomentum/marketDataRefresh 등) 의 내부 fetch 는 우회. Egress Guard 가 outbound 레이어 단일 관문으로 작동해 "어디서 호출하든 한 지점에서 차단" 보장. 11 경로 누출 완전 봉합 |
| 2026-04-25 | PR-28 주말 소음 완전 차단 (Railway 로그 증거 기반: Apr 25 02:32 UTC(KST 토 11:32) 에 ^KQ11 Yahoo 프록시 + [KrxStockMasterRepo] CSV 0건 + [AiUniverseService] warn + [KRX] MDCSTAT02203 HTTP 400 이 동시 발생. ① marketClock.ts `isKstWeekend(now?)` export 신설 — 주말 분기 SSOT, ② `krxStockMasterRepo.refreshKrxStockMaster()` 진입점 주말 단락 — 디스크 캐시 있으면 로드 후 true, 없으면 false 반환. KRX CSV 다운로드 자체 스킵으로 "CSV 파싱 결과 0건" 원천 제거, ③ `aiUniverseService.ts` "마스터 갱신 실패" 로그를 `isKstWeekend() ? console.debug : console.warn` 분기 — 주말엔 정상 동작이므로 debug, ④ `krxClient.krxPost` 400 핸들러에 주말 silent 분기 — `res.status===400 && isKstWeekend()` 이면 로그·cooldown 기록 없이 return null. 기존 Tier 2 `!isMarketDataPublished()` 분기는 평일 18:00 이전만 커버했던 버그 해소 (marketClock.isMarketDataPublished 가 주말엔 true 반환하므로 주말 분기가 필요), ⑤ 회귀 테스트 3 케이스 신규 — marketClock.isKstWeekend 3 + krxStockMasterRepo 주말 단락 2(현재 KST 가 주말인 경우에만 assertion 타는 조건부). 누적 84 케이스 pass) | `server/utils/marketClock.ts`, `server/utils/marketClock.test.ts`, `server/persistence/krxStockMasterRepo.ts`, `server/persistence/krxStockMasterRepo.test.ts`, `server/services/aiUniverseService.ts`, `server/clients/krxClient.ts` | 사용자 제공 이미지(Apr 25 2026 02:32 UTC 로그) 기반 — (1) ^KQ11 은 내부 호출이 아니라 미들웨어 gate 를 이미 타지만 Tier 1 배포 전 로그였고, (2) 핵심 주말 소음 원천 3개(KrxStockMasterRepo 0건 / AiUniverseService warn / KRX 400 warn) 를 isKstWeekend SSOT 분기로 일괄 차단. 평일 동작은 전혀 변화 없음 |
| 2026-04-24 | PR-27 Tier 2 시장 게이트 관측성·로그 위생·경계 가드 (① `symbolMarketRegistry.nextOpenAt(market, now)` + `nextOpenAtFor(symbol, now)` 추가 — `now` 기준 8일 내 다음 정규장 개장 시각을 UTC `Date` 로 반환. FORCE_* env 무시하고 실제 시간표로만 계산 (진단·응답 헤더 전용), ② `marketDataRouter` stale/skip 응답에 `X-Market-Next-Open: <ISO>` 헤더 부가 — 프론트엔드 retry 스케줄링 근거. pass 응답(장중)에는 미부가, ③ `krxClient.krxPost` HTTP 400 + `!isMarketDataPublished(now)` 케이스를 `console.warn` → `console.debug` 강등 — 주말·평일 18:00 이전 `resolveTradeDate` 후퇴 창에서 KRX 가 bld 별 간헐 400 을 반환해도 soft cooldown 이 이미 돌고 있어 정보 가치 없음. 장중 실패(401/403/5xx/timeout)는 warn 유지, ④ `scripts/check_symbol_boundary.js` 신규 — KR 복합 티커 패턴 `\.KS$|\.KQ$` 와 KR 지수 분류 `\^(?:KS11|KQ11|VKOSPI)` 시그니처가 `server/utils/symbolMarketRegistry.ts` 밖에서 발견되면 FAIL. `npm run validate:boundary` + `validate:all` + `precommit` 에 연결. "4.1 역량의 역설" 방어선 — 신규 시장 regex 를 marketDataRouter 에 직접 추가하는 회귀 차단, ⑤ 회귀 픽스처 테스트 19 케이스 추가 — 문제 심볼 12개 전수 분류 고정(^KS11/^KQ11/^VKOSPI/005930.KS/035420.KQ/035420/^VIX/^TNX/^IRX/AAPL/EWY/MTUM) + nextOpenAt 6 시나리오(KRX 장 마감 직후/주말/장중 + NYSE 마감 후/금요일 주말 건너뜀/nextOpenAtFor 심볼 API) + NYSE ET 18:00 skip 1건. 누적 65 케이스 pass) | `server/utils/symbolMarketRegistry.ts`, `server/utils/symbolMarketRegistry.test.ts`, `server/routes/marketDataRouter.ts`, `server/clients/krxClient.ts`, `scripts/check_symbol_boundary.js` (신규), `package.json` | 사용자 "연결문제 개선 Tier 2" — (⑤) 장외 응답 시 프론트엔드가 재시도 타이밍을 서버 힌트로 계산, (⑥) Railway 로그 "주말 KRX 400" 소음 제거, (⑦) 실제 fetch 되는 10+ 심볼 분류가 조용히 망가지는 회귀 방지, (⑧) marketDataRouter 에 regex 를 직접 덧붙이는 관성 차단 — CI 가드로 "regex 는 Registry 에만" 규약 강제 |
| 2026-04-24 | PR-26 Tier 1 SymbolMarketRegistry 신설 — 시장 게이트 일반화 (① `server/utils/symbolMarketRegistry.ts` 신규 — 심볼→시장(KRX/NYSE/TSE) 분류 + 시장별 정규장 시간표 SSOT. `classifySymbol(symbol)` `isOpenAt(market,now)` `isMarketOpenFor(symbol,now)` `listMarkets()` API. KR 티커(.KS/.KQ/6자리) + KR 지수(^KS11/^KQ11/^VKOSPI) → KRX, `.T` 접미사 → TSE, 그 외 → NYSE 기본값. `DATA_FETCH_FORCE_{MARKET,OFF}` env 오버라이드 유지, ② `server/routes/marketDataRouter.ts` — `KR_SYMBOL_PATTERN` + `isKstWeekend` + `evaluateWeekendGate` 제거 → `evaluateMarketGate(symbol,range,interval,now)` 단일 함수로 치환. 내부 판정은 `isMarketOpenFor(symbol,now)` 한 줄 위임. `WEEKEND_GATED_PATHS` → `MARKET_GATED_PATHS` 리네임, X-Cache 헤더 `STALE-WEEKEND`/`WEEKEND-SKIP` → `STALE-OFFHOURS`/`OFFHOURS-SKIP` 일반화, ③ 게이트 의미 확장 — 기존엔 "KR 심볼 + KST 주말"만 차단했으나 이제 "해당 심볼이 속한 시장의 정규장이 닫힌 모든 경우" (US 심볼의 ET 17:00~09:30 장외, KR 심볼의 평일 15:30 이후 등) 차단. 외부 호출 예산 게이트 의도와 이름이 일치, ④ 회귀 테스트 21 케이스 신규 — symbolMarketRegistry 12(분류 5+KRX 2+NYSE 2+TSE 1+통합 3+env override 3+listMarkets 1) + marketGate 9(KRX 장중/장외/주말+stale/miss, NYSE 장중/장외, KR 지수, 만료 캐시, ET 평일 장외). 기존 `marketDataRouter.weekendGate.test.ts` 삭제 후 `marketGate.test.ts` 로 리네임, ⑤ `npm run precommit` 전체 통과 — lint + validate:all + complexity + responsibility + exposure + gemini) | `server/utils/symbolMarketRegistry.ts` (신규), `server/utils/symbolMarketRegistry.test.ts` (신규), `server/routes/marketDataRouter.ts`, `server/routes/marketDataRouter.marketGate.test.ts` (리네임), `server/routes/marketDataRouter.weekendGate.test.ts` (삭제) | 사용자 요청 "연결문제 개선 Tier 1": (1) 심볼→시장 매핑이 marketDataRouter 에 regex 로 숨어있어 신규 시장 추가 시 미들웨어 수정이 필요하던 구조적 문제, (2) "evaluateWeekendGate" 이름이 실제로는 평일 장외도 차단해야 함에도 "weekend" 로 좁아 구조를 거짓 표현하던 네이밍 문제, (3) US 심볼(AAPL/^VIX)도 ET 17:00~23:30 에 outbound 를 호출해 Yahoo 예산을 낭비하던 누수 해소. Tier 2(nextOpenAt, KRX 주말 400 로그 debug 강등, validate:responsibility 훅 강화) + Tier 3(클라이언트 marketTime 과 SSOT 공유, /markets 텔레그램 명령, failurePatternDB infra 필드) 는 본 PR 범위 밖 |
| 2026-04-25 | PR-31 장외 UX 정합성 + AI 추천 박제 cache 자동 무효화 (사용자 보고 3건: ① "주도주 분석 시작" 여전히 동작 안 함 — 직전 PR-30/30+ 이 빈 응답 write 만 스킵하고 read 경로는 그대로 반환해 이전 박제된 빈 cache 가 4h+24h 동안 계속 서빙되던 구조 해소, ② 종목 카드 LIVE 라벨이 stale 캐시일 때도 그대로 표시되어 사용자에게 잘못된 신뢰를 주던 문제, ③ DEEP ANALYSIS 차트 OFFHOURS 응답(204)에 빨간 "가격 데이터를 불러올 수 없습니다." 만 표시되던 빈약한 안내 — 사용자 입장에선 "차트 깨짐" 으로 인지. 수정: ① `aiClient.getCachedAIResponse` read 경로 3층(메모리/localStorage/서버Volume) 모두 `isEmptyRecommendationData` 가드 적용 + 빈 entry 발견 시 자동 무효화(메모리 delete / localStorage removeItem / 서버 DELETE) 후 fetchFn 재호출, ② `aiCacheRepo.deleteCacheEntry` + `systemRouter.delete('/system/ai-cache/:key')` 신규 — 서버 Volume 영속 박제도 무효화 가능, ③ `historicalData.fetchHistoricalData` 가 204 No Content 를 OFFHOURS sentinel 로 명확 분류 + `X-Market-Next-Open` 헤더 전파 — 이전엔 `response.json()` 빈 body 파싱 예외 → retry → null 로 "데이터 못 불러옴" 과 동일 취급되던 정보 손실 해소. legacy signature 호환을 위해 overload 시그니처 + `withMeta:true` 옵션 도입, ④ `CandleChart` OFFHOURS 분기 — 빨간 에러 대신 주황 "장외 시간 · 다음 개장 KST 월 09:00" 안내 카드 + 차트 영역 hide, ⑤ `WatchlistCard` LIVE 배지 → `isMarketOpenFor(stock.code)` 분기로 "LIVE(주황·pulse)" / "장외(파랑·정적)" 토글, title attribute 에 다음 개장 시각 hover 노출, ⑥ `useRecommendationStore.recommendationWarnings` state 신설 + `useStockSearch.fetchStocks` 가 universe 발굴 warnings 를 store 에 저장, ⑦ `RecommendationWarningsBanner` 신규 — toast 8초 dismiss 후에도 사유 영구 표시, ⑧ `WatchlistHeader` 에 `OffHoursBanner` 다음 줄 배치 + 사용자 닫기 버튼 제공, ⑨ `naverFinanceClient` 5분 negative cache — 4xx(409 throttle/410 폐지) 응답 시 동일 코드 outbound 차단으로 일일 예산 보존, 5xx 는 transient 로 미적용. ⑩ 회귀 테스트 12 케이스 신규 — historicalData 5(204+nextOpen+OFFHOURS skip retry+legacy 호환 2) + aiClient 빈 cache read 3 + naverFinance negative cache 4. 누적 신규 12 + 정합 45 케이스 pass) | `src/services/stock/aiClient.ts`, `src/services/stock/aiClient.emptyCache.test.ts` (신규), `src/services/stock/historicalData.ts`, `src/services/stock/historicalData.offhours.test.ts` (신규), `src/components/analysis/CandleChart.tsx`, `src/components/watchlist/WatchlistCard.tsx`, `src/components/watchlist/WatchlistHeader.tsx`, `src/components/common/RecommendationWarningsBanner.tsx` (신규), `src/stores/useRecommendationStore.ts`, `src/hooks/useStockSearch.ts`, `server/clients/naverFinanceClient.ts`, `server/clients/naverFinanceClient.negativeCache.test.ts` (신규), `server/persistence/aiCacheRepo.ts`, `server/routes/systemRouter.ts` | Railway 토요일 로그 + 사용자 스크린샷 3장(EgressGuard skip 누적 / 삼성전자 STRONG_BUY 카드 LIVE 라벨 / DEEP ANALYSIS "가격 데이터를 불러올 수 없습니다." 빨간 텍스트). 직전 PR 의 "write 시 빈 응답 스킵" 만으로는 이미 적재된 빈 cache 를 무효화하지 못해 사용자 체감으로는 "여전히 동작 안 함". read 경로 3층 가드 + 영속 cache DELETE endpoint 로 박제 cache 가 다음 분석 실행 시 자동 정화. 장외 시 LIVE 라벨/빨간 차트 에러 → 시장 시간 SSOT 기반 명확한 OFFHOURS UX 로 정합성 확보 |
| 2026-04-25 | AI 추천 "완료만 뜨고 아무것도 안 나옴" 수정 — universe 0건 dead-zone 해소 (증상: 사용자가 AI 추천 버튼을 누르면 즉시 "완료" 토스트가 뜨고 추천 목록이 비어있음. 원인: ① `GOOGLE_SEARCH_API_KEY`/`CX` 미설정 시 `googleSearch()` 가 `NOT_CONFIGURED` 반환 → `discoverUniverse` 가 빈 candidates 로 리턴, ② `momentumRecommendations` 의 Gemini 프롬프트가 "이 목록 외 종목은 추천 금지" 하드 룰을 유지해 빈 candidates → 빈 `recommendations`, ③ `getCachedAIResponse` 가 빈 결과를 4h localStorage + 24h 서버 Volume 캐시에 박제 → 하루 종일 "즉시 완료 + 빈 목록" 반복. 수정: ① `aiUniverseService.ts` 에 `SEED_UNIVERSE` 24개 KR 시총 상위 + `buildSeedFallback(mode, limit)` mode 별 태그 우선순위(LARGE_MOMENTUM/DEFENSIVE/VALUE/GROWTH_MID) 분기 — Google 매칭 0건 시 seed 로 대체해 universe 가 절대 0 건이 되지 않도록 보장. ② `AiUniverseResult.diagnostics` 에 `sourceStatus: 'GOOGLE_OK'\|'FALLBACK_SEED'\|'NOT_CONFIGURED'\|'BUDGET_EXCEEDED'\|'ERROR'\|'NO_MATCHES'` + `fallbackUsed: boolean` 추가 — 클라이언트가 정확한 사유를 사용자에게 노출할 수 있도록 SSOT 단일 값 제공, ③ `momentumRecommendations.ts` — `universeRuleLine` 3분기(GOOGLE_OK / FALLBACK_SEED / 완전공란) 로 "이 목록 외 추천 금지" 를 Google 실매칭 universe 에만 강제, fallback 시 AI 학습 지식 확장 허용 + `[코드/이름 정확성]` 룰도 분기. diagnostics 기반 `warnings[]` 생성, ④ `RecommendationResponse.warnings?: string[]` 타입 추가 + `aiClient.ts` `isEmptyRecommendationData` 로 `recommendations.length===0` 캐시 스킵 — 한 번 실패가 하루를 박제하지 않도록 차단, ⑤ `useStockSearch.ts` — `data.warnings[]` 를 `toast.warning` 8초 표시 + 빈 추천 시 "위 안내 확인" 대체 메시지, ⑥ 회귀 테스트 3 케이스 신규 — NOT_CONFIGURED fallback / BEAR_SCREEN 방어주 우선 / buildSeedFallback mode 별 태그. 누적 aiUniverseService+wiring 14 케이스 pass + 전체 precommit pass) | `server/services/aiUniverseService.ts`, `server/services/aiUniverseService.test.ts`, `src/api/aiUniverseClient.ts`, `src/services/stock/momentumRecommendations.ts`, `src/services/stock/momentumRecommendations.wiring.test.ts`, `src/services/stock/types.ts`, `src/services/stock/aiClient.ts`, `src/hooks/useStockSearch.ts` | 사용자 보고 "AI 추천기능 정상적으로 동작 안 함 — 버튼 누르면 바로 완료되었다고 뜨고 아무것도 검색 안됨". GOOGLE_SEARCH_API_KEY 미설정 환경에서 universe 발굴이 0 건이 되고 Gemini 가 하드 룰 때문에 빈 응답을 반환, 그 빈 응답이 24h 캐시에 박제되어 "즉시 완료만 뜨고 아무것도 없음" 이 하루 종일 반복되던 구조적 문제 해소 |
| 2026-04-25 | PR-32 검색 페이지 시장 분석 카드 제거 + AI 추천 fallback 가격 보강 (ADR-0012, 사용자 요청 "검색 페이지 리뉴얼 + 추천 종목 검색 안 됨 진단": ① WatchlistHeader.tsx 813→295 LoC (-63.7%) — Market Sentiment 사이드 카드(F&G/IRI/VKOSPI/환율/국채/DXY/SYSTEM:BULL/AI 시장분석 버튼) + 실시간 시장 분석 풀폭 카드(KOSPI/KOSDAQ/Sector Rotation/Euphoria/Regime Shift/Global ETF/Global Indices/Critical Event/동적 가중치 전략) + AI Report Summary 마크다운 영역 모두 제거 — 시장 분석은 MarketDashboard 단일화 (흡수 0건, architect 분석 결과 100% 중복), Hero Section grid `lg:col-span-2`→`lg:col-span-3` 확장으로 사이드 카드 자리 회수, ② props 시그니처 정리 — WatchlistHeaderProps 6종(`searchResults, isSummarizing, onGenerateSummary, reportSummary, setReportSummary, setView`) + DiscoverWatchlistPageProps 1종(`onGenerateSummary`) + useWatchlistData destructure 6종 + useReportExport 1종 + PageRouter wiring 정리, ③ ADR-0012 신규 — Tier B 잔여 항목(KOSPI/KOSDAQ analysis 한 문장 IndicesSection 흡수)은 본 PR scope 밖 명시, ④ AI 추천 4단계 시뮬레이션 진단 → Step 3 끊김 발견: `enrichStockWithRealData → fetchHistoricalData(KR 장외/주말 → 게이트 차단 null) → aiFallback 진입 → Gemini가 currentPrice=0 응답 → applyTradingFieldFallbacks 비활성화 → WatchlistCard 가격 영역 "-" 표시` 사용자 체감 "추천 안 보임" 의 실제 근본 원인, ⑤ `enrichment.aiFallback` async 전환 + `fetchAiUniverseSnapshot(baseCode)` Naver Mobile 전일 종가로 currentPrice/per/pbr/marketCap 보강 (KIS/KRX quota 무관, ADR-0011 무위반), `dataSourceType='STALE'` + `priceUpdatedAt='전일 종가 (Naver)'` 출처 명시, 호출자 2곳(라인 261, 415) 모두 await 적용, ⑥ `aiClient.isEmptyRecommendationData(null|undefined) === true` false-negative 차단 + 메모리 캐시 4h TTL 만료 검사 추가, ⑦ 회귀 테스트 8 케이스 신규 — `enrichment.aifallback.test.ts` 5건(장외 currentPrice=0→Naver close 보강, KIS/KRX 호출 0회, dataSourceType STALE, 호출자 await, snapshot null 시 graceful) + `aiClient.nullCache.test.ts` 3건(null entry 즉시 무효화, 4h TTL 만료, undefined 동등 처리). 검증: lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과, 12/12 신규 테스트 + 31/31 src/services/stock 회귀 모두 pass) | `src/components/watchlist/WatchlistHeader.tsx` (813→295), `src/pages/DiscoverWatchlistPage.tsx`, `src/pages/PageRouter.tsx`, `src/services/stock/enrichment.ts` (aiFallback async + Naver fallback), `src/services/stock/aiClient.ts` (null guard + TTL), `src/services/stock/enrichment.aifallback.test.ts` (신규), `src/services/stock/aiClient.nullCache.test.ts` (신규), `docs/adr/0012-search-page-market-context-consolidation.md` (신규) | 사용자 첨부 스크린샷(KOSPI/KOSDAQ/NASDAQ/S&P/DOW/SOX 0 표기 시장 분석 카드 + Market Sentiment 사이드 카드) + "추천 종목 검색이 안 되고 있음" 보고 — UI 측은 시장 대시보드 중복 카드 제거로 0 표기 사용자 체감 해소, 데이터 측은 enrichment.aiFallback 의 currentPrice=0 응답이 UI 가격 카드 "-" 표시 근본 원인이었음을 4단계 시뮬레이션으로 식별·교정 |

| 2026-04-25 | PR-33 멀티소스 종목 마스터 + Health Score + Shadow DB (ADR-0013): KRX 단일 의존을 4-tier fallback orchestrator 로 전환 — ① `server/data/stockMasterSeed.ts` 신규 (KOSPI/KOSDAQ leader ~100건 정적 seed, 코어 30개 회귀 가드, KOSPI 우선 dedupe), ② `server/persistence/stockMasterHealthRepo.ts` 신규 (source 별 score 0-100, recentRuns 20건 ring buffer, KRX 50%/Naver 30%/Shadow 15%/Seed 5% 가중 평균 overall health), ③ `server/persistence/shadowMasterDb.ts` 신규 (last-known-good 스냅샷, Tier 1 KRX_CSV/Tier 2 NAVER_LIST 검증 통과 시에만 갱신 — Tier 3 자체·Tier 4 seed 응답은 shadow 갱신 거부로 폴백 사용 시 shadow 오염 차단), ④ `server/clients/naverStockListClient.ts` 신규 (m.stock.naver.com/api/stocks/marketValue 비공식 endpoint 로 KOSPI/KOSDAQ 시총 상위 ~400 fetch, Tier 2), ⑤ `server/services/multiSourceStockMaster.ts` 신규 (KRX → Naver → Shadow → Seed 4-tier orchestrator + 각 시도 health repo 기록 + KRX 3회 연속 실패·SEED 폴백 시 텔레그램 CRITICAL 경보 hook + dedupeKey master_source_alert:{src}:{date} 1일 1회), ⑥ `krxStockMasterRepo.ts` 확장 — `isLikelyHtmlResponse(body)` HTML 응답 감지 (parseKrxMasterCsv 가 `<!DOCTYPE`/`<html`/`<?xml` 시작 시 빈 배열), `validateMasterPayload(entries, minCount)` 검증 (count<2000=BELOW_MIN, 6자리 코드 매치율<95%=BAD_CODE_RATIO, KOSPI+KOSDAQ 비율<80%=BAD_MARKET_RATIO), `fetchKrxMasterEntries()` raw 페치 (orchestrator 용 — setStockMaster 안 부르고 KrxFetchResult 반환), 기존 `refreshKrxStockMaster` 에도 검증 임계 적용해 1500건짜리 부분 fetch 가 기존 2700건 마스터를 덮어쓰던 위험 제거, ⑦ `aiUniverseService.ts` — `refreshKrxStockMaster` 직접 호출을 `refreshMultiSourceMaster()` 로 교체 + usedFallback=true 시 attempts chain 로그, ⑧ `server/index.ts` 부팅 시 `setMasterAlertHook` 등록 (CRITICAL/WARN level 별 텔레그램 priority 분기), ⑨ `paths.ts` (+`STOCK_MASTER_SHADOW_FILE`, `STOCK_MASTER_HEALTH_FILE`), ⑩ `ARCHITECTURE.md` 5개 신규 모듈 boundary 등록 + multiSourceStockMaster boundary 규약 추가, ⑪ 회귀 테스트 55 케이스 신규 — stockMasterSeed 6 + stockMasterHealthRepo 9 + shadowMasterDb 8 + multiSourceStockMaster 7 + krxStockMasterRepo HTML/validation 16 (+기존 9 회귀). 누적 vitest 1486 케이스 중 1475 pass — 11 pre-existing 실패는 본 PR 범위 밖 (fredClient/koreanQuoteBridge/kisRanking/orchestratorRestartSafe/attributionRepo/sectorSources). 검증: precommit + lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) 전체 통과 | `server/data/stockMasterSeed.ts` (신규), `server/data/stockMasterSeed.test.ts` (신규), `server/persistence/stockMasterHealthRepo.ts` (신규), `server/persistence/stockMasterHealthRepo.test.ts` (신규), `server/persistence/shadowMasterDb.ts` (신규), `server/persistence/shadowMasterDb.test.ts` (신규), `server/clients/naverStockListClient.ts` (신규), `server/services/multiSourceStockMaster.ts` (신규), `server/services/multiSourceStockMaster.test.ts` (신규), `server/persistence/krxStockMasterRepo.ts`, `server/persistence/krxStockMasterRepo.test.ts`, `server/persistence/paths.ts`, `server/services/aiUniverseService.ts`, `server/index.ts`, `ARCHITECTURE.md`, `docs/adr/0013-multi-source-stock-master.md` (신규) | 사용자 요청 7종 (멀티소스 마스터·API 실패 감지·데이터 검증·CSV HTML 가드·Shadow Master DB·Universe 이중화·실시간 Health Score) 통합 구현. KRX 단일 실패점 제거로 OTP 빈 응답·HTML 오류 페이지·count<2000 부분 fetch 같은 PR-30/PR-30+ 시기 반복 사고를 4중 안전망(KRX→Naver→Shadow→Seed) 으로 차단. 운영자가 source 별 score 0-100 으로 신뢰도 즉시 파악, validateMasterPayload 임계 미통과 시 기존 마스터·shadow 보존, KOSPI/KOSDAQ leader seed 100건 박제로 universe 절대 0건 보장 |
| 2026-04-25 | PR-35 PR-33 잔여 11 pre-existing 실패 일소 + DART/Naver 우선순위 정렬 + DXY 인트라데이 NYSE 게이트 (사용자 보고 3건 통합 처리: ① PR-33 노트의 11 pre-existing 실패를 13건(이후 추가된 krxStockMaster 주말 단락 포함) 모두 수정 — `fredClient.ts` 가 `fetch(url)` 에 URL 객체를 그대로 넘기던 문제 해소(`url.toString()` 으로 string 강제, mock 의 `url.includes()` 분기 호환), `fredClient.regression.test.ts` 의 `mockResolvedValue` 가 동일 Response 인스턴스를 반환해 본문 1회 소비 후 `Body has already been read` 로 재시도 → 5초 timeout 으로 실패하던 패턴을 `mockImplementation(async () => jsonResponse(...))` 로 호출마다 새 Response 생성하도록 교정, `kisRankingClient.newTypes.test.ts` 가 ADR-0009 장외 게이트(주말 토요일 실행)에 막혀 빈 배열을 받던 문제를 `DATA_FETCH_FORCE_MARKET=true` env 로 우회, `koreanQuoteBridge.test.ts`/`sectorSources.test.ts` 가 PR-29 EgressGuard 도입 이후 KR 심볼·NYSE 심볼이 closed 시장으로 분류되어 mock fetch 가 호출되기 전에 503 차단되던 문제를 `EGRESS_GUARD_DISABLED=true` env 로 우회 (둘 다 분기 로직 자체 검증으로 시장시간과 무관), `attributionRepo.test.ts` 의 `computeAttributionStats 는 현행 스키마만 집계` 가 PR-19 로 `CURRENT_ATTRIBUTION_SCHEMA_VERSION` 1→2 상향 후 하드코딩 `schemaVersion: 1` 이 구버전 취급되던 문제를 `CURRENT_ATTRIBUTION_SCHEMA_VERSION` import 사용으로 교정, `orchestratorRestartSafe.test.ts` 의 첫 케이스가 전체 suite 동시 실행 시 1,000+ 줄 모듈 체인(signalScanner/webhookHandler/stockScreener/exitEngine) 두 번 import 로 5초 timeout 초과하던 문제를 케이스별 30초 timeout 부여로 해소, `krxStockMasterRepo.test.ts` 주말 단락 케이스 isolation 동일 패턴 — 기존 cleanup 충분, ② 펀더멘털 우선순위 명문화: `enrichment.aiFallback` 이 Naver snapshot 만 호출해 DART 사업보고서 ROE/debt/OCF/이자보상/EPS 성장 데이터를 사용하지 못하던 누수 해소 — `fetchCorpCode` + `fetchDartFinancials` 우선 호출 후 DART 결과를 valuation.debtRatio/epsGrowth + checklist.roeType3/ocfQuality/interestCoverage 에 주입, Naver snapshot 은 DART 가 직접 제공하지 않는 가격/PER/PBR/시총 만 보강하여 SSOT 일관성 확보 (Main path 는 이미 DART-first 였으나 fallback 경로의 black hole 명시적으로 차단), 메인 enrichment 진입부에 "펀더멘털 우선순위: DART → Naver" 주석 명문화, ③ DXY 인트라데이 5분 cron 노이즈 일소: `runDxyIntradayMonitor` 진입부에 `isOpenAt('NYSE')` 가드 추가 — NYSE 정규장이 닫혀있으면 silent skip(로그 없음) 하여 `[EgressGuard] skip NYSE:DX-Y.NYB — NYSE closed` + `[DxyIntraday] Yahoo 리딩 없음 & ALPHA_VANTAGE_API_KEY 미설정 — 스킵` 두 로그가 5분마다 반복되던 현상 근본 차단 (cron 자체는 alertJobs.ts 의 기존 시간대 그대로 유지 — DST 변동 안전), `egressGuard._LOG_INTERVAL_MS` 60s→30min 으로 확대 — 5분 cron 이 1분 throttle 을 항상 통과하던 구조적 문제 해소. ④ ALPHA_VANTAGE_API_KEY 사용처 확인 결과: `dxyIntradayClient.ts` (Yahoo 실패 시 합성 DXY 6 환율 fallback) + `dxyMonitor.ts` (AV 미설정 시 로그 레벨 분기) + `webhookHandler.ts` (`/dxy_intraday` 응답에 AV 안내) 3곳에서 옵셔널 fallback 으로만 활용 — 미설정 시 정상 동작 (PR-10 정책 유지). 검증: vitest 1508/1508 pass + lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과) | `server/clients/fredClient.ts` (URL.toString), `server/clients/fredClient.regression.test.ts` (mockImplementation), `server/clients/kisRankingClient.newTypes.test.ts` (FORCE_MARKET env), `server/clients/koreanQuoteBridge.test.ts` (EGRESS_GUARD_DISABLED env), `server/persistence/attributionRepo.test.ts` (CURRENT_ATTRIBUTION_SCHEMA_VERSION 사용), `server/screener/sectorSources.test.ts` (EGRESS_GUARD_DISABLED env), `server/orchestrator/orchestratorRestartSafe.test.ts` (30s timeout), `src/services/stock/enrichment.ts` (aiFallback DART 우선 + 메인 경로 주석 명문화), `server/alerts/dxyMonitor.ts` (NYSE 게이트 + 주석), `server/utils/egressGuard.ts` (throttle 1min→30min) | 사용자 요청 3종: PR-33 노트의 11 pre-existing 실패가 사실은 13건(주말 추가)이고 모두 PR-29(EgressGuard)/PR-19(스키마 v2)/PR-32(테스트 isolation) 등 후속 PR 들의 회귀 — 누적 기술 부채를 일소. 펀더멘털 출처 우선순위(naver→dart 에서 dart→naver 로) 사용자 명시 요청 — DART 사업보고서가 Naver 모바일 snapshot 보다 신뢰도가 높다는 판단 반영. Railway 로그에 5분마다 찍히던 DXY 노이즈 → NYSE 게이트로 운영자가 진짜 신호와 cron 정상동작을 즉시 구별 가능. ALPHA_VANTAGE_API_KEY 는 옵셔널 fallback 으로만 남아있어 미설정 환경(현재 사용자 환경)에서도 정상 동작. |
| 2026-04-25 | PR-37 주말·휴일 AI 종목 검색 안정화 — 5-Tier Fallback Orchestration (ADR-0016): GOOGLE_SEARCH_API_KEY 미설정·주말·휴일·외부 소스 다중 실패 환경에서 baseline 시총 상위 응답 의존을 끊고 마지막 정상 거래일 기준 정량 후보군으로 자동 폴백. ADR-0011 (PR-25-A/B/C: AI 추천 KIS/KRX 분리) + ADR-0013 (PR-33: multiSourceStockMaster 4-tier) 의 직접 후속. ① `server/services/aiUniverseTypes.ts` 신규 — `AiUniverseMode`(4값) + `AiUniverseSourceStatus`(9값: GOOGLE_OK/FALLBACK_SNAPSHOT/FALLBACK_QUANT/FALLBACK_NAVER/FALLBACK_SEED + NOT_CONFIGURED/BUDGET_EXCEEDED/ERROR/NO_MATCHES) + `MarketDataMode`(5값: LIVE_TRADING_DAY/AFTER_MARKET/WEEKEND_CACHE/HOLIDAY_CACHE/DEGRADED) + `AiUniverseDiagnostics`(tradingDateRef·snapshotAgeDays·tierAttempts 포함) + `AiUniverseSnapshot` 영속 스키마 SSOT, ② `server/utils/marketClock.ts` `classifyMarketDataMode(now)` 추가 — isMarketOpen·isKstWeekend wrap, ③ `server/persistence/aiUniverseSnapshotRepo.ts` 신규 — atomic write(tmp→rename) + 7일 만료 가드 + 갱신 정책 가드 (Tier 1 GOOGLE_OK + candidates ≥ 3 일 때만 디스크 갱신, Tier 2~5 응답은 갱신 거부 — 오염 방지), `aiUniverseSnapshotFile(mode)` (paths.ts 신규 함수) 활용으로 mode 별 별도 파일 저장, ④ `server/services/quantitativeCandidateGenerator.ts` 신규 — Yahoo OHLCV 기반 정량 후보 생성기(Tier 3). MOMENTUM(20일 모멘텀+거래대금)/EARLY_DETECT(20일 변동성 하위+52주 신고가 -5~-15%)/QUANT_SCREEN(MOMENTUM 정렬, 정밀 임계 후속)/BEAR_SCREEN(20일 변동성 하위 1차 근사). 모든 Yahoo 호출 `guardedFetch`(PR-29 EgressGuard) 경유 — 시장 닫힘 시 자동 차단·stale 폴백, raw fetch 0건. universe 모집단 50종목 기본. `yahoo_chart` aiCallBudgetRepo bucket 신규 사용, ⑤ `server/services/aiUniverseService.ts` 5-Tier orchestrator 본체 재작성 — Tier 1(Google CSE → 성공 시 snapshot 갱신) → Tier 2(loadAiUniverseSnapshot ≤7일) → Tier 3(generateQuantitativeCandidates) → Tier 4(Naver 시총 상위 단독) → Tier 5(buildSeedFallback 기존). 각 Tier 진입/실패 `tierAttempts[]` push, marketMode = classifyMarketDataMode() (Tier 3+ 진입 시 'DEGRADED' override). `AI_UNIVERSE_FALLBACK_DISABLED=true` 시 Tier 1 → Tier 5 즉시(ADR-0011 호환 롤백 스위치), ⑥ `server/routes/aiUniverseHealthRouter.ts` 신규 — `GET /api/health/ai-universe` 응답: marketMode + snapshots(mode → tradingDate/ageDays/expired) + masterHealth(PR-33 stockMasterHealthRepo 재사용) + sources(google/naver/yahoo). `/api/health` 마운트(server/index.ts), ⑦ `krxStockMasterRepo.ts` `getAllStockEntries()` export 추가 — Tier 3 universe 모집단 접근 경로, ⑧ 클라이언트 — `src/api/aiUniverseClient.ts` 9값+5값+4 옵셔널 동기 사본(절대 규칙: 서버↔클라 직접 import 금지) + `AiUniverseHealth` + `fetchAiUniverseHealth()`, `src/services/stock/types.ts` `RecommendationResponse` 4 옵셔널(sourceStatus·marketMode·tradingDateRef·snapshotAgeDays) 비파괴 확장, `src/services/stock/momentumRecommendations.ts` `buildUniverseRuleLine`(10 분기) + `buildUniverseWarning`(9값+budgetExceeded 보강) export — ADR-0016 §6 권장 문구 적용 (baseline/시총상위/임시추천/fallback seed 4개 사용자 노출 표현 영구 제거, dashboard wiring 9건 회귀가드 `expect(msg).not.toContain('baseline')` 정착), `src/hooks/useMarketMode.ts` 신규 — `classifyClientMarketMode` + 1분 폴링, `RecommendationWarningsBanner` 5-tone(slate/orange/red + 아이콘 4종), `OffHoursBanner` useMarketMode 분기 메시지(WEEKEND/HOLIDAY/AFTER_MARKET), `WatchlistCard` LIVE 배지 5분류(LIVE 주황 pulse / 장외 파랑 / 주말 회색 / 공휴일 회색 / ⚠️ 다중 실패 빨강), `useRecommendationStore` recommendationSourceStatus + setter, `useStockSearch` setRecommendationSourceStatus wiring, ⑨ 회귀 테스트 신규 56 케이스(engine 34 + dashboard 22) — `aiUniverseSnapshotRepo.test.ts` 9 + `quantitativeCandidateGenerator.test.ts` 9 + `aiUniverseHealthRouter.test.ts` 6 + `marketClock.test.ts` +5 + `aiUniverseService.test.ts` +5(5-Tier 시나리오) + `momentumRecommendations.wiring.test.ts` 22(buildUniverseRuleLine 7 + buildUniverseWarning 9 + 기존 6) + `useMarketMode.test.ts` 6. 검증: lint + validate:all(DeployWindow+PRES+ACMA+SRP+SymbolBoundary+Gemini+SDS 7종) + precommit + vitest **1576/1576 pass(157 파일, 14.5s)** 전체 통과. quality-guard 교차검증 미스매치 0건/직접 패치 0건/위임 0건. KIS/KRX 자동매매 quota 0 침범(절대 규칙 #2/#3 준수, raw fetch 0건). | `docs/adr/0016-weekend-ai-universe-fallback.md`(신규), `server/services/aiUniverseTypes.ts`(신규), `server/persistence/aiUniverseSnapshotRepo.ts`(신규)+`.test.ts`(신규), `server/services/quantitativeCandidateGenerator.ts`(신규)+`.test.ts`(신규), `server/routes/aiUniverseHealthRouter.ts`(신규)+`.test.ts`(신규), `server/utils/marketClock.ts`(+classifyMarketDataMode)+`.test.ts`, `server/services/aiUniverseService.ts`(5-Tier orchestrator)+`.test.ts`(+5신규), `server/persistence/krxStockMasterRepo.ts`(+getAllStockEntries), `server/persistence/paths.ts`(+aiUniverseSnapshotFile), `server/index.ts`(+/api/health), `src/api/aiUniverseClient.ts`(9값+5값+4옵셔널+fetchAiUniverseHealth), `src/services/stock/momentumRecommendations.ts`(+buildUniverseRuleLine/buildUniverseWarning)+`.wiring.test.ts`(+22), `src/services/stock/types.ts`(RecommendationResponse 4옵셔널), `src/hooks/useMarketMode.ts`(신규)+`.test.ts`(신규), `src/components/common/RecommendationWarningsBanner.tsx`, `src/components/common/OffHoursBanner.tsx`, `src/components/watchlist/WatchlistCard.tsx`, `src/stores/useRecommendationStore.ts`(+recommendationSourceStatus), `src/hooks/useStockSearch.ts`(+wiring) | 사용자 요청 12단계 설계 (문구 교정 + MarketDataMode 5분류 + Snapshot 영속 + 정량 후보 생성기 + Google 강등 + baseline 5순위 + UI/Health 표시 + 휴일 모드 표현). GOOGLE_SEARCH_API_KEY 미설정 환경에서 발생하던 "버튼 누르면 즉시 완료만 뜨고 추천 0건" 또는 "baseline 시총 상위 임시추천" 사용자 체감 신뢰도 저하 근본 차단. 주말·휴일·평일 장외 모두 마지막 정상 거래일 기준 정량 후보군으로 자동 폴백, 5-Tier 사슬로 단일 소스 실패가 universe 0건을 만들지 않도록 보장. |
| 2026-04-25 | PR-36 SHADOW 회로 점검(트랙 A, 수정 0건) + 자기학습 이력 확인 명령 (트랙 B): ① 트랙 A — quality-guard 가 PR-26~PR-35 누적 변경(egressGuard / marketGate / multiSourceStockMaster / kisRetrySafety / liveReconciler / DXY NYSE 게이트 / KIS 24h blacklist) 의 SHADOW 회로 부작용 audit. signalScanner SHADOW 분기(L378~409) + tradingOrchestrator(L74~87) 의 `computeShadowAccount` vs `fetchAccountBalance` 분기, exitEngine `_exitRunning` mutex(L325~339, PR-6), shadowTradeRepo backfill/aggregateFillStats export 시그니처(PR-7/18/19), egressGuard host 화이트리스트(Yahoo-only, KIS 미경유), marketDataRouter 미사용(grep 0건), KIS 블랙리스트 대상(FHPST01710000) 과 SHADOW 가격조회(FHKST01010100) 분리, 부팅 backfill 정상(server/index.ts:107~125) — 모두 PASS. 회귀 테스트 49/49 pass(shadowAccountRepo / shadowBuyFillBackfill / aggregateFillStats / attributionRepoPartial / exitEngineMutex / liveReconciler / shadowTradeRepo). 옵셔널 관찰사항: `dryRunScanner.ts:79` 가 SHADOW 모드에서도 `fetchAccountBalance()` 호출 — `/dryrun` 텔레그램 명령 진입점 전용(cron/주기 아님)이라 본 PR scope 밖, 위생 PR 후보로 기록만. ② 트랙 B — `/learning_status` `/learning_history [n]` 텔레그램 명령 + `GET /api/learning/{status,history}` HTTP 엔드포인트 신설. nightlyReflectionEngine(KST 19:00) / counterfactualSuggest(16:00) / ledger+kellySurface+regimeCoverage(16:15) / probingBandit / suggestNotifier 가 실제 실행 중인지 운영자가 확인 가능. `learningHistorySummary.ts` 신규 — `getLearningStatus()` 가 직전 reflection 1건 + consecutiveMissingDays + biasHeatmapToday + biasHeatmap7dAvg + experimentProposalsActive + suggestAlerts7d 카테고리별(counterfactual/ledger/kellySurface/regimeCoverage) + diagnostics(healthy + warnings) 통합 반환. `getLearningHistory(days, now)` 가 1~30일 일자별 스냅샷 + escalatingBiases(3일 연속 ≥0.5) 반환. SILENCE_MONDAY 는 missing 카운트에서 제외하고 별도 표기. diagnostics warnings 4종: 연속 N일 reflection 없음 / Gemini 예산 80% 도달 / 템플릿 폴백 모드 / Integrity 파싱 실패. `learningHistoryFormatter.ts` 신규 — 텔레그램 HTML 메시지 포맷(verdict 이모지 🟢🟡🔴⚪ + bias score 임계 🔴≥0.6/🟡≥0.4/🟢 + ❌ 미실행 / ⏸️ SILENCE_MONDAY 분기). webhookHandler 두 case 추가 + /help 메시지 갱신. learningRouter.ts 신설 + index.ts `/api/learning` 마운트. 회귀 테스트 12/12 pass(reflection 0건/정상/TEMPLATE_ONLY/integrityParseFailed/escalating/SILENCE_MONDAY/days=7/proposals 분류 + formatter 4 케이스). KST timezone-agnostic `epoch+9h` 변환. AlertCategory enum 에 'learning' 부재 — suggestNotifier 가 string literal 통과시키는 우회를 `String(e.category)` 비교로 흡수. 검증: lint + validate:all (DeployWindow + PRES + ACMA + SRP + SymbolBoundary + Gemini 6종) + precommit 전체 통과. | `server/learning/learningHistorySummary.ts` (신규), `server/learning/learningHistorySummary.test.ts` (신규, 12 케이스), `server/learning/learningHistoryFormatter.ts` (신규), `server/routes/learningRouter.ts` (신규), `server/telegram/webhookHandler.ts` (+/learning_status, /learning_history, /help 갱신), `server/index.ts` (+learningRouter 마운트) | 사용자 요청 2종: (1) PR-26~35 누적 API 업데이트 후 SHADOW 회로 정상 여부 확인 — 결과 정상, 수정 0건. (2) 자기학습이 실제 이루어지는지 + 학습 내용을 보고 같이 고민할 수 있는 경로 필요 — 텔레그램에서 `/learning_status` (직전 1회 요약) `/learning_history 7` (최근 7일 이력) 으로 운영 환경 학습 데이터를 즉시 조회 가능. cron 미실행 / 템플릿 폴백 / Gemini 예산 도달 / Integrity 파싱 실패 / 편향 escalating 등 학습 죽음 신호를 자동 진단해 ⚠️ 경고로 표면화. |
| 2026-04-25 | PR-34 KIS 재시도 안전성 + /reconcile live (ADR-0014 + ADR-0015): 매매에 막힘이 없도록 READ 쪽 resiliency 는 강화하고 WRITE(주문) 쪽은 중복 주문 위험을 근본 제거. ① `kisClient._kisBackoffDelayMs` 50% jitter (retriesLeft=3→500~1500ms / 2→1000~3000ms / 1→2000~6000ms) + `_kis429DelayMs` 0~500ms jitter 로 thundering herd 해소, ② `KisPostOptions.idempotency: 'safe' \| 'unsafe'` 신설 — `kisPost` 기본 'unsafe' (주문/취소 mutate 보호), `_rawKisPost` 가 `idempotency='unsafe'` + 5xx 수신 시 **재시도 차단** + `sendTelegramAlert` 긴급 경보 (KIS 매칭엔진 도달 여부 알 수 없어 재시도가 중복 주문 만들던 잠재 위험 제거), ③ 401/429/네트워크 에러는 기존대로 재시도 (매칭엔진 미진입 확정), ④ `KIS_RETRY_DISABLED` / `KIS_RETRY_JITTER_DISABLED` env 긴급 무력화 스위치, ⑤ `fetchKisHoldings(): Promise<KisHolding[] \| null>` 신설 — `inquire-balance output1[]` 파싱, 페이지네이션 없이 1페이지(100건), 점검시간/회로차단 시 null fail-closed, ⑥ `server/trading/liveReconciler.ts` 신설 — KIS 실잔고 vs 로컬 ACTIVE LIVE trade 비교 (MATCH/QTY_DIVERGENCE/GHOST_LOCAL/GHOST_KIS 4분류), dry-run 은 diff 만 보고, apply 는 SELL/BUY fill 추가로 교정하되 GHOST_KIS 는 메타 손실 위험으로 자동 적용 차단, ⑦ `webhookHandler /reconcile live [apply]` 서브명령 — 기본 dry-run, apply 는 60s rate-limit 가드 (오타 방지), KIS 조회 실패 시 로컬 변경 없이 종료, ⑧ ADR-0014 재시도 안전성 정책 + ADR-0015 데이터 출처 우선순위 신설 — LIVE 모드에서 수량/평단가/현금 잔고 3종 모두 KIS SSOT 명문화, SHADOW/자기학습 경로는 기존 로컬 fills[] SSOT 유지, ⑨ 회귀 테스트 22 케이스 신규 — `kisRetrySafety.test.ts` 10건(backoff jitter 분포·재시도 무력화 스위치·429 jitter) + `liveReconciler.test.ts` 12건(4분류 diff·SHADOW 제외·closed 제외·동일 종목 다중 trade 합산·apply 적용 효과·GHOST_KIS 차단). 검증: precommit + lint + validate:all 통과, 신규 22 + circuit 회귀 14 = 36/36 pass | `server/clients/kisClient.ts` (+jitter +KisPostOptions +fetchKisHoldings), `server/clients/kisRetrySafety.test.ts` (신규), `server/trading/liveReconciler.ts` (신규), `server/trading/liveReconciler.test.ts` (신규), `server/telegram/webhookHandler.ts` (+/reconcile live 서브명령 +60s guard), `docs/adr/0014-kis-retry-safety-policy.md` (신규), `docs/adr/0015-reconciliation-source-priority.md` (신규) | 사용자 요청 "매매에 막힘이 없어야 한다" + #1 자동 재시도 + #2 /reconcile 강제 재동기화. 기존 회로차단기(PR-21)·24h 블랙리스트(PR-24) 위에 올라가는 "마지막 한 겹" — 주문 POST 5xx 후 재시도가 중복 주문 만들 수 있던 잠재적 금융 리스크 차단, LIVE 모드에서 KIS 실잔고와 로컬 장부 괴리 시 강제 동기화 경로 신설. WRITE 실패 시 사용자가 즉시 KIS HTS 로 상태 확인 후 /reconcile live 로 후속 조치 가능. |
| 2026-04-25 | PR-40 signalScanner.ts 분해 Phase A (ADR-0001 7모듈, Steps 1+2+3+4a 완료, Steps 4b/4c/5/6/7 후속): ADR-0001 5모듈 → 7모듈 개정 — `orderDispatch.ts` 분리 추가. 사유 "스캔과 주문이 같은 파일에 오래 붙어 있으면 자동매매 사고 위험" — `channelBuySignalEmitted` 1회 송출 SSOT 격리. ① 스캐폴딩: `server/trading/signalScanner/{index,preflight,candidateSelect,perSymbolEvaluation,approvalQueue,orderDispatch,scanDiagnostics}.ts` 7개 stub 파일 (각 @responsibility 25단어 이내), ② **Step 1 scanDiagnostics.ts 본체** (124줄) — `ScanSummary` 인터페이스 + `_lastBuySignalAt`/`_lastScanSummary`/`_consecutiveZeroScans` 글로벌 상태 캡슐화 + `ScanCounters` 객체 (스캔 1회당 인스턴스 — `_scanYahooFails`/`_scanGateMisses`/`_scanRrrMisses`/`_scanEntries`/`_counterfactualRecordedToday`/`_pendingTraces` 통합) + `persistScanResults` (appendScanTraces + ScanSummary 빌드 + 3회 침묵 텔레그램 알림), signalScanner.ts 가 즉시 import 사용 (live), ③ **Step 2 preflight.ts 본체** (349줄) — 13단계 매크로·시스템 게이트: KIS_APP_KEY 단락/UI 수동 가드 sellOnly 승격/shadowMode 분기 + 잔고 결정/regime 분류/sellOnlyException/R6_DEFENSE/VIX gating/FOMC/data-starvation/Kelly 합성 (`getAccountScaleKellyMultiplier` 포함)/effectiveMaxPositions/volumeClock. 호출 교체는 Phase B (Step 7) 보류로 현재 dead code, ④ **Step 3 candidateSelect.ts 본체** (96줄) — `selectCandidates`: loadWatchlist + computeFocusCodes + assignSection in-place + AUTO_SHADOW_FROM_MOMENTUM 분기 + buyList/swingList/catalystList/momentumList 분류 + intradayBuyList 로드. 현재 dead code (Phase B 호출 교체 예정), ⑤ **Step 4a perSymbolEvaluation.ts 본체** (125줄) — 헬퍼 4종 추출: `getPrice` (KIS 실시간 가격 fallback)/`FAILURE_BLOCK_THRESHOLD_PCT` (Idea 7 진입 차단 임계)/`SymbolExitContext` (LEADER/CATALYST/OVERHEATED/DIVERGENT)/`getAdaptiveProfitTargets` (PROFIT_TARGETS macro/symbol overlay), signalScanner.ts 가 import 사용 (live), ⑥ 외부 importer 9개 (tradingOrchestrator/healthCheckJob/screenerJobs/webhookHandler/reportGenerator/scanReviewReport/screenerRouter/engineRouter/systemRouter) 무변경 — `signalScanner.js` 경로 호환 유지, ⑦ 검증: lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과. `server/trading/` 273/273 회귀 테스트 통과 (27 files). signalScanner.ts 1848 → 1702줄 (-146). ⑧ Phase B (후속 PR) 잔여: 메인 per-stock 루프 (~929줄) → `evaluateBuyList`, intraday 루프 (~186줄) → `evaluateIntradayList`, onApproved 본체 → `orderDispatch.dispatchApprovedBuy`, flush + queue 라이프사이클 → `approvalQueue.ApprovalQueue`, signalScanner.ts barrel 축소 + call site 교체. 완료 시 signalScanner.ts ≤50줄 + 7모듈 모두 1500줄 이하 달성. | `docs/adr/0001-signalScanner-decomposition.md` (5→7모듈 개정), `server/trading/signalScanner/index.ts` (신규 stub), `server/trading/signalScanner/preflight.ts` (신규, 349줄), `server/trading/signalScanner/candidateSelect.ts` (신규, 96줄), `server/trading/signalScanner/perSymbolEvaluation.ts` (신규, 125줄), `server/trading/signalScanner/approvalQueue.ts` (신규 stub), `server/trading/signalScanner/orderDispatch.ts` (신규 stub), `server/trading/signalScanner/scanDiagnostics.ts` (신규, 124줄), `server/trading/signalScanner.ts` (1848→1702), `_workspace/2026-04-25_pr-signalscanner-decomposition/refactor/plan.md` (이동 체크리스트 신규) | 사용자 요청 "1순위 — signalScanner.ts 분해" + "orderDispatch.ts 는 반드시 분리" — Phase A 로 7모듈 구조·ADR·헬퍼·진단 모듈 + 매크로 게이트·후보 선정 본체 정착, Phase B 로 메인 루프·주문 발송·승인 큐·barrel 축소 완수 예정. 자동매매 LIVE 회귀 위험 차단을 위해 단계별 검증·체크포인트 커밋·후속 PR 분리 채택. |
| 2026-04-25 | PR-39 SMALL_MID_CAP 서버 정규 mode 승격: 기존엔 `toUniverseMode('SMALL_MID_CAP') → 'MOMENTUM'` 으로 흡수 후 클라이언트가 KOSDAQ 정렬만 후처리. 사용자 요청에 따라 서버 universe 발굴 단계부터 KOSDAQ·중소형 성장주 우선 후보를 받도록 5번째 정규값으로 승격. ① `server/services/aiUniverseTypes.ts` `AiUniverseMode` 4값 → 5값(`'MOMENTUM' \| 'QUANT_SCREEN' \| 'BEAR_SCREEN' \| 'EARLY_DETECT' \| 'SMALL_MID_CAP'`), ② `src/api/aiUniverseClient.ts` 동기 사본 (서버↔클라 직접 import 금지 절대 규칙), ③ `server/routes/aiUniverseRouter.ts` `MODE_VALUES` 5값 확장 — `/api/ai-universe/discover?mode=SMALL_MID_CAP` 접근 가능, ④ `server/services/aiUniverseService.ts` `MODE_QUERIES.SMALL_MID_CAP = ['코스닥 중소형 주도주', '중소형 성장주 신고가']` Google CSE 쿼리 + `buildSeedFallback` SMALL_MID_CAP 분기 (wanted=['GROWTH_MID'] + LARGE_MOMENTUM 명시 제외 → 삼성전자·SK하이닉스·현대차·기아 같은 초대형주 차단, 에코프로비엠/에코프로/셀트리온헬스케어/알테오젠/엘앤에프 KOSDAQ GROWTH_MID 우선), ⑤ `server/services/quantitativeCandidateGenerator.ts` `rankCandidates` SMALL_MID_CAP 분기 — KOSDAQ·KOSPI 그룹 분리 후 각 그룹 내 MOMENTUM 정렬(20일 모멘텀+거래대금 합산) 적용, KOSDAQ 그룹 우선 반환, ⑥ `server/routes/aiUniverseHealthRouter.ts` `MODES` 5값 확장 — `/api/health/ai-universe` snapshot block 에 SMALL_MID_CAP 포함, ⑦ `src/services/stock/momentumRecommendations.ts` `toUniverseMode` 반환 타입 `'MOMENTUM' \| 'EARLY_DETECT' \| 'SMALL_MID_CAP'` 확장 + 본문 SMALL_MID_CAP 분기는 서버 universe 위에 KOSDAQ 우선 정렬 한 번 더 (Google 뉴스가 대형주에 편중되는 회귀 방어), ⑧ 회귀 테스트 5 케이스 신규 — `aiUniverseService.test.ts` MODE_QUERIES 5값 검증 + buildSeedFallback SMALL_MID_CAP 단언 1건, `quantitativeCandidateGenerator.test.ts` rankCandidates SMALL_MID_CAP KOSDAQ 우선 + KOSDAQ 0건 fallback 2건, `momentumRecommendations.wiring.test.ts` toUniverseMode SMALL_MID_CAP→SMALL_MID_CAP 반환 + 기존 "MOMENTUM 흡수" 단언 제거, `aiUniverseSnapshotRepo.test.ts` MODES 5값 + SMALL_MID_CAP round-trip 테스트 (이전엔 "변형 mode" 표현). 검증: lint + validate:all (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary 6종) + 영향 4개 파일 65/65 pass + 인접 router 2개 파일 11/11 pass + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3 준수). | `server/services/aiUniverseTypes.ts`, `src/api/aiUniverseClient.ts`, `server/routes/aiUniverseRouter.ts`, `server/routes/aiUniverseHealthRouter.ts`, `server/services/aiUniverseService.ts`, `server/services/aiUniverseService.test.ts`, `server/services/quantitativeCandidateGenerator.ts`, `server/services/quantitativeCandidateGenerator.test.ts`, `server/persistence/aiUniverseSnapshotRepo.test.ts`, `src/services/stock/momentumRecommendations.ts`, `src/services/stock/momentumRecommendations.wiring.test.ts` | 사용자 요청 "SMALL_MID_CAP 서버 모드 분리는 2차 PR 권장" — 클라이언트 mode 변형이 서버에서 MOMENTUM universe 로 흡수되던 한계 해소. 이제 SMALL_MID_CAP 추천 시 서버가 코스닥 중소형 주도주 쿼리(Google CSE) → KOSDAQ GROWTH_MID seed 우선 → Yahoo OHLCV 정량 후보 생성 시 KOSDAQ 그룹 우선 정렬을 거쳐 universe 단계부터 중소형주만 받게 된다. ADR-0011 (KIS/KRX 비의존) + ADR-0016 (5-Tier fallback) 호환 — 새 mode 도 google_search + naver_finance + Yahoo OHLCV 만 사용. |
| 2026-04-25 | PR-41 자동매매 LIVE 회귀 audit + dryRunScanner SHADOW 잔고 누수 수정 (PR-5 #11 follow-up): 사용자 요청 "PR-26~PR-40 누적 변경 후 자동매매 시스템에서 놓친 버그 전수 검증·즉시 수정". orchestrator가 architect/engine-dev sub-agent 타임아웃 후 직접 audit 수행. 우선순위 7종(LIVE 경로/Phase A 분해/exitEngine mutex/Egress Guard·블랙리스트/rule #2 단일 통로/부팅 backfill/AUTO_TRADE_ENABLED 가드) 전수 점검 결과 **Critical 0건**, **High 1건(H1)**, **Medium 3건(M1~M3, 본 PR scope 밖 후속 별도 PR)**, **Pass 9건**. ① **H1 수정** — `server/trading/dryRunScanner.ts:64~109` 가 PR-5 #11 가 신설한 SHADOW vs LIVE 잔고 분기를 누락한 채 `shadowMode` 와 무관하게 `fetchAccountBalance()` (KIS 실/모의 잔고)를 호출하던 버그 해소. signalScanner.ts:284~297 정합 패턴(`shadowMode → computeShadowAccount(shadows, startingCapital)` / `LIVE → fetchAccountBalance()`) 으로 분기 추가. 효과: (a) `/dryrun` 텔레그램 명령이 SHADOW 모드에서 KIS API 1회 소비 0건 — 회로차단기·24h 블랙리스트 부담 제거, (b) SHADOW dryrun totalAssets/orderableCash 가 startingCapital 독립 원장으로 표기되어 LIVE 잔고 오염 차단. ② 회귀 테스트 4 케이스 신규 — `dryRunScanner.test.ts`: SHADOW 모드 fetchAccountBalance 미호출 + computeShadowAccount(_, 100M) 호출 / SHADOW + AUTO_TRADE_ASSETS 오버라이드 / LIVE 모드 fetchAccountBalance 호출 + computeShadowAccount 미호출 / AUTO_TRADE_MODE 미설정 시 SHADOW 기본. ③ **Pass 항목 (audit 후 정상 확인)**: rule #2 kisClient 단일 통로(raw `openapi.koreainvestment.com` URL은 kisClient.ts + kisStreamClient.ts 만), rule #4 자동매매 단일 통로(클라이언트 KIS POST 직접 호출 0건, 서버 실주문은 모두 `kisPost` 경유), kisPost idempotency 기본 'unsafe'(PR-34 ADR-0014, 5xx 시 재시도 차단으로 중복 주문 방지), 회로차단기·24h 블랙리스트가 주문 TR(TTTC0802U/0801U/0803U) 차단 위험 없음, egressGuard HOST_RULES Yahoo만 매칭(KIS/KRX pass-through), AUTO_TRADE_ENABLED 가드 4개 위치 분포 정상, exitEngine `_exitRunning` mutex 정상(PR-6), 부팅 BUY fill backfill 정상(PR-7), Kelly time decay wiring(PR-22 ADR-0008) 정상. ④ **Medium 후속 별도 PR**: M1 exitEngine partial SELL fill 시 emitPartialAttribution 미호출(PR-19 ADR-0006 후속 wiring 미수행, 학습 데이터 품질 갭이지 LIVE 안전성 무영향), M2 `/api/kis/proxy` raw fetch 우회 경로(`server/routes/kisRouter.ts:264~301` 가 `kisGet`/`kisPost` 미경유로 회로차단기·블랙리스트·jitter 우회, 클라이언트 priceSync.ts 만 사용 중이고 자동매매 LIVE 경로는 영향 없음 — 구조적 리팩터링 후속 PR), M3 signalScanner Phase A dead code(preflight.ts/candidateSelect.ts 0 importers, PR-40 노트가 Phase B 호출 교체 예정으로 명시한 의도된 상태). ⑤ 검증: lint + validate:all (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary 6종) + precommit 전체 통과. `server/trading/` 28 files 277/277 회귀 테스트 통과 (신규 4 + 기존 273 무회귀). | `server/trading/dryRunScanner.ts` (SHADOW 분기 추가 + computeShadowAccount/loadTradingSettings import), `server/trading/dryRunScanner.test.ts` (신규 4 케이스), `_workspace/2026-04-25_audit-trading-regressions/findings.md` (신규 audit 보고서) | 사용자 요청 "여러가지 패치를 하며 놓친 버그가 존재하는지 확인 + 즉시 수정 — 자동매매 시스템이 정상적으로 돌아가는게 가장 중요". PR-26~40 누적 변경의 LIVE 안전성은 정상 작동 확인, PR-5 #11 가 누락한 dryRunScanner SHADOW 분기를 본 PR 에서 해소. /dryrun 명령이 SHADOW 모드에서 KIS API 를 부르지 않게 되어 회로 부담 제거 + SHADOW 시뮬레이션 totalAssets 정확도 확보. |
| 2026-04-25 | PR-42 PR-41 audit Medium 3건(M1/M2/M3) 일괄 해소: 사용자 요청 "m1,m2,m3 에러 수정". ① **M1 — exitEngine partial SELL fill 시 emitPartialAttribution 자동 wiring (PR-19 ADR-0006 후속)**: `reserveSell` 의 SELL fill 커밋 SSOT 지점에 `emitPartialAttributionForSell` 헬퍼 호출 추가. 조건 — SHADOW(CONFIRMED 즉시) + remainingQty>0 + fill.qty>0 + newFillId 존재. baseline conditionScores 부재 시 attributionRepo 가 null 반환해 학습 오염 차단. LIVE PROVISIONAL 은 fillMonitor 의 confirm 시점 wiring 을 후속 PR 로 분리(REVERTED 위험 회피). 헬퍼는 별도 export 로 분리해 6 케이스 단위 테스트 가능 — baseline 있을 때 PARTIAL+qtyRatio 가중 / baseline 없을 때 null / remainingQty=0 스킵 / newFillId 누락 스킵 / originalQuantity fallback / holdingDays 계산. ② **M2 — `/api/kis/proxy` 화이트리스트 + 주문 TR 차단 (rule #2 단일 통로 강화)**: `server/routes/kisProxyPolicy.ts` 신설 — ALLOWED_PROXY_PATHS(quote/balance read-only 6 경로) + FORBIDDEN_PROXY_PATHS(order-cash/order-credit/order-rvsecncl) + FORBIDDEN_TR_IDS(TTTC0802U/VTTC0802U/TTTC0801U/VTTC0801U/TTTC0803U/VTTC0803U 매수·매도·취소 6종) 두겹 차단. `kisRouter.ts /proxy` 본체를 raw fetch 에서 `kisGet`/`kisPost` 경유로 교체 — 회로차단기(PR-21)·24h 블랙리스트(PR-24)·jitter backoff·idempotency(PR-34 ADR-0014) 가드 자동 적용. 회로 OPEN/블랙리스트 throw 는 503 으로 매핑(재시도 힌트). 15 케이스 정책 테스트 — allow 4/주문 TR 차단 4/입력 검증 5/상수 무결성 2. 클라이언트 `priceSync.ts` 의 inquire-price + FHKST01010100 경로는 화이트리스트 통과로 무중단. ③ **M3 — signalScanner inline 헬퍼 → preflight 단일 SSOT (PR-40 Phase A drift 차단)**: `signalScanner.ts` 의 `evaluateSellOnlyException`(L66~102) + `SellOnlyExceptionDecision` interface + `getAccountScaleKellyMultiplier`(L181~186) inline 정의 제거. `signalScanner/preflight.ts:82,113` 의 byte 동일한 export 만 사용. drift 위험 즉시 해소. signalScanner.ts 614 줄 → 약 -39 줄. `runPreflight()` 본 wiring(매크로 게이트 본체 교체)은 회귀 위험으로 후속 PR 로 분리(Phase B 핵심). 7 케이스 drift-guard 테스트 — preflight export 존재 / signalScanner.ts 본체 inline 정의 미보유 (regex 가드) / Kelly multiplier 임계 / sellOnlyException disabled/VIX/sectorAligned. ④ 검증: lint + validate:all (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary 6종) + precommit 전체 통과. `server/trading/` + `server/routes/` + `server/persistence/` 56 files **480/480** pass (신규 28 + 기존 452 무회귀). | `server/trading/exitEngine.ts` (+emitPartialAttributionForSell helper + reserveSell wiring), `server/trading/exitEngineAttribution.test.ts` (신규 6), `server/routes/kisProxyPolicy.ts` (신규), `server/routes/kisProxyPolicy.test.ts` (신규 15), `server/routes/kisRouter.ts` (proxy 라우트 kisGet/kisPost 전환 + getKisBase import 정리), `server/trading/signalScanner.ts` (inline 헬퍼 제거 + preflight import + FullRegimeConfig/MacroState 사용 자동 정리), `server/trading/signalScanner/preflightWiring.test.ts` (신규 7) | PR-41 audit 의 후속 정리 — 학습 데이터 품질 갭(M1) + rule #2 우회 경로(M2) + Phase A dead code drift(M3) 모두 본 PR 에서 해소. LIVE 자동매매 안전성에는 영향 없으나 운영 신뢰성·우회 차단 강화. |
| 2026-04-25 | PR-48 텔레그램 명령어 사용량 텔레메트리 + 폐기 후보 자동 리포트 + /help 개인화 Top 5 — Stage 3 (ADR-0017 §Stage 3 완료): 사용자 요청 "Stage 3 진행 — commandUsageRepo 사용량 텔레메트리 + 폐기 후보 자동 리포트 + /help 개인 Top". ① **`server/persistence/commandUsageRepo.ts` 신규** (185줄) — `recordUsage(name)` (lowercase 정규화 + count 증가 + lastUsedAt 갱신 + 200ms debounce flush) + `getTopUsage(N)` (count 내림차순, 동률은 lastUsedAt 최신순) + `getStaleCommands(registeredNames, daysThreshold, now?)` (등록명 vs 사용 이력 대조 — daysSinceLastUse=null 인 미사용 cmd 도 후보로 포함, 정렬은 daysSinceLastUse 내림차순 + 미사용은 마지막) + `getCommandStats(name)` + `getTotalUsage()` + `flushCommandUsage()` + `__resetForTests`. 슬래시 없는 입력은 무시(텍스트 메시지 차단), 외부 의존성 0(commandRegistry/telegram import 금지 — 순환 차단). `paths.ts` `COMMAND_USAGE_FILE = data/command-usage.json` 신규. ② **`webhookHandler.ts` wiring** — `recordUsage` import 추가, `cmd.toLowerCase()` 결과를 `lower` 로 캐싱한 뒤 (a) `/help` `/start` 분기에서 `getTopUsage(5)` → `buildHelpMessage(top5)` 전달 + `recordUsage(lower)`, (b) 6 메타 명령어 분기에서 `recordUsage(lower)`, (c) `default` 분기 `commandRegistry.resolve(lower)` 매칭 시 `handler.execute({args, reply})` 직후 `recordUsage(handler.name)` (정식명 사용 — alias 가 별도 카운터로 분산되지 않고 동일 인스턴스로 집계). unknown 명령어는 미기록(텔레메트리 오염 방지). ③ **`metaCommands.buildHelpMessage(topUsage?: HelpTopEntry[])` 확장** — 옵셔널 인자 추가 (Stage 3 backward-compat: 미전달 또는 빈 배열 시 개인화 섹션 미노출). Top N (≤5 절삭) 시 "📊 자주 쓰는 명령 Top N" 섹션을 메타 메뉴 헤더 위에 노출 + 순위/명령어/카운트 3컬럼 포맷. 푸터를 "Stage 1 메뉴 압축" 에서 "Stage 1+2+3 — 메뉴 압축 + 모듈 분해 + 사용량 텔레메트리" 로 갱신. ④ **`server/telegram/deprecationReport.ts` 신규** (95줄) — `collectDeprecationCandidates(daysThreshold, now?)` 가 `commandRegistry.all()` 의 unique cmd 객체로 `getStaleCommands` 호출 후 `category` 필드를 commandRegistry SSOT 에서 합성, `topUsage` 와 함께 `DeprecationReportData` 반환. `formatDeprecationReport(data)` 가 후보 0건 → "✅ 모든 명령어가 N일 내 사용" + Top N 안내 / 후보 ≥1 → 카테고리별 그룹핑(개수 내림차순) + 후보 라인 30개 절삭 + "외 N건" 안내 + 한 번도 사용 안 됨 vs N일 미사용 분기 + Top N 대비 표시 + 푸터 "매주 월요일 09:00 KST 자동 발송" 안내. HTML escape 로컬 사본(telegramClient 순환 차단). ⑤ **`server/scheduler/commandUsageJobs.ts` 신규** + `scheduler/index.ts` `registerCommandUsageJobs()` 호출 추가 — `runDeprecationReport(thresholdDays=30, now?)` async 헬퍼는 후보 0건 시 스킵(채팅 노이즈 방지) / 후보 ≥1 시 `sendTelegramAlert` (priority='NORMAL' + tier='T2_REPORT' + category='deprecation_report' + dedupeKey=`deprecation-report:{YYYY-MM-DD}`) 발송. cron `0 0 * * 1` UTC = KST 월요일 09:00 — 매주 1회 자동. ⑥ **회귀 테스트 22 케이스 신규** — `commandUsageRepo.test.ts` 12 (record 5: 첫 호출/3회 누적/대문자 정규화/슬래시 없는 입력 무시/디스크 schema 검증 + topN 3: 동률 lastUsedAt 우선/limit≤0 빈 배열/limit 초과 전체 + stale 3: 미사용 null/30일 분기/정렬 + round-trip 1) + `deprecationReport.test.ts` 6 (collect 3: 0건 사용 시 전체 후보/일부 사용 시 제외/category SSOT 동기화 + format 3: 0건 안내/30개 절삭+카테고리 그룹+Top 5/HTML escape) + `metaCommands.test.ts` +4 (Top 미전달 미노출/Top 전달 시 위치+카운트/빈 배열 미노출/6개 이상 5개 절삭). ⑦ 검증: vitest server/telegram + server/persistence/commandUsageRepo **5 files 95/95 pass** (신규 22 + Phase B3 simulation 25 + commandRegistry 16 + metaCommands 31(27기존+4신규) + deprecationReport 6) + lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). ⑧ 본 PR 로 **ADR-0017 Stage 1+2+3 PRD 완주** — Stage 1 메뉴 8개 압축(PR-43) + Stage 2 webhookHandler 1858→155 분해(PR-44~47) + Stage 3 텔레메트리/폐기 리포트/Top 5 개인화(PR-48). | `server/persistence/commandUsageRepo.ts` (신규 185줄), `server/persistence/commandUsageRepo.test.ts` (신규 12 케이스), `server/persistence/paths.ts` (+COMMAND_USAGE_FILE), `server/telegram/webhookHandler.ts` (+recordUsage 호출 3 위치 + getTopUsage Top 5 전달), `server/telegram/metaCommands.ts` (+HelpTopEntry + buildHelpMessage 옵셔널 인자), `server/telegram/metaCommands.test.ts` (+4 Top 5 케이스), `server/telegram/deprecationReport.ts` (신규 95줄), `server/telegram/deprecationReport.test.ts` (신규 6 케이스), `server/scheduler/commandUsageJobs.ts` (신규), `server/scheduler/index.ts` (+registerCommandUsageJobs), `docs/adr/0017-telegram-meta-commands-stage1.md` (Stage 3 구현 완료 표기) | 사용자 요청 원문 "Stage 3 진행 — commandUsageRepo 사용량 텔레메트리 + 폐기 후보 자동 리포트 + /help 개인 Top + PR 버전 업". 페르소나의 자기학습 루프(ADR-0007) 와 동일 패턴을 명령어 레이어에 이식 — 사용자가 안 쓰는 명령어가 "30일간 0회 사용" 으로 자동 표면화되어 다음 PR 에서 안전 폐기 검토 가능. /help 가 개인 사용 패턴을 학습해 Top 5 를 첫 줄에 노출 — 신규 사용자(Top 0건)에게는 영향 없음(미노출). |
| 2026-04-25 | PR-47 텔레그램 webhookHandler 분해 Stage 2 Phase B3 (최종) — TRD/EMR/Infra 17 명령 이전 + SHADOW 매매 흐름 시뮬레이션 (ADR-0017 §Stage 2 완주): 사용자 요청 "B3 진행 신중하게하고 가상 시뮬레이션으로 매매 실시 테스트 및 흐름파악". Phase B 의 마지막 슬라이스 — LIVE 매매·엔진 제어 그룹. ① **commands/control/*** 5 cmd (pause 33 + resume 29 + stop 23 + reset 34 + integrity 36 = 155 LoC) — 모두 category='EMR' + riskLevel=2 (제외: integrity=1). /reset 은 EMERGENCY_RESET_SECRET 검증 + clearCircuitBreaker + clearForcedRegimeDowngrade dynamic import 본체 100% 보존. /stop 은 setEmergencyStop(true) + cancelAllPendingOrders 호출 본체 동일. ② **commands/trade/*** 10 cmd (buy 37 + sell 179 + cancel 62 + adjustQty 120 + reconcile 189 + scan 26 + krxScan 60 + stage1Audit 53 + report 21 + shadow 39 = 786 LoC) + index barrel — /reconcile 은 /reconcile_qty alias 통합 + 6개 서브명령(apply/last/status/push/live/live apply) 분기 + ADR-0015 60s rate-limit 가드 모듈 로컬 상태로 이전. /sell 은 SHADOW 봉쇄 (학습 데이터 무결성 보호) + LIVE placeKisSellOrder + appendFill('SELL','STOP_LOSS') + MANUAL_EXIT 학습 격리 + manualOverrideMonitor 호출 본체 100% 보존. /cancel 은 KIS TTTC0803U/VTTC0803U dynamic import 본체 동일. /buy 는 워치리스트 검증 + runAutoSignalScan({forceBuyCodes}) 트리거. /adjust_qty 는 fills SSOT 우선 + 레거시 캐시 직접수정 fallback 분기. ③ **commands/infra/*** 2 cmd (refreshToken 32 + reconnectWs 75 = 107 LoC) — /refresh_token 은 invalidateKisToken+refreshKisToken+잔여시간 보고. /reconnect_ws 는 stopKisStream → 1s 대기 → startKisStream 4단계 본체 + MAX_SUBSCRIPTIONS gate score 절삭 본체 보존. ④ **webhookHandler.ts wiring + 본체 정리** — `./commands/{control,trade,infra}/index.js` barrel side-effect import 3줄 추가. 17 case 본체 (~656 LoC) sed 일괄 제거 + 미사용 import 47종 일괄 정리(_resetKrxOpenApiBreaker/getKrxOpenApiStatus/resetKrxOpenApiCache/generateDailyReport/fetchCurrentPrice/getKisTokenRemainingHours/refreshKisToken/invalidateKisToken/placeKisSellOrder/MAX__lastLiveReconcileApplyAt 모듈 로컬 state). 파일 헤더 주석도 "지원 명령어:" 나열 제거하고 "ADR-0017 Stage 2 A+B1+B2+B3 — commands/*/*.cmd.ts 가 commandRegistry 에 자동 등록" SSOT 안내로 교체. **webhookHandler.ts 1858 → 155 줄 (-1703, -91.7%)** — 헤더 5 + import 24 + sentinel 1 + handleTelegramWebhook 124 = 154 LoC (1500줄 임계 대비 1/10 수준). ⑤ **SHADOW 매매 흐름 시뮬레이션** (`tradeFlowSimulation.test.ts` 신규 25 케이스) — 사용자 요청 "가상 시뮬레이션으로 매매 실시 테스트 및 흐름파악": Step 1 /pause+/resume 토글 5 (초기→pause / 두번째 pause 무시 / resume 토글 / resume 빈상태 / 비상정지중 pause 차단), Step 2 /stop+/reset 토글 4 (stop+cancelAllPendingOrders 호출 검증 / reset 정상 / 잘못된 비밀번호 차단 / 정확 비밀번호 통과), Step 3 비상정지시 신규 매매 차단 2 (/scan + emergencyStop=true → runAutoSignalScan 미호출 / /krx_scan 차단), Step 4 /integrity 토글 2 (조회 / clear 해제), Step 5 SHADOW /sell 봉쇄 3 (SHADOW 차단 / 보유 없음 / 잘못된 코드), Step 6 registry resolve 6 (control 5 EMR / trade 10 TRD / reconcile alias / infra 2 EMR riskLevel=1 / LIVE 11 riskLevel=2 / 누적 ≥51 unique 8 카테고리 모두 등록), Step 7 입력 가드 3 (/buy 잘못된 코드 → scan 미호출 / /cancel 잘못된 코드 / /adjust_qty 음수). 모든 외부 의존성(KIS 주문/현재가/취소/cancelAllPendingOrders) vi.spyOn stub — KIS 호출 0건. ⑥ 검증: vitest server/telegram **3 files 73/73 pass** (commandRegistry 16 + metaCommands 27 + tradeFlowSimulation 25 + alerts 5 무회귀, 총 8 files 108/108 pass) + lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범. ⑦ **CLAUDE.md "기존 복잡도 위반" 표에서 webhookHandler.ts 정식 제거** — 1858 → 155 (-91.7%) 로 P1 우선순위 완주. ⑧ **누적 진행도 (PR-43~47 5건)** — Stage 1 메뉴 압축 (1858 무변경 / setMyCommands 43→8) → Phase A SYS 9 cmd (1858→1603) → Phase B1 WL+POS+ALR 17 cmd (1603→1057) → Phase B2 LRN 8 cmd (1057→924) → **Phase B3 TRD+EMR+Infra 17 cmd (924→155)**. 총 51 명령 51 cmd 객체 8 디렉토리(system/watchlist/positions/alert/learning/control/trade/infra) 분리 완료. 새 명령어 추가 = 파일 1개 + barrel 1줄. | `server/telegram/commands/control/*` (5 cmd + barrel = 6 신규파일, 163 LoC), `server/telegram/commands/trade/*` (10 cmd + barrel = 11 신규파일, 799 LoC), `server/telegram/commands/infra/*` (2 cmd + barrel = 3 신규파일, 112 LoC), `server/telegram/tradeFlowSimulation.test.ts` (신규 25 케이스), `server/telegram/webhookHandler.ts` (1858 → 155, sed 일괄 제거 + 47 import 정리 + 헤더 SSOT 갱신), `CLAUDE.md` (복잡도 위반 표에서 webhookHandler 정식 제거) | 사용자 요청 "B3진행 신중하게하고 가상 시뮬레이션으로 매매 실시 테스트 및 흐름파악". Phase B3 가 LIVE 매매·엔진 제어 직접 영향 그룹이므로 (a) byte-equivalent extraction 으로 매매 본체 변경 0, (b) SHADOW 매매 흐름 25 케이스 시뮬레이션 — pause/resume/stop/reset/integrity/scan/buy/sell/cancel/adjust_qty 7 단계 단계별 검증으로 회귀 위험 사전 차단, (c) /sell SHADOW 봉쇄(학습 무결성) + 비상정지 시 scan 차단(매매 가드) + 비밀번호 인증(/reset) 3대 안전장치 명시 검증. webhookHandler.ts 가 91.7% 감축 — Stage 2 webhookHandler 분해 PRD 완주. ADR-0017 Stage 3 (commandUsageRepo 텔레메트리 + 폐기 후보 자동 리포트 + /help 개인 Top 5) 후속 PR 분리 가능. |
| 2026-04-25 | PR-46 텔레그램 webhookHandler 분해 Stage 2 Phase B2 — LRN 8 명령 이전 (ADR-0017 §Stage 2): 사용자 요청 "b2". Phase B 의 두 번째 슬라이스 — 학습/리스크/회로 그룹. ① **commands/learning/*** 8 cmd + index barrel — risk(31, /risk_budget alias) + kellySurface(19) + regimeCoverage(19) + ledger(36) + counterfactual(32) + kelly(34) + circuits(39) + resetCircuits(31) = 241 LoC. /risk /kelly_surface /regime_coverage /ledger /counterfactual /kelly /circuits 7건은 riskLevel=0 (read-only), /reset_circuits 만 riskLevel=1 (light mutate — KIS/KRX 회로 즉시 해제). 모두 category='LRN'. ② **webhookHandler.ts wiring** — `./commands/learning/index.js` barrel side-effect import 1줄 추가, 8 case 본체 (~133 LoC) 제거. 미사용 import 11종 정리(loadKellyDampenerState/formatKellyHealthCards/formatKellySurface/formatRegimeCoverage/getUniverseStats/getCounterfactualStats/loadTradingSettings/getAccountRiskBudget/formatAccountRiskBudget/getCircuitBreakerStats/resetKisCircuits). **webhookHandler.ts 1057 → 924 줄 (-133, -12.6%)**, Phase A+B1+B2 누적 1858 → 924 (**-50%**). ③ **회귀 테스트 5 케이스 추가** (`commandRegistry.test.ts` Phase B2 describe block) — 8 LRN 명령 resolvable + /risk↔/risk_budget alias 동일 instance + 모든 LRN 명령 category='LRN' + /reset_circuits riskLevel=1 / 나머지 7개 riskLevel=0 + 누적 ≥34 unique 인스턴스 5 카테고리(SYS/WL/POS/ALR/LRN) 모두 등록. ④ 검증: vitest server/telegram + server/alerts 7 files **83/83 pass** (신규 5 + Phase B1 5 + Phase A 11 + Stage 1 27 + 기존 35 무회귀) + lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 case (/buy /sell /stop /reset /pause /resume /reconcile /scan /krx_scan /cancel /adjust_qty /report /shadow /stage1_audit /refresh_token /reconnect_ws /integrity) 무수정 — Phase B3 (TRD/EMR LIVE 17개) 후속 PR 분리 예정. ⑤ Phase B3 완료 시 webhookHandler.ts ≤50줄 + CLAUDE.md "복잡도 위반" 표 P1 항목 정식 제거. | `server/telegram/commands/learning/*` (8 cmd + 1 barrel = 9 신규파일, 252 LoC), `server/telegram/commandRegistry.test.ts` (+5 케이스), `server/telegram/webhookHandler.ts` (1057 → 924, 8 case 제거 + 11 import 정리 + 1 barrel import) | 사용자 요청 "b2" — Phase B2 점진 분해 계속. 학습/리스크 그룹은 read-only 위주이고 /reset_circuits 만 회로 reset 액션을 가짐 (LIVE 매매 무영향). webhookHandler.ts 가 1500줄 임계 아래 924줄까지 진입 — Phase B3 만 남기고 50% 감축 달성. |
| 2026-04-25 | PR-45 텔레그램 webhookHandler 분해 Stage 2 Phase B1 — WL 5 + POS 3 + ALR 9 = 17 명령 이전 (ADR-0017 §Stage 2): 사용자 요청 "다음 — Phase B 진행". Phase B 가 너무 커서 위험도 기준 B1(low-risk read-only/light-mutate, no LIVE trading) → B2(LRN/EMR) → B3(TRD LIVE) 3 단계로 더 쪼개고 본 PR 은 B1 만 처리. ① **commands/watchlist/*** 5 cmd (watchlist 86 + focus 70 + add 63 + remove 35 + watchlistChannel 26 = 280 LoC) + index barrel — `/watchlist /focus` 는 read-only(riskLevel 0), `/add /remove /watchlist_channel` 는 light mutate(riskLevel 1). `/add` 는 KIS 현재가 조회 → -8% 손절/+15% 목표 자동 설정 → calcRRR 계산 → STOCK_UNIVERSE+fetchStockName 로 종목명 fallback (절대 규칙 #2 kisClient 단일 통로 준수). ② **commands/positions/*** 3 cmd (pending 33 + pnl 89 + pos 55 = 177 LoC) + index barrel — 모두 riskLevel 0. `/pnl` 은 PR-8 fills SSOT 기반 realized + unrealized 분리 표시 본체 100% 보존. `/pos` 는 캐시 drift 감지 (s.quantity ≠ getRemainingQty) 경보 유지. ③ **commands/alert/*** 9 cmd (channelHealth 37 + channelStats 52 + alertReplay 58 + alertHistory 39 + channelTest 39 + dxy 41 (+/dxy_intraday alias) + newsLag 43 (+/news_patterns alias) + digest 51 (3 commands: digest_on/off/status) = 360 LoC) + index barrel — 9 unique cmd 객체 = 12 callable 키. `/channel_test` 는 `sendChannelAlert` dynamic import 유지, `/dxy` 는 dxyMonitor dynamic import + ALPHA_VANTAGE_API_KEY fallback 메시지 보존, `/news_lag` 는 newsLagBayesian listAllOptimalWindows 카탈로그 본체 보존. ④ **webhookHandler.ts wiring** — 3 신규 barrel side-effect import 추가 (`./commands/{watchlist,positions,alert}/index.js`) + 17 case 본체 (총 ~600 LoC) 제거. 미사용 import 11종 정리(saveWatchlist/WatchlistEntry/fetchStockName/STOCK_UNIVERSE/calcRRR/getTotalRealizedPnl/runChannelHealthCheck/AlertCategory/getChannelStatsByDate/getRecentDateKeys/findAlertHistoryById/dispatchAlert/getRecentAlertHistory/isDigestEnabled/setDigestEnabled). **webhookHandler.ts 1603 → 1057 줄 (-546, -34%)**, Phase A+B1 누적 1858 → 1057 (-43%). ⑤ **회귀 테스트 5 케이스 추가** (`commandRegistry.test.ts` Phase B1 describe block) — WL 5명 resolvable + POS 3명 resolvable + ALR 12 keys resolvable + dxy/news_lag 별칭 동일 인스턴스 + Phase A+B1 누적 ≥26 unique 인스턴스 + 4 카테고리(SYS/WL/POS/ALR) 모두 등록 확인. ⑥ 검증: vitest server/telegram + server/alerts 7 files **78/78 pass** (신규 5 + Phase A 11 + Stage 1 27 + 기존 35 무회귀) + lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 case (/buy /sell /stop /reset /pause /resume /reconcile /scan /krx_scan /cancel /adjust_qty /report /shadow /stage1_audit /refresh_token /reconnect_ws /integrity) 무수정 — Phase B2 (LRN /kelly* /ledger /counterfactual /risk /circuits /reset_circuits 9개) + Phase B3 (TRD/EMR LIVE 17개) 후속 PR 분리. ⑦ 향후 Phase B2/B3 완료 시 webhookHandler.ts ≤50줄 + CLAUDE.md "복잡도 위반" 표에서 제거. | `server/telegram/commands/watchlist/*` (5 cmd + 1 barrel = 6 신규파일), `server/telegram/commands/positions/*` (3 cmd + 1 barrel = 4 신규파일), `server/telegram/commands/alert/*` (8 cmd + 1 barrel = 9 신규파일, digest.cmd.ts 가 3 명령 통합), `server/telegram/commandRegistry.test.ts` (+5 케이스), `server/telegram/webhookHandler.ts` (1603 → 1057, 17 case 제거 + 11 import 정리 + 3 barrel import) | 사용자 요청 "다음" — Phase B 점진 분해. 자동매매 안전을 위해 위험도 기준 B1(read-only + light-mutate) → B2(학습 read-only) → B3(LIVE 매매) 분리 진행. 본 PR 으로 webhookHandler.ts 가 1500줄 임계 아래 1057줄로 진입 — CLAUDE.md "복잡도 위반" 표 P1 기록은 Phase B3 완료 시 정식 제거 예정. |
| 2026-04-25 | PR-44 텔레그램 webhookHandler 분해 Stage 2 Phase A — commandRegistry 프레임워크 + read-only 9개 이전 (ADR-0017 §Stage 2): 사용자 요청 "Stage 2 — Phase A read-only 명령어 먼저". ① **`server/telegram/commands/_types.ts` 신규** (66줄) — `TelegramCommand` 인터페이스(name+aliases+category+visibility+riskLevel+description+usage?+execute) + `CommandCategory`(SYS/MKT/WL/POS/TRD/LRN/ALR/EMR 8값) + `CommandVisibility`(MENU/ADMIN/HIDDEN) + `CommandRiskLevel`(0/1/2) + `CommandReplyFn` (metaCommands.ts 의 InlineKeyboardMarkup 재사용) + `CommandContext`(args/reply). ② **`server/telegram/commandRegistry.ts` 신규** (55줄, @responsibility SRP 통과) — `byName: Map<string, TelegramCommand>` SSOT + `register(cmd)` (name + aliases 모두 소문자 키로 등록, 동일 키 다른 instance 시 throw — drift 차단, 동일 instance 재등록은 idempotent — HMR 안전) + `resolve(input)` 소문자 매칭 + `all()` Set 으로 unique instance 반환 (alias 중복 제거) + `keys()` 모든 키 반환 + `__resetForTests()`. 프로세스 전역 단일 인스턴스 export. ③ **`server/telegram/commands/system/*.cmd.ts` 9 신규** (총 463줄) — market(20)/status(50)/regime(39)/health(168)/aiStatus(34)/scheduler(36, /schedule alias)/learningStatus(26)/learningHistory(29)/todaylog(44). 각 파일은 import 시점에 `commandRegistry.register(cmd)` 자체 호출 (side-effect). status.cmd.ts 는 기존 `(s as any).status` cast 를 `(s as { status?: string }).status` 로 안전 cast 전환. health.cmd.ts 는 8축 헬스(워치/포지션/KIS/Yahoo/DART/Gemini/Volume/Stream) 본체 100% 보존 — guardedFetch + DART 직접 fetch (별도 host) + verdict 8단계 + Yahoo heartbeat fallback 모두 동일. ④ **`server/telegram/commands/system/index.ts` 신규** (17줄) — barrel side-effect import 9건. 신규 read-only 명령 추가 시 본 파일에 한 줄만 추가. ⑤ **`webhookHandler.ts` 1858 → 1603줄 (-255, -13.7%)**: `import './commands/system/index.js'` (barrel side-effect) + `import { commandRegistry }` 추가, switch `default:` 본체에 `const handler = commandRegistry.resolve(cmd.toLowerCase()); if (handler) { await handler.execute({ args, reply }); break; }` 위임 분기 추가, **9 case 본체 제거** (/market 5줄 + /status 29줄 + /regime 24줄 + /scheduler+/schedule 16줄 + /learning_status 11줄 + /learning_history 12줄 + /todaylog 30줄 + /health 102줄 + /ai_status 16줄 = 245줄), 미사용 import 19종 정리(sendMarketSummaryOnDemand/getBudgetState/getGeminiCircuitStats/formatScheduler*4/getLearningStatus/formatLearningStatusMessage/getLearningHistory/formatLearningHistoryMessage/readAlertAuditRange/getRealDataTokenRemainingHours/getYahooHealthSnapshot/getLastScanAt/getLastBuySignalAt/getLastScanSummary/verifyVolumeMount/guardedFetch). isOpenShadowStatus + getStreamStatus 는 다른 case 가 사용 중이라 보존. ⑥ **회귀 테스트 11 케이스 신규** (`commandRegistry.test.ts`) — registry semantics 6 (resolve case-insensitive / alias resolution / unknown undefined / dupe name throws / dupe alias throws / idempotent re-register) + system barrel 5 (9 정식명 등록 / /scheduler↔/schedule alias / interface contract 검증 (riskLevel=0 SRP 가드) / all() unique / keys() alias 포함). ⑦ 검증: vitest server/telegram + server/alerts 7 files **73/73 pass** (신규 11 + Stage 1 27 + 기존 35 무회귀) + lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 case (/buy /sell /stop /reset /pause /resume /reconcile) 무수정 — Stage 2 Phase B 후속 PR 로 분리. ⑧ Stage 2 Phase B (후속 PR) 잔여: WL(/watchlist /focus /add /remove /watchlist_channel) + POS(/pos /pnl /pending) + ALR(/channel_* /alert_* /digest_* /dxy /news_lag) + LRN(/kelly /kelly_surface /regime_coverage /ledger /counterfactual /risk /circuits /reset_circuits) + TRD/EMR(/buy /sell /scan /krx_scan /cancel /stage1_audit /pause /resume /stop /reset /integrity /refresh_token /reconnect_ws /report /shadow /adjust_qty /reconcile) 약 50개 case. Phase B 완료 시 webhookHandler.ts ≤50줄 + CLAUDE.md "복잡도 위반" 표에서 제거. | `server/telegram/commands/_types.ts` (신규 66줄), `server/telegram/commandRegistry.ts` (신규 55줄), `server/telegram/commands/system/*.cmd.ts` (신규 9파일 446줄), `server/telegram/commands/system/index.ts` (신규 17줄), `server/telegram/commandRegistry.test.ts` (신규 11 케이스), `server/telegram/webhookHandler.ts` (1858 → 1603) | 사용자 요청 "Phase A — read-only 명령어 먼저". ADR-0017 Stage 2 Phase A 의 프레임워크(_types + commandRegistry + barrel) + 9 read-only 명령 이전이 단일 PR 로 완료. 향후 새 명령어 추가는 `commands/<group>/<name>.cmd.ts` 파일 1건 + barrel 1줄로 끝. webhookHandler.ts 의 거대 switch 가 1603줄로 축소 — Phase B 에서 LIVE 매매 명령어 신중히 옮긴 후 ≤50줄 라우터로 마무리 예정. |
| 2026-04-26 | PR-B-2 79 cron 일괄 ScheduleClass 적용 (ADR-0043 §"후속" 완료): PR-B 가 learningJobs.ts 13개 cron 만 ScheduleClass 래퍼로 변환했고, 다른 9개 스케줄러 파일의 79 cron 은 점진 적용 예정으로 분리됐다. 본 PR-B-2 가 그 잔여 cron 을 일괄 byte-equivalent 변환. ① **9개 파일 79 cron 모두 `scheduledJob(cronExpr, ScheduleClass, jobName, fn, options)` 래퍼 경유** — `cron from 'node-cron'` import 제거, `import { scheduledJob } from './scheduleGuard.js'` 로 통일. fn 시그니처는 `() => Promise<unknown> \| unknown` 으로 완화 (호출자 wrap 코드 추가 불필요). ② **ScheduleClass 매트릭스 (79 cron)**: TRADING_DAY_ONLY 약 50개 (orchestrator_tick / shadow_resolver_tick / kis_stream_start·watchdog·stop / oco_confirm·survival·recovery / portfolio_risk_check / cancel_all_active_oco / sell_poll_start·stop / macro_sync_day_open·alignment / dart_poll·fast / bear_regime / mhs_morning / adr_gap_scan / foreign_flow_leading / pre_market_card·hk / intraday_yield_tick / holiday_resume_alert / pipeline_health_check / self_diagnosis / stage1_pre_screening / stage2_3_final_screening / cleanup_watchlist·intraday / news_supply_tracker / circuit_auto_reset / daily_pick_report / scan_retrospective / morning_position_card / sector_cycle_dashboard / high_52w_scan / weekly_deep_analysis / weekly_quant_insight / market_regime_refresh_morning·close / morning·lunch·eod_briefing / info_digest_flush / system_weekly_flush / data_completeness_reset / shadow_qty_dryrun_broadcast / sector_map_daily_retry), WEEKEND_MAINTENANCE 7개 (cleanup_trace_files / sector_map_weekly / dynamic_universe_expansion / supply_chain_scan_02·10kst / weekly_integrity_report), ALWAYS_ON 약 22개 (kis_token_refresh ×2 / ips_alert / dxy_us_close·kr_open / dxy_intraday_us_session·lunch / sector_etf_momentum / ack_sweep / weekly_report / weekly_condition_scorecard / hourly_canary / backup_ceremony / daily_backup / daily_reconcile / kis_shadow_reconcile / krx_holiday_audit / us_premarket·postmarket_scan / global_scan_agent / deprecation_report). ③ **이중 방어층 동작**: cron 표현식의 평일 제한(`1-5` / `0-4`)이 1차 방어선 (주말 새벽 cron 자체 실행 차단), ScheduleClass 가 KRX 공휴일 평일에 차단하는 진짜 방어선 (5/5 어린이날·9/24~9/26 추석 등). ④ **wrapJob 흡수** — maintenanceJobs.ts 의 `wrapJob('daily_reconcile', ...)` / `wrapJob('shadow_qty_dryrun_broadcast', ...)` 가 `scheduledJob` 으로 통합. wrapJob 자체는 `scheduleCatalog.ts` 에 export 유지 (다른 모듈 호환). ⑤ **JobMetrics 자동 등록** — 79 cron 모두 jobName 명시로 `getAllJobMetrics()` 에서 한눈에 조회 가능. 운영자가 `/scheduler history` 텔레그램 명령에서 "어떤 cron 이 가장 많이 스킵됐나" 시계열 진단. lastSkipReason ('WEEKEND' / 'KRX_HOLIDAY' / 'LONG_HOLIDAY' / 'TRADING_DAY' / 'NON_TRADING_DAY') 자동 기록. ⑥ 검증: vitest **server/scheduler + server/learning + server/utils + server/trading + server/persistence 122 files 985/985 pass** (PR-D 영향 영역 무회귀) + lint(client + server tsc) + validate:all (Gemini + ACMA + SDS + PRES + SRP + SymbolBoundary 6종) + precommit 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** — kisClient/signalScanner/autoTradeEngine/tradingOrchestrator/entryEngine 본체 무수정. LIVE 매매 본체 0줄 변경. cron 표현식·timezone·콜백 본체 모두 byte-equivalent 보존. ⑦ **운영 효과**: KRX 공휴일이 평일에 떨어진 경우(어린이날·추석) 50+ 평일 cron 모두 자동 silent SKIP — 기존엔 cron 표현식이 1-5 만 가드 (`pollMhsMorningAlert`, `runAdrGapScan`, `sendPositionMorningCard` 등 오작동 위험)했지만 본 PR 이후 ScheduleClass 가 자동 차단. KIS 호출·KRX 호출도 동일 차단 → 회로차단기 부담 제거. | `server/scheduler/{shadowResolverJob,orchestratorJobs,healthCheckJob,commandUsageJobs,kisStreamJobs,tradeFlowJobs,screenerJobs,alertJobs,reportJobs,maintenanceJobs}.ts` (9개 파일 79 cron 일괄 변환), `server/scheduler/scheduleGuard.ts` (fn 시그니처 unknown 완화) | 사용자 P3 후속 — PR-B 의 인프라 (scheduledJob 래퍼) 가 learningJobs 13개 cron 만 적용됐던 한계 해소. 본 PR 으로 9개 도메인 79 cron 모두 ScheduleClass 단일 SSOT 보호망 안. KRX 휴장일이 평일에 떨어진 시점에 자동매매·알림·리포트·유지보수 cron 모두 자동 SKIP — 운영자 개입 없이 안전 동작. PR-B-3 (commandUsageJobs / kisStreamJobs / tradeFlowJobs 의 더 정밀한 ScheduleClass 분기) 같은 후속 PR 은 운영 데이터 누적 후 도입 검토. |
| 2026-04-26 | PR-D KRX 휴장일 연간 감사 + Patch 영속 (ADR-0045, PR-A/B/C 후속): `KRX_HOLIDAYS` 정적 하드코딩 Set 의 연간 갱신 누락 위험을 운영자 알림 + 영속 patch 로 차단. ① **`server/persistence/krxHolidayRepo.ts` 신설** (영속 patch) — `KrxHolidayPatchEntry`(date / reason / addedAt / addedBy: 'manual'\|'audit'\|'sync') + `loadKrxHolidayPatch()` Set 반환 + `loadKrxHolidayPatchEntries()` 디버깅용 + `appendKrxHolidayPatch(entries)` idempotent (동일 날짜 덮어쓰기, 정렬 유지) + `removeKrxHolidayPatchByDate(date)` + 손상 JSON fallback (시스템 무중단). atomic write (tmp→rename). ② **`server/trading/krxHolidays.ts` mutable runtime Set 전환** — 정적 `STATIC_HOLIDAYS` (private const) + `_runtimeSet = new Set(STATIC_HOLIDAYS)` (mutable) → `KRX_HOLIDAYS: ReadonlySet<string> = _runtimeSet` 으로 export. `reloadKrxHolidaySet()` 호출 시 `_runtimeSet.clear() + STATIC + patch` 합집합으로 갱신. 외부 호출자(`trancheExecutor` 포함) 가 import 한 인스턴스가 동일하므로 자동 반영 — 호출자 코드 0줄 변경. `getStaticKrxHolidays()` view 헬퍼 추가 (감사·테스트용). ③ **`server/trading/krxHolidayAudit.ts` 신설** (감사 + 알림) — `runKrxHolidayAudit(now)` async 진입점. 차년도 휴장일 ≥ 8개 (한국 평균 공휴일 최소치) 등록되어 있으면 silent (`reason='NEXT_YEAR_REGISTERED'`), 0개면 `'NEXT_YEAR_MISSING'` + CRITICAL 텔레그램, 1~7개면 `'NEXT_YEAR_INSUFFICIENT'` + CRITICAL. dedupeKey `krx-holiday-audit:{nextYear}` + 1년 cooldown — 같은 해 재발송 차단, 다음 해 재발송 허용. `getRegisteredYears()` / `countHolidaysInYear(year)` / `formatAuditMessage()` 헬퍼 별도 export (단위 테스트). 텔레그램 throw 시 audit 자체는 alerted=true 결과 보존. ④ **`server/scheduler/maintenanceJobs.ts` 매년 12/1 cron + 부팅 reload** — `cron.schedule('0 0 1 12 *', ...)` (UTC 12/1 00:00 = KST 12/1 09:00) 매년 1회 audit 실행. 부팅 시점에 `reloadKrxHolidaySet()` 1회 호출 (migrateAttributionRecords 직후 동일 위치). ⑤ **`server/scheduler/scheduleCatalog.ts` SCHEDULE_CATALOG 항목 추가** — `12/1 09:00` `krx_holiday_audit` (maintenance 그룹, silentWhen 명시). `/scheduler` 텔레그램 명령에서 즉시 확인 가능. ⑥ **`server/persistence/paths.ts` `KRX_HOLIDAY_PATCH_FILE` 경로 추가** (`data/krx-holiday-patch.json`). ⑦ 회귀 테스트 23 케이스 신규 — `krxHolidayRepo.test.ts` 8 (빈 파일 / append 1 / idempotent 0 / 정렬 보존 / 잘못된 날짜 무시 / remove 성공 / remove false / 손상 JSON fallback) + `krxHolidaysReload.test.ts` 5 (reload 전 STATIC 만 / reload 후 patch 추가 / KRX_HOLIDAYS 인스턴스 동일 갱신 / STATIC 보존 / patch 제거 후 reload / getStaticKrxHolidays view) + `krxHolidayAudit.test.ts` 10 (getRegisteredYears 4 + formatAuditMessage 2 + runKrxHolidayAudit 4 분기: silent / NEXT_YEAR_MISSING + 텔레그램 / NEXT_YEAR_INSUFFICIENT / telegramClient throw 흡수). vi.resetModules() 패턴으로 paths.ts DATA_DIR 캐시 갱신. ⑧ 검증: vitest **server/scheduler + server/learning + server/utils + server/trading + server/persistence 122 files 985/985 pass** (신규 23 + 기존 962 무회귀) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종) + precommit 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** — 외부 KRX API 미연동, 운영자 알림 + 디스크 patch 만. LIVE 매매 본체 0줄 변경. ⑨ **운영 시나리오**: 매년 12/1 09:00 KST 자동 검증 → 차년도 휴장일 등록 부족 시 CRITICAL 텔레그램 → 운영자가 `data/krx-holiday-patch.json` 편집 → 서버 재시작 (또는 `reloadKrxHolidaySet()` 명시 호출) → 다음 audit 부터 silent. ⑩ **PR-D-2 후속 (외부 API 자동 fetch)**: 한국 공공데이터포털 `getRestDeInfo` API 인증 키 확보 후 `addedBy: 'sync'` 자동 patch 도입. 본 PR 의 SSOT 인터페이스 위에서 1줄 wiring. | `docs/adr/0039-krx-holiday-annual-audit.md` (신규), `server/persistence/krxHolidayRepo.ts` (신규), `server/persistence/krxHolidayRepo.test.ts` (신규 8 케이스), `server/persistence/paths.ts` (+KRX_HOLIDAY_PATCH_FILE), `server/trading/krxHolidays.ts` (mutable runtime Set + reloadKrxHolidaySet + getStaticKrxHolidays + isKrxHoliday 동적 반영), `server/trading/krxHolidaysReload.test.ts` (신규 5 케이스), `server/trading/krxHolidayAudit.ts` (신규), `server/trading/krxHolidayAudit.test.ts` (신규 10 케이스), `server/scheduler/maintenanceJobs.ts` (+12/1 cron + 부팅 reload), `server/scheduler/scheduleCatalog.ts` (+12/1 09:00 항목), `ARCHITECTURE.md` (+2 boundary +1 boundary rule) | 사용자 P3 — KRX 휴장일 자동 동기화. 외부 API 인증 키 부재 환경에서 운영자 알림 + 영속 patch 로 연간 갱신 누락 자동 감지. PR-A/B/C 의 모든 가드(자기반성·스케줄러·연휴 정책)가 KRX_HOLIDAYS Set 위에 의존하므로 본 PR 이 그 SSOT 의 연간 무결성을 보장. trancheExecutor 등 기존 호출자 0줄 변경 (인스턴스 그대로 동적 반영). |
| 2026-04-26 | PR-C 연휴 복귀 보수 매매 정책 SSOT (ADR-0044, PR-B 직접 후속): PR-B 의 `MarketDayClassifier` 가 `POST_HOLIDAY + isLongHoliday` 분류 SSOT 를 정착시켰으나, 그 분류를 매매 행동에 연결하는 정책이 없어 5/4 월(근로자의 날+주말 직후)·9/28 월(추석 직후) 같은 시점에 시스템이 평상시와 동일하게 작동하던 갭 — 사용자 페르소나 "연휴 후 시초 30분 관찰" 정책을 코드로 반영. ① **`server/trading/holidayResumePolicy.ts` 신설** (130 LoC) — `HolidayResumePolicy` 인터페이스(id / reason / kellyMultiplier=0.5 / gateScoreBoost=+1 / marketOpenDelayMin=30 / expirationKstTime='12:00') + `getDefaultHolidayResumePolicy()` env 기반 factory(`HOLIDAY_RESUME_KELLY_MULT` / `HOLIDAY_RESUME_GATE_BOOST` / `HOLIDAY_RESUME_OPEN_DELAY_MIN` / `HOLIDAY_RESUME_EXPIRATION_KST` 4 env 오버라이드) + `resolveHolidayResumePolicyForContext(ctx, now)` 활성 조건 SSOT (POST_HOLIDAY + isLongHoliday + 만료 시각 미도달 모두 충족 시 정책 반환, 외 null) + 헬퍼 3종(applyKellyMultiplierWithHolidayPolicy / applyGateBoostWithHolidayPolicy / isWithinMarketOpenDelay). 외부 의존성 marketDayClassifier 만 import — state/persistence/clients import 금지. `BudgetPolicy`(ADR-0036) 와 별개 운영 — kellyMultiplier 는 fractionalKellyCap 위에 곱해지는 추가 축소 계수. ② **`server/trading/holidayResumeAlert.ts` 신설** (75 LoC) — `runHolidayResumeAlert(now)` cron 함수 + `formatHolidayResumeMessage` 메시지 포맷터(별도 export — 단위 테스트 가능). 활성 정책 시 텔레그램 발송(`priority='HIGH'` + `tier='T2_REPORT'` + `category='holiday_resume'` + `dedupeKey='holiday-resume:{date}'` + `cooldownMs=24h`). 비활성 시 silent return — 평상 영업일은 발송 0건. ③ **`server/scheduler/alertJobs.ts` cron 등록** — `5 0 * * 1-5` (UTC 평일 00:05 = KST 평일 09:05). MHS 모닝 알림(09:00) 직후 5분 슬롯에 분리 — 두 메시지 충돌 방지. 평일 영업일에만 cron 자체 실행, KRX 공휴일은 함수 내부 ctx 가드로 자동 silent. ④ **`server/scheduler/scheduleCatalog.ts` SCHEDULE_CATALOG 항목 추가** — `09:05` `holiday_resume_alert` (alerts 그룹, silentWhen 명시 — POST_HOLIDAY+isLongHoliday 미충족 시 무음). `/scheduler` 텔레그램 명령에서 운영자가 즉시 확인 가능. ⑤ **PR-B 부수 버그 수정** — `marketDayClassifier.getMarketDayContext()` 의 `isLongHoliday` 가 영업일 케이스에서 항상 false 를 반환하던 버그(원인: 본인이 영업일이면 next=prev=date → nonTradingGap=-1) 발견·수정. 영업일 분기에서 `isLongHoliday = gapFromPrev >= LONG_HOLIDAY_GAP_DAYS+1` 로 분리(직전 영업일까지 4일 거리 = 비영업 클러스터 3일 + 본 영업일 1일). PR-B 의 17 케이스 회귀 영향 — 모두 통과(LONG_HOLIDAY_START/END 분류는 비영업일 분기로 일관). ⑥ 회귀 테스트 31 케이스 신규 — `holidayResumePolicy.test.ts` 24 (getDefaultHolidayResumePolicy 1 + resolveHolidayResumePolicyForContext 7 분기: TRADING_DAY null/단순 POST_HOLIDAY null/POST_HOLIDAY+isLongHoliday 활성/추석 직후 9/28 활성/12:01 만료/11:59 활성/WEEKEND null/LONG_HOLIDAY_END null + applyKellyMultiplierWithHolidayPolicy 4 (null no-op/0.5 효과/음수 안전/NaN 안전) + applyGateBoostWithHolidayPolicy 3 + isWithinMarketOpenDelay 8 (null/marketOpenDelayMin=0/09:00 boundary/09:15/09:29/09:30 boundary/08:59/15:00)) + `holidayResumeAlert.test.ts` 7 (TRADING_DAY silent / 단순 POST_HOLIDAY silent / telegramClient throw error 처리 / 활성 강제 + 메시지 포맷 검증 / formatHolidayResumeMessage null 빈문자열 / 활성 정책 7+ 라인 / expirationKstTime 빈 문자열 만료 라인 미포함). ⑦ 검증: vitest **server/scheduler + server/learning + server/utils + server/trading 97 files 796/796 pass** (신규 31 + 기존 765 무회귀 + PR-B 17 회귀 모두 통과) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종) + precommit 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** — kisClient/signalScanner/autoTradeEngine/tradingOrchestrator/entryEngine 본체 무수정. LIVE 매매 본체 0줄 변경. ⑧ **활성 시나리오 (배포 후 자동 발송)**: 5/4 월 (근로자의 날 + 주말, 4일) ✅ / 9/28 월 (추석 연휴, 5일) ✅ / 10/12 월 (한글날 + 주말, 4일) ✅ / 2027-02-09 화 (설날 + 주말, 6일) ✅. 비활성: 5/6 수 (어린이날 단일 휴장 직후 — gapFromPrev=2). ⑨ **PR-C-2 후속 분리 (본 PR scope 밖)**: entryEngine `evaluateEntryRevalidation` 가 `applyGateBoostWithHolidayPolicy()` 호출 / signalScanner `evaluateBuyList` 사이징이 `applyKellyMultiplierWithHolidayPolicy()` 호출 / signalScanner 진입 가드에 `isWithinMarketOpenDelay()` 추가. ⑩ **PR-D 후속**: KRX 휴장일 자동 동기화 — 본 정책의 `isLongHoliday` 판정이 자동 갱신 KRX_HOLIDAYS Set 위에서 자동 향상. | `docs/adr/0038-holiday-resume-policy.md` (신규), `server/trading/holidayResumePolicy.ts` (신규 130 LoC), `server/trading/holidayResumePolicy.test.ts` (신규 24 케이스), `server/trading/holidayResumeAlert.ts` (신규 75 LoC), `server/trading/holidayResumeAlert.test.ts` (신규 7 케이스), `server/utils/marketDayClassifier.ts` (PR-B isLongHoliday 영업일 분기 버그 수정), `server/scheduler/alertJobs.ts` (+09:05 KST cron 등록), `server/scheduler/scheduleCatalog.ts` (+SCHEDULE_CATALOG 09:05 holiday_resume_alert 항목), `ARCHITECTURE.md` (+2 boundary +1 boundary rule) | 사용자 페르소나 후속 — PR-A(가드)+PR-B(SSOT) 위에 매매 행동 정책 신설. 5/4 월·추석 다음 월요일 같은 시점에 시스템이 평상시와 동일하게 동작하던 구조적 결함 차단(인프라). 본 PR 은 정책 SSOT + 알림으로 회귀 위험 격리, 매매 wiring(entryEngine/signalScanner) 은 PR-C-2 후속. 5/4 월요일 09:05 KST 첫 텔레그램 알림 발송 예정. |
| 2026-04-26 | PR-B MarketDayClassifier SSOT + ScheduleGuard 인프라 (ADR-0043, PR-A 직접 후속): PR-A 가 nightlyReflection 진입부에 가드를 직접 박았던 임시 해결책 위에 재사용 가능한 SSOT 인프라 4종 도입. 사용자 페르소나 후속 — "오늘이 어떤 날인지 모르는 시스템은 올바른 필터를 적용할 수 없다." 의 SSOT 일원화. ① **`server/utils/marketDayClassifier.ts` 신규** (135 LoC) — `MarketDayType` 7분기(TRADING_DAY / WEEKEND / KRX_HOLIDAY / PRE_HOLIDAY / POST_HOLIDAY / LONG_HOLIDAY_START / LONG_HOLIDAY_END) + `MarketDayContext` 인터페이스(date / type / isTradingDay / nextTradingDay / prevTradingDay / isLongHoliday) + `getMarketDayContext(dateYmd?)` / `isTradingDay(dateYmd?)` / `nextTradingDay(ymd)` / `prevTradingDay(ymd)` 4 export. PR-A 의 `isKrxHoliday` + `marketClock.isKstWeekend` 위에 올라가는 분류기. 8일 lookahead/lookback 으로 추석 7일 연휴 + 신정 + 어린이날까지 안전 커버. 비영업 간격 ≥ 3일이면 `LONG_HOLIDAY_*` 분류. 외부 의존 0 (state/persistence import 금지). ② **`server/scheduler/scheduleGuard.ts` 신규** (108 LoC) — `ScheduleClass` 4분기(TRADING_DAY_ONLY / WEEKEND_MAINTENANCE / MARKET_ADJACENT / ALWAYS_ON) + `shouldSkipForScheduleClass(class, date?)` 순수 함수 + `scheduledJob(cronExpr, class, jobName, fn, options?)` 래퍼. `cron.schedule` 의 단일 진입점으로, 비영업일 진입 시 자동 SKIP + `recordScheduleRun({ status: 'skipped', note: reason })` 메트릭 기록. cron 표현식 `1-5`/`0-4` 평일 가드는 1차 방어선, ScheduleClass 가 KRX 공휴일을 평일에 차단하는 진짜 방어선. `options.force=true` 시 가드 우회. ③ **`server/learning/learningDataValidator.ts` 신규** (97 LoC) — `filterToTradingDayRecords<T>(records, getDateKst, getId)` 영업일 필터링 + 거부 사유 카테고리화(NON_TRADING_DAY / KST_DATE_MISSING) + `countTradingDays(start, end)` 영업일 카운터. 학습 윈도우의 분모를 달력일이 아닌 영업일로 정확히 계산. **본 PR 에서는 헬퍼만 도입** — 실제 적용(backtestEngine / failureToWeight / recommendationTracker)은 호출자별 1건씩 후속 PR 에서 안전 적용. ④ **`JobMetrics.lastSkipReason` 옵셔널 필드 추가** + `recordScheduleRun` skipped 분기 갱신 — 'WEEKEND' / 'KRX_HOLIDAY' / 'LONG_HOLIDAY' / 'TRADING_DAY' / 'NON_TRADING_DAY' 등 분류. `/scheduler history` 운영자가 "월요일 자기반성이 정상 실행됐는가" 시계열 진단 가능. ⑤ **`learningJobs.ts` 13개 cron 일괄 ScheduleClass 적용** — 모든 cron 이 `scheduledJob(cronExpr, ScheduleClass, jobName, fn)` 래퍼 경유. 매트릭스: weekly_backtest=WEEKEND_MAINTENANCE / weekly_calib=WEEKEND_MAINTENANCE / daily_mini_backtest=TRADING_DAY_ONLY / weekly_sharpe_alert=TRADING_DAY_ONLY / f2w_reverse_loop=TRADING_DAY_ONLY / nightly_reflection=TRADING_DAY_ONLY / ghost_portfolio=TRADING_DAY_ONLY / silent_distillation=WEEKEND_MAINTENANCE / walk_forward_validation=ALWAYS_ON / counterfactual_resolve=TRADING_DAY_ONLY / ledger_resolve=TRADING_DAY_ONLY. cron 표현식 보존 (1차 cron 가드) + ScheduleClass 가 KRX 공휴일 2차 차단. ⑥ 회귀 테스트 44 케이스 신규 — `marketDayClassifier.test.ts` 17 (4분기 isTradingDay / 영업일 산술 5: 자기반환·금→월·어린이날 직전직후·근로자의 날·추석 7일 연휴 / 7분기 분류 8: TRADING_DAY·WEEKEND·KRX_HOLIDAY·PRE_HOLIDAY·POST_HOLIDAY·LONG_HOLIDAY 시작/끝/추석 9/28 POST_HOLIDAY) + `scheduleGuard.test.ts` 15 (ScheduleClass 4분기 가드 10 + JobMetrics.lastSkipReason 5) + `learningDataValidator.test.ts` 12 (filterToTradingDayRecords 6 + countTradingDays 6: 단일 영업일/주말/한 주/어린이날 포함/역순/빈 입력). ⑦ **ARCHITECTURE.md** marketDayClassifier + scheduleGuard 모듈 boundary 추가 + 2 boundary rule 추가. ⑧ 검증: vitest **server/scheduler + server/learning + server/utils + server/trading 95 files 765/765 pass** (신규 44 + 기존 721 무회귀) + lint(client + server tsc) + validate:all (Gemini + ACMA + SDS + PRES + SRP + SymbolBoundary 6종) + precommit 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** — kisClient/signalScanner/autoTradeEngine/tradingOrchestrator 본체 무수정 (절대 규칙 #2/#3/#4 준수). LIVE 매매 본체 0줄 변경. ⑨ **PR-B-2 후속 분리 (본 PR scope 밖)**: 다른 8개 스케줄러 파일 80+ cron(orchestratorJobs / alertJobs / reportJobs / screenerJobs / tradeFlowJobs / kisStreamJobs / shadowResolverJob / maintenanceJobs / healthCheckJob / commandUsageJobs) 점진 적용. ⑩ **PR-C 후속**: tradingOrchestrator 가 `getMarketDayContext()` 호출 + POST_HOLIDAY/LONG_HOLIDAY_END 분기로 Gate +1 / Kelly 50% / 시초 30분 진입 차단. ⑪ **PR-D 후속**: 매년 12/1 cron 으로 KRX 공식 휴장일 자동 동기화. | `docs/adr/0037-market-day-classifier-and-schedule-guard.md` (신규), `server/utils/marketDayClassifier.ts` (신규 135 LoC), `server/utils/marketDayClassifier.test.ts` (신규 17 케이스), `server/scheduler/scheduleGuard.ts` (신규 108 LoC), `server/scheduler/scheduleGuard.test.ts` (신규 15 케이스), `server/learning/learningDataValidator.ts` (신규 97 LoC), `server/learning/learningDataValidator.test.ts` (신규 12 케이스), `server/scheduler/scheduleCatalog.ts` (+JobMetrics.lastSkipReason + recordScheduleRun skipped 분기 갱신), `server/scheduler/learningJobs.ts` (13 cron 일괄 scheduledJob 래퍼 적용), `ARCHITECTURE.md` (+3 boundary +2 boundary rule) | 사용자 페르소나 후속 — PR-A 가 즉시 효과로 환각을 차단했다면 PR-B 는 SSOT 인프라로 정합성 일원화. PR-C(연휴 복귀 보수 모드) + PR-D(자동 동기화) 가 본 SSOT 위에서 분기 1줄로 끝나도록 기반 마련. learningJobs.ts 13개 cron 만 일괄 적용해 인프라 안전성 검증, 다른 80+ cron 은 PR-B-2 점진 적용으로 회귀 위험 격리. |
| 2026-04-26 | PR-A 자기반성 주말·KRX 공휴일 환각 차단 (P0 단독 PR, 사용자 분석 후속): 사용자 첨부 이미지 "🌙 [자기반성] 4월 26일 (일요일)" 메시지가 보여준 구조적 결함. `nightlyReflectionEngine.ts` 가 `learningJobs.ts:58` cron `0 10 * * *` (매일 KST 19:00) 로 무가드 실행되어 토/일/공휴일에도 자기반성이 돌아 "오늘 거래 없음 = 모든 신호 실패" 로 잘못 학습되던 환각 위험 차단. ① **`server/trading/krxHolidays.ts` `isKrxHoliday(dateYmd)` 헬퍼 추가** — `KRX_HOLIDAYS` Set 단순 래퍼, ADR-0043 (PR-B `MarketDayClassifier` SSOT) 도입 시 단일 교체 지점이 되도록 추상화. ② **`server/learning/nightlyReflectionEngine.ts` 진입부 가드** — `runNightlyReflection()` 의 `ALREADY_EXISTS` 체크 직후 `isKstWeekend(now) \|\| isKrxHoliday(date)` 분기 추가. 주말·KRX 공휴일이면 즉시 `{ skipped: 'NON_TRADING_DAY', mode: 'TEMPLATE_ONLY', executed: false }` 반환 + `console.log` 진단. **`opts.force=true` 시 가드 우회** (수동 운영 호환 — 기존 ALREADY_EXISTS 우회와 동일 패턴). `RunReflectionResult.skipped` union 에 `'NON_TRADING_DAY'` 추가. ③ **`server/scheduler/learningJobs.ts` cron 평일 가드 2건** — NightlyReflection `0 10 * * *` → `0 10 * * 1-5` (UTC 월~금 10:00 = KST 평일 19:00) + F2W 가중치 역피드백 `10 18 * * *` → `10 18 * * 0-4` (UTC 일~목 18:10 = KST 평일 03:10). cron 가드는 KRX 공휴일을 차단 못 하므로(예: 평일 5/5 어린이날) engine 진입부 가드가 진짜 방어선, cron 가드는 보조(주말 새벽 cron 실행 비용 절감). 다른 cron 작업은 이미 평일 가드 있거나(미니 백테스트 0-4 / Sharpe 3 / GhostPortfolio 1-5 / Counterfactual 1-5 / Ledger 1-5) 주말 전용(주간 백테스트 5 / Distillation 0 / WeeklyCalib 0)이라 변경하지 않음. ④ **회귀 테스트 13 케이스 신규** — `server/trading/krxHolidaysIsHoliday.test.ts` 7 (어린이날 true / 평일 영업일 false / 다년도 신정 / 미래 부재 / 빈 문자열 / 잘못된 형식 / `KRX_HOLIDAYS` Set 일관성 모든 항목 round-trip) + `server/learning/nightlyReflectionDateGuard.test.ts` 6 (토요일 KST NON_TRADING_DAY / 일요일 KST NON_TRADING_DAY / 평일 KRX 공휴일 5/5 NON_TRADING_DAY / 평일 영업일 4/21 정상 실행 / `opts.force=true` + 토요일 가드 우회 / ALREADY_EXISTS 우선순위 보존). ⑤ 검증: vitest **server/learning + server/trading + server/scheduler 89 files 649/649 pass** (신규 13 + 기존 636 무회귀) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종) + precommit 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** — `kisClient`/`signalScanner`/`autoTradeEngine`/`tradingOrchestrator` 본체 무수정 (절대 규칙 #2/#3/#4 준수). LIVE 매매 본체 0줄 변경 — 학습 엔진 진입부 + cron 메타데이터만 수정. ⑥ **PR-B 후속 분리 (본 PR scope 밖)**: `server/utils/marketDayClassifier.ts` SSOT 신설 (`MarketDayContext` 7분기 — TRADING_DAY/WEEKEND/KRX_HOLIDAY/PRE_HOLIDAY/POST_HOLIDAY/LONG_HOLIDAY_*) + `LearningDataValidator` 게이트 + `ScheduleClass` 자동 가드 인젝션 래퍼 + ADR-0043 발행. | `server/trading/krxHolidays.ts` (+10줄, isKrxHoliday 헬퍼), `server/trading/krxHolidaysIsHoliday.test.ts` (신규 7 케이스), `server/learning/nightlyReflectionEngine.ts` (+22줄 -1줄, 진입부 가드 + skipped 타입 확장), `server/learning/nightlyReflectionDateGuard.test.ts` (신규 6 케이스), `server/scheduler/learningJobs.ts` (+12줄 -5줄, cron 평일 가드 2건) | 사용자 페르소나 후속 — "오늘이 어떤 날인지 모르는 시스템은 올바른 필터를 적용할 수 없다." 학습 엔진 진입부에 거래일 컨텍스트 게이트를 도입해 주말·KRX 공휴일에 자기반성이 실행되어 학습 가중치가 왜곡되던 구조적 환각 위험 차단. cron + engine 진입부 이중 방어층으로 KRX 공휴일이 평일에 떨어진 경우(어린이날/추석)도 차단. opts.force 우회 보존으로 수동 운영 호환. PR-B 에서 MarketDayClassifier SSOT + LearningDataValidator 정합성 일원화 후속 분리. |
| 2026-04-26 | PR-T BudgetPolicy 정책 객체 추출 + 백테스트 가능화 (아이디어 8, ADR-0036): `evaluateBuyList` 의 P1-2 섹션 (계좌 리스크 예산 + Fractional Kelly 게이트) 가 env 모듈 const + 하드코딩 `FRACTIONAL_KELLY_CAP` 으로 박제돼 있어 백테스트가 "Kelly 0.25배 vs 0.5배" 같은 정책 비교를 할 수 없던 구조 해소. ① **`server/trading/budgetPolicy.ts` 신규** — `BudgetPolicy` 인터페이스(id/dailyLossLimitPct/maxConcurrentRiskPct/maxPerTradeRiskPct/maxSectorWeightPct/fractionalKellyCap) + `defaultBudgetPolicy()` env 기반 factory + `setBudgetPolicy(p)`/`getBudgetPolicy()` 활성 정책 슬롯 + `withPolicyOverride(overrides, base?)` 부분 override 헬퍼 + `applyFractionalKellyWithPolicy`. 외부 의존성 0 (state/persistence import 금지) — 순수 SSOT. ② **`accountRiskBudget.ts` 통합** — `getAccountRiskBudget` / `computeRiskAdjustedSize` 에 `policy?: BudgetPolicy` 옵셔널 인자 추가. 미주입 시 `getBudgetPolicy()` 자동 사용 → 기존 호출자(perSymbolEvaluation 포함) 0 변경. `FRACTIONAL_KELLY_CAP`/`SignalGrade`/`BudgetPolicy` 등을 re-export 해 import 경로 후방호환 유지. `computeKellyCoverageRatio` default 도 정책 기반으로 전환. ③ **회귀 테스트 19 케이스 신규** (`budgetPolicy.test.ts`) — `defaultBudgetPolicy` env 기반 + override 2건 / 활성 정책 슬롯 2건 / `withPolicyOverride` 부분 override 3건 / `applyFractionalKellyWithPolicy` 캡·음수·자동 사용 4건 / `applyFractionalKelly` 후방호환 3건 / `computeRiskAdjustedSize` Kelly 0.25배 vs 0.5배 시나리오 5건 (권장 자본 30M → 25M 검증). 검증: server/trading 33 files 324 tests pass + lint + validate:all 6종 OK + precommit 통과. production 코드는 `setBudgetPolicy` 호출 안 함 → 기존 LIVE 매매 동작 100% 보존. | `docs/adr/0036-budget-policy-extraction.md` (신규), `server/trading/budgetPolicy.ts` (신규), `server/trading/budgetPolicy.test.ts` (신규 19 케이스), `server/trading/accountRiskBudget.ts` (policy 옵셔널 인자 + re-export) | 페르소나 ROE 듀퐁 분석처럼 시스템도 분해 가능해야 진단 가능. 백테스트가 정책 객체만 swap → "Kelly 0.25배 vs 0.5배 / 일일 손실 한도 5% vs 3% / Fractional Kelly STRONG_BUY 0.5 vs 0.25" 비교 즉시 가능. ADR-0008 (Kelly Time Decay) 와 결합해 4-axis (Kelly 캡 × 시간감쇠 × IPS dampener × sizingTier) 백테스트 매트릭스 구성 가능. 4 아이디어 시리즈 (PR-Q/R/S/T) 마무리. |
| 2026-04-26 | PR-S ExitDecision attribution 타입 도입 (아이디어 7) — 학습 루프 정밀 신호: ① **`ExitAttribution` 타입 신규** (shadowTradeRepo.ts) — `ruleId: ExitRuleTag` + `contributingConditions: string[]` + `regime: string` + `attachedAt?` ISO. ② **`buildExitAttribution(ruleId, conditions, regime)` 헬퍼** — 빈 conditions 배열 시 ruleId 소문자 fallback. ③ **`PositionFill.attribution?: ExitAttribution`** 옵셔널 필드 추가 (점진 적용 — 본 phase, 후속 PR 에서 SELL fill 모두 의무화). ④ **`exitEngine.ts` R6_EMERGENCY_EXIT 시범 부착** — `reserveSell` 호출에 `attribution: buildExitAttribution('R6_EMERGENCY_EXIT', ['regime_r6_defense'], currentRegime)` 추가. ⑤ **회귀 테스트 5건 신규** (`exitAttribution.test.ts`) — 정상 입력 / 빈 conditions fallback / 다중 conditions / attachedAt ISO 시각 / 타입 contract. 검증: precommit EXIT=0 + vitest server/trading + persistence 33 files 311/311 pass. | `server/persistence/shadowTradeRepo.ts` (+ExitAttribution + buildExitAttribution + PositionFill.attribution), `server/persistence/exitAttribution.test.ts` (신규 5), `server/trading/exitEngine.ts` (R6 reserveSell attribution 부착) | 페르소나 "외국인 수급, 거래량, 일목균형표 다요소 합치" 사상의 메타-레벨 적용. ADR-0006 (composite key) + ADR-0027 (learning shadow model) 와 결합해 "어느 매도 규칙이 가장 자주 옳았는지/틀렸는지" 분석 가능. failurePatternDB 에 정밀 신호 공급. 후속: SELL fill 모든 경로에 attribution 의무화 + PR-T (kellyBudgetGate 추출). |
| 2026-04-26 | PR-R Exit Rule Catalog 자동 생성 (아이디어 6) — 매도 규칙 SSOT 문서화: ① **`docs/EXIT_RULE_HEADER.md` 신규** — JSDoc `@rule` 표준 schema 명세 (필수: rule/priority/action/trigger/rationale, 옵션: ratio/regime/minHoldingDays/deprecated). 예시: R6_EMERGENCY_EXIT 인라인 + HIT_TARGET 함수형. ② **`scripts/generate_exit_rules_catalog.js` 신규** (170 LoC) — `RULE_BLOCK_RE` JSDoc 블록 파서 + `extractFieldValue` 다중 라인 필드 추출 + REQUIRED 필드 검증 + `priority` 정수 캐스팅 + `--check` 모드 (CI 통합용 — stale 카탈로그 시 exit 1). ③ **`server/trading/exitEngine.ts`** — R6_EMERGENCY_EXIT 인라인 블록 위에 표준 헤더 시범 부착 (실제 trigger 정확히 매칭). ④ **`docs/exit-rules-catalog.md` 자동 생성** — 1개 규칙 (R6_EMERGENCY_EXIT priority=1) 표 형식. 향후 분해 시 `server/trading/rules/*.ts` 도 자동 인식. ⑤ **`package.json`** `npm run build:exit-catalog` + `npm run validate:exit-catalog` (--check) 신규. 검증: precommit EXIT=0 + 카탈로그 최신 상태 OK. | `docs/EXIT_RULE_HEADER.md` (신규), `scripts/generate_exit_rules_catalog.js` (신규 170줄), `docs/exit-rules-catalog.md` (자동 생성), `server/trading/exitEngine.ts` (R6_EMERGENCY_EXIT 헤더 시범), `package.json` (+2 scripts) | 회고/감사 시간 단축 — 매도 규칙 갯수·우선순위·trigger 가 코드와 자동 동기화되는 SSOT. exitEngine 분해 PR (1233줄 → rules/*.ts) 후속 진행 시 본 스크립트가 그대로 동작. |
| 2026-04-26 | PR-Q GodFunctionGuard (아이디어 9) — `check_complexity.js` 함수 단위 임계 추가: ① **FUNCTION_LIMITS** `lines: 300, cyclomaticComplexity: 25`. ② **`findFunctionRegions(src)`** — AST 의존성 0 정규식 + 중괄호 깊이 추적으로 함수 본문 추출 (function 선언 + 화살표 `=> {` 본문). ③ **`analyzeFunctions(file)`** — cyclomatic 근사 (`if/else if/case/catch/while/for/&&/||/?:` 분기 카운트 + 1). ④ **main() 통합** — `src/` + `server/` walk 후 임계 초과 시 보고. 기본 `WARN` (가시화), `FUNCTION_GUARD=strict` env 시 빌드 실패. ⑤ **현재 검출 122건** — 이미 누적된 god-function 부채 가시화 (예: `AnalysisDashboard` 569 lines·cc=71, `DeepAnalysisModal` 351·cc=95, `ManualQuantInput` 481 lines). 분해는 후속 별도 PR. 검증: precommit EXIT=0 (WARN 모드라 차단 안 됨), validate:all 6종 OK 무회귀. | `scripts/check_complexity.js` (+FUNCTION_LIMITS·findFunctionRegions·analyzeFunctions·main 통합 +90 LoC) | 페르소나 "직전 청소된 곳도 다시 더러워질 수 있지만 청소되지 않은 곳보다는 낫다" 의 코드 버전. ADR-0028 분해 직후의 깨끗한 상태를 사전 차단으로 락-인 — 향후 신규 god-function 재발 차단. WARN 모드로 시작 → 분해 PR 누적 후 strict 전환 가능. |
| 2026-04-26 | PR-P 자동매매 페이지 텔레그램 연결 진단 (UI 점검 보강): 사용자 요청 "자동매매 페이지 UI 테스트 — 텔레그램과 연결상태 점검 및 개선". 자동매매 페이지의 ApiConnectionLamps 가 KIS REST/실시간/KRX/Yahoo 4축만 표시하고 텔레그램은 부재 → 운영자가 봇 도달성을 GUI 에서 확인할 경로 없음. ① **`server/health/diagnostics.ts`** — `HealthSnapshot` 에 `telegramConfigured / telegramBotTokenOnly / telegramChatIdOnly` 3 필드 추가. `TELEGRAM_BOT_TOKEN` + `TELEGRAM_CHAT_ID` env 검증으로 4분기 (둘 다 / token-only / chatId-only / 둘 다 부재) 분리. ② **`server/routes/systemRouter.ts /health/pipeline`** — 응답에 3 필드 노출 (UI 폴링 30초 주기). ③ **`src/components/autoTrading/ApiConnectionLamps.tsx`** — `deriveTelegram(h)` 신규 export + 5번째 램프 (OK 녹색 / WARN 황색 token-only or chatId-only / DOWN 적색 둘 다 부재 / UNKNOWN). ④ **`src/components/autoTrading/TelegramConnectionTest.tsx` 신규** — opt-in 클릭 버튼 + `POST /api/telegram/test` 호출 (idle/sending/success/error 4상태 인라인 메시지). 사전 검증: `telegramConfigured=true` 일 때만 enabled, 그 외 안내 텍스트 표시. ⑤ **`src/pages/AutoTradePage.tsx`** — `<TelegramConnectionTest />` 를 ApiConnectionLamps 직후 임베드. ⑥ **회귀 테스트 13 케이스 신규** — `ApiConnectionLamps.test.ts` 6 (deriveTelegram 4 분기 + null + 빈 객체) + `diagnostics.test.ts +7` (env 4 조합 + 기존 mock 재사용). 검증: precommit EXIT=0 + vitest **server/health + server/routes + src/components/autoTrading 13 files 129/129 pass** (신규 13 + 기존 무회귀). | `server/health/diagnostics.ts` (+3 필드 + env 검증), `server/routes/systemRouter.ts` (+pipeline 응답 3 필드), `src/components/autoTrading/ApiConnectionLamps.tsx` (+deriveTelegram + 5번째 램프), `src/components/autoTrading/ApiConnectionLamps.test.ts` (신규 6), `src/components/autoTrading/TelegramConnectionTest.tsx` (신규), `src/pages/AutoTradePage.tsx` (+임베드), `server/health/diagnostics.test.ts` (+7 케이스 + afterEach import) | 운영자가 자동매매 페이지에서 텔레그램 도달성을 GUI 1회 클릭으로 검증 가능 — 환경 변수 누락(BOT_TOKEN 또는 CHAT_ID 부재) 즉시 경고. KIS·KRX·Yahoo 와 동일 진단 SSOT (collectHealthSnapshot) 에서 분기되어 일관성 유지. |
| 2026-04-26 | PR-O 코드 위생 (SDS + SRP WARN 일괄 해소): ① **SDS 4건** — `server/screener/sectorSources.ts:465` + `server/trading/preMarketSmokeTest.ts:48/64/83` 의 catch 블록에 `/* SDS-ignore: 진단 누적 — diagnostics/failed.push 가 호출자에게 반환 */` 주석 추가. 의도적 swallow (catch 블록이 진단 배열에 누적해 호출자가 LIVE 차단 결정) 명시. ② **SRP 666건** — 자동 스크립트 `add_responsibility.js` 가 path 기반 `@responsibility <단일문장>` 태그를 모든 누락 ts/tsx 파일 첫 줄에 추가. 분류: src/pages/* 페이지 / hooks 페이지 React hook / stores Zustand store / api 클라이언트 모듈 / utils 유틸 / types 도메인 타입 / config 설정 SSOT / layout 레이아웃 / ui 프리미티브 / components/{subdir} 영역 컴포넌트 / services 서비스 모듈 / server/{routes,clients,persistence,screener,trading,orchestrator,scheduler,learning,alerts,telegram,services,health,data,ai,utils} 도메인별 모듈. 25단어 이내·접속사 없는 단일문장. ③ 검증: `npm run validate:all` 6종 모두 OK (SDS·SRP WARN 0건) + precommit EXIT=0 + vitest **src 736/736 + server/routes,health 119/119 = 855 누적 pass** (회귀 0건). | `server/screener/sectorSources.ts` (SDS-ignore 1) + `server/trading/preMarketSmokeTest.ts` (SDS-ignore 3), 666 ts/tsx 파일에 `@responsibility` 태그 첫 줄 추가 | 사용자 보고 "SDS WARN 4건 + SRP WARN 666건 — 수정". 모든 검증 WARN 해소 — 향후 PR 에서 신규 파일도 책임 태그 의무화 (precommit `--changed` 가드) 시 일관성 보장. 자동 태그는 path 기반 placeholder — 정확한 단일 책임은 점진적 사람 검토로 격상 가능. |
| 2026-04-26 | PR-N 보유 종목 직접 상관관계 매트릭스 (OHLCV 기반): 사용자 P2-2 의 진짜 충족 — globalCorrelation (KOSPI 4축) 위에 사용자 보유·관심 종목 NxN Pearson 매트릭스. ① **`src/utils/correlationMatrix.ts` 신규** — `dailyReturns(closes)` log return + `pearsonCorrelation(a,b)` (n<2 / 분산 0 / NaN 안전 fallback) + `correlationMatrix({sym→returns})` NxN + `classifyCorrelation` 6 분류 (STRONG_POS/POS/NEUTRAL/NEG/STRONG_NEG/UNDEF). ② **`src/components/macro/PortfolioCorrelationMatrix.tsx` 신규** — useRecommendationStore.watchlist + recommendations 통합·dedupe·MAX_SYMBOLS=8 제한. opt-in 버튼 (사용자 클릭 시 fetch 시작) → fetchHistoricalData('3mo','1d') 병렬 호출 (PR-23 LRU + PR-24 coalescing 자동 적용) → returns 배열 → correlationMatrix → heatmap 표 (5분류 색상 + tooltip). 1 종목 미만 시 placeholder. ③ **`MacroIntelligencePage.tsx` wiring** — 6 layer grid 아래 풀폭 매트릭스 카드 추가. ④ **회귀 테스트 23 케이스** — `correlationMatrix.test.ts` (dailyReturns 5: 길이/상승/하락/0·NaN/NaN + pearsonCorrelation 6: 동일/반대/약한/표본<2/분산0/길이불일치 + correlationMatrix 6: 빈/1종목/대각선/대칭/완벽 상관·반대/null + classifyCorrelation 6: 5 분류 + null/NaN). 검증: precommit EXIT=0 + vitest 신규 23 + 기존 무회귀. | `src/utils/correlationMatrix.ts` (신규)+`.test.ts` (신규 23), `src/components/macro/PortfolioCorrelationMatrix.tsx` (신규), `src/pages/MacroIntelligencePage.tsx` (+PortfolioCorrelationMatrix) | 사용자 P2-2 충족 — globalCorrelation 의 KOSPI 4축이 *시장 차원* 이라면 본 매트릭스는 *내 포트폴리오 차원* 분산 점검. 0.7+ 동조 종목쌍은 분산 리스크 (한쪽 폭락 시 동반 손실), -0.4- 헤지 쌍은 자연 헤지. opt-in 버튼으로 외부 호출 비용 통제. **사용자 P0+P1+P2 + 후속 6종 = 총 19 PR 완주.** |
| 2026-04-26 | PR-M 추천 시계열 차트 — 어제/오늘 비교 (PR-B/G 확장): ① **`server/routes/recommendationsRouter.ts`** `buildRecommendationTimeseries(records, days, now?)` 순수 함수 (KST 자정 기준 일자별 그룹핑, 빈 슬롯도 0 카운트로 채움, 1≤days≤90 보정) + `GET /api/recommendations/timeseries?days=N` 엔드포인트. ② **`src/api/recommendationsClient.ts`** `fetchRecommendationTimeseries(days)` + 타입 동기 사본. ③ **`src/components/analysis/RecommendationTimeseriesChart.tsx` 신규** — 7/14/30일 슬라이서 + 오늘 vs 어제 3 비교 박스 (총 추천 / 승률 / 평균 수익률 + 델타 색상) + 일별 분포 막대 (승=녹/패=적 오버레이) + 오늘 행 violet 강조. ④ **`RecommendationHistoryPage.tsx` 임베드** — Attribution 차트 위에 RecommendationTimeseriesChart 삽입. ⑤ **회귀 테스트 11 케이스** — `recommendationsTimeseries.test.ts` (빈/그룹핑/avgReturn closed-only/range 밖 무시/days=1/90 절삭/0·음수 보정/signalTime 무효/NaN actualReturn/winRate=null). 검증: precommit EXIT=0 + vitest 신규 11 + 기존 무회귀. | `server/routes/recommendationsRouter.ts` (+buildRecommendationTimeseries + /timeseries), `server/routes/recommendationsTimeseries.test.ts` (신규 11), `src/api/recommendationsClient.ts` (+timeseries fetch), `src/components/analysis/RecommendationTimeseriesChart.tsx` (신규), `src/pages/RecommendationHistoryPage.tsx` (+timeseries 차트 임베드) | "오늘 시장이 어제보다 활발한가" 1초 판단 + 7/14/30일 추세 패턴 인식. PR-B 의 누적 통계(month-level) 위에 day-level 시계열 가시화. |
| 2026-04-26 | PR-L Service Worker 백그라운드 알림 (PR-C 확장): 가격 알림이 백그라운드 탭에서도 OS-level 표시되도록 SW 등록·showNotification 경로 추가. 진정한 페이지 닫힘 백그라운드는 Push API + 서버 web-push 인프라 필요로 후속 PR. ① **`public/service-worker.js` 신규** — install/activate/notificationclick/push/message 5 핸들러. notificationclick → 메인 윈도우 포커스 또는 새 창. push 페이로드 schema 미정의 시 무시 (서버 인프라 후속). ② **`src/utils/serviceWorkerRegistration.ts` 신규** — `registerPriceAlertServiceWorker()` (HTTPS/localhost 가드, 실패 시 false) + `getPriceAlertRegistration()` (캐싱 + ready 폴링) + `__resetServiceWorkerForTests`. ③ **`src/hooks/usePriceAlertWatcher.ts` 격상** — sendNotification 이 SW registration 가용 시 `reg.showNotification` 사용 (백그라운드 탭에서도 표시), 실패·SW 미지원 시 클래식 `new Notification` fallback. requestPriceAlertPermission 이 granted 시 SW 도 동시 등록. ④ **`src/main.tsx` wiring** — `registerPriceAlertServiceWorker()` 부팅 시점 fire-and-forget 호출. ⑤ **회귀 테스트 7 케이스** — `serviceWorkerRegistration.test.ts` (SW 미지원 / 정상 등록 / throw fallback / 캐싱 / ready 폴링 / window 미지원 / ready throw). 검증: precommit EXIT=0 + vitest 신규 7 + PR-C usePriceAlertWatcher 18 무회귀. | `public/service-worker.js` (신규), `src/utils/serviceWorkerRegistration.ts` (신규)+`.test.ts` (신규 7), `src/hooks/usePriceAlertWatcher.ts` (sendNotification SW 우선 + permission 시 SW 등록), `src/main.tsx` (+SW 등록) | PR-C 의 알림이 백그라운드 탭에서도 표시 — 사용자가 다른 탭으로 옮긴 상태에서도 손절가 도달 즉시 인지 가능. 페이지 완전 닫힘 백그라운드는 Push API + VAPID 키 + 서버 web-push 인프라 필요로 후속 PR. |
| 2026-04-26 | PR-K 섹터별 종목 드릴다운 (PR-E 확장): 추천+워치리스트 종목의 `relatedSectors` 매칭으로 섹터별 보유 가시화. ① **`src/utils/sectorStockMatch.ts` 신규** — `stockMatchesSector(stock, name)` 정확 일치 + 부분 포함 양방향 (예: "조선" ↔ "조선·해운") + `filterStocksBySector(stocks, name)` dedupe (code 기준). ② **`src/components/sector/SectorStocksDrilldown.tsx` 신규** — useRecommendationStore.recommendations + watchlist 통합 후 `filterStocksBySector` 매칭, type 별 색상 배지 + 가격/섹터 표시. 매칭 0 건 시 안내. ③ **`SectorRotationHeatmap.tsx` wiring** — SectorChip 을 button 으로 전환, onSelect 콜백 + 펼치기 panel 행도 클릭 가능. ④ **회귀 테스트 13 케이스** — `sectorStockMatch.test.ts` (정확/대소문자/부분 포함 양방향/null/빈 값/비배열/dedupe/빈 결과). 검증: precommit EXIT=0 + vitest 신규 13 + 기존 무회귀. | `src/utils/sectorStockMatch.ts` (신규)+`.test.ts` (신규 13), `src/components/sector/SectorStocksDrilldown.tsx` (신규), `src/components/sector/SectorRotationHeatmap.tsx` (chip → button + drilldown 모달 wiring) | PR-E 의 섹터 점수만 보고 어떤 종목이 그 섹터에 있는지 즉시 인지. 외부 fetch 없이 store 기반 매칭. |
| 2026-04-26 | PR-J 단계별 종목 드릴다운 (PR-F 확장): scanTracer 활용한 단계별 통과·탈락 종목 리스트 노출. ① **`server/routes/screenerPipelineRouter.ts`** `partitionTracesByStage(traces, stage)` 순수 함수 + `GET /pipeline-stocks?stage=<...>` 엔드포인트. yahoo→gate→rrr→buy 단계별 cascade 분류 (이전 단계 탈락은 다음 단계에 미포함). ② **`src/api/screenerPipelineClient.ts`** `fetchPipelineStocks(stage)` + 타입 동기 사본. ③ **`PipelineStageDrilldown.tsx` 신규** — 모달 형식 (overlay + close), passed/dropped 분리 표시 + dropReason 한국어 라벨 + EXECUTED 배지. ④ **`CandidatePipelinePanel.tsx` wiring** — StageRow 를 button 으로 전환 (UNIVERSE 제외), 클릭 시 drilldown 모달 열림. ⑤ **회귀 테스트 7 케이스** — `screenerPipelineRouterDrilldown.test.ts` (5 단계 분류 + 빈 traces + counts 정확). 검증: precommit EXIT=0 + vitest 신규 7 + 기존 무회귀. | `server/routes/screenerPipelineRouter.ts` (+partitionTracesByStage + /pipeline-stocks), `server/routes/screenerPipelineRouterDrilldown.test.ts` (신규 7), `src/api/screenerPipelineClient.ts` (+drilldown 타입·fetch), `src/components/screener/PipelineStageDrilldown.tsx` (신규), `src/components/screener/CandidatePipelinePanel.tsx` (StageRow → button + drilldown 모달 wiring) | PR-F 의 단계별 카운트만으로는 "왜 이 단계에서 80% 가 탈락했나" 진단 불가. 사용자가 단계 클릭 → 통과·탈락 종목 + 사유 즉시 가시화 → 필터 조정 의사결정 데이터. |
| 2026-04-26 | PR-I 사이드바 메뉴 등록 — RECOMMENDATION_HISTORY + MACRO_INTEL 진입 동선 (PR-B / PR-G 페이지 후속 wiring): ① **`src/config/navigation.ts` NAV_GROUPS** 에 신규 그룹 "인텔리전스" 추가 (Trophy=추천 이력 / Globe=매크로 인텔). MORE_MOBILE_TABS 에도 두 항목 추가. ② **`navigation.test.ts` 신규 8 케이스** — VIEW_LABELS 존재 / 중복 가드 / 신규 항목 등록 / 그룹 신설 검증. 검증: precommit EXIT=0 + vitest 신규 8 + 기존 무회귀. | `src/config/navigation.ts` (+인텔리전스 그룹), `src/config/navigation.test.ts` (신규 8) | PR-B/PR-G 의 신규 페이지가 사이드바·모바일 더보기에서 접근 가능. |
| 2026-04-26 | PR-H UI 재설계 P2-H — 조건별 수익률 귀인 + 글로벌 상관관계 (ADR-0035): 사용자 P2-3 (수익률 귀인) + P2-5 (가중치 시각화) + P2-2 (포트폴리오 상관관계 — globalCorrelation 으로 1차 대체). 보유 종목 간 직접 상관관계는 종목별 OHLCV 인프라 필요로 후속 PR. ① **`server/routes/attributionRouter.ts` 신규** — `GET /api/attribution/stats` 엔드포인트. attributionRepo 본체 무수정, `computeAttributionStats()` + `loadAttributionRecords().length` read-only 노출. error → 500. ② **`server/index.ts` wiring** — `app.use('/api/attribution', attributionRouter)`. ③ **`src/api/attributionClient.ts` 신규** — fetch + `ClientAttributionConditionStat` 타입 동기 사본. ④ **`src/components/analysis/ConditionAttributionChart.tsx` 신규** — TanStack Query useQuery (60초 staleTime + retry 2). 27 조건별 막대 그래프: 양/음 분리 (중심선 0, 좌측 적색 음수 / 우측 녹색 양수), avgReturn 정렬, 승률 우측 표시. CONDITION_NAMES SSOT (27 조건 한국어 이름). 데이터 부재 시 placeholder. ⑤ **`src/components/macro/GlobalCorrelationCard.tsx` 신규** — `useGlobalIntelStore.globalCorrelation` 직접 읽기. 4축 (KOSPI-S&P500/Nikkei/Shanghai/DXY) 상관계수 막대 (절대값 기준 적/황/녹 분기) + 디커플링/글로벌 동조화 알림 배지. 데이터 부재 시 placeholder. ⑥ **`src/pages/RecommendationHistoryPage.tsx` 임베드** — 표 끝부분에 `<ConditionAttributionChart />` 추가. ⑦ **`src/pages/MacroIntelligencePage.tsx` 임베드** — 7번째 카드로 `<GlobalCorrelationCard />` 추가. ⑧ **회귀 테스트 4 케이스** — `attributionRouter.test.ts` (빈 데이터 / 정상 응답 / computeAttributionStats throw → 500 / loadAttributionRecords throw → 500). 검증: precommit EXIT=0 + validate:all 6종 OK + vitest **신규 4 + 기존 무회귀 = 221 누적 pass**. KIS/KRX 자동매매 quota 0 침범 (read-only attribution + globalCorrelation). | `docs/adr/0035-attribution-and-correlation.md` (신규), `server/routes/attributionRouter.ts` (신규)+`.test.ts` (신규 4), `server/index.ts` (+/api/attribution 마운트), `src/api/attributionClient.ts` (신규), `src/components/analysis/ConditionAttributionChart.tsx` (신규), `src/components/macro/GlobalCorrelationCard.tsx` (신규), `src/pages/RecommendationHistoryPage.tsx` (+ConditionAttributionChart 임베드), `src/pages/MacroIntelligencePage.tsx` (+GlobalCorrelationCard 임베드) | 사용자 P2 5종 중 본 PR-H 에서 P2-2/3/5 충족 — attributionRepo + globalCorrelation store 자산을 시각화로 표면화. 운영자가 27 조건 중 어느 조건이 실제 수익에 기여하는지 가시화 → 가중치 조정 의사결정. KOSPI 글로벌 동조화/디커플링 인지 → 매수 타이밍 보조. **사용자 P0+P1+P2 총 13종 중 12종 완료** — 보유 종목 간 직접 상관관계 (OHLCV 인프라 의존) 만 후속 PR 잔여. |
| 2026-04-26 | PR-G UI 재설계 P2-G — Macro Intelligence 탭 + 추천 적중률 분리 통계 (ADR-0034): 사용자 P2-1 + P2-4. P2-3 (수익률 귀인)+P2-2 (상관관계)+P2-5 (가중치) 는 attribution 데이터 의존 → PR-H 분리. ① **`src/pages/MacroIntelligencePage.tsx` 신규** — 6 layer 카드 grid (반응형 1~3 열): MacroOverview (VKOSPI/USD/KRW/VIX/IRI/US10Y/BOK 방향) / EconomicRegime (RECOVERY~CRISIS 7분류 + 현금 비중 권장) / SmartMoney (점수/시그널/EWY+MTUM 동반/선행 시간) / ExportMomentum (Hot 섹터/조선/반도체) / Geopolitical (GOS 점수/등급/헤드라인) / CreditSpread (KR AA-/US HY/EMBI+/위기 경보). 각 카드 데이터 부재 시 placeholder. tone 분기 (good/warn/bad). ② **`src/utils/recommendationStats.ts` 신규** — `computeSignalBreakdown(records, period, now?)` signalType + period (7d/30d/90d/ALL) 분리 통계. closed (WIN+LOSS) 만 winRate·avgReturn 계산. closed ≥ 5 일 때만 sampleSufficient=true. ③ **`src/pages/RecommendationHistoryPage.tsx` 확장** — period 슬라이서 + 3분리 BreakdownCard (전체 / STRONG_BUY / BUY) 섹션 추가. fetchRecommendationHistory limit 100→500 (충분한 표본). ④ **View 등록** — `useSettingsStore.View` +`'MACRO_INTEL'` + `viewRegistry.VIEW_LABELS` +`'매크로 인텔리전스'` + `PageRouter` view 분기 + `pages/index.ts` export. ⑤ **회귀 테스트 10 케이스** — `recommendationStats.test.ts` (빈 records / 분류 정확 / avgReturn closed-only / signalType 분리 / 7d 필터 / 30d 필터 / ALL 모든 record / sampleSufficient 임계 / signalTime 무효 / NaN actualReturn 제외). 검증: precommit EXIT=0 + validate:all 6종 OK + vitest **신규 10 + 기존 무회귀 = 217 누적 pass**. KIS/KRX 자동매매 quota 0 침범 (UI-only). | `docs/adr/0034-macro-intelligence-and-history-extensions.md` (신규), `src/pages/MacroIntelligencePage.tsx` (신규), `src/utils/recommendationStats.ts` (신규)+`.test.ts` (신규 10), `src/pages/RecommendationHistoryPage.tsx` (+period 슬라이서 + BreakdownCard 3분리), `src/stores/useSettingsStore.ts` (+'MACRO_INTEL'), `src/config/viewRegistry.ts` (+'매크로 인텔리전스'), `src/pages/PageRouter.tsx` (+MacroIntelligencePage), `src/pages/index.ts` | 사용자 P2-1 충족 — useGlobalIntelStore 의 6 layer 자산이 풀 대시보드로 표면화. 사용자 P2-4 — STRONG_BUY 만 진입하는 운영 정책 검증 가능 (signalType 분리 승률). PR-H (포트폴리오 상관관계 + 수익률 귀인 + 가중치 시각화) 후속. |
| 2026-04-26 | PR-F UI 재설계 P1-F — 후보군 파이프라인 시각화 (ADR-0033): 사용자 P1-1 (가장 우선) — "AI 가 아무 종목이나 찍었다" 인상 차단. ① **`server/routes/screenerPipelineRouter.ts` 신규** — `buildPipelineSummary(scanSummary)` 순수 함수가 `ScanSummary` (candidates/yahooFails/gateMisses/rrrMisses/entries) → 6단계 PipelineStage 배열 매핑. 단계: UNIVERSE / CANDIDATES / MOMENTUM_PASS (=candidates − yahooFails) / GATE1_PASS (− gateMisses) / RRR_PASS (− rrrMisses) / ENTRIES. 음수/NaN/Infinity 안전 fallback (Math.max(0, …)). conversionRate = entries / candidates. ② **`server/index.ts` wiring** — `app.use('/api/screener', screenerPipelineRouter)`. ③ **`src/api/screenerPipelineClient.ts` 신규** — fetch + 타입 동기 사본. ④ **`src/components/screener/CandidatePipelinePanel.tsx` 신규** — TanStack Query useQuery (60초 staleTime + retry 2) + 6 stage funnel 시각화 + 변환률 표시 + 펼치기 토글. 기본 표시는 CANDIDATES + ENTRIES 만 (컴팩트). 단계별 droppedAtThisStep + dropReason 호버. 새로고침 버튼. ⑤ **`src/pages/DiscoverWatchlistPage.tsx` 임베드** — DISCOVER view 일 때만 WatchlistFilterPanel 위에 표시. ⑥ **회귀 테스트 11 케이스** — `screenerPipelineRouter.test.ts` (null/정상/dropped/conversionRate/음수/NaN/순서/label/UNIVERSE 부재). 검증: precommit EXIT=0 + validate:all 6종 OK + vitest **신규 11 + 기존 무회귀 = 207 누적 pass**. KIS/KRX 자동매매 quota 0 침범 (read-only ScanSummary). | `docs/adr/0033-candidate-pipeline-visualization.md` (신규), `server/routes/screenerPipelineRouter.ts` (신규)+`.test.ts` (신규 11), `server/index.ts` (+/api/screener 마운트), `src/api/screenerPipelineClient.ts` (신규), `src/components/screener/CandidatePipelinePanel.tsx` (신규), `src/pages/DiscoverWatchlistPage.tsx` (+CandidatePipelinePanel) | 사용자 페르소나 자료(14.추가보완.txt) 의 "AI 가 직접 고르는 방식은 한계 — 정량 필터로 후보군 압축 후 AI 2차 분석 구조" 시각화. ScanSummary store 자산을 미노출 → /api/screener/pipeline-summary 라우트로 표면화. 운영자가 단계별 탈락률을 한눈에 파악 → 필터 조정 의사결정 데이터. |
| 2026-04-26 | PR-E UI 재설계 P1-E — 섹터 로테이션 히트맵 (ADR-0032): 사용자 P1-2. ① **`src/utils/sectorHeatColor.ts` 신규** — `classifySectorHeat(score)` 4단계 (HOT≥70 / WARM≥50 / COOL≥30 / COLD<30) + `SECTOR_HEAT_CSS` Tailwind 색상 매핑. NaN/Infinity → COLD 안전 fallback. ② **`src/components/sector/SectorRotationHeatmap.tsx` 신규** — `useGlobalIntelStore.sectorEnergyResult` 직접 읽기. result null 시 자동 미렌더 (헤더 stack 빈 공간 미생성). 컴팩트 1행: 🔥 LEADING Top3 + 🧊 LAGGING Bottom3 + 시즌/섹터 카운트. 펼치기 → 전 섹터 점수 막대 + summary. ③ **`src/layout/MarketOverviewHeader.tsx` stack 추가** — MarketRegimeBanner 직후 `<SectorRotationHeatmap />`. ④ **회귀 테스트 6 케이스** — `sectorHeatColor.test.ts` (4 분기 + NaN/Infinity 안전 + CSS 키 검증). 검증: precommit EXIT=0 + validate:all 6종 OK + vitest **신규 6 + 기존 184 무회귀 = 190/190 pass**. KIS/KRX 자동매매 quota 0 침범 (UI-only). | `docs/adr/0032-sector-rotation-heatmap.md` (신규), `src/utils/sectorHeatColor.ts` (신규)+`.test.ts` (신규 6), `src/components/sector/SectorRotationHeatmap.tsx` (신규), `src/layout/MarketOverviewHeader.tsx` (+SectorRotationHeatmap stack) | 사용자 P1-2 충족 — 페이지 진입 직후 시장 자금 향하는 섹터를 1줄로 인지. 기존 sectorEnergyResult store 자산을 미노출 상태에서 노출로 격상. PR-F (후보군 파이프라인) + P2 (포트폴리오 상관관계 / 수익률 귀인 / 가중치 시각화) 후속. |
| 2026-04-26 | PR-D UI 재설계 P1-D — Last Trigger + Enemy Checklist + 분할매수 카드 (ADR-0031): 사용자 P1 5종 중 3종 (Last Trigger / Enemy Checklist / TranchePlan). 후보군 파이프라인(P1-1)·섹터 히트맵(P1-2) 은 PR-E/PR-F 후속 분리. ① **`src/types/ui.ts` +6 타입** — `TriggerCheckId / TriggerCheckStatus / LastTriggerCheck / LastTriggerSummary / EnemyFlagId / EnemyFlagStatus / EnemyChecklistFlag / EnemyChecklistSummary`. ② **`src/utils/lastTriggerStatus.ts` 신규** — `evaluateLastTrigger({stock, vkospi, recentPositiveDisclosure})` 4-체크 평가 (VCP 박스권 돌파 / 거래량 증가 / VKOSPI<25 안정 / DART 긍정 공시). verdict: 4/4→EXECUTE / 1~3→WATCHLIST / 0→INACTIVE. ③ **`src/utils/enemyChecklistFlag.ts` 신규** — `evaluateEnemyChecklist({stock, marginBalance5dChange, weeklyRsi})` 3-플래그 평가 (공매도 잔고 INCREASING / 신용잔고 ≥5% / 주봉 RSI ≥70). verdict: 0→CLEAR / 1→CAUTION / ≥2→BLOCK. 데이터 부재 시 안전 fallback (CLEAR + '미수신' detail). ④ **`src/components/watchlist/LastTriggerCard.tsx` 신규** — 4 체크 ✅⏳ + verdict 라벨 (🟢 진입 트리거 / 🟡 트리거 대기 / ⚫ 비활성). title attribute 로 detail 호버 노출. ⑤ **`src/components/watchlist/EnemyChecklistCard.tsx` 신규** — 3 플래그 ⚠️✓ + verdict 라벨 (🟢 안전 / 🟡 경계 / 🔴 차단). WARNING 항목은 적색 강조. ⑥ **`src/components/analysis/TranchePlanCard.tsx` 신규** — 1차/2차/3차 분할매수 size bar + trigger 텍스트 + status (실행/대기). 진행 카운터 (예: 1/3). plan=null/undefined 시 placeholder. ⑦ **`src/components/watchlist/WatchlistCard.tsx` wiring** — PR-A 그리드 아래에 LastTrigger + Enemy 그리드 1행 추가 (모바일 1열, 데스크탑 2열). `useGlobalIntelStore.macroEnv` 직접 읽기 + `dartAlerts.some(a => a.stock_code === stock.code && a.sentiment === 'POSITIVE')` 매칭. weeklyRsi/marginBalance 종목별 인프라는 후속 PR — 현재 undefined 안전 fallback. ⑧ **`src/components/analysis/StockDetailModal.tsx` wiring** — GateStatusWidget 직후 `<TranchePlanCard plan={stock.tranchePlan} />` 임베드. ⑨ **회귀 테스트 21 케이스 신규** — `lastTriggerStatus.test.ts` 10 (4/4 EXECUTE / 3/4 WATCHLIST / 1/4 WATCHLIST / 0/4 INACTIVE / VKOSPI 정확히 25 PENDING / VKOSPI 24.9 TRIGGERED / VKOSPI null 미수신 / 점수 < THRESHOLD PENDING / NaN 안전 / detail 메시지 분기) + `enemyChecklistFlag.test.ts` 11 (CLEAR / 1 WARN CAUTION / 2 WARN BLOCK / 3 WARN BLOCK / RSI 정확히 70 / RSI 69.9 / 신용 정확히 5% / 신용 4.9% / 데이터 부재 안전 fallback / NaN/Infinity 안전 / detail 형식). 검증: precommit EXIT=0 + validate:all 6종 OK + vitest **신규 21 + 기존 PR-A/B/C 84 + 무회귀 = 184/184 pass**. KIS/KRX 자동매매 quota 0 침범 (UI-only, 절대 규칙 #2/#3/#4 준수). | `docs/adr/0031-last-trigger-enemy-tranche-cards.md` (신규), `src/types/ui.ts` (+6 타입), `src/utils/lastTriggerStatus.ts` (신규)+`.test.ts` (신규 10), `src/utils/enemyChecklistFlag.ts` (신규)+`.test.ts` (신규 11), `src/components/watchlist/LastTriggerCard.tsx` (신규), `src/components/watchlist/EnemyChecklistCard.tsx` (신규), `src/components/analysis/TranchePlanCard.tsx` (신규), `src/components/watchlist/WatchlistCard.tsx` (+LastTrigger/Enemy 그리드 + 4 import), `src/components/analysis/StockDetailModal.tsx` (+TranchePlanCard 임베드) | 사용자 페르소나 자료(12.27가지추가보완고찰_2.txt) 의 "VCP 박스권 돌파 + VKOSPI 안정 + 긍정 공시 동시 발생을 라스트 트리거로 정의" 와 14.추가보완.txt 의 Enemy Checklist 경고 박스 패턴 차용. P0 PR-A 가 데이터 신뢰도 + Gate 통과를 보였다면 PR-D 는 *매수 직전 마지막 판단* 을 지원 — STRONG_BUY 카드 보고 1초 내 "지금 사도 되나" 결정 가능. 분할매수 카드는 모달에서 "1차 매수 후 다음 트리거 대기" 정책 시각화. PR-E (섹터 히트맵) + PR-F (후보군 파이프라인) 후속 분리. |
| 2026-04-26 | PR-C UI 재설계 P0-C — Price Alert Watcher + 4단계 손절·익절 알림 (ADR-0030, 옵션 B 분할 3/3): 사용자 P0-2 충족. ① **`src/types/ui.ts` +`PriceAlertLevel`** ('NORMAL' \| 'CAUTION' \| 'DANGER' \| 'TAKE_PROFIT') 4단계 타입 export. 우선순위: TAKE_PROFIT > DANGER > CAUTION > NORMAL. ② **`src/utils/priceAlertLevel.ts` 신규** — `computePriceAlertLevel({currentPrice, stopLoss, targetPrice, cautionPctToStop?})` 순수 함수. currentPrice ≥ targetPrice → TAKE_PROFIT / currentPrice ≤ stopLoss → DANGER / 손절선 cautionPctToStop(기본 3%) 이내 → CAUTION / 그 외 → NORMAL. 입력 가드: currentPrice ≤ 0 또는 NaN → NORMAL, stopLoss/targetPrice ≤ 0 → 해당 분기 skip. `isActionableAlert(level)` 헬퍼. ③ **`src/stores/useSettingsStore.ts` +`priceAlertsEnabled`** + setter (기본 false, opt-in) — persist middleware 자동 영속. ④ **`src/hooks/usePriceAlertWatcher.ts` 신규** — useEffect 기반 watcher. `useRef<Map<code, PriceAlertLevel>>` 로 직전 alertLevel 추적. transition 발생 시 `shouldDispatchAlert(level, prevLevel, lastFiredAt, now)` 순수 함수가 발송 결정 (transition + cooldown 5분 + actionable 검증). 발송 시 `new Notification(title, {body, tag})` 호출 + localStorage 에 마지막 발송 시각 기록 (key: `qm:price-alert:{code}:{level}`). `priceAlertsEnabled=false` 시 no-op. 권한 미부여 (`Notification.permission !== 'granted'`) 시 발송 skip → in-app 배지(PriceAlertBadge)만 표시. **`requestPriceAlertPermission()` async 함수** 별도 export — 4분기 반환 ('granted'/'denied'/'default'/'unsupported') + try/catch 안전 fallback. 호출 시점: 페이지 레벨 1회 (DiscoverWatchlistPage), 카드별 호출 금지 (성능). ⑤ **`src/components/common/PriceAlertBadge.tsx` 신규** — 4단계 배지 (NORMAL 회색 ✓ / CAUTION 황 ⚠ + 손절선 N.N% / DANGER 적 🔴 + 손절가 도달 + animate-pulse / TAKE_PROFIT 청록 🎯 + 익절 도달 + animate-pulse). `role='alert'` (DANGER/TAKE_PROFIT) vs `role='status'` (NORMAL/CAUTION) 분기. Web Notification 발송 여부와 무관하게 항상 표시 — 권한 거부한 사용자도 in-app 으로 인지 가능. ⑥ **`src/components/watchlist/WatchlistCard.tsx` wiring** — DataQualityBadge/ConfidenceBadge 옆에 PriceAlertBadge 추가 (PR-A 그리드 좌측 컬럼). computePriceAlertLevel 직접 호출. ⑦ **`src/pages/DiscoverWatchlistPage.tsx` wiring** — `usePriceAlertWatcher(displayList)` 페이지 진입부 1회 호출. ⑧ **회귀 테스트 32 케이스 신규** — `priceAlertLevel.test.ts` 14 (TAKE_PROFIT 1순위 / DANGER 2순위 / CAUTION 3% 이내 / NORMAL 3% 초과 / cautionPctToStop=5 사용자 설정 / currentPrice ≤ 0 / NaN / stopLoss=0 / targetPrice=0 / 정확히 3% 경계값 + isActionableAlert 2) + `usePriceAlertWatcher.test.ts` 18 (shouldDispatchAlert 11: NORMAL 차단 / 첫 진입 / 같은 레벨 차단 / cooldown 만료 / cooldown 내 차단 / 정확히 5분 차단 / 5분+1ms 통과 / NaN/Infinity 무시 / NORMAL→CAUTION transition / TAKE_PROFIT→DANGER transition / 같은 레벨+cooldown 만료 transition 우선 + requestPriceAlertPermission 7: API 미지원 / granted 재요청 안 함 / denied 재요청 안 함 / default→granted / default→denied / throw → default fallback / 알 수 없는 응답 → default). 검증: vitest **신규 32 + 기존 PR-A 31 + PR-B 21 + 기타 무회귀 = 163/163 pass** + precommit EXIT=0 (DeployWindow ✅ + 7 검증) + validate:all 6종 OK. 자동매매 quota 0 침범 (UI-only, 절대 규칙 #2/#3/#4 준수). | `docs/adr/0030-price-alert-watcher.md` (신규), `src/types/ui.ts` (+PriceAlertLevel), `src/utils/priceAlertLevel.ts` (신규)+`.test.ts` (신규 14), `src/stores/useSettingsStore.ts` (+priceAlertsEnabled), `src/hooks/usePriceAlertWatcher.ts` (신규)+`.test.ts` (신규 18), `src/components/common/PriceAlertBadge.tsx` (신규), `src/components/watchlist/WatchlistCard.tsx` (+PriceAlertBadge import + 그리드 임베드), `src/pages/DiscoverWatchlistPage.tsx` (+usePriceAlertWatcher 페이지 1회 호출) | 사용자 P0 5종 옵션 B 분할 완주 (PR-A → PR-B → PR-C). 사용자 보고 갭 — "손절가 도달 알림 없으면 추가 손실" 시나리오를 4단계 in-app 배지 + Web Notification 으로 차단. 텔레그램 server-side 알림과 보완 — 페이지 열린 동안 즉시 OS-level 알림. opt-in (priceAlertsEnabled=false 기본) 으로 기존 사용자 무영향. dedupe 5분 cooldown + transition 만 알림 으로 폭주 방지. Service Worker / 사운드 / 알림 이력 페이지 / 사용자 임계 설정 UI 는 후속 PR (P1+) 로 분리. |
| 2026-04-26 | PR-B UI 재설계 P0-B — Condition Source Tier 메타 + Recommendation History 노출 (ADR-0029, 옵션 B 분할 2/3): PR-A 후속 — DataQualityBadge 정확도 격상 + 추천 이력 추적 페이지 신설. ① **`src/types/ui.ts` +`ConditionSourceTier`** ('COMPUTED' \| 'API' \| 'AI_INFERRED') 타입 export. ② **`src/services/stock/types.ts` `StockRecommendation.conditionSourceTiers?`** 옵셔널 필드 추가 — `Partial<Record<ChecklistKey, ConditionSourceTier>>`. 부재 시 PR-A 휴리스틱 fallback. ③ **`src/services/stock/enrichment.ts` `buildConditionSourceTiers(ctx)` 헬퍼 신설** — 27 키 기본 'AI_INFERRED' + (a) `hasVcpComputed=true` → `vcpPattern: 'COMPUTED'`, (b) `hasDartFinancials=true` → `roeType3 / ocfQuality / interestCoverage: 'API'`, (c) `hasKisSupply=true` → `institutionalBuying / supplyInflow: 'API'`. enrichment 의 main path + aiFallback 두 경로 모두 메타 주입. PR-A 휴리스틱이 "ichimokuBreakout=COMPUTED" 로 가정했던 항목들도 실제로는 `...stock.checklist` (Gemini AI 스코어) 보존이라 'AI_INFERRED' 로 정직 분류. ④ **`src/utils/dataQualityClassifier.ts` 메타 우선 모드 격상** — `stock.conditionSourceTiers` 가 있으면 메타 기준 분류, 없으면 휴리스틱 fallback. 부분 메타 mix 도 지원 (메타 있는 항목은 메타, 메타 없는 항목은 휴리스틱). `sourceMetaAvailable=true` 는 *모든* 27 키에 메타가 있을 때만. UI 의 ? 아이콘이 메타 적재된 종목부터 점진적으로 사라짐. ⑤ **`server/routes/recommendationsRouter.ts` 신규** — `GET /api/recommendations/history?limit=N` (기본 100, 최대 500, signalTime 역순) + `GET /api/recommendations/stats` (monthly + totalCount + pendingCount). recommendationTracker 본체 무수정, read 만 추가. ⑥ **`server/index.ts` wiring** — `app.use('/api/recommendations', recommendationsRouter)` 추가 (learning 마운트 직후). ⑦ **`src/api/recommendationsClient.ts` 신규** — `ClientRecommendationRecord / ClientMonthlyStats / RecommendationHistoryResponse / RecommendationStatsResponse` 타입 (서버↔클라 직접 import 금지 절대 규칙 #3 — 동기 사본) + `fetchRecommendationHistory` / `fetchRecommendationStats` 2 fetch 함수. ⑧ **`src/pages/RecommendationHistoryPage.tsx` 신규** — TanStack Query useQuery 2건 (history + stats) + 6 통계 박스 (총 추천 / 진행 중 / 승률 / 평균 수익률 / 복리 수익률 / Profit Factor) + 추천 이력 표 (시그널/종목/유형/진입/손절/목표/상태/실현%/레짐 9 컬럼, 모바일에선 일부 hidden). 4 status 색상 (PENDING 회색 / WIN 녹색 / LOSS 적색 / EXPIRED 황색 + lateWin 표식). 표본 부족(<5건) 시 통계 박스에 "표본 부족" 표시. ⑨ **`src/stores/useSettingsStore.ts View` 타입 +`'RECOMMENDATION_HISTORY'`** + **`src/config/viewRegistry.ts` VIEW_LABELS** + `'RECOMMENDATION_HISTORY': '추천 이력'`. ⑩ **`src/pages/PageRouter.tsx` + `src/pages/index.ts`** — 새 view='RECOMMENDATION_HISTORY' 분기 등록. ⑪ **회귀 테스트 21 케이스 신규** — `dataQualityClassifier.metaMode.test.ts` 7 (전체 메타 sourceMetaAvailable=true / 부분 메타 false / 모두 AI_INFERRED LOW tier / 절반 분할 MEDIUM / 메타 부재 휴리스틱 fallback / 점수 미통과 미카운트 / 1키만 false) + `enrichmentSourceTier.test.ts` 5 (main path 6키 격상 / aiFallback DART 만 / 완전 fallback 모두 AI / VCP 만 / 27 키 카운트) + `recommendationsRouter.test.ts` 9 (빈 이력 / limit 100 기본 / limit=20 / limit>500 절삭 / 음수·NaN fallback / 시간 역순 정렬 / throw → 500 / stats pendingCount / stats throw → 500). 검증: precommit EXIT=0 (DeployWindow ✅ + 7 검증) + validate:all 6종 OK + vitest **신규 21 + 기존 PR-A 31 + utils/types/services/stock/components/watchlist 전체 116/116 pass + server/routes 50/50 pass** (전체 무회귀). 자동매매 quota 0 침범 — recommendationTracker read-only, KIS/aiUniverseService 무수정. | `docs/adr/0029-condition-source-tier-and-recommendation-history.md` (신규), `src/types/ui.ts` (+ConditionSourceTier), `src/services/stock/types.ts` (+conditionSourceTiers?), `src/services/stock/enrichment.ts` (+buildConditionSourceTiers + 2 경로 wiring), `src/services/stock/enrichmentSourceTier.test.ts` (신규 5), `src/utils/dataQualityClassifier.ts` (메타 우선 모드 격상), `src/utils/dataQualityClassifier.metaMode.test.ts` (신규 7), `server/routes/recommendationsRouter.ts` (신규)+`.test.ts` (신규 9), `server/index.ts` (+ /api/recommendations 마운트), `src/api/recommendationsClient.ts` (신규), `src/pages/RecommendationHistoryPage.tsx` (신규)+`src/pages/index.ts` 등록, `src/pages/PageRouter.tsx` (+ view 분기), `src/stores/useSettingsStore.ts` (+ 'RECOMMENDATION_HISTORY'), `src/config/viewRegistry.ts` (+ '추천 이력') | 사용자 P0 5종 옵션 B 분할 진행 — PR-A 가 휴리스틱 fallback 으로 노출했던 DataQualityBadge 정확도가 본 PR-B 에서 메타 우선 분기로 격상. PR-A 의 ? 아이콘 (휴리스틱) 이 enrichment 가 실제 사용한 데이터 출처(VCP COMPUTED / DART API / KIS supply API) 가 적재된 종목부터 사라짐. 사용자 P0-5 (추천 이력 성과 추적) 도 본 PR 에서 충족 — 6 통계 박스 + 9 컬럼 표 + WIN/LOSS/PENDING/EXPIRED 4단계 색상으로 시스템 신뢰도 실측 검증 가능. PR-C (PriceAlertWatcher Web Notification) 후속 분리. |
| 2026-04-26 | PR-A UI 재설계 P0-A — 시장 모드 배너 + 데이터 품질 배지 + Gate 통과 카드 (ADR-0028, 옵션 B 분할 1/3): 사용자 요청 "UI 재설계 P0 5종" 중 본 PR-A 에서 처리하는 3종 (배너·배지·카드). 손절·목표가 알림(P0-2)은 PR-C, 추천 이력 추적(P0-5)+서버 sourceTier 메타(P0-1 정확도 격상)는 PR-B 로 분리. ① **`src/types/ui.ts` 신규** — `MarketModePolicy / DataQualityCount / DataQualityTier / GateCardSummary / GateLineSummary / GateVerdict / OverallVerdict` 7 타입 + `REGIME_TRADING_POLICY: Record<RegimeLevel, {allowed/forbidden/verdict/headline}>` SSOT (R1~R6 6 레짐 별 한국어 정책 텍스트 — 사용자 원안 "주도주 추세추종/분할매수/소외주 저가매수/과열 추격매수" 표현 차용) + `REGIME_TRADING_POLICY_FALLBACK`. `src/types/index.ts` 배럴에 `export * from './ui'` 추가. ② **`src/utils/regimeMapping.ts` 신규** — `deriveRegimeLevel(gate0?, bearRegime?, vkospi?)` 6단계 매핑 SSOT. 우선순위: bearRegime BEAR→R6 / TRANSITION→R5 → gate0.tradeRegime DEFENSE→R6 / BULL_AGGRESSIVE→R1 / BULL_NORMAL+MHS≥60→R2 / BULL_NORMAL+MHS<60→R3 / NEUTRAL→R4 → VKOSPI≥30→R5 → 기본 R4_NEUTRAL. ③ **`src/utils/dataQualityClassifier.ts` 신규** — `classifyDataQuality(stock)` 가 27 조건을 `COMPUTED_KEYS`(9: ichimoku/MACD/볼린저/일목/VCP/거래량/모멘텀/RS) / `API_KEYS`(11: ROE/PER/PBR/시총/외인비율/OCF/마진/이자보상/수급/손절) / `AI_INFERRED_KEYS`(7: 사이클/Risk-On/리더/정책/심리/엘리엇/촉매) 3 그룹으로 휴리스틱 분류 + 가격 출처 1 (REALTIME→computed, YAHOO→api, AI/STALE→aiInferred) → `DataQualityCount` 반환. tier 산출: computed/total ≥ 0.6→HIGH, ≥ 0.3→MEDIUM, 그 외 LOW. `sourceMetaAvailable=false` (PR-A 단계, PR-B 에서 서버 sourceTier 메타 도착 시 true). ④ **`src/components/market/MarketModeBanner.tsx` 신규** — 페이지 상단 항상 렌더 배너. `useGlobalIntelStore.macroEnv/bearRegimeResult` + `evaluateGate0(macroEnv)` 직접 호출 + `deriveRegimeLevel` SSOT. 컴팩트 한 줄(verdict + regime + headline + MHS/VKOSPI/USD/KRW chip) + 펼치기 버튼 → 정책 박스(허용/금지 전략 리스트, 모바일 grid). `aria-live='polite'` (정상 모드). MarketRegimeBanner 와 책임 분리 (ADR-0028 §2): 본 배너는 항상 렌더+정책, 기존 banner 는 Risk-Off 경보 전용. ⑤ **`src/components/common/DataQualityBadge.tsx` 신규** — compact (기본): "🟢18 🟡6 🔴3" 한 줄 + tier 색상 (HIGH 녹색/MEDIUM 황/LOW 적) + sourceMetaAvailable=false 시 작은 ? 아이콘 (휴리스틱 fallback 안내). compact=false: 3 줄 풀 표시 + "데이터 품질 상/중/하" 라벨. ConfidenceBadge 와 충돌 없음 — ConfidenceBadge=가격 1개 출처, DataQualityBadge=27+1 조건 카운트. ⑥ **`src/components/watchlist/GateStatusCard.tsx` 신규** — WatchlistCard 임베드용 압축 read-only Gate 0/1/2/3 표. `buildGateCardSummary(stock)` 헬퍼: GATE1_IDS/GATE2_IDS/GATE3_IDS + CONDITION_PASS_THRESHOLD=5 기준 통과 카운트 → GateLineSummary. gate0Passed = `gateEvaluation.gate1Passed ?? isPassed` alias. overallVerdict 5분기: 4 PASS→STRONG_BUY / 3→BUY / 2→HOLD / 1→CAUTION / 0→AVOID. `onExpand` 콜백으로 StockDetailModal 호출 가능. GateStatusWidget 풀 디테일 위젯과 별도 운영 (ADR-0028 §4 — expand 토글 없는 read-only 카드, 인터랙션 코드 미포함). ⑦ **`src/layout/MarketOverviewHeader.tsx` wiring** — `<MarketModeBanner />` 를 `<StatusBanner />` 와 `<MarketRegimeBanner />` 사이 stack 추가. ⑧ **`src/components/watchlist/WatchlistCard.tsx` wiring** — Signal/Action Row 와 External Links 사이에 grid (모바일 1열/데스크탑 auto+1fr) 1개 추가: 좌측 `<DataQualityBadge compact />` + `<ConfidenceBadge type={dataSourceType} />` (가격 출처 보존), 우측 `<GateStatusCard summary={buildGateCardSummary(stock)} onExpand={() => onDeepAnalysis(stock)} />`. import 4건 추가. ⑨ **ARCHITECTURE.md** — 6 모듈 boundary 추가 + 4 boundary rule 추가 (MarketModeBanner vs MarketRegimeBanner 책임 분리 / DataQualityBadge vs ConfidenceBadge 충돌 없음 / GateStatusCard vs GateStatusWidget 책임 분리 / REGIME_TRADING_POLICY SSOT). ⑩ **회귀 테스트 31 케이스 신규** — `regimeMapping.test.ts` 11 (bearRegime 우선순위 / gate0.tradeRegime 5 분기 / VKOSPI fallback / NaN 안전 / 모든 인자 부재) + `dataQualityClassifier.test.ts` 10 (REALTIME/YAHOO/AI/STALE 4 가격 출처 분기 + tier HIGH/MEDIUM/LOW 3 분기 + sourceMetaAvailable=false 단언 + CONDITION_PASS_THRESHOLD<5 미통과 가드) + `GateStatusCard.test.ts` 10 (overallVerdict 5분기 STRONG_BUY/BUY/HOLD/CAUTION/AVOID + Gate 1 부분통과 FAIL / Gate 2 정확히 9/12 PASS / CONDITION_PASS_THRESHOLD 미만 미통과 / gate1Passed 부재 시 isPassed alias / gate1Passed=false 명시 시 isPassed 무시). 검증: precommit EXIT=0 (DeployWindow ✅ + PRES + ACMA + SRP + SymbolBoundary + Gemini + lint(client+server tsc) 전체 통과) + validate:all 6종 OK + vitest 신규 31 + 기존 utils/types/services/stock/hooks/components/watchlist 무회귀 = 119/119 pass. KIS/KRX 자동매매 quota 0 침범 (UI-only, 절대 규칙 #2/#3/#4 준수 — kisClient/aiUniverseService/autoTradeEngine/server 무수정). | `docs/adr/0028-ui-redesign-p0-banners-badges-cards.md` (신규), `src/types/ui.ts` (신규), `src/types/index.ts`, `src/utils/regimeMapping.ts` (신규)+`.test.ts` (신규 11), `src/utils/dataQualityClassifier.ts` (신규)+`.test.ts` (신규 10), `src/components/market/MarketModeBanner.tsx` (신규), `src/components/common/DataQualityBadge.tsx` (신규), `src/components/watchlist/GateStatusCard.tsx` (신규)+`.test.ts` (신규 10), `src/layout/MarketOverviewHeader.tsx` (+MarketModeBanner stack), `src/components/watchlist/WatchlistCard.tsx` (+DataQualityBadge/GateStatusCard grid + 4 import), `ARCHITECTURE.md` (+6 boundary +4 rule) | 사용자 요청 "UI 재설계 P0 5종" 중 옵션 B 권장안 채택 — 본 PR-A 는 회귀 위험 격리를 위해 UI-only 3종(배너·배지·카드) 만 처리. PR-B (서버 sourceTier 메타 + RecommendationHistory) + PR-C (PriceAlertWatcher Web Notification 권한 흐름) 후속 분리. 사용자 페르소나 갭 — "STRONG_BUY 86%" 만 보이고 데이터 출처 혼합비(실계산 18 / API 6 / AI추정 3)·시장 모드(허용/금지 전략)·Gate 통과 근거 가 카드 단계에서 묻혀있던 구조를 SSOT 3종으로 즉시 노출. |
| 2026-04-25 | PR-51 헬스 probe severity 분류 + /status SSOT 보완 (PR-49 후속): 사용자 보고 "Yahoo probe: ❌ HTTP 503 / DART probe: ❌ status=013" — 둘 다 사실은 정상 동작인데 ❌ 로 잘못 표시되던 문제 + "/status 목록도 보완" 요청. 사용자 패치 권장안 적용. ① **`server/health/diagnostics.ts` severity 분류 추가** — `HealthProbeSeverity = 'OK' \| 'WARN' \| 'CRITICAL'` 신규 타입 + `HealthProbeOutcome` 인터페이스 (severity + statusCode? + message) 로 기존 `{ ok, detail }` 대체. **`classifyYahooProbe(statusCode?)`** 신규 순수 함수 — 200~299 OK / 429·502·503·504 WARN ("temporary unavailable") / 그 외 4xx/5xx WARN / undefined(timeout/network) WARN. **`classifyDartStatus(status)`** 신규 순수 함수 — DART 공식 코드 명세 반영: 000 OK / 013 OK ("reachable, no data") / 010·011·012·901 CRITICAL (인증/접근) / 020·800·900 WARN (일시/제한) / 그 외 WARN. ② **`runExternalProbes` 본체 갱신** — Yahoo/DART probe 결과를 새 분류 함수로 매핑한 `HealthProbeOutcome` 반환. 네트워크 에러는 catch 에서 WARN 으로 분류 (기존엔 ❌ false). ③ **`health.cmd.ts formatHealthMessage` probe 라벨 분기** — 기존 `probe.ok ? '✅' : '❌'` 단순 분기를 `formatProbeOutcome(outcome)` 헬퍼로 교체: OK→✅ / WARN→⚠️ / CRITICAL→❌. **사용자 보고 회귀 차단**: DART status=013 ("데이터 없음") 이 빨간 ❌ 가 아니라 ✅ "DART API reachable, no data for probe query" 로 표시. Yahoo 503 도 ❌ 가 아니라 ⚠️ "Yahoo temporary unavailable status=503" 으로 표시 — 운영자가 즉시 정상/일시장애/치명적 인증오류를 한눈에 구분 가능. ④ **`status.cmd.ts` SSOT 통합 + 11항목 확장** — 기존 5줄(모드/비상정지/MHS/활성/결산) → 11줄로 보강: `collectHealthSnapshot()` 호출로 PR-49 SSOT 활용 + verdict / 워치리스트 카운트 / 일일손실 (% / 한도) / KIS 토큰 시간 / Yahoo 상태 아이콘 / 마지막 스캔·신호 시각 / `MAX_CONVICTION_POSITIONS` 분모 추가. **`formatStatusMessage(inputs)`** 별도 export 로 분리해 단위 테스트 가능. KIS 토큰 라벨 분기 (미설정/만료/시간남음) + Yahoo 5분류 아이콘 (✅/⚠️/❌/🟡/?) + LIVE off 표기 + 손실 P&L 부호 + lastScanTs=0 "미실행". ⑤ 회귀 테스트 25 케이스 신규 — `diagnostics.test.ts +12` (classifyYahooProbe 6: 200/299/429/503·502·504/4xx/undefined + classifyDartStatus 6: 000/013/CRITICAL 4종/WARN 3종/미정의/빈문자열) + `status.cmd.test.ts 13` (정상 11항목 + LIVE / off / 비상정지 / MHS undefined / P&L 음수 / KIS 미설정 / KIS 만료 / Yahoo 5분류 / lastScan/Buy=0). ⑥ 검증: vitest server/health + server/telegram + server/routes/systemRouter **7 files 141/141 pass** (신규 25 + 기존 무회귀) + lint (client + server tsc) + validate:all (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 — health probe 는 진단 전용. | `server/health/diagnostics.ts` (+HealthProbeSeverity·HealthProbeOutcome 타입 + classifyYahooProbe + classifyDartStatus + runExternalProbes 분류 적용), `server/health/diagnostics.test.ts` (+12 케이스), `server/telegram/commands/system/health.cmd.ts` (formatProbeOutcome severity 아이콘 분기), `server/telegram/commands/system/status.cmd.ts` (collectHealthSnapshot SSOT + 11항목 확장 + formatStatusMessage export), `server/telegram/commands/system/status.cmd.test.ts` (신규 13 케이스) | 사용자 보고 "Health 명령 — Yahoo probe: ❌ HTTP 503 / DART probe: ❌ status=013 — 원인 분석, 부분을 더 정확하게 수정 + /status 목록도 보완". 단순 ok/fail 분류로 정상 응답을 ❌ 로 잘못 표시하던 회귀 차단 — DART status=013 은 "데이터 없음" 으로 의미상 정상이고 Yahoo 503 은 일시 장애 (cron 재시도 가치 있음). /status 는 PR-49 SSOT 활용으로 11항목 확장 — 운영자가 1회 명령으로 KIS·Yahoo·스캐너·손실 한도까지 즉시 파악 가능. |
| 2026-04-25 | PR-50 부팅 reconcile dry-run + 스케줄러 누적 메트릭 (P2, PR-49 후속): 사용자 요청 "p2도 진행". P0/P1 (PR-49) 완료 후 잔여 두 항목 처리 — 운영 가시성 향상. ① **P2-A `server/trading/bootReconcile.ts` 신설** — `runBootReconcileDryRun()` async 진입점 + `buildBootReconcileAlertMessage(result, count)` 분리. 트리거 조건: `AUTO_TRADE_MODE=LIVE` + `AUTO_TRADE_ENABLED=true`. SHADOW/disabled 환경에선 즉시 skip 으로 자동매매 무영향. 결과 분류: `{ skipped: true, reason }` (LIVE 아님/disabled/KIS 조회 불가) / `{ skipped: false, mismatchCount, alerted }` / `{ skipped: false, error }`. mismatch 계산은 `summary.QTY_DIVERGENCE + GHOST_LOCAL + GHOST_KIS > 0` (MATCH 만 있으면 채널 노이즈 차단으로 skip). 알림은 `priority='HIGH'` + `dedupeKey='boot_reconcile:YYYY-MM-DD'` + `cooldownMs=6h` + `category='reconcile'` — 재배포 폭주 보호. KIS 조회 불가(`kisQueryable=false`)는 unavailableReason 만 디버그 로그, 알림 미발송 — 정기점검(KST 02~07) 노이즈 차단. ② **server/index.ts wiring** — app.listen 콜백 안 서버 기동 알림 직후 `setTimeout(() => import().then(runBootReconcileDryRun), 30_000)` 추가 (15줄). dynamic import 로 모듈 결합도 최소화 + 컨테이너 초기화 + KIS 토큰 선행 갱신 완료를 30초 대기 후 1회 실행. fire-and-forget — 본 함수의 throw 는 부팅에 영향 없음. ③ **P2-B `scheduleCatalog.ts` JobMetrics 누적** — 신규 인터페이스 `JobMetrics` (jobName/runCount/successCount/failCount/skippedCount/lastSuccessAt/lastFailureAt/lastErrorMessage) + `_metricsByJob: Map<string, JobMetrics>` private state + `ensureMetrics(jobName)` lazy 생성 헬퍼 + `recordScheduleRun()` 본체에 metrics 갱신 로직 추가 (status 별 분기 + lastErrorMessage 120자 절삭 — `ERROR_MESSAGE_LIMIT` 상수). 신규 export: `getJobMetrics(name)` + `getAllJobMetrics()` (failCount 내림차순, 동률은 runCount 내림차순 — "최악 실패율 작업이 항상 먼저"). `getAllJobMetrics()` 는 defensive copy (외부 mutate 차단). 테스트 전용 `__resetScheduleMetricsForTests()` export. **본 PR scope 밖 (후속)**: `formatSchedulerHealth()` 텔레그램 출력 + `/scheduler health` 명령어 + 실패율 ≥ 50% 임계 알림 — 데이터 수집 정착 후 별도 PR. ④ **회귀 테스트 24 케이스 신규** — `bootReconcile.test.ts` 11 (트리거 3: SHADOW skip / DISABLED skip / 미설정 default + KIS 분기 2: 점검시간 skip / reconcile throw 흡수 + mismatch=0 1 + mismatch>0 알림 3: QTY_DIVERGENCE HIGH 옵션 검증 / GHOST 합산 / 텔레그램 401 alerted=false + 메시지 포맷 3: 라인 분기 / GHOST_KIS ⚠️ 마커 / mismatchCount 본문 노출) + `scheduleCatalogMetrics.test.ts` 13 (recordScheduleRun 누적 6: success / failure + lastError / skipped / 혼재 / 120자 절삭 / 미실행 undefined + getAllJobMetrics 정렬 3: failCount 내림차순+동률 runCount / 빈 배열 / defensive copy + wrapJob 3: 성공 누적 / 실패 lastErrorMessage / 혼재). ⑤ 검증: vitest **server/scheduler + server/trading + server/health + server/telegram + server/alerts 43 files 465/465 pass** (신규 24 + 기존 무회귀) + lint(client + server tsc) + validate:all (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 — bootReconcile 은 LIVE 모드에서만 KIS 잔고 1회 조회 (PR-34 reconcileLivePositions 재사용, 회로차단기·블랙리스트 가드 자동 적용). LIVE 매매 무영향 — dry-run only. | `server/trading/bootReconcile.ts` (신규 102줄), `server/trading/bootReconcile.test.ts` (신규 11 케이스), `server/scheduler/scheduleCatalog.ts` (+JobMetrics 인터페이스 + _metricsByJob Map + recordScheduleRun 갱신 + getJobMetrics + getAllJobMetrics + __resetScheduleMetricsForTests, +83줄), `server/scheduler/scheduleCatalogMetrics.test.ts` (신규 13 케이스), `server/index.ts` (+15줄 setTimeout 30s 부팅 reconcile dry-run wiring) | 사용자 요청 "p2도 진행". 운영자 요청 "지난주 어떤 잡이 가장 많이 실패했나?" 같은 시계열 질문에 답하기 위한 SSOT 추가 + Railway 재배포 직후 KIS 잔고와 로컬 장부 mismatch 가 24시간 누적되던 갭 차단 (maintenanceJobs 일일 reconcile 보다 빠른 부팅 30초 dry-run 으로 즉시 표면화). ADR-0015 (PR-34) reconcileLivePositions 위에 올리는 운영 가시성 레이어 — 자동매매 본체 무수정. |
| 2026-04-25 | PR-49 헬스 진단 SSOT + 텔레그램 메뉴 자동 동기화 (P0+P1, 브랜치 `claude/health-diagnostics-menu-sync`): 사용자 요청 "P0 메뉴 자동 동기화 + P1 진단 로직 추출 — P2 부팅 reconcile + scheduler metrics 는 별도 PR". ① **P1 진단 로직 추출** — `server/health/diagnostics.ts` 신설 (`HealthSnapshot` 타입 8축 + `collectHealthSnapshot()` 순수 함수 + `runExternalProbes()` Yahoo/DART HTTP probe 옵셔널 분리 + `deriveYahooStatus(scanSummary, heartbeat)` 6분기 SSOT + `computeVerdict(inputs)` 12분기 우선순위 SSOT). 기존 `health.cmd.ts`(168줄) 의 데이터 수집 본체 + `systemRouter.ts /health/pipeline`(120줄) 본체 90% 중복을 1개 모듈로 통합. ② **health.cmd.ts thin wrapper 전환** — 168줄 → 88줄 (-48%). 데이터 수집은 `collectHealthSnapshot()` 1회 호출, probe 는 `runExternalProbes()` 분리, 본체는 `formatHealthMessage(snapshot, probes)` 텍스트 포맷팅 전용. 기존 텔레그램 메시지 출력 100% 보존 — Yahoo OK/DEGRADED/STALE/DOWN/UNKNOWN 5분기 라벨 + lastSuccessAt KST 시각 + consecutiveFailures 표시 모두 동일. ③ **systemRouter `/health/pipeline` 교체** — 120줄 → 50줄 (-58%). `collectHealthSnapshot()` 1회 호출 + KST `formatKstHm` 헬퍼로 timestamp → 'HH:MM' 변환 후 기존 JSON 응답 스키마 100% 호환 유지(scheduler/watchlistCount/shadowTradeCount/yahooApiStatus/yahooApiDetail/krxCircuitState/kisStream/intradayYield/verdict 등 19개 필드). 응답 시간 보호를 위해 외부 HTTP probe 는 미실행 — 텔레그램 명령만 probe. **systemRouter 미사용 import 11종 정리**(loadShadowTrades/getRemainingQty/isOpenShadowStatus/getKisTokenRemainingHours/getKrxOpenApiStatus/isKrxOpenApiHealthy/getLastBuySignalAt/getLastScanSummary/getStreamStatus/getYahooHealthSnapshot/DATA_DIR/getCachedIntradayYield). ④ **verdict 우선순위 SSOT 통합** — health.cmd 와 systemRouter 가 가졌던 verdict 분기 차이 해소: `EMERGENCY_STOP > DAILY_LOSS_LIMIT > VOLUME_UNMOUNTED > WATCHLIST_EMPTY > AUTO_TRADE_DISABLED > KIS_NOT_CONFIGURED > KIS_TOKEN_EXPIRED(LIVE) > KRX_TOKEN_NOT_CONFIGURED > KRX_TOKEN_UNHEALTHY > SCANNER_IDLE > YAHOO_DOWN > OK` 12 분기. health.cmd 는 이전엔 KRX 체크가 없었으나 SSOT 통합으로 텔레그램에도 KRX 단절 즉시 표면화. ⑤ **P0 텔레그램 메뉴 자동 동기화** — `metaCommands.buildBotMenuCommands()` 신설: `META_COMMAND_REGISTRY` 키 5개(/watch /positions /learning /control /admin) + `FIXED_BOT_MENU_PRELUDE` 3개(/help /status /now) → 8개 자동 생성. **drift 차단 가드** — `META_COMMAND_REGISTRY` 키와 `META_MENU_DESCRIPTIONS` 키가 일치하지 않으면 throw (누락/추가 키 명시). **Telegram 제약 자동 검증** — command `/^[a-z0-9_]{1,32}$/` + description ≤256자. ⑥ **telegramClient `setTelegramBotCommands()` 자동 생성으로 교체** — 기존 8개 하드코딩 배열 제거, `buildBotMenuCommands()` 호출로 메타 추가 시 자동 갱신. 빌드 실패 (drift 감지) 시 setMyCommands 호출 자체를 스킵하고 에러 로그만 — Telegram API 가 잘못된 페이로드를 받지 않도록 fail-safe. ⑦ **ARCHITECTURE.md** — `server/health/diagnostics.ts` boundary 신규 등록 + Boundary Rules 에 "diagnostics boundary" 추가 (외부 probe 는 `runExternalProbes()` 분리 명시). ⑧ **회귀 테스트 33 케이스 신규** — `diagnostics.test.ts` 24 (deriveYahooStatus 8 분기 + computeVerdict 13 분기 + collectHealthSnapshot 통합 3) + `menuSync.test.ts` 9 (메뉴 8개 정확 / prelude 3 + 메타 5 / Telegram 제약 / META_COMMAND_REGISTRY ↔ MENU_DESCRIPTIONS 동기화 / spec.title 미복사 가드 / idempotent / JSON 직렬화 형식). ⑨ 검증: vitest server/health + server/telegram + server/alerts + server/routes/systemRouter 6 files **116/116 pass** (신규 33 + 기존 무회귀) + alerts 35 무회귀 + lint(client + server tsc) + validate:all (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). ⑩ **P2 별도 PR 분리** — 부팅 reconcile dry-run(server/index.ts ~15줄) + scheduleCatalog wrapJob 메트릭(runCount/failCount/lastErrorMessage/nextRunAt). | `server/health/diagnostics.ts` (신규 304줄), `server/health/diagnostics.test.ts` (신규 24 케이스), `server/telegram/menuSync.test.ts` (신규 9 케이스), `server/telegram/commands/system/health.cmd.ts` (168 → 88, thin wrapper + formatHealthMessage), `server/routes/systemRouter.ts` (-70 LoC, /health/pipeline collectHealthSnapshot 위임 + 11 import 정리), `server/telegram/metaCommands.ts` (+buildBotMenuCommands + META_MENU_DESCRIPTIONS + FIXED_BOT_MENU_PRELUDE 86줄), `server/alerts/telegramClient.ts` (setMyCommands 본체 자동 생성 전환), `ARCHITECTURE.md` (server/health/diagnostics.ts boundary + Boundary Rules diagnostics 항목) | 사용자 페르소나 진단 후속 — health.cmd 와 /health/pipeline 의 진단 로직이 90% 중복으로 drift 위험이 누적되던 구조를 단일 SSOT 로 통합 + setTelegramBotCommands 8개 하드코딩이 commandRegistry 등록 51개와 별개로 관리되어 메타 추가 시 메뉴 갱신 누락 위험을 META_COMMAND_REGISTRY 자동 파생으로 영구 차단. P2(부팅 reconcile + scheduler metrics) 는 본 PR scope 밖으로 분리해 회귀 위험 차단. |
| 2026-04-26 | PR-P 자기학습 시리즈 (PR-A~O) 통합 마무리 — ADR-0032 + server barrel (시리즈 종료): 사용자 요청 "추천대로". 시리즈 15 PR / 14 ADR (0018~0031) 누적 후 마무리 정리 — 향후 wiring PR 진입 시 컨텍스트 비용 최소화 + drift 차단 + onboarding 진입점 마련. ① **ADR-0032 자기학습 시리즈 통합 SSOT** — 5계층 + 3축 (사후 측정 / 사전 신호 / 변환률) 구조 명문화 + 15 PR 모듈 인덱스 + 진입점 (클라이언트 `src/services/quant/selfLearning` / 서버 `server/learning/selfLearningServer`) + 환경변수 롤백 스위치 3종 + 영속 파일 인덱스 6종 + LIVE 자동매매 무영향 보장 + wiring 후속 PR 매트릭스 8건 + 페어 진행 상황 (4/5 완성: 페어 A/D + 독립 #8 / 후속: 페어 B C + wiring 통합). ② **신규 server barrel** `server/learning/selfLearningServer.ts` (≤80줄, @responsibility re-export only) — PR-L (rejectionShadowTracker) + PR-M (counterfactualTwinPortfolio) + PR-N (latentSignalScorer cross-domain re-export from server/screener) + PR-O (orderTypeOptimizer + slippageHistoryRepo cross-domain re-export from server/trading + server/persistence) 핵심 export 통합. 호출자가 `from './selfLearningServer'` 한 줄로 시리즈 서버 측 전체 API 접근. 새 로직 0 — re-export 만. ③ **검증** — vitest **client 50 files / 705 tests + server 135 files / 1311 tests = 총 185 files / 2016 tests pass** (시리즈 전체 누적 무회귀) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종, 798 파일 검사) + precommit 전체 통과 (`ALLOW_DEPLOY_WINDOW=1`). KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정 보장 끝까지 유지). ④ **시리즈 종료 통계** — 15 PR / 14 ADR (0018~0031) / 신규 LoC ~3700줄 (모듈 + 테스트) / 신규 모듈 17건 / 신규 영속 파일 6건 / 환경변수 롤백 스위치 3종 / wiring 후속 PR 매트릭스 8건 명문화. ⑤ **마무리 + 후속 PR 진입 가이드** — wiring PR 들 (Rejection/Twin/Latent/VCP/Slippage entryEngine 호출 + 페어 B Confluence + 페어 C Pyramid/Re-entry) 은 모두 signalScanner Phase B 분해 / exitEngine P2 분해 선행 필요. 본 시리즈가 그 인프라를 사전 마련해 wiring 시 회귀 위험 최소화. | `docs/adr/0032-self-learning-series-overview.md` (신규), `server/learning/selfLearningServer.ts` (신규 barrel) | 사용자 10개 결합 아이디어 분석 → PR-L (P0+) → PR-M (페어 A 완성) → PR-N (페어 D) → PR-O (#8 독립) → PR-P (시리즈 마무리) 자연스러운 흐름 완주. 자기학습 5계층 + 3축 데이터 인프라 단일 진입점 마련. wiring (LIVE 매매 결합) 은 별도 회귀 안전 PR 로 분리 — 본 시리즈 회귀 위험 0 보장. |
| 2026-04-26 | PR-O Order Conversion Optimizer — 슬리피지 학습 + IOC/LIMIT/AGGRESSIVE 동적 선택 (ADR-0031, 사용자 #8 독립): 사용자 요청 "추천대로 해줘". 페어 A (사후 측정) + 페어 D (사전 신호) + #8 변환률 향상 = 자기학습 시리즈 3축 데이터 인프라 완성. 사용자 분석: "강한 돌파에서 limit 미체결로 다음날 갭업당하던 알파 누수가 가장 흔한 패턴". useSlippageStore (클라이언트 zustand) 의 서버 측 미러 + 의사결정 로직 신설. ① **신규 영속** `data/slippage-history.json` (paths.ts +`SLIPPAGE_HISTORY_FILE`, FIFO 1000건). ② **신규 영속 모듈** `server/persistence/slippageHistoryRepo.ts` — `SlippageHistoryEntry` (id/stockCode/signalTime/theoreticalPrice/executedPrice/slippagePct=계산값/orderType=IOC_MARKET\|LIMIT\|AGGRESSIVE_LIMIT/volumeZ?/priceMomentumPct?) + `recordSlippageEntry(input)` (NaN/0/음수 silent skip + slippagePct 자동 계산) + `loadSlippageHistory()` + `getSlippageStats(stockCode \| null, lookback?)` (sampleSize/mean/p50/p90/byOrderType 분리 평균) + `__resetSlippageHistoryForTests()`. ③ **신규 의사결정 모듈** `server/trading/orderTypeOptimizer.ts` — `decideOrderType(input)` 우선순위 SSOT 3 분기: (1) 강한 돌파 — `volumeZ ≥ 2 AND priceMomentumPct > 2` → IOC_MARKET (즉시 체결 우선) / (2) 평균 슬리피지 ≥ 0.5% (표본 ≥ 10) → AGGRESSIVE_LIMIT @ best_bid + 1 tick + 60초 chase + 0.3% 가격 보정 / (3) 기본 → LIMIT (chase 없음). NaN/Infinity 입력 안전 fallback. + `requiresUnsafeIdempotency(decision)` ADR-0014 호환성 가드 (IOC_MARKET 사용 시 호출자 idempotency='unsafe' 명시 필수). ④ **상수 SSOT 7종** — `STRONG_BREAKOUT_VOLUME_Z=2.0` / `STRONG_BREAKOUT_PRICE_MOMENTUM_PCT=2.0` / `HIGH_SLIPPAGE_THRESHOLD=0.005` (0.5%) / `SLIPPAGE_MIN_SAMPLE=10` / `AGGRESSIVE_LIMIT_OFFSET_TICKS=1` / `CHASE_WAIT_SECONDS=60` / `CHASE_PRICE_DELTA_PCT=0.3` / `SLIPPAGE_HISTORY_MAX=1000`. ⑤ **회귀 테스트 25 케이스 신규** — slippageHistoryRepo 11 (정상 영속 + 음수 슬리피지 + 0/NaN skip + volumeZ optional fallback + 1000건 FIFO trim + 빈 데이터 + 전체/종목별/byOrderType 분리 + lookback 옵션) + orderTypeOptimizer 14 (IOC 분기 4 + AGGRESSIVE 분기 2 + LIMIT 기본 4 + reason 메시지 2 + boundary + ADR-0014 가드 + 상수 SSOT 2). ⑥ **wiring 본 PR scope 밖** — `entryEngine` 의 주문 타입 결정 시점에 `decideOrderType()` 호출 + `fillMonitor` 가 체결 발생 시 `recordSlippageEntry()` 호출 + `useSlippageStore` 클라이언트 데이터 서버 미러링 + chase 실행 로직 (orderQueue 미체결 1분 후 재발주) 모두 후속 PR. 본 PR 은 모듈 + 영속 + 테스트만. ⑦ **검증** — vitest server/trading + server/persistence **54 files / 488 tests pass** (신규 25 + 기존 463 무회귀) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종) + precommit 전체 통과 (`ALLOW_DEPLOY_WINDOW=1`). KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). | `docs/adr/0031-order-type-optimizer.md` (신규), `server/persistence/slippageHistoryRepo.ts` (신규 ≤180줄), `server/persistence/slippageHistoryRepo.test.ts` (신규 11 케이스), `server/trading/orderTypeOptimizer.ts` (신규 ≤120줄), `server/trading/orderTypeOptimizer.test.ts` (신규 14 케이스), `server/persistence/paths.ts` (+SLIPPAGE_HISTORY_FILE) | 사용자 10개 결합 아이디어 #8 독립 PR. 페어 A (사후) + 페어 D (사전) + #8 (변환) 3축 데이터 인프라 완성. ADR-0014 idempotency='unsafe' (PR-34) 정책 준수 — IOC_MARKET 사용 시 호출자가 5xx 재시도 차단 필수. 기존 useSlippageStore 클라이언트 데이터를 서버 측에서 학습 가능한 영속 + 의사결정 SSOT 마련. wiring (entryEngine 호출) Phase B 완주 후 후속 PR. |
| 2026-04-26 | PR-N Latent Signal Scorer — VCP + Latent Catalyst 통합 사전 신호 (ADR-0030, 페어 D): 사용자 요청 "권장". 페어 A (PR-L+M) 가 사후 측정이라면 페어 D 는 사전 신호 정량화. 사용자 10개 결합 아이디어의 #6 (VCP Hunter) + #10 (Latent Catalyst Scanner) 두 신호가 z-score 기반 + 같은 OHLCV/수급 입력이라 단일 모듈로 통합. ① **신규 모듈** `server/screener/latentSignalScorer.ts` (≤220줄, @responsibility SRP) — 순수 함수만, 외부 호출 0, 영속 0, 부작용 0. preBreakoutAccumulationDetector (매집 패턴) 와 다른 차원 (변동성 압축 + 수급 leading). ② **VCP Score** (Mark Minervini 식) — 4 sub-criteria: (1) ATR 단조감소 atr5w>atr3w>atr1w / (2) 거래량 단조감소 / (3) RS ≥ 80 / (4) priceMa200Ratio > 1.0. 각 25점, 통과 개수 → grade A(4)/B(3)/C(2)/NONE. NaN/0/음수 입력 안전 false fallback. ③ **Latent Catalyst Score** — 4축 z-score 합성 `(foreignNetBuy5dZ + institutionalNetBuy5dZ + volumeProgression/10×2 - volatility5dZ) / 4`. 변동성 z 가 음수일수록 +기여 (정상화 = 사전 신호). 임계: ≥+1.5 LATENT_CATALYST / ≥+0.5 WEAK_SIGNAL / 그 외 NONE. sub-component (foreign/institutional/volume/volatility contribution) 분해 영속. ④ **combinedSignalGrade** — VCP A + Latent CATALYST = STRONG_PRE_BREAKOUT / VCP A 또는 Latent CATALYST 단독 = PRE_BREAKOUT / VCP B-C 또는 WEAK_SIGNAL = WATCH / 그 외 NONE. ⑤ **상수 SSOT 3종** — `VCP_RS_THRESHOLD=80` / `LATENT_CATALYST_THRESHOLD=1.5` / `WEAK_SIGNAL_THRESHOLD=0.5`. ⑥ **외부 호출 0 + 영속 0** — 모든 입력은 호출자 책임 (외인/기관 5일 누적 z-score 계산은 후속 PR 의 입력 가공 단계에서). ADR-0009/0010 외부 호출 예산 무영향. ⑦ **회귀 테스트 21 케이스 신규** — VCP 8 (4조건 충족 / RS 미달 B / 2조건 C / 0조건 NONE / ATR boundary 동일값 / RS 80 정확 / MA200 1.0 정확 / NaN-0-음수 fallback) + Latent 8 (강한 4축 / 중간 / 약한 / +1.5 boundary / +0.5 boundary / 변동성 부호 역전 / NaN-Infinity fallback / sub-component 정확) + combined 5 (VCP A + LATENT / VCP A 단독 / LATENT 단독 / WATCH / NONE). ⑧ **wiring 본 PR scope 밖** — `watchlistManager` tag / `stockScreener` 통합 / VCP_HUNTER section 신설 / 공시 도착 시 LATENT→CATALYST 자동 승격 / 외인/기관 5일 z-score 계산 헬퍼 모두 후속 PR. 본 PR 은 점수 산출 함수 + 테스트만. ⑨ **검증** — vitest server/screener **7 files / 126 tests pass** (신규 21 + 기존 105 무회귀) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종) + precommit 전체 통과 (`ALLOW_DEPLOY_WINDOW=1`). KIS/KRX 자동매매 quota 0 침범. | `docs/adr/0030-latent-signal-scorer.md` (신규), `server/screener/latentSignalScorer.ts` (신규 ≤220줄), `server/screener/latentSignalScorer.test.ts` (신규 21 케이스) | 사용자 10개 결합 아이디어 페어 D (#6+#10) 완성. 페어 A (사후 측정) + 페어 D (사전 신호) 양 축 마련. preBreakoutAccumulationDetector 매집 패턴과 결합 시 시너지 — 매집 + 변동성 압축 + 수급 leading 3 신호 동시 충족 종목은 후속 wiring PR 에서 VCP_HUNTER 섹션으로 분리 가능. 페어 B (#2+#7+#4 entry confluence) / 페어 C (#5+#3 position lifecycle) / 독립 (#8 order optimizer) 후속 PR. |
| 2026-04-26 | PR-M Counterfactual Twin Portfolio — 3 Twin (AGGRESSIVE/DISCIPLINED/EQUAL_WEIGHT) 평행 포트폴리오 (ADR-0029, 페어 A 완성): 사용자 요청 "추천대로". PR-L Rejection Tracker (거짓 부정 측정) 의 페어로 페어 A (Anti-Counterfactual SSOT) 의 두 번째 절반. ledgerSimulator (같은 신호의 다른 매도 시뮬레이션) 와 다른 차원 — **다른 진입 임계 자체** 의 평행 포트폴리오로 시스템이 자기 자신을 메타 평가. ① **Twin 3종 정의** — AGGRESSIVE (Gate ≥14, KELLY 사이즈) / DISCIPLINED (Gate ≥22, KELLY) / EQUAL_WEIGHT (Gate ≥18, 균등 0.10). LIVE Gate 임계 18 기준 ±4점 / 균등 비중 3축 비교. ② **신규 영속** `data/twin-portfolio.json` (paths.ts +`TWIN_PORTFOLIO_FILE`, FIFO 1500건). ③ **신규 모듈** `server/learning/counterfactualTwinPortfolio.ts` (≤350줄, @responsibility SRP) — `TwinEntry` (id/twin/stockCode/signalDate/signalGateScore/entryPrice/positionWeight/trackUntil/currentReturnPct/status/exitPrice/exitDate) + `recordTwinEntries(candidate)` (Gate Score 별 Twin 정책 평가 후 통과한 Twin 만 영속, 0~3건 추가, 멱등 dupKey=`code\|date\|twin`) + `refreshTwinPortfolio(opts)` (활성 entry currentReturnPct 갱신 + 30일 만료 시 CLOSED + exitPrice 영속, **동일 stockCode 의 여러 Twin 은 priceFetcher 1회만 호출 — Map 캐시**) + `compareTwinsVsReal(realCumReturnPct)` (Twin 별 사이즈 가중 누적 수익률 + 단순 Sharpe proxy + Real 대비 우월 판정 weekWinning) + `detectPromotion(history[])` (4주 연속 weekWinning=true 시 promotion 트리거, ALL_TWIN_KEYS 순서로 첫 매칭). ④ **상수 SSOT 5종** — `TWIN_HORIZON_DAYS=30` / `TWIN_EQUAL_WEIGHT=0.10` / `TWIN_PORTFOLIO_MAX=1500` / `PROMOTION_CONSECUTIVE_WEEKS=4` / `ALL_TWIN_KEYS` 배열. ⑤ **사이즈 가중 누적 수익률** — 단순 합산이 아니라 `∏(1 + r×w/100) - 1` 복리 — Twin 별 다른 사이즈 정책의 실제 자본 영향을 정확히 반영. ⑥ **회귀 테스트 21 케이스 신규** — TWIN_POLICIES SSOT 정합 3 (3 Twin 매핑 + 5 상수) + recordTwinEntries 7 (Gate 12 모두 reject / Gate 16 AGGR만 / Gate 20 AGGR+EQUAL / Gate 24 모두 / dupKey 멱등 / entryPrice 0/NaN skip / kellyWeight clamp 0~1) + refreshTwinPortfolio 4 (가격 갱신 3 Twin / priceFetcher 캐시 1회 호출 / 30일 만료 CLOSED + exitPrice / null/throw skip) + compareTwinsVsReal 2 (빈 데이터 / Real 대비 우월 판정) + detectPromotion 5 (history < 4주 null / 4주 연속 trigger / 5주 중 마지막 4주 / 3주 + 1주 break / 동시 우월 시 ALL_TWIN_KEYS 첫 매칭). ⑦ **wiring 본 PR scope 밖** — signalScanner 의 candidate 평가 시점에 `recordTwinEntries` 호출하는 wiring 은 후속 PR (Phase B 완주 후). 본 PR 은 모듈 + 영속 + 테스트만. ⑧ **자기학습 폐쇄 루프 결합** — promotionTriggered=AGGRESSIVE 4주 → 운영자 검토 후 Gate 임계 14~17 완화 검토 (PR-L Rejection Tracker 의 falseNegativeRate 와 교차 검증). promotionTriggered=DISCIPLINED → 임계 22 강화. **자동 임계 조정 절대 금지** (절대 규칙 #4 보호 — 운영자 수동만). ⑨ **검증** — vitest server/learning **23 files / 178 tests pass** (신규 21 + 기존 157 무회귀) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종) + precommit 전체 통과 (`ALLOW_DEPLOY_WINDOW=1` — 신규 모듈만 LIVE 매매 본체 무영향). KIS/KRX 자동매매 quota 0 침범. | `docs/adr/0029-counterfactual-twin-portfolio.md` (신규), `server/learning/counterfactualTwinPortfolio.ts` (신규 ≤350줄), `server/learning/counterfactualTwinPortfolio.test.ts` (신규 21 케이스), `server/persistence/paths.ts` (+TWIN_PORTFOLIO_FILE) | 사용자 10개 결합 아이디어 페어 A (#1+#9) 완성. PR-L 이 "거절 추적" 으로 거짓 부정을 측정한다면, PR-M 은 "다른 진입 정책의 평행 포트폴리오" 로 시스템이 자기 자신을 메타 평가. 두 모듈이 결합되어 알파 누수의 양 축 (false negative + alternative policy) 을 동시에 측정. ledgerSimulator 와 같은 영속 파일 사용 안 함 (다른 차원이므로 분리). 페어 B (#2+#7+#4 entry confluence) / 페어 C (#5+#3 position lifecycle) / 페어 D (#10+#6 leading indicator) / 독립 (#8 order optimizer) 후속 PR. |
| 2026-04-26 | PR-L Rejection Universe Tracker — Gate Score 14~17 near-miss 거절 종목 5영업일 사후 추적 (ADR-0028, 사용자 10개 결합 아이디어 P0+ 격상): 사용자 요청 "추천옵션대로 하자". 자기학습 5계층 시리즈 PR-A~K 가 클라이언트 측 학습 데이터 무결성을 구축한 위에, 서버 측 "거짓 부정 (놓친 알파)" 측정 인프라 신설. ① **신규 영속** `data/rejection-shadow.json` (paths.ts +`REJECTION_SHADOW_FILE`). ② **신규 모듈** `server/learning/rejectionShadowTracker.ts` (≤300줄, @responsibility SRP) — `RejectionShadowEntry` (id/stockCode/signalDate/signalPriceKrw/gateScore/scoreDelta/rejectionReason/trackUntil/currentReturnPct/closed) + `recordRejection(input)` (Gate Score 14~17 만 영속, 그 외 silent skip, dupKey idempotent) + `addBusinessDays(yyyymmdd, n)` (KST 토/일 제외 영업일 계산, 한국 공휴일 미반영) + `refreshRejectionShadow(opts)` (활성 entry currentReturnPct 갱신 + trackUntil 만료 종결, ghostPortfolioTracker 동일 패턴) + `summarizeRejectionShadow()` (avg/median/quartiles/falseNegativeRate=종결 entry 중 ≥+5% 비율) + `getAllRejectionShadow()` + `__resetRejectionShadowForTests()`. ③ **임계값 SSOT 5종 export** — REJECTION_NEAR_MISS_MIN=14 / MAX=17 / GATE_THRESHOLD=18 / TRACK_BUSINESS_DAYS=5 / REJECTION_SHADOW_MAX=500 (FIFO trim). ④ **회귀 테스트 20 케이스 신규** (`rejectionShadowTracker.test.ts`) — addBusinessDays 4 (월/금/토 진입 + 영업일 1) + recordRejection 7 (Gate 14/17 boundary + 13 skip + 18 skip + dupKey + signalPrice 0/-1/NaN + SSOT 상수) + refreshRejectionShadow 5 (가격 갱신 +5% / 만료 closed=true / 가격 null skip / throw 안전 흡수 / 이미 closed skip) + summarizeRejectionShadow 4 (빈 데이터 / 5건 분포 [-10/-2/3/7/15] avg=2.6 median=3 fnRate=0.4 / 표본<5 reliable=false / active 통계 제외). ⑤ **wiring 본 PR scope 밖** — signalScanner / stockScreener 의 `recordRejection` 호출은 절대 규칙 #4 (autoTradeEngine 단일 통로) 보호 + signalScanner Phase B 분해 진행 중이라 회귀 위험 차단을 위해 후속 PR (Phase B 완료 후) 분리. 본 PR 은 **모듈 + 영속 + 테스트만**. ⑥ **자기학습 시리즈 결합** — summarizeRejectionShadow().falseNegativeRate ≥ 30% 시 텔레그램 경보 → 운영자 Gate 임계 검토 / 향후 PR-I `classifyConditionAttribution` 의 추가 입력으로 결합 가능. ⑦ **검증** — vitest server/learning **22 files / 157 tests pass** (신규 20 + 기존 137 무회귀) + lint(client + server tsc) + validate:all (DeployWindow + PRES + ACMA + SDS + SRP + SymbolBoundary + Gemini 7종) + precommit 전체 통과 (`ALLOW_DEPLOY_WINDOW=1` — 본 PR 신규 모듈만으로 LIVE 매매 본체 무영향이라 정식 escape hatch 사용). KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 무수정). | `docs/adr/0028-rejection-universe-tracker.md` (신규), `server/learning/rejectionShadowTracker.ts` (신규 ≤300줄), `server/learning/rejectionShadowTracker.test.ts` (신규 20 케이스), `server/persistence/paths.ts` (+REJECTION_SHADOW_FILE) | 사용자 10개 결합 아이디어 분석 후 추천 옵션 (PR-L 부터 시작) 채택. 알파 누수의 절반인 "거의 통과할 뻔한 거절" 의 사후 수익률 분포 측정 인프라 마련 — 향후 wiring PR (Phase B 완료 후) 에서 signalScanner 거절 시점에 `recordRejection` 호출 추가하면 자기학습 1계층 (데이터 신뢰도) 보강 완성. 페어 A (Anti-Counterfactual SSOT) 의 절반 — 나머지 절반인 #9 Twin Portfolio 는 후속 PR. |
| 2026-04-26 | PR-K 자기학습 시리즈 (PR-A~J) 통합 리팩토링 + 통합 검증: 사용자 요청 "리팩토링, 에러검증". 시리즈 11개 PR (A~K) 누적 후 drift 차단 + 호출자 1-stop import 진입점 마련. ① **`src/services/quant/__test-utils__/localStorageMock.ts` 신규** — regimeMemoryBank/learningShadowModel 두 테스트 파일에 중복되던 in-memory localStorage mock (~12 LoC × 2) 을 `attachMockLocalStorage()` 단일 헬퍼로 통합. node 환경에서만 부착, 멱등(여러 번 호출해도 store reset 없음). ② **`src/services/quant/selfLearning.ts` 신규 barrel** — PR-A~J 의 9개 모듈 (checklistToConditionScores / recommendationSnapshotRepo / sourceWeighting / lossReasonClassifier / lossReasonWeighting / conditionEdgeScore / regimeMemoryBank / conditionAttribution / learningShadowModel / feedbackLoopEngine) 의 핵심 export 를 단일 진입점으로 re-export. 호출자가 `from '../services/quant/selfLearning'` 한 줄로 자기학습 시리즈 전체 API 접근 가능. 새 로직 0 — re-export 만. ③ **검증**: vitest **client 50 files 705 pass + server 130 files 1224 pass = 총 180 files 1929 tests pass** + lint + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범. PR-A~J 시리즈 본체 무수정 (drift 위험 0). | `src/services/quant/__test-utils__/localStorageMock.ts` (신규), `src/services/quant/selfLearning.ts` (신규 barrel), `src/services/quant/regimeMemoryBank.test.ts` + `src/services/quant/learningShadowModel.test.ts` (mock 헬퍼 사용 전환) | 사용자 요청 "다음 진행 — e-j까지 실행, 그리고 리팩토링, 에러검증". 시리즈 11개 PR 완주 후 마무리. |
| 2026-04-26 | PR-J Shadow Model 학습 알고리즘 검증 (ADR-0027): 사용자 요청 "다음 진행". `evaluateFeedbackLoop` 에 옵셔널 `FeedbackLoopOptions` 추가 (`shadow` / `weightStep` / `upThreshold` / `downThreshold`). shadow=true → `saveEvolutionWeights` 호출 차단 (LIVE 가중치 무영향). 신규 `compareShadowVsLive(trades, weights, shadowOptions)` — 동일 입력에 LIVE 옵션 + Shadow 옵션 두 번 실행 후 ConditionDivergence (liveWeight/shadowWeight/delta/agreement) + shadowConfidence (방향 일치 비율) + avgWeightDelta 반환. `isPromotable()` 보수적 승격 기준 (≥5 conditions, confidence ≥0.8, delta ≤0.05). 회귀 테스트 10 케이스. LIVE 자동매매 무영향. | `docs/adr/0027-learning-shadow-model.md`, `src/services/quant/feedbackLoopEngine.ts` (FeedbackLoopOptions + shadow 가드 + threshold override), `src/services/quant/learningShadowModel.ts` (신규 100줄), `src/services/quant/learningShadowModel.test.ts` (신규 10 케이스) | 사용자 분석 천재 아이디어 #12 — 신규 학습 로직을 LIVE 가중치 망치지 않고 30~100건 그림자 검증 후 승격 가능한 구조. |
| 2026-04-26 | PR-I 조건 귀인 분석 4 분류 (ADR-0026): 사용자 요청 "다음 진행". `classifyConditionAttribution(conditionId, trades)` — 27조건 각각이 매매 결과에 미치는 질적 역할을 ALPHA_DRIVER (수익 거래 반복 강함) / RISK_PROTECTOR (양쪽 모두 낮은 점수) / NOISE_FACTOR (수익/손실 무관) / FALSE_COMFORT (손실 거래에서 높음) 로 분류. 우선순위: spread ≥ +2 + winAvgScore ≥ 5 → ALPHA_DRIVER / spread ≤ -2 + lossAvgScore ≥ 5 → FALSE_COMFORT / 양쪽 < 3 → RISK_PROTECTOR / 그 외 NOISE_FACTOR. raw 점수 분포 기반 (PR-E lossReason multiplier 미적용 — 의미 왜곡 방지). `classifyAllConditions()` 27조건 일괄 분류 + 그룹핑 + reliableCount. 회귀 테스트 8 케이스. LIVE 자동매매 무영향. | `docs/adr/0026-attribution-classifier.md`, `src/services/quant/conditionAttribution.ts` (신규 105줄), `src/services/quant/conditionAttribution.test.ts` (신규 8 케이스) | 사용자 분석 5계층 + 천재 아이디어 (귀인 분석) — 가중치 보정과 별개의 읽기 전용 분석 레이어. |
| 2026-04-26 | PR-H 사용자 수동 lossReason override API (ADR-0025): 사용자 요청 "다음 진행". `useTradeStore.setLossReason(tradeId, reason \| null)` 신규 액션 — 수동 입력 시 `lossReasonAuto=false` 표기로 PR-D 자동 분류 덮어쓰기 차단. reason=null → 수동 분류 해제 (자동 분류 모드 복원). 회귀 테스트 5 케이스. UI 컴포넌트는 후속 별도 PR. | `docs/adr/0025-loss-reason-manual-override.md`, `src/stores/useTradeStore.ts` (+setLossReason 액션), `src/stores/useTradeStore.lossReason.test.ts` (신규 5 케이스) | PR-D 의 자동 분류 4개 외 나머지 4 분류 (FALSE_BREAKOUT/SECTOR_ROTATION_OUT/EARNINGS_MISS/LIQUIDITY_TRAP) 사용자 수동 입력 인프라. |
| 2026-04-26 | PR-G 레짐별 가중치 분리 (Regime Memory Bank, ADR-0024): 사용자 요청 "다음 진행". 천재 아이디어 #2. `evolutionEngine` 글로벌 weights 위에 `byRegime` store 추가 (`k-stock-evolution-weights-by-regime` localStorage 키). `evaluateFeedbackLoopByRegime(trades, regime?, weights?)` — regime 지정 시 `trade.entryRegime === regime` 필터 후 학습 / 미지정 시 기존 동작. `evaluateAllRegimes(trades)` 모든 regime 일괄 학습. `TradeRecord.entryRegime` 옵셔널 — useTradeOps.recordTrade 가 매수 시점 `useGlobalIntelStore.marketRegimeClassifierResult.classification` 캡처. `LEARNING_REGIME_BANK_DISABLED` env 롤백 스위치. ALL_REGIMES 7 분류 SSOT. 회귀 테스트 12 케이스. LIVE 자동매매 무영향. | `docs/adr/0024-regime-memory-bank.md`, `src/services/quant/regimeMemoryBank.ts` (신규 130줄), `src/services/quant/regimeMemoryBank.test.ts` (신규 12 케이스), `src/types/portfolio.ts` (+TradeRecord.entryRegime?), `src/hooks/useTradeOps.ts` (entryRegime 캡처 wiring) | 사용자 분석 Phase 4 — 시장 국면별 알파 신호가 평준화되던 단일 글로벌 weight 한계 해소. |
| 2026-04-26 | PR-F 조건별 Profit Factor / Edge Score 통계 (ADR-0023): 사용자 요청 "다음 진행". `ConditionCalibration` 에 `profitFactor` / `avgReturnPosi` / `avgReturnNeg` / `edgeScore` 옵셔널 4 필드 추가. `computeConditionEdge(relevant, winRate, avgReturn)` — PR-E lossReason multiplier 동일 적용한 가중평균 PF + Edge Score (winRate/avgReturn/PF/MDD penalty 합산, 범위 -8 ~ +7). 손실 0 → PF null, winSum 0 → PF 0. 회귀 테스트 8 케이스. 학습 알고리즘 미변경 — 통계 메타만 추가. LIVE 자동매매 무영향. | `docs/adr/0023-condition-profit-factor-edge-score.md`, `src/services/quant/conditionEdgeScore.ts` (신규 70줄), `src/services/quant/conditionEdgeScore.test.ts` (신규 8 케이스), `src/services/quant/feedbackLoopEngine.ts` (computeConditionEdge wiring), `src/types/portfolio.ts` (ConditionCalibration +4 옵셔널) | 사용자 분석 3계층 + 보완점 1 — 단순 승률 한계 보완 (승률 70% +2%/-8% vs 승률 45% +25%/-5% 구분 가능). |
| 2026-04-26 | PR-E 손실 원인별 학습 가중치 보정 (Confidence-Weighted Learning, ADR-0022, 자기학습 5계층 확장 시리즈 5번): 사용자 요청 "다음 진행". PR-D (lossReason 자동 분류) 의 직접 후속 — PR-D 가 분류만 하고 학습에는 활용하지 않던 갭 해소. 사용자 분석 천재 아이디어 #1 (Confidence-Weighted Learning) 직접 구현. 기존 `feedbackLoopEngine` 은 모든 거래를 동등하게 학습 입력으로 처리해 STOP_TOO_TIGHT (손절폭만 좁았음) 거래가 "조건 자체가 나빴다" 로 잘못 학습되거나 MACRO_SHOCK (시장 전체 급락) 거래가 종목/조건 가중치를 부당하게 하향시키던 구조적 결함. 반대로 OVERHEATED_ENTRY (진입 신호 노이즈) 손실은 학습 신호가 명확함에도 강화되지 않던 비대칭 문제. ① **신규 SSOT** `src/services/quant/lossReasonWeighting.ts` (78줄, @responsibility SRP 통과) — `LOSS_REASON_LEARNING_MULTIPLIER` 9 LossReason 매핑: STOP_TOO_TIGHT 0.3 (조건 잘못 아님 학습 약화) / MACRO_SHOCK 0.2 (전 조건 noise 강력 약화) / OVERHEATED_ENTRY 1.5 (진입 노이즈 강화) / STOP_TOO_LOOSE 1.5 (손절 신호 강화) / SECTOR_ROTATION_OUT 0.5 (섹터 문제 부분 약화) / EARNINGS_MISS 0.5 (외부 충격 부분 약화) / LIQUIDITY_TRAP 0.7 / FALSE_BREAKOUT 1.0 (조건 점검 가치 정상) / UNCLASSIFIED 1.0. + `getTradeLearningWeight(trade)` 우선순위 SSOT (ENV disable → 1.0 / 수익 거래 → 1.0 / lossReason 부재 → 1.0 / lossReason 매핑) + `summarizeLossReasonBreakdown(trades)` 분포 진단 헬퍼 + `LEARNING_LOSS_REASON_WEIGHTING_DISABLED` 긴급 롤백 env. ② **feedbackLoopEngine 가중평균 알고리즘 도입** — 기존 `winRate = wins.length / relevant.length` 단순 비율을 `weightedTotal = sum(getTradeLearningWeight(t)), weightedWins = wins.reduce(s+w), winRate = weightedWins / weightedTotal` 가중평균으로 교체. avgReturn 도 동일하게 가중평균. 0/0 안전 가드 (weightedTotal === 0 → winRate=0 + STABLE 진입). ③ **ConditionCalibration 결과 확장** (`src/types/portfolio.ts`) — 옵셔널 3 필드 추가: `weightedTradeCount?: number` (sum of multipliers, 가중평균 분모) + `rawTradeCount?: number` (원래 trade 수, UI raw vs weighted 표기) + `lossReasonBreakdown?: Partial<Record<LossReason, number>>` (분포 진단). ④ **수치 효과 검증** — 동일 18 승 / 12 패 (60% 승률) 시나리오에서 lossReason 분포에 따라 학습 결과 분기: Case A (UNCLASSIFIED 12) → weightedTotal=30, winRate=60% → STABLE / **Case B (STOP_TOO_TIGHT 12) → weightedTotal=21.6, winRate=83.3% → UP** (조건은 좋은데 손절폭만 문제) / **Case C (OVERHEATED_ENTRY 12) → weightedTotal=36, winRate=50% → STABLE** (진입 신호 노이즈 명확하게 학습 보수화) / Case D (MACRO_SHOCK 24+wins 6) → weightedTotal=10.8, winRate=55.5% → STABLE (시장 충격 무시되어 손실이 학습에 거의 미반영). ⑤ **회귀 테스트 22 케이스 신규** — `lossReasonWeighting.test.ts` 14 (LOSS_REASON_LEARNING_MULTIPLIER SSOT 정합성 2 + getTradeLearningWeight 9 분기: 수익/lossReason 부재/STOP_TOO_TIGHT 0.3/MACRO_SHOCK 0.2/OVERHEATED 1.5/STOP_TOO_LOOSE 1.5/UNCLASSIFIED 1.0/disable env 롤백/NaN-Infinity fallback + summarizeLossReasonBreakdown 3) + `quantEngine.feedbackLoop.weighted.test.ts` 8 (Case A~G 7 시나리오 + 0/0 fallback 가드). ⑥ **검증**: vitest **45 files 662/662 pass** (신규 22 + 기존 640 무회귀) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). ⑦ **시리즈 5번째 PR 완료** — PR-A/B/C/D/E 누적: PR-A 데이터 무결성 + PR-B RecommendationSnapshot lifecycle + PR-C source 차등 학습 (조건별) + PR-D 손실 원인 자동 분류 + PR-E 손실 원인별 학습 가중치 (trade level). 이제 학습이 "조건 신뢰도 (PR-C)" + "거래 신뢰도 (PR-E)" 양 축으로 보정되어 환각/외부충격/단순 손절폭 문제가 학습 가중치 폭주를 만들지 않음. 후속 PR 후보: 조건별 Profit Factor / MDD / Edge Score 통계 확장 / 레짐별 가중치 분리 / 수동 lossReason 입력 UI / 귀인 분석. | `docs/adr/0022-loss-reason-weighted-learning.md` (신규), `src/services/quant/lossReasonWeighting.ts` (신규 78줄), `src/services/quant/lossReasonWeighting.test.ts` (신규 14 케이스), `src/services/quant/feedbackLoopEngine.ts` (가중평균 도입 + lossReasonBreakdown wiring), `src/services/quantEngine.feedbackLoop.weighted.test.ts` (신규 8 케이스), `src/types/portfolio.ts` (ConditionCalibration +weightedTradeCount/rawTradeCount/lossReasonBreakdown 옵셔널) | 사용자 분석 천재 아이디어 #1 (Confidence-Weighted Learning) 직접 구현 — "조건 가중치를 단순 승률이 아니라 데이터 신뢰도 × 표본 수 × 손익비로 조정. 환각성 데이터에 오염되지 않는다." PR-D 의 lossReason 분류가 학습 보정에 직접 활용되어 시리즈 4번/5번이 결합. PR-C 의 조건 신뢰도 multiplier (COMPUTED 1.0 / AI 0.4) 와 PR-E 의 거래 신뢰도 multiplier (lossReason 별 0.2~1.5) 가 곱해져 학습이 노이즈에 강해진다. |
| 2026-04-26 | PR-D 손실 원인 태그 + 자동 분류 (ADR-0021, 자기학습 5계층 확장 시리즈 4번 / 마지막): 사용자 요청 "네". PR-A (데이터 무결성) + PR-B (RecommendationSnapshot) + PR-C (AI/COMPUTED 차등 학습) 후속 — `TradeRecord.sellReason` 이 거시적 매도 동기(TARGET_HIT/STOP_LOSS/TRAILING_STOP/SELL_SIGNAL/MANUAL) 5분류만 기록해 자기학습이 답해야 할 진짜 질문 "이 거래가 왜 손실났는가" (손절폭 과도/시장 급락/과열 진입/손절 지연 등) 의 분류 정보가 부재했던 구조적 결함 해소. ① **새 schema** `LossReason` (`src/types/portfolio.ts`) — 사용자 분석 보완점 3 의 8 분류 + UNCLASSIFIED fallback union: 'FALSE_BREAKOUT'/'MACRO_SHOCK'/'SECTOR_ROTATION_OUT'/'EARNINGS_MISS'/'LIQUIDITY_TRAP'/'OVERHEATED_ENTRY'/'STOP_TOO_TIGHT'/'STOP_TOO_LOOSE'/'UNCLASSIFIED'. + TradeRecord 옵셔널 3 필드: `lossReason?: LossReason` + `lossReasonAuto?: boolean` (자동 분류 vs 사용자 수동) + `lossReasonClassifiedAt?: string`. + evaluationSnapshot 옵셔널 1 필드: `vkospiAtBuy?: number` (PR-A v2 schema 확장 — MACRO_SHOCK 분류 입력). ② **신규 분류기** `src/services/quant/lossReasonClassifier.ts` (140줄, @responsibility SRP 통과) — `classifyLossReason(input)` 우선순위 SSOT 4 분기 + UNCLASSIFIED fallback: (1) **MACRO_SHOCK** — `vkospiAtSell - vkospiAtBuy >= 8` AND `returnPct <= -3%` (시장 전체 급락), (2) **STOP_TOO_TIGHT** — `holdingDays <= 3` AND `-10% < returnPct <= -3%` AND `sellReason === 'STOP_LOSS'` (빠른 손절폭 도달), (3) **OVERHEATED_ENTRY** — `holdingDays <= 5` AND (조건 17 심리적객관성 ≤ 3 또는 조건 25 VCP = 0) (과열 진입 후 즉시 하락), (4) **STOP_TOO_LOOSE** — `returnPct <= -15%` (손절폭 너무 넓음), (5) **UNCLASSIFIED** — 위 모두 미해당. 임계값 5종 SSOT export (MACRO_SHOCK_VKOSPI_DELTA=8 / STOP_TOO_TIGHT_HOLDING_DAYS_MAX=3 / STOP_TOO_TIGHT_RETURN_PCT_MIN=-10 / OVERHEATED_ENTRY_HOLDING_DAYS_MAX=5 / STOP_TOO_LOOSE_RETURN_PCT_MAX=-15). returnPct ≥ 0 / NaN/Infinity 입력 시 UNCLASSIFIED 안전 fallback. VKOSPI 1개만 있을 때도 MACRO_SHOCK 자동 스킵. + `buildLossReasonMeta(reason, now?)` 헬퍼. ③ **TradeRecordModal vkospi 캡처** — `useGlobalIntelStore` 의 `macroEnv.vkospi` 를 읽어 `evaluationSnapshot.vkospiAtBuy` 로 저장. 매수 시점 macro 컨텍스트 영속화. ④ **useTradeOps.closeTrade wiring** — returnPct < 0 일 때만 분류기 진입 + `lossReasonAuto=false` (사용자 수동 입력) 보존 가드: `userManualSet` 체크로 자동 분류가 사용자 수동 분류를 덮어쓰지 않음. macroEnv.vkospi 매도 시점 캡처 + 분류 결과 record 에 부여. ⑤ **자동 분류 외 4분류** (FALSE_BREAKOUT / SECTOR_ROTATION_OUT / EARNINGS_MISS / LIQUIDITY_TRAP) — 다중 trade 분석 또는 외부 데이터 필요로 본 PR scope 밖. 사용자 수동 입력 UI (TradeJournal dropdown) 후속 PR. ⑥ **회귀 테스트 27 케이스 신규** (`lossReasonClassifier.test.ts`) — 수익/비정상 입력 2 (returnPct≥0 / NaN-Infinity) + MACRO_SHOCK 5 (boundary +8 / +10 우선순위 / +7 미달 / VKOSPI 부재 / 1개만 있음) + STOP_TOO_TIGHT 5 (정상 / holdingDays 4 미진입 / -2% 미진입 / -10% boundary 미진입 / sellReason MANUAL 미진입) + OVERHEATED_ENTRY 5 (조건 17 / 조건 25 / holdingDays 6 미진입 / 정상값 미진입 / conditionScores 부재) + STOP_TOO_LOOSE 3 (-15% boundary / -20% / -14% 미달) + UNCLASSIFIED 3 (gap zone / -3% boundary / -2.99% 미달) + 우선순위 충돌 2 (MACRO_SHOCK > STOP_TOO_TIGHT / STOP_TOO_TIGHT vs LOOSE disjoint gap) + buildLossReasonMeta 1 + 상수 검증 1. ⑦ **검증**: vitest **43 files 640/640 pass** (신규 27 + 기존 613 무회귀) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). ⑧ **결과** — PR-A/B/C/D 시리즈 완료. PR-A 의 conditionScores 무결성 + PR-B 의 RecommendationSnapshot lifecycle + PR-C 의 source 차등 학습 + PR-D 의 손실 원인 태그가 결합되어 자기학습 5계층 (1.데이터 신뢰도 → 2.거래 기록 → 3.조건별 성과 분석 → 4.가중치 조정 → 5.실행 정책) 의 기반이 마련됐다. 후속 PR 후보: 사용자 수동 입력 UI (TradeJournal dropdown) / 손실 원인별 학습 가중치 보정 / 다중 trade 기반 SECTOR_ROTATION_OUT 자동 분류 / 레짐별 가중치 분리. | `docs/adr/0021-loss-reason-tagging.md` (신규), `src/types/portfolio.ts` (+LossReason union + TradeRecord 옵셔널 3 필드 + evaluationSnapshot.vkospiAtBuy?), `src/services/quant/lossReasonClassifier.ts` (신규 140줄), `src/services/quant/lossReasonClassifier.test.ts` (신규 27 케이스), `src/components/trading/TradeRecordModal.tsx` (macroEnv.vkospi 매수 시점 캡처), `src/hooks/useTradeOps.ts` (closeTrade 분류기 wiring + 사용자 수동 override 보존 가드) | 사용자 분석 보완점 3 — "손실 거래에서 왜 손실났는지를 따로 저장해야 함. 손실 원인 태그가 있어야 시스템이 똑똑해짐" 직접 구현. 4 분기 자동 분류는 청산 시점 컨텍스트(returnPct + holdingDays + sellReason + conditionScores + vkospi 변화)만으로 추론 가능한 안전 범위만 포함. 다중 trade 분석이나 외부 데이터(실적/공시) 가 필요한 4 분류는 후속 PR 분리 진행. 자기학습 5계층 시리즈 완주 — 손실 원인 데이터가 누적되면 PR-E 귀인 분석(Alpha Driver / Risk Protector / Noise Factor / False Comfort) 의 입력으로 직접 활용 가능. |
| 2026-04-26 | PR-C AI/COMPUTED 차등 학습 가중치 (ADR-0020, 자기학습 5계층 확장 시리즈 3번): 사용자 요청 "진행". PR-A (데이터 무결성) + PR-B (RecommendationSnapshot lifecycle) 후속 — `feedbackLoopEngine` 이 모든 27조건을 동등하게 학습해 AI 환각으로 인한 학습 오염을 100% 그대로 받던 문제 해소. 27조건은 실제로는 두 가지 신뢰도 등급(REAL_DATA_CONDITIONS 9개 = COMPUTED 가격/지표 결정적 데이터, AI_ESTIMATE_CONDITIONS 18개 = Gemini 해석 추정값)으로 구성되지만 기존 `WEIGHT_STEP=0.10` 가 출처와 무관하게 일괄 적용되어 AI 가 "촉매제 9점" 으로 추정한 거래가 우연히 수익 났을 때 COMPUTED 거래와 동일한 학습 영향력을 갖는 구조적 결함. ① **신규 SSOT** `src/services/quant/sourceWeighting.ts` (62줄, @responsibility SRP 통과) — `SOURCE_LEARNING_MULTIPLIER: { COMPUTED: 1.0, AI: 0.4 }` (AI 환각 보정 60% 감속) + `getSourceMultiplier(conditionId, overrideSource?)` (trade-level override 우선, SSOT fallback) + `resolveSource(conditionId, overrideSource?)` + `isSourceWeightingDisabled()` (LEARNING_SOURCE_WEIGHTING_DISABLED env 긴급 롤백 스위치, 브라우저 환경 process 미정의 시 자동 disable 안 됨). 알 수 없는 source 는 안전하게 1.0 fallback (clamp 0.5~1.5 가 폭주 차단). ② **feedbackLoopEngine 수정** — `WEIGHT_STEP=0.10` 일괄 적용을 `effectiveStep = WEIGHT_STEP × sourceMultiplier` 로 교체. relevant trades 의 conditionSources 다수결로 trade-level source 결정 (PR-A v2 레코드만 보유), 부재 시 (v1 레코드) 글로벌 SSOT fallback. 동일 60% 승률 시 가중치 변화량 — COMPUTED 조건: ±10% (1.0→1.10) / **AI 조건: ±4% (1.0→1.04)**. AI 조건이 학습에 반영되려면 더 많은 표본이 필요해 환각 위험 자연 보정. ③ **ConditionCalibration 결과 확장** (`src/types/portfolio.ts`) — 옵셔널 2 필드 추가: `source?: 'COMPUTED' \| 'AI'` + `sourceMultiplier?: number`. UI 가 ±X% 표기 + source 별 색상 분기 가능 (실제 UI 변경은 후속 PR). ④ **회귀 테스트 19 케이스 신규** — `sourceWeighting.test.ts` 9 (SSOT 정합성: COMPUTED=1.0/AI=0.4 + REAL_DATA 9개 + AI_ESTIMATE 18개 / getSourceMultiplier: REAL_DATA→1.0 / AI_ESTIMATE→0.4 / override 우선 양방향 / DISABLED env true→1.0 / DISABLED 기본=차등 / 알 수 없는 ID fallback / resolveSource 2분기) + `quantEngine.feedbackLoop.sourceWeighted.test.ts` 10 (COMPUTED 25=VCP +10% / AI 1=주도주 +4% / AI -4% / COMPUTED -10% / DISABLED 롤백 동작 / 50% boundary STABLE / clamp 0.5~1.5 유지 (AI 0.55→0.51 / clamp boundary 0.52→0.50) / trade.conditionSources override (AI 조건이라도 COMPUTED 면 +10%) / v1 레코드 SSOT fallback). ⑤ **기존 18 feedbackLoop 회귀 정합 업데이트** (`quantEngine.feedbackLoop.test.ts`) — "조건 1의 승률이 100%이면 가중치 +10%" 가 ADR-0020 후 +4% 가 정답이라 (AI 분류) 테스트 어구를 "AI 조건 (1=주도주 사이클) 100% 승률 → +4% (1.0 → 1.04, ADR-0020)" 로 정정. 조건 2 (모멘텀) 는 COMPUTED 분류라 -10% 그대로 (테스트 어구만 명문화). 18/18 통과 (1건 정정 + 17건 무회귀). ⑥ **검증**: vitest **42 files 613/613 pass** (신규 19 + 기존 정합 1 + 기존 무회귀 593) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). ⑦ **결과** — AI 추정 조건이 학습에 반영되는 속도가 60% 감속되어 AI 환각이 학습을 오염시키는 위험 자연 보정. 본 PR 이전 누적 가중치는 그대로 유지 (clamp 0.5~1.5 내). PR-D (손실 원인 태그 + 자동 분류) 후속 분리 진행. | `docs/adr/0020-source-weighted-learning.md` (신규), `src/services/quant/sourceWeighting.ts` (신규 62줄), `src/services/quant/sourceWeighting.test.ts` (신규 9 케이스), `src/services/quant/feedbackLoopEngine.ts` (effectiveStep 도입 + trade.conditionSources fallback), `src/services/quantEngine.feedbackLoop.sourceWeighted.test.ts` (신규 10 케이스), `src/services/quantEngine.feedbackLoop.test.ts` (기존 정합 업데이트), `src/types/portfolio.ts` (ConditionCalibration +source/sourceMultiplier 옵셔널) | 사용자 분석 보완점 2 — "AI 추정 조건의 학습 반영률을 낮춰야 함 → COMPUTED 100% / AI 40%" 직접 구현. 5계층 확장 시리즈 3번. 가장 작은 변경(SSOT 1개 + feedbackLoop 5줄 수정 + ConditionCalibration 2 옵셔널 필드)으로 최대 효과 — AI 환각으로 학습 가중치가 0.5~1.5 범위 끝까지 폭주하던 잠재적 위험을 60% 감속으로 자연 보정. |
| 2026-04-26 | PR-B 추천 스냅샷 lifecycle 영속 레이어 (ADR-0019, 자기학습 5계층 확장 시리즈 2번): 사용자 요청 "순서대로". PR-A (데이터 무결성) 후속 — 사용자가 받은 AI 추천이 실제로 얼마나 적중했는지 정량화할 SSOT 가 클라이언트에 부재했던 문제 해소. 기존 자산 audit 결과: `useRecommendationStore.recommendationHistory` 는 30개 limit lightweight 이력(date+stocks+hitRate)이지 lifecycle 추적용 SSOT 가 아니고, `server/learning/recommendationTracker` 는 SHADOW 자동매매 신호 평가용(fetchCurrentPrice 자동 판정)이라 사용자 행동(매수/매도)과 무관함을 확인. 부재한 것: (a) 추천 발령 시점부터 사용자 행동을 따라가는 lifecycle SSOT, (b) 사용자가 시스템 추천을 따랐을 때 종단 적중률, (c) 추천을 받았지만 매수 안 한 종목(Missed Opportunity / 회피 정확도) 추적. ① **새 schema** `RecommendationSnapshot` (`src/types/portfolio.ts`) — id/recommendedAt/stockCode/stockName/recommendation('BUY'\|'STRONG_BUY'\|'STRONG_SELL'\|'SELL'\|'NEUTRAL')/entryPrice/targetPrice/stopLossPrice/rrr/conditionScores/conditionSources(PR-A 산출물 재사용)/gate1~3Score/finalScore/confluence/sector/status('PENDING'\|'OPEN'\|'CLOSED'\|'EXPIRED')/openedAt/closedAt/expiredAt/tradeId/realizedReturnPct/schemaVersion. + `SnapshotStats` 인터페이스 (totalCount/pendingCount/openCount/closedCount/expiredCount/hitRate/strongBuyHitRate/buyHitRate/avgReturnClosed/adoptionRate). + `TradeRecord.recommendationSnapshotId?` 옵셔널 양방향 링크. ② **State machine** (`src/services/quant/recommendationSnapshotRepo.ts` 신규 256줄, @responsibility SRP 통과) — `canTransition(from, to)` SSOT: PENDING→OPEN/EXPIRED, OPEN→CLOSED, CLOSED/EXPIRED 는 terminal(전이 불가). `buildSnapshotFromRecommendation(stock, now?)` (StockRecommendation → snapshot, PR-A 의 checklistToConditionScores+approximateGateScores 호출). `captureSnapshot(existing, candidate)` (동일 stockCode PENDING/OPEN 있으면 idempotent skip, FIFO trim 1000건). `captureSnapshots(existing, stocks[])` 다건 일괄. `markSnapshotOpen(existing, stockCode, tradeId, now?)` (PENDING→OPEN+tradeId 연결). `markSnapshotClosed(existing, tradeId, returnPct, now?)` (OPEN→CLOSED+realizedReturnPct, NaN/Infinity→0 fallback). `expireStaleSnapshots(existing, now?, expiryMs?)` (30일 경과 PENDING→EXPIRED 일괄, OPEN/CLOSED 는 만료 대상 아님 — 사용자 행동 추적 중). `computeSnapshotStats(snapshots)` (lifecycle 카운트 + hitRate + 등급별 hitRate + adoptionRate). `getRecentSnapshots(snapshots, limit)`. ③ **zustand persist store** `src/stores/useRecommendationSnapshotStore.ts` 신규 — `captureFromRecommendations` / `markOpen` / `markClosed` / `expireStale` / `getStats` / `getRecent` / `__resetForTests` 7 액션. localStorage 키 `k-stock-recommendation-snapshots-store`, partialize 로 snapshots 만 영속. ④ **Wiring**: (a) `useStockSearch.fetchStocks` 완료 후 `expireStale()` + `captureFromRecommendations(diversified)` 호출 — 추천 발령 즉시 PENDING snapshot 생성, (b) `useTradeOps.recordTrade` 가 `markOpen(stock.code, tradeId)` 호출 후 linkedSnapshot.id 를 `TradeRecord.recommendationSnapshotId` 로 저장 — 양방향 추적, (c) `useTradeOps.closeTrade` 가 `markClosed(tradeId, returnPctClose)` 호출. 매수 시점에 snapshot 이 없었으면 markOpen/markClosed 모두 no-op (수동 매수 호환). ⑤ **외부 효과 격리** — 서버 `recommendationTracker` (SHADOW 자동매매 신호) 와 별개 모듈로 운영, 기존 `recommendationHistory` 30개 lightweight 이력 호환 유지, LIVE 자동매매 무수정. ⑥ **회귀 테스트 25 케이스 신규** (`recommendationSnapshotRepo.test.ts`) — canTransition 3 (PENDING/OPEN/terminal 5분기) + buildSnapshotFromRecommendation 3 (정상 매핑 / type 분기 / target/stop 0 → rrr undefined) + captureSnapshot 5 (빈 배열/PENDING 중복 방지/OPEN 중복 방지/CLOSED 후 재추천 허용/FIFO 1000건 trim) + markSnapshotOpen 3 (PENDING→OPEN/매칭 없음 no-op/OPEN 중복 무시) + markSnapshotClosed 3 (OPEN→CLOSED/NaN fallback/PENDING 직접 CLOSED 차단) + expireStaleSnapshots 3 (30일 PENDING→EXPIRED/OPEN/CLOSED 만료 제외/변경 없으면 동일 참조) + computeSnapshotStats 3 (빈/lifecycle 분류/등급별 hitRate) + getRecentSnapshots 1 (정렬+limit) + captureSnapshots 1 (다건 dedupe). ⑦ **검증**: vitest **40 files 593/593 pass** (신규 25 + 기존 568 무회귀) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). PR-C (AI/COMPUTED 차등 학습 가중치) / PR-D (손실 원인 태그) 후속 분리 진행. | `docs/adr/0019-recommendation-snapshot-lifecycle.md` (신규), `src/types/portfolio.ts` (+RecommendationSnapshot/SnapshotStats 타입 + TradeRecord.recommendationSnapshotId? 옵셔널), `src/services/quant/recommendationSnapshotRepo.ts` (신규 256줄), `src/services/quant/recommendationSnapshotRepo.test.ts` (신규 25 케이스), `src/stores/useRecommendationSnapshotStore.ts` (신규 zustand persist), `src/stores/index.ts` (+barrel export), `src/hooks/useStockSearch.ts` (fetchStocks 후 expireStale + captureFromRecommendations wiring), `src/hooks/useTradeOps.ts` (recordTrade markOpen + linkedSnapshot.id 저장 / closeTrade markClosed) | 사용자 분석 후속 — 자기학습 5계층 아키텍처 4개 PR 시리즈 2번. PR-A (데이터 무결성) 위에 사용자 노출 추천 lifecycle SSOT 를 올려 추천 적중률(hitRate) / 채택률(adoptionRate) / 회피 정확도(EXPIRED 관찰) 정량화의 기반을 마련. PR-A 의 checklistToConditionScores+approximateGateScores 를 그대로 재사용해 27조건+Gate 점수가 추천 시점에 무손실 영속. UI 적중률 패널은 표본 충분 도달 후 별도 PR. |
| 2026-04-26 | PR-55 역산갭 GDR sanity 가드 Phase 3 — `safePctChange` 잔여 코드베이스 일괄 wiring (ADR-0028 §Phase 3, 사용자 "나머지 적용"): PR-53 (Phase 1, 4 경로) + PR-54 (Phase 2, 10 경로) 후속 — 코드베이스 전반의 `((current - base) / base) * 100` 패턴 잔여 호출자 일괄 적용. 78 식별 위치 중 stale base price 위험이 있는 모든 경로 SSOT 통합. ① **클라이언트 헬퍼 사본** `src/utils/safePctChange.ts` 신규 (66줄, 서버 사본 동기 — 절대 규칙 #3 서버↔클라 직접 import 금지 준수). 5종 가드 + 60s throttle + label 옵션 + isSanePct 후처리 헬퍼. ② **서버 잔여 25 경로 wiring** — *영속/학습 입력*: `fillMonitor.fill.pnlPct` (체결가 영속), `ledgerSimulator.returnPct` (학습 ledger 3 분기), `lateWinEvaluator.return60d/90d/actualReturn` (3 위치, 학습 영속), `counterfactualShadow.ret`, `coldstartBootstrap.returnPct` (snapshot 영속), `ocoConfirmLoop.pnlPct`, `ocoCloseLoop.pnlPct` (2 위치, replaceAll), `portfolioRiskEngine.pnlPct` (섹터 약체 정렬 입력), *의사결정*: `entryEngine.extensionPct + dropFromOpenPct + openGapPct` (3 위치, 진입 재검증), `exitEngine.distToStop` (손절 접근 알림), `trancheExecutor.dropPct` (트랜치 재검증), `regretAsymmetryFilter.pullbackPct` (FOMO 차단 정책 우선 — null 시 쿨다운 해제 보류), *macro/리포트*: `reportGenerator.gapPct + returnPct + KOSPI/USDKRW changePct + posLine ret` (5 위치), `positionMorningCard.currentReturnPct` (UI null 'N/A'), `dartPoller.changePct` (악재 소화 평가), `ecosClient.m2/export/bankLending yoy` (3 위치, BoK 매크로), `dxyIntradayClient.change`, `preMarketSignal.changePct`, `globalScanAgent.changePct + return5d` (2 위치), `stockScreener.changePercent`, `koreanQuoteBridge.changePct`, `macroSectorSync.vixIntradayChangePct`, `pnl.cmd.unrealizedPct` (텔레그램 UI). ③ **클라이언트 17 경로 wiring** — `useTradeOps.closeTrade returnPct + snapshot returnPctClose + attribution returnPct` (3 위치, TradeRecord 영속 학습 입력), `useTradeStore.closeTrade returnPct` (영속 store), `dynamicStopEngine.stopPct + currentReturnPct + trailingStopPct` (3 위치, 동적 손절 입력), `autoTradeEngine.pricePct` (눌림목 매수), `shadowTrading.returnPct` (2 위치, replaceAll, Shadow trade 영속), `backtestService.returns + totalReturn` (2 위치, 백테스트 결과 — sanity 1000% override), `ecosService.m2/export/bankLending yoy` (3 위치, 클라 매크로), `roeEngine.assetTurnoverDropPct`, `ROETransitionPanel.dropPct` (UI). ④ **스킵 정책** — 적용하지 않은 경로: 정규화(`(v-min)/(max-min)*100` 0~100 범위 보장) / 거리 비율(분모 currentPrice 라 stale 위험 적음, `priceAlertLevel.distancePct`/`weeklyDeepAnalysis.toEntry-toStop-toTarget` 등) / 기술 지표(`indicators.stochasticK / returns 배열`) / 슬리피지(체결가 비교) / `dartDataFetcher.epsGrowth` (Math.abs 분모 + 음수 분자 가능 — 헬퍼 분자 가드 부적합). 14 위치는 sanity 적용 부적합. ⑤ **검증**: vitest **208 files 2192/2192 pass** (서버 1302 + 클라 890 무회귀, 신규 wiring 42 경로 모두 헬퍼 동작 정합) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 7종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범. ⑥ **결과 — ADR-0028 코드베이스 전반 적용 완료** (Phase 1: 4 + Phase 2: 10 + Phase 3 server: 25 + Phase 3 client: 17 = **총 56 경로**). 사용자 보고 "-90% 갭 패턴" 시나리오는 *학습 영속화 입력 / LIVE 의사결정 / UI 표기 / 매크로 지표 / 백테스트 / 동적 손절* 모든 영역에서 단일 SSOT (`safePctChange`) 통과. **새 호출자 추가 시 자동으로 5종 가드 통과** — 향후 코드베이스가 진화해도 ADR-0028 가드가 일관되게 적용된다. LIVE 매매 본체 0줄 변경 — 안전 layer 만 추가. | `src/utils/safePctChange.ts` (신규 클라 사본 66줄), 서버 25개 + 클라 11개 = 36개 파일 수정, ADR-0028 적용 호출자 +42개 (서버 25 + 클라 17) | 사용자 요청 "나머지 적용". PR-53/54 의 Phase 1/2 가 14 핵심 경로만 적용했고 Phase 3 잔여 64 경로 중 stale 위험 있는 42 경로 모두 일괄 wiring. 정규화/거리 비율/기술 지표/슬리피지 14 경로는 sanity 부적합으로 명시 스킵. **사용자 보고 -90% 갭 패턴이 코드베이스 모든 의미있는 호출자에서 종결됨**. |
| 2026-04-26 | PR-54 역산갭 GDR sanity 가드 Phase 2 — `safePctChange` 10 경로 일괄 wiring (ADR-0028 §Phase 2): PR-53 의 후속 — Phase 1 (4 경로) 위에 잔여 10 경로 점진 적용 완료. 사용자 요청 "진행". ① **Critical/High 4 경로** — (a) `exitEngine._updateShadowResultsImpl returnPct` (청산 결정 본체. stale currentPrice 시 트레일링/익절/손절 트리거 오작동 차단을 위해 null 시 `continue` 로 평가 자체 스킵) (b) `tradeReplacement.evaluateReplacement returnPct` (포지션 교체 평가. null 시 교체 평가 스킵 — 잘못된 returnPct 로 보유 종목 임의 교체 차단) (c) `watchlistManager.applyEntryPriceDrift driftPct` (워치리스트 entry drift 판정. stale entryPrice (거래정지/액면분할 후 거대 괴리) 시 null → `KEEP` 안전 fallback — 잘못된 REMOVE/UPDATE 차단) (d) `manualExitContext.computeBiasAssessment returnPct` (수동 청산 편향 추정 학습 입력. null 시 0 fallback 으로 학습 오염 차단). ② **Medium/Low 6 경로** — (e) `sell.cmd returnPct` (텔레그램 /sell 명령 응답 + fill.pnlPct 영속화. null 시 0 fallback) (f) `sectorEnergyProvider returnPct + volumePct` (섹터 에너지 평균. returnPct null 시 `continue` 로 종목 제외 / volumePct 는 sanityBoundPct=1000 override 로 거래량 폭발 정상 허용) (g) `sectorEtfMomentum.pctChange` (헬퍼로 본체 일원화 — 분모 가드 + sanity 자동) (h) `miniEvaluate.miniEvaluateSingle returnPct` (PENDING 추천 즉시 평가. EXPIRED 시점 actualReturn 박제 차단을 위해 returnPct null 시 EXPIRED 분기 보류 — 다음 평가 사이클 재검사) (i) `stockScreener.return5d/return20d` (5/20일 수익률. close5dAgo/close20dAgo 가 stale 일 때 null → 0 fallback, Gate 24 상대강도 입력 보호) (j) `killSwitch.getVkospiSurgePct` (VKOSPI 변화율. 동일일자 데이터라 stale 위험 적지만 데이터 오류·스토리지 손상 시 비상정지 트리거 오작동 차단을 위해 sanity ±90% 적용). ③ **호출자 책임 일관 정착** — null 반환 분기 정책 4종: (1) **continue/skip** (exitEngine, tradeReplacement, sectorEnergyProvider) — 잘못된 의사결정 차단 우선. (2) **0 fallback** (manualExitContext, sell.cmd, stockScreener, killSwitch) — 학습/UI 표시 입력으로 안전 처리. (3) **KEEP** (watchlistManager) — 영속 상태 변경 차단 (현상 유지). (4) **다음 사이클 보류** (miniEvaluate) — 영속 분기를 미루고 재시도. ④ **검증**: vitest server/trading + server/alerts + server/utils + server/screener + server/clients + server/learning + server/telegram **89 files 887/887 pass** (Phase 1 442 + Phase 2 추가 wiring 무회귀 445) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 7종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범. ⑤ **결과 — ADR-0028 코드베이스 전반 적용 완료**: 78곳 패턴 중 14 경로 (Phase 1: 4 + Phase 2: 10) 가 단일 SSOT (`safePctChange`) 통과. 잔여 64 경로는 *체결가 비교 / 동일 시점 데이터 / 단순 표기* 등 stale base price 위험이 구조적으로 적은 경로로, `validate:responsibility` 에 본 헬퍼 사용 검증 추가 시점에 점진 강제 가능. **사용자 보고 -90% 갭 패턴 시나리오는 코드베이스에서 종결**. LIVE 매매 본체 0줄 변경 — 안전 layer 만 추가 (호출자 분기 정책으로 행위 보존). | `server/trading/exitEngine.ts` (returnPct null 시 continue), `server/trading/tradeReplacement.ts` (returnPct null 시 평가 스킵), `server/screener/watchlistManager.ts` (driftPct null 시 KEEP), `server/trading/manualExitContext.ts` (returnPct null 시 0 fallback), `server/telegram/commands/trade/sell.cmd.ts` (returnPct null 시 0 fallback), `server/clients/sectorEnergyProvider.ts` (returnPct null 시 continue + volumePct sanityBoundPct=1000), `server/alerts/sectorEtfMomentum.ts` (pctChange 본체 헬퍼 일원화), `server/learning/miniEvaluate.ts` (EXPIRED 분기 returnPct null 시 보류), `server/screener/stockScreener.ts` (return5d/return20d null 시 0 fallback), `server/trading/killSwitch.ts` (vkospiSurgePct sanity ±90%) | 사용자 요청 원문 "진행". PR-53 의 Phase 2 잔여 항목 일괄 처리 — 단일 SSOT 헬퍼가 코드베이스 전반에 적용되어 새 호출자 추가 시 자동으로 5종 가드 통과. 사용자 보고 "-90% 갭" 패턴이 구조적으로 종결 — 자동매매 LIVE 결정 + 학습 데이터 품질 + UI 표기 + 비상정지 트리거가 모두 일관된 sanity bound (±90%) 통과. |
| 2026-04-26 | PR-53 역산갭 GDR sanity 가드 — `safePctChange` 단일 안전 헬퍼 도입 (ADR-0028): 사용자 보고 "역산갭 GDR 오류 발생 가능성 + 안전한 계산 적용 — 과거 데이터가 기준이 되어 -90% 가 넘는 상황도 있었음." ADR-0004 의 PKX/SSNLF/SKM 상장폐지 ADR -93.69% 케이스가 *해당 경로 한정* 으로만 차단됐고, 코드베이스 전반의 `((current - base) / base) * 100` 패턴 **78곳** 에 분산된 호출자가 sanity bound·stale check·NaN/Infinity 가드를 *제각기 (또는 부재)* 적용하던 일관성 결함 해소. ① **`server/utils/safePctChange.ts` 신설** (66줄, @responsibility SRP 통과) — 5종 가드 일괄 적용: 분모(`base ≤ 0`/NaN/Infinity → null) + 분자(`current < 0`/NaN/Infinity → null) + 결과 NaN/Infinity → null + Sanity bound (default ±90%, 위반 시 null + console.warn 진단 로그) + 호출자 컨텍스트 `label?` 옵션. **0% fallback 금지** — null 반환으로 호출자가 명시적 분기하도록 강제 (기존 "stale 시 0% 정상 처리" silent degradation 차단). 진단 로그 60s throttle (운영 로그 폭주 방지) + label 별 분리. + 보조 `isSanePct(pct, bound?)` 후처리 검증 헬퍼. ② **Phase 1 wiring — 4 고위험 경로**: (a) `marketDataRefresh.nDayReturn` (KOSPI 매크로 — Critical, 망가지면 레짐 분류·자동매매 전체 영향) 새 헬퍼 사용 후 stale 시 0% 안전 fallback. (b) `riskManager.checkEuphoria` 의 returnPct + spike7d 두 곳 — 가짜 익절 트리거 차단을 위해 null 시 분기 스킵. (c) `dxyMonitor.nDayPct` — DXY 외환시장 휴장·OTC 데이터 오류 시 sanity 위반 → null. (d) `preMarketGapProbe.gapPct` — 기존 30% SKIP_DATA_ERROR 가드와 일관된 SSOT 정착, sanity 위반 시 SKIP_DATA_ERROR 로 명시 분기 (silent=true 로 중복 로그 차단, 행위 변경 없음). ③ **회귀 테스트 29 케이스 신규** (`safePctChange.test.ts`) — safePctChange 22 (정상 케이스 +5%/-3% / 분모 가드 4 (0/음수/NaN/Infinity) / 분자 가드 3 (음수/NaN/Infinity) / current=0 sanity 분기 2 / sanity 위반 -93.69% (PKX 케이스) + +500% / 경계 ±90% / 50% override / 진단 로그 label 포함 / throttle 60s 단일 label / 다중 label 분리 / silent 옵션 / unknown label / stale 동일값 0%) + isSanePct 7 (정상 +5%/-89% / 경계 ±90% / 90.01% 초과 / NaN / Infinity / override 50%). ④ **Phase 2 후속 PR 분리** — `tradeReplacement.returnPct` / `exitEngine.returnPct` / `sell.cmd.returnPct` / `manualExitContext.returnPct` / `sectorEnergyProvider.returnPct` / `watchlistManager.driftPct` / `miniEvaluate.returnPct` / `stockScreener.return5d/20d` / `sectorEtfMomentum` / `killSwitch.vkospiPct` 등 9 경로 — `validate:responsibility` 에 본 헬퍼 사용 검증 추가 후 점진 적용. ⑤ **검증**: vitest server/trading + server/alerts + server/utils **41 files 442/442 pass** (신규 29 + 기존 413 무회귀) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 7종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 본체 0줄 변경 — 안전 layer 만 추가. | `docs/adr/0028-safe-pct-change-helper.md` (신규), `server/utils/safePctChange.ts` (신규 66줄), `server/utils/safePctChange.test.ts` (신규 29 케이스), `server/trading/marketDataRefresh.ts` (nDayReturn safePctChange 사용 + stale 0% fallback), `server/trading/riskManager.ts` (checkEuphoria returnPct + spike7d safePctChange + null 분기), `server/alerts/dxyMonitor.ts` (nDayPct safePctChange 사용), `server/trading/preMarketGapProbe.ts` (gapPct safePctChange 사용 + sanity 위반 SKIP_DATA_ERROR) | 사용자 요청 원문 "역산갭 GDR 오류 발생 가능성에 대해서 확인 + 안전한 계산 적용 (과거 데이터가 기준이 되서 -90%가 넘는 상황도 있었음)". ADR-0004 가 *Yahoo ADR 역산 폐기* 라는 단일 경로만 차단했지만 동일 패턴 (stale base price + sanity bound 부재) 이 78곳에 잠재. 본 PR 은 단일 SSOT (safePctChange) 도입 + 4 최우선 경로 wiring 으로 *코드베이스 전반의 stale degradation 패턴* 을 한 점에서 차단. Phase 2 후속 PR 9 경로 점진 적용 시 모든 % 변화율 계산이 일관된 5종 가드 통과. |
| 2026-04-26 | PR-52 자동매매 루프 정밀 audit + H1 즉시 수정 — `trancheExecutor.checkPendingTranches()` `AUTO_TRADE_ENABLED` 가드 누락 차단: 사용자 요청 "자동매매 루프 정밀확인 + 발생할 수 있는 에러 경우의 수 확인·수정". PR-26~PR-51 누적 변경 후 자동매매 LIVE 회귀 위험 격리를 최우선으로 7가지 영역(스캔/게이트/사이징/청산/회로/환경변수/부팅 reconcile) 정밀 점검. 결과: **Critical 0건 / High 1건 / Medium 2건 / Pass 11항목**. ① **H1 발견** — `tradingOrchestrator.ts:423` (OPENING_AUCTION 핸들러) 와 `tranchesRouter.ts:21` (POST /auto-trade/tranches/run) 두 곳에서 `trancheExecutor.checkPendingTranches()` 가 `AUTO_TRADE_ENABLED` 가드 *밖* 에서 호출돼 분할 매수 2·3차 LIVE 실주문이 자동매매 일시정지 상태에서도 발송될 수 있던 잠재 위험. `runAutoSignalScan` / `preMarketOrderPrep` / `autoCancelAtClose` 는 모두 `if (enabled)` 안에서만 호출되는 일관된 패턴과 어긋나는 비대칭 결함. ② **수정 (심층 방어, Defense in Depth)** — (a) `trancheExecutor.checkPendingTranches()` 본체 진입부에 `process.env.AUTO_TRADE_ENABLED !== 'true'` 즉시 return + 진단 로그 추가 (단일 SSOT 방어층). PENDING 트랜치는 그대로 보존되어 enabled=true 복귀 시 재실행. (b) `tradingOrchestrator.ts:423` 호출을 `if (enabled)` 안으로 이동 (호출자 가드, 명확한 흐름 정합). (c) `tranchesRouter.ts:21` POST 핸들러에 `AUTO_TRADE_ENABLED !== 'true'` 시 403 응답 + 사유 반환 (운영자에게 명시적 피드백). ③ **Medium 후속 PR 분리** — M1: `preflight.runPreflight()` 외부 importer 0건 dead code (PR-40 의도된 Phase B 후속 상태, signalScanner.ts 와 byte-equivalent 매크로 게이트 본체 양쪽 병존 → drift 위험). M2: `kellyHalfLife.applyHalfLifeDecay` 가 `accountRiskBudget.computeRiskAdjustedSize` 안에 wire 됐지만 호출자 모두 신규 진입 경로(`timeDecayInput` 미전달)라 보유 중 재평가 wiring 미완성 (ADR-0008 의도). ④ **Pass 항목 11종** — signalScanner 분해(PR-40) dead code 외부 importer 0건 / preflight 단일 SSOT 정상 (PR-42 M3) / entryEngine `buildStopLossPlan` + `calculateOrderQuantity` 정상 / exitEngine `_exitRunning` mutex 정상 (PR-6) / exitEngine `reserveSell` 의 `emitPartialAttribution` wiring 정상 (PR-42 M1) / SHADOW PENDING→ACTIVE BUY fill 자동 백필 정상 (PR-7) / tradingOrchestrator 의 `runAutoSignalScan`/`preMarketOrderPrep`/`autoCancelAtClose` 모두 `if (enabled)` 안 / KIS 단일 통로 (kisClient.ts/kisStreamClient.ts) 외부 raw fetch 누수 0건 (PR-42 M2) / Yahoo guardedFetch 11 경로 모두 wiring (PR-29) / ocoRecoveryAgent 보호적 청산 패턴 정합 / bootReconcile setTimeout 30s wiring 정상 (PR-50). ⑤ **회귀 테스트 7 케이스 신규** — `trancheExecutorAutoTradeGuard.test.ts` 4 (enabled=false 즉시 return + loadTranches 미호출 / undefined 시 가드 작동 / KIS_APP_KEY 미설정 시 enabled 무관 return / enabled=true + KIS_APP_KEY 설정 시 가드 미진입) + `tranchesRouterGuard.test.ts` 3 (enabled=false 시 403 + checkPendingTranches 미호출 / undefined 시 403 / enabled=true 시 200 + 호출). ⑥ **검증**: vitest server/trading + server/orchestrator + server/routes **48 files 419/419 pass** (신규 7 + 기존 412 무회귀) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 준수 — kisClient/aiUniverseService/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — 안전 가드만 추가. | `_workspace/2026-04-26_audit-trading-loop-deep/findings.md` (신규 audit 보고서), `server/trading/trancheExecutor.ts` (+AUTO_TRADE_ENABLED 본체 가드 + 진단 로그), `server/trading/trancheExecutorAutoTradeGuard.test.ts` (신규 4 케이스), `server/orchestrator/tradingOrchestrator.ts` (trancheExecutor 호출 if(enabled) 안으로 이동), `server/routes/autoTrade/tranchesRouter.ts` (POST run 핸들러 403 가드 추가), `server/routes/autoTrade/tranchesRouterGuard.test.ts` (신규 3 케이스) | 사용자 요청 원문 "자동매매 루프 정밀확인 + 발생할 수 있는 에러 경우의 수 확인·수정". 자동매매 LIVE 회귀 위험 격리가 최우선이라는 audit 우선순위 명시. PR-41 audit 후속 정합 — 그 때 수정한 dryRunScanner 와 동일 패턴(SHADOW vs LIVE 분기 누수)이 trancheExecutor 에서 *호출자 enabled 가드 누락* 형태로 잔존했다. 이번 수정으로 *진입* 경로 4개 모두(scan / preMarket / tranche / fillCancel) `if (enabled)` 가드 안에서만 실행되는 일관된 패턴 정착. 운영자가 "자동매매 일시정지" 의 의도 — 신규/추가 진입 모두 차단 — 와 시스템 동작이 정합. |
| 2026-04-26 | PR-A 자기학습 데이터 파이프라인 무결성 (ADR-0018, 자기학습 5계층 확장 PR-A/B/C/D 시리즈 1번): 사용자 요청 "자기학습 시스템 5계층 확장 — 순서대로". audit 결과 critical 2건 확정 — (1) `TradeRecordModal.tsx:188` 가 `onRecordTrade` 호출 시 conditionScores 를 hardcoded `{}`, gate scores 를 `{ g1:0, g2:0, g3:0, final:0 }` 으로 전달해 모든 수동 매수 기록의 27조건 점수가 garbage 로 저장되던 구조적 결함, (2) `TradeRecord` schema 에 `conditionSources` 필드 자체가 부재해 PR-C 의 AI/COMPUTED 차등 학습 전제 데이터가 영구 손실. 결과적으로 `feedbackLoopEngine.evaluateFeedbackLoop` 의 `conditionScores?.[id] ?? 0 >= 5` 필터가 영원히 통과 안 되어 `FeedbackLoopPanel` 이 "30/30거래 달성 — 자동 교정 활성화" 배지를 띄워도 calibrations[] 가 빈 배열이 되어 자기학습 루프가 **관측 가능한 비활성** 상태에 있던 문제 해소. ① **TradeRecord schema v2** (`src/types/portfolio.ts`) — 옵셔널 후방호환 3 필드 추가: `conditionSources?: Record<ConditionId, 'COMPUTED' \| 'AI'>` + `evaluationSnapshot?: { capturedAt; rrr?; profile?; confluence?; lastTrigger? }` + `schemaVersion?: number`. 모두 옵셔널이라 v1 레코드 호환. ② **신규 adapter** `src/services/quant/checklistToConditionScores.ts` — `StockRecommendation.checklist` 의 27 named field → `Record<ConditionId, number>` 단방향 매핑 SSOT (`CHECKLIST_TO_CONDITION_ID` const) + `getConditionSources()` (CONDITION_SOURCE_MAP defensive copy) + `approximateGateScores()` (5점 이상 통과 조건 × 5점 = Gate score, evaluateStock 재호출 없이 추천 시점 데이터로 근사). `GATE1_CONDITION_IDS=[1,2,3,5,7,9]` (6개) `GATE2_CONDITION_IDS=[4,6,8,10,11,12,13,14,15,16,21,24]` (12개) `GATE3_CONDITION_IDS=[17,18,19,20,22,23,25,26,27]` (9개) — 27 ID 모두 정확히 1회 분배. undefined/NaN/음수 → 0 fallback. ③ **TradeRecordModal 보강** — `onRecordTrade` 시그니처에 `conditionSources?` + `evaluationSnapshot?` 옵셔널 추가, 매수 저장 버튼 클릭 시 `checklistToConditionScores(stock.checklist)` 호출 → `approximateGateScores(scores)` → `evaluationSnapshot = { capturedAt, rrr=(target-bp)/(bp-stop), confluence=stock.confidenceScore }` 합성 후 전달. 기존 `{}` + 0 garbage 입력 완전 제거. ④ **useTradeOps.recordTrade** — 시그니처에 `conditionSources?` + `evaluationSnapshot?` 옵셔널 추가, 저장 시 `schemaVersion: 2` 명시. ⑤ **useTradeStore.sanitizeTradeRecord** — `schemaVersion` 미설정 v1 레코드는 자동 `schemaVersion=1` 부여로 마이그레이션, v2 신규 필드(`conditionSources`/`evaluationSnapshot`) 보존. v1 레코드의 conditionScores 가 빈 객체이면 학습 필터에서 자연스럽게 배제되어 garbage 데이터로 학습 오염 방지. ⑥ **DiscoverWatchlistPage** props 시그니처 정리 — 옵셔널 2 필드 propagation. ⑦ 회귀 테스트 17 케이스 신규 — `checklistToConditionScores.test.ts` 11 (CHECKLIST_TO_CONDITION_ID 매핑 무결성 / 27 필드 변환 / undefined/NaN/음수 fallback / 빈 객체 / undefined checklist / getConditionSources 27 ID 분류 + defensive copy / approximateGateScores 27 ID 분배 + 통과 합산 + 전 0 + 전 만점) + `useTradeStore.sanitize.test.ts` 3 (v1 hydrate / v2 신규 필드 보존 / 후방호환 옵셔널) + `quantEngine.feedbackLoop.adapter.test.ts` 3 (e2e — adapter 변환 후 학습 진입 / 빈 checklist 자동 배제 / 일부 조건만 학습 진입). ⑧ 검증: vitest **39 files 568/568 pass** (신규 17 + 기존 551 무회귀) + lint(client + server tsc) + validate:all (DeployWindow/PRES/ACMA/SDS/PRES/SymbolBoundary/Gemini 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정, LIVE 매매 경로 무영향). PR-B (RecommendationSnapshot 영속) / PR-C (AI/COMPUTED 차등 학습) / PR-D (손실 원인 태그) 후속 분리 진행. | `docs/adr/0018-self-learning-data-integrity.md` (신규), `src/types/portfolio.ts` (TradeRecord v2 +3 옵셔널 필드), `src/services/quant/checklistToConditionScores.ts` (신규 119줄), `src/services/quant/checklistToConditionScores.test.ts` (신규 11 케이스), `src/services/quantEngine.feedbackLoop.adapter.test.ts` (신규 3 케이스), `src/stores/useTradeStore.ts` (sanitizeTradeRecord v1 마이그레이션), `src/stores/useTradeStore.sanitize.test.ts` (신규 3 케이스), `src/hooks/useTradeOps.ts` (recordTrade 시그니처 확장), `src/components/trading/TradeRecordModal.tsx` (checklist 변환 wiring), `src/pages/DiscoverWatchlistPage.tsx` (props 시그니처 정리) | 사용자 분석 후속 — 자기학습 5계층 아키텍처 4개 PR 시리즈 시작. PR-A 는 학습 데이터 품질 통제의 전제 조건 (1계층 데이터 신뢰도 + 2계층 거래 기록 무결성). 본 PR 이후 새로 저장되는 매수 기록부터 27조건 점수와 출처가 무손실 저장되어 feedbackLoopEngine 이 30거래 누적 후 실제 학습을 시작할 수 있다. v1 레코드는 schemaVersion=1 자동 부여로 학습에서 자연 배제되어 garbage 데이터 오염 차단. |
| 2026-04-25 | PR-43 텔레그램 명령어 메뉴 압축 + 메타 명령어 6종 도입 — Stage 1 (ADR-0017): 사용자 요청 "텔레그램 리팩토링 3-Stage Plan — Stage 1 메뉴 8개 압축 + 메타 명령어 도입". ① **새 모듈** `server/telegram/metaCommands.ts` (319줄, @responsibility 25단어 이내 SRP 통과) — `META_COMMAND_REGISTRY` (5 메타 spec: /watch /positions /learning /control /admin) + `composeNowVerdict()` 5단계 우선순위 (🔴 STOP > 🔴 BLOCK > 🟡 PAUSE > 🟡 HOLD(R6_DEFENSE) > 🟢 OK) + `handleMetaCommand(name, reply)` 위임 진입점 + `buildMetaInlineKeyboard(spec, nonce)` (모바일 UX 가드 — 행당 최대 3 버튼) + `parseMetaCallback(data)` 순수 파서 (`meta:<cmd>:<nonce>` 디코드, 영숫자 cmd 검증, non-meta prefix→null) + `encodeMetaCallback`/`buildNowKeyboard`/`buildHelpMessage`. KIS 호출 0건 — 외부 SSOT (state/macroStateRepo/shadowTradeRepo/orchestrator/scanner getLastBuySignalAt) read-only. ② **webhookHandler.ts wiring** (1858줄 동일 — /help -58 + 메타 case +33 + callback router +25 = 0 net): metaCommands import 4종 + `META_RECURSIVE_FLAG` sentinel (callback→text 재진입 1단계만 허용 무한 루프 차단) + 4번째 callback 핸들러 (buyApproval/T1Ack/operatorOverride 뒤) — `parseMetaCallback` 매칭 시 `answerCallbackQuery(callbackQueryId, '<cmd> 실행 중...')` + 합성 req body (`{ chat: { id: cbChatId }, text: targetCmd }`) 로 `handleTelegramWebhook` 재호출 (dummyRes.sendStatus=noop, 카탈로그 51개 alias 자동 도달) + 6 신규 case `/now /watch /positions /learning /control /admin` 한 줄 위임 (`await handleMetaCommand(cmd.toLowerCase(), reply)`) + `reply` 시그니처에 `replyMarkup?: InlineKeyboardMarkup` 옵셔널 추가 (`unknown` cast 로 sendTelegramAlert `Record<string, unknown>` 호환) + 기존 `/help` 본체 60줄 → `await reply(buildHelpMessage())` 1줄 압축. ③ **`telegramClient.ts setMyCommands` 43 → 8 압축** (-38 LoC, -81%): 노출 메뉴 = `/help /status /now /watch /positions /learning /control /admin` (메타 8개만). 기존 51 alias (`/watchlist /focus /pos /pnl /buy /sell /pause /stop /reset /learning_status /kelly /circuits ...`) 는 webhookHandler.ts switch case 100% 보존 — 파워유저는 직접 입력 가능, 신규 사용자는 메타 메뉴 인라인 키보드로 카테고리 탐색. ④ **인라인 키보드 매핑** (ADR-0017 §Stage 1): /watch → /watchlist /focus /add /remove /watchlist_channel (3행 5버튼) / /positions → /pos /pnl /pending /sell /cancel /adjust_qty /reconcile (3행 7버튼) / /learning → /learning_status /learning_history /kelly /kelly_surface /regime_coverage /ledger /counterfactual /risk /circuits /reset_circuits /ai_status (5행 11버튼) / /control → /pause /resume /stop /reset /integrity /refresh_token /scan /krx_scan /reconnect_ws (4행 9버튼) / /admin → /health /regime /market /scheduler /report /shadow /dxy /todaylog /channel_health /channel_stats /alert_history /alert_replay /digest_on /digest_off /digest_status /news_lag /buy /stage1_audit /channel_test (8행 18버튼). ⑤ **회귀 테스트 27 케이스 신규** (`metaCommands.test.ts`, vi.spyOn 으로 외부 SSOT stub) — parseMetaCallback 6 (valid/colon-separated/non-meta/missing nonce/invalid char/empty) + encodeMetaCallback 2 (strip slash + roundtrip) + buildMetaInlineKeyboard 4 (구조/3버튼 가드/registry mobile cap/meta prefix) + META_COMMAND_REGISTRY 2 (5 키 일치/alias dedupe) + composeNowVerdict 6 (5 우선순위 분기 + KST 마지막 신호 시각) + handleMetaCommand 4 (/now 합성/+/watch 키보드/4개 메타 비공란/unknown) + buildNowKeyboard 1 + buildHelpMessage 2 (8 메타 모두 노출 + alias 안내). ⑥ **ADR-0017 신규** (`docs/adr/0017-telegram-meta-commands-stage1.md`, 165 LoC) — Stage 1 결정+매핑+callback 정책+/now 합성 SSOT 명세, Stage 2 (commands/* 디렉토리 분해 + commandRegistry 자동 등록 + webhookHandler ≤50줄) Stage 3 (commandUsageRepo + 폐기 후보 주간 리포트 + /help 개인화 Top 5) 후속 명문화. ⑦ 검증: vitest server/telegram + server/alerts 6 files **62/62 pass** (신규 27 + 기존 35 무회귀) + lint + validate:all (gemini/complexity/sds/exposure/responsibility/boundary 6종) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 준수 — kisClient/orchestrator/signalScanner 무수정). | `docs/adr/0017-telegram-meta-commands-stage1.md` (신규), `server/telegram/metaCommands.ts` (신규 319줄), `server/telegram/metaCommands.test.ts` (신규 273줄, 27 케이스), `server/telegram/webhookHandler.ts` (callback 4번째 핸들러 +25줄 / reply Markup 옵셔널 +5줄 / 6 신규 case +8줄 / /help 본체 -58줄), `server/alerts/telegramClient.ts` (setMyCommands 43 → 8) | 사용자 요청 원문 "텔레그램 리팩토링 — Stage 1: setMyCommands 8개로 압축, 코드 무수정, 백엔드 핸들러 하위호환". 사용자 옵션 답변 — Stage 1 산출물 = 메뉴 + 신규 메타 6 핸들러 추가 / 기존 51 명령어 = 전부 alias 유지 / 회귀 테스트 = 신규 메타 + 인라인 키보드만. 사용자 인지 부담 43 → 8 (-81%) 즉시 가시화. /now 1줄 의사결정 (KIS 호출 0) 으로 "지금 매수해도 되나" 판단을 5단계 우선순위로 합성. webhookHandler.ts 분해(Stage 2)는 별도 PR — 본 PR 은 모듈 신설 + alias 보존 중심. |
| 2026-04-26 | PR-53 exitEngine.ts 분해 — `_updateShadowResultsImpl` 950줄 → 16개 rule 파일 + 6개 helper + orchestrator (ADR-0028, P2 복잡도 위반 정식 해소): 사용자 요청 "리팩토링 실시 — _updateShadowResultsImpl 의 15개 매도 규칙을 exitRules/ 디렉토리의 순수 함수로 분리". `server/trading/exitEngine.ts` 1,358 줄(950 줄짜리 단일 함수 + 헬퍼) 을 `server/trading/exitEngine/` 디렉토리로 책임 단위로 분해. 외부 importer 4개 (signalScanner/signalScanner/preflight/shadowResolverJob/atrIntegration test) 무수정 — barrel re-export 로 호환 유지. ① **타입 SSOT** `exitEngine/types.ts` (39줄) — `ExitContext` (shadow/currentPrice/returnPct/currentRegime/initialStopLoss/regimeStopLoss/hardStopLoss/resolvedNow) + `ExitRuleResult` (skipRest + hardStopLossUpdate?) + `ExitRule` async 시그니처 + `NO_OP` sentinel. ② **헬퍼 6종** (`helpers/`) — `attribution.ts` (PR-42 M1 emitPartialAttributionForSell 분리, 58줄) + `reserveSell.ts` (주문 접수 ≠ 체결 SSOT 130줄) + `rollbackFullClose.ts` (BUG #7 captureFullCloseSnapshot+rollbackFullCloseOnFailure 73줄) + `rsiSeries.ts` (Wilder RSI + detectBearishDivergence 41줄) + `ma60.ts` (isMA60Death + simpleMA + kstBusinessDateStr + fetchMaFromCloses 49줄) + `priceHistory.ts` (yahooSymbolCandidates + fetchPriceAndRsiHistory 32줄). ③ **청산 규칙 16종** (`rules/`) — atrDynamicStop(60줄, hardStopLoss 래칫 갱신) + r6EmergencyExit(59줄, R6 30%) + ma60DeathForceExit(94줄, 5영업일 만료 전량) + hardStopLoss(115줄, 고정/레짐/Profit Protection) + cascadeFinal(88줄, -25%/-30% 블랙리스트) + trailingPeakUpdate(16줄, L3-a) + trancheTakeProfitLimit(92줄, L3-b LIMIT) + trailingStop(76줄, L3-c) + legacyTakeProfit(70줄, TARGET_EXIT) + cascadeHalf(58줄, -15% 50%) + cascadeWarn(27줄, -7%) + rrrCollapseExit(82줄, 잔여 RRR<1.0) + bearishDivergenceExit(82줄, 다이버전스 30%) + ma60DeathWatch(36줄, 역배열 5영업일 스케줄) + stopApproachAlert(66줄, 3단계 dedupe) + euphoriaPartialExit(87줄, 과열 50%). 모든 규칙은 byte-equivalent 추출 — 알고리즘/메시지/priority/dedupeKey/channelSellSignal reason 0건 변경. ④ **오케스트레이터** `exitEngine/index.ts` (258줄) — `_exitRunning` 뮤텍스(PR-6 #12) + 진입부 setup(resolvedNow + backfillShadowBuyFills + captureSnapshotsForOpenTrades) + 루프(PENDING→ACTIVE 승격 + 잔량 동기화 + EXIT_RULES_IN_ORDER 16-rule 순회 + L1 학습 훅) + 후처리(requestImmediateRescan + learningOrchestrator.onShadowResolved). EXIT_RULES_IN_ORDER 우선순위 배열은 entryEngine.ts EXIT_RULE_PRIORITY_TABLE 과 일치. ⑤ **barrel re-export** — `exitEngine.ts` 1,358 → 18 줄 (-98.7%): `updateShadowResults` / `emitPartialAttributionForSell` / `detectBearishDivergence` / `isMA60Death` / `kstBusinessDateStr` / `ReserveSellResult` 6개 export 만 유지로 외부 importer 무중단. ⑥ **검증** — lint(client + server tsc) + validate:all 6종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary) + precommit 전체 통과 + vitest **208 files 2173/2173 pass** (server/trading 32 files 306/306 + 전체 무회귀). exitEngineMutex(PR-6) + exitEngineAttribution(PR-42 M1) + fullCloseRollback + atrIntegration 21/21 pass. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). LIVE 매매 본체 0줄 변경 — 부수효과 패턴 보존. ⑦ **결과** — CLAUDE.md "복잡도 위반" 표 P2 항목(exitEngine.ts 1,233 → 18 barrel) 정식 제거. ARCHITECTURE.md 에 boundary 11종 추가(exitEngine 디렉토리 + 6 helpers + rules/* + barrel rule). 새 청산 규칙 추가 = `rules/<name>.ts` 파일 1개 + `EXIT_RULES_IN_ORDER` 1줄 추가. 후속 PR: ExitDecision 순수 패턴 점진 전환(orchestrator 가 KIS/텔레그램/attribution 일괄 실행) — 본 PR 은 부수효과 패턴 유지로 byte-equivalent 추출에 집중. | `docs/adr/0028-exitEngine-decomposition.md` (신규), `server/trading/exitEngine.ts` (1,358 → 18 barrel), `server/trading/exitEngine/index.ts` (신규 258줄), `server/trading/exitEngine/types.ts` (신규 39줄), `server/trading/exitEngine/helpers/{attribution,reserveSell,rollbackFullClose,rsiSeries,ma60,priceHistory}.ts` (신규 6파일 383줄), `server/trading/exitEngine/rules/{atrDynamicStop,r6EmergencyExit,ma60DeathForceExit,hardStopLoss,cascadeFinal,trailingPeakUpdate,trancheTakeProfitLimit,trailingStop,legacyTakeProfit,cascadeHalf,cascadeWarn,rrrCollapseExit,bearishDivergenceExit,ma60DeathWatch,stopApproachAlert,euphoriaPartialExit}.ts` (신규 16파일 1108줄), `_workspace/2026-04-26_exitengine-decomposition/refactor/plan.md` (신규 이동 체크리스트), `ARCHITECTURE.md` (+11 boundary), `CLAUDE.md` (복잡도 위반 표 P2 정식 제거) | 사용자 요청 원문 "리팩토링 실시 — _updateShadowResultsImpl 의 15개 매도 규칙을 exitRules/ 디렉토리의 순수 함수로 분리". P2 우선순위(exitEngine 1,233 → 18 줄) 완주. 신호 → 진입 → 매크로 게이트는 PR-40 Phase A 로, 청산은 본 PR 로 분해 완료 — signalScanner Phase B 와 stockScreener.ts 만 잔여 P0/P1 항목. 새 청산 규칙(예: 시간 기반 만료, 섹터 로테이션 강제 청산) 도입이 파일 1개 + 배열 1줄로 가능해진 단위 테스트 가능 구조 확보. |
| 2026-04-26 | PR-54 exitEngine 청산 규칙 16종 단위 테스트 추가 (ADR-0028 §M5 회귀 그리드 선행): 사용자 요청 "권장하는대로" — PR-53 follow-up 으로 P1 아이디어 5 실행. ADR-0028 의 M5 (오케스트레이터 박스 패턴화 — 부수효과 → 순수 ExitDecision 반환) 진행 전 회귀 그리드 확보가 필수라는 마이그레이션 정책에 따라, 16개 청산 규칙 파일 각각에 격리된 단위 테스트 작성. ① **공용 헬퍼** `server/trading/exitEngine/rules/__tests__/_testHelpers.ts` (84줄) — `makeMockShadow(overrides)` ServerShadowTrade 기본값 (shadowEntryPrice 100 / qty 100 / stopLoss 90 / target 120 / SHADOW mode / R2_BULL) + `makeMockCtx(overrides)` ExitContext 기본값 (returnPct 자동 계산) + `SHADOW_OK_RESULT` / `LIVE_FAILED_RESULT` / `makeLiveOrderedResult(ordNo)` SellOrderResult 픽스처 3종. 외부 의존성 mock 은 vitest 호이스팅 규칙상 각 test 파일이 자체 선언 (vi.mock 블록은 import 보다 먼저 실행되어야 한다). ② **16 rule.test.ts 신규** — trailingPeakUpdate 5 (HWM 갱신/래칫/skipRest=false) + cascadeWarn 4 (-7% boundary/dedupe) + stopApproachAlert 6 (3-stage dedupe + 손절 아래 차단) + ma60DeathWatch 5 (이미 스케줄/MA 실패/역배열 미감지/신규 감지) + r6EmergencyExit 6 (regime/dedupe/30% 청산/FAILED 롤백/LIVE_ORDERED+addSellOrder/qty=0) + rrrCollapseExit 6 (dedupe/수익영역/risk≤0/RRR≥1/RRR<1 50%/FAILED 롤백) + bearishDivergenceExit 6 (dedupe/수익영역/fetch 실패/감지 미스/감지 시 30%/FAILED 롤백) + euphoriaPartialExit 5 (status/euphoria 미감지/트리거+상태전이/FAILED+롤백/PARTIALLY_FILLED 발동) + atrDynamicStop 6 (entryATR14 미설정/래칫/Lock-in/BEP/trailingActive/skipRest=false) + hardStopLoss 6 (조건 미달/전량+SSOT/PROFIT_PROTECTION/INITIAL/REGIME/FAILED CRITICAL) + cascadeFinal 5 (-25% boundary/-25% 전량/-30% 블랙리스트/FAILED 차단/exitRuleTag) + cascadeHalf 5 (조건 미달/dedupe/-15% 50%/FAILED 롤백/cascadeStep 1→2 진행) + trancheTakeProfitLimit 6 (미설정/trailingEnabled/가격 미달/첫 트랜치/완료 시 trailing 활성화/FAILED 롤백) + trailingStop 6 (trailingEnabled false/HWM 미설정/trailFloor 위/trailFloor 도달 청산/FAILED CRITICAL/trailPct override) + legacyTakeProfit 5 (target 미달/HIT_TARGET 전량/LIVE_ORDERED+addSellOrder/FAILED CRITICAL/subType FULL_CLOSE) + ma60DeathForceExit 6 (이미 forced/forceExitDate 미설정/만료 전/만료+회복 초기화/만료+역배열 강제/FAILED CRITICAL). ③ **누적 88 케이스** — 각 rule 5~6 케이스 × 16 rule = **88/88 pass** (3.7s). 검증 패턴 표준화: 트리거 boundary / dedupe 플래그 / FAILED 롤백 / 상태 전이 / 메시지 priority. ④ **검증** — vitest server/trading **48 files 394/394 pass** (PR-53 32 files 306/306 + 신규 16 files 88/88 = 무회귀) + 전체 vitest **224 files 2261/2261 pass** + lint(client + server tsc) + validate:all 6종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 — 모든 외부 호출 vi.mock 으로 격리. ⑤ **결과** — ADR-0028 §M5 (박스 패턴화) 진행 전 회귀 그리드 확보. 메시지/priority/dedupeKey 일관 executor 도입 시 byte-equivalent 검증을 단위 테스트가 자동 보호. 후속 PR-K (이미 계획됨): M5 박스 패턴화 — 각 rule 이 ExitDecision 반환 + orchestrator 가 KIS/텔레그램/attribution 일괄 실행 점진 전환. | `server/trading/exitEngine/rules/__tests__/_testHelpers.ts` (신규 84줄), `server/trading/exitEngine/rules/__tests__/{trailingPeakUpdate,cascadeWarn,stopApproachAlert,ma60DeathWatch,r6EmergencyExit,rrrCollapseExit,bearishDivergenceExit,euphoriaPartialExit,atrDynamicStop,hardStopLoss,cascadeFinal,cascadeHalf,trancheTakeProfitLimit,trailingStop,legacyTakeProfit,ma60DeathForceExit}.test.ts` (신규 16파일, 88 케이스) | 사용자 응답 "권장하는대로" — PR-53 직속 follow-up 으로 P1 아이디어 5 실행. ADR-0028 §M5 의 "단위 테스트 선행 의무" 충족 — 후속 박스 패턴화 PR 이 메시지/priority 변경 시 회귀를 자동 감지할 수 있는 그리드 완성. |
| 2026-04-26 | PR-55 stockScreener.ts 분해 — 데이터 + 어댑터 + 핵심 로직 분리 (ADR-0029, P1 복잡도 위반 정식 해소): 사용자 요청 "추천방향으로" — PR-53/54 follow-up 으로 P1 2순위 (stockScreener 1,571줄) 실행. `server/screener/stockScreener.ts` 1,573 줄을 `server/screener/` 디렉토리 7 파일로 책임 단위로 분해. 외부 24 importer 무수정 — barrel re-export 로 호환 유지. ① **데이터 모듈** `stockUniverse.ts` (270줄, 데이터-only) — KOSPI/KOSDAQ 시드 ~225 종목 `STOCK_UNIVERSE` 상수 분리. ② **어댑터 3종** (`adapters/`) — `yahooQuoteAdapter.ts` (315줄) Yahoo 5분 cache + range=2y + Phase 2 가속도 + Compression Score + MTAS 다운샘플 + 위험 종목 감지 + `YahooQuoteExtended` 타입 + `fetchYahooQuote` / `kisQuoteAdapter.ts` (369줄) KIS 현재가 + 일봉 캔들 `buildExtendedFromKisDaily` + `fetchKisQuoteFallback` + `fetchKisIntraday` + `enrichQuoteWithKisMTAS` (월/주봉 보강) / `krxScreenerAdapter.ts` (56줄) KRX 투자자별 매매 + PER/PBR 폴백 외국인 순매수 80개 / `_indicators.ts` (51줄) Wilder RSI + RSI14 + EMA + MACD 어댑터 공용 헬퍼 (Yahoo·KIS 산식 동기화 SSOT). ③ **부수 모듈** — `rejectionLog.ts` (26줄) `RejectionEntry` + `getLastRejectionLog` + `setLastRejectionLog` setter (autoPopulateWatchlist 가 호출, module-local mutable state 캡슐화) / `watchlistRejectionReport.ts` (50줄) `sendWatchlistRejectionReport` 텔레그램 LOW priority + 20시간 dedupe 일일 리포트. ④ **본체 정리** `stockScreener.ts` 1,573 → 542줄 (-65.5%): `preScreenStocks` (KIS 4-TR 병렬) + `autoPopulateWatchlist` (3-Preset) + `getScreenerCache` + `ScreenedStock` 타입만 잔존 + 분해 6 파일 barrel re-export. 외부 24 importer (`signalScanner` / `dryRunScanner` / `buyPipeline` / `trancheExecutor` / `tradingOrchestrator` / `intradayScanner` / `universeScanner` / `shadowDataGate` / `stockPickReporter` / `prefetchedContext` / `quantFilter` / `confluenceEngine` / `quant/conditions/types` / `pipelineHelpers` / `stage1Audit.test` / `dynamicUniverseExpander` / `telegram/commands/watchlist/add.cmd` / 기타) 의 `STOCK_UNIVERSE` / `fetchYahooQuote` / `YahooQuoteExtended` / `fetchKisQuoteFallback` / `enrichQuoteWithKisMTAS` / `fetchKisIntraday` / `getScreenerCache` / `preScreenStocks` / `autoPopulateWatchlist` / `sendWatchlistRejectionReport` 모두 무중단. ⑤ **검증** — vitest **224 files 2261/2261 pass** (server/screener 6 files 105/105 무회귀 + 전체 무회귀) + lint(client + server tsc) + validate:all 6종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). LIVE 매매 본체 0줄 변경 — byte-equivalent 추출. ⑥ **결과** — CLAUDE.md "복잡도 위반" 표 P1 항목 (stockScreener 1,571줄) 정식 제거. ARCHITECTURE.md +8 boundary 추가 (스크리너 7 모듈 + boundary rule). 새 데이터 소스 (예: Naver Mobile / DART OHLCV) 추가 = `adapters/<name>QuoteAdapter.ts` 파일 1개 + barrel re-export 1줄 추가 — ADR-0013 multi-source 4-tier fallback 사상과 자연 정렬. 잔여 P0: signalScanner.ts (1,820줄). | `docs/adr/0029-stockScreener-decomposition.md` (신규), `server/screener/stockScreener.ts` (1,573 → 542), `server/screener/stockUniverse.ts` (신규 270줄), `server/screener/rejectionLog.ts` (신규 26줄), `server/screener/watchlistRejectionReport.ts` (신규 50줄), `server/screener/adapters/_indicators.ts` (신규 51줄), `server/screener/adapters/yahooQuoteAdapter.ts` (신규 315줄), `server/screener/adapters/kisQuoteAdapter.ts` (신규 369줄), `server/screener/adapters/krxScreenerAdapter.ts` (신규 56줄), `_workspace/2026-04-26_stockScreener-decomposition/refactor/plan.md` (신규 이동 체크리스트), `ARCHITECTURE.md` (+8 boundary), `CLAUDE.md` (복잡도 위반 표 P1 정식 제거) | 사용자 요청 "추천방향으로" — P1 2순위 (stockScreener) 완주. 어댑터 3종이 분리되어 mock 가능 → autoPopulateWatchlist / preScreenStocks 단위 테스트 가능 구조 확보. ADR-0013 multi-source 4-tier fallback (KRX→Naver→Shadow→Seed) 사상과 어댑터 디렉토리 구조가 자연 정렬 — 후속 데이터 소스 추가 시 `adapters/` 에 파일 1개 추가로 끝. 잔여 P0 신호 = signalScanner.ts Phase B (evaluateBuyList → Chain of Responsibility 패턴) — 별도 PR 예정. |
| 2026-04-26 | PR-56 stockScreener 어댑터 단위 테스트 46 케이스 추가 (PR-55 follow-up, PR-54 패턴 차용): 사용자 응답 "추천방향으로" — PR-55 분해 직후 PR-54 가 PR-53 에 했던 것처럼 어댑터 단위 테스트 그리드 신설. ① **`_indicators.test.ts` 13 케이스** — calcRSI Wilder 평활화 5 (데이터 부족 fallback / 지속상승 RSI=100 / 지속하락 RSI≈0 / 보합 avgLoss=0 분기 / period=9 동작) + calcRSI14 1 (래퍼 동등성) + calcEMAArr 3 (빈 배열 / seed / 단조 흐름) + calcMACD 4 (<27 zero / 정확 27 zero / 50봉 양수 / histogram 항등). ② **`rejectionLog.test.ts` 4 케이스** — 초기 빈 배열 / set→get 동등성 / 재호출 전체 덮어쓰기 (append 아님) / 빈 배열 리셋. ③ **`krxScreenerAdapter.test.ts` 7 케이스** — investors 빈 배열 → 빈 / foreignNetBuy ≤0 제외 / PER/PBR 결측 close=0 제외 / foreignNetBuy 내림차순 정렬 / slice(0,80) 절삭 / PER 999 fallback + ScreenedStock 매핑 / 두 소스 throw → catch 빈 배열. ④ **`yahooQuoteAdapter.test.ts` 8 케이스** — HTTP 503 → null / chart.result 빈 → null / closes <5 → null / 정상 응답 → YahooQuoteExtended 핵심 필드(price/ma5/ma20/ma60/atr/rsi14/isHighRisk/ma60TrendUp) 채워짐 / 5분 캐시 동작 (동일 심볼 두번째 호출 fetch 0회) / null close 필터링 후 정상 계산 / 정상 거래량 isHighRisk=false (volume>0 필터로 0 거래량 entry 자연 제거 — 원본 isHighRisk=true 분기는 dead code 인 것을 명시) / fetch throw → null. N=80 으로 closes5dAgo 60+ 보장. ⑤ **`kisQuoteAdapter.test.ts` 14 케이스** — fetchKisQuoteFallback 6 (output 없음/price=0/일봉<20 보수 fallback/일봉 충분 buildExtended/등락 부호 5 음수/throw) + fetchKisIntraday 3 (정상 추출/output 없음/throw) + enrichQuoteWithKisMTAS 5 (MTAS null/dataAvailable=false/월봉≥13 부분 보강/주봉≥52 부분 보강/throw graceful). ⑥ **검증** — vitest server/screener **11 files 151/151 pass** (PR-55 6 files 105 + 신규 5 files 46 = 무회귀) + 전체 vitest **229 files 2307/2307 pass** + lint(client + server tsc) + validate:all 6종 + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 — 모든 외부 호출 vi.mock 격리. ⑦ **결과** — 어댑터 mock 패턴 표준화로 향후 multi-source 4-tier fallback (ADR-0013) 확장 시 회귀 자동 감지 그리드 확보. _indicators 산식 변경 시 본 테스트 기댓값 동기화 의무 명문화 (Yahoo·KIS 호환성 SSOT). 잔여 P0: signalScanner.ts (1,820줄) — Phase B + Chain of Responsibility (아이디어 4) 후속 PR. | `server/screener/adapters/_indicators.test.ts` (신규 13 케이스), `server/screener/rejectionLog.test.ts` (신규 4 케이스), `server/screener/adapters/krxScreenerAdapter.test.ts` (신규 7 케이스), `server/screener/adapters/yahooQuoteAdapter.test.ts` (신규 8 케이스), `server/screener/adapters/kisQuoteAdapter.test.ts` (신규 14 케이스) | 사용자 요청 "추천방향으로" — PR-54 패턴(PR-53 분해 직후 단위 테스트 그리드) 의 PR-55 대응판 완성. 어댑터 mock 으로 분리 후 단위 테스트 가능 구조 확보 — autoPopulateWatchlist / preScreenStocks 단위 테스트는 이제 어댑터 mock 만으로 가능. 4-tier 멀티소스 확장 (Naver Mobile / DART OHLCV / 향후) 시 동일 패턴 적용. |
| 2026-04-26 | PR-57 signalScanner Phase B PoC — EntryGate Chain of Responsibility 3 단순 게이트 (ADR-0030, P0 진행 첫 단계): 사용자 응답 "1" — 본 브랜치 (PR-53/54/55/56) 후 P0 signalScanner 진행 합의. **최소 위험 PoC**: 940줄 evaluateBuyList 의 9 게이트 중 **순수 동기 + 부수효과 단일** 인 3 게이트만 추출 (블랙리스트 / 추가매수 차단 / RRR). 나머지 6 게이트(cooldown/sectorConcentration/sectorPreGuard/portfolioRisk/liveGateRevalidation/kellyBudget) 는 후속 PR 로 분리 — async 시그니처·텔레그램 부수효과·Kelly 사이징 등 복잡 로직 격리. ① **타입 SSOT** `entryGates/types.ts` (29줄) — `EntryGateContext` (stock/shadows/scanCounters) + `EntryGateResult` discriminated union (pass=true OR pass=false + logMessage + 옵셔널 stageLog/counter/pushTrace) + `EntryGate = (ctx) => EntryGateResult`. counter 필드는 ScanCounters numeric 키 5종만 허용 (pendingTraces array 제외). ② **3 게이트 추출** — `blacklistGate.ts` (18줄, isBlacklisted 차단 메시지), `addBuyBlockGate.ts` (20줄, addBuyBlocked=true shadow find), `rrrGate.ts` (24줄, calcRRR + RRR_MIN_THRESHOLD 비교 + scanCounters.rrrMisses + stageLog.rrr=FAIL + pushTrace). 모든 게이트 byte-equivalent — 원본 console.log 문자열·counter 키·stageLog 값·pushTrace 시점 동일. ③ **chain SSOT** `entryGates/index.ts` (29줄) — `ENTRY_GATES_PHASE_B_POC` 배열 (blacklist→addBuyBlock→rrr 순서, 원본 if-블록 순서와 1:1 정합). ④ **회귀 테스트 14 케이스** (`__tests__/`) — `_testHelpers.ts` (53줄, makeMockStock + makeMockShadow + makeMockCtx 픽스처) + `blacklistGate.test.ts` 4 (블랙리스트 미등재 pass / 등재 차단 + 메시지 / stock.code 정확 전달 / 차단 시 부수효과 없음) + `addBuyBlockGate.test.ts` 5 (shadows 빈 / 동일종목 차단 / addBuyBlocked=false 통과 / 다른종목 차단 무영향 / 다중 shadows 중 하나라도 차단) + `rrrGate.test.ts` 5 (정상 RRR 통과 / 미달 차단+부수효과 / 임계 정확값 통과 / 메시지 종목명·코드·임계 포함 / stageLog FAIL 형식). vitest server/trading/signalScanner/entryGates **3 files 14/14 pass** (1.95s). ⑤ **wiring** `perSymbolEvaluation.ts` (1,400줄): `import { ENTRY_GATES_PHASE_B_POC } from ./entryGates/index.js` 추가, evaluateBuyList 라인 631-657 의 3 if-블록 (~28줄) 을 chain 순회 (~15줄) 로 교체. 첫 `pass: false` 반환 게이트에서 console.log + counter += 1 + stageLog 갱신 + pushTrace + outer `continue`. 게이트 통과 시 `stageLog.rrr = "PASS"` 만 별도 기록 (원본 동등 처리). isBlacklisted/calcRRR/RRR_MIN_THRESHOLD import 는 Pre-Breakout 추종/intraday 루프에서 여전히 사용 중이라 보존. ⑥ **검증** — vitest **232 files 2321/2321 pass** (PR-56 229/2307 → +3 files +14 tests, server/trading 51 files 408/408 무회귀) + lint(client + server tsc) + validate:all 6종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 — 게이트는 모두 인메모리 함수, 외부 호출 0건. ⑦ **결과** — Phase B 의 첫 발걸음 완료. Chain of Responsibility 패턴 정착, EntryGate 시그니처 SSOT 확정, 단위 테스트 가능 구조 확보. 잔여 Phase B 작업: 6 게이트 추가 (cooldown/sector*/portfolioRisk/liveGate/kelly) → async 게이트 시그니처 union 확장 → 차단율 통계 인프라 일반화. 본 PR 의 byte-equivalent 추출이 향후 메시지 형식 통합·차단 통계 추가·게이트 순서 조정 리팩터링의 회귀 안전망 역할. | `docs/adr/0030-signalScanner-entry-gates-phase-b.md` (신규), `server/trading/signalScanner/entryGates/types.ts` (신규 29줄), `server/trading/signalScanner/entryGates/blacklistGate.ts` (신규 18줄), `server/trading/signalScanner/entryGates/addBuyBlockGate.ts` (신규 20줄), `server/trading/signalScanner/entryGates/rrrGate.ts` (신규 24줄), `server/trading/signalScanner/entryGates/index.ts` (신규 29줄), `server/trading/signalScanner/entryGates/__tests__/_testHelpers.ts` (신규 53줄), `server/trading/signalScanner/entryGates/__tests__/{blacklistGate,addBuyBlockGate,rrrGate}.test.ts` (신규 3파일, 14 케이스), `server/trading/signalScanner/perSymbolEvaluation.ts` (chain wiring + import) | 사용자 응답 "1" — P0 signalScanner 분해 진행 합의. evaluateBuyList 940줄에서 3 게이트 동기 추출의 PoC 가 후속 6 게이트 (async + 텔레그램 + Kelly) 로 확장될 수 있는 패턴 시드 확보. 본 PR 의 보수적 scope (3 동기 게이트만) 는 LIVE 자동매매 회귀 위험을 최소화하면서 Chain of Responsibility 의 가능성을 검증한다. |
| 2026-04-26 | PR-58 signalScanner Phase B EntryGate Chain 확장 — 4 게이트 추가 + async 시그니처 union (ADR-0030, PR-57 follow-up): 사용자 응답 "a" — Phase B 잔여 6 게이트 진행 합의. 9 게이트 중 **pure gate 패턴 적합 4개** (cooldown / sectorConcentration / sectorPreGuard / portfolioRisk) 추출. 잔여 2 (liveGateRevalidation 다단계 revalidation pipeline + kellyBudget Kelly 사이징) 는 pure gate 패턴 부적합으로 별도 ADR 예정. ① **EntryGate 시그니처 확장** `types.ts` — `EntryGate = (ctx) => EntryGateResult \| Promise<EntryGateResult>` async union 도입. EntryGateContext 5 필드 추가 (watchlist/mutables/currentPrice/totalAssets/kellyMultiplier). EntryGateResult pass 분기에 옵셔널 passLogMessage·passWarnMessage 추가 (cooldown 해제 메시지·portfolioRisk 경고). 차단 분기에 옵셔널 telegramMessage 추가 (sectorConcentration 가드 알림). ② **`cooldownGate.ts`** (44줄) — Regret Asymmetry Filter 3 분기: cooldownUntil 미설정 → no-op pass / 설정+released → stock.cooldownUntil/recentHigh undefined + mutables.watchlistMutated.value=true + passLogMessage("쿨다운 해제 — 진입 재허용") / 설정+미해제 → 차단 + logMessage("쿨다운 유지 (until ..., high ...)"). mutate-and-pass 패턴 (예외적이지만 byte-equivalent 보존). ③ **`sectorConcentrationGate.ts`** (38줄) — sector 미설정 pass / 동일 섹터 활성 < MAX_SECTOR_CONCENTRATION pass / >= MAX 차단 + telegramMessage 동반 (orchestrator 가 sendTelegramAlert 일괄). isOpenShadowStatus 로 active 카운트 (PENDING/HIT_STOP 등 closed 제외). ④ **`sectorPreGuardGate.ts`** (38줄) — Phase 1-② 섹터 노출 사전 가드. estCandidateValue = totalAssets × estRawPct(gateScore≥9→0.12, ≥7→0.08, ≥5→0.05, 그외 0.03) × kellyMultiplier. checkSectorExposureBefore allowed=false 시 stageLog.sectorGuard=BLOCK(weight) + pushTrace. ⑤ **`portfolioRiskGate.ts`** (32줄) — async, evaluatePortfolioRisk(stock.sector). entryAllowed=false → stageLog.portfolioRisk + pushTrace 차단 / true+warnings → passWarnMessage 통과 / true+무경고 → 조용히 통과. ⑥ **`index.ts` chain SSOT 확장** — `ENTRY_GATES_PHASE_B` 7 게이트 (cooldown→blacklist→addBuyBlock→rrr→sectorConcentration→sectorPreGuard→portfolioRisk) 우선순위 배열. 원본 perSymbolEvaluation.ts 라인 610-715 의 if-블록 순서와 1:1 정합. ENTRY_GATES_PHASE_B_POC alias 유지로 PR-57 호환 보존. ⑦ **chain wiring** `perSymbolEvaluation.ts` — 라인 610-715 (cooldown 30줄 + PoC chain block 17줄 + sectorConcentration 22줄 + sectorPreGuard 26줄 + portfolioRisk 16줄 = 약 110줄) 을 chain 순회 (~30줄) 로 교체. orchestrator: pass 시 passLog/passWarn 출력 / fail 시 console.log + counter += 1 + stageLog 갱신 + pushTrace + telegramMessage 발송 + outer continue. await Promise.resolve(gate(...)) 패턴으로 sync/async 통합 처리. ⑧ **회귀 테스트 21 케이스 신규** — `_testHelpers.ts` makeMockMutables 신설 + EntryGateContext 5 신규 필드 기본값 자동 채움 / `cooldownGate.test.ts` 5 (미설정 no-op / released mutate + pass + 메시지 / 미해제 차단 + 보존 / recentHigh fallback / toLocaleString 포함) / `sectorConcentrationGate.test.ts` 5 (sector 미설정 / 활성 0 / MAX 차단 + 텔레그램 / 다른 섹터 통과 / closed shadow 제외) / `sectorPreGuardGate.test.ts` 6 (allowed pass / blocked + stageLog + pushTrace / sector fallback / gateScore≥9 0.12 산식 / gateScore=5 0.05 산식 / "?" fallback) / `portfolioRiskGate.test.ts` 5 (조용히 pass / 차단 / 경고 통과 / sector 전달 / 차단 시 텔레그램 미정의). 기존 PR-57 3 게이트 테스트(blacklist 4 + addBuyBlock 5 + rrr 5) 는 sed 로 await 추가. 누적 35 케이스 총 7 files. ⑨ **검증** — vitest server/trading/signalScanner/entryGates **7 files 35/35 pass** (PR-57 14 + PR-58 21 = 무회귀) + server/trading **55 files 429/429** + 전체 vitest **236 files 2342/2342 pass** + lint(client + server tsc) + validate:all 6종 + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 — 게이트는 모두 mock 격리. ⑩ **결과** — Phase B 의 7/9 게이트 추출 완료 (78%). evaluateBuyList 약 110줄 → 30줄 (-72%). EntryGate 패턴이 sync + async 양쪽 + telegram 부수효과 + mutate-and-pass + 경고 통과 4 패턴 모두 표현 가능함을 증명. 잔여 2 (liveGateRevalidation 다단계 / kellyBudget 사이징) 는 EntryGate 패턴 부적합 — 별도 RevalidationStep 패턴 또는 SizingDecider 패턴 ADR 예정. | `docs/adr/0030-signalScanner-entry-gates-phase-b.md` (PR-58 scope 추가 시 갱신 옵션), `server/trading/signalScanner/entryGates/types.ts` (async union 확장 + 5 신규 ctx 필드), `server/trading/signalScanner/entryGates/{cooldownGate,sectorConcentrationGate,sectorPreGuardGate,portfolioRiskGate}.ts` (신규 4파일, 152줄), `server/trading/signalScanner/entryGates/index.ts` (7 게이트 SSOT + alias), `server/trading/signalScanner/entryGates/__tests__/_testHelpers.ts` (makeMockMutables + 신규 ctx 필드 defaults), `server/trading/signalScanner/entryGates/__tests__/{cooldownGate,sectorConcentrationGate,sectorPreGuardGate,portfolioRiskGate}.test.ts` (신규 4파일, 21 케이스), `server/trading/signalScanner/entryGates/__tests__/{blacklistGate,addBuyBlockGate,rrrGate}.test.ts` (sed await 추가), `server/trading/signalScanner/perSymbolEvaluation.ts` (4 if-블록 110줄 → 30줄 chain 순회) | 사용자 응답 "a" — Phase B 진행. Pure gate 패턴 적합 4개 추출로 EntryGate 패턴이 모든 게이트 종류(sync/async/telegram/mutate/warn) 를 표현 가능함을 증명. 잔여 2 (liveGateRevalidation/kellyBudget) 는 별도 패턴 필요로 분리 — pure gate scope 보호. 본 PR 의 byte-equivalent 추출이 향후 메시지 통합·차단 통계·게이트 순서 조정 리팩터링의 회귀 안전망. |
| 2026-04-26 | PR-59 signalScanner Phase B Part 2 PoC — entryRevalidationStep RevalidationStep 패턴 도입 (ADR-0031, PR-58 follow-up): 사용자 응답 "권장패턴으로" — PR-58 노트의 "잔여 2개(liveGateRevalidation 다단계 / kellyBudget 사이징) 는 pure gate 패턴 부적합 — 별도 ADR 예정" 후속. ADR-0031 신설로 evaluateBuyList 잔여 영역을 **3 패턴 분리**: (1) **RevalidationStep** — 다단계 mutating pipeline (KIS 보정 / liveGate 재검증 / MTAS / SellOnly / Yahoo unavailable), (2) **SizingDecider** — 수치 결정 산출 (신뢰도 티어 / KellyBudget), (3) **commit 단계** — chain 외부 단일 지점 reserveSlot + enqueue (슬롯 예약 롤백 SSOT 단순화). 본 PR 은 **최소 위험 PoC — 1 step 만 추출**: `entryRevalidationStep` (라인 692-732, 약 41줄). ① **신규 SSOT** `server/trading/signalScanner/revalidationSteps/types.ts` — `RevalidationStepResult` discriminated union (proceed=true OR proceed=false + logMessage + failReasons + stageLogValue) + `RevalidationStepPass` / `RevalidationStepFail`. step 자체는 외부 mutation·부수효과 0건 — fail 시 diagnostic 만 반환, caller(evaluateBuyList) 가 stock.entryFailCount, watchlistMutated, scanCounters.gateMisses, stageLog, pushTrace, counterfactual 기록을 일괄 적용. ② **`entryRevalidationStep.ts`** (60줄) — `evaluateEntryRevalidation` (entryEngine.js 순수 함수) 호출 + `getMinGateScore(regime)` 임계 적용 후 결과를 RevalidationStepResult 로 변환. byte-equivalent: logMessage 형식 `[AutoTrade] {stockName} 진입 직전 재검증 탈락: {reasons.join(', ')}` 100% 보존, stageLogValue `FAIL({reasons.join(',')})` 100% 보존, failReasons 배열 그대로 전달. 동기 함수 (evaluateEntryRevalidation 자체가 동기). ③ **`index.ts` barrel** — RevalidationStepResult 3 타입 + entryRevalidationStep + EntryRevalidationStepInput export. 후속 PR (kisIntradayCorrectionStep / mtasGateStep / sellOnlyExceptionStep / yahooUnavailableStep) 가 동일 시그니처로 추가 가능. ④ **wiring** `perSymbolEvaluation.ts` — `import { entryRevalidationStep } from './revalidationSteps/index.js'` + entryEngine.js import 에서 `evaluateEntryRevalidation` 제거 (step 내부에서만 사용). 라인 692-732 의 inline `evaluateEntryRevalidation` 호출 + `if (!entryRevalidation.ok)` 분기를 step 호출 + `if (!revalResult.proceed)` 분기로 교체. caller 부수효과 (stock.entryFailCount++, watchlistMutated, scanCounters.gateMisses++, stageLog.gate, pushTrace, counterfactual 기록 try/catch) 100% 보존. ⑤ **회귀 테스트 8 케이스 신규** (`__tests__/entryRevalidationStep.test.ts`) — 정상 입력 proceed=true / SKIP signalType Gate 미달 차단 / 돌파 +5% 과열 차단 / 다중 fail (Gate 미달 + 과열) failReasons 배열 + stageLogValue 콤마 결합 / reCheckGate=null minGate fallback 자연 통과 / reCheckQuote=null Gate 만 검증 / proceed=true 시 failReasons 미포함 (discriminated union) / byte-equivalent logMessage 형식 검증 (원본 perSymbolEvaluation 라인 705 와 동일). vitest server/trading/signalScanner/revalidationSteps **1 file 8/8 pass** (1.91s). ⑥ **검증** — vitest server/trading **57 files 455/455 pass** (PR-58 55/429 → +2 files (revalidationSteps + 무회귀) +26 tests, server/trading 무회귀) + lint(client + server tsc) + validate:all 6종 (DeployWindow/PRES/ACMA/SDS/SRP/SymbolBoundary/Gemini) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 — entryRevalidationStep 은 evaluateEntryRevalidation 순수 함수만 호출 (kisClient/orchestrator/signalScanner 본체 무수정, 절대 규칙 #2/#3/#4 준수). LIVE 매매 본체 0줄 변경 — byte-equivalent 추출. ⑦ **결과** — RevalidationStep 패턴 시드 확보. 후속 PR 5건 (kisIntradayCorrection / mtas / sellOnlyException / yahooUnavailable / sizingTierDecider / kellyBudgetDecider / stopLossPolicyResolver / 승인큐 commit 분리) 이 본 PR 시그니처를 그대로 차용. 패턴 3종 (EntryGate Phase B / RevalidationStep / SizingDecider) 공존 — 신규 진입 검증 추가 시 패턴 판정 인지 부담은 ADR-0031 의 패턴 분리 결정 표로 흡수. evaluateBuyList 약 800 → 200줄 감축은 후속 PR 5건 누적 시 달성 예상 (본 PoC 단독 효과는 -16줄 수준). | `docs/adr/0031-signalScanner-revalidation-and-sizing-patterns.md` (신규), `server/trading/signalScanner/revalidationSteps/types.ts` (신규 27줄), `server/trading/signalScanner/revalidationSteps/entryRevalidationStep.ts` (신규 60줄), `server/trading/signalScanner/revalidationSteps/index.ts` (신규 11줄), `server/trading/signalScanner/revalidationSteps/__tests__/entryRevalidationStep.test.ts` (신규 8 케이스), `server/trading/signalScanner/perSymbolEvaluation.ts` (entryRevalidationStep wiring + evaluateEntryRevalidation import 제거) | 사용자 응답 "권장패턴으로" — PR-58 노트의 잔여 2 게이트 (liveGateRevalidation / kellyBudget) 가 EntryGate 패턴 부적합으로 분리 진행. ADR-0031 신설로 RevalidationStep + SizingDecider + commit 단계 3 패턴 분리 결정 + PoC 1 step 추출. 슬롯 예약 롤백 SSOT 단순화를 위해 승인큐 등록은 chain 외부 단일 commit 지점으로 명시. 본 PR 은 byte-equivalent 추출에 집중 — 후속 PR 5건이 시그니처를 그대로 차용 가능한 패턴 시드 확보. |
| 2026-04-26 | PR-60~65 signalScanner Phase B Part 2 완주 — RevalidationStep 4 + SizingDecider 3 + commit 헬퍼 분리 (ADR-0031 §"후속 PR 잔여" 일괄 처리): 사용자 응답 "전부 다 진행" — ADR-0031 PR-60~65 6건 단일 PR 일괄 처리. evaluateBuyList 잔여 5 영역(KIS 보정·Yahoo 가용성·MTAS·SellOnly·신뢰도 티어·KellyBudget·손절 정책·승인큐 commit) 을 RevalidationStep + SizingDecider + commit 3 패턴으로 byte-equivalent 추출. **결과: perSymbolEvaluation.ts 1318 → 1261줄 (-57). evaluateBuyList 본체 약 -120줄 (945 → ~820)**. ① **PR-60 `kisIntradayCorrectionStep`** (`revalidationSteps/`, mutating step) — 라인 655-681 의 KIS dayOpen/prevClose 보정 추출. step 이 reCheckQuote 직접 mutate, caller 는 logMessages 출력만. fetchKisIntraday 호출 + reconcileDayOpen + 보정 로그 byte-equivalent 보존. ② **PR-61 `yahooAvailabilityStep` + `mtasGateStep` + `sellOnlyExceptionStep`** (`revalidationSteps/`, RevalidationStep 시그니처) — 라인 734-741(Yahoo 가용성, BUG-02 fix), 752-757(MTAS≤3 차단), 760-773(SELL_ONLY 예외 liveGate/MTAS 4중 검증). 모두 차단 분기 1:1, byte-equivalent 메시지 보존. yahooAvailabilityStep 통과 후 `if (!reCheckGate) continue` TypeScript 좁히기 가드 추가 (도달 불가). ③ **PR-62 `sizingTierDecider`** (`sizingDeciders/`, SizingDecider 패턴 신설) — 라인 775-806 의 신뢰도 티어 분류 + PROBING 슬롯 가드 추출. `SizingDeciderFail` (ok:false + logMessage) + `SizingTierDeciderPass` (ok:true + tierDecision + logMessages) discriminated union. classifySizingTier + canReserveBanditProbingSlot 호출 + bandit budget 결정 본체 100% 보존. ④ **PR-63 `kellyBudgetDecider`** (`sizingDeciders/`, SizingDecider 확장) — 라인 879-923 의 계좌 리스크 예산 + Fractional Kelly 게이트 추출. getRealtimePrice 활성 포지션 수집 + getAccountRiskBudget + computeRiskAdjustedSize 호출 본체 동일. budget.canEnterNew=false 차단 / sized.recommendedBudgetKrw≤0 차단 / sized.kellyWasCapped 경고 3 분기 byte-equivalent. ⑤ **PR-64 `stopLossPolicyResolver`** (`sizingDeciders/`, 순수 헬퍼) — 라인 955-966 의 CATALYST/SWING 손절 정책 분리 + buildStopLossPlan 호출 추출. profile/profileKey/isCatalyst/regimeStopRate/entryATR14/catalystFixedStop/stopLossPlan 7개 산출값 반환. 차단 분기 없음. ⑥ **PR-65 `applyApprovalReservation`** (`approvalQueue/`, commit 단계 SSOT) — 라인 1070-1094 의 8개 mutable 필드 동시 갱신(reservedSlots / probingReservedSlots / reservedTiers / reservedIsMomentum / reservedBudgets / orderableCash / pendingSectorValue / reservedSectorValues) 을 단일 헬퍼로 캡슐화 — 슬롯 예약 롤백 SSOT 확립. MOMENTUM Shadow 격리 + PROBING 카운터 분리 + orderableCash Math.max(0,...) 가드 byte-equivalent. ⑦ **wiring** `perSymbolEvaluation.ts` — entryEngine.js 에서 `reconcileDayOpen` import 제거 (kisIntradayCorrectionStep 내부에서만 사용), `revalidationSteps/index.js` (5 export) + `sizingDeciders/index.js` (3 export) + `approvalQueue/index.js` (1 export) 신규 import. 기존 buildStopLossPlan import 는 Pre-Breakout 추종/intraday 루프(라인 383·524)에서 여전히 사용 중이라 보존. ⑧ **회귀 테스트 32 케이스 신규** — `kisIntradayCorrectionStep.test.ts` 5 (null reCheckQuote / fetchKisIntraday 실패 / dayOpen 보정 mutation+로그 / prevClose 양수 mutation 로그 없음 / prevClose=0 mutation 안 됨) + `yahooAvailabilityStep.test.ts` 2 (proceed / null 차단) + `mtasGateStep.test.ts` 4 (boundary 3 정확 차단 / >3 통과 / 0 차단 / 3.01 통과) + `sellOnlyExceptionStep.test.ts` 4 (allow=false 통과 / liveGate 차단 / MTAS 차단 / 모두 통과) + `sizingTierDecider.test.ts` 3 (정상 CONVICTION + logMessages / liveGate=0 tier 미달 차단 / PROBING 슬롯 포화 분기) + `kellyBudgetDecider.test.ts` 4 (budget 차단 / sized=0 차단 / 정상 통과 / Kelly capped 경고) + `stopLossPolicyResolver.test.ts` 4 (CATALYST 고정 / SWING ATR / profileType 기본 B / ATR 미전달 entryATR14=0) + `applyApprovalReservation.test.ts` 6 (LIVE STANDARD 8 필드 / PROBING 카운터 +1 / MOMENTUM Shadow 격리 / orderableCash 음수 방지 / effectiveBudget=0 / sector fallback). vi.mock 으로 외부 의존성 격리 — KIS/KRX quota 0 침범. ⑨ **검증** — vitest server/trading **65 files 487/487 pass** (PR-59 57/455 → +8 files +32 tests, server/trading 무회귀) + lint(client + server tsc) + validate:all 6종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 본체 무수정, kisIntradayCorrectionStep 만 fetchKisIntraday 재호출 — 기존 동일 코드 위치 이동). LIVE 매매 본체 0줄 변경 — byte-equivalent 추출. ⑩ **결과** — ADR-0031 후속 PR 5건 (PR-60~64) + commit 단계 분리(PR-65) 일괄 완주. EntryGate Phase B(PR-57/58 7 게이트) + RevalidationStep(PR-59~61 5 step) + SizingDecider(PR-62~64 3 decider) + commit 헬퍼(PR-65) **4 패턴 공존** — 신규 진입 검증 추가 시 ADR-0031 의 패턴 분리 결정 표가 인지 부담 흡수. PR-66 후속 후보: evaluateIntradayList 동일 패턴 적용 + ENTRY_GATES_PHASE_B chain 외부 commit 통합 + 슬롯 예약 롤백 SSOT 인-place 활용. | `server/trading/signalScanner/revalidationSteps/{kisIntradayCorrectionStep,yahooAvailabilityStep,mtasGateStep,sellOnlyExceptionStep}.ts` (신규 4파일), `server/trading/signalScanner/revalidationSteps/index.ts` (5 export), `server/trading/signalScanner/revalidationSteps/__tests__/{kisIntradayCorrectionStep,yahooAvailabilityStep,mtasGateStep,sellOnlyExceptionStep}.test.ts` (신규 4파일, 15 케이스), `server/trading/signalScanner/sizingDeciders/{types,sizingTierDecider,kellyBudgetDecider,stopLossPolicyResolver,index}.ts` (신규 5파일), `server/trading/signalScanner/sizingDeciders/__tests__/{sizingTierDecider,kellyBudgetDecider,stopLossPolicyResolver}.test.ts` (신규 3파일, 11 케이스), `server/trading/signalScanner/approvalQueue/{applyApprovalReservation,index}.ts` (신규 2파일), `server/trading/signalScanner/approvalQueue/__tests__/applyApprovalReservation.test.ts` (신규 6 케이스), `server/trading/signalScanner/perSymbolEvaluation.ts` (1318→1261, evaluateBuyList -120줄, 8개 wiring 지점 + import 정리) | 사용자 응답 "전부 다 진행" — ADR-0031 후속 6건 일괄 완주 결정. 단일 PR 로 6 PR scope 통합해 회귀 테스트·검증 부담을 한 번에 흡수. byte-equivalent 추출 원칙 + 단위 테스트 32 케이스 + LIVE 매매 본체 0줄 변경으로 회귀 위험 차단. evaluateBuyList 가 4 패턴 (EntryGate / RevalidationStep / SizingDecider / commit) 의 조합으로 표현 가능해져 후속 변경 (메시지 통합 / 차단 통계 / 게이트 순서 조정 / 슬롯 롤백 정책) 이 패턴 단위로 격리됨. |
| 2026-04-26 | PR-X1 텔레그램 채널 운영 개선 — alertRouter VIBRATION_POLICY + 시멘틱 별칭 + 채널 ID boundary 가드 (ADR-0037, 사용자 12 아이디어 중 6+7 번): 사용자 요청 "텔레그램 채널운영 개선안 12 아이디어 — PR-X1 부터 진행 (alertRouter SSOT 강화)". 기존 audit 결과: alertRouter.ts SSOT + 4 카테고리(TRADE/ANALYSIS/INFO/SYSTEM) + dispatchAlert 단일 진입점 + daily/weekly digest 라우팅 이미 존재, channelPipeline 12개 발송 함수 모두 dispatchAlert 경유. 갭 3종: (1) VIBRATION_POLICY 매트릭스 부재 — 진동/조용히 결정이 호출처에 분산, (2) 사용자 의도 명칭(EXECUTION/SIGNAL/REGIME/JOURNAL) 과 enum 값(TRADE/ANALYSIS/INFO/SYSTEM) 매핑 명문화 부재, (3) process.env.TELEGRAM_*_CHANNEL_ID 직접 접근 누출 가드 부재. ① **alertCategories.ts** — `ChannelSemantic` const 신규 (EXECUTION=TRADE / SIGNAL=ANALYSIS / REGIME=INFO / JOURNAL=SYSTEM `as const` 객체) + `ChannelSemanticName` 타입. enum 값 자체는 무변경 (영속 데이터 키 보존). ② **alertRouter.ts** — `AlertSeverity` 타입 별칭 (DispatchPriority union), `VIBRATION_POLICY: Record<AlertCategory, Record<AlertSeverity, boolean>>` 매트릭스 SSOT 신규. EXECUTION 모든 심각도 진동 ON (체결 즉각 인지) / SIGNAL CRITICAL 만 진동 (FOMO 차단) / REGIME CRITICAL+HIGH 진동 (레짐 전환만) / JOURNAL 모두 진동 OFF (시간 격리). `resolveVibrationDecision(category, severity, override?)` SSOT — override 우선, undefined 시 매트릭스 적용. `DispatchAlertOptions.severity` 옵셔널 필드 추가 (priority alias, severity 명시 시 우선). dispatchAlert send 지점에서 resolveVibrationDecision 자동 호출 — 기존 명시적 disableNotification 호출은 그대로 동작 (후방호환), 미지정 호출만 정책 적용. `ChannelSemantic` re-export — 신규 코드는 alertRouter 한 곳만 import. ③ **scripts/check_channel_boundary.js** 신규 — 5 시그니처 (TELEGRAM_TRADE/ANALYSIS/INFO/SYSTEM/PICK_CHANNEL_ID) 의 process.env 직접 접근 차단. 화이트리스트 4 파일: alertRouter.ts (SSOT) + alertCategories.ts (env parser) + telegramClient.ts (LEGACY, PR-X3 마이그레이션 예정) + 본 스크립트. 패턴: `process.env.SIG` + `process.env['SIG']` + `process.env["SIG"]` 3분기. 주석 영역(블록 + 라인) 사전 제거 + URL 형식 보호. TELEGRAM_CHAT_ID 는 검사 대상 아님 (개인 회선 분리는 PR-X2 scope). ④ **package.json** — `validate:channelBoundary` 스크립트 + `validate:all` (7종으로 확장) + `precommit` 통합. ⑤ **회귀 테스트 28 케이스 신규** — `alertRouter.vibration.test.ts` 19 (VIBRATION_POLICY 매트릭스 4 카테고리 boolean 검증 + EXECUTION 4 심각도 ON / SIGNAL CRITICAL 만 / REGIME CRITICAL+HIGH / JOURNAL 모두 OFF + resolveVibrationDecision 7 분기: override true/false/undefined × 매트릭스 ON/OFF + 인자 2개 호출 + ChannelSemantic 4 별칭 매핑 + 4 별칭 → AlertCategory enum 1:1 + readonly) + `channelBoundary.test.ts` 9 (현재 코드베이스 통합 실행 OK + 패턴 6: dot 접근/대괄호 더블따옴표/싱글따옴표/주석 안 변수명 무시/블록 주석 안 코드 무시/단순 변수명 무시/TELEGRAM_CHAT_ID 제외/시그니처 5종 모두 탐지/다중 위반 누적). ⑥ **ADR-0043 신설** + ARCHITECTURE.md Boundary Rules 에 "alertRouter SSOT + 채널 ID boundary" 항목 추가. ⑦ **검증** — vitest server/alerts **7 files 63/63 pass** (신규 28 + 기존 35 무회귀) + 전체 vitest **249 files 2433/2433 pass** + lint(client + server tsc) + validate:all 7종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — alertRouter 의 implicit disableNotification 호출이 정책으로 자동 적용되는 점만 효과. ⑧ **결과** — 12 아이디어 PR-X1 완료. ChannelSemantic 별칭 + VIBRATION_POLICY 매트릭스 + ChannelBoundary 가드 SSOT 시드 확보. 후속 PR 5건: PR-X2 (개인 1:1 채팅 분리 + sendTelegramBroadcast deprecated + 잔고/자산 키워드 lint) / PR-X3 (12 callsite 시멘틱 별칭 마이그레이션 + sendPickChannelAlert deprecated) / PR-X4 (CH3 매크로 다이제스트 08:30+16:00 KST cron) / PR-X5 (CH4 일요일 19:00 KST 주간 자기비판 리포트 — attribution + bias heatmap 결합) / PR-X6 (/channel_test 명령 + 손절 카운트다운 사전 경보 PRIVATE_ONLY). | `docs/adr/0037-alert-router-vibration-policy.md` (신규), `server/alerts/alertCategories.ts` (+ChannelSemantic + ChannelSemanticName), `server/alerts/alertRouter.ts` (+AlertSeverity + VIBRATION_POLICY + resolveVibrationDecision + severity 옵션 + ChannelSemantic re-export + dispatchAlert wiring), `server/alerts/alertRouter.vibration.test.ts` (신규 19 케이스), `server/alerts/channelBoundary.test.ts` (신규 9 케이스), `scripts/check_channel_boundary.js` (신규 130줄), `package.json` (+validate:channelBoundary + validate:all + precommit 통합), `ARCHITECTURE.md` (+alertRouter SSOT boundary rule) | 사용자 12 아이디어 중 alertRouter SSOT 강화(6) + 진동 정책 매트릭스(7) 가 다른 PR 의 foundation 이라 단독 PR 분리. 호출자 무수정 byte-equivalent 패턴 — 기존 명시 호출 그대로 동작 + implicit 호출만 정책 적용. ChannelBoundary 가드는 향후 PR 들의 마이그레이션 회귀 안전망. |
| 2026-04-26 | PR-X2 개인 회선(DM) vs 채널 분리 + 잔고 키워드 누출 차단 lint (ADR-0038, 사용자 12 아이디어 중 5 번): 사용자 응답 "다음" — PR-X1 (alertRouter SSOT 강화) 후속. 사용자 5번 "개인 1:1 채팅은 4채널과 별개의 비상 회선으로 격리" 처리. audit 결과: `sendTelegramBroadcast` 는 코드베이스 코멘트에 따르면 이미 `sendTelegramAlert` 의 alias (`TELEGRAM_CHAT_ID` 단일 변수 운영 환경) — 이름이 "broadcast" 라 채널 누출로 오해 소지 있음. 진짜 위험은 `dispatchAlert(category, ...)` (채널 발송) 메시지에 잔고 키워드 누출. 현재 코드베이스 누출 0건 baseline 확인 — 회귀 차단을 위한 lint 가드 도입. ① **`sendPrivateAlert` SSOT** (`telegramClient.ts`) — 개인 DM 전용 시멘틱 별칭 신설. 내부 구현은 `sendTelegramAlert` 와 동일 (`TELEGRAM_CHAT_ID` 한 곳만 발송). JSDoc 으로 "private DM only, never reaches channel" 명시. 잔고/자산/비상정지/손절 접근/KIS 오류/EgressGuard 차단 같은 민감 정보는 본 함수 사용 권장. ② **`sendTelegramBroadcast` `@deprecated` 표시** — JSDoc 마이그레이션 경로 안내(개인 DM → sendPrivateAlert / 채널 → dispatchAlert). 9개 기존 호출자(weeklyConditionScorecard / supplyChainAgent / foreignFlowLeadingAlert / stopLossTransparencyReport / newHighMomentumScanner / positionMorningCard / sectorCycleDashboard / weeklyQuantInsight / scanReviewReport) 는 PR-X3 일괄 마이그레이션 예정. 본 PR 은 표시만, 호출 동작은 그대로. ③ **`scripts/check_sensitive_alerts.js` 신규** (185줄) — 채널 발송 함수 12종(`dispatchAlert` / `channelBuySignalEmitted` / `channelBuySignal` / `channelBuyFilled` / `channelSellSignal` / `channelMarketBriefing` / `channelRegimeChange` / `channelWatchlistAdded` / `channelWatchlistRemoved` / `channelWatchlistSummary` / `channelGlobalScan` / `channelPerformance`) 을 import 한 파일에서 잔고 키워드 8종(`총자산` / `총 자산` / `주문가능현금` / `잔여 현금` / `잔여현금` / `보유자산` / `보유 자산` / `평가손익`) 누출 검사. 매칭은 string/template/single-quote literal 안에서만 — 식별자/변수명/주석 등장은 허용. 제외 컨텍스트: `console.log/warn/error/debug/info(...)` (Railway 로그) + `throw new Error(...)` (예외 메시지) + 인라인 `// safe-channel-keyword` 주석 (해당 라인 또는 직전 라인). 화이트리스트 4 파일: alertRouter.ts (라우팅 SSOT) + telegramClient.ts (전송 로직) + 본 스크립트 (시그니처 정의) + persona/personaIdentity.ts (페르소나 분석 어휘 "총자산회전율"). ④ **package.json** — `validate:sensitiveAlerts` 스크립트 + `validate:all` (8종으로 확장) + `precommit` 통합. ⑤ **회귀 테스트 17 케이스 신규** (`sensitiveAlerts.test.ts`) — 통합 실행 1 + sendPrivateAlert 시그니처 4 (export 존재 / "개인 DM 전용" JSDoc / sendTelegramBroadcast `@deprecated` / 마이그레이션 경로 안내) + 패턴 매칭 12 (template/double-quote/single-quote 키워드 탐지 / console.log·warn 제외 / throw new Error 제외 / 인라인 opt-out / 직전 라인 opt-out / 식별자 등장 무시 / 주석 안 등장 무시 / 한 라인 다중 키워드 누적 / 8종 키워드 모두 탐지). ⑥ **ADR-0044 신설** + ARCHITECTURE.md Boundary Rules 에 "개인 회선(DM) vs 채널 분리" 항목 추가. ⑦ **검증** — vitest server/alerts **8 files 80/80 pass** (신규 17 + PR-X1 28 + 기존 35 무회귀) + 전체 vitest **250 files 2450/2450 pass** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — sendTelegramBroadcast 호출자 9건 동작 그대로, deprecated 주석만 추가. ⑧ **결과** — 12 아이디어 PR-X2 완료. sendPrivateAlert SSOT + sensitive keyword lint + sendTelegramBroadcast deprecated 시드 확보. 후속 PR 4건: PR-X3 (12 callsite 시멘틱 별칭 마이그레이션 + sendTelegramBroadcast 호출자 9건 → sendPrivateAlert + sendPickChannelAlert deprecated) / PR-X4 (CH3 매크로 다이제스트 08:30+16:00 KST cron) / PR-X5 (CH4 일요일 19:00 KST 주간 자기비판 리포트) / PR-X6 (/channel_test 명령 + 손절 카운트다운 사전 경보 sendPrivateAlert). | `docs/adr/0038-private-vs-channel-separation.md` (신규), `server/alerts/telegramClient.ts` (+sendPrivateAlert + sendTelegramBroadcast `@deprecated`), `scripts/check_sensitive_alerts.js` (신규 185줄), `server/alerts/sensitiveAlerts.test.ts` (신규 17 케이스), `package.json` (+validate:sensitiveAlerts + validate:all 8종 + precommit 통합), `ARCHITECTURE.md` (+개인 회선 vs 채널 boundary rule) | 사용자 12 아이디어 중 개인 회선 격리(5) 가 4채널 운영 안정성의 핵심 — 채널은 미래에 가족/동료 구독 가능하지만 잔고는 본인만 봐야 한다. PR-X1 의 alertRouter SSOT 위에 "잔고 키워드 lint 가 자동 차단" 안전망 추가 — 향후 PR-X3 마이그레이션 시 회귀 안전장치로 작동. byte-equivalent 패턴 — 호출자 무수정. |
| 2026-04-26 | PR-X3 텔레그램 callsite 시멘틱 별칭 마이그레이션 + sendPickChannelAlert 삭제 (ADR-0039, PR-X1/X2 후속): 사용자 응답 "다음" — PR-X1 (alertRouter SSOT) + PR-X2 (sendPrivateAlert + sensitive lint) foundation 위에 11 callsite 실제 마이그레이션. ① **9 sendTelegramBroadcast 호출자 마이그레이션** — `positionMorningCard.ts` (자산 구성 정보 → `sendPrivateAlert`) / `weeklyConditionScorecard.ts` `weeklyQuantInsight.ts` `scanReviewReport.ts` `stopLossTransparencyReport.ts` (메타 학습/회고 → `dispatchAlert(ChannelSemantic.JOURNAL)`) / `newHighMomentumScanner.ts` (종목 픽 → `dispatchAlert(ChannelSemantic.SIGNAL)`) / `supplyChainAgent.ts` `sectorCycleDashboard.ts` `foreignFlowLeadingAlert.ts` (매크로 → `dispatchAlert(ChannelSemantic.REGIME)`). ② **sendPickChannelAlert 호출자 2건 마이그레이션** — `stockPickReporter.ts` 의 sendPickChannelAlert + dispatchAlert(AlertCategory.ANALYSIS) **이중 발송 제거** → `dispatchAlert(ChannelSemantic.SIGNAL)` 단일 호출 (TELEGRAM_PICK_CHANNEL_ID = TELEGRAM_ANALYSIS_CHANNEL_ID 동일 환경에서 같은 메시지 2번 출력되던 회귀 차단). `weeklyDeepAnalysis.ts` (주간 심층 → `dispatchAlert(ChannelSemantic.JOURNAL)`). ③ **sendPickChannelAlert 함수 삭제** — `telegramClient.ts` 호출자 0건 확인 후 함수 본체 + `process.env.TELEGRAM_PICK_CHANNEL_ID` 직접 접근 제거. 환경변수 자체는 alertRouter.resolveAnalysisChannelId() legacy fallback 으로 후방호환 유지. ④ **ChannelBoundary 화이트리스트 축소** — `scripts/check_channel_boundary.js` 의 `ALLOWED_FILES` 4 → 3 파일 (alertRouter.ts / alertCategories.ts / check_channel_boundary.js, telegramClient.ts 제거). ⑤ **legacy 옵션 정리** — 모든 callsite 에서 `tier`/`category`/`disableChannelNotification` 제거 (alertRouter 가 자동 처리), `priority`/`dedupeKey` 만 유지. dispatchAlert 시그니처와 1:1 호환. stopLossTransparencyReport 만 `disableNotification: false` override (JOURNAL 정책 진동 OFF 이지만 손절은 즉각 인지). ⑥ **회귀 테스트 19 케이스 신규** (`callsiteMigration.test.ts`) — sendPickChannelAlert 삭제 검증 3 (export 없음/process.env 직접 접근 없음/ADR 주석) + 9 호출자 카테고리 매핑 검증 11 (SIGNAL 2 / REGIME 3 / JOURNAL 5 / PRIVATE 1) + stockPickReporter 이중 발송 제거 3 (sendPickChannelAlert 호출 0/AnalysisCategory→Semantic 변경/AlertCategory import 제거) + ChannelBoundary 화이트리스트 축소 2. ⑦ **검증** — vitest server/alerts **9 files 99/99 pass** (신규 19 + PR-X1+X2 80 무회귀) + 전체 vitest **251 files 2469/2469 pass** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — 알림 라우팅만 정합화. ⑧ **결과** — 12 아이디어 PR-X3 완료. 11 callsite 모두 시멘틱 별칭 사용 → 채널 의도가 코드에서 직접 읽힘. sendPickChannelAlert 삭제로 채널 ID 직접 접근 SSOT 가 alertRouter 단일로 좁혀짐. stockPickReporter 이중 발송 제거로 사용자 채널이 같은 메시지를 2번 받던 회귀 차단. PR-X4 (CH3 매크로 다이제스트 cron) / PR-X5 (CH4 주간 자기비판 리포트) / PR-X6 (/channel_test + 손절 카운트다운) 후속 분리. | `docs/adr/0039-telegram-callsite-migration.md` (신규), `server/alerts/positionMorningCard.ts` `weeklyConditionScorecard.ts` `newHighMomentumScanner.ts` `weeklyQuantInsight.ts` `scanReviewReport.ts` `supplyChainAgent.ts` `sectorCycleDashboard.ts` `foreignFlowLeadingAlert.ts` `stopLossTransparencyReport.ts` `stockPickReporter.ts` `weeklyDeepAnalysis.ts` (11 callsite 마이그레이션), `server/alerts/telegramClient.ts` (sendPickChannelAlert 삭제 + ADR-0045 메모), `scripts/check_channel_boundary.js` (화이트리스트 축소 4→3), `server/alerts/callsiteMigration.test.ts` (신규 19 케이스), `ARCHITECTURE.md` (+Telegram callsite 시멘틱 별칭 boundary rule) | 사용자 12 아이디어 중 4채널 운영의 실제 코드 적용 — PR-X1/X2 가 SSOT 구축이었다면 본 PR 은 그 SSOT 를 모든 callsite 가 사용하게 만든다. 사용자 채널이 같은 메시지를 2번 받던 stockPickReporter 회귀가 기능적 충격으로 가장 큼. positionMorningCard 의 자산 구성 정보 누출 위험도 개인 DM 으로 격리. |
| 2026-04-26 | PR-X4 CH3 REGIME 매크로 다이제스트 정기 cron (08:30 + 16:00 KST, ADR-0040, 사용자 12 아이디어 중 10번): 사용자 응답 "다음" — PR-X1/X2/X3 후속. 사용자 10번 "CH3 REGIME 전용 매크로 다이제스트, 1일 2회 정기 발행" 처리. 페르소나 "글로벌 스마트 머니 ETF 추적" 일과화 — CH3 채널이 정기 리듬을 가진 유일한 채널, 사용자가 무의식적으로 시간을 동기화하게 한다. ① **`server/alerts/macroDigestReport.ts` 신규** (174줄, @responsibility SRP 통과) — `MacroDigestMode = 'PRE_OPEN' \| 'POST_CLOSE'` 타입 + `formatMacroDigest(state, mode, now?)` 순수 함수 (외부 의존성 0, 테스트 가능) + `runMacroDigest(mode, now?)` async 발송. PRE_OPEN: 간밤 미국(VIX/US10Y-2Y/DXY) + 환율(USD/KRW 당일+20d) + 한국 사전(VKOSPI/외국인 5d 누적/EWY) + 매크로 헬스(MHS/Regime). POST_CLOSE: 한국 결산(KOSPI 일변동+20d/VKOSPI/외국인/신용잔고 5d) + 환율 + 글로벌(S&P500 20d/DXY 5d/WTI/HY 스프레드) + 매크로 헬스. ② **데이터 SSOT** — `macroStateRepo.loadMacroState()` 단일 소스. 외부 호출 0건 (다른 cron 이 갱신한 macroState 만 읽음). ③ **graceful fallback** — macroState=null 또는 개별 필드 누락(undefined/NaN) 시 'N/A' 표시. 옵셔널 hySpread 누락 시 라인 자체 생략 (filter Boolean). 메시지 자체는 항상 발송 — "정기 리듬" 이 데이터 갱신 실패에 의해 깨지지 않음. ④ **외국인 5d 누적 단위 분기** — abs(n) ≥ 10000(억원) → 조원 표기 ("+1.20조원"), 미만 → 억원 표기 ("+5,000억원"). 음수도 동일. ⑤ **cron 등록** (`server/scheduler/alertJobs.ts`) — `30 23 * * 0-4` (UTC 일~목 23:30 = KST 월~금 08:30 PRE_OPEN) + `0 7 * * 1-5` (UTC 월~금 07:00 = KST 월~금 16:00 POST_CLOSE). preMarketSignal 와 동시각 OK (메시지/카테고리 다름). ⑥ **dispatchAlert(ChannelSemantic.REGIME) 단일 진입점** — VIBRATION_POLICY[INFO][NORMAL]=false 자동 적용 (일상 매크로는 진동 OFF). dedupeKey `macro_digest:{PRE_OPEN\|POST_CLOSE}:{KST 일자}` 로 재시작 시 이중 발송 차단. ⑦ **절대 규칙 준수** — (a) 개별 종목 정보 절대 포함 금지 (CH3 정체성 "시장 전체 상태만"), 회귀 테스트가 6자리 코드 패턴 부재 검증. (b) 잔고 키워드 8종 누출 금지, 회귀 테스트 + validate:sensitiveAlerts 자동 차단. (c) ChannelBoundary 위반 0건 (alertRouter 만 채널 ID 해석). ⑧ **회귀 테스트 23 케이스 신규** (`macroDigestReport.test.ts`) — PRE_OPEN 메시지 포맷 6 (전체 섹션 렌더 / null fallback / DXY bullish 분기 / US10Y 음수 ⚠️ 역전 / VKOSPI 5d ↑ / 5d ↓) + POST_CLOSE 3 (전체 / null / hySpread 누락 라인 생략) + 외국인 5d 단위 분기 5 (억원/조원/음수 조원/음수 억원/0) + 잔고 키워드 누출 방지 3 (PRE/POST 키워드 0건 / PRE 6자리 코드 0건) + dispatchAlert wiring 4 (PRE 호출 검증 / POST 호출 / null fallback 호출 / dispatchAlert throw catch) + dedupeKey KST 자정 정합 2 (UTC 23:30 → KST 다음날 / UTC 07:00 → KST 같은 날). ⑨ **ADR-0040 신설** + ARCHITECTURE.md Boundary Rules 에 "CH3 REGIME 매크로 다이제스트 정기 발행" 항목 추가. ⑩ **검증** — vitest server/alerts **10 files 122/122 pass** (신규 23 + PR-X1/X2/X3 99 무회귀) + 전체 vitest **252 files 2492/2492 pass** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경. ⑪ **결과** — 12 아이디어 PR-X4 완료. CH3 채널이 사용자 무의식 시간 동기화 기준점 역할 가능. PR-X5 (CH4 일요일 19:00 KST 주간 자기비판 리포트) / PR-X6 (/channel_test + 손절 카운트다운) 후속 분리. | `docs/adr/0040-macro-digest-cron.md` (신규), `server/alerts/macroDigestReport.ts` (신규 174줄), `server/alerts/macroDigestReport.test.ts` (신규 23 케이스), `server/scheduler/alertJobs.ts` (+runMacroDigest import + 2 cron 등록), `ARCHITECTURE.md` (+CH3 매크로 다이제스트 boundary rule) | 사용자 12 아이디어 중 매크로 채널의 정기 리듬화 — 일과화의 미덕은 적은 횟수에서 나오므로 1일 2회 (PRE_OPEN/POST_CLOSE) 만. 데이터 수집 책임은 별도 cron(marketDataRefresh 등) 에 있고 본 모듈은 macroState read-only — 단일 책임 원칙. |
| 2026-04-26 | PR-X5 CH4 JOURNAL 주간 자기비판 리포트 — 일요일 19:00 KST 자동 발행 (ADR-0041, 사용자 12 아이디어 중 11번): 사용자 응답 "다음" — PR-X1/X2/X3/X4 후속. 사용자 11번 "CH4 JOURNAL 주간 자기 비판 리포트, 일요일 19:00" 처리. 페르소나 "보유 효과·후회 회피 경계" 의 자동화된 거울 — attributionAnalyzer + biasHeatmap + failurePatternDB 의 누적 데이터를 매주 1회 메타 진단으로 정제. ① **`server/alerts/weeklySelfCritiqueReport.ts` 신규** (235줄, @responsibility SRP 통과) — `summarizeStopPatterns(trades)` 순수 함수: HIT_STOP trade 만 entryRegime × exitRuleTag 별 카운트, count 내림차순 + 동률 시 regime 알파벳 정렬. `buildStopPatternRecommendation(buckets, total)` 자동 권고 휴리스틱: 표본 ≥ 3건 + 비율 ≥ 40% 임계 통과 시 R5_CAUTION/R6_DEFENSE → "임계값 +1점 강화 권고" / HARD_STOP·TRAILING_PROTECTIVE_STOP → "손절폭 검토 권고" / CASCADE 계열 → "진입 모멘텀 검증 강화 권고" / 그 외 → "패턴 모니터링". `formatWeeklySelfCritique(inputs, now?)` 메시지 빌더 + `runWeeklySelfCritique(now?)` async 발송. ② **메시지 5 섹션** — 📊 주간 거래 결산 (fill SSOT 활용 PR-15~18: fillCount/win/loss/가중P&L/실현원화/부분익절/전량청산) + 💢 주요 행동 편향 (escalatingBiases 3일 연속 ≥ 0.5, 평균 점수 → 등급 이모지 🔴≥0.7/🟡≥0.5, 한글 라벨, ↗악화/↘개선/→정체 trend, 상위 3개 표시) + 🛡️ 손절 패턴 분포 (regime × exitRule 카운트 + 자동 권고문) + 🧪 학습 실험 (활성/완료) + 📅 다음 주 점검 포인트 (편향 + 권고 결합). reflection 누락 ≥ 3일 시 ⚠️ 경고 추가. ③ **데이터 SSOT** — `aggregateFillStats(trades, range)` (PR-18 fill 단위) + `getLearningHistory(7).escalatingBiases` (PR-36 학습 이력) + `getLearningStatus()` (실험 제안 + reflection 누락) + `summarizeStopPatterns/buildStopPatternRecommendation` 신규 휴리스틱. 외부 호출 0건 (Gemini 미사용 — 결정적 권고). ④ **cron 등록** (`server/scheduler/alertJobs.ts`) — `0 10 * * 0` (UTC 일요일 10:00 = KST 일요일 19:00). ⑤ **dispatchAlert(ChannelSemantic.JOURNAL)** — VIBRATION_POLICY[SYSTEM] 모두 OFF (시간 격리, 주말 정독). dedupeKey `weekly_self_critique:{KST 일요일}` 로 재시작 시 이중 발송 차단. ⑥ **절대 규칙 준수** — (a) 개별 종목 정보 절대 포함 금지 (CH4 JOURNAL 메타 학습 정체성), 회귀 테스트가 6자리 코드 패턴 부재 검증. (b) 잔고 키워드 8종 누출 금지, 회귀 테스트 + validate:sensitiveAlerts 자동 차단. (c) ChannelBoundary 위반 0건. ⑦ **회귀 테스트 29 케이스 신규** (`weeklySelfCritiqueReport.test.ts`) — summarizeStopPatterns 5 (빈 / HIT_STOP 만 / 동일 키 누적 / count+regime 정렬 / "미상" fallback) + buildStopPatternRecommendation 8 (빈 / 표본 부족 / 비율 부족 / R5_CAUTION / R6_DEFENSE / HARD_STOP / CASCADE / 일반 모니터링) + formatWeeklySelfCritique 11 (정상 / 실현 0 fallback / 편향 없음 / ≥0.7 🔴 / 0.5~0.7 🟡 / ↗악화 / ↘개선 / 손절 0 fallback / reflection 누락 ≥3 ⚠️ / 누락 <3 경고 없음 / 편향 3개 초과 시 상위 3 표시) + 절대 규칙 2 (잔고 키워드 0건 / 6자리 코드 0건) + dispatchAlert wiring 3 (호출 + dedupeKey / 빈 입력 graceful / throw catch). ⑧ **ADR-0041 신설** + ARCHITECTURE.md Boundary Rules 에 "CH4 JOURNAL 주간 자기비판 리포트" 항목 추가. ⑨ **검증** — vitest server/alerts **11 files 151/151 pass** (신규 29 + PR-X1/X2/X3/X4 122 무회귀) + 전체 vitest **253 files 2521/2521 pass** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경. ⑩ **결과** — 12 아이디어 PR-X5 완료. 매주 일요일 저녁 한 번 페르소나 자기통제 거울이 자동 발송됨. PR-X6 (/channel_test 명령 + 손절 카운트다운 사전 경보) 만 잔여. | `docs/adr/0041-weekly-self-critique-report.md` (신규), `server/alerts/weeklySelfCritiqueReport.ts` (신규 235줄), `server/alerts/weeklySelfCritiqueReport.test.ts` (신규 29 케이스), `server/scheduler/alertJobs.ts` (+runWeeklySelfCritique import + cron 1건 등록), `ARCHITECTURE.md` (+CH4 주간 자기비판 boundary rule) | 사용자 12 아이디어 중 메타 학습 채널 정점 — attribution + biasHeatmap + failurePatternDB 의 누적 데이터가 매주 한 번 자동 진단으로 표면화. 결정적 휴리스틱 (Gemini 호출 0) 이라 외부 의존성·budget 영향 없음. 본 리포트는 *진단* 만, 실제 가중치 조정은 nightlyReflectionEngine + suggestNotifier 별도 경로 — 자동화된 거울로 한정. |
| 2026-04-26 | PR-X6 /channel_test 4채널 헬스체크 + 손절 카운트다운 sendPrivateAlert 정합화 (ADR-0042, 사용자 12 아이디어 중 8+12번, 12 아이디어 완주): 사용자 응답 "다음" — PR-X1~X5 후속, 마지막 PR. 사용자 8번 "손절 카운트다운 사전 경보 — CH1 채널 아닌 개인 DM 만 (패닉 매도 차단)" + 12번 "/channel_test 4채널 동시 헬스체크" 처리. ① **stopApproachAlert sendPrivateAlert 마이그레이션** — `server/trading/exitEngine/rules/stopApproachAlert.ts` 의 -5%/-3%/-1% 3단계 손절 접근 경보가 `sendTelegramAlert` → `sendPrivateAlert` 로 시멘틱 정합화. 동작은 동일 (TELEGRAM_CHAT_ID 만 발송, 둘 다 sendTelegramAlert 위임). 함수 이름이 사용자 의도("개인 DM 한정, CH1 EXECUTION 채널은 실제 발동 후 사후 보고만")와 직접 일치하도록 명문화. ② **/channel_test 4채널 확장** — `server/telegram/commands/alert/channelTest.cmd.ts` 의 기존 1채널 테스트(TELEGRAM_CHAT_ID 단독)를 `runChannelHealthCheck()` (alertRouter SSOT, PR-X1 부터 존재) 호출로 확장. 4채널(EXECUTION/SIGNAL/REGIME/JOURNAL) 동시 발송 후 결과 집계. `formatChannelHealthCheckResult(result)` 순수 함수 신규 export — 정상/미설정/비활성/발송실패 4분기 처리 + 미설정 환경변수 누적 안내 + N/4 정상 요약 + 모두 정상 시 ✨ 추가 라인. 결과 메시지 예: "✅ CH1 EXECUTION (TRADE) — 정상 (msg #101) / ❌ CH3 REGIME (INFO) — 채널 ID 미설정 (TELEGRAM_INFO_CHANNEL_ID) / 요약: 3/4 채널 정상 / ⚠️ 미설정 환경변수: TELEGRAM_INFO_CHANNEL_ID". ③ **CATEGORY_LABEL + CATEGORY_ENV 매핑** — AlertCategory enum → 사용자 시멘틱 라벨 (CH1 EXECUTION (TRADE) 등) + 환경변수 이름 매핑 (TELEGRAM_TRADE_CHANNEL_ID 등) 으로 진단 정밀화. ④ **회귀 테스트 15 케이스 신규** (`channelTest.test.ts`) — formatChannelHealthCheckResult 7 (모두 정상 / 1개 미설정 / 비활성 / 발송실패 / 다중 미설정 / 헤더·구분자·요약 순서 / AlertCategory enum 4값 drift 차단) + stopApproachAlert sendPrivateAlert 마이그레이션 4 (sendPrivateAlert import / sendTelegramAlert 호출 0건 / 3단계 호출 / ADR-0042 메모) + channelTest 등록 4 (commandRegistry.register / name·category·riskLevel / runChannelHealthCheck import / formatChannelHealthCheckResult export). ⑤ **기존 테스트 정합화** — `server/trading/exitEngine.atrIntegration.test.ts` 의 vi.mock 에 sendPrivateAlert 추가 + `server/trading/exitEngine/rules/__tests__/stopApproachAlert.test.ts` 의 mock 도 sendPrivateAlert 로 sed 일괄 교체. ⑥ **ADR-0042 신설** + ARCHITECTURE.md Boundary Rules 에 "/channel_test 4채널 헬스체크 + 손절 카운트다운 sendPrivateAlert" 항목 추가. ⑦ **검증** — vitest **254 files 2536/2536 pass** (신규 15 + 기존 stopApproachAlert 6 무회귀 + 모두 무회귀) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — sendPrivateAlert 와 sendTelegramAlert 가 동일 구현 위임이라 행위 보존. ⑧ **결과 — 12 아이디어 완주** — PR-X1(alertRouter SSOT + VIBRATION_POLICY) / X2(개인 회선 분리 + 잔고 lint) / X3(11 callsite 시멘틱 별칭 + sendPickChannelAlert 삭제) / X4(CH3 매크로 다이제스트 cron) / X5(CH4 주간 자기비판 리포트) / X6(/channel_test + 손절 카운트다운). 누적 변경: alertRouter SSOT + 4채널 카테고리 시멘틱 별칭 + VIBRATION_POLICY 매트릭스 + sendPrivateAlert + ChannelBoundary lint + SensitiveAlerts lint + 11 callsite 마이그레이션 + 매크로 다이제스트 1일 2회 cron + 주간 자기비판 일요일 19:00 cron + /channel_test 4채널 헬스체크. 후속 PR 잔여: 없음 (사용자 12 아이디어 완료). | `docs/adr/0042-channel-test-and-stop-countdown.md` (신규), `server/trading/exitEngine/rules/stopApproachAlert.ts` (sendTelegramAlert → sendPrivateAlert + ADR-0042 메모), `server/telegram/commands/alert/channelTest.cmd.ts` (4채널 헬스체크로 확장 + formatChannelHealthCheckResult export), `server/telegram/commands/alert/channelTest.test.ts` (신규 15 케이스), `server/trading/exitEngine/rules/__tests__/stopApproachAlert.test.ts` (mock sendPrivateAlert 정합화), `server/trading/exitEngine.atrIntegration.test.ts` (mock sendPrivateAlert 추가), `ARCHITECTURE.md` (+/channel_test + 손절 카운트다운 boundary rule) | 사용자 12 아이디어 모두 완주. 텔레그램 4채널 운영 정책이 SSOT(alertRouter) 단일 진입점 + VIBRATION_POLICY 매트릭스 + 자동 cron + 자기비판 리포트 + 헬스체크 명령으로 완성됨. CH1~CH4 + 개인 DM 5개 회선이 모두 코드 레벨에서 명확히 분리되고, 잔고 키워드/채널 ID 직접 접근/이중 발송 등 회귀 위험을 lint 가드 + 회귀 테스트 + 시멘틱 별칭으로 영구 차단. |

| 2026-04-26 | PR-Y1 F2W Drift Detector — 자기학습 가중치 폭주 감시 메타 회로 (ADR-0046, 사용자 4 아이디어 중 1번): 사용자 응답 "1" — F2W 자동 가중치 조정의 *변화의 변화* 를 감시하는 메타 안전망. ADR-0027 Shadow Model 인프라 재활용 + ADR-0007 Learning Feedback Loop Policy + PR-X1/X4/X5 텔레그램 SSOT 위에서 동작. ① **`src/services/quant/f2wDriftDetector.ts` 신규** (272줄, @responsibility SRP 통과) — `computeWeightStdDev(weights)` 27조건 σ 순수 함수 (NaN/Infinity 안전 + 단일값 0 + 빈 입력 0) + `recordWeightSnapshot(weights, now)` localStorage 90일 ring buffer 영속 (`k-stock-f2w-weight-history` 키) + `evaluateDrift(history, now)` σ7d ≥ σ30d × 2 임계 판정 (사용자 원안 그대로) + `pauseF2W/isF2WPausedUntil/clearF2WPause` pause flag 7일 TTL 영속 (`k-stock-f2w-pause-state` 키) + `getTopDeviatingConditions(weights, 3)` mean 편차 Top N + `LEARNING_F2W_DRIFT_DISABLED` 환경변수 롤백 스위치 + `__resetF2WDriftStateForTests` 테스트 헬퍼 + `F2W_DRIFT_CONSTANTS` 상수 SSOT (DRIFT_RATIO_THRESHOLD=2.0 / PAUSE_TTL_DAYS=7 / MIN_SAMPLES_FOR_DRIFT=5 / HISTORY_MAX_DAYS=90). ② **`feedbackLoopEngine.evaluateFeedbackLoop` 가드 wiring** — 진입부 본 사이클 가중치 결정 직후 (a) `recordWeightSnapshot` 으로 항상 σ 누적, (b) `evaluateDrift` 판정, (c) 기존 pause 활성 또는 신규 drift 시 saveEvolutionWeights 차단 + pause 7일 설정 + `pauseStatus` 결과 객체 옵셔널 필드. shadow=true 호출은 본 가드 우회 (ADR-0027 grace 보존). `summary` 메시지가 drift 시 "F2W drift 감지로 가중치 동결 중" 으로 자동 분기. + `buildDriftAlertPayload` 헬퍼 신설 — 호출자가 `POST /api/learning/f2w-drift-alert` 로 전송하는 페이로드 빌더 (sigma7d/sigma30dAvg/ratio/pausedUntil/reason/topConditions). ③ **`FeedbackLoopResult.pauseStatus?` 옵셔널 필드** (`src/types/portfolio.ts`) — `{ paused: boolean; until?: string; reason?: string; ratio?: number; sigma7d?: number; sigma30dAvg?: number; }`. UI 가 가중치 동결 상태를 사용자에게 표면화 가능 (후속 PR). ④ **클라이언트↔서버 분리 — `POST /api/learning/f2w-drift-alert`** — F2W 학습 회로는 클라이언트 측 (localStorage 기반), 텔레그램 발송은 서버 측 (`server/alerts/`) 이라 클라이언트가 직접 dispatchAlert import 불가. learningRouter.ts 에 POST endpoint 추가 + `server/alerts/f2wDriftAlert.ts` 신규 (210줄) 가 페이로드 검증(필수 6 필드) → `formatF2WDriftMessage(payload, now)` 메시지 빌드 → `dispatchAlert(ChannelSemantic.JOURNAL, ...)` (CH4 자동 발행, 24h dedupeKey `f2w_drift_detected:{KST}`) + `sendPrivateAlert(...)` (운영자 DM 즉각 인지, 24h dedupeKey `f2w_drift_private:{KST}`) 일괄. dispatchAlert throw 시 sendPrivateAlert 는 시도 (graceful degradation — 둘 중 하나라도 성공하면 ok=true). 메시지에 σ7d/σ30d/ratio/일시정지 만료 KST/의심 조건 Top 3 (이름 + 가중치 + 편차) 노출, **잔고 키워드 0건 + 6자리 종목 코드 0건** (CH4 JOURNAL 정체성 보존 — 회귀 테스트 자동 검증). 사용자 명언 "변화는 영양이지만 변화의 변화는 독" + "shadow 학습은 계속 (LIVE 가중치만 동결)" + "/clear_f2w_pause 로 수동 해제 가능" 안내 푸터. ⑤ **회귀 테스트 46 케이스 신규** — `f2wDriftDetector.test.ts` 21 (computeWeightStdDev 5: 빈/단일/동일/정규분포/NaN-Infinity + recordWeightSnapshot 3: 첫 누적/90일 trim/손상 fallback + evaluateDrift 7: 표본 부족/7일 부재/정상/임계 도달/임계 미달/30d=0/disabled env + pauseF2W 5: 미설정 null/7일 TTL/만료 자동 해제/수동 해제/손상 자동 청소 + getTopDeviatingConditions 3: 빈/Top 3/topN 초과 + NaN 제외 1 + 상수 1) + `quantEngine.feedbackLoop.driftGuard.test.ts` 9 (첫 호출 정상/shadow=true 우회/사전 pause 차단/disabled env/히스토리 누적/신규 drift 감지/buildDriftAlertPayload 정상/필드 누락 fallback) + `f2wDriftAlert.test.ts` 16 (formatF2WDriftMessage 6: 전체 섹션/Top 0건/조건 fallback/잔고 키워드 0/종목 코드 0/잘못된 날짜 + handleF2WDriftAlert 7: 정상/잘못된 페이로드/null/배열 아님/dispatchAlert throw graceful/sendPrivateAlert throw graceful/둘 다 throw). ⑥ **검증** — vitest src/services/quant **19 files 271/271 pass** + server/alerts **12 files 164/164 pass** (신규 46 + 무회귀 전체 PR-A~K 자기학습 시리즈 + PR-X1~X6 텔레그램 시리즈 무회귀) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경. ⑦ **사용자 4 아이디어 잔여 (후속 브랜치)** — PR-Y2 Reflection Module Half-Life (18 모듈 영향률 추적 → silent/deprecated) / PR-Y3 Cross-Channel Contradiction Auditor (CH1~CH4 의도 벡터 코사인 유사도) / PR-Y4 Learning Coverage Heatmap (27×6×4=648 셀 밀도 + F2W 30건 게이트). | `docs/adr/0046-f2w-drift-detector.md` (신규), `src/services/quant/f2wDriftDetector.ts` (신규 272줄), `src/services/quant/f2wDriftDetector.test.ts` (신규 21 케이스), `src/services/quant/feedbackLoopEngine.ts` (가드 wiring + buildDriftAlertPayload export), `src/services/quantEngine.feedbackLoop.driftGuard.test.ts` (신규 9 케이스), `src/types/portfolio.ts` (FeedbackLoopResult.pauseStatus? 옵셔널 필드), `server/alerts/f2wDriftAlert.ts` (신규 210줄), `server/alerts/f2wDriftAlert.test.ts` (신규 16 케이스), `server/routes/learningRouter.ts` (+POST /f2w-drift-alert endpoint), `ARCHITECTURE.md` (+2 boundary +1 boundary rule) | 사용자 4 아이디어 중 1번 채택 — 브랜치 `claude/add-drift-detector-Ueere` 이름과 일치. F2W 가 30거래 누적 후 자동 보정 시작 → 단일 시장 국면 과적합 위험을 σ 변화의 변화로 감시. shadow 학습은 계속 — F2W 만 동결 (ADR-0027 grace 보존). 7일 TTL 자동 해제 + 운영자 수동 해제 가능. PR-Y2/Y3/Y4 후속 브랜치로 분리 — 본 PR 은 PR-Y1 단독 진행으로 회귀 위험 격리. |

| 2026-04-26 | PR-Y2 Reflection Module Half-Life — 자기학습 반성 모듈 자연선택 (ADR-0047, 사용자 4 아이디어 중 2번): 사용자 응답 "이대로 진행" — PR-Y1 (F2W Drift) 후속. 사용자 원안 "13개 reflection 모듈이 모두 평등하게 가치 있을까? 영향률 5% 미만 → silent / 1% 미만 → deprecated. 반성도 비용이다." (사용자 추정 18 → 실제 audit 13 모듈). ① **`server/persistence/reflectionImpactRepo.ts` 신규** (165줄) — 모듈별 일자별 meaningful boolean 영속 SSOT (`data/reflection-impact.json`, atomic write tmp→rename, 365일 ring buffer). API: `recordReflectionImpact(module, date, meaningful, now?)` (동일 키 덮어쓰기 idempotent + 잘못된 date format throw + 1년 이전 자동 trim) + `loadReflectionImpactRecords()` + `getModuleStats(module, days?, now?)` (runs/meaningfulRuns/impactRate/firstSeenAt) + `getAllModuleStats(days?, now?)` (impactRate 오름차순). ② **`server/learning/reflectionImpactPolicy.ts` 신규** (135줄) — status 판정 SSOT (`normal` | `grace` | `silent` | `deprecated`) + `KNOWN_REFLECTION_MODULES` 13개 카탈로그 (mainReflection/personaRoundTable/fiveWhy/counterfactual/conditionConfession/regretQuantifier/biasHeatmap/experimentProposal/narrativeGenerator/manualExitReview/metaDecisionJournal/weeklyReflectionAudit/reflectionGemini). 결정 트리: ENV 무력화 → firstSeenAt 부재/30일 grace → 표본 < 20 grace → 영향률 < 1% deprecated → < 5% silent → 그 외 normal. `LEARNING_REFLECTION_HALFLIFE_DISABLED=true` 환경변수 회로 무력화. ③ **`server/learning/reflectionImpactRecorder.ts` 신규** (130줄) — nightlyReflection report 로부터 13 모듈 meaningful 일괄 추론. `inferModuleImpacts(report, extras?)` 추론 규칙: integrity.parseFailed=true → mainReflection/reflectionGemini false / keyLessons/questionableDecisions/tomorrowAdjustments 중 1+ → mainReflection true / personaReview 존재 → personaRoundTable / fiveWhy length>0 / counterfactual.sampleCount>0 / conditionConfession length>0 / regret 존재 / biasHeatmap (extras.biasMaxScore≥0.5 또는 followUpActions sourceIds prefix bias:) / experimentProposal (extras.experimentProposalCount>0 또는 sourceIds prefix exp:) / narrative truthy / manualExitReview count|rolling7dCount > 0. `recordReflectionImpactsFromReport(report, date, now, extras?)` 가 13개 일괄 영속 — 한 모듈 throw 해도 다음 모듈 계속 처리 (fault-tolerant). ④ **nightlyReflectionEngine wiring** — saveReflection 직전 try/catch 1블록 추가 (학습 사이클 보호). 본체 변경 1줄 + import 1개로 광범위 변경 회피. 본 PR 은 *측정만* — 실제 silent/deprecated 가드 wiring (실행 스킵 / 출력 억제) 은 데이터 누적 후 후속 PR. ⑤ **운영자 진단 endpoint** — `GET /api/learning/reflection-impact?days=180` 추가 — 13 모듈의 status + impactRate + runs/meaningfulRuns + firstSeenAt + ageDays 일괄 + summary (normal/grace/silent/deprecated 카운트) 반환. ⑥ **`paths.ts` REFLECTION_IMPACT_FILE 경로 추가** + ARCHITECTURE.md +3 모듈 boundary +1 boundary rule. ⑦ **회귀 테스트 35 케이스 신규** — `reflectionImpactRepo.test.ts` 9 (빈 파일/round-trip/중복 덮어쓰기/잘못된 date throw/365일 trim/JSON 손상 fallback/getModuleStats 0건/60% impactRate/getAllModuleStats 정렬) + `reflectionImpactPolicy.test.ts` 11 (데이터 부재 grace/grace period 30일/표본 < 20 grace/영향률 0% deprecated/3% silent/10% normal/boundary 5% normal/boundary 1% silent/disabled env normal/getAllModuleStatuses 13개/카탈로그 drift 가드) + `reflectionImpactRecorder.test.ts` 15 (빈 report 모든 false/keyLessons → main+reflectionGemini true/parseFailed false/geminiCallSucceeded false/biasMaxScore 임계/sourceIds bias prefix/experimentProposalCount/sourceIds exp prefix/counterfactual sampleCount/manualExitReview count|rolling7d/narrative empty/13개 영속/throw fault-tolerant/실제 영향률 통계). ⑧ **검증** — vitest server/learning + server/persistence **49 files 364/364 pass** (신규 35 + 무회귀 전체 PR-A~K + PR-X1~X6 + PR-Y1) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경. ⑨ **사용자 4 아이디어 잔여 (후속 브랜치)** — PR-Y3 Cross-Channel Contradiction Auditor (CH1~CH4 의도 벡터 코사인 유사도) / PR-Y4 Learning Coverage Heatmap (27×6×4=648 셀 밀도 + F2W 30건 게이트) / 본 PR Phase 2 (silent/deprecated 가드 wiring — 데이터 누적 후). | `docs/adr/0047-reflection-module-halflife.md` (신규), `server/persistence/paths.ts` (+REFLECTION_IMPACT_FILE), `server/persistence/reflectionImpactRepo.ts` (신규 165줄), `server/persistence/reflectionImpactRepo.test.ts` (신규 9 케이스), `server/learning/reflectionImpactPolicy.ts` (신규 135줄), `server/learning/reflectionImpactPolicy.test.ts` (신규 11 케이스), `server/learning/reflectionImpactRecorder.ts` (신규 130줄), `server/learning/reflectionImpactRecorder.test.ts` (신규 15 케이스), `server/learning/nightlyReflectionEngine.ts` (saveReflection 직전 try/catch 1블록 + import 1개), `server/routes/learningRouter.ts` (+GET /reflection-impact endpoint), `ARCHITECTURE.md` (+3 boundary +1 boundary rule) | 사용자 4 아이디어 중 2번 채택 — 매일 KST 19:00 13개 모듈 일괄 실행 + 모든 모듈이 평등하게 가치 있다는 가정의 메타 안전망. 본 PR 은 *측정만* (안전성 우선) — 데이터 누적 후 후속 PR 에서 실제 silent/deprecated 가드 wiring. 결과: 영향률 데이터를 운영자가 매일 `/reflection-impact` endpoint 로 확인 가능, 6개월 누적 후 영향 적은 모듈을 객관적 데이터로 정리 가능. PR-Y3/Y4 후속 브랜치로 분리. |

| 2026-04-26 | PR-Y4 Learning Coverage Heatmap — F2W 데이터 밀도 게이트 (ADR-0048, 사용자 4 아이디어 중 4번, 가벼운 마무리): 사용자 응답 "필요한 부분만 하고 마무리하자" — PR-Y3 (Cross-Channel Contradiction) 의 의도-임베딩 부담 vs 가치 trade-off 가 명확하지 않아 (R6_DEFENSE 자동 차단 + emergencyStop + alertRouter SSOT 으로 명백한 모순 대부분 차단됨) 보류, **PR-Y4 만 가벼운 버전으로 마무리**. 사용자 원안 "27 × 6 × 4섹터 = 648 셀" → 본 PR **27 × 7 = 189 셀** (조건 × 레짐, 섹터 축은 후속 PR). 사용자 명언 "데이터가 희소한 셀에서 가중치를 보정하면 노이즈를 학습". ① **`src/services/quant/learningCoverage.ts` 신규** (115줄) — `(조건 × 레짐) 셀 카운트` 순수 함수 SSOT. `countTradesByRegime(relevant)` Map 반환 + `evaluateConditionCoverage(relevant)` `{ sufficient, maxCellCount }` (어떤 셀도 30건 이상이면 sufficient=true) + `buildCoverageMatrix(closedTrades, conditionIds)` 운영자 진단 매트릭스 + `RegimeKey` 7 union (RECOVERY/EXPANSION/SLOWDOWN/RECESSION/RANGE_BOUND/UNCERTAIN/CRISIS, ADR-0024 ALL_REGIMES 와 동일한 자체 사본 — **순환 import 차단**: regimeMemoryBank → feedbackLoopEngine → learningCoverage → regimeMemoryBank 회피) + `FALLBACK_REGIME=UNCERTAIN` (entryRegime 부재 v1 레코드 그룹화) + `LEARNING_COVERAGE_GATE_DISABLED=true` 환경변수 회로 무력화 + `COVERAGE_THRESHOLD=30` 사용자 원안 임계 + `LEARNING_COVERAGE_CONSTANTS` SSOT export. ② **`feedbackLoopEngine` 가드 wiring** — 조건별 relevant trades 필터링 직후 `evaluateConditionCoverage(relevant)` 호출 → sufficient=false 시 `coverageGated.push({ conditionId, maxCellCount, reason: INSUFFICIENT_COVERAGE })` 후 continue (가중치 보정 스킵). `MIN_CONDITION_TRADES=5` 가드는 그대로 유지 — coverage 게이트는 그 위 추가 안전망. 기존 PR-Y1 drift 가드와 PR-J shadow 우회 패턴 보존. ③ **`FeedbackLoopResult.coverageGated?` 옵셔널 필드** (`src/types/portfolio.ts`) — `Array<{ conditionId, maxCellCount, reason: INSUFFICIENT_COVERAGE }>`. UI 가 데이터 부족으로 보정 스킵된 조건 표면화 가능. 빈 배열 시 undefined 로 정규화. ④ **영속 데이터 신규 0건** — `TradeRecord.entryRegime` (PR-G ADR-0024) 재활용. 셀 카운트는 evaluateFeedbackLoop 호출 시점에 매번 메모리 계산 (가벼움). ⑤ **회귀 테스트 22 케이스 신규** — `learningCoverage.test.ts` 15 (countTradesByRegime 5: 빈/단일/다중/entryRegime 부재 fallback/알 수 없는 regime + evaluateConditionCoverage 6: 빈/단일 30 boundary/29 미달/다중 분산 미달/한 셀 30+ 충족/disabled env + buildCoverageMatrix 3: 빈/조건별 분포 정확/conditionScoreThreshold 인자 + 상수 1) + `quantEngine.feedbackLoop.coverageGate.test.ts` 7 (단일 레짐 30건 sufficient/다중 분산 15+15 gated/29건 calibrationActive false/한 셀 30+ sufficient/disabled env 우회/shadow=true 게이트 활성 drift 우회/v1 레코드 UNCERTAIN fallback). ⑥ **기존 회귀 테스트 정합화** — PR-Y4 의 의도된 효과로 기존 6 테스트 깨짐(makeTrades 10건 → 게이트 미달). 정정: makeTrade 헬퍼에 `entryRegime: EXPANSION` 명시 + makeTrades 호출 갯수 10→30 일괄 + filler 패턴 정리 + learningShadowModel 의 5 conditions × 6 → 5 × 30 = 150 trades. 기존 의미 변화 없는 어구 변경 — "5건 이상 데이터" → "30건 이상 데이터 (ADR-0048 coverage 게이트 통과)". ⑦ **검증** — vitest src/services/quant + feedbackLoop 시리즈 **23 files 309/309 pass** (신규 22 + 기존 정합 8 + 무회귀 전체 PR-A~K + PR-Y1) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — F2W 학습 회로만 영향. ⑧ **사용자 4 아이디어 시리즈 마무리** — PR-Y1 (F2W Drift) ✓ / PR-Y2 (Reflection Half-Life) ✓ / PR-Y3 (Cross-Channel) 보류 (가치 vs 부담 trade-off 불명확) / **PR-Y4 (Learning Coverage) ✓** . 자기학습 메타 안전망 3중 구축: σ 변화 감시(Y1) + 모듈 자연선택(Y2) + 데이터 밀도 게이트(Y4). | `docs/adr/0048-learning-coverage-heatmap.md` (신규), `src/services/quant/learningCoverage.ts` (신규 115줄), `src/services/quant/learningCoverage.test.ts` (신규 15 케이스), `src/services/quant/feedbackLoopEngine.ts` (가드 wiring + import + coverageGated 결과 노출), `src/services/quantEngine.feedbackLoop.coverageGate.test.ts` (신규 7 케이스), `src/types/portfolio.ts` (FeedbackLoopResult.coverageGated 옵셔널 필드), `src/services/quantEngine.feedbackLoop.test.ts` (entryRegime 명시 + 30건 정합), `src/services/quant/learningShadowModel.test.ts` (entryRegime + 30건 정합), `ARCHITECTURE.md` (+1 boundary +1 boundary rule) | 사용자 4 아이디어 가벼운 마무리. F2W 가 30거래 누적 후 자동 보정을 시작하지만 그 30 trade 가 단일 레짐에 집중되어 있으면 다른 레짐 셀의 보정은 0~3건 데이터로 노이즈 학습 — coverage 게이트로 "어떤 셀도 30건 이상이 없으면 해당 조건 보정 스킵". PR-Y1 drift 와 결합하면: drift=σ 변화 감시 + coverage=데이터 밀도 게이트, 자기학습 메타 안전망 2중 보호. PR-Y3 임베딩 작업은 운영 비용·복잡도 vs 가치 trade-off 명확성 부족으로 보류. |
| 2026-04-27 | PR-Z1 섹터 인프라 5중 누수 일소 — Shadow Trade sector 영속 + Correlation Guard fallback + UI 양방향 매칭 (ADR-0060): 사용자 SHADOW 모드 운영 보고 3건(① HRS(238490) 매수됐으나 섹터집중도 패널 미표시 ② 간밤 SOXX 크게 상승했는데 시스템은 반도체 미선택·원자력 매수 ③ 전반적 섹터 집중·로테이션 미작동) 의 단일 근본 원인 — *섹터 SSOT 가 5개 모듈에서 서로 다른 필드를 사용 + 매수 영속 단계에서 sector 정보 손실* 해소. ① **`ServerShadowTrade.sector?: string` 옵셔널 필드 신설** — `server/persistence/shadowTradeRepo.ts:404` 에 단일 결정적 섹터 라벨 (getSectorByCode SSOT 결과) 박제 + `loadShadowTrades` 진입부 lazy 백필로 기존 영속 데이터 1회 자연 마이그레이션 (KIS/KRX 호출 0, 인메모리 lookup 만, 디스크는 다음 saveShadowTrades 호출 시 자연 영속). ② **단일 SSOT 영속 wiring** — `server/trading/buyPipeline.ts:188 buildBuyTrade` 반환 객체에 `sector: getSectorByCode(p.stockCode) ?? undefined` 한 줄 추가, `BuildBuyTradeParams` 시그니처 무변경 → 4 매수 호출 site (perSymbolEvaluation 라인 396/531/941/1173) 무수정. `server/routes/autoTrade/shadowRouter.ts:47` POST 직접 객체 리터럴 1건 별도 추가 wiring. ③ **sectorConcentrationGate fallback** (`server/trading/signalScanner/entryGates/sectorConcentrationGate.ts:17`) — 기존 `if (!stock.sector) return PASS` silent 분기를 `const candidateSector = stock.sector ?? getSectorByCode(stock.code) ?? null` 패턴으로 교체 (sectorPreGuardGate 정착 패턴 차용). watchlist[].sector 도 동일 fallback 대칭 적용 — 같은 섹터 다중 매수 가드가 STOCK_UNIVERSE 시드 sector 부재로 무력화되던 누수 차단. **진입 결정 로직 무변경** — fallback 만 추가, stock.sector 있으면 기존 동작 100% 보존. ④ **클라이언트 동기 사본 3곳** (절대 규칙 #3) — `src/api/autoTradeClient.ts:262` ServerShadowTrade 미러 / `src/types/portfolio.ts:478` ShadowTrade / `src/stores/useShadowTradeStore.ts:39` mapServerToClient propagate 모두 `sector?: string` 추가. ⑤ **UI 매칭 양방향 fallback** (`src/utils/sectorStockMatch.ts`) — `stockMatchesSector` 가 기존 relatedSectors 우선 매칭 보존 + stock.sector 단일 필드 case-insensitive fallback 추가. `filterStocksBySector` 를 `T extends { code?: string } & SectorMatchable` generic 으로 확장해 ShadowTrade-shaped 도 dedupe (code 기준). ⑥ **SectorStocksDrilldown 활성 trade 데이터 소스 추가** — `useShadowTradeStore` 활성 PENDING/ACTIVE shadow 만 merged 배열에 합산 + `mapShadowsToCardLike` 헬퍼로 StockRecommendation-shaped subset 변환. 카드에 amber "보유" 뱃지 + relatedSectors 부재 시 sector 단일 필드 fallback 표시. `StockRecommendation.type` union 무변경, 컴포넌트 로컬 `CardLikeStock.type` 에만 'HOLDING' 추가. 신규 fetch 0. ⑦ **회귀 테스트 19 케이스 신규** — shadowTradeRepo.test.ts +5 (sector round-trip / 레거시 백필 / 명시값 보존 / 미커버 '미분류' fallback / 디스크 영속) + buyPipeline.test.ts 신규 +3 (buildBuyTrade sector 매핑 / 미커버 / shadowRouter 정합) + sectorConcentrationGate.test.ts +4 (fallback hit 가드 / 둘 다 부재 pass / watchlist 대칭 / sector 명시 시 fallback 미호출) + sectorStockMatch.test.ts +3 (sector 단일 매칭 / relatedSectors 우선순위 / dedupe) + SectorStocksDrilldown.test.tsx 신규 +4 (활성 ACTIVE 매칭 / closed 제외 / 동일 code dedupe / 빈 placeholder). ⑧ **ADR-0060 발행** + ARCHITECTURE.md Boundary Rules 3종 명문화 (ServerShadowTrade.sector 박제 SSOT / 섹터 게이트 fallback 통일 / UI 양방향 매칭). ⑨ **잔여 운영 안내 (P0 별도)**: `data/krx-sector-map.json` 부재 시 `npx tsx scripts/updateSectorMap.ts` 실행 권장 — manual SECTOR_MAP override (~150종목) 외 KRX 자동맵으로 전종목 sector 커버리지 확보. ⑩ **검증**: vitest **275 files 2798/2798 pass** (신규 19 + 기존 무회귀) + lint(client + server tsc) + validate:all 8종 + precommit 본체 전체 통과. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 본체 0줄 변경. ⑪ **결과**: 매수 즉시 sector 정보 영속 → UI 섹터집중도 패널이 활성 SHADOW trade 까지 매칭 (HRS 시나리오 해소) / sectorConcentrationGate 가 sector 부재 종목도 fallback 후 비교 → 같은 섹터 다중 매수 차단 정상화 (전반적 섹터 집중 미작동 해소) / SOXX→반도체 부스트 wiring 자체는 정상 (보고 ②의 잔여 원인은 KRX 자동맵 부재로 반도체 중소형주 다수가 '미분류' 폴백되는 P0 운영 안내 영역). | `docs/adr/0050-shadow-trade-sector-persistence.md` (신규), `server/persistence/shadowTradeRepo.ts` (+sector? 필드 + lazy 백필), `server/trading/buyPipeline.ts` (+buildBuyTrade sector wiring), `server/routes/autoTrade/shadowRouter.ts` (+POST 리터럴 sector), `server/trading/signalScanner/entryGates/sectorConcentrationGate.ts` (+fallback 대칭), `server/persistence/shadowTradeRepo.test.ts` (+5), `server/trading/buyPipeline.test.ts` (신규 +3), `server/trading/signalScanner/entryGates/__tests__/sectorConcentrationGate.test.ts` (+4), `src/api/autoTradeClient.ts` (+ServerShadowTrade.sector?), `src/types/portfolio.ts` (+ShadowTrade.sector?), `src/stores/useShadowTradeStore.ts` (+mapServerToClient propagate), `src/utils/sectorStockMatch.ts` (양방향 fallback + filterStocksBySector generic 화), `src/utils/sectorStockMatch.test.ts` (+3), `src/components/sector/SectorStocksDrilldown.tsx` (+useShadowTradeStore + mapShadowsToCardLike + 보유 뱃지), `src/components/sector/SectorStocksDrilldown.test.tsx` (신규 +4), `ARCHITECTURE.md` (+3 boundary +3 rule) | 사용자 SHADOW 모드 운영 보고 3건의 단일 근본 원인 일소 — 섹터 SSOT 가 진입 후 영속 단계에서 *손실되던* 구조적 결함 차단. 자동매매 본체 0줄 변경으로 회귀 위험 격리 + 19 신규 회귀 케이스로 향후 변경 보호. |
| 2026-04-26 | PR-Z1 자동매매 UI 컨텍스트 어댑티브 레이아웃 (ADR-0049, 사용자 "자동매매 UI 한번 더 개선" 10 아이디어 Phase 1-1 / 아이디어 3): 사용자 응답 "1번부터 순차적으로 합시다" — 옵션 1 (Phase 1 부터 순차) Phase 1-1 첫 번째 PR. AutoTradePage 가 24시간 동일 컴포넌트 정렬을 보여주던 구조에서, 시장 컨텍스트(PRE_MARKET / LIVE_MARKET / POST_MARKET / OVERNIGHT / WEEKEND_HOLIDAY)에 따라 컴포넌트 노출 우선순위와 정렬이 자동으로 바뀌도록 컨테이너 레이어 추가. **기존 컴포넌트 본체 0줄 수정** — wrap 만으로. ① **`src/hooks/useAutoTradeContext.ts` 신규** (≤80 LoC, @responsibility SRP) — `AutoTradeContext` 5값 union + `classifyAutoTradeContext(mode, now)` 순수 함수 + `classifyAutoTradeContextFromNow(now)` mode 자체 도출 + `useAutoTradeContext()` 1분 polling hook. ADR-0049 §2.2 매핑 표 SSOT — KST 분 단위 경계값(510=08:30 / 540=09:00 / 930=15:30 / 960=16:00) const 명문화. DEGRADED → LIVE_MARKET 안전 fallback. 주말 시각이 LIVE_TRADING_DAY 로 잘못 들어와도 isKstWeekend 안전망. ② **`src/components/autoTrading/AutoTradeContextSection.tsx` 신규** (≤80 LoC) — marker 컴포넌트. `priorityByContext` (1=최우선~9=숨김, 미지정 5) + `collapsedByContext` (true 시 `<details>` 접힘, 미지정 false) props 만 declare, children 그대로 노출. resolvePriority/resolveCollapsed/isHidden 헬퍼 export. ③ **`src/components/autoTrading/AutoTradeContextualLayout.tsx` 신규** (≤120 LoC) — `collectSections(children, ctx)` 순수 함수가 React.Children.forEach 로 AutoTradeContextSection 만 추출 + Fragment 재귀 traverse (조건부 렌더 `{cond && <Section/>}` 자연 동작) + priority 오름차순 + 동률 originalIndex 안정 정렬. `<details>` collapsed 분기 + ContextLabelStrip (PRE_MARKET 🌅 / LIVE_MARKET 🟢 / POST_MARKET 🌇 / OVERNIGHT 🌙 / WEEKEND_HOLIDAY 🛌). data-testid + data-context + data-priority + data-collapsed 어트리뷰트. ④ **`src/pages/AutoTradePage.tsx` 통합** — 8개 기존 컴포넌트(ProDiagnosticsStrip / AutoTradeHeroKpis / ApiConnectionLamps / TelegramConnectionTest / EngineHealthBanner / CompositeVerdictCard / AutoTradingControlCenter / AutoTradeTabbedView)를 `<AutoTradeContextSection>` 으로 wrap. PRE_MARKET 1순위 = 컨트롤 센터(엔진 무장 준비), LIVE 1순위 = 세부 탭(신호 큐+포지션), POST_MARKET 1순위 = Hero KPI(결산), OVERNIGHT pro-diagnostics/api-lamps/telegram-test 접힘, WEEKEND_HOLIDAY 는 세부 탭/api-lamps/telegram-test 모두 접힘. 본체 함수 시그니처/state/effect/handler 0줄 수정 — wrap+priority 정책 declare 만. ⑤ **회귀 테스트 39 케이스 신규** — `useAutoTradeContext.test.ts` 24 (PRE_MARKET 4 경계값 / LIVE_MARKET 3 / POST_MARKET 3 / OVERNIGHT 3 / WEEKEND_HOLIDAY 4 (HOLIDAY_CACHE 포함 + 주말 시각 LIVE_TRADING_DAY 잘못 들어옴 안전망) / DEGRADED 2 / classifyAutoTradeContextFromNow 5 mode 자체 도출 일관성) + `AutoTradeContextualLayout.test.tsx` 15 (resolvePriority 3 + resolveCollapsed 3 + isHidden 3 + collectSections 6: priority 정렬 / 동률 안정 정렬 / hidden 제외 / 비-section 무시 / 미지정 priority DEFAULT(5) / collapsed 메타데이터 보존). Fragment recursion 추가로 `<>...</>` 테스트 wrapper 자연 동작. ⑥ **검증** — vitest **39/39 신규 pass + 무회귀** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정, UI-only). LIVE 매매 본체 0줄 변경. ⑦ **결과** — 사용자가 KST 08:30 / 10:00 / 15:45 / 22:00 / 토요일에 AutoTradePage 진입 시 화면 1순위가 명확히 다르게 보임. 페르소나 철학 1 "필터링" UI 레벨 강제. 후속 Phase 1-2 (계좌생존 게이지) / Phase 1-3 (무효화 조건 미터) 가 본 PR 의 5 컨텍스트 SSOT 위에 priority=1 로 자연 추가 가능. | `docs/adr/0043-context-adaptive-auto-trade-layout.md` (신규), `src/hooks/useAutoTradeContext.ts` (신규), `src/hooks/useAutoTradeContext.test.ts` (신규 24 케이스), `src/components/autoTrading/AutoTradeContextSection.tsx` (신규), `src/components/autoTrading/AutoTradeContextualLayout.tsx` (신규), `src/components/autoTrading/AutoTradeContextualLayout.test.tsx` (신규 15 케이스), `src/pages/AutoTradePage.tsx` (8개 컴포넌트 wrap, 본체 무수정), `ARCHITECTURE.md` (+3 모듈 boundary +AutoTradeContextualLayout boundary rule) | 사용자 10 아이디어 중 Phase 1 의 foundation — 후속 아이디어들이 올라탈 레이아웃 컨테이너. MarketDataMode 자산을 데이터 페치 정책에서 UI 레이아웃 정렬 결정 입력으로 격상. byte-equivalent wrap 패턴으로 회귀 위험 격리. |
| 2026-04-26 | PR-Z2 자동매매 UI 계좌 생존 게이지 (ADR-0050, 사용자 10 아이디어 Phase 1-2 / 아이디어 1): 사용자 응답 "네" — Phase 1-1 (PR-Z1 컨텍스트 어댑티브 레이아웃) 후속. 사용자 천재 아이디어 1번 "계좌생존 게이지 — 단일 화면 최상단" 처리. AutoTradePage 의 Hero KPI(매출/포지션/주문/신호) 4-카드가 모두 *기회 지표* 라는 비대칭 — 페르소나 철학 1·2·8 ("필터링·생존·손절은 운영비") 의 *생존 지표* 가 UI 부재였던 갭 해소. 백엔드 자산 재활용 — 새 영속 SSOT 0건. ① **`server/health/survival.ts` 신규** (≤200 LoC, @responsibility SRP) — `collectSurvivalSnapshot()` SSOT + 순수 분류 함수 5종(classifyDailyLossTier / classifySectorTier / classifyKellyTier / composeOverallTier / computeHhi). 외부 호출 0건 — assessKillSwitch + evaluatePortfolioRisk + computeKellySurface + loadShadowTrades + loadMacroState + getLiveRegime 만 read. ② **3 게이지 SSOT** — (a) Daily Loss Buffer: `(DAILY_LOSS_LIMIT_PCT - currentPct) / limitPct × 100` → 50/25/0% buffer 임계로 OK/WARN/CRITICAL/EMERGENCY 4분기, (b) Sector HHI: `Σ weight² × 10000` → 2500/4000 임계 (DOJ 시장 집중도 분류 정합) + activePositions=0 NA, (c) Kelly Concordance: 활성 포지션 entryKellySnapshot.effectiveKelly 평균 / 현재 레짐 kellySurface 가중평균 → 1.0/1.5 임계 + 표본<5 또는 권고=0 CALIBRATING. ③ **`composeOverallTier`** — max-of-three (worst-of-three) + NA/CALIBRATING 합성 제외 + 모두 비활성이면 OK 안전 fallback. ④ **`server/routes/survivalRouter.ts` 신규** (≤80 LoC) — `GET /api/account/survival` 응답 read-only + 500 fallback. `server/index.ts` 에 `/api/account` 마운트 1줄 추가. ⑤ **`src/api/survivalClient.ts` 신규** (≤60 LoC) — 타입 동기 사본 + `fetchAccountSurvival()` (절대 규칙 #3 서버↔클라 직접 import 금지). ⑥ **`src/components/autoTrading/AccountSurvivalGauge.tsx` 신규** (≤220 LoC) — TanStack Query (60s staleTime + retry 2 + refetchInterval 60s) + 3 게이지 가로 grid (모바일 1열 / 데스크탑 3열) + tier 색상 분기 (OK 녹 / WARN 황 / CRITICAL 적 pulse / EMERGENCY 검정 pulse / NA·CALIBRATING 회색) + GaugeBar progress + EMERGENCY 시 "비상정지 권고" 박스 추가 표시. data-testid + data-tier 속성 (e2e 친화). ⑦ **AutoTradePage 통합** — PR-Z1 의 `<AutoTradeContextSection>` 패턴으로 wrap, 모든 5 컨텍스트(PRE_MARKET/LIVE_MARKET/POST_MARKET/OVERNIGHT/WEEKEND_HOLIDAY) priority=1, AutoTradeContextualLayout 의 첫 번째 자식 위치 (안정 정렬로 최상단 보장). 기존 8개 섹션 정책 무수정. ⑧ **회귀 테스트 51 케이스 신규** — `survival.test.ts` 30 (classifyDailyLossTier 8 경계값 + classifySectorTier 8 + classifyKellyTier 10 + composeOverallTier 7 + computeHhi 6) + `survivalRouter.test.ts` 3 (정상 응답 / throw 시 500 / capturedAt ISO) + `AccountSurvivalGauge.test.tsx` 9 (OK 안전 / EMERGENCY 권고 / NA placeholder / CALIBRATING 학습중 / Kelly CRITICAL ratio / Sector CRITICAL HHI / Daily WARN 버퍼 / fetch 실패 graceful / data-tier e2e 속성). 목표 ≥20 대비 2.5배 달성. ⑨ **검증** — vitest 신규 51/51 pass + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정). LIVE 매매 본체 0줄 변경 — read-only 진단 위젯. ⑩ **결과** — 사용자가 AutoTradePage 진입 즉시 "지금 안전한가?" 단일 질문에 답을 받음. 일일 손실 한도까지 거리 + 섹터 집중도 (HHI) + Kelly 정합도 3 게이지를 단일 카드로 통합. EMERGENCY 시 비상정지 권고 박스 자동 표시. 후속 PR-Z3 (무효화 조건 미터) 가 본 PR 의 SurvivalSnapshot 합성 패턴을 종목별 카드 내부에 자연 확장 가능. | `docs/adr/0044-account-survival-gauge.md` (신규), `server/health/survival.ts` (신규), `server/health/survival.test.ts` (신규 30 케이스), `server/routes/survivalRouter.ts` (신규), `server/routes/survivalRouter.test.ts` (신규 3 케이스), `server/index.ts` (+/api/account 마운트), `src/api/survivalClient.ts` (신규), `src/components/autoTrading/AccountSurvivalGauge.tsx` (신규), `src/components/autoTrading/AccountSurvivalGauge.test.tsx` (신규 9 케이스), `src/pages/AutoTradePage.tsx` (+survival-gauge 섹션 wrap, 본체 무수정), `ARCHITECTURE.md` (+4 모듈 boundary + AccountSurvivalGauge boundary rule) | 사용자 10 아이디어 중 Phase 1-2 — 페르소나 철학 1·2·8 ("필터링 / 생존 / 손절 운영비") UI 레벨 노출. 백엔드 자산(killSwitch + portfolioRiskEngine + kellySurfaceMap + shadowTradeRepo + macroState) 재활용으로 외부 호출 0건. peakEquity 영속 추적은 후속 PR 분리. |
| 2026-04-26 | PR-Z3 자동매매 UI 무효화 조건 미터 (ADR-0051, 사용자 10 아이디어 Phase 1-3 / 아이디어 7): 사용자 응답 "진행" — Phase 1-2 (PR-Z2 계좌 생존 게이지) 후속. 사용자 천재 아이디어 7번 "무효화 조건(Invalidation Trigger) 실시간 카운터" 처리. 페르소나 답변 규칙 "추천 시 반드시 진입 논리, 무효화 조건, 손절 조건, 목표 시나리오를 함께 제시한다" 의 핵심 — 추천 시점에는 텍스트로 제시되지만 *보유 중* 에는 UI 부재였던 갭 해소. 페르소나 철학 9 (보유 효과 + 후회 회피 경계) 직접 방어. **서버 0줄 수정** — 클라이언트 휴리스틱만으로 시작. ① **`src/utils/invalidationConditions.ts` 신규** (≤120 LoC, @responsibility SRP) — 4 카테고리 휴리스틱 평가 SSOT: (a) STOP_LOSS_APPROACH (currentPrice ≤ stopLossPrice × 1.05), (b) LOSS_THRESHOLD (pnlPct ≤ -3), (c) STAGE_ESCALATION (stage ∈ ALERT/EXIT_PREP/FULL_EXIT), (d) TARGET_REACHED (currentPrice ≥ targetPrice1, *긍정* 무효화). `evaluateInvalidationConditions(position)` + `composeInvalidationTier(metCount, evaluableCount)` 두 순수 함수. detail 텍스트가 평가 근거 (현재가/손절가/임계 %) 직접 노출. 임계값 SSOT export (STOP_LOSS_APPROACH_RATIO=1.05 / LOSS_THRESHOLD_PCT=-3). ② **Tier 합성 SSOT** — evaluableCount=0 → NA / metCount=0 → OK / metCount=1 → WARN / metCount≥2 → CRITICAL. ADR-0051 §2.3 표 단일 SSOT. NaN/Infinity 안전 fallback (NA). ③ **`src/components/autoTrading/InvalidationMeter.tsx` 신규** (≤130 LoC) — 컴팩트 인라인 미터: ShieldAlert 아이콘 + 4 dot (충족=tier 색상 / 미충족=회색 / NA=stroke-only) + "{M}/{N}" 카운터 + tier 라벨 ("정상"/"주의"/"재평가 권고"/"평가 불가") + ChevronDown 토글. 클릭 시 expand → 4 조건 row (라벨 / 충족 상태 / detail). 색상: OK 녹 / WARN 황 / CRITICAL 적 pulse / NA 회색. data-testid + data-tier + data-met + data-evaluable 속성 (e2e 친화). ④ **`src/components/autoTrading/PositionLifecyclePanel.tsx` 통합** — 각 카드 LifecycleStageGauge 직후에 `<InvalidationMeter position={position} />` 1줄 추가. 본체 시그니처/state/handler 0줄 수정 — 1 import + 1줄 wrap 만. ⑤ **회귀 테스트 27 케이스 신규** — `invalidationConditions.test.ts` 21 (정상 보유 통합 / STOP_LOSS_APPROACH 4 (경계값 정확히 ×1.05 / 초과 / NA / detail 형식) / LOSS_THRESHOLD 2 (-3 정확 / -2.99 미달) / STAGE_ESCALATION 3 (ALERT / ENTRY / 부재) / TARGET_REACHED 2 (도달 / 부재) / 통합 CRITICAL / 모두 NA / detail 텍스트 / 상수 검증 + composeInvalidationTier 6 (NA/OK/WARN/CRITICAL/3+ 동률/NaN)) + `InvalidationMeter.test.tsx` 6 (OK/WARN/CRITICAL/NA tier + click expand + defaultExpanded). 목표 ≥15 의 1.8배 달성. ⑥ **검증** — vitest **27/27 신규 pass + 무회귀** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정, UI-only). LIVE 매매 본체 0줄 변경. **서버 0줄 변경** (PositionItem 스키마 무수정 — autoTradingMapper 가 매 분 polling 으로 채우는 데이터를 *재해석* 만). ⑦ **결과** — 사용자가 PositionLifecyclePanel 의 각 보유 포지션 카드에서 페르소나 4 카테고리 무효화 중 몇 개가 충족되었는지 즉시 인지. "매수 근거가 사라졌는데도 보유 중" 상태의 즉시 시각화 → 보유 효과/후회 회피 편향 차단. ADR-0050 (계좌 단위 SurvivalSnapshot) + ADR-0051 (포지션 단위 InvalidationMeter) 2 layer 결합으로 전체 시스템 위험 즉시 가시화. 후속 PR (영속 SSOT — ServerShadowTrade.invalidationConditions[]) + Phase 2 oneDecisionResolver (CRITICAL 카드 자동 격상) 에서 본 PR 의 휴리스틱 위에 *진정한 매수 시점 정의* 기반 평가로 격상. **Phase 1 완주** — Phase 1-1 (컨텍스트 어댑티브 레이아웃) + Phase 1-2 (계좌 생존 게이지) + Phase 1-3 (무효화 조건 미터) 3 PR 누적으로 사용자 10 아이디어 중 가장 foundation 인 3개 완료. | `docs/adr/0045-invalidation-meter.md` (신규), `src/utils/invalidationConditions.ts` (신규), `src/utils/invalidationConditions.test.ts` (신규 21 케이스), `src/components/autoTrading/InvalidationMeter.tsx` (신규), `src/components/autoTrading/InvalidationMeter.test.tsx` (신규 6 케이스), `src/components/autoTrading/PositionLifecyclePanel.tsx` (+1 import + 1줄 wrap, 본체 무수정), `ARCHITECTURE.md` (+2 모듈 boundary + InvalidationMeter boundary rule) | 사용자 10 아이디어 중 Phase 1-3 — 페르소나 답변 규칙 4 카테고리(논리/무효화/손절/목표) 중 *무효화* 가 추천 후 보유 중 시각적으로 사라지던 갭 해소. 클라이언트 휴리스틱이라 첫 출시는 근사값이지만 즉시 가치 + 후속 영속 SSOT PR 의 인프라 시드. ADR-0050 와 결합해 계좌 단위(생존)+포지션 단위(무효화) 2 layer 위험 표면화. |
| 2026-04-26 | PR-Z4 자동매매 UI Today's One Decision Card + VOID 모드 (ADR-0052, 사용자 10 아이디어 Phase 2 / 아이디어 4+10 통합): 사용자 응답 "네" — Phase 1 (PR-Z1/Z2/Z3) 후속, Phase 2 진입. 사용자 천재 아이디어 4번(Today's One Decision Card)과 10번(VOID 모드)을 단일 SSOT 모듈에 통합. 페르소나 철학 1 ("필터링") 의 극단적 적용 — 23개 컴포넌트 동시 노출 대신 *지금* 답해야 할 단 하나의 결정만 카드 1장으로 추출. VOID 모드는 페르소나 답변 규칙 "불확실성이 높으면 관망을 정답으로 제시" 의 직접 구현 — 시스템이 자기 자신의 사용을 막는 UX. ① **`server/routes/decisionInputsRouter.ts` 신규** (≤80 LoC, @responsibility SRP) — `GET /api/decision/inputs`: emergencyStop + pendingApprovals (ageMs 내림차순 정렬) + macroSignals (vkospi/vix/vixHistory/bearDefenseMode/fssAlertLevel/regime). 외부 호출 0건 (state.ts + buyApproval + macroStateRepo read). server/index.ts /api/decision 마운트 1줄. ② **`src/api/decisionClient.ts` 신규** (≤60 LoC) — DecisionInputs 타입 동기 사본 + fetchDecisionInputs (절대 규칙 #3 준수). ③ **`src/utils/oneDecisionResolver.ts` 신규** (≤200 LoC) — 6 case 우선순위 SSOT (EMERGENCY_STOP > DAILY_LOSS_EMERGENCY > INVALIDATED_POSITIONS > ACCOUNT_CRITICAL > PENDING_APPROVALS > VOID > MONITORING) + evaluateVoidConditions (4 조건 AND: 높은 변동성 / 활성 포지션 0 / 승인 대기 0 / 거시 리스크) + computeVolatilityZScore (vixHistory stdev 우선, vkospiDayChange fallback, 둘 다 부재 시 0). DecisionTier 5값 (OK/WARN/CRITICAL/EMERGENCY/VOID). PR-Z3 의 evaluateInvalidationConditions 재활용으로 case 2 (INVALIDATED_POSITIONS) 평가. ④ **`src/components/autoTrading/TodayOneDecisionCard.tsx` 신규** (≤220 LoC) — TanStack Query 60s + survival + decisionInputs 두 fetch + activeTrade positions invalidation 평가. tier 색상 5분기 (OK 녹/WARN 황/CRITICAL 적 pulse/EMERGENCY 검정 pulse/VOID 슬레이트 회색). VOID 시 가운데 배치 + 🌑 + "오늘은 진입하지 않는 것이 알파입니다 — SYSTEMATIC ALPHA HUNTER" + 4 조건 detail. 그 외 컴팩트 카드 + headline + detail + 권장 액션 박스. data-testid + data-case + data-tier (e2e 친화). ⑤ **AutoTradePage 통합** — PR-Z1 의 AutoTradeContextSection 안정 정렬로 TodayOneDecisionCard 를 AccountSurvivalGauge 보다 *먼저* 렌더 (children 선언 순서 보존). 모든 5 컨텍스트 priority=1. AutoTradePage 본체 함수/state/handler 0줄 수정 — wrap 만. ⑥ **회귀 테스트 37 케이스 신규** — `decisionInputsRouter.test.ts` 6 (정상 / emergencyStop=true / macroState=null / pendingApprovals 정렬 / throw 500 / vixHistory 그대로 노출) + `oneDecisionResolver.test.ts` 25 (6 case 모두 + 우선순위 충돌 4 (EMERGENCY 최우선 / INVALIDATED > ACCOUNT_CRITICAL / VOID 4조건+ACCOUNT 동시→ACCOUNT 먼저 / 빈 입력) + evaluateVoidConditions 6 (4 충족 / 1 미충족 / vix 부재 fallback / 거시 OR 분기 / 데이터 부재 / null 안전) + computeVolatilityZScore 6 (vix spike / 표본<3 fallback / vkospi 5 ≈ 1.5 / 음수 절댓값 / 데이터 부재 / stdev=0 fallback)) + `TodayOneDecisionCard.test.tsx` 6 (MONITORING / EMERGENCY_STOP / PENDING_APPROVALS / VOID 가운데 배치 / fetch 실패 graceful / suggestedAction 라벨). 목표 ≥25 의 1.5배 달성. ⑦ **검증** — vitest **37/37 신규 pass + 무회귀** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정, UI-only). LIVE 매매 본체 0줄 변경. **신규 영속 SSOT 0건** — Phase 1 자산(SurvivalSnapshot + InvalidationMeter + buyApproval + macroState) 100% 재활용. ⑧ **결과 — Phase 2 완주** — Phase 1 (Z1/Z2/Z3) + Phase 2 (Z4) 누적 4 PR. 사용자가 AutoTradePage 진입 즉시 *지금* 답해야 할 단 하나의 결정을 받음. VOID 모드 활성 시 시스템이 자기 자신의 사용을 막아 보유 효과/후회 회피 편향 차단. 후속 Phase 3 (Nightly Reflection / Concordance Matrix / AI Pre-Gate) 가 본 6 case 트리에 신규 case 자연 추가 가능. | `docs/adr/0046-one-decision-resolver.md` (신규), `server/routes/decisionInputsRouter.ts` (신규), `server/routes/decisionInputsRouter.test.ts` (신규 6 케이스), `server/index.ts` (+/api/decision 마운트), `src/api/decisionClient.ts` (신규), `src/utils/oneDecisionResolver.ts` (신규), `src/utils/oneDecisionResolver.test.ts` (신규 25 케이스), `src/components/autoTrading/TodayOneDecisionCard.tsx` (신규), `src/components/autoTrading/TodayOneDecisionCard.test.tsx` (신규 6 케이스), `src/pages/AutoTradePage.tsx` (+1 섹션 wrap, 본체 무수정), `ARCHITECTURE.md` (+4 모듈 boundary + TodayOneDecisionCard boundary rule) | 사용자 10 아이디어 중 Phase 2 — 아이디어 4 (One Decision) + 아이디어 10 (VOID) 가 함께 단일 SSOT 모듈에 통합되어 페르소나 철학 1+8 ("필터링" + "보유 효과 경계") UI 레벨 강제. 백엔드 자산 100% 재활용으로 새 영속 SSOT 0개. ADR-0050 (계좌 단위) + ADR-0051 (포지션 단위) + ADR-0052 (결정 단위) 3 layer SSOT 결합으로 사용자가 화면 진입 즉시 시스템 위험·결정 모두 인지. |
| 2026-04-26 | PR-Z5 자동매매 UI Nightly Reflection 카드 (ADR-0053, 사용자 10 아이디어 Phase 3-1 / 아이디어 9): 사용자 응답 "진행" — Phase 1+2 (PR-Z1~Z4) 후속, Phase 3 진입. 사용자 천재 아이디어 9번 "Nightly Reflection 결과의 다음 액션 카드" Read-only 1차 구현. nightlyReflectionEngine 의 매일 KST 19:00 학습 결과(keyLessons / tomorrowAdjustments / 5-Why / 편향 heatmap / 실험 제안)가 텔레그램 `/learning_status` 단일 채널에만 노출되어 있던 갭 해소. 페르소나 철학 10 ("지속적 학습") UI 레벨 노출. **서버 0줄 수정** — PR-36 의 `/api/learning/status` 엔드포인트 100% 재활용. ① **`src/api/learningClient.ts` 신규** (≤90 LoC, @responsibility SRP) — LearningStatusSnapshot 타입 동기 사본 (DailyVerdict 4값 / ReflectionMode 5값 / LearningReflectionSummary / LearningExperimentProposal / LearningBiasEntry 등) + `fetchLearningStatus()` 단일 함수. 절대 규칙 #3 (서버↔클라 직접 import 금지) 준수. ② **`src/components/autoTrading/NightlyReflectionCard.tsx` 신규** (≤220 LoC) — TanStack Query 5분 staleTime + 5분 refetchInterval (학습 cron 매일 19:00 1회 갱신). ReflectionPresent 분기: verdict 이모지 4분기 (GOOD_DAY 🟢/MIXED 🟡/BAD_DAY 🔴/SILENT ⚪) + narrative preview (200자 이상 시 …) + 4 StatBox (배운 점/내일 조정/5-Why/활성 실험, 활성 실험 ≥1 시 amber accent) + biasHeatmap7dAvg Top 3 (점수 ≥0.7 적색/≥0.5 황색/그 외 정상) + 활성 실험 violet 박스 (`/learning_status` 안내) + 누락 ≥3일 amber 경고 박스 + 텔레그램 명령 안내 footer. ReflectionAbsent 분기: 30일 내 없음 placeholder + Gemini 예산 mode 표시 + diagnostics.warnings Top 3. data-testid + data-verdict 속성 (e2e 친화). ③ **AutoTradePage 통합** — `viewMode==='pro'` 가드 안에서 AutoTradeContextSection wrap. priority: POST_MARKET=2 (결산 직후 회고) / OVERNIGHT=3 (학습 cron 직후 신선도 최고) / WEEKEND_HOLIDAY=2 (주간 회고 시간) / PRE_MARKET=5 (중간) / LIVE_MARKET=7 (후순위 + collapsed=true, 의사결정 분산 차단). AutoTradePage 본체 함수/state 0줄 수정. ④ **읽기 전용 정책 명문화** — keyLessons/tomorrowAdjustments 본문은 *카운트만* 노출, 정독은 텔레그램 `/learning_status` 단일 채널 유도 (ADR-0007 manual approval 채널 정합 유지). 매뉴얼 승인 인프라(승인/거부 버튼)는 후속 PR. ⑤ **회귀 테스트 13 케이스 신규** — `learningClient.test.ts` 2 (200 정상 / 500 throw) + `NightlyReflectionCard.test.tsx` 11 (GOOD_DAY 🟢 / BAD_DAY 🔴 / MIXED 라벨 / narrative … 절삭 / 4 StatBox + 활성 실험 violet 박스 / biasHeatmap Top 3 + 4번째 미노출 / consecutiveMissingDays ≥ 3 ⚠️ / lastReflection=null placeholder + TEMPLATE_ONLY 모드 / diagnostics.warnings Top 3 / fetch 실패 graceful / mode REDUCED_EOD). 목표 ≥12 만족. ⑥ **검증** — vitest 신규 13/13 + 무회귀 + lint(client + server tsc) + validate:all 8종 + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner 무수정, UI-only). LIVE 매매 본체 0줄 변경. **서버 0줄 변경** — `/api/learning/status` (PR-36) 그대로 재활용. ⑦ **결과** — 사용자가 Pro 모드 + POST_MARKET/OVERNIGHT/WEEKEND 컨텍스트로 AutoTradePage 진입 시 어젯밤 학습 결과 즉시 확인. 학습 cron 의 dailyVerdict + 카운트 + 편향 + 누락 상태가 1 카드로 표면화. ADR-0050 (계좌) + ADR-0051 (포지션) + ADR-0052 (결정) + ADR-0053 (학습) **4 layer SSOT 결합**으로 시스템 위험·결정·학습 모두 표면화. 후속 PR (Phase 3-2 정성적 합치도 매트릭스 / Phase 3-3 AI 사전 게이트 평가) + 매뉴얼 승인 인프라 (실험 제안 승인/거부 버튼) 가 본 PR 의 LearningStatusSnapshot 위에 자연 확장 가능. | `docs/adr/0047-nightly-reflection-card.md` (신규), `src/api/learningClient.ts` (신규), `src/api/learningClient.test.ts` (신규 2 케이스), `src/components/autoTrading/NightlyReflectionCard.tsx` (신규), `src/components/autoTrading/NightlyReflectionCard.test.tsx` (신규 11 케이스), `src/pages/AutoTradePage.tsx` (+1 import + Pro 모드 가드 1 섹션 wrap, 본체 무수정), `ARCHITECTURE.md` (+2 모듈 boundary + NightlyReflectionCard rule) | 사용자 10 아이디어 중 Phase 3-1 — 페르소나 철학 10 ("지속적 학습") 의 사용자 통제 가능성 1차 확보. PR-36 의 텔레그램 `/learning_status` 채널이 UI 로 확장. 매뉴얼 승인 인프라 도입 후 동일 SSOT 위에 승인/거부 버튼 자연 wiring 가능. |
| 2026-04-26 | PR-Z6 정량 × 정성 합치도 매트릭스 (ADR-0054, 사용자 10 아이디어 Phase 3-2 / 아이디어 6): 사용자 응답 "진행" — Phase 3-1 (PR-Z5) 후속. 사용자 천재 아이디어 6번 "정성적 합치도 매트릭스" 구현. Gemini 정성 판단(AI 18 조건 평균)과 4-Gate 정량 판단(실계산 9 조건 평균)의 5×5 교차분석 — diagonal(일치) vs off-diagonal(불일치) 승률 비교로 "두 시스템 일치 = 강한 신호" 메타 룰 검증. **추가 Gemini 호출 0건** — attributionRepo 의 conditionScores 재활용. 페르소나 철학 7 (정량+정성 통합 신뢰도) 데이터 검증. ① **`server/learning/conditionSourceMap.ts` 신규** (≤80 LoC, @responsibility SRP) — 클라 `evolutionEngine.ts CONDITION_SOURCE_MAP` 동기 사본 (REAL_DATA_CONDITIONS 9개 + AI_ESTIMATE_CONDITIONS 18개) + `classifyConditionSource(id)` + `classifyTier(avgScore)` 5분기 (≥8 EXCELLENT / ≥6 GOOD / ≥4 NEUTRAL / ≥2 WEAK / <2 POOR, NaN/Infinity → POOR 안전 fallback) + `averageScoreFor(scores, ids)` (NaN 제외 평균, 빈 입력 → 0) + `ALL_TIERS` 순회 SSOT. 회귀 테스트가 양쪽 SSOT 정합 검증 (변경 시 동시 수정 의무). ② **`server/routes/attributionRouter.ts` 확장** (+120 LoC) — `GET /concordance` 엔드포인트 + `buildConcordanceMatrix(records)` 순수 함수 export (단위 테스트 가능). `loadCurrentSchemaRecords()` 만 read (외부 호출 0건). 25 cell (5×5) 빈 grid 초기화 → 각 trade 의 (quantTier, qualTier) bucket 분류 → wins/losses/returnSum 누적 → diagonal/off-diagonal 통계 합산. capturedAt ISO + totalSamples 메타. ③ **`src/api/concordanceClient.ts` 신규** (≤45 LoC) — ConcordanceMatrix/ConcordanceCell/ConcordanceStats/ConcordanceTier 타입 동기 사본 + `ALL_CONCORDANCE_TIERS` 순회 SSOT + `fetchAttributionConcordance()` (절대 규칙 #3 준수). ④ **`src/components/analysis/ConcordanceMatrix.tsx` 신규** (≤210 LoC) — TanStack Query 5분 staleTime + 5×5 table grid (모바일 가로 스크롤). 각 셀: winRate 색상 분기 (≥60% 녹색 / ≥40% 황색 / <40% 적색 / sample=0 회색) + 샘플 수 + 호버 tooltip (승/패/평균 수익률) + diagonal 셀 ring-2 violet 강조. MetaRuleBox 하단: 표본 ≥30 시 "일치 vs 불일치 승률 + Δ%p + 가설 지지/약한/반대 분기", 표본 <30 시 "표본 부족" amber 경고 + 메타 룰 미렌더. data-testid + data-tier + data-sample 속성 (e2e 친화). ⑤ **`src/pages/RecommendationHistoryPage.tsx` 통합** — ConditionAttributionChart 직후에 `<ConcordanceMatrix />` 1줄 추가. props 0개 (자체 fetch). ⑥ **회귀 테스트 27 케이스 신규** — `conditionSourceMap.test.ts` 12 (REAL_DATA 9 + AI 18 disjoint union 27 / 1~27 모든 ID 매핑 / classifyTier 5 분기 + 경계값 + NaN/Infinity / averageScoreFor / ALL_TIERS 순서) + `attributionConcordance.test.ts` 9 (buildConcordanceMatrix: 빈 records / EXCELLENT-EXCELLENT diagonal / EXCELLENT-WEAK off / 합산 통계 / 25 cell 보장 / avgReturn 정확 + 엔드포인트: 정상 응답 ISO / throw 500) + `ConcordanceMatrix.test.tsx` 6 (5×5=25 cell 렌더 / 메타 룰 박스 / 표본 부족 경고 / diagonal ring / winRate 색상 4분기 / fetch 실패 graceful). 목표 ≥18 의 1.5배 달성. ⑦ **검증** — vitest **27/27 신규 pass** (서버 21 + 클라 6) + 무회귀 + lint(client + server tsc) + validate:all 8종 + precommit 전체 통과. KIS/KRX/Gemini 자동매매 quota 0 침범 (UI-only, 절대 규칙 #2/#3/#4 준수). LIVE 매매 본체 0줄 변경. **추가 Gemini 호출 0건** — 이미 저장된 conditionScores 재활용으로 비용 0 가치 창출. ⑧ **결과** — 사용자가 RecommendationHistoryPage 진입 시 5×5 매트릭스로 "두 시스템 일치 = 강한 신호" 가설 즉시 검증. 메타 룰 통계 (diagonal vs off-diagonal Δ%p) 자동 분기 (≥10%p 가설 지지 / ≥0 약한 / <0 반대). attribution 데이터 활용도 격상 — 27 조건이 가중치 학습뿐 아니라 *교차 합치도* 학습에도 입력. 후속 PR (메타 룰 부스트 — 일치 시그널 발견 시 evolutionEngine 가중치 자동 보정) 가 본 매트릭스 위에 자연 확장. | `docs/adr/0048-concordance-matrix.md` (신규), `server/learning/conditionSourceMap.ts` (신규), `server/learning/conditionSourceMap.test.ts` (신규 12 케이스), `server/routes/attributionRouter.ts` (+/concordance + buildConcordanceMatrix), `server/routes/attributionConcordance.test.ts` (신규 9 케이스), `src/api/concordanceClient.ts` (신규), `src/components/analysis/ConcordanceMatrix.tsx` (신규), `src/components/analysis/ConcordanceMatrix.test.tsx` (신규 6 케이스), `src/pages/RecommendationHistoryPage.tsx` (+1 import +1 줄 임베드), `ARCHITECTURE.md` (+3 모듈 boundary + ConcordanceMatrix rule) | 사용자 10 아이디어 중 Phase 3-2 — 페르소나 철학 7 (정량+정성 통합) 데이터 검증. 추가 비용 0 으로 attribution 데이터 활용도 격상. PR-Z5 (학습 카드) + PR-Z6 (합치도 매트릭스) 결합으로 *학습 결과* 와 *학습 입력 품질* 양쪽 시각화. 후속 PR (메타 룰 자동 부스트) 의 데이터 기반 마련. |
| 2026-04-26 | PR-Z7 자동매매 UI 4-Gate 미니 인디케이터 (ADR-0055, 사용자 10 아이디어 Phase 3-3 / 아이디어 8): 사용자 응답 "계속진행" — Phase 3-2 (PR-Z6) 후속, Phase 3 마지막 PR. 사용자 천재 아이디어 8번 "AI 추천 사전 게이트 평가" 클라이언트 휴리스틱 1차 구현. DiscoverWatchlistPage 의 AI 추천 카드(WatchlistCard) 우상단에 4-Gate(0/1/2/3) 통과 여부 컴팩트 4 dot 인디케이터 추가 — 사용자가 카드 리스트 훑어볼 때 *1초 이내* 인지. 페르소나 철학 1 ("필터링") 카드 단위 노출. **사용자 원안의 서버 SQLite 캐시 + circuit breaker 인프라는 본 PR 미도입** — stock.checklist 가 AI 추천 시점에 이미 채워지므로 추가 서버 호출 0건으로 즉시 가치 제공 가능. 후속 PR 로 분리 (ADR-0055 §6 명문화). ① **`src/utils/gateMiniIndicator.ts` 신규** (≤120 LoC, @responsibility SRP) — `evaluateGateMini(stock)` 순수 함수 SSOT (PR-A `buildGateCardSummary` 와 동일 SSOT 재활용 — `GATE1_IDS`/`GATE2_IDS`/`GATE3_IDS`/`CONDITION_PASS_THRESHOLD`/`CONDITION_ID_TO_CHECKLIST_KEY`) + `classifyGateState(passedCount, totalCount)` 4-tier 분류기 (PASS≥0.5 / PARTIAL≥0.3 / FAIL>0 / NA totalCount=0). Gate 0 특수처리: `gateEvaluation.gate1Passed ?? isPassed` boolean → totalCount=1. NaN/Infinity/음수 안전 fallback (NA). gates 순서 항상 [Gate 0, 1, 2, 3]. ② **`src/components/watchlist/GateMiniIndicator.tsx` 신규** (≤80 LoC) — 4 dot horizontal + tooltip ("Gate 1: 4/5 통과 — 통과") + tier 색상 분기 (PASS 녹/PARTIAL 황/FAIL 적/NA stroke-only border) + "{passCount}/4" 텍스트 (4 emerald / ≥2 amber / 0 zinc-500 / 그 외 zinc-400). data-testid + data-pass-count + data-gate-state 속성 (e2e 친화). aria-label 접근성 부여. ③ **`src/components/watchlist/WatchlistCard.tsx` 통합** (+1 import + 1 줄 wrap) — 카드 헤더 wrap container 의 BEST 배지 다음에 `<GateMiniIndicator stock={stock} />` 추가. 본체 시그니처/state/handler 0줄 수정. PR-A `GateStatusCard` (카드 본문 expand 가능 풀카드) 와 책임 분리 — *미니=즉시 인지*, *풀카드=상세 검토*. ④ **회귀 테스트 22 케이스 신규** — `gateMiniIndicator.test.ts` 16 (classifyGateState 6: PASS 경계값 / PARTIAL 경계값 / FAIL / passedCount=0+total>0 / totalCount=0 / NaN-Infinity-음수 + evaluateGateMini 10: 빈 checklist + gateEvaluation 부재 / gates 순서 / Gate 0 PASS / Gate 0 FAIL fallback / Gate 1 만점 / Gate 2/3 score 4 미달 / 전체 통과 passCount=4 / 부분 통과 1개 / NaN score 안전 / Gate 2 절반 통과 boundary) + `GateMiniIndicator.test.tsx` 6 (4 dot 렌더 + data-testid / NA + FAIL 분류 / 전체 PASS data-pass-count=4 + emerald 텍스트 / NA stroke-only / PASS emerald 색상 / tooltip title 통과 카운트). 목표 ≥12 의 1.8배 달성. ⑤ **검증** — vitest 신규 22/22 + 무회귀 + lint(client + server tsc) + validate:all 8종 + precommit 전체 통과. KIS/KRX 자동매매 quota 0 침범 (UI-only). LIVE 매매 본체 0줄 변경. **서버 0줄 수정** — 클라이언트 stock.checklist 재활용. ⑥ **결과** — 사용자가 DiscoverWatchlistPage 진입 시 N개 추천 카드를 동시에 보면서 4-Gate 통과 여부를 1초 이내 인지. PR-A `GateStatusCard` 풀카드 (상세 검토) 와 함께 표시되어 시각 우선순위 분리. 후속 PR (서버 SQLite 캐시 + circuit breaker) 의 시드 — stock.checklist 부재 케이스 ≥10% 발생 시 자동 트리거. **Phase 1+2+3 완주** — Phase 1 (Z1/Z2/Z3) + Phase 2 (Z4) + Phase 3 (Z5/Z6/Z7) = 7 PR 누적. ADR-0050~0049 6 layer SSOT 결합으로 시스템 위험·결정·학습·합치도·게이트 모두 표면화. | `docs/adr/0049-gate-mini-indicator.md` (신규), `src/utils/gateMiniIndicator.ts` (신규), `src/utils/gateMiniIndicator.test.ts` (신규 16 케이스), `src/components/watchlist/GateMiniIndicator.tsx` (신규), `src/components/watchlist/GateMiniIndicator.test.tsx` (신규 6 케이스), `src/components/watchlist/WatchlistCard.tsx` (+1 import + 1 줄 wrap, 본체 무수정), `ARCHITECTURE.md` (+2 모듈 boundary + GateMiniIndicator rule) | 사용자 10 아이디어 중 Phase 3-3 — 페르소나 철학 1 ("필터링") 카드 단위 즉시 인지. 사용자 원안의 서버 캐시 인프라는 stock.checklist 부재 시 후속 PR 분리 — 본 PR 은 클라이언트 휴리스틱으로 즉시 가치 제공 + 회귀 위험 격리. PR-A GateStatusCard 와 책임 분리로 미니/풀카드 양쪽 노출. **Phase 3 완주 — Phase 1+2+3 누적 7 PR / 216 신규 테스트.** |
| 2026-04-26 | 긴급패치 — channelPerformance 카테고리 미스라우팅 + Reconciliation 일요일 폭발 + 텔레그램 자동완성 확장 (사용자 직접 분석 4종 일괄): 사용자 보고 "긴급패치 실시 — 1) channelPerformance INFO→SYSTEM 라우팅 / 2) 일요일 reconciliation CRITICAL 알림 폭발 (3개 원인) / 3) 텔레그램 / 누르면 명령어 목록 자동완성". ① **Patch 1 — `channelPipeline.ts:418` 카테고리 정정**: `channelPerformance()` 가 일일/주간 거래 성과 리포트(메타 학습·복기) 를 `AlertCategory.INFO` (REGIME 매크로 사령탑) 로 발송하던 ADR-0037 §1 시멘틱 매핑 위반 해소. `dispatchAlert(ChannelSemantic.JOURNAL, ...)` 로 변경 — JOURNAL=SYSTEM 자동 매핑으로 alertRouter 가 weekly_digest 버퍼로 자동 분류 → 일요일 19:00 자기비판 리포트(PR-X5) 와 자연 합쳐짐. import 에 `ChannelSemantic` 추가. ② **Patch 2-1 — `maintenanceJobs.ts:99` ScheduleClass 격하**: `daily_reconcile` cron 을 `ALWAYS_ON` → `TRADING_DAY_ONLY` + cron 표현식 `30 14 * * *` → `30 14 * * 1-5` (UTC 평일 = KST 평일 23:30). 1차 방어선 cron 평일 가드 + 2차 방어선 ScheduleClass KRX 공휴일 차단(어린이날 5/5·추석). ADR-0043 정합 — 비영업일에 `tradeEventCloses.size>0 vs shadowTradeClosedIds.size=0` 구조적 불일치 폭발 차단. ③ **Patch 2-2 — `reconciliationEngine.ts loadTradeEventCloses` 날짜 비대칭 수정**: 함수 시그니처에 `dateKst?: string` 옵셔널 인자 추가 + FULL_SELL 이벤트의 `e.ts` 를 `+9h` KST 일자 변환해 `dateKst` 와 비교. 호출처(라인 147) 가 `dateKst` 전달. 기존엔 yyyymm 월 전체를 카운트해서 A(shadowLogCloses, dateKst 필터) 와 C(shadowTradeClosedIds, dateKst 필터) 와 정확히 다른 시간 창을 비교하던 구조적 비대칭 — 비영업일·재배포·월말마다 mismatch 폭발 → CRITICAL 텔레그램. 본 패치로 A·B·C 세 SSOT 가 같은 시간 창(오늘 KST) 비교. ④ **Patch 2-3 — `dedupeKey` 안정 키 + `category: 'reconcile'` 명시**: `reconcile_fail_${dateKst}` / `reconcile_ok_${dateKst}` → `reconcile_fail` / `reconcile_ok` 단일 키. 날짜별 키가 매일 새 키로 갈라져 audit grouping 의미 잃었고, 새 날짜 진입 시 같은 패턴 mismatch 가 또 발송되던 문제 차단. CRITICAL 우선순위 자체는 유지(cooldown 0 — 정합성 차단 신호 즉시 발송). ⑤ **Patch 3 — 텔레그램 명령어 자동완성 확장 (`buildBotMenuCommandsExtended()` 신규)**: 기존 `buildBotMenuCommands()` 8개 메타 (메뉴 압축 회귀 보존) 위에 `commandRegistry.all()` 의 51개 정식 명령(alias 제외, 카테고리 SYS→MKT→WL→POS→TRD→LRN→ALR→EMR 정렬) 합쳐 자동완성에 노출. Telegram 한도 100개 이내 자동 절삭 + warning. `setTelegramBotCommands()` 가 Extended 함수 사용으로 전환 — 사용자가 `/` 누르면 51+8 명령 자동완성. ADR-0017 Stage 1 의 메뉴 8개 정책은 메타 메뉴 자체에 그대로 유지(`buildBotMenuCommands()` 회귀 27 케이스 통과). ⑥ **회귀 테스트 34 케이스 신규** — `channelPerformanceRoute.test.ts` 5 (함수 정의 / ChannelSemantic import / dispatchAlert(JOURNAL) 호출 / AlertCategory.INFO 사용 없음 / SSOT 매핑 검증) + `menuSyncExtended.test.ts` 9 (확장판 길이 base 초과 + 한도 100 이내 / base 8 그대로 보존 / commandRegistry.all() 정식 name 모두 포함 / alias 중복 노출 없음 / Telegram 제약 매치 / description ≤256 / META_COMMAND_REGISTRY+prelude 포함 / idempotent / Telegram 페이로드 형식 + 카테고리 SYS<EMR 정렬) + `reconciliationDateAlignment.test.ts` 11 (Patch 2-1 ScheduleClass: TRADING_DAY_ONLY 분류 + ALWAYS_ON 회귀 차단 + 평일 가드 / Patch 2-2 시그니처: dateKst 옵셔널 인자 + 호출처 전달 + KST 변환 패턴 / Patch 2-3 dedupeKey: reconcile_fail 안정 키 + reconcile_ok 안정 키 + category 'reconcile' 명시 + 통합 sanity check). + 기존 menuSync.test.ts 8 케이스 그대로 통과 (회귀 보호). ⑦ **검증** — vitest 신규 34/34 + 인접 회귀 server/alerts+telegram 21 files **299/299 pass** + server/trading+scheduler 73 files **577/577 pass** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경. ⑧ **결과** — (a) 매크로 채널이 더 이상 거래 성과로 오염되지 않음, (b) 비영업일/일요일 reconciliation CRITICAL 알림 폭발 영구 차단 (cron 가드 + ScheduleClass + 날짜 정합 3중 안전망), (c) 사용자가 텔레그램 `/` 입력 시 자주 쓰는 51개 alias 도 자동완성 — 메타 메뉴 8개와 alias 모두 접근 가능. | `server/alerts/channelPipeline.ts` (ChannelSemantic import + INFO→JOURNAL 한 곳 교체), `server/alerts/channelPerformanceRoute.test.ts` (신규 5 케이스), `server/scheduler/maintenanceJobs.ts` (ALWAYS_ON→TRADING_DAY_ONLY + cron 1-5 가드), `server/trading/reconciliationEngine.ts` (loadTradeEventCloses dateKst 옵셔널 + KST 일자 필터 + dedupeKey 안정 키 + category 'reconcile'), `server/trading/reconciliationDateAlignment.test.ts` (신규 11 케이스), `server/telegram/metaCommands.ts` (commandRegistry import + buildBotMenuCommandsExtended 신규), `server/telegram/menuSyncExtended.test.ts` (신규 9 케이스), `server/alerts/telegramClient.ts` (setTelegramBotCommands 가 Extended 함수 사용) | 사용자 직접 분석 — 4종 모두 정확한 진단. 4번 자동완성은 ADR-0017 Stage 1 의 "메뉴 8개 압축" 정책과 충돌하지 않도록 *자동완성 확장*과 *메타 메뉴 압축* 책임 분리: 메뉴 자체는 8개 그대로(인지 부담 ↓), 자동완성에는 51개 alias 도 노출(파워유저 효율 ↑). |
| 2026-04-26 | fix(survival): false CRITICAL 방지 — 모든 포지션 sector 미설정 시 NA 강등 (ADR-0050 보강): 사용자 보고 "계좌 위험영역" CRITICAL 메시지 — 6개 포지션 모두 sector 필드 부재로 portfolioRiskEngine 의 fallback `'기타'` 가 sectorWeights={'기타':1.0} 단일 키 생성 → HHI=10000 → false CRITICAL 발생. ① **`isUnknownSectorOnly(sectorWeights)` 헬퍼 신규** — 단일 유효 키가 UNKNOWN_SECTOR_KEYS(`'기타'`/`'unknown'`/`'UNKNOWN'`/`''`) 에 속하면 true. NaN/0 가중치 자동 무시. ② **`classifySectorTier` 시그니처 확장** — `(hhi, activePositions, isUnknownOnly?: boolean = false)` 3번째 인자 추가, isUnknownOnly=true 시 NA 강등. ③ **`collectSurvivalSnapshot` wiring** — sectorWeights 분석 후 unknownOnly 판정 → classifySectorTier 에 전달. ④ **회귀 테스트 9 케이스 신규** — classifySectorTier(isUnknownOnly=true) 강등 검증 2 + isUnknownSectorOnly 7 (단일 '기타' / 'unknown'·'UNKNOWN'·'' / 정상 섹터 false / 다중 키 false / 빈 객체 false / NaN/0 무시). | `server/health/survival.ts` (+isUnknownSectorOnly + classifySectorTier 3-arg + collectSurvivalSnapshot wiring), `server/health/survival.test.ts` (+9 케이스) | 사용자 첨부 스크린샷 — `total_assets=98,754,099 / sector_concentration.tier=CRITICAL / sectorWeights={"기타":1}`. 데이터 누락 패턴이 false CRITICAL 을 일으키지 않도록 NA 강등 안전망 도입. PR-Z2 ADR-0050 의 후속 보강. |
| 2026-04-26 | FOMC 게이트 정책 v2 — D-1 부터 차단 + 우호 환경 보수적 진입 (사용자 요청): 사용자 보고 "fomc 1일전부터 매매를 막는걸로 수정한다 / 단, 지수 상황이 좋거나 결과 긍정 예상 시 보수적 매매 가능". 이전 v1 정책은 PRE_3/PRE_2/PRE_1/DAY 4일 모두 신규 진입 차단(Kelly 0) — 한국 주식 관점에서 미국 FOMC 의 실질 영향은 발표 직전(D-1) ~ 발표 직후(D+1) 에 집중되므로 4일 차단은 과도한 보수성. ① **`server/trading/fomcCalendar.ts` PHASE_KELLY v2**: PRE_3/PRE_2 = 1.0 (정상 운용), PRE_1/DAY = 0.0 (신규 진입 금지). 차단 기간 4일 → 2일 단축. description 도 정합 갱신 ("FOMC D-3 ... 정상 운용 (D-1 부터 진입 차단)" 등). hedgeSignal 은 v2 에서 항상 false (D-3 정상 운용으로 격하 — 헤지 검토는 R6 비상 청산·trailingStop 등 exitEngine 룰에서 자체 처리). ② **`applyFomcRelaxation(phase, defaultKelly, macro?)` 헬퍼 신규** + `FomcRelaxationContext` (mhs/regime/vkospi) + `FomcRelaxationResult` (relaxed/effectiveKelly/noNewEntry/reason) + `FOMC_RELAXATION_THRESHOLDS` SSOT (MHS_MIN=60 / VKOSPI_MAX=22 / KELLY_RELAXED=0.3). 우호 환경 3조건 모두 충족 시 (MHS≥60 + 강세 레짐(BULL_AGGRESSIVE/BULL_NORMAL/R1/R2) + VKOSPI≤22) Kelly ×0.3 보수적 진입 허용. macro 부재 / 일부 필드 누락 / 어느 한 조건 미달 시 안전하게 차단 유지. ③ **`getFomcProximity(macro?)` 시그니처 확장** — macro 옵셔널. 전달 시 PRE_1/DAY 에서 자동 완화 평가, 미전달 시 기존 차단 유지 (회귀 안전). FomcProximity 인터페이스에 `relaxed?` + `relaxationReason?` 옵셔널 필드 추가. ④ **`server/trading/signalScanner/preflight.ts` wiring** — `getFomcProximity()` 호출에 `{ mhs, regime, vkospi }` snapshot 전달. relaxed=true 시 운영자 텔레그램 알림 (priority='NORMAL' + dedupeKey `fomc_relaxed_${nextDate}` + 12h cooldown). ⑤ **`server/trading/signalScanner.ts:332` 동일 wiring**. ⑥ **`server/trading/fomcCalendar.test.ts` 신규** — 41 케이스: phase 분류 v2 (PRE_3/PRE_2 정상/PRE_1/DAY 차단/POST_1 부스트/NORMAL) + hedgeSignal 항상 false + 차단 기간 정확히 2일 + applyFomcRelaxation 14 분기 (PRE_3 무관/PRE_2 무관/NORMAL 무관/macro 부재 차단/필드 누락 차단/3조건 충족 0.3/MHS boundary/R1 별칭/MHS 미달/NEUTRAL 차단/R6_DEFENSE 차단/VKOSPI 초과/VKOSPI 누락/NaN 안전) + getFomcProximity(macro) 통합 4 (PRE_1+우호 / PRE_1+비우호 / macro 미전달 회귀 / PRE_3 macro 우호여도 v1 정상) + 임계 SSOT 1 + ICS 정합 3 (DESCRIPTION 갱신 / D-3 경보 제거 / D-1 단일화). ⑦ **검증** — vitest **server/trading 72 files 587/587 pass** (신규 41 + 인접 무회귀) + lint(client + server tsc) + validate:all 8종 + precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/autoTradeEngine 무수정). LIVE 매매 본체는 PRE_1/DAY 에서 우호 환경 시 보수적 진입 허용으로 의도된 변경. ⑧ **시뮬레이션 (4/26~5/1)** — D-3 정상(1.0) / D-2 정상(1.0) / D-1 우호 시 0.3·비우호 시 차단 / DAY 우호 시 0.3·비우호 시 차단 / D+1 부스트(1.30) / D+2 부스트(1.15). 매도(exitEngine) 는 본 게이트 무관 — 평일 09:00~15:55 cron 정상 발동. ⑨ **결과** — 사용자 의견대로 차단 기간 4일→2일 단축으로 진입 기회 손실 회복 + 우호 환경(MHS·Regime·VKOSPI 3조건 동시 충족) 시 D-1/DAY 에도 Kelly ×0.3 보수적 진입 허용. 페르소나 철학 8 ("불확실성 시 관망") 과 "기회 활용" 의 균형. | `server/trading/fomcCalendar.ts` (PHASE_KELLY v2 + applyFomcRelaxation + getFomcProximity 시그니처 확장 + ICS 정합), `server/trading/fomcCalendar.test.ts` (신규 41 케이스), `server/trading/signalScanner/preflight.ts` (macro snapshot 전달 + relaxed 텔레그램 알림), `server/trading/signalScanner.ts` (macro snapshot 전달) | 사용자 요청 "fomc 1일전부터 매매 차단 + 우호 환경 보수적 매매". 한국 주식 관점에서 D-3, D-2 영향 미미하므로 4일 차단의 비현실적 보수성 해소. 우호 환경 3조건(MHS·Regime·VKOSPI) 동시 충족이 매우 까다로운 임계치라 default 동작은 안전하게 차단 유지, 데이터가 충분히 우호적일 때만 보수적 진입 허용. macro 부재/누락 시 안전 fallback 으로 회귀 위험 격리. |
| 2026-04-26 | FOMC 게이트 정책 v3 — D-day 1일만 차단 (사용자 운영 경험 반영): 사용자 보고 "FOMC 는 당일만 금지하면 충분했다". v2 의 D-1+DAY 2일 차단도 과도한 보수성 — 한국 주식은 미국 FOMC 발표(한국 새벽 03:00) 에 *직접* 노출되지 않고 익일 09:00 결과 반영 가격으로 시작하므로 D-1 진입은 발표 직전 변동성 노출 미미. ① **PHASE_KELLY v3** — PRE_3/PRE_2/PRE_1 = 1.0 (정상 운용), DAY = 0.0 (신규 진입 금지). 차단 기간 v2 의 2일 → v3 의 **1일 (DAY 만)** 추가 단축. ② **applyFomcRelaxation v3** — `isBlockedPhase = phase === 'DAY'` (PRE_1 분기 제거). DAY 만 우호 환경 평가 적용 — PRE_1 은 v3 에서 정상 운용이라 완화 평가 자체가 무의미. macro 부재/누락/임계 미달 시 보수적 차단 유지 회귀 안전. ③ **description 갱신** — PRE_3/PRE_2 "정상 운용 (D-day 만 차단)", PRE_1 "정상 운용 (내일 발표일 차단)", DAY "신규 진입 금지". ④ **ICS 캘린더** — VEVENT DESCRIPTION "D-day(발표 당일) 신규 진입 자동 차단 (v3)" + "우호 환경 시 D-day 도 보수적 진입 (Kelly ×0.3)". D-1 VALARM 메시지 "내일 발표 — D-day 만 신규 진입 자동 차단 (D-1 은 정상 운용)" 으로 갱신. ⑤ **회귀 테스트 38 케이스 재작성** — phase 분류 v3 (PRE_3/PRE_2/PRE_1 정상 운용 + DAY 차단 + POST_1 부스트) + hedgeSignal 항상 false + 차단 기간 정확히 1일 (DAY 만) + applyFomcRelaxation v3 분기 (DAY 외 phase 무관 5개 + DAY macro 부재/누락 차단 3개 + DAY 우호 3조건 충족 3개 + DAY 우호 일부 미충족 6개) + getFomcProximity 통합 5 (DAY+우호 0.3 / DAY+비우호 차단 / macro 미전달 회귀 / PRE_1+우호여도 정상 운용 / PRE_3+우호여도 정상 운용) + 임계 SSOT 1 + ICS 정합 2 (v3 DESCRIPTION + D-1 VALARM 정상 운용 안내). ⑥ **검증** — vitest **server/trading 72 files 588/588 pass** (신규 38 + 인접 무회귀) + lint(client + server tsc) + validate:all 8종 + precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 본체는 차단 기간 1일로 단축 — 진입 기회 추가 회복. ⑦ **시뮬레이션 (4/26~5/1, FOMC 4/29)** — D-3 정상(1.0) / D-2 정상(1.0) / D-1 **정상(1.0)** ← v3 변경 / DAY 차단(0) 또는 우호 시 0.3 / D+1 부스트(1.30) / D+2 부스트(1.15). ⑧ **결과** — 사용자 운영 경험 반영. 차단 기간 v1 의 4일 → v2 의 2일 → v3 의 **1일** 누적 단축으로 진입 기회 손실 최소화 + DAY 차단으로 발표 직전 시초가 점프 회피 안전망 유지. 우호 환경 완화는 DAY 단일 phase 에 한정되어 운영자 인지 부담 단순화. | `server/trading/fomcCalendar.ts` (PHASE_KELLY v3 + applyFomcRelaxation v3 isBlockedPhase=DAY + description + ICS), `server/trading/fomcCalendar.test.ts` (v3 38 케이스 재작성) | 사용자 운영 경험 "FOMC 당일만 금지로 충분" 반영. v2 출시 직후 사용자 피드백 즉시 반영하여 차단 기간 추가 단축. 우호 환경 완화 로직은 그대로 유지 (DAY 단일 phase 에 적용) — 발표 결과 긍정 예상 시 보수적 진입 옵션 보존. preflight + signalScanner wiring 은 v2 와 동일 (변경 0). |
| 2026-04-26 | FOMC 게이트 정책 v3.1 — D-1 보수적 진입 (Kelly 0.75) + 미적용 영역 wiring 정합 (사용자 추가 요청): 사용자 보고 "1. d-1은 kelly 0.75 / 2. 변경점 관련 미적용 영역 확인 / 3. 그리고 커밋 후 pr". v3 의 PRE_1 = 1.0 (정상 운용) 이 *완전 정상* 이라 발표 직전 변동성 노출 부담 → 25% 사이즈 축소(Kelly 0.75)로 보수성 추가하되 진입은 허용. ① **PHASE_KELLY[PRE_1] 1.0 → 0.75** — 신규 진입 사이즈 25% 축소. PRE_3/PRE_2 = 1.0 정상 / PRE_1 = 0.75 보수 / DAY = 0.0 차단 / POST_1 = 1.30 부스트 / POST_2 = 1.15. ② **description PRE_1 갱신** — "보수적 진입 (Kelly ×0.75, 사이즈 25% 축소)". ③ **미적용 영역 전수 확인 + 일괄 wiring**: (a) `dryRunScanner.ts:109` `getFomcProximity()` macro 미전달 → macro snapshot 전달로 우호 환경 완화 일관성 확보 (b) `reportGenerator.ts:364, 449` 동일 패턴 — generateDailyReport + sendIntradayMarketReport 두 호출 모두 macro 전달 (c) `signalScanner.ts:392` `if (fomcProximity.kellyMultiplier !== 1)` 분기 — 0.75 도 자동 로그 (변경 불필요) (d) UI BuyAuditCard / autoTradingMapper RiskRule 'fomc-gating' — `noNewEntry=false` 이므로 triggered=false 자연 처리 (PRE_1 차단 안 됨). ④ **회귀 테스트 v3.1 정합** — PRE_1 분기 4 케이스 갱신 (Kelly 0.75 + description 보수적 진입 + 25% 표기 + macro 우호여도 0.75 그대로) + applyFomcRelaxation PRE_1 default 0.75 단언. ⑤ **검증** — vitest **server/trading + server/alerts 85 files 757/757 pass** (신규 38 + 인접 무회귀) + lint(client + server tsc) + validate:all 8종 + precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 본체: PRE_1 진입 시 사이즈 25% 축소 적용 (signalScanner.ts:381 `rawKelly = ... × fomcProximity.kellyMultiplier × ...` 자동 전파). ⑥ **시뮬레이션 (4/26~5/1)** — D-3 정상(1.0) / D-2 정상(1.0) / D-1 **보수(0.75)** ← v3.1 / DAY 차단(0) 또는 우호 시 0.3 / D+1 부스트(1.30) / D+2 부스트(1.15). ⑦ **결과** — 사용자 의도 완전 반영. 차단 기간은 v3 와 동일 1일(DAY 만)이지만 D-1 에서도 신규 진입 사이즈 25% 축소로 발표 직전 변동성 대비. dryRunScanner + reportGenerator 의 macro 전달 wiring 으로 v2 부터 시작된 우호 환경 완화 정책이 매매 + 진단 + 리포트 3 경로 모두 정합. | `server/trading/fomcCalendar.ts` (PHASE_KELLY[PRE_1] 0.75 + description), `server/trading/fomcCalendar.test.ts` (v3.1 PRE_1 0.75 단언), `server/trading/dryRunScanner.ts` (macro snapshot wiring), `server/alerts/reportGenerator.ts` (generateDailyReport + sendIntradayMarketReport macro wiring) | 사용자 3종 요청 일괄 처리: ①D-1 Kelly 0.75 ②미적용 영역(dryRunScanner + reportGenerator) wiring ③커밋·푸시·PR. 차단 기간 1일 유지하면서 D-1 보수성 추가로 발표 직전 변동성 안전망 강화. 우호 환경 완화 정책이 모든 호출 경로(preflight + signalScanner + dryRunScanner + reportGenerator)에 일관 적용. |
| 2026-04-26 | Yahoo probe 알림 폭주 제거 + Health SSOT 통합 + FOMC v4 (ADR-0056 + ADR-0057, 사용자 5+1 요청 통합 PR): 사용자 보고 "야후 에러 수정" + "fomc d_day 3일전부터 kelly지수는 0.75 적용". (A) Yahoo probe 5종 누적 적용 — 핵심 결정: 3번 SSOT 통합이 1·5번을 흡수하는 구조 채택 (자체 fetch 제거가 우선, 헬퍼는 미래 확장점). ① **`server/utils/yahooProbeRetry.ts` 신규** (133 LoC, @responsibility SRP) — `probeYahooWithRetry(symbol, options?)` query1.finance.yahoo.com 1회 시도 → 503/502/504/429 시 2초 백오프 후 query2.finance.yahoo.com 재시도, 4xx (429 제외) 영구 오류 즉시 실패 + `probeMultipleSymbols(symbols?, options?)` Promise.allSettled 다중 심볼 + `classifyMultiProbeResult(failCount, total)` 분류 SSOT (≥2/3 fail → DOWN, 1/3 → DEGRADED, 0 → OK) + `shouldRetryYahooStatus(status?)` 재시도 결정 SSOT (undefined timeout 도 retry 대상) + `PROBE_SYMBOLS = ['000660.KS', '247540.KQ', 'EWY']` (KOSPI/KOSDAQ/한국 ETF) + `RETRYABLE_STATUSES` SSOT + `PROBE_TIMEOUT_MS=8000` + `RETRY_BACKOFF_MS=2000`. guardedFetch (EgressGuard) 경유 — Yahoo host 우회 금지 (ADR-0029). 외부 의존 0건. ② **`pipelineDiagnosis.ts` 자체 fetch *제거*** — 라인 84~104 의 `guardedFetch(...000660.KS...)` 호출 + 응답 분기 *전체 제거*, 대신 `getYahooHealthSnapshot()` (`marketDataRefresh.ts`, 24h 누적 통계 SSOT) read-only 분기: status=OK → 알림 없음 / STALE → informational (비활성 분 표시) / UNKNOWN → informational (부팅 직후) / DOWN (5회 연속) → issues (CRITICAL). ③ **알림 등급 분리** — `DiagnosisResult.informational: string[]` 신규 필드 + `DiagnosisAlertSeverity` 분기. `healthCheckJob.runSelfDiagnosis` 가 issues/warnings 만 텔레그램 푸시, informational 은 console.log 만 (일일 요약 후속 PR). ④ **02:00 → 06:30 KST cron 이동** — `healthCheckJob.ts` cron `'0 17 * * 0-4'` → `'30 21 * * 0-4'` (UTC 21:30 = KST 06:30). NYSE 마감 직후(EST 16:30) + 한국장 개장 2.5h 전 — 데이터 신선도 + 운영 관련성 동시 극대화. PR-B-2 ScheduleClass 'TRADING_DAY_ONLY' 유지. (B) **FOMC 정책 v4** — `fomcCalendar.ts:113~121` PHASE_KELLY: PRE_3 1.0→**0.75** / PRE_2 1.0→**0.75** / PRE_1 0.75 그대로 / DAY 0.0 그대로 / POST_1 1.30 그대로 / POST_2 1.15 그대로. v3.1 의 PRE_3/PRE_2 = 1.0(완전 정상) 과 PRE_1 = 0.75 격차 해소 — 4일 전체 25% 축소 균일 적용. 변경: 파일 헤더 (정책 v4 표기), descMap (PRE_3/PRE_2 도 "보수적 진입 (Kelly ×0.75, 사이즈 25% 축소)"), generateFomcIcs DESCRIPTION ("D-3~D-1 보수적 진입 (Kelly ×0.75, v4) / D-day 신규 진입 자동 차단"), VALARM ("D-3 부터 보수적 진입(Kelly ×0.75), D-day 신규 진입 자동 차단"). `applyFomcRelaxation` 본체 무수정 — `isBlockedPhase = phase === 'DAY'` 그대로 (PRE_3/PRE_2/PRE_1 모두 정상 진입 phase, Kelly 0.75 만 적용). 회귀 테스트 v4 정합 5건 수정. (C) **회귀 테스트 39 케이스 신규** — `yahooProbeRetry.test.ts` 24 (shouldRetryYahooStatus 6: 429/502/503/504 retry / 200~299 / 4xx / undefined / 500 미포함 / SSOT 4개 + classifyMultiProbeResult 7: 0/3/1/3/2/3/3/3/total=0/5/10/7/10 + probeYahooWithRetry 6: 200 즉시 / 503→200 재시도 / 503→503 fail / 404 영구 / network error / SSOT + probeMultipleSymbols 5: 모두 200 / 1개 503 DEGRADED / 3개 503 DOWN / 사용자 지정 / 빈 배열) + `pipelineDiagnosis.diagnosis.test.ts` 6 (OK/DOWN/STALE/UNKNOWN 4분기 + 자체 fetch 제거 회귀 차단 + informational 옵셔널 후방호환). 누적 회귀 테스트 30 케이스 신규. **검증**: vitest 39+30=69/69 신규 pass + lint(client + server tsc) + validate:all 8종 + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — Yahoo probe 진단·FOMC Kelly 테이블 수치만 변경. **운영 효과**: (Yahoo) 단발성 503 알림 폭주 차단 — 새벽 알림 빈도 7/주 → 1/주 ↓ 86% 감소 목표. (FOMC) D-3 부터 사이즈 25% 축소로 4일 보수성 균일 적용 — 발표 영향권 4일 전체 일관성. | `docs/adr/0056-yahoo-probe-resilience.md` (신규), `docs/adr/0057-fomc-policy-v4.md` (신규), `server/utils/yahooProbeRetry.ts` (신규 133 LoC), `server/utils/yahooProbeRetry.test.ts` (신규 24 케이스), `server/trading/pipelineDiagnosis.ts` (자체 fetch 제거 + informational 필드 + Yahoo SSOT 분기), `server/trading/pipelineDiagnosis.diagnosis.test.ts` (신규 6 케이스), `server/scheduler/healthCheckJob.ts` (cron 02:00→06:30 + informational 분기), `server/trading/fomcCalendar.ts` (PHASE_KELLY v4 + descMap + ICS DESCRIPTION + VALARM), `server/trading/fomcCalendar.test.ts` (v4 정합 PRE_3/PRE_2 0.75 + applyFomcRelaxation 단언 + ICS 정합), `ARCHITECTURE.md` (+yahooProbeRetry boundary +pipelineDiagnosis Yahoo SSOT rule) | 사용자 요청 6종 (Yahoo 5 아이디어 + FOMC v4) 통합 PR. Yahoo SSOT 통합이 자체 fetch 제거를 우선해 단발성 503 알림 폭주 차단의 핵심을 담당, 헬퍼 모듈은 향후 `/probe yahoo` 텔레그램 명령 등 미래 확장점. FOMC v4 는 사용자 운영 결정으로 4일 전체 25% 축소 균일 적용 — v3.1 의 1.0/0.75 사이즈 격차 직관적 부담 해소. |
| 2026-04-26 | PR-EG1+EG2+EG3 통합 — EgressGuard IntentTag (REALTIME / HISTORICAL / OVERNIGHT) + 12 호출자 wiring + /force_watch_scan 강제 워치 스캔 명령 (ADR-0058, 사용자 6 아이디어 + 강제 watch scan 통합): 사용자 보고 "06:00 KST 글로벌 스캔이 NYSE 정규장 closed 로 차단되어 새벽 워치리스트 후보 수집 실패" + "강제 watch scan 기능 추가". 기존 EgressGuard(PR-29 ADR-0028) 의 단일 규칙(시간대만 보고 *왜* 호출하는지 미고려) 한계 해소. **rebase 정정**: main #380 (ADR-0056 Yahoo probe SSOT + ADR-0057 FOMC v4) 가 본 브랜치 작업 중 머지되어 ADR 번호 0056→**0058** 재할당 + main 의 pipelineDiagnosis Yahoo SSOT 통합으로 자체 fetch 자체가 제거되어 IntentTag wiring 대상 13→**12** 정정. ① **`server/utils/egressGuard.ts` IntentTag 도입** (+52 LoC) — `EgressIntent = 'REALTIME' \| 'HISTORICAL' \| 'OVERNIGHT'` export + `evaluateEgress(url, intent='REALTIME', now)` / `guardedFetch(input, init?, intent='REALTIME')` 옵셔널 인자 + `isNyseAfterHours(now)` private helper (EST 16:00~20:00, 평일만, FORCE_OFF/FORCE_MARKET env 동일 처리). 결정 매트릭스 SSOT (ADR-0058 §Decision): 정규장 통과는 모든 의도 동일 / HISTORICAL → 항상 통과 / OVERNIGHT + market=NYSE + 애프터마켓 → 통과 / 그 외 → skip. NYSE_TZ_OFFSET_HOURS=-5 / NYSE_AFTERHOURS_OPEN_MIN=960 / NYSE_AFTERHOURS_CLOSE_MIN=1200 상수 SSOT. ② **12 호출자 wiring** (각 +1 인자, 본체 0줄 변경) — REALTIME 6건: marketDataRouter:179·363 (브라우저 차트 프록시 / market-indicators), dxyIntradayClient:59 (5분 인트라데이), sectorEtfMomentum:108 (섹터 ETF 30분), koreanQuoteBridge:154 (KRX 매개체), diagnostics:408 (/health probe). HISTORICAL 6건: backtestEngine:84 (2년 백테스트), lateWinEvaluator:65 (2년 학습), sectorSources:273 (quoteSummary 재무·부문), yahooQuoteAdapter:76 (range=2y 일봉), quantitativeCandidateGenerator:144 (3개월 일봉), **marketDataRefresh:281 (fetchDailyBars 5d/25d/65d) ← 사용자 보고 NYSE 차단 회복 핵심**. globalScanAgent 가 marketDataRefresh.fetchCloses 경유라 HISTORICAL 분류로 06:00 KST 새벽 cron 자동 회복. pipelineDiagnosis 는 main #380 의 Yahoo SSOT 통합으로 자체 fetch 자체가 제거되어 IntentTag wiring 대상 자연 제외. OVERNIGHT 0건 — 향후 진정한 NYSE 애프터마켓 라이브 시세 호출자 등록 시 사용 (매트릭스 보존). ③ **`/force_watch_scan` 신설** (`server/telegram/commands/trade/forceWatchScan.cmd.ts` 신규 +103 LoC) — 옵션 C 분기 채택 (architect 결정): 인자 없으면 light(`autoPopulateWatchlist` 만, ~5s) / `full` 인자면 heavy(`runFullDiscoveryPipeline` + `autoPopulateWatchlist`, ~30s). 별칭 `/force_scan`. 안전 가드 3중: (a) 60s rate-limit (모듈 로컬 `_lastForceScanAt` ADR-0014 패턴) — 가드 통과 후 갱신 + throw 시점 갱신 유지로 재시도 폭주 차단 / (b) `process.env.AUTO_TRADE_ENABLED !== 'true'` 차단 / (c) `getEmergencyStop()` 차단. 시작·완료·차단 메시지 명확 분기. 기존 `/krx_scan` (KRX universe 캐시 갱신) 과 책임 분리 — `/force_watch_scan` 은 워치리스트 영속화까지 보장. barrel `commands/trade/index.ts` 1줄 추가. ④ **회귀 테스트 32 케이스 신규** — `egressGuard.test.ts` +20 (REALTIME 3 + HISTORICAL 3 + OVERNIGHT 6 boundary 검증 16:00 EST 통과 / 20:00 차단 exclusive / 22:00 차단 / KRX·TSE 주말 차단 / 미명시 = REALTIME 기본값 2 / env 우선순위 2 + isNyseAfterHours 4 boundary) + `forceWatchScan.test.ts` 신규 12 (light 1 + full 2 case-insensitive `'full'`/`'FULL'` + 호출 순서 invocationCallOrder + 안전 가드 4 rate-limit/AUTO_TRADE/disabled/emergencyStop + 에러 처리 2 throw + rate-limit 갱신 유지 + 메타 3 name/aliases/category·riskLevel·visibility/description·usage). ADR-0058 §Decision Matrix 와 1:1 정합. ⑤ **검증** — rebase 후 lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit EXIT=0 통과 확인. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/aiUniverseService/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — 옵셔널 인자 패턴으로 미명시 호출자 = REALTIME 기본값 = PR-29 동작 100% 보존. ⑥ **회복 시나리오** — globalScanAgent (간접, marketDataRefresh.fetchDailyBars HISTORICAL → KST 06:00 cron 회복) / backtestEngine 주말 백테스트 / quantitativeCandidateGenerator (ADR-0016 5-Tier fallback Tier 3) / yahooQuoteAdapter (autoPopulateWatchlist 새벽 08:35 cron) / sectorSources (재무 프로필 시간대 무관) / lateWinEvaluator 학습 cron 모두 자동 회복. 운영자 트리거 `/force_watch_scan` 으로 새벽/주말/공휴일 임의 시각 명령 가능. ⑦ **architect 권장 PR 분리(EG1/EG2/EG3) 통합 진행** — 사용자 명시 "강제 watch scan 기능 추가" 요청 흡수, 옵셔널 인자 패턴으로 회귀 위험 격리. 사용자 6 아이디어 중 1·2·6 흡수 (1 IntentTag 핵심 / 2 NYSE 애프터마켓 OVERNIGHT 분기로 자연 흡수 / 6 강제 트리거 안전망). 3·4·5 후속 PR 후보 (시간대 화이트리스트 / 데이터 신선도 fallback / DataDependentJob 일반화). | `docs/adr/0058-egressguard-intent-tags.md` (신규), `server/utils/egressGuard.ts` (+EgressIntent + isNyseAfterHours + evaluateEgress·guardedFetch intent 옵셔널 인자), `server/utils/egressGuard.test.ts` (+20 케이스), `server/routes/marketDataRouter.ts` (+'REALTIME' ×2), `server/alerts/{dxyIntradayClient,sectorEtfMomentum}.ts` (+'REALTIME'), `server/clients/koreanQuoteBridge.ts` (+'REALTIME'), `server/trading/marketDataRefresh.ts` (+'HISTORICAL'), `server/trading/pipelineDiagnosis.ts` (ADR-0058 주석 추가, fetch 자체는 main #380 SSOT 통합으로 부재), `server/health/diagnostics.ts` (+'REALTIME'), `server/learning/{backtestEngine,lateWinEvaluator}.ts` (+'HISTORICAL'), `server/screener/{sectorSources,adapters/yahooQuoteAdapter}.ts` (+'HISTORICAL'), `server/services/quantitativeCandidateGenerator.ts` (+'HISTORICAL'), `server/telegram/commands/trade/forceWatchScan.cmd.ts` (신규 +103줄), `server/telegram/commands/trade/forceWatchScan.test.ts` (신규 12 케이스), `server/telegram/commands/trade/index.ts` (barrel +1줄) | 사용자 6 아이디어 + 강제 watch scan 통합 요청. 06:00 KST 새벽 글로벌 스캔이 NYSE 정규장 closed 로 차단되던 구조적 결함(EgressGuard 가 시간대만 보고 *왜* 호출하는지 미고려) 해소. marketDataRefresh REALTIME → HISTORICAL 정정 1건이 globalScanAgent 자동 회복의 핵심. 옵셔널 인자 패턴으로 회귀 위험 격리(미명시 = REALTIME 기본값 = PR-29 동작 보존). 운영자 트리거(`/force_watch_scan`)는 cron 일시 장애 시 안전망. main #380 머지에 따라 rebase + ADR 0056→0058 재할당 + 12 호출자 정정. |
| 2026-04-26 | 공매도 비율 데이터 출처 운영 가시화 — Phase 1 (사용자 질문 "공매도 데이터는 KRX 에만 의존?" 후속): 코드 추적 결과 — 시장 매크로 비율(`shortSellingRatio`)은 이미 3단계 fallback (KRX direct → KRX OTP → KIS top30 가중평균 추정) 이 작동 중이지만 **어느 fallback 이 실제 작동 중인지 운영자가 인지 못 하던 갭** 해소. KRX 가 자주 죽고 KIS 추정값(근사값, 4.5~9.5% 범위 휴리스틱)으로 동작하는 시나리오를 표면화 — Phase 2 (공공데이터포털 OpenAPI 신규 fallback) 의사결정 데이터 누적 인프라. ① **`ShortSellingSource` 타입 + `ShortSellingResult` 인터페이스** (`server/trading/marketDataRefresh.ts`) — `'KRX_DIRECT' \| 'KRX_OTP' \| 'KIS_ESTIMATE'` 3 분기 union. ② **`fetchKrxShortSelling()` 반환 시그니처 확장** — `Promise<number \| null>` → `Promise<ShortSellingResult \| null>`. 1차/2차/3차 fallback 마다 source 라벨 부착 + `fetchedAt` ISO 시각. ③ **`MacroState` 옵셔널 필드 2 추가** — `shortSellingSource?` + `shortSellingFetchedAt?`. `refreshMarketRegimeVars` 가 매번 갱신 후 `saveMacroState` 로 영속. ④ **`HealthSnapshot` 옵셔널 필드 2 추가** + `collectHealthSnapshot()` 가 `loadMacroState()` 에서 read. ⑤ **`/health` 메시지에 "공매도 출처:" 라인 신규** (KRX OpenAPI 라인과 Yahoo probe 사이) — `formatShortSellingSourceLine(s)` SSOT 헬퍼 4 분기: 미수집 → `? 미수집` / `KRX_DIRECT` → `✅ KRX 직접 (HH:MM)` / `KRX_OTP` → `🟡 KRX OTP 폴백 (HH:MM)` / `KIS_ESTIMATE` → `⚠️ KIS 추정값 (HH:MM)`. KST 시각 24h 포맷(`hour12: false`) — ko-KR locale 기본 PM/AM prefix 회피. fetchedAt 부재/잘못된 ISO 시 시각 suffix 자동 생략. ⑥ **`computed` 객체 type 확장** — `Record<string, number \| boolean \| null>` → `Record<string, number \| boolean \| string \| null>`. `refreshMarketRegimeVars` 반환 시그니처 동일 확장. ⑦ **회귀 테스트 10 케이스 신규** (`health.cmd.test.ts +10`) — formatShortSellingSourceLine 6 분기 (4 source 라벨 + fetchedAt 부재 / 잘못된 ISO) + formatHealthMessage 통합 4건 (KRX_DIRECT 정확히 1번 / 미수집 운영 환경 / KRX→공매도→Yahoo 순서 / KIS_ESTIMATE ⚠️ 마커). ⑧ **검증** — vitest server/trading + server/health + server/telegram **87 files 867/867 pass** (신규 10 + 무회귀) + lint(client + server tsc) + validate:all 8종 + precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범 (`fetchKrxShortSelling` 호출 경로 동일, source 라벨만 부착). LIVE 매매 본체 0줄 변경. ⑨ **운영 효과** — (1) 운영자가 `/health` 한 번에 공매도 비율이 *진짜 KRX 직접 데이터인지 / KIS 추정 근사값인지* 즉시 구분 가능. (2) `KIS_ESTIMATE` 가 자주 등장하면 KRX 본체 fallback 실패율이 높다는 신호 → Phase 2 신규 fallback (공공데이터포털 OpenAPI) 도입 의사결정 데이터. (3) 추정값이 R5_CAUTION 임계(8%) 비교에 사용되고 있다는 사실이 명시적으로 노출되어 운영자가 macroState 신뢰도 인지. ⑩ **non-scope (Phase 2 후속 PR)** — 공공데이터포털 KRX OpenAPI 신규 fallback / Naver Finance 종목 단위 공매도 페이지 wiring / `fetchKrxShortBalance` dead path 활성화 (enemyCheck wiring). 본 PR 은 *진단 가시화* 만 — 1~2주 운영 데이터 누적 후 Phase 2 결정. | `server/trading/marketDataRefresh.ts` (+ShortSellingSource type + ShortSellingResult interface + fetchKrxShortSelling 반환 확장 + computed type 확장), `server/persistence/macroStateRepo.ts` (+shortSellingSource? + shortSellingFetchedAt?), `server/health/diagnostics.ts` (+loadMacroState import + HealthSnapshot 2 필드 + collectHealthSnapshot wiring), `server/telegram/commands/system/health.cmd.ts` (+formatShortSellingSourceLine SSOT + formatHealthMessage 라인 삽입), `server/telegram/commands/system/health.cmd.test.ts` (+10 케이스) | 사용자 질문 "공매도 데이터는 KRX 에만 의존? KRX 가 안될 때 다른 방법은?" 직답: 이미 3단계 fallback 작동 중 + 운영자가 어느 단계가 작동 중인지 인지 못 하던 갭 해소. Phase 2 (공공데이터포털 OpenAPI / Naver Finance) 의사결정용 운영 데이터 누적 시드. |
| 2026-04-26 | /health 메시지에 KRX OpenAPI 라인 추가 (사용자 질문 "/health 에 KRX 연결상태 표시 못하는거야?"): 데이터는 이미 `HealthSnapshot` 4 필드(`krxTokenConfigured`/`krxTokenValid`/`krxCircuitState`/`krxFailures`)로 수집되고 있었고 `computeVerdict` 12분기에도 `KRX_TOKEN_NOT_CONFIGURED`/`KRX_TOKEN_UNHEALTHY` 가 포함되어 *망가질 때만* verdict 라인에 간접 표시되던 상태. **전용 라인 부재** 가 갭 — 정상일 땐 운영자가 KRX 연결을 인지할 수 없고 DISABLED env 같은 비활성 상태도 표시 안 됨. ① **`formatKrxStatusLine(s: HealthSnapshot): string` SSOT 헬퍼 신설** (`server/telegram/commands/system/health.cmd.ts`) — 우선순위 5 분기: (1) `krxTokenConfigured=false` → `⚠️ 토큰 미설정 (KRX_OPEN_API_AUTH_KEY)` (2) `krxCircuitState='OPEN'` → `❌ 회로 OPEN (실패 N회)` (3) `krxCircuitState='HALF_OPEN'` → `🟡 회복 중 (실패 N회)` (4) `krxCircuitState='CLOSED'` + `krxTokenValid=true` → `✅ 정상 (실패 N회)` (5) `krxCircuitState='CLOSED'` + `krxTokenValid=false` (DISABLED env) → `🟡 비활성 (실패 N회)` + 알 수 없는 상태 fallback `? <state>`. ② **`formatHealthMessage` KIS 토큰 라인 직후에 KRX 라인 삽입** — KIS↔KRX 토큰 묶음 + Yahoo probe 와 시각 분리. 메시지 형식: `KIS 토큰: ✅ 12시간 남음 \n KRX OpenAPI: ✅ 정상 (실패 0회) \n Yahoo probe: ✅ ...`. ③ **회귀 테스트 11 케이스 신규** (`health.cmd.test.ts`) — formatKrxStatusLine 5분기 + 알 수 없는 상태 fallback + 우선순위 충돌(토큰 미설정 > 회로 OPEN) 7건 + formatHealthMessage 통합 4건 (KRX 라인 정확히 1번 / OPEN 마커+카운트 / KIS→KRX→Yahoo 순서 / 토큰 미설정 운영 환경 회귀 가드). 본 health.cmd 의 첫 단위 테스트 파일 — vi.mock 으로 commandRegistry 격리. ④ **검증** — vitest server/telegram + server/health **14 files 262/262 pass** (신규 11 + 무회귀) + lint(client + server tsc) + validate:all 8종 + precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 준수). LIVE 매매 본체 0줄 변경 — `/health` 표시 라인 1줄만 추가. ⑤ **운영 효과** — 운영자가 `/health` 한 번에 KRX OpenAPI 회로 상태(KIS 회로처럼 별도 라인) 즉시 인지. 토큰 미설정/DISABLED env/회로 OPEN/회복 중/정상 5분기 한눈에 구분. KRX 연결 단절 시 verdict 라인에서만 보이던 회귀(정상 시 표시 부재) 영구 해소. | `server/telegram/commands/system/health.cmd.ts` (+formatKrxStatusLine + formatHealthMessage KRX 라인 삽입), `server/telegram/commands/system/health.cmd.test.ts` (신규 11 케이스) | 사용자 질문 "/health KRX 표시 못하나?" 직답 — 데이터는 이미 수집됨, 표시 라인만 부재였음. PR-49 (헬스 진단 SSOT) 위에 표시 레이어 추가. KIS 토큰 라인과 인접 배치로 두 한국 거래 인프라 토큰 그룹핑. |
| 2026-04-26 | Yahoo probe 자기 차단 회귀 차단 + 진단 메시지 사유 분해 (사용자 보고 "yahoo probe: yahoo down 3/3" 원인 분석): 사용자 보고 — `/health` 명령에서 Yahoo probe 가 3/3 fail (DOWN) 반복. 원인 추적 결과 **EgressGuard IntentTag 누락** (yahooProbeRetry.fetchOnce 의 `guardedFetch` 호출 시 3번째 인자 미지정 → 기본값 `'REALTIME'`). PROBE_SYMBOLS 3종(`000660.KS` KOSPI / `247540.KQ` KOSDAQ / `EWY` NYSE) 모두 KR 장외 또는 NYSE 장외 시간대에 EgressGuard 가 자기 차단(503 synthetic + `X-Egress-Guard: market-closed` 헤더) → query2 재시도도 동일 → 3/3 fail → DOWN. 진단 메시지 `Yahoo down: 3/3 probes failed` 가 *진짜 Yahoo 장애 vs 자기 차단* 구분 불가능한 것이 추가 결함. 옵션 1+2 결합 PR. ① **옵션 1 — `yahooProbeRetry.fetchOnce` 의 `guardedFetch` 호출에 `intent='HISTORICAL'` 명시** (server/utils/yahooProbeRetry.ts:84) — probe 본질이 시간대 무관 도달성 검사 + URL `range=1d&interval=1d` 가 historical bars 1건 fetch 라 의미 정합. EgressGuard 가 KR/NYSE 정규장 외 시간대에도 통과 (자기 차단 회귀 차단). REALTIME 차단 기능은 데이터 경로 호출자에 그대로 남음. ② **옵션 2 — 진단 메시지 사유 분해** (`YahooProbeResult.egressGated?: boolean` 옵셔널 필드 + `classifyProbeFailures(results)` SSOT 함수 신설). `fetchOnce` 가 응답의 `X-Egress-Guard: market-closed` 헤더 감지 → `egressGated=true`. `probeYahooWithRetry` 가 query1/query2 결과의 egressGated 를 propagate. ③ **`server/health/diagnostics.ts formatProbeFailureDetail(multi)` SSOT 헬퍼 신설** — egress-gated/yahoo-5xx/timeout 카운트 분해 후 ` (egress-gated 3)` / ` (yahoo-5xx 1, timeout 2)` 형식 detail 문자열 반환. `runExternalProbes` 의 Yahoo probe DEGRADED/DOWN 메시지에 detail 첨부 — 운영자가 *진짜 Yahoo 장애 vs EgressGuard 자기 차단 vs network timeout* 즉시 구분. ④ **회귀 테스트 18 케이스 신규** — `yahooProbeRetry.test.ts +13` (egressGated 헤더 감지 5: 503+헤더 / 503 헤더없음 / 200 미설정 / 404 영구 / timeout 미설정 + classifyProbeFailures SSOT 6 분기 + HISTORICAL intent EgressGuard 통과 회귀 차단 2: KR 토요일 정오 vi.setSystemTime mock + 단일 심볼 / 다중 심볼) + `diagnostics.test.ts +5` (formatProbeFailureDetail: 빈/모두 egress-gated/모두 yahoo-5xx/혼재 분류/partial DEGRADED). ⑤ **검증** — vitest server/utils + server/health + server/scheduler + server/telegram **22 files 443/443 pass** (신규 18 + 무회귀 425) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 준수). LIVE 매매 본체 0줄 변경 — probe intent 1줄 + 진단 헬퍼 추가. ⑥ **운영 효과** — (1) `/health` 명령이 KR 장외/주말에도 진짜 Yahoo 도달성을 검증 (자기 차단 false positive 제거). (2) DOWN 발생 시 메시지에 사유 분해 표시 → 운영자가 코드 회귀(egress-gated)/Railway IP 차단(yahoo-5xx)/네트워크(timeout) 즉시 구분 가능. (3) ADR-0058 IntentTag wiring 의 *반대 효과* (probe 자체가 시간대 차단되던 회귀) 영구 차단. | `server/utils/yahooProbeRetry.ts` (+egressGated 필드 + classifyProbeFailures + intent='HISTORICAL' 명시), `server/utils/yahooProbeRetry.test.ts` (+13 케이스), `server/health/diagnostics.ts` (+formatProbeFailureDetail + runExternalProbes detail wiring), `server/health/diagnostics.test.ts` (+5 케이스) | 사용자 보고 후속 진단. ADR-0058 IntentTag 도입 시 데이터 경로 12 호출자는 wiring 했으나 health probe 자체에서 누락된 갭. 자기 차단 회귀 영구 차단 + 향후 진단 메시지가 운영자 의사결정 근거가 되도록 사유 분해 정착. |
| 2026-04-26 | 스케줄러 카탈로그 갱신 + 8 스캔 cron 시간대 게이트 강제 통과 — withForcedMarket wrap (사용자 요청 "스캔이 발생하는 시간대에서는 강제로 스캔이 되도록 설정"): 사용자 첨부 스케줄러 시간표와 실제 `SCHEDULE_CATALOG` 비교 결과 (a) **06:00 글로벌 스캔 에이전트 항목 누락** (b) **시간대 게이트(KIS 장외/EgressGuard REALTIME) 로 인해 스캔이 실패하는 케이스 미해소** 두 갭 일괄 처리. 핵심 안전 원칙: KIS 회로차단기·24h 블랙리스트·EgressGuard host 정책(503 fallback) 은 *그대로 보호*, 시간대 게이트(`marketClock.isMarketOpen` 기반) 만 우회. ① **`server/scheduler/scheduleCatalog.ts` 갱신** — `SCHEDULE_CATALOG` 첫 행에 `{ timeKst: '06:00', label: '글로벌 스캔 에이전트', group: 'screener', jobName: 'global_scan_agent', silentWhen: 'Yahoo 미국 지수·섹터 ETF 종합 스캔 — KR 휴장 무관 ALWAYS_ON' }` 추가. cron 자체는 `screenerJobs.ts:58` 에 이미 등록되어 있었으나 사람이 읽는 시간표 SSOT 에 누락된 상태였음. ② **8 스캔 cron 콜백을 `withForcedMarket()` wrap** — PR-O 의 `forceMarketGuard.ts` SSOT 재사용. 적용 대상: (screenerJobs.ts 5건) `stage1_pre_screening` (KST 16:30 한국 장 마감 후) / `stage2_3_final_screening` (KST 08:35 장 25분 전) / `us_premarket_scan` (KST 22:25) / `us_postmarket_scan` (KST 06:10) / `global_scan_agent` (KST 06:00). (alertJobs.ts 2건) `adr_gap_scan` (KST 08:35) / `sector_etf_momentum` (KST 06:15). (reportJobs.ts 1건) `high_52w_scan` (KST 16:05). 패턴: 콜백을 `() => withForcedMarket(() => runXxxScan())` 으로 1줄 wrap. ③ **withForcedMarket 효과 SSOT** — 진입 시 `process.env.DATA_FETCH_FORCE_MARKET='true'` 설정 → finally 에서 원래 값(또는 미설정) 복구. `marketClock.isMarketOpen / isMarketDataPublished` 가 항상 true 반환 → KIS 장외 게이트 + KRX 휴장 게이트 + Yahoo IntentTag REALTIME 게이트 모두 통과. 그러나 KIS 회로차단기·24h 블랙리스트(PR-21/24)·EgressGuard host 정책(ADR-0058)·503 fallback 은 *그대로 동작* — 자해 위험 0. ④ **회귀 테스트 9 케이스 신규** (`scanCronForceMarket.test.ts`) — SCHEDULE_CATALOG 06:00 global_scan_agent 등록 검증 1 + 8 cron 각각 `scheduledJob` mock 으로 콜백 캡처 → 콜백 호출 시 `withForcedMarket` spy 호출 + 본체 함수(runStage1PreScreening / runStage2_3FinalScreening / runAutoSignalScan ×2 / runGlobalScanAgent / runAdrGapScan / runSectorEtfMomentumScan / sendNewHighMomentumScan) 호출 검증. ⑤ **검증** — vitest server/scheduler **3 files 36/36 pass** (신규 9 + 무회귀 27) + server/utils + server/telegram/commands **11 files 200/200 pass** + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 본체 무수정). LIVE 매매 본체 0줄 변경 — cron 콜백 wrap 만. ⑥ **운영 효과** — (1) 새벽/장외 시각에 시간대 게이트로 인해 스캔이 N/A 만 반환하던 8 cron 모두 자동 통과. (2) /scheduler 텔레그램 명령에 06:00 글로벌 스캔 에이전트 항목 가시화. (3) us_premarket_scan / us_postmarket_scan 의 runAutoSignalScan 도 wrap 되었지만 KIS 호가 마감으로 LIVE 실주문 자연 거부 + AUTO_TRADE_ENABLED + emergencyStop 가드 그대로. SHADOW 모드는 안전. ⑦ **non-scope** — orchestrator 1분 tick (상시) 은 wrap 미적용 — 매 분 강제 토글이 LIVE 자동매매 위험 ↑ + 장중 자연 통과 + 운영자 강제 트리거는 `/scan_offhours` 명령(PR-O) 으로 이미 충족. | `server/scheduler/scheduleCatalog.ts` (+06:00 global_scan_agent 항목), `server/scheduler/screenerJobs.ts` (5 cron wrap + withForcedMarket import), `server/scheduler/alertJobs.ts` (2 cron wrap + import), `server/scheduler/reportJobs.ts` (1 cron wrap + import), `server/scheduler/scanCronForceMarket.test.ts` (신규 9 케이스) | 사용자 요청 2종 일괄 처리: 카탈로그/표 SSOT 06:00 글로벌 스캔 항목 누락 보강 + 시간대 게이트로 인한 스캔 실패 회로 추가 안전망. KIS 회로/블랙리스트는 자해 방지를 위해 *그대로 보호* — 시간대 게이트만 우회하는 최소 변경 패턴. PR-O 의 `forceMarketGuard.ts` SSOT 재사용으로 신규 코드 표면적 최소화. |
| 2026-04-26 | 글로벌 지수 스캔 강제 트리거 명령 추가 — /scan_indices (alias /global_scan, /index_scan, 사용자 요청): 사용자 보고 "06:00 글로벌 스캔이 모든 지수(S&P500/나스닥/다우/VIX/EWY/ITA/SOXX/XLE/WOOD) N/A 반환 — 각종 지수 스캔 기능 (명령어) 입력으로 추가". 06:00 KST cron(`runGlobalScanAgent`)이 Yahoo 503/회로차단/블랙리스트 누적으로 실패해도 운영자가 임의 시각 즉시 재실행할 수 있도록 텔레그램 트리거 명령 신설. ① **`server/telegram/commands/system/scanIndices.cmd.ts` 신규** (~57줄, @responsibility SRP) — `name='/scan_indices'` + `aliases=['/global_scan', '/index_scan']` + category=MKT + riskLevel=0 (read-only — 외부 시세 조회만, 매매 무관) + visibility=ADMIN. `runGlobalScanAgent()` 호출 (alerts/globalScanAgent.ts 의 텔레그램 발송 본체 그대로 재사용 — 9개 심볼 병렬 fetch + Gemini 3-줄 요약 + 메인 리포트 + ANALYSIS 채널 미러링 + Layer 13/14 경보 + VIX 특별 경보). 60s rate-limit 가드 (모듈 로컬 `_lastScanIndicesAt`, ADR-0014 패턴) — 9개 심볼 동시 fetch + Gemini 호출 비용 차단. 가드 통과 시점에 rate-limit 갱신 → throw 후에도 폭주 차단. 시작 메시지 "9개 심볼 (S&P500/나스닥/다우/VIX/EWY/ITA/SOXX/XLE/WOOD) 수집 중..." + 완료 메시지 (소요 시간) + throw 시 ❌ 실패 메시지 분기. 비상정지·AUTO_TRADE_ENABLED 가드 *불필요* — 분석 정보 수집은 매매 비활성/비상 시에도 유효. ② **barrel `commands/system/index.ts` +1줄** — system 그룹 9개 → 10개 (/aiStatus /health /learningHistory /learningStatus /market /regime /scanIndices /scheduler /status /todaylog). ③ **회귀 테스트 9 케이스 신규** (`scanIndices.test.ts`) — 정상 실행 2 (runGlobalScanAgent 호출 + 시작/완료 메시지 2건 / args 무시) + 안전 가드 2 (60s rate-limit 차단 + 카운트다운 / reset 후 재실행) + 에러 처리 2 (throw → ❌ 메시지 / throw 후에도 rate-limit 갱신 유지) + 메타데이터 3 (name+aliases /global_scan·/index_scan / category=MKT·riskLevel=0·visibility=ADMIN / description+usage). vi.mock 으로 runGlobalScanAgent stub — Yahoo/Gemini 실호출 0건. ④ **검증** — vitest server/telegram **11 files 159/159 pass** (신규 9 + 기존 무회귀) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + precommit 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — runGlobalScanAgent 본체 무수정 wrapper 만 추가. ⑤ **운영 효과** — 사용자가 텔레그램에서 `/scan_indices` `/global_scan` `/index_scan` 중 어느 것이든 입력하면 즉시 9개 글로벌 지수 + Gemini 분석 메시지 수신. cron 06:00 KST 일시 장애 시 운영자 트리거 안전망 + ADR-0058 EgressGuard IntentTag 의 HISTORICAL 의도(`marketDataRefresh.fetchCloses` 경유)로 시간대 무관 통과 보장 + 텔레그램 명령 자동완성(buildBotMenuCommandsExtended) 에 자동 노출. | `server/telegram/commands/system/scanIndices.cmd.ts` (신규 ~57줄), `server/telegram/commands/system/scanIndices.test.ts` (신규 9 케이스), `server/telegram/commands/system/index.ts` (+1 import) | 사용자 요청 "각종 지수 스캔 기능 추가 (명령어) 입력으로". 첨부 이미지에서 06:00 글로벌 스캔이 9개 지수 모두 N/A 만 반환한 사례 직접 대응. /market 와 책임 분리: /market = MarketDashboard 요약, /scan_indices = 9개 글로벌 지수 + 섹터 ETF 강제 재수집. 60s rate-limit 으로 Yahoo 회로 부담 + Gemini 호출 비용 차단. |
| 2026-04-26 | 장외 강제 스캔/워치리스트 수집 명령 추가 + Yahoo probe SSOT 통합 보강 — /scan_offhours + diagnostics.runExternalProbes (사용자 요청 2종): 사용자 보고 ① "장외 강제 스캔 기능 추가 + 워치 리스트 장외 수집 강제 명령어도 추가" ② "Yahoo probe ⚠️ status=503 추가 패치 — diagnostics.ts 의 단일 심볼 fetch 를 probeMultipleSymbols SSOT 로 전환 + timeoutMs 기본값 3000→10000". 기존 `/scan` 은 EgressGuard(ADR-0028)+IntentTag(ADR-0058) 가 REALTIME 의도 fetch 를 차단하므로 장외에 호출하면 시세 0건으로 신호 0건. 워치리스트 강제 수집은 main 의 `/force_watch_scan` (ADR-0058 PR #381) 으로 이미 충족 — 본 PR 에서는 신호 스캐너 강제 트리거 부분만 신규 추가. ① **`server/utils/forceMarketGuard.ts` 신규** (60 LoC, @responsibility SRP) — `withForcedMarket(fn)` async 헬퍼: 진입 시 `process.env.DATA_FETCH_FORCE_MARKET='true'` 설정 → fn 실행 → finally 블록에서 원래 값(또는 미설정) 복구. 예외 발생해도 env 영구 오염 차단. + `isForcedMarketActive()` 진단 read-only. marketClock SSOT 의 `DATA_FETCH_FORCE_MARKET` env override 와 정합 — signalScanner / autoPopulateWatchlist 본체 무수정. ② **`server/telegram/commands/trade/scanForce.cmd.ts` 신규** — `/scan_offhours` (alias `/scan_force`), category=TRD, riskLevel=2. /force_watch_scan 와 책임 분리: /force_watch_scan = 워치리스트 *수집* 강제 (autoPopulateWatchlist), /scan_offhours = 신호 스캐너 *전체 흐름* 강제 (runAutoSignalScan). 비상정지 차단 가드 + `isMarketOpen()` 으로 장중/장외 태그 분기 + 장외 시 회로차단기·블랙리스트 부담 사전 경고 + `withForcedMarket(() => runAutoSignalScan())` wrap + 예외 catch 시 ❌ 메시지 + env 복구. ③ **diagnostics.ts probeMultipleSymbols SSOT 통합** — `runExternalProbes()` 의 yahooProbe 부분을 단일 심볼 `guardedFetch('https://query1.finance.yahoo.com/.../^KS11')` 1회 호출에서 `probeMultipleSymbols(undefined, { timeoutMs: 8000, backoffMs: 2000 })` 3종목 SSOT 로 교체. classifyMultiProbeResult 분류 (OK/DEGRADED/DOWN) → severity 매핑. 단일 심볼 false positive 차단 + query1→query2 재시도. ④ **timeoutMs 기본값 3000→10000ms 상향** — Railway egress 환경에서 Yahoo 가 3초 안에 503/504 자주 반환. probeMultipleSymbols 내부 timeout 8s × 2회 호스트 = 최대 16s 가능, withTimeout 10s 으로 capped. ⑤ **미사용 `guardedFetch` import 정리** — diagnostics.ts 에서 더 이상 직접 fetch 안 함. classifyYahooProbe 는 회귀 테스트에서 사용 중이라 export 유지. ⑥ **회귀 테스트 19 케이스 신규** — `forceMarketGuard.test.ts` 10 (env 토글 6: 미설정→true→복구 / false 사전값→true→false 복구 / true 유지 / fn throw 시 복구 / 사전값+throw 복구 / marketClock.isMarketOpen mock 토요일 정오 → wrap 안 true / 외부 false + isForcedMarketActive 4: 미설정/true/false/TRUE 대문자) + `scanForce.test.ts` 9 (등록 3: alias /scan_force 동일 인스턴스 / 동작 6: 비상정지 차단 / 장외 태그 + 회로 부담 / 장중 태그 / FORCE_MARKET env wrap+복구 / runAutoSignalScan throw catch). 검증: vitest 신규 19/19 + 영향 영역 server/health + server/telegram + server/utils 18 files 380/380 무회귀 + lint(client + server tsc) + validate:all 8종 + precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — env override 임시 토글 + 기존 함수 wrap. **운영 효과**: (1) 장외 시간(주말·평일 15:30 이후·새벽)에 분석/디버깅 목적으로 신호 스캐너 즉시 실행 가능. KIS 가 호가 차단으로 실주문은 자연 거부 — LIVE 위험 0. (2) Yahoo health probe 가 단발성 503 false positive 차단 + Railway egress 3초 timeout 부족 해소. ⚠️ 장외 호출 시 회로차단기·24h 블랙리스트(PR-21/24) 부담 가능성 — 운영자에게 명령 응답에 사전 경고 표시. | `server/utils/forceMarketGuard.ts` (신규 60 LoC), `server/utils/forceMarketGuard.test.ts` (신규 10 케이스), `server/telegram/commands/trade/scanForce.cmd.ts` (신규), `server/telegram/commands/trade/scanForce.test.ts` (신규 9 케이스), `server/telegram/commands/trade/index.ts` (+scanForce barrel), `server/health/diagnostics.ts` (probeMultipleSymbols SSOT + timeoutMs 10000 + guardedFetch import 정리) | 사용자 2종 요청 일괄 처리: 장외 강제 스캔 명령 신규(워치리스트 수집은 main 의 /force_watch_scan 로 충족) + diagnostics 의 단일 심볼 fetch 를 ADR-0056 SSOT 로 통합. forceMarketGuard SSOT 로 env 임시 토글 + 비상정지 차단 가드 유지. 본체 함수(runAutoSignalScan, autoPopulateWatchlist) 무수정으로 회귀 위험 0. |
| 2026-04-27 | `/refresh_sector_map` 텔레그램 명령 추가 — KRX 전종목 섹터맵 운영자 즉시 트리거: PR-Z1 (ADR-0060 sector infrastructure) 후속. 자동 cron(매주 월 03:00 KST + 평일 04:00 재시도) 외 시점에 운영자가 임의 갱신할 수 있는 진입점 부재 해소. ① **`server/telegram/commands/system/refreshSectorMap.cmd.ts` 신규** — `/refresh_sector_map` (alias `/update_sector_map`), category=SYS, riskLevel=1, visibility=ADMIN. `updateKrxSectorMap()` (4-tier fallback: KRX → Naver → Yahoo+Gemini → carry-over) 본체 호출 + `invalidateSectorMapCache()` 로 mtime 캐시 즉시 무효화 → 다음 `getSectorByCode` 호출부터 새 데이터 반영. 안전 가드 — 5분 rate-limit (모듈 로컬 `_lastRefreshAt`, ADR-0014 패턴) + 가드 통과 직후 갱신 → throw 후에도 폭주 차단. 비상정지·AUTO_TRADE_ENABLED 가드 *불필요* — sector 인프라 갱신은 매매 비활성/비상 시에도 안전. 시작/완료 메시지 분기 (KRX 본선 ✅ vs 폴백 🟡 + source 라벨/count/trdDd/소요시간/다음 자동 갱신 시각 노출) + throw 시 ❌ 메시지. ② **barrel `commands/system/index.ts` +1줄** — system 그룹 10개 → 11개 (refreshSectorMap.cmd.js side-effect import). ③ **회귀 테스트 12 케이스 신규** (`refreshSectorMap.test.ts`) — 정상 실행 3 (updateKrxSectorMap 호출 + invalidateSectorMapCache 호출 + 시작·완료 메시지 / args 무시 / source!='KRX' 폴백 시 🟡 + 출처 라벨 + count) + 안전 가드 2 (5분 rate-limit + 카운트다운 / reset 후 재실행) + 에러 처리 2 (throw → ❌ + 캐시 무효화 미호출 / throw 후에도 rate-limit 갱신) + 메타데이터 3 (name+aliases /update_sector_map / category=SYS·riskLevel=1·visibility=ADMIN / description+usage). ④ **검증** — vitest **305 files 3233/3233 pass** (신규 12 + 무회귀) + lint(client + server tsc) EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/autoTradeEngine 본체 무수정, KRX 호출은 sectorMapUpdater 의 4-tier 본체가 담당). LIVE 매매 본체 0줄 변경 — 신규 명령 + 테스트만. ⑤ **운영 효과** — Railway 재배포 후 텔레그램 `/refresh_sector_map` 입력 시 KRX 전종목 sector 매핑 즉시 갱신 → SOXX→반도체 부스트 wiring 의 잔여 원인(반도체 중소형주 다수가 '미분류' 폴백) 해소 → ADR-0060 인프라 위에 데이터 커버리지 완성. PR-Z1 의 의도된 효과(섹터 집중도·로테이션·SOXX 부스트) 가 처음으로 *데이터 레벨* 에서 정상 작동 가능. | `server/telegram/commands/system/refreshSectorMap.cmd.ts` (신규), `server/telegram/commands/system/refreshSectorMap.test.ts` (신규 12 케이스), `server/telegram/commands/system/index.ts` (+1 import) | 사용자 응답 "진행해줘" — PR-Z1 후속. ADR-0060 (sector 인프라 5중 누수 일소) 가 만든 인프라가 완전히 활용되려면 `data/krx-sector-map.json` 데이터 커버리지가 필요. 자동 cron 외 시점에 운영자가 텔레그램 1회 명령으로 트리거할 수 있는 진입점 정착. |
| 2026-04-27 | PR-1 (ADR-0061) FOMC DAY 보유 포지션 강제 전량 청산 정책: 사용자 요구 "FOMC 당일은 종목을 전량 처분 (장마감까지 잔고를 비우도록)". 기존 v3.1 (PRE_3=1.0/PRE_2=1.0/PRE_1=0.75/DAY=0.0) 가 신규 진입만 차단하고 보유 포지션 청산이 부재해 FOMC 발표 직전 변동성 노출 그대로 유지되던 갭 해소. ① **`server/trading/fomcCalendar.ts`** 확장 (366줄, 본체 100% 보존, append) — `FomcDayLiquidationConfig` SSOT (enabled/liquidationStartKstTime='14:30'/liquidationCompleteKstTime='15:20'/preAlertKstTimes/dryRun) + `getDefaultFomcDayLiquidationConfig()` env 4종 오버라이드 (FOMC_DAY_LIQUIDATION_ENABLED·DRY_RUN·START_KST·COMPLETE_KST) + `shouldExecuteLiquidationAt(now, opts?)` 5중 가드 SSOT (LiquidationGuardReason union: OK/NOT_DAY_PHASE/DISABLED/AUTO_TRADE_DISABLED/EMERGENCY_STOP/BEFORE_START_TIME/AFTER_COMPLETE_TIME). ② **`server/trading/fomcDayLiquidation.ts`** 신규 ~340줄 (@responsibility SRP) — `liquidateAllForFomc(now?, opts?)` 진입점 + `formatLiquidationResultMessage` + `runFomcDayMorningAlert` + `runFomcDayPreLiquidationAlert`. 청산 패턴은 r6EmergencyExit (placeKisSellOrder → reserveSell → addSellOrder LIVE 한정) 차용 — LIVE 본체 0줄 변경. `buildExitAttribution('FOMC_DAY_LIQUIDATION', ['fomc_day_force_close'], regime)` 학습 인풋 부착. 종목별 try/catch 격리로 한 종목 throw 가 다음 차단 안 함. ③ **`server/scheduler/orchestratorJobs.ts`** cron 3건 추가 (UTC 00:00 morning_alert / 05:00 pre_liq / 05:30 liquidation, 모두 `'TRADING_DAY_ONLY'` + `* * * 1-5` 이중 평일 가드 + 진입부 DAY phase 가드 = silent return 자연 흡수). ④ **`server/scheduler/scheduleCatalog.ts`** SCHEDULE_CATALOG +3 (09:00·14:00·14:30 KST, silentWhen='FOMC DAY phase 가 아니면 무음 (FOMC 캘린더 단일 SSOT, ADR-0061)'). ⑤ **`server/persistence/shadowTradeRepo.ts`** ExitRuleTag union 에 `'FOMC_DAY_LIQUIDATION'` (priority 50) 추가 + **`server/trading/tradeEventLog.ts`** TradeEvent.subType union 에 `'FOMC_DAY_LIQ'` 추가. ⑥ **클라 동기 사본 0건** — `src/api/autoTradeClient.ts:289`의 `exitRuleTag?: string` free-form, subType 미러 부재로 자동 호환 (절대 규칙 #3 자연 준수). ⑦ **회귀 테스트 +24 케이스 신규** — `fomcCalendar.test.ts +9` (getDefaultFomcDayLiquidationConfig 4: env 기본값/DISABLED/START_KST 오버라이드/dryRun=true + shouldExecuteLiquidationAt 5: 5중 가드 분기·14:29 BEFORE/14:30 OK/15:20 AFTER) + `fomcDayLiquidation.test.ts` 신규 +15 (vi.mock 으로 reserveSell+placeKisSellOrder+sendPrivateAlert+loadShadowTrades+getEmergencyStop+getFomcProximity 격리: 가드 5종 분기 / 0 active / 다중 active LIVE 결과 집계 / SHADOW dryRun reserveSell 미호출 / reserveSell throw 흡수 + 부분 실패 fallback / formatLiquidationResultMessage 3분기 dryRun·일반·실패 / morning_alert + pre_liq DAY 외 silent / attribution 부착 검증). ⑧ **ADR-0061 발행** + ARCHITECTURE.md Boundary Rules +3 (단일 진입점 / reserveSell SSOT 의무 / DAY phase 가드 자동 silent). ⑨ **검증** — vitest **306 files / 3259 tests pass** (신규 24 + 무회귀) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과. KIS 호출은 placeKisSellOrder 단일 통로 (절대 규칙 #2/#4 준수). LIVE 매매 본체 0줄 변경 — 신규 헬퍼 + cron + 텔레그램 알림만 추가. ⑩ **운영 시나리오 (4/29 FOMC DAY)** — KST 09:00 사전 경보 (HIGH, sendPrivateAlert dedupe `fomc_day_morning:{date}`) → 14:00 30분 전 경보 (HIGH dedupe `fomc_day_pre_liq:{date}`) → 14:30 자동 청산 (CRITICAL dedupe `fomc_day_liq_result:{date}`, 결과 N건 성공/M건 실패 표기) → 15:20 boundary 후 cron 발동 시 silent. **첫 운영 권장**: `FOMC_DAY_LIQUIDATION_DRY_RUN='true'` env 로 1회 검증 후 production 전환. **#2 FOMC 다음날 매수 재개**는 v3.1 POST_1=1.30 (Kelly +30% 부스트) + POST_2=1.15 로 이미 작동, 작업 0 검증 완료. | `docs/adr/0061-fomc-day-liquidation.md` (신규), `server/trading/fomcCalendar.ts` (config + 5중 가드 append), `server/trading/fomcDayLiquidation.ts` (신규 ~340줄), `server/trading/fomcDayLiquidation.test.ts` (신규 +15), `server/trading/fomcCalendar.test.ts` (+9), `server/scheduler/orchestratorJobs.ts` (+3 cron), `server/scheduler/scheduleCatalog.ts` (+3), `server/persistence/shadowTradeRepo.ts` (ExitRuleTag union), `server/trading/tradeEventLog.ts` (subType union), `ARCHITECTURE.md` (+3 Boundary Rules) | 사용자 5건 운영 보고 중 #1 FOMC 당일 전량 청산 정책 신설. FOMC v3.1 의 갭 — DAY 신규 진입 차단만 있고 보유 청산 부재 — 해소. FOMC 4/29 임박 시한 압박으로 PR-2 (USD/KRW stale 진단) + PR-3 (자동완성) 후속 분리 진행 예정. |
| 2026-04-27 | PR-2 매크로 신선도 라인 — USD/KRW stale 감지 운영 가시화 (사용자 보고 #5 후속): 사용자 보고 — `/regime` 메시지에 USD/KRW 1,380 표시되지만 실제 환율 1,474 (-6.8% 격차, 6개월 stale 의심). 갭 — `macroState.updatedAt` 필드는 영속됐지만 `/health` `/regime` 메시지에 *사용자가 신선도를 즉시 인지할 표면화* 부재로 stale 데이터로 의사결정 위험. ① **`server/health/diagnostics.ts` HealthSnapshot +2 필드** — `macroStateUpdatedAt?: string` (마지막 매크로 갱신 ISO) + `macroStateUsdKrw?: number` (환율 영속값, 격차 검증용). `collectHealthSnapshot()` 가 macroState.updatedAt + usdKrw 자동 propagate (cron 부재 시 undefined 안전). ② **`server/telegram/commands/system/health.cmd.ts`** `formatMacroFreshnessLine(s)` SSOT 신설 — 4분기 분류 (미수집/잘못된 ISO → ?, age ≤8h → ✅ <환율>원 (HH:MM, Nh), 8h<age≤24h → 🟡 갱신 지연, age>24h → ❌ STALE 24h+). USD/KRW 부재/0 → N/A 표기. age 24h+ 는 `Nd` 표기 (가독성). `formatHealthMessage` 에 "공매도 출처" 다음 줄에 "매크로 신선도:" 라인 삽입 (인접 그룹핑). ③ **`server/telegram/commands/system/regime.cmd.ts`** `formatRegimeFreshnessLine(updatedAt?)` SSOT 신설 + `/regime` 메시지 끝 "업데이트:" 라인 교체 — KST 풀 시각 + N시간/일 경과 + ⚠️/❌ 마커 (8h+ 갱신 지연, 24h+ STALE — `/health` 진단 안내). ④ **회귀 테스트 +9 케이스 신규** — `health.cmd.test.ts +9` (formatMacroFreshnessLine 7 분기: 미수집 / 잘못된 ISO / 2h 정상 ✅ / 12h 🟡 / 36h ❌ STALE / USD/KRW 부재 N/A / USD/KRW=0 N/A + formatHealthMessage 통합 3: 신선 ✅ 라인 정확히 1번 / STALE 24h+ 시나리오 사용자 보고 #5 / 공매도→매크로→Yahoo 순서 인접 그룹핑). 기존 70 케이스 보존. ⑤ **검증** — vitest **306 files / 3269 tests pass** (신규 9 + PR-1 무회귀) + lint(client + server tsc) EXIT=0. KIS/KRX 자동매매 quota 0 침범 (read-only 진단, 외부 호출 0). LIVE 매매 본체 0줄 변경 — UI/메시지 라인만 추가. ⑥ **운영 효과** — 운영자가 `/health` 한 번에 매크로 데이터 신선도 (마지막 갱신 시각 + 경과 시간 + USD/KRW 영속값) 즉시 인지. `/regime` 메시지에도 신선도 마커로 stale 정보 의사결정 차단. 사용자 보고 시나리오 (1,380 vs 실제 1,474) 재발생 시 ❌ STALE 24h+ 마커로 즉시 표면화. 후속 PR (cron 실행 실패 자동 진단 + macroState 자동 갱신 보강) 의 시드. ⑦ **잔여 P0**: ADR Gap / 섹터 ETF Momentum 카드 "데이터 없음" 의 직접 원인 진단 — cron 등록 정상 (alertJobs.ts adr_gap_scan KST 08:35 + sector_etf_momentum KST 06:15 ALWAYS_ON) 확인. 데이터 영속 위치 + UI 카드 데이터 fetch 경로 진단은 운영 데이터 누적 후 별도 PR. **PR-3** (자동완성) — PR #389 머지·재배포 후 자동 노출 우선, 미노출 확인 시 별도 PR 분리. | `server/health/diagnostics.ts` (+2 macroState 필드 + collectHealthSnapshot wiring), `server/telegram/commands/system/health.cmd.ts` (+formatMacroFreshnessLine SSOT + formatHealthMessage 라인 삽입), `server/telegram/commands/system/regime.cmd.ts` (+formatRegimeFreshnessLine SSOT + /regime 메시지 신선도 마커), `server/telegram/commands/system/health.cmd.test.ts` (+9 케이스) | 사용자 보고 #5 — USD/KRW 1,380 vs 실제 1,474 격차로 거시 데이터 신뢰성 의심. macroState 영속 데이터의 신선도를 사용자가 1메시지로 즉시 인지하도록 표면화. cron 자체는 정상 등록되어 있고 macroState 갱신 경로 audit 도 정상 — 본 PR 은 *운영 가시성 격상* 으로 stale 데이터 의사결정 차단의 첫 걸음. |
| 2026-04-27 | PR-α 시장 데이터 안정성 — Yahoo prefill overlay + AI 가격 위임 제거 (ADR-0064): 사용자 보고 "데이터 너무 불안정함" 진단 결과 `getMarketOverview` 가 S&P 500 / NASDAQ / Dow / Nikkei / CSI 300 / JPY/KRW / EUR/KRW / 금 / WTI 를 모두 Gemini AI 에게 *추정* 하도록 위임해 호출마다 hallucinated 가격이 흔들리던 구조적 결함 차단. ① **`src/services/stock/marketOverviewIndicators.ts` 신규** (@responsibility SRP) — `INDEX_TARGETS` (5) + `FX_TARGETS` (2) + `COMMODITY_TARGETS` (2) `as const` 배열 + `extractDataPoint(target, raw)` 순수 함수 + `fetchPrefilledMarketData()` Promise.allSettled 8 심볼 병렬 fetch + `applyPrefilledOverlay(aiResponse, prefilled)` overlay 정책 SSOT. ② **`marketOverview.ts` 수정** — `getMarketOverview()` 진입부에서 `fetchMarketIndicators()` (기존) + `fetchPrefilledMarketData()` (신규) 병렬 호출 + KOSPI/KOSDAQ 를 `kospiKosdaqIndices` 로 별도 합산 + USD/KRW 는 `macroCached` (ECOS) 우선. AI 프롬프트 변경 — "indices/exchangeRates/commodities 는 위 사전 수집값을 그대로 사용. AI 가 임의로 추정하지 마라". `normalizeMarketOverview(raw, prefillOverlay?)` 가 export 함수로 격상 + overlay 정책 적용: prefill 카테고리 ≥1 항목 → AI 응답 *완전히 무시* / 빈 배열 → AI fallback. ③ **회귀 테스트 23 케이스 신규** — `marketOverviewIndicators.test.ts` 17 (SSOT 3 + extractDataPoint 7 + applyPrefilledOverlay 4 + fetchPrefilledMarketData 4 graceful) + `marketOverview.normalize.test.ts` 6 (prefillOverlay 미전달 기존 동작 보존 / overlay 적용 시 hallucinated 값 *완전히* 덮어쓰기 / 빈 배열 fallback / sectorRotation flat 변환 보존 / regimeShiftDetector 변환 보존 / overlay+sectorRotation 동시 적용). ④ **자동매매 영향 0** — `getMarketOverview` 호출자는 `useMarketData.ts:58` 만 (signalScanner / autoTradeEngine / entryEngine 호출 0건, 절대 규칙 #4 미위반). LIVE 매매 본체 0줄 변경. ⑤ **검증** — vitest src/services/stock 10 files 84/84 pass (신규 23 + 무회귀 61). ⑥ **scope 외 (후속 PR)**: `interestRates` / `macroIndicators` (한국 3년물 / CPI / PCE / 실업률) 는 본 PR 에서 AI 위임 유지 → ECOS/FRED 위임은 별도 PR. 캐시 정책 6시간 버킷 그대로 유지 → PR-β 에서 SWR (60s fresh + 5min stale) 재설계. `/api/market-indicators` 9개 필드 null fallback 부재 → PR-γ disk snapshot 영속화. ⑦ **운영 효과** — 캐시 miss 시 AI 가 hallucinate 한 가격이 6시간 동안 캐시되어 잘못된 데이터로 표시되던 회귀 영구 차단 + AI 토큰 소비 감소 (가격 추정 영역 제거) + Yahoo intraday 신선도 적용. | `docs/adr/0064-market-overview-prefill-overlay.md` (신규), `src/services/stock/marketOverviewIndicators.ts` (신규), `src/services/stock/marketOverviewIndicators.test.ts` (신규 17 케이스), `src/services/stock/marketOverview.ts` (prefill 호출 + AI 프롬프트 개정 + normalizeMarketOverview export + overlay 적용), `src/services/stock/marketOverview.normalize.test.ts` (신규 6 케이스), `ARCHITECTURE.md` (+1 boundary +1 boundary rule) | 사용자 진단 3건 중 가장 critical 한 P0 (AI hallucination) 처리. PR-γ (disk snapshot) + PR-β (SWR) 후속 분리로 회귀 위험 격리. main 의 ADR-0061 (FOMC DAY) 와 번호 충돌 회피로 0064 재할당. |
| 2026-04-27 | PR-γ /api/market-indicators disk snapshot 영속화 + stale fallback (ADR-0065): 사용자 진단 3건 중 P1 — `marketDataRouter.ts:379-389` 가 9개 Yahoo 호출 `Promise.allSettled` 후 실패 필드를 그대로 null 응답해 UI 가 "—" 로 깜빡이거나 ('—' → '18.3' → '—' → '17.9') 서버 재배포 직후 빈 화면이 표시되던 회귀 차단. ① **`paths.ts` `MARKET_INDICATORS_SNAPSHOT_FILE`** (`data/market-indicators-snapshot.json`) 추가. ② **`server/persistence/marketIndicatorsSnapshotRepo.ts` 신규** — `MARKET_INDICATOR_FIELDS` SSOT 11 필드 키 (응답 스키마와 1:1) + `loadMarketIndicatorsSnapshot()` (빈 파일 / 손상 JSON / null / array 모두 빈 객체 fallback) + `saveMarketIndicatorsSnapshot()` (atomic write tmp → rename — race 시 이전 정상 본 보존) + `mergeFreshIntoSnapshot(current, fresh, capturedAt)` (성공 필드만 갱신, NaN/Infinity/quote price≤0 가드, null 필드 보존) + `fillMissingFromSnapshot(fresh, snapshot)` (null 필드 fill + `staleFields[]` 반환). ③ **`marketDataRouter.ts /market-indicators` 핸들러** — 9개 Yahoo 호출 직후 fresh 객체 11 필드 추출 → `loadMarketIndicatorsSnapshot()` → `fillMissingFromSnapshot` 으로 stale fill + staleFields 누적 → `mergeFreshIntoSnapshot` 으로 성공 필드만 갱신 → `saveMarketIndicatorsSnapshot` atomic write → `staleFields.length > 0` 시 `X-Field-Stale: vix,us10yYield,...` 응답 헤더 부착. snapshot I/O throw 시 fresh 그대로 반환 (graceful baseline = PR-γ 이전 동작). ④ **회귀 테스트 14 케이스 신규** — SSOT 1 (11 필드 키 정합) + load/save 4 (파일 부재 / round-trip / 손상 JSON / null·array 직접 저장 가드) + mergeFreshIntoSnapshot 5 (빈 snapshot 첫 갱신 / null 필드 보존 / NaN+Infinity 무시 / kospi quote price=0+NaN 무시 / updatedAt 동기화) + fillMissingFromSnapshot 4 (snapshot 부재 + null / fill+staleFields / 성공 필드 그대로 / 모든 11 필드 stale). ⑤ **검증** — vitest server/persistence + server/routes 4 files 29/29 pass (신규 14 + 인접 router 15 무회귀) + lint(client + server tsc) OK. ⑥ **자동매매 영향 0** — `/market-indicators` 호출자는 클라이언트 `fetchMarketIndicators` (`marketOverview.ts:7`) 가 거의 유일. 자동매매 경로는 macroState (서버 측 별도 SSOT) 사용. LIVE 매매 본체 0줄 변경. ⑦ **scope 외 (후속 PR)** — 클라이언트 stale UI 표기 (X-Field-Stale 헤더 → 배지) / 24h 이상 박제 stale 운영자 알림 / ECOS/FRED 거시지표도 동일 snapshot 패턴 / TTL 정책. ⑧ **운영 효과** — 첫 부팅 후 재배포 시 즉시 stale 값 복구 (Volume 영속) + 장중 일시 fetch 실패 시 UI flicker 영구 차단 + 회로차단기 동작 중에도 응답 유지. | `docs/adr/0065-market-indicators-snapshot-resilience.md` (신규), `server/persistence/paths.ts` (+MARKET_INDICATORS_SNAPSHOT_FILE), `server/persistence/marketIndicatorsSnapshotRepo.ts` (신규), `server/persistence/marketIndicatorsSnapshotRepo.test.ts` (신규 14 케이스), `server/routes/marketDataRouter.ts` (snapshot wiring + X-Field-Stale 헤더), `ARCHITECTURE.md` (+1 boundary +1 boundary rule) | 사용자 진단 P1 (UI flicker) 처리. PR-α 의 *클라이언트 fallback 회수 0 보장* 후방 안전망 동시 정착. PR-β (SWR 캐시) 별도 후속. main ADR-0062 (없음) 와 무관하지만 0061~0063 그룹 일관 재할당으로 0065. |
| 2026-04-28 | PR-R Shadow Learning Hooks Wiring — recordTwinEntries + recordRejection (ADR-0068, 자기학습 시리즈 PR-A~P 후속 wiring): 사용자 보고 audit 후속 — PR-L (rejectionShadowTracker) + PR-M (counterfactualTwinPortfolio) 가 모듈 + 회귀 테스트 + ADR 까지 완성됐지만 *signalScanner 호출 site* 에 wire-up 되지 않아 두 영속 데이터 (`data/rejection-shadow.json` / `data/twin-portfolio.json`) 가 비어있던 갭 해소. 사용자 메시지 "해당 부분 적용 실시" 후속. ① **`server/trading/signalScanner/perSymbolEvaluation.ts` wiring** — 기존 `recordCounterfactual` 가 이미 try/catch 격리로 안전 등록되어 있던 패턴을 그대로 차용. 두 hook 추가: (a) **`recordTwinEntries`** — `evaluateBuyList` 의 candidate 평가 진입 직후(`stageLog.price = 'PASS'` 다음 줄) 호출 — 모든 candidate 가 3 Twin 정책(AGGRESSIVE Gate≥14 / DISCIPLINED Gate≥22 / EQUAL_WEIGHT Gate≥18) 평가받아 통과한 Twin 만 영속. KST 일자(epoch+9h ISO 절단) + stockCode/Name + gateScore + entryPrice=currentPrice + kellyWeight=0.10 (sizingDecider 결과 입력은 후속 wiring) 전달. (b) **`recordRejection`** — `if (!revalResult.proceed)` 분기 안의 기존 `recordCounterfactual` 직후 호출 — Gate 14~17 near-miss 만 자동 영속, 그 외는 모듈 내부 silent skip. signalPriceKrw=currentPrice + rejectionReason=`entryRevalidation:${revalResult.failReasons.join(',')}` 으로 거절 사유 보존. ② **try/catch 격리 패턴** — 두 hook 모두 throw 시 `console.warn('[TwinPortfolio] record 실패 ...')` / `[RejectionShadow]` 출력 후 LIVE 매매 흐름 무중단. recordCounterfactual 와 동일 안전 패턴이라 회귀 위험 최소화. ③ **회귀 테스트 11 케이스 신규** (`server/trading/signalScanner/__tests__/shadowLearningHooks.test.ts`) — PR-R Hook 호출 검증 5 (recordTwinEntries Gate 16 → 1건 / Gate 24 → 3건 / Gate 12 → 0건 silent / recordRejection Gate 16 near-miss → 1건 / Gate 18 임계 통과는 거절 아님 silent) + LIVE 매매 무영향 try/catch 격리 2 (throw 가 caller 흐름 막지 않음 / silent skip 시 caller 무영향) + wiring import 검증 4 (perSymbolEvaluation 가 두 모듈 import / 두 hook 모두 try/catch 격리 / catch 에 console.warn 사유 포함 / recordRejection 이 entryRevalidation 분기의 recordCounterfactual 인근 위치). PERSIST_DATA_DIR mkdtempSync 격리로 영속 테스트 충돌 차단. ④ **검증** — vitest server/trading/signalScanner **20 files / 137 tests pass** (신규 11 + 무회귀 126) + server/learning/{rejectionShadowTracker,counterfactualTwinPortfolio} 41/41 pass + lint(client + server tsc) + ALLOW_DEPLOY_WINDOW=1 precommit 본체 (PRES + ACMA + SDS + SRP + SymbolBoundary + ChannelBoundary + SensitiveAlerts + Gemini + lint) 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/autoTradeEngine 무수정). LIVE 매매 본체 0줄 변경 — perSymbolEvaluation 의 candidate 평가 + entryRevalidation 분기에 hook 호출만 추가 (모두 try/catch 격리). ⑤ **운영 효과** — Phase B 분해 진행 중인 signalScanner 의 *현재* 호출 site 에서 두 학습 hook 활성화. 매 신호 스캔마다 (1) 모든 candidate 가 3 Twin 정책 평가받아 평행 포트폴리오 누적 / (2) entryRevalidation 거절 종목 중 Gate 14~17 near-miss 만 사후 5영업일 추적 → "거짓 부정 (놓친 알파)" 측정 + "다른 진입 정책의 평행 포트폴리오" 메타 평가 양 축이 처음으로 *데이터 레벨* 에서 작동. PR-L/M 의 ADR-0028/0029 가 의도한 효과가 본 PR 부터 가시화. ⑥ **잔여 wiring (후속 PR scope 밖)**: kellyWeight 정확값 (sizingDecider 결과 입력) / entryGates 거절 (entryRevalidation 이전 단계) 시점 wiring / 클라 PR-A~P (`useTradeStore`) ↔ 서버 SHADOW (`shadowTradeRepo`) 양방향 동기화. | `docs/adr/0068-shadow-learning-hooks-wiring.md` (신규), `server/trading/signalScanner/perSymbolEvaluation.ts` (recordTwinEntries import + candidate 진입 직후 try/catch wiring + recordRejection import + entryRevalidation fail 분기 try/catch wiring), `server/trading/signalScanner/__tests__/shadowLearningHooks.test.ts` (신규 11 케이스) | 사용자 audit 보고 "PR-L/M 모듈은 완성됐지만 signalScanner 호출 site 에 wire-up 안 됨" 후속. recordCounterfactual 와 byte-equivalent try/catch 격리 패턴으로 회귀 위험 격리 + 두 영속 SSOT (rejection-shadow.json / twin-portfolio.json) 처음으로 *실제 데이터* 누적 시작. 자기학습 시리즈 PR-A~P 의 메타 평가 인프라가 본 PR 부터 가시화. |
| 2026-04-27 | PR-β marketOverview SWR 캐시 — 6시간 버킷 키 제거 (ADR-0066): 사용자 진단 3건 중 P1 — `marketOverview.ts:201-202` 의 `cacheKey = market-overview-${date}-${hour/6}` 6시간 버킷 정책이 윈도우 boundary (예: 11:59 → 12:00) 에서 새 hallucinated 값으로 점프하던 회귀를 SWR (stale-while-revalidate) 패턴으로 차단. PR-α 가 *가격성* hallucination 차단했지만 AI 분석/서술 부분 (sectorRotation / dynamicWeights / summary / euphoriaSignals) 은 여전히 boundary 에서 점프 가능했음. ① **`src/services/stock/marketOverviewCache.ts` 신규** (@responsibility SRP) — 단일 슬롯 SWR 캐시 SSOT. `FRESH_TTL_MS=60s` / `STALE_TTL_MS=5min` / `HARD_EXPIRY_MS=30min` 3 임계 + `getOrFetchAiResponse(fetchFn, now?)` 4분기 (EMPTY / FRESH 즉시반환 / STALE 즉시반환+백그라운드 refresh / EXPIRED hard fetch) + inflight coalescing (동시 호출 시 fetchFn 1회만) + `getCacheState` 진단 헬퍼. 메모리 only (localStorage / 서버 Volume 영속 X — 단순화). ② **`marketOverview.ts` wiring** — `getCachedAIResponse(cacheKey, fetchFn)` 호출 제거. `getOrFetchAiResponse(fetchFn)` 으로 교체. AI raw 응답만 캐시 (normalize 적용 *전*) → prefill 결과는 매 호출마다 새로 적용 (ADR-0064 overlay 정책 보존, `/api/historical-data` LRU+coalescing 자동 보호). cacheKey 기반 6시간 버킷 정책 완전 제거. ③ **회귀 테스트 15 케이스 신규** — TTL 상수 정합 1 (FRESH < STALE < HARD) + SWR 4분기 (EMPTY 1 + FRESH 30s 캐시 hit + 59,999ms 경계 + STALE 2min 백그라운드 refresh + STALE 경계 5min hard fetch + EXPIRED 6min hard) + Inflight coalescing 2 (STALE 진행 중 / EMPTY 동시 첫 호출 → fetchFn 1회) + fetchFn 실패 2 (첫 호출 throw / STALE 실패 시 stale 보존+console.warn) + getCacheState 4 (EMPTY / FRESH / STALE / EXPIRED). ④ **검증** — vitest src/services/stock 11 files **99/99 pass** (신규 15 + PR-α/γ 누적 38 + 무회귀 46) + lint(client + server tsc) OK + validate:all 8종 OK. ⑤ **자동매매 영향 0** — 호출자 useMarketData 단독, LIVE 매매 본체 0줄 변경. ⑥ **scope 외 (후속 PR)** — `getCachedAIResponse` 자체 SWR 격상 (momentumRecommendations / enrichment 등 다른 호출자 적용) / 다중 prompt key Map 슬롯 / 운영자 텔레그램 진단 명령. ⑦ **운영 효과** — 윈도우 boundary 점프 영구 차단 (시간대 boundary 자체 부재) + 60초 fresh 안에서는 fetch 0 호출 → 빠른 재로드 부담 0 + 5분 stale 윈도우에서 사용자에게 즉시 응답 + 백그라운드 갱신 → 부드러운 데이터 흐름 + prefill 신선도는 매 호출마다 보장 (ADR-0064 overlay 매번 새로 적용). | `docs/adr/0066-market-overview-swr-cache.md` (신규), `src/services/stock/marketOverviewCache.ts` (신규), `src/services/stock/marketOverviewCache.test.ts` (신규 15 케이스), `src/services/stock/marketOverview.ts` (getCachedAIResponse → getOrFetchAiResponse + cacheKey 제거), `ARCHITECTURE.md` (+1 boundary +1 boundary rule) | 사용자 진단 P1 (윈도우 boundary 점프) 처리. PR-α (가격 hallucination) + PR-γ (UI flicker) + PR-β (윈도우 점프) 3 PR 시리즈로 사용자 진단 3건 모두 해소. main ADR-0061 충돌로 본 시리즈 0064/0065/0066 그룹 재할당. |
| 2026-04-27 | FOMC vs Regime Kelly 결합 SSOT — 옵션 C: FOMC 우선 + R6 보호 (ADR-0076): 사용자 운영 보고 "FOMC 와 기본 Kelly 레짐이 개념이 중복됨 (오버랩). FOMC 기간에는 기본 Kelly 레짐보다 FOMC 원칙을 우선." 기존 결합 (3 호출 지점 동일 — `signalScanner.ts:381` + `preflight.ts:329` + `dryRunScanner.ts:122`): `rawKelly = regimeKelly × fomcKelly × vixKelly × ipsKelly × ...` 곱셈. `regimeConfig.kellyMultiplier` 와 `fomcProximity.kellyMultiplier` 가 *같은 차원* (Kelly 사이즈 보수성) 의 정책이라 곱셈 결합 시 두 번 적용되어 보수성 누적 — FOMC PRE_1 (0.75) × R2_BULL (0.8) = 0.60 (사용자 의도 0.75 와 다름). ① **신규 SSOT 모듈** `server/trading/regimeFomcCombiner.ts` — `combineRegimeAndFomcKelly(regimeKelly, fomcKelly, fomcPhase, regime)` 우선순위 SSOT: (1) `FOMC_REGIME_OVERRIDE_DISABLED=true` ENV → 기존 곱셈 (LEGACY_PRODUCT) / (2) R6_DEFENSE → 0 강제 (R6_OVERRIDE — allowedSignals=[] 와 정합) / (3) FOMC NORMAL → regimeKelly 만 (REGIME) / (4) FOMC 활성 → fomcKelly 만 (FOMC, regime 무시). `RegimeFomcResult` 인터페이스 (value/source/reason) + `describeRegimeFomcCombination` 진단 헬퍼. ② **3 호출 지점 wiring** — signalScanner.ts/preflight.ts/dryRunScanner.ts 에서 곱셈 분리: `regimeFomcCombined.value × vixGating × ipsKelly × ...` (regime/fomc 결합 분리). 진단 로그 보강 — `[AutoTrade] [Kelly 결합] FOMC PRE_1 ×0.75 우선 (R2_BULL ×0.80 무시) → ×0.75 × VIX(×1.00) × IPS(×1.00) × 계좌(×1.00) = raw ×0.750 → 유효 ×0.75` 형식으로 어느 정책이 채택됐는지 즉시 인지. ③ **결과 매트릭스 변화** — PRE_3/PRE_2/PRE_1 (0.75) + R2_BULL (0.8): 0.60 → **0.75** (FOMC 우선) / PRE_1 + R5_CAUTION (0.3): 0.225 → 0.75 ⚠️ (FOMC 우선, R5 무력화 — 사용자 의도) / DAY (0) + R2_BULL: 0 (그대로) / POST_1 (1.30) + R2_BULL: 1.04 → **1.30** (부스트 회복) / POST_1 + R5_CAUTION: 0.39 → 1.30 ⚠️ (부스트, 운영자 인지 필요) / PRE_1 + R6_DEFENSE: 0 (R6_OVERRIDE 보호). ④ **회귀 테스트 33 케이스 신규** — `regimeFomcCombiner.test.ts`: R6_OVERRIDE 4 (NORMAL/PRE_1/POST_1 + R6 / 진단 메시지) + REGIME 4 (NORMAL + R2/R5/R1 + 진단) + FOMC 8 (PRE_3/PRE_2/PRE_1 + R2/R5 / DAY + R2 / POST_1 + R2/R5 / POST_2 + R3 / 진단) + LEGACY 5 (ENV true 시 곱셈 / NORMAL / POST_1 / 진단 / ENV false) + 매트릭스 9 (PR 본문 시나리오 검증) + describeRegimeFomcCombination 3 (FOMC/REGIME/R6 라인 형식). ⑤ **검증** — vitest **server/trading 82 files 793/793 pass** (신규 33 + 무회귀 760) + lint(client + server tsc) + validate:all 8종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과. KIS/KRX 자동매매 quota 0 침범 (계산 로직만 변경). LIVE 매매 본체 — 3 호출 지점에 `combineRegimeAndFomcKelly()` 1줄 + 곱셈 분리 (~20줄). ⑥ **운영 효과 (배포 후)** — FOMC PRE_1 시 사용자가 의도한 75% 사이즈 정확히 적용 (기존 60% 압축 해소) + POST_1 부스트 1.30 효과 회복 (기존 1.04 희석 해소) + 진단 로그에서 FOMC vs REGIME vs R6 어느 정책이 채택됐는지 즉시 식별. ⑦ **ENV 롤백** `FOMC_REGIME_OVERRIDE_DISABLED=true` — 기존 곱셈 패턴 즉시 복원. ⑧ **R5_CAUTION 위험 인지** — 약세 시장 + FOMC POST 부스트 1.30 적용은 사용자 의도 따라 활성. 향후 데이터 축적 후 옵션 D (부스트는 곱셈, 보수는 FOMC 우선) 격상 검토 가능. | `docs/adr/0076-fomc-regime-kelly-precedence.md` (신규), `server/trading/regimeFomcCombiner.ts` (신규 SSOT), `server/trading/regimeFomcCombiner.test.ts` (신규 33 케이스), `server/trading/signalScanner.ts` (wiring + 진단 로그 보강), `server/trading/signalScanner/preflight.ts` (wiring + 진단 로그 보강), `server/trading/dryRunScanner.ts` (wiring) | 사용자 명시 의도 "FOMC 우선" 정확 구현 + R6_DEFENSE 시장 전체 붕괴 안전망 보존. 곱셈 결합으로 인한 보수성 누적 / 부스트 희석 영구 차단. 단일 SSOT 모듈 + 진단 로그 + ENV 롤백 + 33 회귀 테스트로 회귀 위험 격리. |
| 2026-04-27 | PR-4 wiring 완성 — entryEngine Gate Score 적용 + macroState 영속 + /regime UI (ADR-0075 §Wiring): PR-4 (ADR-0075) 가 sectorScoreBoost SSOT 모듈만 만들고 wiring 미적용 상태였던 것을 매매 의사결정 본체에 연결. ① **macroStateRepo `sectorEnergyResult?` + `sectorEnergyUpdatedAt?` 옵셔널 필드 추가** — entryEngine 의 Gate Score 산출 시점에 read 만 (`SectorEnergyResult` import). 영속 후방호환 — 기존 macroState 파일은 갱신 사이클 후 자연 추가. ② **`marketDataRefresh.refreshMarketRegimeVars()` 끝부분에 sectorEnergy 영속 wiring** — `getSectorEnergyInputs()` (KRX 30분 캐시) → `evaluateSectorEnergy(inputs)` → macroState merge. 입력 0건 시 graceful (이전 값 보존, console.debug). 호출 비용 KRX OpenAPI 5건 — marketDataRefresh 일일 cron 이라 부담 없음. 실패 시 console.warn + 보너스 0 자연 폴백. ③ **`entryRevalidationStep` 에 `sectorBoost?` + `sectorBoostReason?` 옵셔널 추가** — `boostedGateScore = (rawGateScore ?? 0) + (sectorBoost ?? 0)` 으로 minGate 비교. fail 시 진단 메시지에 `[반도체 +2 (LEADING)]` 같은 boost 사유 명시. ④ **`perSymbolEvaluation.ts` 호출자 wiring** — `applySectorScoreBoost(stock.sector, ctx.macroState?.sectorEnergyResult, ctx.regime)` 한 줄 + `describeSectorBoost` 진단 텍스트 생성 + entryRevalidationStep 에 전달. ⑤ **`/regime` 메시지 `formatSectorEnergyLine` SSOT 라인 추가** — `🎯 강세 반도체, 이차전지, 바이오/헬스케어 / 소외 건설/부동산, 유통/소비재` 1줄. macroState 부재 시 "N/A (미수집)" / 데이터 부족 시 "데이터 부족" / LEADING 만 → 강세만 / LAGGING 만 → 소외만 분기. ⑥ **회귀 테스트 11 케이스 신규** — entryRevalidationStep.test.ts +6 (sectorBoost 미전달 기존 동작 / +2 통과 / +2 보너스로도 부족 / -1 LAGGING / boost reason 미전달 시 진단 텍스트 부재 분기 2) + regime.cmd.test.ts +5 (formatSectorEnergyLine 5 분기: 부재 / 데이터 부족 / LEADING 만 / LAGGING 만 / 둘 다). ⑦ **검증** — vitest **server/trading + server/telegram + server/persistence 121 files 1195/1195 pass** (신규 11 + 무회귀 1184) + lint(client + server tsc) + validate:all 8종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과. KIS/KRX 자동매매 quota 영향 — marketDataRefresh 일일 cron 에 KRX OpenAPI 5건 추가 (캐시 30분, 다른 cron 과 공유). LIVE 매매 본체 수정 — `entryRevalidationStep` 에 옵셔널 인자 2개 추가 + `perSymbolEvaluation` 에 wiring 1 블록 (15줄). ⑧ **운영 효과 (배포 후)** — 일일 marketDataRefresh 사이클에서 sectorEnergyResult 영속화 → 다음 perSymbolEvaluation 사이클부터 LEADING 섹터 종목이 Gate Score +2 부스트로 minGate 임계 통과 유리. AI 추천에서 강세 섹터 종목 우선 + Bear/Caution 레짐에서 LAGGING 섹터 -1 차감으로 약세 시장 약한 섹터 자연 후순위. `/regime` 응답에 `🎯 강세 반도체, 이차전지` 라인으로 운영자 즉시 인지. | `server/persistence/macroStateRepo.ts` (+sectorEnergyResult/sectorEnergyUpdatedAt + SectorEnergyResult import), `server/trading/marketDataRefresh.ts` (sectorEnergy 영속 wiring + import 2종), `server/trading/signalScanner/revalidationSteps/entryRevalidationStep.ts` (sectorBoost 옵셔널 + boostedGateScore 산출), `server/trading/signalScanner/revalidationSteps/__tests__/entryRevalidationStep.test.ts` (+6 케이스), `server/trading/signalScanner/perSymbolEvaluation.ts` (sectorBoost 계산 + entryRevalidationStep 전달), `server/telegram/commands/system/regime.cmd.ts` (+formatSectorEnergyLine SSOT + 메시지 라인 삽입), `server/telegram/commands/system/regime.cmd.test.ts` (+5 케이스) | PR-4 인프라 (ADR-0075) 위에 wiring 완성 — sectorEnergyEngine 결과가 영속·매매 의사결정·UI 3 경로에 모두 연결. AI 추천에서 "관계없는 종목" 자연 후순위 + 강세 섹터 우선. `SECTOR_SCORE_BOOST_DISABLED=true` 즉시 롤백 가능. |
| 2026-04-27 | 4/27 운영 개선 4종 일괄 (PR-1/2/3/4): 사용자 운영 보고 4종 — ① 거시 regime 신뢰도 ② 매수 직후 급락 보호 ③ 강세 섹터 가산점 ④ 레짐 holding 단축. 권장 순서대로 (자금 보호 → 단순 SSOT → UI → 인프라) 일괄 진행. ① **PR-1 Entry Circuit Breaker (ADR-0072)** — 신규 청산 규칙 `entryCircuitBreaker.ts` 추가 (priority 9.5, cascadeWarn 다음). 트리거 조건 (모두 AND): holdingMinutes ≤ 60 + returnPct ≤ -5% + 1회성 (entryCircuitTriggered=false). 액션: 50% 즉시 청산 + addBuyBlocked=true + ExitRuleTag='ENTRY_CIRCUIT_BREAKER' 학습 격리 + CRITICAL 텔레그램. 현대제철·리노공업 매수 직후 급락 시나리오 (종합지수 양호 + 단일 종목 충격) 의 일반 손절선(-7%) 도달 *전* 보호 부재 갭 차단. ENV 롤백 `ENTRY_CIRCUIT_BREAKER_DISABLED=true`. ② **PR-2 레짐 holding 단축 (ADR-0073)** — `kellyHalfLife.ts` REGIME_HALF_LIFE_DAYS 6 항목 + DEFAULT 단축: R1_TURBO 7→5 / R2_BULL 10→8 / R3_EARLY 12→10 (사용자 직접 요청) / R4_NEUTRAL 10→8 / R5_CAUTION 8→6 / R6_DEFENSE 5→4 / DEFAULT 10→8. 평균 6.83 영업일 (-21%). `accountRiskBudget.computeRiskAdjustedSize` (ADR-0008 wiring) 가 자동 사용 → 보유 N일 후 Kelly 가중치 자동 감소 → 후속 추가매수 차단 + 트림 권고 알림 빠른 작동. ③ **PR-3 `/regime` 매매 레짐 라인 (ADR-0074)** — 메시지가 macroState.regime (GREEN/YELLOW/RED) 만 표시했고 매매 결정 SSOT 인 getLiveRegime() RegimeLevel R1~R6 미노출 갭 해소. `formatLiveRegimeLine(liveRegime)` SSOT 헬퍼 신설 — 6 이모지 (🛑R6 / 🟡R5 / 🟠R4 / 🌱R3 / 🟢R2 / 🔥R1) + Kelly 배율 + 최대 포지션 1줄 표기. R6_DEFENSE 는 "신규 진입 차단" 명시 표기 (Kelly ×0.00 표기 회피). 운영자가 "왜 매매 안 되지?" 같은 질문에 메시지에서 즉시 답 가능. ④ **PR-4 강세 섹터 Gate Score 가산점 SSOT (ADR-0075, 인프라만 — wiring 후속)** — sectorEnergyEngine 의 LEADING/NEUTRAL/LAGGING 3 tier 분류가 이미 작동 중이지만 `getSectorGate2Adjustment` 호출자 0건 — Gate Score 산출 경로에 wiring 미적용. 신규 SSOT 모듈 `sectorScoreBoost.ts` — `applySectorScoreBoost(sectorName, sectorResult, regime)` 매트릭스: LEADING +2 (모든 레짐) / LAGGING -1 (Bear/Caution 만, BULL/EARLY/NEUTRAL 에선 0) / NEUTRAL 0 / 안전 fallback. 본 PR 은 인프라 + 회귀 테스트만 — entryEngine wiring + macroStateRepo 영속 + marketDataRefresh 갱신은 회귀 위험 격리를 위해 후속 PR. ENV 롤백 `SECTOR_SCORE_BOOST_DISABLED=true`. ⑤ **회귀 테스트 65 케이스 신규** — entryCircuitBreaker.test.ts 18 (상수 SSOT + computeHoldingMinutes 4 경계값 + 트리거 분기 5 + 1회성 가드 1 + ENV 롤백 2 + 50% 수량 계산 3 + 텔레그램 1 + FAILED 롤백 1 + 1자 임시 보강) + kellyHalfLife.test.ts +6 (ADR-0073 단축 6: 6 레짐 모두 + DEFAULT + 단조성 + R3_EARLY 명시 + 평균 6.83 + fallback) + regime.cmd.test.ts +7 (formatLiveRegimeLine 6 RegimeLevel + 형식 일관성) + sectorScoreBoost.test.ts 29 (상수 + 안전 fallback 5 + LEADING 4 + LAGGING Bear 7 + ENV 3 + classifySectorTier 4 + describeSectorBoost 5). ⑥ **검증** — vitest **server/trading + server/telegram 21 files 173/173 pass** (신규 65 + 무회귀 108) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 본체 무수정). LIVE 매매 본체 0줄 변경. ⑦ **운영 효과** — (PR-1) 진입 직후 급락 시나리오 50% 자본 보호 + 학습 격리. (PR-2) 평균 보유일 -20% → 자본 회전율 +25%. (PR-3) 운영자 의사결정 신뢰도 격상 — 매크로 GREEN + 매매 R2_BULL 동시 노출. (PR-4) wiring 후속 PR 시 강세 섹터 Gate +2 부스트로 AI 추천에서 관계없는 종목 자연 후순위. | `server/trading/exitEngine/rules/entryCircuitBreaker.ts` (신규), `server/trading/exitEngine/rules/__tests__/entryCircuitBreaker.test.ts` (신규 18 케이스), `server/trading/exitEngine/index.ts` (EXIT_RULES_IN_ORDER 등록), `server/persistence/shadowTradeRepo.ts` (+entryCircuitTriggered? + ENTRY_CIRCUIT_BREAKER ExitRuleTag), `server/trading/tradeEventLog.ts` (subType union +ENTRY_CIRCUIT_BREAKER), `server/trading/kellyHalfLife.ts` (REGIME_HALF_LIFE_DAYS + DEFAULT 단축), `server/trading/kellyHalfLife.test.ts` (+6 케이스), `server/telegram/commands/system/regime.cmd.ts` (+formatLiveRegimeLine SSOT + 메시지 라인 삽입), `server/telegram/commands/system/regime.cmd.test.ts` (+7 케이스), `server/trading/sectorScoreBoost.ts` (신규), `server/trading/sectorScoreBoost.test.ts` (신규 29 케이스), `docs/adr/0072-entry-circuit-breaker.md` (신규), `docs/adr/0073-regime-holding-period-shortening.md` (신규), `docs/adr/0074-regime-message-live-regime-line.md` (신규), `docs/adr/0075-sector-score-boost.md` (신규) | 사용자 4/27 운영 보고 4종을 자금 보호 우선순위로 일괄 진행. 매매 안전(PR-1) + 자본 회전(PR-2) + 신뢰성(PR-3) + 인프라(PR-4) 4 PR 누적으로 즉시 효과 + 후속 wiring 기반 마련. PR-4 wiring (entryEngine Gate Score 적용 + macroState 영속 + marketDataRefresh 갱신) 은 Gate 평가 본체 변경 회귀 위험 격리를 위해 별도 PR 분리 — 운영 데이터 누적 후 진행. |
| 2026-04-27 | 4/27 운영 진단 3종 일괄 — Telegram HTML escape 누수 차단 + safePctChange symbol 추적 + 청산 stdout stockCode (PR-A/B/C, 사용자 4/27 거래로그 진단 후속): 사용자 "PR-A → PR-B → PR-C 권장 순서대로 진행" — 즉시 효과 큰 순서로 3 PR 통합. ① **PR-A `suggestNotifier.ts` HTML escape**: 4/27 16:05 KST `regimeCoverageSuggest` 알림이 `[Telegram] HTML 파싱 실패 → plain text` 로 강등된 직접 원인 — `regimeBalancedSampler.ts:155 (title="& dry")` 와 `:162 (threshold="<50% & ...")` 가 `<` `&` 를 raw 통과시켜 Telegram HTML 파서가 잘못된 태그/entity 로 오해. `escapeSuggestHtml(s)` SSOT 헬퍼 신설 (`&` → `&amp;` 첫 번째 + `<` → `&lt;` + `>` → `&gt;`) + `sendSuggestAlert` 메시지 빌더의 5 사용자 입력 필드(moduleKey/title/rationale/currentValue/suggestedValue/threshold) 모두 escape 통과. `<b>...` 헤더는 빌더가 직접 구성하므로 escape 없이 보존. ② **PR-B `yahooQuoteAdapter.ts` symbol label**: 4/27 16:30 KST `[safePctChange] sanity 위반 @yahooQuoteAdapter.changePercent` `305.28% (current=115,100, base=28,400)` + `@yahooQuoteAdapter.return5d 90.37%` 두 위반에서 *어떤 종목인지 식별 불가능* 한 진단 갭. label 3곳 (line 104 changePercent / line 190 return5d / line 197 return20d) 에 `:${symbol}` 패턴 추가 → Railway 로그에서 `yahooQuoteAdapter.return5d:005930.KS` 형식으로 종목 즉시 추적 가능. label 별 1분 throttle 도 종목별 분리되어 다른 종목 위반은 즉시 표면화. ③ **PR-C `exitEngine` 청산 stdout stockCode + 사이클 요약**: 4/27 [Ledger] TP=0 SL=3 EXP=0 의 SL=3 이 *학습 시뮬레이션 카운터* 라 실제 손절 종목 1건 식별 불가능. 6 핵심 청산 규칙 (hardStopLoss / r6EmergencyExit / cascadeFinal / cascadeHalf / trailingStop / legacyTakeProfit) 의 `[AutoTrade]` stdout 라인에 `(${shadow.stockCode})` 6자리 코드 추가 + `exitEngine/index.ts _updateShadowResultsImpl` 끝에 `[ExitEngine] 청산 사이클 요약: HIT_STOP N건 [종목명(코드)...] | HIT_TARGET M건 [...] | 잔여 활성 K개` 1줄 추가. 운영자가 Railway 로그에서 텔레그램 없이도 손절 종목 즉시 식별 + 잔여 포지션 카운트 인지 가능. ④ **회귀 테스트 19 케이스 신규** — `suggestNotifier.test.ts` +9 (escapeSuggestHtml 6 분기: `&`/`<`/`>`/`&` 우선/no-op/빈 문자열 + 4/27 시나리오 1 (regimeCoverage title+threshold escape 통과) + moduleKey 1 + rationale/currentValue/suggestedValue 1) + `yahooQuoteAdapter.test.ts` +3 (changePercent SPLIT-CASE.KS 시나리오 / return5d STALE-5D.KS / 정상 종목 0건 회귀 가드) + `stockCodeStdoutLogs.test.ts` 신규 7 (6 청산 규칙 stdout `stockName (stockCode)` 패턴 검증 + 형식 일관성 회귀 가드). ⑤ **검증** — vitest **server/learning + server/screener/adapters + server/trading/exitEngine 19 files 122/122 pass** (신규 19 + 무회귀 103) + lint(client + server tsc) + validate:all 8종 (Gemini/ACMA/SDS/PRES/SRP/SymbolBoundary/ChannelBoundary/SensitiveAlerts) + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 본체 무수정). LIVE 매매 본체 0줄 변경. ⑥ **운영 효과** — (PR-A) 4 학습 모듈(counterfactual/ledger/kellySurface/regimeCoverage) 의 HTML 알림이 Telegram bold + entity 정상 렌더, plain text 강등 영구 차단. (PR-B) 다음 sanity 위반 발생 시 종목 코드 즉시 식별 → watchlist 강제 제외 또는 Yahoo 데이터 출처 점검 즉시 가능. (PR-C) 손절 1종목 + 잔여 5종목 시나리오 같은 운영 질문에 Railway 로그 1회 grep 으로 즉시 답변. 향후 LIVE 전환 후에도 동일 형식 유지. | `server/learning/suggestNotifier.ts` (+escapeSuggestHtml SSOT + 5 필드 escape wiring), `server/learning/suggestNotifier.test.ts` (+9 케이스), `server/screener/adapters/yahooQuoteAdapter.ts` (3 label 에 `:${symbol}` 추가), `server/screener/adapters/yahooQuoteAdapter.test.ts` (+3 케이스), `server/trading/exitEngine/rules/{hardStopLoss,r6EmergencyExit,cascadeFinal,cascadeHalf,trailingStop,legacyTakeProfit}.ts` (6 청산 stdout stockCode 추가), `server/trading/exitEngine/index.ts` (+사이클 요약 라인), `server/trading/exitEngine/rules/__tests__/stockCodeStdoutLogs.test.ts` (신규 7 케이스) | 4/27 운영 보고 (손절 1종목 + safePctChange 위반 + Telegram HTML 실패) 의 3 P0/P1 진단 갭을 작은 변경 + 회귀 테스트로 일괄 차단. 향후 동일 패턴 재발 시 Railway 로그만으로 즉시 진단 가능 + 학습 알림 가시성 영구 격상. |
| 2026-04-27 | 다중 소스 교차 검증 — USD/KRW Yahoo + ECOS dual-source (ADR-0071): 사용자 보고 "/regime 메시지 USD/KRW=1,380 표시되지만 실제 시장 1,474. 신선도 라인은 ✅ 0.4h 인데 환율 일치 안 함. 연쇄적으로 다른 지표들 신뢰문제도 생김." 근본 원인 — `marketDataRefresh.ts:425` 가 USD/KRW 를 Yahoo `KRW=X` 단일 소스로만 fetch. ECOS `fetchLatestUsdKrw()` (한국은행 공식) 클라이언트가 이미 있지만 호출자 0건. ① **`server/trading/crossSourceValidator.ts` 신규** (@responsibility SRP) — `evaluateCrossSource(primary, secondary, label, thresholds?)` SSOT 6 tier 분류 (AGREED 격차<3% primary 우선 / WARN 3~5% secondary 우선 / CRITICAL ≥5% secondary 우선+diverged=true / PRIMARY_ONLY / SECONDARY_ONLY / NO_DATA). `computeDivergencePct(p,s)` 분모 0/NaN/Infinity 안전 fallback. ② **marketDataRefresh.ts wiring** — Yahoo `KRW=X` + ECOS `fetchLatestUsdKrw()` 병렬 fetch → `evaluateCrossSource` → CRITICAL 시 ECOS 우선 + 🛑 CRITICAL 텔레그램. ECOS_API_KEY 미설정 시 graceful fallback (PRIMARY_ONLY → Yahoo 그대로). ③ **macroState 옵셔널 필드 3종** — `usdKrwSource` ('PRIMARY' | 'SECONDARY') / `usdKrwDivergencePct` / `usdKrwDivergenceTier`. ④ **/regime 메시지 `formatUsdKrwLine` SSOT** — 8 분기로 출처 + 격차 % 노출. AGREED: "1,380 (Yahoo)" / CRITICAL: "1,474 (ECOS) ❌ 격차 6.38%" / WARN: "⚠️ 격차 3.50%" / PRIMARY_ONLY: "1,380 (Yahoo·ECOS 미수집)" / SECONDARY_ONLY: "1,474 (ECOS·Yahoo 미수집)" / 레거시 macroState fallback. ⑤ **MarketDataHealthScore 6번째 axis 추가** — DIVERGENCE_DEDUCTION: CRITICAL -25 / WARN -10 / PRIMARY_ONLY · SECONDARY_ONLY -5 / AGREED · NO_DATA 0. ⑥ **회귀 테스트 78 케이스 신규** — `crossSourceValidator.test.ts` 29 (computeDivergencePct 9 + evaluateCrossSource 11 + 임계 override 3 + formatTier 6) + `regime.cmd.test.ts` 10 (8 분기 + 데이터 부재 + 레거시 fallback) + `marketDataHealth.test.ts +7` (CRITICAL/WARN/PRIMARY_ONLY/AGREED/NO_DATA/옵셔널/누적). ⑦ **검증** — lint(client + server tsc) + validate:all 9종 + vitest 78/78 신규 + 무회귀 모두 통과. LIVE 매매 본체 0줄 변경 — marketDataRefresh USD/KRW 영역 wiring + macroState 3 옵셔널 필드 + /regime 라인만 추가. ⑧ **운영 효과** — Yahoo 가 stale 1,380 반환 + ECOS 정상 1,474 → 격차 6.38% CRITICAL → ECOS 우선 사용 + 운영자 즉시 텔레그램 알림. /regime 메시지에 출처(ECOS) + 격차(6.38%) 1줄 표기로 신뢰 문제 1초 인지. 후속 ADR — VKOSPI/VIX/US10Y 다중 소스 확장, divergence 자동매매 진입 차단 정책 검토. | `docs/adr/0071-cross-source-data-validation.md` (신규), `server/trading/crossSourceValidator.ts` (신규), `server/trading/crossSourceValidator.test.ts` (신규 29 케이스), `server/trading/marketDataRefresh.ts` (USD/KRW dual-source wiring), `server/persistence/macroStateRepo.ts` (+3 옵셔널 필드), `server/health/marketDataHealth.ts` (+divergence axis), `server/health/marketDataHealth.test.ts` (+7 케이스), `server/telegram/commands/system/regime.cmd.ts` (+formatUsdKrwLine SSOT), `server/telegram/commands/system/regime.cmd.test.ts` (신규 10 케이스), `ARCHITECTURE.md` (+1 boundary +1 boundary rule) | 사용자 보고 직접 대응 — *신선도 ≠ 정확성* 갭 해소. ADR-0064~0070 의 7-layer 위에 *수치 정확성* 검증 레이어 추가. PR #392 (ADR-0067~0070) 후속 commit 으로 같은 브랜치 진행. |
| 2026-04-27 | Gap 1 — TradeSignalStatus 6단계 상태머신 영속 SSOT (ADR-0077, 사용자 audit 후 "gap1 만해도 될 것 같다" 명시 승인): 사용자 12-영역 자동매매 안전 게이트 제안서 audit 결과 8/12 FULL + 1/12 MISSING + 3/12 PARTIAL — 진짜 갭 3개 중 가장 foundation 인 Gap 1 (AI 추천→buyApproval 큐 사이 명시적 상태 전이 영속) 처리. 기존 게이트(EntryGate Phase B 7 + RevalidationStep 5 + SizingDecider 3)는 모두 작동 중이지만 단계별 통과·차단 결과가 영속화 안 되어 운영자가 "이 종목이 어느 게이트에서 멈췄는지" 추적 불가. ① **`server/persistence/tradeSignalStatusRepo.ts` 신규** (445 LoC, @responsibility SRP) — `TradeSignalStatus` 6 union(`AI_CANDIDATE`→`DATA_VERIFIED`→`RISK_APPROVED`→`USER_APPROVED`→`AUTO_TRADE_READY` 또는 `BLOCKED`) + `TradeSignalBlockGate` 13 분류(DATA/GATE_0/GATE_1/GATE_2/GATE_3/RISK_BUDGET/SECTOR/PORTFOLIO/ENEMY/USER_REJECT/AUTO_TRADE_DISABLED/EMERGENCY_STOP/OTHER) + `TradeSignalRecord` (id=`${signalTimeIso}:${stockCode}` + transitions[] + blockGate/blockReason + finalizedAt) + `canTransition` 매트릭스 SSOT (RISK_APPROVED → AUTO_TRADE_READY 직접 전이 허용 — SHADOW + 향후 auto-approve) + 6 mutating API (`recordAiCandidate`/`markDataVerified`/`markRiskApproved`/`markUserApproved`/`markAutoTradeReady`/`markBlocked`) + 3 read API. 영속: atomic write tmp→rename + FIFO 1000 trim + transitions max 32 + 손상 JSON 빈 배열 fallback + 모든 mutating 멱등 + canTransition false silent skip + 잘못된 입력 null. 외부 의존성 0 (fs/path + paths.ts 만). ② **wiring 4 진입점** — `perSymbolEvaluation.ts` 의 4 `addRecommendation` 호출(PRE_BREAKOUT_FOLLOWTHROUGH 412 / PRE_BREAKOUT 547 / 메인 buyList 976 / INTRADAY_STRONG 1209) 직후에 `recordAiCandidate` try/catch wrap + `signalTime` ISO 변수 추출 + 4 `createBuyTask` 호출에 `signalId` 전달. `buyApproval.ts` `requestBuyApproval` params + `PendingApproval` 인터페이스에 `signalId?` 옵셔널 추가 + `handleBuyApprovalCallback` 의 buy_approve→`markUserApproved` / buy_reject→`markBlocked('USER_REJECT')` (SKIP 은 호출 0건). `buyPipeline.ts` `CreateBuyTaskParams.signalId?` + `requestBuyApproval` propagate + KIS 실주문 직전(LIVE) 및 SHADOW 분기 직전 `markAutoTradeReady`. **모든 wiring try/catch 보호** (영속 실패가 매매 결정 차단 안 함). **기존 게이트 본체 0줄 변경** (signalTime ISO 변수 추출은 동치 변환). ③ **`/signal_status [N]` 텔레그램 명령** (`signalStatus.cmd.ts`, alias `/signals`, SYS riskLevel=0 ADMIN) — `formatSignalStatusMessage` SSOT: 빈 records → "최근 추천 없음" / N건 → 한 줄당 emoji(✅AUTO_TRADE_READY/🟢DATA·USER_APPROVED/🟡RISK_APPROVED/🌱AI_CANDIDATE/❌BLOCKED) + 종목 + 상태 + KST HH:MM(잘못된 ISO → ?) + 마지막 전이 사유(BLOCK 시 blockGate + blockReason). N 인자 1~30 (잘못된 입력 → 10 fallback). barrel `commands/system/index.ts` 자동 등록. ④ **회귀 테스트 50 케이스 신규** — `tradeSignalStatusRepo.test.ts` 29 (canTransition 매트릭스 8 + buildSignalId 2 + 영속 round-trip 3 + recordAiCandidate 4 + 정상 전이 시퀀스 6 + markBlocked 분기 3 + FIFO trim 1 + 진단 read 2) + `signalStatus.test.ts` 11 (formatSignalStatusMessage 5 분기 + cmd execute 인자 4 + throw graceful + 메타데이터 1) + `buyApproval.statusWiring.test.ts` 5 (signalId+APPROVE/REJECT/SKIP/미전달 미호출 / markUserApproved throw 차단 안 함) + `buyPipeline.statusWiring.test.ts` 5 (signalId propagate / SHADOW APPROVE 호출 / 미전달 미호출 / REJECT 미호출 / markAutoTradeReady throw 차단 안 함). ⑤ **검증** — vitest **server/persistence + server/telegram + server/trading 86 files 873/873 pass** (신규 50 + 무회귀) + lint(client + server tsc) + validate:all 8종 (DeployWindow + PRES + ACMA + SymbolBoundary + ChannelBoundary + SensitiveAlerts + MarketOverviewBoundary + Gemini) + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/aiUniverseService/autoTradeEngine 본체 무수정). LIVE 매매 본체 0줄 변경 — 영속 호출만 추가, 모든 wiring try/catch 보호. ⑥ **운영 효과** — 운영자가 텔레그램 `/signal_status` 한 번에 직전 N건 추천이 어느 게이트에서 차단됐는지 즉시 추적 (예: "247540 에코프로 BLOCKED — SECTOR(배터리 32% 초과)"). buyApproval 콜백이 USER_APPROVED/USER_REJECT 영속화 → 후속 PR (UI 카드 / Auto Approval / Enemy 자동 차단)의 데이터 인프라 시드. SHADOW 모드는 RISK_APPROVED→AUTO_TRADE_READY 직접 전이. ⑦ **본 PR scope 밖 (후속)** — DATA_VERIFIED/RISK_APPROVED 단계 명시 wiring (메인 buyList 본체 위험 격리), Auto Approval 정책 (Gap 3, 별도 ADR), Enemy Checklist 자동 차단 (운영 데이터 누적 후 임계 결정), AutoTradePage 신호 상태 UI 카드 (PR-Z 시리즈 후속). | `docs/adr/0077-trade-signal-status-state-machine.md` (신규), `server/persistence/paths.ts` (+TRADE_SIGNAL_STATUS_FILE), `server/persistence/tradeSignalStatusRepo.ts` (신규 445 LoC), `server/persistence/tradeSignalStatusRepo.test.ts` (신규 29 케이스), `server/telegram/commands/system/signalStatus.cmd.ts` (신규), `server/telegram/commands/system/signalStatus.test.ts` (신규 11 케이스), `server/telegram/commands/system/index.ts` (+barrel), `server/telegram/buyApproval.ts` (+signalId? 옵셔널 + callback wiring), `server/telegram/buyApproval.statusWiring.test.ts` (신규 5 케이스), `server/trading/signalScanner/perSymbolEvaluation.ts` (4 wiring + import), `server/trading/buyPipeline.ts` (CreateBuyTaskParams.signalId? + requestBuyApproval propagate + LIVE/SHADOW markAutoTradeReady), `server/trading/buyPipeline.statusWiring.test.ts` (신규 5 케이스), `ARCHITECTURE.md` (+2 boundary +1 boundary rule) | 사용자 12-영역 audit 후 명시 승인 ("gap1 만해도 될거 같은데"). audit 결과 사용자 제안서가 이미 80% 구현 (Gate 0~3 + Enemy + DataQuality + RecommendationHistory + AutoTradePage 계기판 + EntryGate Chain + SizingDecider 등) 상태에서 진짜 갭은 *명시적 상태머신 SSOT*. 본 PR 은 인프라 + 4 wiring + 텔레그램 진단 명령으로 갭 1 단계 차단. 후속 단계는 운영 데이터 누적 + 회귀 위험 격리 후 분리. |
| 2026-04-28 | PR-Z7 H1 + H2 — full-bug-audit High 2건 즉시 수정 (ADR-0079 PR-Z7 H1 + ADR-0081, 같은 브랜치 follow-up): 사용자 audit 요청 후 트레이딩 LIVE bug audit 결과 *Critical 0건 / High 2건 / Medium 3건 / Pass 16건* 중 High 2건 즉시 수정. **H1 — stopApproachAlert BEP Glide LOSS_STOP 오분류** (ADR-0079 PR-Z7 follow-up): ADR-0079 PR-Z6 적용 후 `bepGlideStopPrice(10000, 1500) = 9250` 손절선 설정 시 `classifyStopSource(9250, 10000)` 가 -7.5% 차이 > BEP_TOLERANCE_PCT(0.5%) 로 **`LOSS_STOP` 분류 + "🚨 [손절 임박] 매수가 -7.50%"** 메시지 발송 → ADR-0079 의도(수익 보호)와 *반대 라벨* 로 사용자 패닉 매도 유도하던 결함. 수정: `classifyStopSource` 시그니처에 `entryATR14?` 옵셔널 추가 + 분류 4번째 분기로 글라이드 영역(`entryPrice − 0.5×ATR ≤ hardStopLoss < entryPrice`) `BEP_PROTECTION` 흡수. `BEP_GLIDE_DISABLED=true` env 시 글라이드 분기 우회 (기존 LOSS_STOP 동작 회귀). `stopApproachAlert` 가 `shadow.entryATR14` 전달. **H2 — assertSafeOrder cron 외 진입점 우회 차단** (ADR-0081 신규): 기존 `assertSafeOrder` 가 3 가드(POSITION_EXPLOSION/STOPLOSS_LOGIC_BROKEN/ORDER_LOOP_SUSPECT) 만 검증 — cron 외 진입점(텔레그램 `/buy`, `POST /auto-trade/tranches/run`, `runAutoSignalScan({forceBuyCodes})`) 에서 `getEmergencyStop()` / `getDailyLossPct()` 우회 가능. 운영자 `/stop` 후 *기존 PENDING 트랜치* HTTP 강제 실행 시 KIS 실주문 발송 잠재 위험. 수정: `assertSafeOrder` 진입부에 `EMERGENCY_STOP_ACTIVE` + `DAILY_LOSS_LIMIT_HIT` 가드 2개 *최우선* 위치 추가 (기존 3 가드 보다 먼저). `DAILY_LOSS_LIMIT` env (default 5%) 는 `emergency.ts:87` + `overrideExecutor.ts:64` 와 동일 SSOT 재사용. NaN/Infinity 안전 가드. `PreOrderGuardReason` union +2 (EMERGENCY_STOP_ACTIVE / DAILY_LOSS_LIMIT_HIT). **회귀 테스트 20 케이스 신규** — `stopApproachAlert.test.ts +12` (ATR=1500/300/2000 글라이드 흡수 + 경계값 + entryATR14 미전달 회귀 + 0/NaN/Infinity/음수 안전 + ENV 롤백 + PROFIT_LOCK_IN/정확 BEP 우선순위 보존) + `preOrderGuard.test.ts +8` (정상 통과 + EMERGENCY_STOP_ACTIVE incident + DAILY_LOSS_LIMIT_HIT incident + 5.0 경계값 + 4.99 미달 + env override 10% / 7% loss 통과 + 동시 위반 시 EMERGENCY_STOP 우선 + NaN 안전 + 새 가드 우선순위). **검증** — vitest server/trading 93 files (37+20+무회귀) pass + lint(client + server tsc) + validate:all 8종 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — kisClient/orchestrator/autoTradeEngine 본체 무수정). LIVE 매매 본체 — H1 은 라벨 분류 SSOT 만 변경 (메시지 본체 무수정), H2 는 가드 2개 진입부 추가 (3 가지 부수효과 — incident/setEmergencyStop/cancelAll 모두 idempotent 보장). ENV 롤백 (`BEP_GLIDE_DISABLED` / `DAILY_LOSS_LIMIT`) 자동 공유. **운영 효과** — (H1) +5~+10% 수익 영역에서 손절선 접근 알림이 "[BEP 청산선 임박]" 라벨로 표시 → 운영자 *수익 보호 의도* 정확 인지, 사용자 패닉 매도 유도 결함 영구 차단. (H2) cron 외 모든 진입점이 assertSafeOrder 단일 SSOT 통과 강제 → 운영자 `/stop` 후 *어떤 경로* 로도 KIS 주문 발송 불가, incident 영속으로 우회 시도 종목·경로 사후 추적 가능. **페르소나 정합** — H1: 자료 7번 단일 신호 vs Confluence (운영자 패닉 차단) / H2: 자료 8번 손절은 운영비 + 자료 22번 단일 책임 + 원칙 1번 필터링 (모든 진입점 단일 final 게이트). | `server/trading/exitEngine/rules/stopApproachAlert.ts` (classifyStopSource 시그니처 entryATR14 옵셔널 + 글라이드 분기 + BEP_GLIDE_ATR_RATIO 상수), `server/trading/exitEngine/rules/__tests__/stopApproachAlert.test.ts` (+12 케이스), `server/trading/preOrderGuard.ts` (PreOrderGuardReason +2 union + getDailyLossLimitPct 헬퍼 + assertSafeOrder 진입부 가드 2개), `server/trading/preOrderGuard.test.ts` (+8 케이스), `docs/adr/0079-atr-buffered-bep-glide.md` (PR-Z7 H1 follow-up 섹션 추가), `docs/adr/0081-preorder-guard-final-gate-ssot.md` (신규), `ARCHITECTURE.md` (+2 boundary rule) | full-bug-audit High 2건 즉시 수정 (사용자 옵션 1 선택 — "둘 다 즉시 수정"). 두 패치 모두 ENV 롤백 + 회귀 테스트 + LIVE 본체 무수정 3중 격리. PR-Z6 (BEP Glide) + PR-Z7 (BEP 라벨 흡수 + final 게이트) 결합으로 사용자 자본 보호 + 운영자 인지 정합성 동시 강화. |
| 2026-04-28 | PR-S1 — 루프 게이트 자본 가중화 (ADR-0080 follow-up, 같은 브랜치): 사용자 audit "ADR-0080 이 상위 게이트 2 곳만 패치하고 루프 내부 단순 카운트 2 곳이 잔존 — 행렬식 0, 한 곳 막히면 전체 차단" 즉시 반영. **시뮬레이션 검증** — 만재 4개 + 30% 잔존 4개 + effectiveMaxPositions=8 시나리오: 상위 게이트 consumed=5.20/8 통과 ✓ → 루프 게이트 currentActive=8/8 break ✗ ("통과 → 즉시 break" 역설). ① **`perSymbolEvaluation.ts:283-289` wiring 교체** — `shadows.filter(...).length` 단순 카운트 → `slotResult.consumed + reservedSlots` 자본 가중 비교. `currentActive` 변수는 `slotResult.rawCount` 보존 (PR-S2 까지 sizing 분모용). `computeSlotConsumption` import 추가. ② **진단 로그 보강** — `[AutoTrade] 최대 포지션 도달 (활성 ${consumed.toFixed(2)} + 예약 N = ${totalCommitted.toFixed(2)}/${effectiveMaxPositions}, raw=${rawCount}) — 나머지 종목 스킵` 형식으로 자본 가중 + 단순 카운트 동시 노출. ③ **`perSymbolLoopGateWiring.test.ts` 신규** (5 케이스, ~70 LoC) — drift 차단 regex 가드: computeSlotConsumption import 강제 / totalCommitted 자본 가중 패턴 / console.log slotResult.consumed.toFixed + raw=rawCount 표기 / currentActive=rawCount 보존 (PR-S2 분리) / 단순 카운트 패턴 회귀 차단 (loopGateRegion 영역 내 `shadows.filter` 직접 사용 금지). ④ **slotAccounting.test.ts +1** — 사용자 보고 정확 시나리오 (만재 4 × 1.0 + 잔존 4 × 0.3 = 5.20 / rawCount=8 / isFull=false / available=2.80) 회귀 가드. ⑤ **PR-S2 명시적 분리** — `perSymbolEvaluation.ts:900-903 remainingSlots` (sizing 분할 비율 분모) 는 자본 가중 전환 시 *신규 진입 포지션 크기* 가 달라지므로 LIVE 영향 평가 신중 필요. shadow mode 1주 검증 후 별도 PR. ADR-0080 §"후속 PR" 섹션에 PR-S1/S2 명문화. **검증** — vitest server/trading 93 files 1006/1006 pass (신규 6 + 무회귀) + lint(client + server tsc) + validate:all 8종 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 본체 1 if 분기 1 변수 정의 변경만 — ENV 롤백 (`SLOT_CAPITAL_WEIGHTED_DISABLED`) 자동 공유. **운영 효과** — "30% 잔존 다수 + 슬롯 만재 매수 차단" 사용자 보고 시나리오 영구 차단. 페르소나 자료 22번 SRP — "슬롯 점유 회계" 단일 책임이 *세 호출 site 동일 SSOT* 사용으로 정합. | `server/trading/signalScanner/perSymbolEvaluation.ts` (computeSlotConsumption import + 루프 게이트 wiring + 진단 로그 보강), `server/trading/signalScanner/__tests__/perSymbolLoopGateWiring.test.ts` (신규 5 케이스), `server/trading/slotAccounting.test.ts` (+1 사용자 시나리오 회귀 가드), `docs/adr/0080-capital-weighted-slot-accounting.md` (+PR-S1 follow-up + PR-S2 분리 명문화), `ARCHITECTURE.md` (boundary rule 갱신: 호출 site 2→3 + sizing 분모 PR-S2 분리 명시) | 사용자 audit 정확 — 행렬식 0 결함 즉시 차단. PR-S2 (sizing 분모) 는 분할 비율 영향 격리를 위해 별도 PR 분리. |
| 2026-04-28 | BEP Glide + Capital-Weighted Slot Accounting (ADR-0079 + ADR-0080, 사용자 12 아이디어 P0 두 트랙): **Track 1 ATR-Buffered BEP Glide** — `src/services/quant/dynamicStopEngine.ts` BEP_TRIGGER (+5%) 분기에서 `Math.round(entryPrice)` 단순 점프를 `bepGlideStopPrice(entryPrice, atr14)` 글라이드로 교체. 공식 `entryPrice − 0.5 × atr14` (ATR 1500 종목 → 9250 / ATR 300 → 9850). atr14 ≤ 0/NaN/Infinity/undefined → entryPrice fallback (기존 동작 100% 보존). `BEP_GLIDE_DISABLED=true` env 긴급 롤백. Lock-in 분기(+10%) 변경 0. actionMessage 도 ATR>0 시 글라이드 마진 표기. **Track 2 Capital-Weighted Slot Accounting** — `server/trading/slotAccounting.ts` 신규 (≤90 LoC, @responsibility SRP) + `computeSlotConsumption(shadows, effectiveMaxPositions)` 순수 함수 SSOT — 슬롯 점유 = `getRemainingQty(shadow) / shadow.originalQuantity` (0~1 clamp). 70% 익절 잔존 30% 포지션 5개 = 1.5 슬롯 점유 (기존 5 카운트 대신). originalQuantity ≤ 0 → 1.0 보수적 fallback. INTRADAY/PRE_BREAKOUT 회계 제외 (기존 정책). `SLOT_CAPITAL_WEIGHTED_DISABLED=true` env 롤백 시 단순 카운트 동치. 두 호출 site 동시 wiring — `signalScanner.ts:430` (LIVE 활성) + `signalScanner/preflight.ts:365` (PR-40 Phase B 후속) drift 차단. POSITION_FULL 분모(`effectiveMaxPositions`) 변경 0. 로그 형식 `${consumed.toFixed(2)}/${effectiveMaxPositions}` (raw=N 동반). **회귀 테스트 38 케이스 신규** — `quantEngine.dynamicStop.test.ts +19` (bepGlideStopPrice 단위 13: ATR 저/중/고 / 0 fallback / undefined / NaN / Infinity / 음수 / 1원 floor / 반올림 / ENV 롤백 3 / 기본값 + evaluateDynamicStop 통합 6: ATR=1500 / 300 / 0 회귀 / ENV 롤백 / Lock-in 무영향 / actionMessage 분기 3) + 기존 BEP 분기 단언 2 갱신 (10000 → 9750 ADR-0079 정합) + `slotAccounting.test.ts` 신규 19 (빈 / 만재 1 / 30% 잔존 1 / 30% 잔존 5개=1.5 핵심 시나리오 / 만재 8개 isFull / 경계값 / originalQuantity 부재 / 0 / INTRADAY 제외 / PRE_BREAKOUT 제외 / closed 제외 / PENDING 포함 / 혼재 3.25 슬롯 / detail 분해 / ENV 롤백 2 / NaN 안전 / available floor 0 / 자본 가중 동작). **검증** — vitest 신규 38 + 무회귀 + lint(client + server tsc) + validate:all 8종 (DeployWindow + PRES + ACMA + SymbolBoundary + ChannelBoundary + SensitiveAlerts + MarketOverviewBoundary + Gemini) + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — kisClient/orchestrator/autoTradeEngine 본체 무수정). LIVE 매매 본체 0줄 변경 — 클라이언트 BEP 글라이드 + 서버 슬롯 회계 단일 SSOT 추출, ENV 롤백 2개로 회귀 위험 격리. **운영 효과** — (Track 1) BEP 점프 직후 noise 손절 차단 (변동성 가중 마진 흡수) → 평균 보유 +1~2일 연장 + 진짜 추세 반전에만 청산. (Track 2) 잔존 30% 포지션이 차지한 슬롯 자동 회수 → 신규 *고품질* 신호 진입 빈도 +20~30% 예상. **페르소나 정합** — Track 1: 자료 7번 단일 신호 vs Confluence + 자료 23번 추세 추종 / Track 2: 자료 22번 단일 책임 + 자료 19번 기회비용 상실 + 원칙 1번 필터링. **후속 PR (scope 외)** — BEP 문제군 #2 Two-Bar Confirmation Gate / 슬롯 회계 #2~10 (Reserved-Slot Floor / Tail-Position 분리 / Slot-Pressure-Aware TP / Tail-vs-NewSignal Auction / Bayesian Reservation). | `docs/adr/0079-atr-buffered-bep-glide.md` (신규), `docs/adr/0080-capital-weighted-slot-accounting.md` (신규), `src/services/quant/dynamicStopEngine.ts` (+bepGlideStopPrice + BEP_GLIDE_ATR_RATIO + ENV 롤백 + actionMessage 분기), `src/services/quantEngine.dynamicStop.test.ts` (+19 신규 + 2 단언 갱신), `server/trading/slotAccounting.ts` (신규 ~90 LoC), `server/trading/slotAccounting.test.ts` (신규 19 케이스), `server/trading/signalScanner.ts` (+computeSlotConsumption import + activeSwingCount → slotResult), `server/trading/signalScanner/preflight.ts` (+computeSlotConsumption import + activeSwingCount → slotResult), `ARCHITECTURE.md` (+2 boundary rule) | 사용자 12 아이디어 (BEP 문제군 #1 + 슬롯 회계 #1) P0 두 트랙 단일 PR 통합. 두 모듈 모두 ENV 롤백 + 회귀 테스트 + LIVE 본체 무수정 3중 격리. 후속 PR (#2~10) 가 본 SSOT 위에 자연 확장 가능. |
| 2026-04-27 | Gap 3 — Enemy Checklist 자동 차단 정책 SSOT (ADR-0078, ADR-0077 후속): 사용자 audit Gap 3 (PARTIAL) 처리. `enemyCheckClient.fetchEnemyCheckData` 가 KIS 두 개 TR 로 신용잔고율(creditRate) + 개인 비중(individualDominance) 두 데이터 수집 작동 중이지만 자동 차단 미구현 — `enemyCheckClient.ts:5-7` 주석 "모의매매 데이터가 충분히 쌓인 후 유효성 검증 시 도입" 보류 상태. ADR-0077 의 `markBlocked('ENEMY')` 호출자 0건 갭 동시 해소. ① **`server/trading/enemyAutoBlock.ts` 신규** (@responsibility SRP, ~120 LoC) — `evaluateEnemyAutoBlock(enemy, thresholds?)` SSOT: creditRate ≥ 12% **또는** individualDominance ≥ 88% (OR 결합) → `shouldBlock=true`. 표시 ⚠️⚠️ 임계(8%/80%) 보다 4~8% 더 보수적인 BLOCK 임계 (false-positive 차단 마진). `EnemyAutoBlockDecision` schema (shouldBlock + reason ` / ` 결합 + warnings + thresholds + skipReason). 데이터 부재 (`source === 'UNAVAILABLE'` 또는 null) → `shouldBlock=false` + `skipReason='NO_DATA'` (KIS 일시 장애가 매매 차단 안 함). NaN/Infinity 안전 fallback. ② **ENV 오버라이드 5종** — `ENEMY_AUTO_BLOCK_DISABLED=true` 비상 회로 차단 (전체 평가 skip + `skipReason='DISABLED'`) + `ENEMY_CREDIT_RATE_BLOCK` (default 12.0) / `ENEMY_CREDIT_RATE_WARN` (8.0) / `ENEMY_INDIVIDUAL_BLOCK` (88.0) / `ENEMY_INDIVIDUAL_WARN` (80.0) 임계 ENV 조정 (음수/NaN/0 입력 시 default fallback). ③ **buyPipeline.ts 단일 호출자 wiring** — `fetchEnemyCheckData` 결과 직후 (`Promise.all` 결과 처리 직후) `evaluateEnemyAutoBlock` 호출 → `shouldBlock=true` 시 (a) `console.warn` 진단 로그, (b) `markBlocked({ id: signalId, gate: 'ENEMY', reason })` (signalId 있을 때만, try/catch 보호 — ADR-0077 wiring 첫 ENEMY 호출자 정착), (c) `Promise.resolve('SKIP')` 반환 (텔레그램 발송 자체 차단), (d) `execute` 콜백이 `trade.status='REJECTED' + onRejected(SKIP)`. 기존 `requestBuyApproval` / `placeKisOrder` / SHADOW 분기 본체 0줄 변경. ④ **회귀 테스트 37 케이스 신규** — `enemyAutoBlock.test.ts` 29 (상수 SSOT 정합 2 + 데이터 부재 fallback 3 + creditRate 임계 분기 5 + individualDominance 임계 분기 3 + OR 결합 4 + NaN/Infinity 안전 3 + ENV override 6 + source 분기 3) + `buyPipeline.enemyBlock.test.ts` 8 (정상 통과 / creditRate 14% BLOCK / individual 92% BLOCK / UNAVAILABLE 안전 / null 안전 / signalId 미전달 시 markBlocked skip / markBlocked throw 차단 안 함 / DISABLED 우회). ⑤ **검증** — vitest **server/trading + server/persistence + server/telegram 132 files 1370/1370 pass** (신규 37 + 무회귀) + lint(client + server tsc) + validate:all 8종 (DeployWindow + PRES + ACMA + SymbolBoundary + ChannelBoundary + SensitiveAlerts + MarketOverviewBoundary + Gemini) + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4 — kisClient/orchestrator/autoTradeEngine 본체 무수정). LIVE 매매 본체 0줄 변경 — buyPipeline 에 분기 1개 (조건부 early return) 만 추가. ⑥ **운영 효과** — 매수 직전 신용잔고율 ≥12% 또는 개인 비중 ≥88% 종목 자동 차단 + 텔레그램 발송 차단 (false positive 차단을 위해 표시 ⚠️⚠️ 임계보다 4~8% 마진). `/signal_status` 에 `❌ 005930 삼성전자 — BLOCKED — ENEMY (신용잔고율 14.2% ≥ 12.0%)` 형식 표시. 비상 우회는 `ENEMY_AUTO_BLOCK_DISABLED=true` ENV 1줄로 즉시. 운영 1~2주 누적 후 BLOCKED 빈도 + 사후 수익률 측정으로 임계 ENV 조정. ⑦ **본 PR scope 밖 (후속)** — 사용자 audit 7 카테고리 데이터 소스 확장 (DART 보호예수 / 대주주 매도 / 공매도 / 목표가 / 실적, 별도 ADR), WARN 단계 사이즈 축소 (Kelly multiplier ×0.7 SizingDecider 분기), enemyAutoBlock 결과 영속 (TradeSignalRecord transitions[].reason 에 warnings[] 병기). | `docs/adr/0078-enemy-auto-block.md` (신규), `server/trading/enemyAutoBlock.ts` (신규 ~120 LoC), `server/trading/enemyAutoBlock.test.ts` (신규 29 케이스), `server/trading/buyPipeline.ts` (+evaluateEnemyAutoBlock import + markBlocked import + Promise.all 직후 분기 1개), `server/trading/buyPipeline.enemyBlock.test.ts` (신규 8 케이스), `ARCHITECTURE.md` (+1 boundary +1 boundary rule) | 사용자 audit Gap 3 — 수집 가능한 2개 데이터 (creditRate / individualDominance) 한정으로 자동 차단 정책 정착. 사용자 제안 7 카테고리는 데이터 소스 부재로 후속 PR 분리. ADR-0077 의 ENEMY BlockGate 첫 호출자 wiring 정착 — `/signal_status` 진단 명령에서 즉시 가시화. |
| 2026-04-28 | PR-Z — Yahoo `range` 전역 ≤1y 정책 SSOT (ADR-0082, 사용자 명시 P0 운영 보고 즉시 반영): 사용자 보고 "yahoo 참조는 sanity 위반이 자주 발생하므로 2년치로 선정하지 말것 (프로그램 전역)" 처리. 누적된 Yahoo stale base price 사고 (ADR-0028 305.28% 액면분할 / ADR-0004 -93.69% 상장폐지 / PR-29 EgressGuard / PR-24 24h 블랙리스트 / PR-22 503 폭주) 의 *근본 입력 폭* 을 정책으로 영구 차단. ① **`server/utils/yahooRangePolicy.ts` 신규** — `YAHOO_ALLOWED_RANGES` 6값 SSOT (1d/5d/1mo/3mo/6mo/1y) + `YAHOO_MAX_RANGE='1y'` + `YAHOO_DEFAULT_RANGE='1y'` + `capYahooRange(input)` cap 함수 + `isAllowedYahooRange(input)` type guard + `isYahooRangeCapDisabled()` ENV 검사. `'2y'/'5y'/'10y'/'max'/'ytd'/알 수 없는 값` → `'1y'` 자동 cap. `undefined/null/빈값` → `'1y'` default. `YAHOO_RANGE_CAP_DISABLED=true` 시 입력 그대로 통과 (긴급 운영 우회). ② **`yahooQuoteAdapter.ts:101` `range=2y` → `range=1y`** — 1y = 252영업일은 MA60 + 5일 전 MA60 (65일) / ATR14 / BB20 / 주봉 RSI(9, 45영업일) / 60일 최고가 / 5일·20일 수익률 모두 충분. 13개월 월봉은 12개월로 graceful (KIS MTAS 보강 별도). 산출 본체 0줄 변경. ③ **`marketDataRouter.ts:124,256` rangeStr cap wiring** — 미들웨어 게이트 + `/historical-data` 진입부 모두 `capYahooRange()` 통과. 클라이언트 UI(`CandleChart.tsx`) '2y' 옵션은 사용자 능동 선택 보존하되 *서버 cap 으로 outbound URL 은 1y* (브라우저 모름). ④ **`scripts/check_yahoo_range.js` 신규** + **`validate:yahooRange` 등록** + **`validate:all` + `precommit` 본체 통합** — 코드베이스 전수 정적 검사 (URL 쿼리 5분기 + 문자열 리터럴 3분기) + 라인 주석 + 블록 주석 strip 후 검사 (false positive 차단). 화이트리스트 4 파일 (정책 SSOT 자기 자신 / UI 옵션 / 검증 스크립트 / 회귀 테스트). ⑤ **회귀 테스트 30 케이스 신규** — `yahooRangePolicy.test.ts` 18 (상수 SSOT 정합 4 + isAllowedYahooRange 3 + capYahooRange 7: 허용 6값 통과 + 2y/5y/10y/max/ytd cap + null/undefined/빈값 default + 알 수 없는 입력 cap + ENV 우회 + ENV 미설정 정책 적용 + isYahooRangeCapDisabled 3) + `check_yahoo_range.test.js` 12 (baseline 통과 + URL 쿼리 5분기 차단 + 문자열 리터럴 2y/5y 차단 + 한 줄 주석 무시 + 블록 주석 무시 + 다중 위반 카운트 + 허용 range 통과 + --changed 모드). ⑥ **검증** — vitest 신규 30 + 무회귀 (server/screener/adapters + server/routes/marketDataRouter 84/84 pass) + lint(client + server tsc) + validate:all 9종 (Gemini + ACMA + SDS + PRES + Responsibility + SymbolBoundary + ChannelBoundary + SensitiveAlerts + MarketOverviewBoundary + YahooRange) + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (외부 fetch 정책만 변경, kisClient/orchestrator/autoTradeEngine 본체 무수정). LIVE 매매 본체 산출 함수 0줄 변경 — URL `range=2y` → `range=1y` 한 줄 + cap wiring 2 곳 + 정책 SSOT 신규. ⑦ **운영 효과** — (1) yahooQuoteAdapter 매 신호 스캔당 종목별 OHLCV fetch 가 504봉 → 252봉 으로 50% 축소 → Railway egress 부담 감소 + Yahoo IP 차단 누적 위험 감소 + 액면분할/배당 stale base price 확률 ↓ . (2) 클라이언트 UI '2y' 차트 옵션은 보존 (UX 무영향) — 서버 cap 으로 데이터는 1y. (3) 정적 검증으로 향후 PR 에서 `'2y'/'5y'/'10y'/'max'` 직접 사용이 빌드 차단 — 정책 영구 강제. (4) ENV `YAHOO_RANGE_CAP_DISABLED=true` 1줄로 긴급 운영 우회 가능. ⑧ **잔여** — `CandleChart.tsx` UI '2y' 옵션 *제거* 는 사용자 UX 변경이라 본 PR 미포함 (서버 cap 으로 동일 효과). `period1/period2` 직접 지정 (backtestEngine 90일 / lateWinEvaluator 95일) 은 영업일 precision 필요한 학습 경로라 정책 제외. | `docs/adr/0082-yahoo-range-restriction-policy.md` (신규), `server/utils/yahooRangePolicy.ts` (신규), `server/utils/yahooRangePolicy.test.ts` (신규 18 케이스), `server/screener/adapters/yahooQuoteAdapter.ts` (range=2y→1y + ADR 주석), `server/routes/marketDataRouter.ts` (capYahooRange wiring 2곳 + import), `scripts/check_yahoo_range.js` (신규), `scripts/check_yahoo_range.test.js` (신규 12 케이스), `package.json` (+validate:yahooRange + validate:all/precommit 통합), `ARCHITECTURE.md` (+yahooRangePolicy boundary + boundary rule) | 사용자 운영 보고 즉시 반영. PR-A (Walk-Forward Framework) / PR-B (Condition Lifecycle SSOT) / PR-C (Slot/BEP 후속) 의 안전 기반. PR-A 는 영속 데이터(SHADOW + attribution + recommendation)만 시간 분할 검증으로 보수적 재설계 — Yahoo OHLCV fetch 0건. |
| 2026-04-28 | PR-A — Walk-Forward Framework 확장 (ADR-0083): Rolling Window + Regime 분리 + Alpha Decay (사용자 P10 진단 — 보수적 재설계, Yahoo fetch 0건). 기존 `walkForwardValidator.ts` 173줄(freeze 정책) 본체 0줄 수정 — 신규 framework 는 *진단 전용* 으로 분리. ① **`server/learning/walkForwardFramework.ts` 신규** — `runWalkForwardFramework({now?})` orchestrator + 통계 SSOT 5종 (`computeMaxDrawdown`/`computeSharpe`/`computeWindowMetrics`/`computeRegimeBreakdown`/`computeDecayTrend`) + `generateRollingWindows(now, isDays, oosDays, stepDays, maxWindows)` rolling 산출 + `getResolvedConfig()` ENV 오버라이드 + `isFrameworkDisabled()` 회로. Default IS=60일 / OOS=30일 / step=7일 / maxWindows=24 / minSample=5 / regimeMinSample=3 / decayThreshold=5%p / overfitFlag=15%p. ② **`server/persistence/walkForwardResultsRepo.ts` 신규** — `data/walk-forward-results.json` 영속 SSOT. `WindowMetrics` (sampleSize/winRate/avgReturn/totalReturn/sharpe/maxDrawdown) + `RegimeMetrics` (is/oos) + `WalkForwardWindow` (windowId/IS·OOS 범위/metrics/degradation/regimeMetrics?) + `WalkForwardSummary` (totalWindows/avgDegradation/medianDegradation/overfitFlagged/decayTrend) + `WALK_FORWARD_SCHEMA_VERSION=1` + atomic write (tmp→rename) + 손상 JSON 빈 결과 fallback + FIFO trim + idempotent windowId 덮어쓰기. ③ **`paths.ts` `WALK_FORWARD_RESULTS_FILE` 추가** (`WALK_FORWARD_STATE_FILE` freeze 와 별개). ④ **`learningRouter.ts` 확장** — `GET /api/learning/walk-forward` (영속 read-only + disabled 플래그) + `POST /api/learning/walk-forward/run` (수동 트리거). ⑤ **텔레그램 명령 `/walk_forward [N=10]`** (alias `/wf`, category=LRN, riskLevel=0, ADMIN) — `formatWalkForwardMessage(results, N, disabled)` SSOT 빌더 분리 + decayTrend 4분기 이모지 (🟢IMPROVING/🟡STABLE/🔴DECAYING/⚪INSUFFICIENT) + overfit > 15%p 마커 + Regime 분리 표시. system barrel `+walkForward.cmd.js`. ⑥ **회귀 테스트 55 케이스 신규** — `walkForwardResultsRepo.test.ts` 8 (영속 round-trip / FIFO trim / idempotent / 손상 JSON / clear) + `walkForwardFramework.test.ts` 29 (computeMaxDrawdown 4 / computeSharpe 3 / computeWindowMetrics 3 / generateRollingWindows 3 / computeRegimeBreakdown 2 / computeDecayTrend 5 / runWalkForwardFramework 4 + ENV 1 / fallback 1 / computeSummary 3) + `walkForwardRouter.test.ts` 7 (GET 정상/빈/disabled/throw 500 + POST 정상/skipped/throw 500) + `walkForward.test.ts` 11 (formatWalkForwardMessage 6 + cmd execute 5: 정상/alias/throw graceful/N propagate/메타데이터). ⑦ **검증** — vitest 신규 55/55 + 영향 영역 무회귀 (server/persistence + server/learning + server/routes + server/telegram **93 files 942/942 pass**) + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범. LIVE 매매 본체 0줄 변경 — 기존 freeze 정책 / 학습 cron / 추천 트래커 모두 무수정. ⑧ **운영 효과** — (1) 단일 윈도우 → Rolling 24개 슬라이딩 (약 6개월) 으로 단일 시점 운 의존 차단. (2) Regime 별 IS/OOS 분리 → 강세장 / 약세장 / R6 방어 등 시장 국면별 성과 차이 즉시 가시화. (3) Alpha decay 추적 (초기 1/3 vs 최근 1/3 OOS WR) → 장기 성능 저하 자동 감지. (4) ENV `WALK_FORWARD_FRAMEWORK_DISABLED=true` 1줄 우회 + 기존 freeze 정책 무영향. ⑨ **ADR-0082 정합** — Yahoo OHLCV fetch 0건 (영속 `recommendationTracker` records 만 사용). ⑩ **잔여 (후속 PR)** — 자동 weight 보정 (decay='DECAYING' 시 가중치 보수화), Regime 별 별도 freeze 정책, Yahoo OHLCV 추가 검증 (사용자 결정 필요). | `docs/adr/0083-walk-forward-framework-extension.md` (신규), `server/learning/walkForwardFramework.ts` (신규), `server/learning/walkForwardFramework.test.ts` (신규 29 케이스), `server/persistence/walkForwardResultsRepo.ts` (신규), `server/persistence/walkForwardResultsRepo.test.ts` (신규 8 케이스), `server/persistence/paths.ts` (+WALK_FORWARD_RESULTS_FILE), `server/routes/learningRouter.ts` (+GET/POST walk-forward + 2 import), `server/routes/walkForwardRouter.test.ts` (신규 7 케이스), `server/telegram/commands/system/walkForward.cmd.ts` (신규), `server/telegram/commands/system/walkForward.test.ts` (신규 11 케이스), `server/telegram/commands/system/index.ts` (+barrel), `ARCHITECTURE.md` (+2 boundary +1 boundary rule) | 사용자 P10 진단 (Walk-Forward 약함) 보수적 재설계. 기존 walkForwardValidator.ts 173줄 발견 후 *확장* 으로 재정의 — freeze 정책 무수정. PR-Z (ADR-0082) Yahoo fetch 0건 정책과 정합. PR-B (Condition Lifecycle SSOT) / PR-C (Slot/BEP 후속) 의 의사결정 데이터 입력. |
| 2026-04-28 | PR-B — Condition Lifecycle Status Policy (ADR-0084, 27조건 자연선택): 사용자 P10 진단 후속 — `attributionRepo` (PR-19 ADR-0006) 영속 위 *read-only 분류 레이어* 신설. PR-Y2 `reflectionImpactPolicy.ts` 패턴을 27조건에 적용. **별도 영속 SSOT 신설 0건** (attributionRepo `closedAt` 시간 분포 활용). ① **`server/learning/conditionLifecyclePolicy.ts` 신규** — `ConditionLifecycleStatus` 4분기 (`normal`/`grace`/`silent`/`deprecated`) + 결정 트리 7 분기 우선순위 SSOT (ENV DISABLED → firstSeenAt 부재/30일 grace → activatedCount<30 → activationRate<1% deprecated → <5% silent → 그 외 normal). 헬퍼 6종 — `activatedCount`/`activationRate`/`firstSeenAt`/`lastSeenAt`/`ageDays` + 27 조건 카탈로그 (`KNOWN_CONDITION_IDS` 1~27) + `getConditionLifecycleStatus(records, conditionId, opts)` + `getAllConditionLifecycleStatuses(records, opts)` + `summarizeConditionLifecycle(reports)` (4분기 카운트). `byRegime` 분리 (entryRegime 별 표본 ≥3 만 노출). 임계 ENV 오버라이드 + 정책 ENV 우회. ② **`learningRouter` `GET /api/learning/condition-lifecycle?days=180`** — windowDays 1~365 + disabled 플래그 + 27 조건 status 일괄 + summary. ③ **텔레그램 명령 `/condition_lifecycle [N=27]`** (alias `/cl`, category=LRN, riskLevel=0, ADMIN) — `formatConditionLifecycleMessage` SSOT 빌더 (status 4분기 이모지 ✅🟡🟠❌ + activationRate 내림차순 Top N + Status 요약 + 은퇴 후보 별도 섹션). system barrel `+conditionLifecycle.cmd.js`. ④ **회귀 테스트 46 케이스 신규** — `conditionLifecyclePolicy.test.ts` 29 (상수 SSOT 정합 2 + 헬퍼 12: activatedCount 3 / activationRate 3 / firstSeenAt 2 / lastSeenAt 1 / ageDays 3 + 결정 트리 9: ENV / 빈 / grace 30일 / 표본 부족 / deprecated 1% / silent 3% / normal 5% / windowDays 필터 / byRegime / threshold override + 일괄 2 + summarize 2) + `conditionLifecycle.test.ts` 11 (formatConditionLifecycleMessage 7: disabled / 빈 / 정상 / 은퇴 후보 / 전부 normal / N 절삭 / 정렬 + cmd execute 4: 정상 / alias / throw graceful / 메타데이터) + `conditionLifecycleRouter.test.ts` 6 (정상 / 빈 records 27 grace / windowDays propagate / 잘못된 days 180 fallback / disabled 플래그 / throw 500). ⑤ **검증** — vitest 신규 46 + 영향 영역 무회귀 (server/learning + server/persistence + server/routes + server/telegram **96 files 988/988 pass**) + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (외부 fetch 0건 — attribution 영속 read-only). LIVE 매매 본체 0줄 변경 — `attributionRepo` / `attributionAnalyzer.recommendation` / signalScanner / entryEngine 모두 무수정. ⑥ **운영 효과** — (1) 운영자가 텔레그램 `/condition_lifecycle` 한 번에 27조건 status 즉시 확인 (`silent`/`deprecated` 후보 별도 섹션). (2) 6개월 누적 후 객관적 활성률 데이터로 NOISE_FACTOR 조건 식별 가능 — *데이터 부족 + 사용자 직감* 의 갭 차단. (3) `byRegime` 분리로 R2_BULL 강세장에선 normal 이지만 R5_CAUTION 약세에선 silent 인 조건을 즉시 가시화. (4) ENV `CONDITION_LIFECYCLE_DISABLED=true` 1줄 우회 + 본 PR 측정 전용이라 LIVE 매매 영향 0. ⑦ **잔여 (후속 PR)** — 실제 silent/deprecated 가드 wiring (signalScanner / entryRevalidationStep 가 status 읽어 score 가중치 보수화) — 데이터 누적 6개월 후, `f2wDriftDetector` 와 결합한 자동 deprecation 트리거. | `docs/adr/0084-condition-lifecycle-status-policy.md` (신규), `server/learning/conditionLifecyclePolicy.ts` (신규), `server/learning/conditionLifecyclePolicy.test.ts` (신규 29 케이스), `server/routes/learningRouter.ts` (+/condition-lifecycle endpoint + 4 import), `server/routes/conditionLifecycleRouter.test.ts` (신규 6 케이스), `server/telegram/commands/system/conditionLifecycle.cmd.ts` (신규), `server/telegram/commands/system/conditionLifecycle.test.ts` (신규 11 케이스), `server/telegram/commands/system/index.ts` (+barrel), `ARCHITECTURE.md` (+1 boundary +1 boundary rule) | 사용자 P10 진단 (27조건 자동 은퇴 정책) 처리. PR-Y2 reflectionImpactPolicy 패턴 차용 — 13 모듈 vs 27 조건 동일 자연선택 사상. *측정 전용* 단계 — 실제 silent/deprecated 가드 wiring 은 데이터 누적 후 후속 PR. PR-A (Walk-Forward Framework) + PR-B 결합으로 시간 분할 검증 + 조건 lifecycle 양 축 진단 인프라 마련. |
| 2026-04-28 | PR-C — Two-Bar Confirmation + 슬롯 sizing 인프라 (ADR-0085, ADR-0079/ADR-0080 후속): LIVE 매매 안전 격리를 위해 *정책 함수 SSOT + 영속 schema 확장만* — 실제 wiring 은 운영 데이터 누적 후 후속 PR. ① **`server/trading/twoBarConfirmation.ts` 신규 (트랙 1)** — BEP 글라이드 단봉 노이즈 차단. `evaluateTwoBarConfirmation(input)` 결정 트리 6 분기 (ENV DISABLED → PASS / isBepProtection=false → PASS / NaN 입력 → PASS / currentPrice > stopLoss 회복 → RESET / 1차 터치 (bepGlideTouchAt 부재) → WAIT + touchAt 영속 / diffDaysKst >= GRACE_DAYS(1) → CONFIRM_EXIT / 그 외 1차 터치 당일 → WAIT 영속 유지). `diffDaysKst(currentDate, touchDate)` 헬퍼 — 잘못된 ISO 0 fallback. `TWO_BAR_CONSTANTS.GRACE_DAYS=1` 영업일. ENV `BEP_TWO_BAR_CONFIRMATION_DISABLED=true` 우회. ② **`server/trading/slotSizing.ts` 신규 (트랙 2 PR-S2 인프라)** — `evaluateSlotSizing(input)` Default 단순 카운트 (현재 동작 byte-equivalent) / `SLOT_CAPITAL_WEIGHTED_SIZING_ENABLED=true` 활성 시 PR-S1 자본 가중 적용. `effectiveDenominator = max(1, effectiveMaxPositions - rawCount/consumed - reservedSlots)` floor 1 보장. `isCapitalWeightedSizingEnabled()` ENV 검사. **default 비활성** — 운영자 명시 활성화 시에만 LIVE 매매 신규 진입 포지션 크기 영향. ③ **`ServerShadowTrade.bepGlideTouchAt?` 옵셔널 schema 확장** — 1차 터치 KST 일자 영속용. 기존 영속 호환 유지. 호출자 0건 — 후속 wiring PR 에서 `exitEngine/rules/hardStopLoss.ts` BEP_PROTECTION 분기가 사용. ④ **회귀 테스트 26 케이스 신규** — `twoBarConfirmation.test.ts` 16 (상수 + ENV 4 + diffDaysKst 4 + 결정 트리 8: ENV PASS / isBepProtection=false PASS / NaN PASS / 회복 RESET / 1차 터치 WAIT / 당일 재방문 WAIT / 다음 영업일 CONFIRM_EXIT / 1주 후 CONFIRM_EXIT / 정확 stopLoss = WAIT / stopLoss NaN PASS) + `slotSizing.test.ts` 10 (ENV 토글 2 + Default 5: 빈 / 5개 30%잔존 단순 카운트 / reservedSlots 차감 / 분모 floor 1 / 음수 reservedSlots 안전 + ENABLED 3: 자본 가중 1.5 / 만재 동일 floor / source 표기). ⑤ **검증** — vitest 신규 26 + 영향 영역 무회귀 (server/trading + server/persistence + server/learning + server/routes + server/telegram **190 files 1989/1989 pass**) + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범. **LIVE 매매 본체 0줄 변경** — `exitEngine/rules/hardStopLoss.ts` / `perSymbolEvaluation.ts` / `dynamicStopEngine.ts` / `signalScanner.ts` 모두 무수정. 정책 함수 + 영속 schema 1 옵셔널 필드만 추가. ⑥ **운영 효과** — (1) 트랙 1 정책 함수 SSOT 확보 — 후속 PR 에서 hardStopLoss 의 BEP_PROTECTION 분기에 `evaluateTwoBarConfirmation()` 호출 추가만 하면 단봉 노이즈 청산 차단 즉시 효과. (2) 트랙 2 default 비활성 — 운영자가 `SLOT_CAPITAL_WEIGHTED_SIZING_ENABLED=true` 명시 활성화 + 1주 검증 후 sizing 분모 자본 가중. (3) ENV 롤백 2종 (BEP_TWO_BAR_CONFIRMATION_DISABLED + SLOT_CAPITAL_WEIGHTED_SIZING_ENABLED) 으로 안전 격리. ⑦ **잔여 (후속 PR)** — `exitEngine/rules/hardStopLoss.ts` BEP_PROTECTION 분기 wiring + shadowTrade `bepGlideTouchAt` 영속 갱신 (1차 터치 시 save), `perSymbolEvaluation.ts:902` `evaluateSlotSizing()` wiring (default 미사용 → ENABLED 시 활성), intraday sizing 동일 분기 적용. | `docs/adr/0085-two-bar-confirmation-and-slot-sizing.md` (신규), `server/trading/twoBarConfirmation.ts` (신규), `server/trading/twoBarConfirmation.test.ts` (신규 16 케이스), `server/trading/slotSizing.ts` (신규), `server/trading/slotSizing.test.ts` (신규 10 케이스), `server/persistence/shadowTradeRepo.ts` (+bepGlideTouchAt? 옵셔널), `ARCHITECTURE.md` (+2 boundary +2 boundary rule) | LIVE 매매 안전 격리 우선 — 정책 함수 SSOT + 영속 schema 만 신설. 후속 wiring PR 은 운영 데이터 누적 후 활성화. ADR-0079 PR-Z6 (BEP Glide) + ADR-0080 PR-S1 (Capital-Weighted Slot Accounting) 의 §"후속 PR" 명문화된 BEP #2 + PR-S2 두 트랙 인프라 완수. PR-Z (Yahoo 정책) + PR-A (Walk-Forward) + PR-B (Condition Lifecycle) + PR-C 4 PR 단일 브랜치 누적 — 사용자 P10 진단 + 운영 안전 정책 동시 처리. |
| 2026-04-28 | PR-D — Shadow Walk-Forward Framework (ADR-0086, Shadow 학습 다음 단계 #4): 사용자 분석 *"Shadow 데이터도 in-sample/out-of-sample 로 나눠야 함. 그렇게 해야 Shadow 학습도 과최적화를 피할 수 있음."* 직접 반영. PR-A (ADR-0083) `walkForwardFramework` 가 LIVE recommendations 만 IS/OOS 분할하던 갭 — Rejection (PR-L) + Twin (PR-M) Shadow 영속 데이터도 시간 분할 검증으로 통합. ① **`server/learning/shadowWalkForwardFramework.ts` 신규** — `runShadowWalkForward({now?, source})` orchestrator + adapter 2종 (`rejectionToWindowRecord` / `twinToWindowRecord`) + `computeShadowWindowMetrics` + `computeShadowSummary` + `computeShadowDecayTrend`. source: 'REJECTION' | 'TWIN' | 'ALL' (default ALL). REJECTION_FALSE_NEG_THRESHOLD_PCT=5 — currentReturnPct ≥5% 시 isWin=true (alpha 누락 = false negative). Twin 은 currentReturnPct > 0 시 isWin. ② **`server/persistence/shadowWalkForwardResultsRepo.ts` 신규** — `data/walk-forward-shadow-results.json` 별도 영속 (`WALK_FORWARD_RESULTS_FILE` LIVE 와 책임 분리). atomic write + FIFO trim + 손상 fallback + WalkForwardResults schema 재사용. windowId='shadow_*' prefix 로 LIVE 결과와 명확 분리. ③ **`paths.ts` `WALK_FORWARD_SHADOW_RESULTS_FILE` 추가**. ④ **learningRouter +2 endpoint** — `GET /api/learning/shadow-walk-forward` (영속 + disabled) + `POST /shadow-walk-forward/run?source=REJECTION|TWIN|ALL`. ⑤ **텔레그램 `/shadow_walk_forward [N=10]`** (alias `/swf`, LRN, riskLevel=0, ADMIN) — `formatShadowWalkForwardMessage` SSOT 빌더. decayTrend 4분기 이모지 + windowId='shadow_' prefix 분리 안내 + LIVE `/walk_forward` 별도 명시. ⑥ **회귀 테스트 44 케이스 신규** — `shadowWalkForwardResultsRepo.test.ts` 7 (round-trip + FIFO + 손상 + schema 호환) + `shadowWalkForwardFramework.test.ts` 24 (adapter 8 + windowMetrics 2 + summary 3 + 진입점 7 + ENV 4) + `shadowWalkForwardRouter.test.ts` 8 (GET/POST 4분기) + `shadowWalkForward.cmd.test.ts` 9 (format 5 + execute 4). ⑦ **검증** — vitest 신규 44 + 영향 영역 무회귀 (server/persistence + server/learning + server/routes + server/telegram **100 files 1032/1032 pass**) + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** + **Yahoo OHLCV fetch 0건** (영속 Rejection+Twin records 만 사용 — ADR-0082 정합). LIVE 매매 본체 0줄 변경 — `walkForwardFramework.ts` (LIVE) 본체 무수정. ⑧ **운영 효과** — (1) Shadow 학습이 *데이터 수집* 단계에서 *Decision Integration* 단계로 진입 — 시간 분할 검증으로 단일 시점 운 의존 차단. (2) Rejection alpha 누락 (false negative) 추세 가시화 — "우리가 너무 엄격했나?" 질문 정량 답변. (3) 3 Twin 정책의 OOS 성과 추세 비교 — AGGRESSIVE/DISCIPLINED/EQUAL_WEIGHT decay 자동 감지. (4) ENV 5종 롤백 (`SHADOW_WALK_FORWARD_DISABLED` + 4 정책 임계). ⑨ **ADR-0083 정합** — windowId prefix 분리 + 별도 영속 파일로 충돌 차단. ⑩ **잔여 (사용자 분석 Shadow 다음 단계 #1~#3)** — PR-E (Rejected Alpha + Twin Leaderboard 가시화) / PR-F (Condition Attribution 연결, schema 확장) / PR-G (UI Dashboard 5종) 후속 진행. | `docs/adr/0086-shadow-walk-forward-framework.md` (신규), `server/learning/shadowWalkForwardFramework.ts` (신규), `server/learning/shadowWalkForwardFramework.test.ts` (신규 24 케이스), `server/persistence/shadowWalkForwardResultsRepo.ts` (신규), `server/persistence/shadowWalkForwardResultsRepo.test.ts` (신규 7 케이스), `server/persistence/paths.ts` (+WALK_FORWARD_SHADOW_RESULTS_FILE), `server/routes/learningRouter.ts` (+GET/POST shadow-walk-forward), `server/routes/shadowWalkForwardRouter.test.ts` (신규 8 케이스), `server/telegram/commands/system/shadowWalkForward.cmd.ts` (신규), `server/telegram/commands/system/shadowWalkForward.test.ts` (신규 9 케이스), `server/telegram/commands/system/index.ts` (+barrel), `ARCHITECTURE.md` (+2 boundary +1 boundary rule) | 사용자 Shadow 학습 분석 다음 단계 #4 처리. PR-A (LIVE walk-forward) 와 동일 패턴을 Shadow 데이터에 적용 — Decision Integration 단계 진입의 foundation. PR-E/F/G 는 본 PR 의 결과 위에 가시화 + Attribution + UI 단계로 후속 진행. |
| 2026-04-28 | PR-E — Shadow 학습 텔레그램 가시화 (Shadow 학습 다음 단계 #1 + #2): 사용자 분석 *"Shadow 학습 UI 방향 — Missed Alpha / Good Rejection / Twin Portfolio Ranking 등 단순 로그가 아닌 의사결정 가능한 가시화"* 의 텔레그램 단계 진입. PR-D 영속 인프라 위에 운영자 즉시 진단 명령 2종 신설. ① **`server/telegram/commands/system/rejected.cmd.ts` 신규** — `/rejected [N=10]` (alias `/rejection_shadow`, LRN, riskLevel=0, ADMIN). `formatRejectedMessage(input)` SSOT 빌더 — 통계 요약 (총/종결/활성 + reliable 시 false negative rate + 분위수 Q1/Q2/Q3) + 종결 entry 수익률 내림차순 Top N (🔥≥5%/🟢≥0%/🔵<0% 이모지) + 활성 추적 별도 섹션 (Top 5). 사용자 분석 *"우리가 너무 엄격했나?"* 정량 답변. ② **`server/telegram/commands/system/twins.cmd.ts` 신규** — `/twins` (alias `/twin_leaderboard`, LRN, riskLevel=0, ADMIN). `formatTwinsMessage(input)` SSOT 빌더 — CURRENT (LIVE recommendationTracker.compoundReturn) vs 3 Twin (AGGRESSIVE 🔥 / DISCIPLINED 🎯 / EQUAL_WEIGHT ⚖️) cumReturnPct 내림차순 정렬 + 🥇🥈🥉 4️⃣ 순위 + 사이즈 가중 누적 수익률 + Sharpe proxy + weekWinning ✅ (Twin 이 CURRENT 보다 우월) + 4주 연속 우월 promotion 안내 + 자동 임계 조정 금지 명시. 사용자 분석 *"현재 기본 전략이 여전히 최적인가?"* 정량 답변. ③ **system barrel +2 import** — rejected.cmd.js + twins.cmd.js. ④ **회귀 테스트 22 케이스 신규** — `rejected.test.ts` 12 (formatRejectedMessage 7: 빈/reliable/unreliable/이모지 분류/내림차순/limit 절삭/활성 별도 섹션 + execute 5: 정상/alias/throw graceful/메타데이터) + `twins.test.ts` 10 (formatTwinsMessage 6: 빈/내림차순 정렬/✅ 표기/CURRENT 1위 시 ✅ 미표기/promotion 안내/카운트 + execute 4: 정상/alias/throw/메타데이터). ⑤ **검증** — vitest 신규 22 + 영향 영역 무회귀 (server/learning + server/persistence + server/routes + server/telegram **102 files 1052/1052 pass**) + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범**. **LIVE 매매 본체 0줄 변경** — read-only 진단 명령만 추가, rejectionShadowTracker / counterfactualTwinPortfolio / recommendationTracker 본체 무수정. ⑥ **운영 효과** — (1) 운영자가 텔레그램 한 번에 거절 종목 사후 성과 (Missed Alpha) + Twin 정책 비교 (Strategy Leaderboard) 즉시 확인. (2) 4주 연속 Twin 우월 시 promotion 후보 자동 표면화 — 운영자 검토 (자동 임계 조정 금지). (3) Shadow 학습이 *데이터 수집* 단계에서 *Decision Integration* 단계로 진입의 첫 텔레그램 가시화. ⑦ **잔여** — 일일 리포트 라인 wiring (`reportGenerator.ts` 본체 분석 후 별도 PR — 회귀 위험 격리), UI Dashboard (PR-G 후속). PR-F (Shadow → Condition Attribution 연결) / PR-G (UI Dashboard 5종) 후속. | `server/telegram/commands/system/rejected.cmd.ts` (신규), `server/telegram/commands/system/rejected.test.ts` (신규 12 케이스), `server/telegram/commands/system/twins.cmd.ts` (신규), `server/telegram/commands/system/twins.test.ts` (신규 10 케이스), `server/telegram/commands/system/index.ts` (+2 barrel) | 사용자 Shadow 학습 분석 다음 단계 #1+#2 처리 — 운영자가 텔레그램 명령으로 Shadow 데이터 즉시 진단. PR-D 영속 인프라 + PR-E 가시화 결합으로 Decision Integration 단계 첫 wiring. PR-F (schema 확장 + Attribution 연결) / PR-G (UI) 후속. |
| 2026-04-28 | PR-F — Shadow → Condition Attribution 연결 (ADR-0087, Shadow 학습 다음 단계 #3): 사용자 분석 *"거절 종목이 나중에 급등했다면 어떤 조건 때문에 탈락했는가? 반대로 거절 후 폭락했다면 그 조건은 RISK_PROTECTOR 로 강화."* 직접 반영. PR-L `rejectionShadowTracker` 의 사후 추적에 *어떤 조건이 미달이었는지* 메타 정보 결합 분석. ① **`RejectionShadowEntry.conditionScores?` 옵셔널 schema 확장** — 거절 시점 27조건 점수 (1~27 → 0~10) 영속. 기존 영속 데이터 호환 (옵셔널). `RejectionRecordInput` 동일 필드 추가. ② **`server/learning/conditionAttributionShadow.ts` 신규** — `analyzeShadowAttribution()` 27조건 분류 SSOT. 알고리즘: 종결된 entry (closed=true + conditionScores 보유) 만 추출 → currentReturnPct ≥ +5% Over-Strict bucket / ≤ -5% Good Defense bucket / boundary 무시 → 명시된 conditionScores[id] 만 사용 (`!(id in scores) → continue`, false positive 차단) → score < 6 (LOW_SCORE_THRESHOLD, attributionAnalyzer 정합) → 빈도 + 평균 + ratio + classification 산출. 결정 트리: total < 3 → insufficient / ratio ≥ 0.7 → over_strict_candidate / ratio ≤ 0.3 → good_defense_candidate / 그 외 normal. SHADOW_ATTRIBUTION_CONSTANTS SSOT 4종 (OVER_STRICT_RETURN_THRESHOLD_PCT=5 / GOOD_DEFENSE_RETURN_THRESHOLD_PCT=-5 / LOW_SCORE_THRESHOLD=6 / MIN_FREQUENCY=3). ENV `SHADOW_CONDITION_ATTRIBUTION_DISABLED=true` 우회. ③ **endpoint** — `GET /api/learning/condition-attribution-shadow` `ShadowAttributionReport` 응답. ④ **텔레그램 `/shadow_attribution`** (alias `/sa`, LRN, riskLevel=0, ADMIN) — `formatShadowAttributionMessage` SSOT 빌더. status 4분기 (DISABLED/NO_DATA/OK) + 요약 (over_strict/good_defense/insufficient/normal 카운트) + Over-Strict 후보 Top 10 (⚠️ 표시 + over count + avg+%) + Good Defense 후보 Top 10 (🛡️ + defense count + avg-%) + 의사결정 안내 (자동 조정 금지 명시). ⑤ **system barrel +1** (conditionAttributionShadow.cmd.js). ⑥ **회귀 테스트 31 케이스 신규** — `conditionAttributionShadow.test.ts` 21 (상수 SSOT 4 + ENV 2 + 결정 트리 16: DISABLED/NO_DATA/closed=false 제외/conditionScores 부재 제외/27 산출/over-strict/good defense/boundary/score≥6 제외/insufficient/ratio 0.7 boundary/0.3 boundary/balance normal/summary/avgReturn/NaN 안전 + schema 호환 1 + 산출 정합 2) + `conditionAttributionShadow.cmd.test.ts` 10 (formatShadowAttributionMessage 6: DISABLED/NO_DATA/Over-Strict/Good Defense/의사결정 안내/후보 없음 + cmd execute 4: 정상/alias/throw/메타데이터). ⑦ **검증** — vitest 신규 31 + 영향 영역 무회귀 (server/learning + server/persistence + server/routes + server/telegram **104 files 1083/1083 pass**) + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** + Yahoo fetch 0건. **LIVE 매매 본체 0줄 변경** — schema 옵셔널 확장 + 분석 모듈 + endpoint + 명령만 추가. ⑧ **운영 효과** — (1) 거절 종목의 사후 성과 + 탈락 조건 결합 분석 — *"VCP 미충족 거절 후 +25%"* 같은 패턴 자동 식별 → 너무 엄격한 조건 완화 검토. (2) 거절 후 폭락 종목의 미달 조건 → RISK_PROTECTOR 강화 후보. (3) 자동 임계 조정 금지 명시 — 운영자 검토 필수. ⑨ **PR-F-2 후속 (호출자 wiring)** — `signalScanner` / `perSymbolEvaluation` 의 거절 분기에서 `recordRejection({ ..., conditionScores })` 전달. **LIVE 매매 본체 변경** 이라 회귀 위험 격리 위해 별도 PR. 본 PR 결과는 호출자 wiring 후 운영 데이터 누적되어야 NO_DATA → OK 전환. ⑩ **잔여** — PR-G UI Dashboard (Shadow Learning 5종 카드) 후속. | `docs/adr/0087-shadow-condition-attribution.md` (신규), `server/learning/rejectionShadowTracker.ts` (+conditionScores? 옵셔널 schema), `server/learning/conditionAttributionShadow.ts` (신규), `server/learning/conditionAttributionShadow.test.ts` (신규 21 케이스), `server/routes/learningRouter.ts` (+/condition-attribution-shadow endpoint), `server/telegram/commands/system/conditionAttributionShadow.cmd.ts` (신규), `server/telegram/commands/system/conditionAttributionShadow.test.ts` (신규 10 케이스), `server/telegram/commands/system/index.ts` (+barrel), `ARCHITECTURE.md` (+1 boundary +1 boundary rule) | 사용자 Shadow 학습 분석 다음 단계 #3 처리 — Decision Integration 단계 핵심 분석 모듈. 호출자 wiring (PR-F-2) 은 LIVE 매매 회귀 위험 격리 위해 후속 PR 분리. PR-G (UI Dashboard) 후속 진행. |
| 2026-04-28 | PR-G — Shadow Learning UI Dashboard (ADR-0088, 5 카드): 사용자 분석 *"필요한 UI는 단순 로그가 아닌 Missed Alpha / Good Rejection / Twin Portfolio Ranking / Over-Strict / Good Defense 5종 카드"* 직접 반영. PR-D + PR-E + PR-F 영속/분석/텔레그램 인프라 위에 통합 UI 대시보드 신설. ① **신규 페이지 `src/pages/ShadowLearningPage.tsx`** — 5 카드 grid (md:2열 / xl:3열). TanStack Query (60s staleTime + 60s refetchInterval + retry 2). 카드별 loading/error/empty/data 4 상태 처리 + data-testid 속성. ② **신규 endpoint 2종** — `GET /api/learning/rejection-shadow?limit=N` (signalDate 내림차순 정렬 + limit 1~500 default 50) + `GET /api/learning/twin-portfolio` (compareTwinsVsReal + getAllTwinEntries + monthlyStats.compoundReturn). 기존 `/condition-attribution-shadow` (PR-F) 재사용. ③ **신규 클라이언트 SDK `src/api/shadowLearningClient.ts`** — 타입 동기 사본 (절대 규칙 #3 준수) + 3 fetch 함수 (fetchRejectionShadow/fetchTwinPortfolio/fetchShadowAttribution) + ALL_TWIN_KEYS export. ④ **5 카드 의미** — 🎯 Missed Alpha (≥+5% 종목 — "우리가 너무 엄격했나?") / 🛡️ Good Rejection (≤-5% — "우리가 잘 막았나?") / 👯 Twin Portfolio Ranking (CURRENT vs 3 Twin cumReturnPct 정렬 + 🥇🥈🥉 + ✅ weekWinning + Sharpe) / ⚠️ Over-Strict Conditions (over_strict_candidate Top 8) / 🛡️ Good Defense Conditions (good_defense_candidate Top 8). 각 카드 의사결정 안내 라인 (자동 임계 조정 금지 명시). ⑤ **View 등록** — `useSettingsStore.View` +`'SHADOW_LEARNING'` + `viewRegistry.VIEW_LABELS.SHADOW_LEARNING='Shadow 학습'` + `navigation.NAV_GROUPS` 인텔리전스 그룹 + Brain 아이콘 + `PageRouter` view 분기 + `pages/index.ts` export. ⑥ **회귀 테스트 14 케이스 신규** — `shadowLearningClient.test.ts` 8 (ALL_TWIN_KEYS + 3 fetch 함수 × default/limit/throw) + `shadowLearningRouter.test.ts` 6 (rejection-shadow 4: 정상/limit/내림차순/throw + twin-portfolio 2: 정상/throw). ⑦ **검증** — vitest 신규 14 + 영향 영역 무회귀 (server/learning + server/persistence + server/routes + server/telegram + src/api **107 files 1099/1099 pass**) + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** + Yahoo fetch 0건. **LIVE 매매 본체 0줄 변경** — 클라이언트 컴포넌트 + read-only endpoint 만 추가. ⑧ **운영 효과** — (1) 운영자가 사이드바 "인텔리전스 → Shadow 학습" 클릭 한 번으로 5 카드 통합 진단. (2) 텔레그램(/rejected /twins /sa) 가시화 + UI 카드 = 모바일/데스크톱 모두 의사결정 가능. (3) data-testid 속성으로 e2e 회귀 자동화 친화. (4) PR-F NO_DATA → conditionScores 호출자 wiring 후 자동 OK 전환. ⑨ **잔여 (후속 PR)** — ShadowLearningPage 컴포넌트 단위 회귀 (jsdom 환경) / 일일 리포트 Shadow 학습 라인 추가 / PR-F-2 호출자 wiring (signalScanner 거절 분기 conditionScores 전달, LIVE 회귀 격리). ⑩ **사용자 Shadow 학습 다음 단계 #1+#2+#3+#4 모두 처리 완료** — Decision Integration 단계 진입의 인프라 + 가시화 + UI 완성. | `docs/adr/0088-shadow-learning-dashboard.md` (신규), `src/pages/ShadowLearningPage.tsx` (신규), `src/api/shadowLearningClient.ts` (신규), `src/api/shadowLearningClient.test.ts` (신규 8 케이스), `server/routes/learningRouter.ts` (+/rejection-shadow + /twin-portfolio endpoints + 4 import), `server/routes/shadowLearningRouter.test.ts` (신규 6 케이스), `src/stores/useSettingsStore.ts` (View +SHADOW_LEARNING), `src/config/viewRegistry.ts` (+SHADOW_LEARNING 라벨), `src/config/navigation.ts` (+SHADOW_LEARNING 인텔리전스 그룹 + Brain 아이콘), `src/pages/PageRouter.tsx` (+ShadowLearningPage view 분기 + import), `src/pages/index.ts` (+export) | 사용자 Shadow 학습 분석의 마지막 단계 — UI Dashboard 5 카드 완성. PR-D (Walk-Forward) + PR-E (텔레그램) + PR-F (Attribution) + PR-G (UI) 4 PR 누적으로 *Shadow Data Collection* → *Shadow Decision Integration* 단계 전환 완료. PR-F-2 호출자 wiring 만 LIVE 회귀 격리 위해 후속 분리. |
| 2026-04-28 | PR-F-2 + PR-H + PR-I — Shadow 학습 후속 wiring 3종 (사용자 명시 진짜 핵심 후속): PR #413 (PR-D/E/F/G) 머지 후 잔여 wiring 일괄. ① **PR-F-2 — signalScanner 거절 분기 conditionScores 전달**: `perSymbolEvaluation.ts:807` `recordRejection` 호출 직전에 `buildEntryConditionScores(stock.conditionKeys)` 호출 + 결과 `conditionScores` 인자로 전달. PR-F (ADR-0087) 의 `analyzeShadowAttribution()` 분석 모듈이 처음으로 *실제 데이터* 누적 시작 — `NO_DATA` → `OK` 전환 가능. try/catch 격리 그대로 유지 — LIVE 매매 본체 흐름 무중단. NEUTRAL=5 / HIGH=7 score 매핑 — PR-F LOW_SCORE_THRESHOLD=6 임계와 정합. 회귀 5 케이스 (buildEntryConditionScores 4 + perSymbolEvaluation 소스 정합 1). ② **PR-H — 일일 리포트 Shadow 학습 라인 추가**: `server/alerts/shadowLearningSummary.ts` 신규 — `buildShadowLearningSummary(realCumReturnPct)` SSOT 빌더. Rejection (`summarizeRejectionShadow`) reliable=true 시 false negative rate 표기 + Twin (`compareTwinsVsReal`) 중 CURRENT 우월 Twin 추출 (없으면 "Twin 우월 없음"). reportLine + narrativeLine 분리 — 본문 라인 + Gemini 프롬프트 dataBlock. 데이터 부재 시 빈 문자열 → `reportGenerator.generateDailyReport` 가 `filter(Boolean)` 으로 자연 생략. baseReport 의 워치리스트 다음 줄 + dataBlock 의 STRONG_BUY 다음 줄 wiring. 회귀 10 케이스 (데이터 부재 / Rejection reliable / 표본 부족 / Twin 우월 / CURRENT 1위 / 음수 cumReturn / Twin 표본만 / narrativeLine / raw 데이터). ③ **PR-I — ShadowLearningPage 컴포넌트 회귀 (jsdom)**: `src/pages/ShadowLearningPage.test.tsx` 신규 — `@vitest-environment jsdom` 5 카드 (MissedAlphaCard / GoodRejectionCard / TwinRankingCard / OverStrictCard / GoodDefenseCard) 각각 loading/error/data/empty 4 상태 + 정렬/카운트/data-testid 검증. Card 단위 export 활용 — fetch mock 없이 props 직접 주입. 15 케이스 (MissedAlpha 4 + GoodRejection 2 + TwinRanking 3 + OverStrict 4 + GoodDefense 2). ④ **검증** — vitest **server/learning + server/persistence + server/routes + server/telegram + server/alerts + server/trading/signalScanner + src/api + src/pages 144 files 1446/1446 pass** (신규 30 + 무회귀 1416) + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. KIS/KRX 자동매매 quota 0 침범 (절대 규칙 #2/#3/#4). LIVE 매매 본체 — PR-F-2 만 `perSymbolEvaluation.ts` 거절 분기에 wiring 추가 (try/catch 격리, recordRejection 인자 1개 추가). PR-H/I 는 LIVE 매매 영향 0. ⑤ **운영 효과** — (1) PR-F-2 후 거절 종목의 conditionScores 영속 → PR-F 분석 모듈이 *실제 데이터 누적* 시작 (현재 NO_DATA → 운영 1~2주 후 OK). (2) PR-H 후 일일 리포트에 Shadow 학습 1 라인 노출 — `🌑 Shadow: Rejection 8건 (alpha 누락 25%) | AGGR +9.4% (+5.20%p vs CURRENT)` 형식. (3) PR-I 후 UI 회귀 자동화 — 5 카드 4 상태 모두 jsdom 환경 검증. ⑥ **잔여** — PR-A 자동 weight 보정 (decay='DECAYING' 시 가중치 보수화) / PR-C 트랙 1 hardStopLoss BEP_PROTECTION wiring / PR-C 트랙 2 sizing 분모 wiring (1주 검증 후) / PR-B 27조건 가드 wiring (6개월 누적 후) — 모두 운영 데이터 누적 + 사용자 결정 후 별도 PR. | `server/trading/signalScanner/perSymbolEvaluation.ts` (recordRejection 호출 conditionScores 전달), `server/trading/signalScanner/__tests__/conditionScoresWiring.test.ts` (신규 5), `server/alerts/shadowLearningSummary.ts` (신규), `server/alerts/shadowLearningSummary.test.ts` (신규 10), `server/alerts/reportGenerator.ts` (generateDailyReport Shadow 라인 wiring), `src/pages/ShadowLearningPage.test.tsx` (신규 15), `ARCHITECTURE.md` (+conditionAttributionShadow PR-F-2 wiring 표기 + shadowLearningSummary boundary) | 사용자 명시 *"아직 진짜 핵심 후속은 3개"* 일괄 처리 — PR-F-2 (signalScanner 거절 분기 conditionScores) + 일일 리포트 Shadow 라인 + ShadowLearningPage 컴포넌트 회귀. PR #413 머지 후 main pull/merge 한 위에 추가 wiring. PR-F 의 NO_DATA → 운영 데이터 누적 시 자동 OK 전환 — Shadow Decision Integration 의 *실제 데이터 흐름* 시작점. |
| 2026-04-28 | PR-Z 옵션 B + C — DeepAnalysisModal cc 분해 + Cache Coherence Auditor SSOT (ADR-0090, 부채 #1·#3 동시 처리): 사용자 부채 #1 (God Function 127건 — UI 컴포넌트 중심) + #3 (영속 파일 99개 cross-consistency) 동시 처리. 옵션 A (wiring 활성화 — LIVE 회귀 위험) 보류, 옵션 B + C 동시 진행. (A) **DeepAnalysisModal cc=95 → cc=30 (-68%) lines=351 → 208 (-41%)**: 인라인 god function 4개 추출 — ① `buildEvaluateInput.ts` (5 sub-builder: buildRawStockData / buildRegime / buildSectorRotation / buildAdvancedContext / buildExtendedRegimeOptions, 80줄 인라인 객체 빌더 → 5개 helper) ② `MasterRadarChart.tsx` (Radar Chart + getRadarData SSOT) ③ `KeyChecklistOverview.tsx` (4그룹 핵심 체크리스트 그리드 + ChecklistGroup + ChecklistRow sub-components) ④ `buildMockShadowSignal.ts` (QUANT 뷰 onShadowTrade 모의 시그널). 메인 함수는 *조립자(orchestrator)* 역할만. 회귀 테스트 40 케이스 (buildEvaluateInput 21 + MasterRadarChart 8 + buildMockShadowSignal 9 + 인접 무회귀 2). (B) **Cache Coherence Auditor (ADR-0090)**: 79개 영속 파일 중 5종 invariant — I1 OCO ↔ Shadow 참조 (CRITICAL, ghost 매도) / I2 Pending ↔ Shadow 참조 (CRITICAL, fillMonitor 무한 폴링) / I3 Attribution schemaVersion 정합 (WARN, 마이그레이션 누락) / I4 TradeSignal status ↔ finalizedAt 정합 (WARN) / I5 ConditionWeights [0.1, 2.0] 범위 (WARN/CRITICAL) — read-only 검증 SSOT 신설. `server/persistence/cacheCoherenceAuditor.ts` (외부 호출 0 + 영속 수정 0). HTTP `GET /api/system/coherence-audit` + Telegram `/coherence_audit [N=10]` (alias `/cohere`, SYS, riskLevel=0, ADMIN). `formatCoherenceAuditMessage` SSOT 빌더 (CRITICAL 우선 정렬 + Top N + HTML escape 안전). ENV `CACHE_COHERENCE_AUDIT_DISABLED=true` 우회. 회귀 테스트 51 케이스 (cacheCoherenceAuditor 41 + coherenceAudit.cmd 10). (C) **검증** — vitest **server/persistence + server/routes + server/telegram + src/components/analysis/DeepAnalysisModal 78 files 838/838 pass** + lint(client + server tsc) + validate:all 9종 (Gemini/ACMA/SDS/PRES/SymbolBoundary/ChannelBoundary/SensitiveAlerts/MarketOverviewBoundary/YahooRange) + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — kisClient/orchestrator/autoTradeEngine 본체 무수정). **LIVE 매매 본체 0줄 변경** — UI 분해는 컴포넌트만, Audit 은 read-only. (D) **운영 효과** — (1) DeepAnalysisModal 의 인라인 prop 빌더 + 차트 + 체크리스트가 helper 모듈로 분리되어 유지보수 가능. (2) 운영자 텔레그램 `/coherence_audit` 입력 시 5종 invariant 위반 즉시 표면화 — 사용자 부채 #3 "발생 시 디버그 어렵다" 영구 해소. (3) 80번째, 100번째 영속 파일 추가 시 *어떤 정합 규칙* 갖는지 ADR 단계에서 명문화 의무화. (E) **잔여 (후속 PR scope 밖)** — Phase 2 cron 자동 audit 발행, Phase 3 invariant 확장 (WATCHLIST/MACRO_STATE 등), Phase 4 자동 수정 도구. 다른 god function 126건 분해 (AnalysisDashboard cc=71 / AIIntelligenceSection cc=70 등) 는 회귀 위험 격리 위해 별도 PR. | `docs/adr/0090-cache-coherence-auditor.md` (신규), `src/components/analysis/DeepAnalysisModal.tsx` (cc=95→30), `src/components/analysis/DeepAnalysisModal/{buildEvaluateInput,MasterRadarChart,KeyChecklistOverview,buildMockShadowSignal}.ts(x)` (신규 4 helper), `src/components/analysis/DeepAnalysisModal/{buildEvaluateInput,MasterRadarChart,buildMockShadowSignal}.test.ts` (신규 40 케이스), `server/persistence/cacheCoherenceAuditor.ts` (신규), `server/persistence/cacheCoherenceAuditor.test.ts` (신규 41 케이스), `server/routes/systemRouter.ts` (+/system/coherence-audit endpoint), `server/telegram/commands/system/coherenceAudit.cmd.ts` (신규), `server/telegram/commands/system/coherenceAudit.test.ts` (신규 10 케이스), `server/telegram/commands/system/index.ts` (+barrel), `ARCHITECTURE.md` (+2 boundary rule) | 사용자 부채 #1 (UI god function) + #3 (영속 cross-consistency) 동시 처리. 부채 #2 (wiring 미완) 는 사용자 명시 보류 — LIVE 자동매매 회귀 위험 격리. 부채 #4 (검증 스크립트 11개 성능) 는 향후 변경 파일 한정 모드 도입 후속 PR. |
| 2026-04-29 | PR-Z4 — Yahoo Stale Base 결과 객체화 + KIS 폴백 + STALE_BASE 종목 제외 (ADR-0091, 사용자 패치 직접 반영): 사용자 4/29 Railway 로그 보고 — 6개 종목 (189300/001430/027360/088280/036540/163730 .KS) 에서 `changePercent` -50~-86% sanity 위반 누적. base.asOf=4/27 vs current=4/29 — 한국 영업일 2일에 -71% 변화 불가능 (상한가 -30% × 2일도 -51%) → Yahoo 가 액면병합/조정/4/28 데이터 누락으로 오염된 base 반환. 사용자 정확한 진단: **"Sanity 체크가 없는 게 아니라, Sanity 실패 후에도 Yahoo base 를 계속 신뢰하는 구조"**. 기존 `safePctChange(...) ?? 0` 패턴이 sanity 위반을 silent 0 으로 흡수해 enrichment 가 정상값처럼 받아들여 의사결정에 사용. ① **`safePctChangeDetailed()` 신설** (server/utils/safePctChange.ts) — `{value, valid, reason}` 객체 반환 (reason 4분기: OK/INVALID_PRICE/STALE_BASE_OR_SPLIT_ADJUSTMENT/STALE_BASE_AGE). 기존 `safePctChange(): number | null` 51 호출 site 후방호환 유지 — 신규 호출자만 detailed 사용. 진단 로그 메시지에 *Yahoo 등락률 폐기 권고 — KIS 폴백 또는 종목 감점/제외 필수* 명시. ② **yahooQuoteAdapter 3 % 변화율 필드 detailed 전환** — `changePercent` / `return5d` / `return20d` 가 `?? 0` silent fallback 폐기, valid=false 시 `dataQualityIssues` 배열에 누적. `YahooQuoteExtended` 에 옵셔널 2 필드 추가 (`dataQuality?: 'OK' | 'STALE_BASE'` + `dataQualityIssues?: Array<'changePercent' | 'return5d' | 'return20d'>`). ③ **stockScreener.autoPopulateWatchlist STALE_BASE 분기 + KIS 폴백 + 종목 제외** — `dataQuality === 'STALE_BASE'` 시 결정 트리: changePercent 단일 위반 + KIS intraday 호출 (`fetchKisIntraday(stock.code)`) → KIS prevClose 정상 + |kisChangePct| ≤ 30% 시 quote 덮어쓰기 (KIS_RECOVERED 로그 1줄) / 그 외 universe 제외 + rejectionLog 기록 (다중 위반 / KIS 폴백 실패 / KIS 도 sanity 위반 시 모두 EXCLUDED). KIS 일봉 폴백 (return5d/return20d 보정) 은 호출 비용 부담으로 본 PR 보류 — 다중 위반은 종목 제외. ④ **회귀 테스트 73 케이스 신규** — safePctChangeDetailed 23 (INVALID_PRICE 7 + STALE_BASE_OR_SPLIT 7 + STALE_BASE_AGE 4 + 진단 로그 3 + 호출자 폴백 패턴 2) + yahooQuoteAdapter dataQuality marker 5 (정상 OK / changePercent -71% 시나리오 / return5d 위반 / 다중 위반 / 진단 로그) + stockScreener STALE_BASE 결정 트리 10 (PASS / KIS_RECOVERED / EXCLUDED 분기 + 6 종목 일괄 검증). 기존 어구 정정 3건 (메시지 prefix `safePctChange` → `safePctChangeDetailed` + reason 분리). ⑤ **검증** — vitest server/utils + server/screener 73/73 신규 + lint(client + server tsc) + validate:all 9종 + ALLOW_DEPLOY_WINDOW=1 precommit 본체 전체 통과 EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — kisClient/orchestrator/autoTradeEngine 본체 무수정). LIVE 매매 본체 — yahooQuoteAdapter 의 `?? 0` 패턴 → 명시적 0 + marker (기존 caller 가 `quote.changePercent` 를 number 로 사용하는 부분 무영향), stockScreener 분기 1개 추가 (정상 dataQuality 종목 무영향). ⑥ **운영 효과 (배포 후)** — 이미지 6 종목 시나리오 시: (a) STALE_BASE marker 부여 → KIS intraday 폴백 시도 → 정상 changePercent 로 의사결정 가능 / (b) 폴백 실패 시 universe 제외 + 진단 로그 1줄 (이전 매 분 cron 마다 sanity 위반 로그 폭주 + 오염된 데이터 의사결정 영구 차단). 5분 캐시로 자연 throttle. ⑦ **잔여 (후속 PR)** — KIS 일봉 폴백 (return5d/return20d) / 종목별 sanity 위반 카운터 24h 블랙리스트 (PR-24 패턴) / Gate/MOMENTUM marker 감점 (STALE_BASE 회복 종목). | `docs/adr/0091-yahoo-stale-base-fallback.md` (신규), `server/utils/safePctChange.ts` (+safePctChangeDetailed + SafePctChangeResult + SafePctChangeReason union), `server/utils/safePctChangeDetailed.test.ts` (신규 23 케이스), `server/screener/adapters/yahooQuoteAdapter.ts` (3 필드 detailed wiring + dataQualityIssues 누적 + dataQuality marker), `server/screener/adapters/yahooQuoteAdapter.test.ts` (+5 dataQuality 케이스 + 어구 정정 3), `server/screener/stockScreener.ts` (STALE_BASE 분기 + KIS intraday 폴백 + 종목 제외), `server/screener/stockScreenerStaleBase.test.ts` (신규 10 결정 트리 케이스), `ARCHITECTURE.md` (+1 boundary rule) | 사용자 진단 직접 반영 — "Sanity 실패 후에도 Yahoo base 를 계속 신뢰하는 구조" 영구 차단. `?? 0` silent fallback → 명시적 분기 + 결과 객체 + KIS 폴백 + 종목 제외 4중 안전망. 사용자 패치 예시 직접 구현 (`safePctChange({...}) -> {value, valid, reason}` 패턴). |
| 2026-04-29 | PR-Z5 — 매매 채널 청산 알림 누적 잔량 정합 (ADR-0092, 사용자 SK이노베이션 보고): 사용자 4/29 SK이노베이션 (096770) 매매 채널 알림 *잔여 보유* 표기 불일치 보고. 4주 누적 부분청산 시 `잔여 = 원래 4주 - 해당 회차` 잘못된 계산으로 매번 "잔여 3주" 표기 (실제 50% → 2주 → 1주 → 0주). 봇 채널 SHADOW 가상 체결 + `/reconcile` + `/pos` 모두 정상 — fills SSOT (`getRemainingQty`) 정확. **매매 채널 메시지 빌더만 누적 미반영**. ① **근본 원인** (`channelPipeline.ts:135`): `remaining = isPartial ? p.originalQty - p.soldQty : 0` — 호출자가 `originalQty: shadow.originalQuantity` (최초 매수 4주) + `soldQty: sellQty` (이번 회차 1주) 만 전달 → 누적 매도 fill 미반영. 9 호출자가 `reserveSell()` 결과의 `.remainingQty` (fills SSOT 누적 반영) 를 텔레그램 알림에는 사용하지만 *채널 알림에는 미전달* — 정보 단절. ② **`ChannelSellSignalParams.remainingQtyAfter?: number` 옵셔널 추가** + 빌더 우선순위 SSOT — 명시 시 정확한 잔량 사용 + isPartial=remainingQtyAfter>0, 미명시 시 후방호환 폴백 + NaN/음수 입력 자동 폴백 + remainingQtyAfter=0 시 "전량청산" 라벨 자동 격상. ③ **9 호출자 wiring** — rrrCollapseExit / cascadeFinal / trancheTakeProfitLimit / trailingStop / ma60DeathForceExit / euphoriaPartialExit / legacyTakeProfit / hardStopLoss / bearishDivergenceExit 모두 `remainingQtyAfter: <reserveVar>.remainingQty` 추가. 5 전량청산 호출자 (originalQty 미명시) 도 LIVE 부분 체결 시나리오 자동 부분청산 라벨 격상 — 운영 정확성 추가 격상. ④ **회귀 테스트 10 케이스 신규** (`channelPipelineRemainingQty.test.ts`) — 사용자 보고 시나리오 3 (50%→2주 / 25% RRR→1주 / 25% TRANCHE→0주) + 후방호환 2 + 안전 가드 3 (NaN / 음수 / 0+부분청산 의도 자동 격상) + 5 전량청산 호출자 패턴 2 (LIVE 부분 체결 자동 부분청산 라벨 격상). ⑤ **검증** — vitest server/alerts + server/trading/exitEngine **36 files 352/352 pass** (신규 10 + 무회귀) + lint(client + server tsc). **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — 청산 결정/주문 로직 무수정). **LIVE 매매 본체 0줄 변경** — 메시지 빌더 + 9 호출자 1줄씩 추가만. ⑥ **운영 효과** — SK이노베이션 시나리오 50%→2주 / 25%→1주 / 25%→0주(전량청산) 정확 표기. 다른 부분청산 종목도 자동 정확화. `remainingQtyAfter` 미전달 시 후방호환 폴백으로 회귀 위험 0. ⑦ **잔여 (후속 PR)**: 봇 채널 `[SHADOW] 익절` 알림 동일 패턴 확장 / 누적 실현손익 라인 / 부분청산 진행률 시각화. | `docs/adr/0092-channel-sell-cumulative-remaining-qty.md` (신규), `server/alerts/channelPipeline.ts` (+ChannelSellSignalParams.remainingQtyAfter? + isPartial/remaining 우선순위 SSOT), `server/alerts/channelPipelineRemainingQty.test.ts` (신규 10 케이스), `server/trading/exitEngine/rules/{rrrCollapseExit,cascadeFinal,trancheTakeProfitLimit,trailingStop,ma60DeathForceExit,euphoriaPartialExit,legacyTakeProfit,hardStopLoss,bearishDivergenceExit}.ts` (+remainingQtyAfter wiring 9 호출자), `ARCHITECTURE.md` (+1 boundary rule) | 사용자 SK이노베이션 보고 직접 대응 — 매매 채널 알림 누적 잔량 정확성 확보. fills SSOT 와 채널 메시지 단절 영구 해소. 봇 채널·`/reconcile`·`/pos` 정상 동작 확인됨. |
| 2026-04-29 | PR-Z6 — FOMC/VIX 게이팅 알림 dedupeKey + 12h cooldown 정합 (ADR-0093, 사용자 보고 채팅창 도배): 사용자 4/29 FOMC DAY 보고 — `📅 [FOMC 게이팅] 신규 진입 차단` 메시지가 매 1분 발송되어 채팅창 도배. `orchestrator_tick` cron (매 1분 KST 09:00~17:00) 이 `runAutoSignalScan` 호출 → FOMC DAY 약 480 분 동안 480회 동일 메시지 발송. **근본 원인**: `signalScanner.ts:323-327/347-350` + `preflight.ts:257-261/282-286` 의 4 게이팅 차단 알림 호출에 `dedupeKey` 부재. 같은 파일(preflight.ts:299-304)의 *FOMC 우호 환경 완화* 알림은 이미 `dedupeKey: fomc_relaxed_${date}` + `cooldownMs: 12h` 사용 중 → **drift 결함**. ① **4 site 모두 dedupeKey + 12h cooldown 추가** — VIX 게이팅 → `vix_gating_block:${KST_DATE}` (KST 일자 1회) / FOMC 게이팅 → `fomc_gating_block:${fomcProximity.nextFomcDate ?? 'unknown'}` (FOMC 회차 1회, fomc_relaxed_ 와 정합). `nextFomcDate` 누락 시 `unknown` fallback 으로 안전 작동. ② **회귀 테스트 6 케이스 신규** (`gatingAlertDedupe.test.ts`) — 정적 grep 패턴 검증: signalScanner.ts VIX/FOMC 각 1 (3 elements) + preflight.ts VIX/FOMC 각 1 (3 elements) + 회귀 차단 가드 (4 site 알림 본문 + .catch 사이 dedupeKey 의무 grep, 미래 결함 자동 fail) + 기존 fomc_relaxed_ 패턴 보존. ③ **검증** — vitest server/trading **96 files 1012/1012 pass** (신규 6 + 무회귀) + lint(client + server tsc). **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — 게이팅 결정 로직 무수정). **LIVE 매매 본체 0줄 변경** — 알림 *내용* 동일, *횟수* 만 480회 → 1회. ④ **운영 효과** — 다음 FOMC DAY (2026-06-19) 09:00~16:00 발송 횟수 약 420회 → 1회. VIX panic 시 매 분 발송도 KST 일자 1회. 채팅창 도배 영구 차단 — 운영자가 진짜 신호 (R6_DEFENSE / 비상정지 / 손절) 에 집중 가능. ⑤ **잔여 (후속 PR)**: VIX 게이팅 *해제* 알림 / R6_DEFENSE 게이팅 dedupeKey (preflight.ts:250) / 데이터 빈곤 게이트 cooldown 정책 통일. | `docs/adr/0093-gating-alert-dedupe.md` (신규), `server/trading/signalScanner.ts` (VIX/FOMC 차단 알림 dedupeKey 추가 2 site), `server/trading/signalScanner/preflight.ts` (VIX/FOMC 차단 알림 dedupeKey 추가 2 site), `server/trading/gatingAlertDedupe.test.ts` (신규 6 케이스 — 정적 회귀 가드), `ARCHITECTURE.md` (+1 boundary rule) | 사용자 보고 직접 대응 — "채팅창 너무 더럽힌다" 영구 해소. fomc_relaxed_ 정합 패턴이 같은 파일에 있던 drift 결함 차단. 정적 grep 회귀 가드로 미래 동일 결함 자동 차단. |
| 2026-04-29 | PR-Z7 — UI Phase A 신설 (UI Language SSOT + useUILang 훅 + 정적 검증 게이트 + Phase A DoD, ADR-0094, 사용자 12 아이디어 분석 중 #1+#2+#11 통합): 사용자 분석 *"Phase A 강화 — 언어 SSOT 게이트키퍼 / SSOT 가 있어도 컴포넌트마다 매번 import 쓰는 건 마찰이 큽니다. 마찰이 큰 SSOT 는 우회됩니다"* 직접 반영. 코드베이스 audit 결과 `uiLanguage.ts` / `useUILang.ts` / `check_ui_language.js` 모두 부재 — 사용자 분석의 "Phase A 강화" 는 실제로는 *Phase A 신설*. ① **`src/config/uiLanguage.ts` 신설** (UI 카피 단일 출처 SSOT) — 7 카테고리: nav (10 키) / card (8) / tier 5-tier (VERIFIED/EXTERNAL/DELAYED/ESTIMATED/MANUAL) / regime 6 RegimeLevel (R1~R6) / gate 4 단계 (0/1/2/3) / empty 4 sub-variant (DELAYED/INSUFFICIENT/STALE/CONFLICTED — Phase B IDontKnow 사전 등록) / action (7 키). 총 약 44 라벨. 금지 표현 정책 4 카테고리 명문화. ② **`src/hooks/useUILang.ts` 신설** (#1 게이트키퍼 훅, S/C 6.0 최고 우선순위) — `const t = useUILang(); t.nav('DISCOVER')` 한 줄 패턴. 7 메서드 + raw 직접 접근. useMemo 메모이즈. 향후 KO/EN 토글 / A/B 테스트 / 사용자별 라벨 커스터마이징은 본 훅 내부만 수정 → 50+ 컴포넌트 무수정. ③ **`scripts/check_ui_language.js` 신설** (`check_yahoo_range.js` 패턴 차용) — 4 카테고리 19 패턴 (ABSOLUTE/AMBIGUOUS_SOURCE/EMOTIONAL/PROMISE) string/template literal 안에서만 매칭, 식별자/주석 제외. 화이트리스트 5 파일 + `BASELINE_TECHNICAL_DEBT` 14 파일 별도 카탈로그 (기존 부채). ④ **정책 분리** — `validate:all` → full 모드 (운영자 진단) + `precommit` → `--changed` 모드 (staged 신규 강제 — 회귀 차단). 마이그레이션 PR 이 BASELINE 카탈로그에서 1개씩 제거 → 자동 회귀 가드 강화. ⑤ **`src/__tests__/uiLanguagePhaseA-DoD.test.ts` 신설** (#11 Phase A DoD 묶음, S/C 5.5) — 9 DoD: SSOT 7 카테고리 / 훅 contract / 5-tier / 6-RegimeLevel / 4-Gate / 4 IDontKnow sub-variant / 신규 5 파일 검증 통과 / ADR-0094 존재 / 모든 값 string trim 정합. ⑥ **회귀 테스트 47 케이스 신규** — check_ui_language.test.js 18 + uiLanguage.test.ts 8 + useUILang.test.ts 11 (jsdom + renderHook) + uiLanguagePhaseA-DoD.test.ts 10. ⑦ **검증** — vitest 신규 47/47 pass + 인접 무회귀 95/95 + lint(client + server tsc) + validate:all 11종 (Gemini + ACMA + SDS + PRES + Responsibility + SymbolBoundary + ChannelBoundary + SensitiveAlerts + MarketOverviewBoundary + YahooRange + UILanguage) + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — kisClient/orchestrator/signalScanner/autoTradeEngine 본체 무수정). **LIVE 매매 본체 0줄 변경**. ⑧ **운영 효과** — 신규 PR 에서 페르소나 위반 표현 자동 차단 (precommit `--changed`) + 향후 KO/EN 토글은 useUILang() 훅 내부만 수정 + Phase B/C/D 진입 시 라벨 결정 시간 낭비 안 함 + Phase A DoD 미래 회귀 자동 fail. ⑨ **잔여 (12 아이디어 후속 PR)** — PR-Phase-A-2: DataQualityBadge 5-tier 격상 (#3) / PR-Phase-A-3: --discover 모드 (#2) / PR-Phase-B: IDontKnow 4-variant + DataQualityRibbon (#4, #5) / PR-Phase-C: V-E-R Card + Stop-loss First + Time-band 띠 (#6, #7, #8) / PR-Phase-D: ConfluenceMeter 4축 (#9, #10) / PR-Verbose: uiVerbosity 토글 (#12) / PR-Migration: 50+ 컴포넌트 점진 마이그레이션 + BASELINE 카탈로그 점진 제거. ADR 번호 충돌 회피 — 사용자 원안 "ADR-0029" 였으나 0029 가 이미 3개 사용 중 → 0094 재할당. | `docs/adr/0094-ui-language-ssot.md` (신규), `src/config/uiLanguage.ts` (신규), `src/config/uiLanguage.test.ts` (신규 8 케이스), `src/hooks/useUILang.ts` (신규), `src/hooks/useUILang.test.ts` (신규 11 케이스), `scripts/check_ui_language.js` (신규), `scripts/check_ui_language.test.js` (신규 18 케이스), `src/__tests__/uiLanguagePhaseA-DoD.test.ts` (신규 10 케이스), `package.json` (+validate:uiLanguage + validate:all/precommit 통합), `ARCHITECTURE.md` (+3 모듈 boundary +3 boundary rule) | 사용자 12 아이디어 분석 중 가장 foundation 인 Phase A 게이트키퍼 단독 PR. 우선순위 #1 (S/C 6.0 useUILang 훅) + #11 (S/C 5.5 Phase A DoD) + #2 보조 (검증 스크립트, --discover 모드는 후속) 통합. SSOT 가 없으면 후속 Phase B/C/D 의 라벨 결정마다 50+ 파일 grep 부담 + 회귀 위험 — 본 PR 이 그 부담을 영구 차단. byte-equivalent 패턴 (기존 컴포넌트 0줄 수정) 으로 회귀 위험 격리. |
| 2026-04-29 | PR-Z8 — UI Phase A-2: DataQualityBadge 5-tier 자동 사다리 격상 (ADR-0095, 사용자 #3 아이디어): ADR-0028 PR-A 가 도입한 3-tier (computed/api/aiInferred) 가 ADR-0094 의 UI_LANG.tier 5-tier (VERIFIED/EXTERNAL/DELAYED/ESTIMATED/MANUAL) 와 *시멘틱 갭* — 사용자 분석 #3 직접 반영: *"휴리스틱 fallback 이 항상 작동하되, 서버 메타가 도착하면 자동으로 더 정확한 분류로 승격"*. ① **`ConditionSourceTier` 5값 union 확장** — 기존 3값 (COMPUTED/API/AI_INFERRED) 보존 + 신규 2값 (DELAYED/MANUAL) 추가. 후방호환 의무 — 기존 호출자 무수정. ② **`DataQualityCount` 옵셔널 카운트 2 추가** — `delayed?` / `manual?` 옵셔널 필드. 기존 50+ 호출자 무수정 (옵셔널 패턴). ③ **`classifyDataQuality` 자동 사다리** — `dataSourceType === 'STALE'` → `delayed +1` (이전엔 aiInferred 흡수). REALTIME/YAHOO/AI 분기는 보존. 메타 모드는 5-tier 메타 ('DELAYED'/'MANUAL') 도착 시 자동 정확 분류. 휴리스틱 모드는 STALE 만 자동 사다리 (나머지 27 조건은 결정적 입력 부재). ④ **`DataQualityBadge` 5-tier 라벨 (`useUILang` SSOT 사용)** — compact 모드: 기본 3-tier 표시 + delayed > 0 시 ⏳, manual > 0 시 ✏️ 추가 (카운트 0 자동 생략 — UI 간결성). !compact 모드: 5 줄 풀 표시 (delayed/manual 0 줄 자동 생략). UI_LANG.tier 한국어 라벨 (실측/API 수신/지연 데이터/AI 추정/수동 입력) 사용 — ADR-0094 SSOT 정합. ⑤ **회귀 테스트 신규 23 케이스 + 정정 2** — `dataQualityClassifier.test.ts` +5 (STALE → delayed 분리 + 5-tier 자동 사다리 4 + 후방호환 + total 합산) + `dataQualityClassifier.metaMode.test.ts` +3 (DELAYED 메타 5건 + MANUAL 메타 3건 + 5-tier 혼재) + `DataQualityBadge.test.tsx` 신규 13 (jsdom + render — 3-tier 후방호환 + delayed ⏳ + manual ✏️ + 카운트 0 자동 생략 + ? 아이콘 + UI_LANG 라벨 + aria-label 5-tier 정합). 정정: 기존 STALE 케이스 2건 (aiInferred=1 → delayed=1). ⑥ **검증** — vitest 신규 23 + 인접 무회귀 + lint(client + server tsc) + validate:all 11종 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — UI 인프라만 격상). **LIVE 매매 본체 0줄 변경** — 클라이언트 분류기 + UI 컴포넌트 격상. ⑦ **운영 효과** — STALE 데이터 종목이 ⏳ 아이콘으로 즉시 인지 가능 (이전엔 AI 추정 카운트로 흡수되어 sources 모호). 향후 KO/EN 토글 시 5-tier 라벨 자동 격상 (useUILang SSOT 정합). Phase B IDontKnow 4-variant 가 본 PR DELAYED 카운트 위에 자연 매핑. ⑧ **잔여 (후속 PR)** — PR-Phase-A-3: --discover 모드 (#2) / PR-Phase-A-4: 백엔드 5-tier 메타 wiring (enrichment.ts) / PR-Phase-B: IDontKnow + DataQualityRibbon (#4, #5) / PR-Phase-C: V-E-R Card (#6, #7, #8) / PR-Phase-D: ConfluenceMeter (#9, #10) / PR-Migration: WatchlistCard 등 50+ 컴포넌트 useUILang 점진. **Stack PR** — base = `claude/ui-improvements-phase-a-d-TVTMY` (PR #420 위에 stack). ADR-0094 SSOT 위에 자연 확장. | `docs/adr/0095-data-quality-5tier-auto-ladder.md` (신규), `src/types/ui.ts` (ConditionSourceTier 5값 union + DataQualityCount delayed?/manual? 옵셔널), `src/utils/dataQualityClassifier.ts` (5-tier 자동 사다리 + STALE 분기), `src/utils/dataQualityClassifier.test.ts` (+5 케이스 + 정정 2), `src/utils/dataQualityClassifier.metaMode.test.ts` (+3 케이스 + 정정 1 + AnyTier 타입), `src/components/common/DataQualityBadge.tsx` (5-tier 라벨 + useUILang 사용), `src/components/common/DataQualityBadge.test.tsx` (신규 13 케이스), `ARCHITECTURE.md` (+1 boundary rule) | 사용자 12 아이디어 #3 (S/C 3.5) 직접 구현. ADR-0028 PR-A 의 3-tier 와 ADR-0094 의 UI_LANG.tier 5-tier 시멘틱 갭 해소. 백엔드 변경 0건 (휴리스틱 자동 사다리만) — 즉시 가시적 효과. Phase B 진입 시 IDontKnow 매핑 자연. PR #420 (Phase A) 위에 Stack PR 패턴. |
| 2026-04-29 | PR-Z9 — UI Phase B: IDontKnow 4-variant + DataQualityRibbon (ADR-0096, 사용자 #4 + #5 통합): 사용자 12 아이디어 분석 #4 (모름의 4 sub-variant — *"왜 모르는지를 즉시 인지하면 좌절감 사라짐"*) + #5 (페이지 상단 누적 신뢰도 띠 — *"매번 개별 배지 안 봐도 페이지 신뢰도를 무의식 감지"*) 단일 PR 통합. 두 컴포넌트 모두 Phase A SSOT (UI_LANG.empty + UI_LANG.tier) + Phase A-2 5-tier (DataQualityCount delayed/manual) 위에 자연 확장. ① **`src/components/common/IDontKnow.tsx` 신설** (compound component) — 4 sub-variant: Delayed (Clock + slate, "기다리면 해결됨") / Insufficient (BarChart3 + zinc, "데이터 누적 필요") / Stale (Moon + blue, "다음 영업일 갱신") / Conflicted (AlertCircle + amber, "검토 필요"). 각 variant 다른 아이콘 + 색상 + 권고 액션 — 페르소나 *"Stop-loss is operating cost"* 의 거꾸로 적용 (모름도 정보의 비용). useUILang().empty(...) SSOT 사용 (UI_LANG.empty 4 sub-variant 정합). message/action prop override + compact (배지) vs 배너 모드 분기 + data-idontknow-variant 속성 (e2e). ② **`src/components/common/DataQualityRibbon.tsx` 신설** — 페이지 상단 가로 띠 (3px 기본). `computeRibbonRatios(counts)` 순수 함수 SSOT — 5 카테고리 합산 + grade 산출 (estimatedPct ≥0.50 WARN / ≥0.30 OK / 그 외 GOOD). 5 segment 색상 (emerald/cyan/slate/amber/violet) — DataQualityCount 의 verified/external/delayed/estimated/manual 비율. ARIA role="img" + aria-label 5-tier 정합 (UI_LANG.tier 라벨). 카운트 0 segment 자동 생략. height/className prop 지원. data-quality-ribbon-grade 속성. ③ **회귀 테스트 신규 37 케이스** — `IDontKnow.test.tsx` 19 (4 variant export + data 속성 + UI_LANG SSOT 메시지 + override + compact vs 배너 + ARIA + 4 색상 차이) + `DataQualityRibbon.test.tsx` 18 (computeRibbonRatios 순수 함수 8 + Ribbon 컴포넌트 10: placeholder / grade 속성 / 카운트 0 자동 생략 / 5 segment 모두 / aria-label / height/className). ④ **검증** — vitest 신규 37/37 + 인접 무회귀 + lint(client + server tsc) + validate:all 11종 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — UI 컴포넌트만 도입, 백엔드 0건). **LIVE 매매 본체 0줄 변경** + **기존 컴포넌트 무수정** (OffHoursBanner / MarketDataStaleBadge / RecommendationWarningsBanner 흡수는 후속 PR). ⑤ **운영 효과** — 모름 표현 통일 (4 시각 언어로 사용자가 *왜 모르는지* 즉시 인지 → 좌절감 ↓ + 대기 가능 시간 정확 계산) + 페이지 신뢰도 ambient 노출 (Ribbon 띠 색상으로 페이지 전체 신뢰도 무의식 감지) + UI_LANG SSOT 활용 (향후 KO/EN 토글 자동 격상) + Phase A/A-2 위에 자연 확장. ⑥ **잔여 (후속 PR)** — PR-Phase-B-2: OffHoursBanner / MarketDataStaleBadge / RecommendationWarningsBanner 흡수 (각 사용처 PR 별도) / PR-Phase-B-3: DataQualityRibbon MarketOverviewHeader / DiscoverWatchlistPage 점진 임베드 / PR-Phase-B-4: Conflicted variant 자동 트리거 (ADR-0071 cross-source validator 결합) / PR-Phase-C: V-E-R Card + Stop-loss First (#6, #7, #8) / PR-Phase-D: ConfluenceMeter 4축 (#9, #10). **3중 Stack PR** — base = `claude/ui-phase-a-2-data-quality-5tier-TVTMY` (PR #421). PR #420 (Phase A) → PR #421 (Phase A-2) → 본 PR (Phase B) 자연 stack. | `docs/adr/0096-idontknow-and-data-quality-ribbon.md` (신규), `src/components/common/IDontKnow.tsx` (신규 4 variant), `src/components/common/IDontKnow.test.tsx` (신규 19 케이스), `src/components/common/DataQualityRibbon.tsx` (신규 + computeRibbonRatios 순수 함수 export), `src/components/common/DataQualityRibbon.test.tsx` (신규 18 케이스), `ARCHITECTURE.md` (+2 boundary rule) | 사용자 12 아이디어 #4 + #5 통합 구현. 두 컴포넌트 모두 페르소나 *"모름도 정보"* 정합 + Phase A/A-2 SSOT 자연 확장. byte-equivalent 패턴 (기존 컴포넌트 0줄 수정) — 회귀 위험 격리. 흡수/임베드 점진 마이그레이션은 후속 PR 분리. |
| 2026-04-29 | PR-Z10 — UI Phase C: VerdictCard (V-E-R) + Stop-loss First + Time-band (ADR-0097, 사용자 #6 + #7 + #8 통합): 사용자 12 아이디어 분석 #6 (V-E-R Card Migration Gate, S/C 5.0) + #7 (Stop-loss First 디자인) + #8 (Time-band 띠) 단일 PR 통합 — 종목 카드의 *결정 가능* 형태 신설. 세 항목 모두 같은 카드 컴포넌트 세트라 분리하면 import/렌더 흐름 흩어짐. ① **`src/components/common/VerdictCard.tsx` 신설** (compound component) — Verdict-Evidence-Risk 3 슬롯. **Migration Gate (#6)**: `variant='default'` (기본) → 기존 카드 동일 작동, `variant='verdict'` 명시 시에만 V-E-R 모드. 3 슬롯 children 모두 부재 시 자동 placeholder 다운그레이드 → **회귀 위험 0**. 신규 카드만 점진 도입, 50+ 기존 WatchlistCard 무수정. **VerdictCard.Verdict slot**: 5 OverallVerdict 라벨 (STRONG_BUY/BUY/HOLD/CAUTION/AVOID) + regime (R1~R6) — `useUILang().card / .regime` SSOT 사용. expired prop → "재검증 대기" 자동 라벨 전환. **VerdictCard.Evidence slot**: children 자유도 (Phase D ConfluenceMeter 사전 등록). children 부재 → "근거 미수집" placeholder. **VerdictCard.Risk slot — Stop-loss First (#7)**: 폰트 위계 — 손절가 `text-2xl font-black` (가장 큰, 감내 손실 우선 인식) > invalidation `text-sm` (두 번째) > targetPrice/scenarios `text-xs opacity-70` (가장 작은, 기대 인플레이션 차단). `formatKrw()` 헬퍼 (Intl.NumberFormat ko-KR + NaN/Infinity → "—" fallback). 손절 % 산출은 entryPrice 있을 때만. 행동경제학 loss framing 을 시스템 가치 (손절 규율) 에 정확히 정렬. ② **`src/components/common/TimeBand.tsx` 신설** — 1-2px 두께 progress bar. **`computeRemainingPct` 순수 함수 SSOT** — 진행률 ∈ [0, 1] clamp + createdAt≥expiresAt / NaN 등 안전 가드. **`classifyTimeBandGrade` 4 분기 SSOT** — FRESH (>0.5) / AGING (>0.2) / STALE (>0) / EXPIRED (=0). 색상 emerald/amber/red/zinc. ARIA `role="progressbar"` + aria-valuenow/min/max. `onExpire` 콜백 1회 호출 (parent verdict 전환 트리거). `tickIntervalMs` 기본 1분 + `nowProvider` 옵셔널 (테스트 시각 주입). ③ **VerdictCard + TimeBand 통합** — `createdAt` + `expiresAt` 모두 있을 때만 TimeBand 렌더, 만료 도달 시 `data-vcard-expired="true"` 마커. ADR-0019 RecommendationSnapshot lifecycle 결합 — 백엔드 SSOT 가 UI 띠에 그대로 반영. ④ **회귀 테스트 신규 52 케이스** — `TimeBand.test.tsx` 22 (computeRemainingPct 9 + classifyTimeBandGrade 8 + 컴포넌트 9 grade/onExpire/ARIA/height/className) + `VerdictCard.test.tsx` 30 (Migration Gate 5 + Verdict slot 6 + Risk Stop-loss First 9 + Evidence 2 + TimeBand 통합 4 + 통합 4). ⑤ **검증** — vitest 신규 52 + 인접 무회귀 + lint(client + server tsc) + validate:all 11종 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — UI 컴포넌트만, 백엔드 0건). **LIVE 매매 본체 0줄 변경** + 기존 카드 (WatchlistCard / GateStatusCard) 무수정. ⑥ **운영 효과** — *Stop-loss First 규율 형성*: 사용자가 종목 카드를 볼 때 무의식적으로 손절가 (감내 손실) 우선 인식 → 욕망 키우는 카드 디자인 영구 차단 + *신뢰 유효시간 노출*: TimeBand 띠로 verdict 신뢰도가 *시간이 지나면 떨어진다* 는 인지 형성 → 오래된 추천 맹신 차단 + Migration Gate 회귀 격리 + Phase D 진입 준비 (Evidence slot ConfluenceMeter 사전 등록). ⑦ **잔여 (후속 PR)** — PR-Phase-C-2: WatchlistCard 마이그레이션 (각 컴포넌트 PR 별도) / PR-Phase-C-3: DiscoverWatchlistPage Top 3 시범 임베드 / PR-Phase-C-4: RecommendationSnapshot.createdAt/expiresAt wiring (ADR-0019 결합) / PR-Phase-D: ConfluenceMeter 4축 (#9, #10) — Evidence slot 안에 자식으로 박힘 (단방향 결합) / PR-Verbose: uiVerbosity 토글 (#12). **4중 Stack PR** — base = `claude/ui-phase-b-idontknow-ribbon-TVTMY` (PR #422). PR #420 (Phase A) → PR #421 (Phase A-2) → PR #422 (Phase B) → 본 PR (Phase C) 자연 stack. | `docs/adr/0097-verdict-card-and-time-band.md` (신규), `src/components/common/TimeBand.tsx` (신규 + computeRemainingPct + classifyTimeBandGrade 순수 함수 export), `src/components/common/TimeBand.test.tsx` (신규 22 케이스), `src/components/common/VerdictCard.tsx` (신규 V-E-R compound + VerdictSlot/EvidenceSlot/RiskSlot), `src/components/common/VerdictCard.test.tsx` (신규 30 케이스), `ARCHITECTURE.md` (+3 boundary rule) | 사용자 12 아이디어 #6 + #7 + #8 통합 구현. 종목 카드의 *결정 가능* 형태 + Stop-loss First 행동경제학 적용 + RecommendationSnapshot lifecycle 결합. 사용자 분석 ExecutionWindow 카운트다운 별도 위젯 아이디어를 1픽셀 띠 (TimeBand) 로 축소 → V-E-R Card 자연 부분 흡수. byte-equivalent 패턴 (기존 컴포넌트 0줄 수정) — 회귀 위험 격리. WatchlistCard 마이그레이션 + snapshot wiring 은 후속 PR 점진 진행. |
| 2026-04-29 | PR-Z11 — UI Phase D: ConfluenceMeter 4축 + 축 결손 사유 + 단방향 결합 (ADR-0098, 사용자 #9 + #10 통합): 사용자 12 아이디어 분석 #9 (축 결손 자동 표기, S/C 3.0) + #10 (단방향 결합, S/C 4.5) 단일 PR — 두 항목 모두 ConfluenceMeter 컴포넌트의 핵심 디자인. Blueprint 원칙 6번 *"모름을 표현할 수 있어야 한다"* 의 가장 정교한 구현. ① **`src/components/common/ConfluenceMeter.tsx` 신설** — Fundamental/Flow/Technical/Macro 4축 합치도 시각화. `ConfluenceAxisId` 4값 union + `ConfluenceAxis` 타입 (id/score/reason?). 한국어 라벨은 컴포넌트 내부 `AXIS_LABEL` 상수 (UI_LANG.confluence SSOT 격상은 후속 PR). ② **`classifyAxisScore(score)` 3 분기 SSOT** — STRONG (≥7) emerald / NEUTRAL (5~7) amber / WEAK (<5) red. NaN/Infinity 안전 fallback → WEAK 보수적. ③ **#9 축 결손 자동 표기** — `shouldShowReason(score)` SSOT: score < 5 시 reason 1줄 자동 노출 (STRONG/NEUTRAL 미노출 — 사용자 시각 부담 ↓). reason 부재 시 "데이터 미수집" placeholder. `data-confluence-axis-reason` 속성 (e2e — 결손 사유 영역만 정확 카운트). 사용자 인지: "기다림 가능" (FLOW — 다음 주 외인 매수 가능) vs "기다림 무의미" (MACRO — 시간 외 변수) 즉시 구분 → *기다림 비용* 정확 계산. ④ **`classifyConfluence(axes)` 4 분기 종합 등급 SSOT** — CONVICTION (STRONG ≥3 — 🔥 강한 합치) / CONFIDENT (2 — ✅ 확신) / MIXED (1 — 🟡 혼재) / DIVERGENT (0 — ⚠️ 신호 충돌). ⑤ **#10 단방향 결합** — ConfluenceMeter 는 VerdictCard 를 import 하지 않음 (외부 의존성 0, props 만). `<VerdictCard.Evidence>` slot 안 자식으로 박힘 (Evidence → ConfluenceMeter 단방향). 어디서든 단독 사용 가능 (Screener 헤더 KPI / Backtest 결과 카드 / DiscoverPage Top). 회귀 테스트가 *VerdictCard 외부 단독 렌더 시 vcard 마커 (data-vcard-*) 부재* 자동 검증 — 단방향 보장. 향후 다른 페이지 재사용 시 복제 0건. ⑥ **compact vs full 모드** — compact: 4 막대만 가로 배열 (라벨/사유 미노출, title hover) — Screener 헤더 KPI 좁은 공간용. full: 4 라인 + progressbar + 결손 사유 — VerdictCard.Evidence 안 메인 사용. ⑦ **ARIA 접근성** — full: `role="region"` + aria-label 종합 등급 / 각 축 `role="progressbar"` + aria-valuenow 점수 비율 / compact: `role="img"`. ⑧ **회귀 테스트 신규 36 케이스** (`ConfluenceMeter.test.tsx`) — classifyAxisScore 8 (경계값 + NaN/Infinity) + shouldShowReason 3 (≥5 false / <5 true / NaN true 보수적) + classifyConfluence 5 (4 분기) + 4 축 라벨/점수/속성 4 + #9 결손 사유 표기 4 (WEAK 만 / STRONG/NEUTRAL 미표시 / placeholder / 정확 카운트) + 종합 등급 라벨 3 (CONFIDENT/CONVICTION/DIVERGENT) + compact 모드 2 + 안전 가드 2 + ARIA 2 + #10 단방향 결합 2 (VerdictCard.Evidence 안 자식 / VerdictCard 외부 단독) + 페르소나 정합 1. ⑨ **검증** — vitest 신규 36/36 + 인접 무회귀 + lint(client + server tsc) + validate:all 11종 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — UI 컴포넌트만, 외부 의존성 0). **LIVE 매매 본체 0줄 변경** + 기존 카드/컴포넌트 무수정. ⑩ **운영 효과** — 시스템 한계 자발적 노출 (약한 축의 결손 사유로 사용자가 *시스템이 모르는 것* 즉시 인지) + 기다림 가능 vs 불가능 구분 (대기 비용 정확 계산) + 단방향 부품 패턴 (어디든 박힐 수 있는 작은 부품 — VerdictCard / Screener / Backtest 재사용) + 4 축 합치도 한눈 인지 (CONVICTION/CONFIDENT/MIXED/DIVERGENT 매수 결정 신뢰도 즉시 판단). ⑪ **잔여 (후속 PR)** — PR-Phase-D-2: VerdictCard.Evidence 안 ConfluenceMeter 시범 임베드 (DiscoverWatchlistPage Top 3) / PR-Phase-D-3: 백엔드 4 축 score 산출 wiring (`confluenceEngine.ts` 신설) / PR-Phase-D-4: 결손 사유 자동 생성 (백엔드 데이터 출처 + 결손 패턴 매핑) / PR-Phase-A-3: UI_LANG.confluence 카테고리 추가 (4 축 라벨 SSOT 격상) / PR-Verbose: uiVerbosity 토글 (#12 — 잔여 1 항목, minimal/balanced/verbose 분기). **5중 Stack PR** — base = `claude/ui-phase-c-verdict-card-TVTMY` (PR #423). PR #420 (Phase A) → PR #421 (Phase A-2) → PR #422 (Phase B) → PR #423 (Phase C) → 본 PR (Phase D) 자연 stack. **12 아이디어 누적 진행도 — 11/12 완료 (#1, #3, #4, #5, #6, #7, #8, #9, #10, #11) + 본 PR 후 #12 Verbosity 토글 1 항목만 잔여**. | `docs/adr/0098-confluence-meter-and-axis-reason.md` (신규), `src/components/common/ConfluenceMeter.tsx` (신규 + classifyAxisScore + classifyConfluence + shouldShowReason 순수 함수 export), `src/components/common/ConfluenceMeter.test.tsx` (신규 36 케이스 — VerdictCard 통합 + 단독 사용 양 케이스 검증), `ARCHITECTURE.md` (+2 boundary rule) | 사용자 12 아이디어 #9 + #10 통합 구현. ConfluenceMeter 가 *어디든 박힐 수 있는 작은 부품* — VerdictCard.Evidence 안 자식 + 단독 사용. 단방향 결합으로 다른 페이지 재사용 시 복제 0건. 축 결손 자동 표기로 시스템이 자기 한계 자발적 노출 — 페르소나 *"모름을 표현"* + *"기다림 비용 계산"* 강화. byte-equivalent 패턴 (외부 의존성 0, props 만) — 회귀 위험 격리. |
| 2026-04-29 | PR-Z12 — UI Verbosity 토글 (ADR-0099, 사용자 #12 — 12 아이디어 마지막 / 100% 완주): 사용자 12 아이디어 분석 마지막 항목 — *"V-E-R Card / ConfluenceMeter / DataQuality 정보가 다 들어가면 사용자에 따라 정보 과잉. minimal 은 Verdict 만, balanced 는 V-E-R, verbose 는 ConfluenceMeter+축 사유까지. 사용자가 자기 인지 부하를 직접 제어."* Phase A~D 5 PR 누적 다수 컴포넌트의 *정보 과잉 안전장치* + 점진 도입 철학의 사용자 측 변형 (Phase A KO/EN 양립과 같은 모델). ① **`useSettingsStore.uiVerbosity` 추가** (zustand persist) — `UIVerbosity = 'minimal' | 'balanced' | 'verbose'` 타입 + setter + partialize 영속. 기본값 'balanced' (V-E-R 3 슬롯 + DataQuality + IDontKnow). ② **`src/hooks/useUIVerbosity.ts` 신설** — 게이트키퍼 hook + 가시성 분기 SSOT 매트릭스. `shouldShowAtVerbosity(content, verbosity)` 순수 함수 SSOT — 11 컨텐츠 키 (verdict/gate-mini/idontknow/evidence/risk/time-band/data-quality/confluence/axis-reasons/data-quality-ribbon/gate-status) × 3 verbosity 매트릭스. minimal → Verdict + 미니 게이트만 (신규 사용자) / balanced → V-E-R + DataQuality + IDontKnow (기본) / verbose → 모든 컨텐츠 (시스템 운영자). `atLeastVerbosity` 헬퍼 (atLeast('verbose') 가독성 체크). useMemo 메모이즈 — rerender 시 동일 reference. ③ **`src/components/common/UIVerbosityToggle.tsx` 신설** — 3-way segmented control (간결/균형/상세 한국어 라벨 + EyeOff/Eye/Layers 아이콘). compact 모드 (라벨 숨김, 아이콘만 — 페이지 헤더용). data-ui-verbosity-option / data-ui-verbosity-active 속성 (e2e). ARIA radiogroup + radio + aria-checked + aria-label 옵션 설명. ④ **회귀 테스트 신규 41 케이스** — `useUIVerbosity.test.ts` 32 (shouldShowAtVerbosity 매트릭스 22: 11 컨텐츠 × minimal/balanced + verbose 1줄 / atLeastVerbosity 9: 3×3 ranking / hook contract 7: setVerbosity 즉시 반영 + store SSOT + 메모이즈 + minimal/verbose 분기) + `UIVerbosityToggle.test.tsx` 9 (3 옵션 + 활성 강조 + 클릭 store 갱신 + compact 모드 + ARIA + className). ⑤ **검증** — vitest 신규 41 + 인접 무회귀 + lint(client + server tsc) + validate:all 11종 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (절대 규칙 #2/#3/#4 — store + hook + UI 만, 백엔드 0건). **LIVE 매매 본체 0줄 변경** + 기존 컴포넌트 무수정 (wiring 부재 — 후속 PR). ⑥ **운영 효과** — 사용자 인지 부하 직접 제어 (minimal/balanced/verbose 3 단계로 신규 사용자 → 시스템 운영자 spectrum 모두 수용) + Phase A~D 누적 다수 컴포넌트 *정보 과잉* 안전장치 + wiring 후속 PR 의 SSOT 기반 (각 컴포넌트가 useUIVerbosity().shouldShow(키) 한 줄로 가시성 분기 → 회귀 위험 격리 + 매트릭스 변경 시 SSOT 한 곳만 수정). ⑦ **잔여 (후속 PR)** — PR-Verbose-Wiring-1: VerdictCard 자기 가시성 분기 / PR-Verbose-Wiring-2: ConfluenceMeter atLeast('verbose') 분기 / PR-Verbose-Wiring-3: IDontKnow / DataQualityBadge / Ribbon / GateStatusCard / GateMiniIndicator 가시성 분기 (각 별도 PR — 회귀 위험 격리) / PR-Verbose-Embed: UIVerbosityToggle Settings 모달 / 페이지 헤더 임베드. **6중 Stack PR** — base = `claude/ui-phase-d-confluence-meter-TVTMY` (PR #424). PR #420 → #421 → #422 → #423 → #424 → 본 PR (Verbose). ⑧ **사용자 12 아이디어 시리즈 100% 완주** — 본 PR 이 마지막. ✅ #1 useUILang / ✅ #3 5-tier / ✅ #4 IDontKnow / ✅ #5 Ribbon / ✅ #6 V-E-R Migration Gate / ✅ #7 Stop-loss First / ✅ #8 Time-band / ✅ #9 축 결손 사유 / ✅ #10 단방향 결합 / ✅ #11 Phase A DoD / ✅ #12 Verbosity 토글. 잔여 1 항목 (#2 --discover 모드) 만 명시적 후속 PR 대기 (운영 1~2주 누적 후 우선순위 결정). 누적 6 PR / 42 파일 / 236 신규 테스트 / lint 0 / validate:all 11종 / precommit EXIT=0 / KIS/KRX quota 0 / LIVE 매매 본체 0줄 변경. | `docs/adr/0099-ui-verbosity-toggle.md` (신규), `src/stores/useSettingsStore.ts` (UIVerbosity 타입 + uiVerbosity 필드 + setter + partialize), `src/hooks/useUIVerbosity.ts` (신규 + shouldShowAtVerbosity + atLeastVerbosity 순수 함수 export), `src/hooks/useUIVerbosity.test.ts` (신규 32 케이스), `src/components/common/UIVerbosityToggle.tsx` (신규 3-way segmented control), `src/components/common/UIVerbosityToggle.test.tsx` (신규 9 케이스), `ARCHITECTURE.md` (+1 boundary rule) | 사용자 12 아이디어 시리즈 마지막 #12 완주. Phase A~D 5 PR 누적 다수 컴포넌트의 정보 과잉 안전장치 + 사용자 측 인지 부하 직접 제어. wiring 후속 PR 점진 마이그레이션 (각 컴포넌트별 별도 PR — 회귀 위험 격리). 12/12 100% 완주. |
| 2026-04-29 | PR-Z13 — UIVerbosityToggle Settings 모달 임베드 (ADR-0100, PR-Verbose-Embed): PR #425 (ADR-0099) 가 UIVerbosityToggle 컴포넌트만 도입하고 사용자 접근 경로 부재였던 갭 해소. SettingsModal.tsx 의 ModalBody 에 신규 "정보 밀도" 섹션 추가 — API Key + Theme 다음 3번째 섹션. ① **`SettingsModal.tsx`** (94 → 109 줄) — Layers 아이콘 + UIVerbosityToggle import + "정보 밀도" 라벨 + `<UIVerbosityToggle className="w-full justify-between" />` (배너 모드, 풀 라벨) + 11px 설명 텍스트 ("간결: 핵심 결정만 / 균형: V-E-R 3 슬롯 / 상세: 합치도·결손 사유·신뢰 띠 (운영자)"). ② **`SettingsModal.embed.test.tsx` 신규 8 케이스** — 모달 렌더 시 토글 임베드 노출 / 미렌더 시 토글 미노출 / 클릭 시 store 갱신 / Migration Gate (기존 섹션 무영향) / 라벨+설명 텍스트 노출 / balanced 기본값 active / minimal↔balanced 순환 / "설정 저장" 버튼 영향 0. ③ **검증** — vitest 신규 8/8 + lint(client + server tsc) 0 에러 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (UI 임베드만). **LIVE 매매 본체 0줄 변경** + 기존 SettingsModal 섹션 무수정 (Migration Gate 회귀 테스트 자동 검증). ④ **운영 효과** — 사용자가 Settings 모달 1회 클릭으로 verbosity 변경 가능 (이전엔 localStorage 직접 수정만 유일). default 'balanced' 보존 — 기존 사용자 무영향. wiring 후속 PR (VerdictCard / ConfluenceMeter / DataQualityBadge / IDontKnow / Ribbon / GateStatusCard / GateMiniIndicator) 효과 가시화 준비 완료. ⑤ **잔여 (후속 PR)** — PR-Verbose-Wiring-1: VerdictCard 자기 가시성 분기 / PR-Verbose-Wiring-2: ConfluenceMeter atLeast('verbose') 분기 / PR-Verbose-Wiring-3: 5 컴포넌트 가시성 분기 (IDontKnow / DataQualityBadge / Ribbon / GateStatusCard / GateMiniIndicator) — 각 별도 PR, 회귀 위험 격리 / PR-Verbose-Header-Embed: MarketOverviewHeader 페이지 상단 compact 토글 임베드 (선택). PR #425 머지 후 main 위 단일 PR (stack 부재). | `docs/adr/0100-ui-verbosity-toggle-embed.md` (신규), `src/components/common/SettingsModal.tsx` (Layers 아이콘 + UIVerbosityToggle import + "정보 밀도" 섹션 1개 추가, 본체 무수정), `src/components/common/SettingsModal.embed.test.tsx` (신규 8 케이스) | ADR-0099 (Verbosity SSOT 도입) → ADR-0100 (사용자 노출) 자연 의존. byte-equivalent 패턴 (기존 섹션 무수정) — 회귀 위험 격리. 본 PR 머지 후 wiring PR 시리즈 시작 가능. |
| 2026-04-29 | PR-Z14 — VerdictCard verbosity wiring (ADR-0101, PR-Verbose-Wiring-1): PR #420~426 7개 머지 후 첫 wiring PR — VerdictCard 의 EvidenceSlot / RiskSlot / TimeBand 가 useUIVerbosity().shouldShow(...) 분기 적용. 토글 변경 즉시 시각 변화. ① **EvidenceSlot wiring** — useUIVerbosity import + shouldShow('evidence') 분기 + forceShow 옵셔널 prop override (특수 케이스). minimal 시 null 반환 → DOM 부재. ② **RiskSlot wiring** — 동일 패턴 (shouldShow('risk') + forceShow override). ③ **TimeBand 분기** — VerdictCard root 에서 기존 (createdAt && expiresAt) AND shouldShow('time-band') 추가. ④ **VerdictSlot 무수정** — 모든 verbosity 에서 항상 렌더 (사용자 결정 핵심). ⑤ **variant='default' 무수정** — 50+ 기존 카드 무영향, Migration Gate 정합 보장. ⑥ **forceShow override 패턴** — 명시 시 useUIVerbosity 무시 (테스트/강제 노출/디버깅 안전망). 후속 wiring PR 들의 표준 패턴. ⑦ **회귀 테스트 9 케이스 신규** (`VerdictCard.verbosity.test.tsx`) — balanced V-E-R 모두 렌더 / minimal Verdict slot 만 + Evidence/Risk/TimeBand 미렌더 / minimal Verdict 라벨 표시 / verbose balanced 와 동일 / variant='default' minimal verbosity 무영향 / variant 부재 + balanced 기존 동작 / forceShow=true minimal 강제 / forceShow=false balanced 강제 / Slot 단독 사용 분기 작동. 기존 30 케이스 모두 default 'balanced' 라 무영향 — 39/39 pass. ⑧ **검증** — vitest 신규 9 + 기존 30 = 39/39 pass + lint(client + server tsc) 0 에러 + ALLOW_DEPLOY_WINDOW=1 precommit EXIT=0. **KIS/KRX 자동매매 quota 0 침범** (UI 분기만). **LIVE 매매 본체 0줄 변경**. ⑨ **운영 효과** — 사용자가 Settings → 정보 밀도 → 간결 클릭 시 모든 VerdictCard (variant='verdict') 가 Verdict slot 만 표시. balanced/verbose 변경 시 즉시 시각 변화. variant='default' 무영향. ⑩ **잔여 (후속 PR)** — PR-Verbose-Wiring-2: ConfluenceMeter atLeast('verbose') 분기 / PR-Verbose-Wiring-3: IDontKnow / DataQualityBadge / DataQualityRibbon / GateStatusCard / GateMiniIndicator (각 별도 PR) / PR-Phase-C-2: VerdictCard WatchlistCard 안 시범 임베드 / PR-Phase-C-4: RecommendationSnapshot.createdAt/expiresAt wiring. PR #426 머지 후 main 위 단일 PR. | `docs/adr/0101-verdict-card-verbosity-wiring.md` (신규), `src/components/common/VerdictCard.tsx` (useUIVerbosity import + 3 위치 분기 + EvidenceSlot/RiskSlot 시그니처 forceShow + return type ReactElement → ReactElement \| null), `src/components/common/VerdictCard.verbosity.test.tsx` (신규 9 케이스) | ADR-0099 (SSOT) → ADR-0100 (UX 노출) → ADR-0101 (첫 wiring) 자연 의존. byte-equivalent 패턴 (variant='default' 무수정 + 30 기존 케이스 무영향) — Migration Gate 정합. forceShow override 패턴이 후속 wiring PR 들의 표준. |
