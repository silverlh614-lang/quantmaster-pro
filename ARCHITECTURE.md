# ARCHITECTURE.md

This document defines module boundaries to guide AI-assisted refactoring and serve as a code-review checklist.
When modifying any file, ensure changes stay within the owning module's stated responsibility.

---

## Module Boundaries

> **Path note**: `src/` contains frontend and shared source; `server/` (root-level) contains the standalone Express server (routes, clients).

| Module | Single Responsibility (<= 25 words) |
|--------|--------------------------------------|
| `src/services/quantEngine.ts` (gateEngine) | Evaluate Gate 0/1/2/3 conditions and compute final stock score |
| `src/services/quantEngine.ts` (bearEngine) | Execute bear-market strategies: seasonality, screener, Kelly, simulator |
| `src/services/stockService.ts` | Fetch and aggregate stock data from Yahoo Finance, DART, KIS, and Gemini |
| `src/services/autoTrading.ts` | Handle client-side manual-trigger trading: shadow trades, slippage, Kelly orders |
| `src/services/ecosService.ts` | Query Bank of Korea ECOS API for macro indicators: rate, FX, M2, GDP, trade |
| `src/utils/indicators.ts` | Compute technical indicators: RSI, MACD, Bollinger Bands, Ichimoku, VCP |
| `server/trading/signalScanner.ts` | Run 24/7 server-side automated signal scanning and trade execution via KIS order API |
| `server/quantFilter.ts` | Evaluate 8 server-side Gate conditions from Yahoo Finance data with adaptive weights |
| `server/clients/kisClient.ts` | Proxy all KIS API calls with token management and VTS/real switching |
| `server/clients/krxClient.ts` | KRX 공개·인증 OpenAPI 단일 통로 barrel (ADR-0502c Phase 1 분해, 후속 Phase 2/3 진행 중) |
| `server/clients/krxClient/types.ts` | 8 외부 노출 + 7 내부 공용 타입 SSOT (ADR-0502c) |
| `server/clients/krxClient/constants.ts` | URL/path/BLD/timeout/cache TTL 상수 + KIS 모드 분기 헬퍼 SSOT (ADR-0502c) |
| `server/clients/krxClient/cache.ts` | 10분 TTL 캐시 + investor-trading diagnostic Map SSOT (ADR-0502c) |
| `server/clients/krxClient/cooldown.ts` | ADR-0009 bld soft cooldown + ADR-0259 recovery probe SSOT (ADR-0502c) |
| `server/clients/krxClient/timeWindow.ts` | ADR-0256 시간대 게이팅 (PRE_DAWN/LUNCH_BREAK/POST_CLOSE_PRE_PUBLISH) SSOT (ADR-0502c) |
| `server/clients/krxClient/dateUtils.ts` | KST 날짜 산술 + 영업일 후퇴 + resolveTradeDate SSOT (ADR-0502c) |
| `server/clients/krxClient/csv.ts` | KRX CSV 파서 SSOT — decodeKrxCsv + parseKrxCsv 순수 함수 (ADR-0502c Phase 2) |
| `server/clients/krxClient/http.ts` | KRX HTTP layer SSOT — krxPost + krxGet + payload sanitize + meta state (ADR-0502c Phase 2) |
| `server/clients/krxClient/otpCsv.ts` | KRX OTP-CSV 다운로드 SSOT — krxInvestorOtpCsv (ADR-0502c Phase 2) |
| `server/routes/kisRouter.ts` | Expose REST endpoints that delegate KIS supply, short-selling, and order calls |
| `server/routes/autoTradeRouter.ts` | Expose REST endpoints for auto-trade control, watchlist management, and macro state |
| `server/trading/preMarketGapProbe.ts` | Compute pre-market gap from KIS previous close; classify as proceed/warn/skip by threshold |
| `server/orchestrator/tradingOrchestrator.ts` | Coordinate pre-market auction order prep via watchlist, gap probe, and gate re-evaluation; trigger throttled intraday screener refresh on INTRADAY tick (ADR-0628, flag-gated) |
| `server/services/aiUniverseService.ts` | Single channel for AI-recommendation universe discovery and enrichment via Google Search + Naver Finance (no KIS/KRX) |
| `server/clients/googleSearchClient.ts` | Proxy Google Custom Search JSON API with whitelist and daily budget guard |
| `server/clients/naverFinanceClient.ts` | Fetch Naver mobile finance snapshots for AI-recommendation enrichment |
| `server/persistence/krxStockMasterRepo.ts` | Persist KRX stock master (24h TTL) shared by AI recommendation and auto-trading |
| `server/services/multiSourceStockMaster.ts` | Orchestrate 4-tier fallback (KRX → Naver → Shadow → Seed) + validation + health updates (ADR-0013) |
| `server/persistence/stockMasterHealthRepo.ts` | Persist per-source health score (0-100) and rolling success/failure stats (ADR-0013) |
| `server/persistence/shadowMasterDb.ts` | Persist last-known-good stock master snapshot — only validated payloads (ADR-0013) |
| `server/clients/naverStockListClient.ts` | Fetch Naver mobile market-cap leaders as Tier 2 fallback (ADR-0013) |
| `server/data/stockMasterSeed.ts` | Hard-coded KOSPI/KOSDAQ leader seed — Tier 4 ultimate fallback (ADR-0013) |
| `server/health/diagnostics.ts` | Collect 8-axis system health snapshot — shared by /health Telegram cmd and /api/health/pipeline HTTP route |
| `server/learning/unifiedForwardOutcomeLabeler.ts` | Normalize Gate3/Gate1/Near-Miss/counterfactual/paper rows and update due forward-return evidence only |
| `server/telegram/renderers/canonicalDebugRawView.ts` | Project persisted ScanSummary canonical slices into debug_raw view with scope tags — zero recomputation (ADR-0525) |
| `server/trading/signalScanner/scanDiagnostics/candidateGateEvaluationView.ts` | Per-candidate Gate0/1/2/3 evaluation SSOT — formatters read this view, never re-infer from raw traces (ADR-0526) |
| `server/trading/signalScanner/scanDiagnostics/intradayMoverCandidateSource.ts` | Map fresh screener.json today-top movers into the buildCandidatePool `intradayMovers` slot as INTRADAY_MOVER source — getScreenerCache() cache read only (zero new KIS fetch, ADR-0561), changeRate-desc top-N (INTRADAY_MOVER_MAX_INJECT), flag INTRADAY_MOVER_CANDIDATE_SOURCE_ENABLED default OFF returns [] (candidate pool byte-identical), bypasses intradayReady 15-min gate via direct pool injection — universe freshness only, Gate0~3/requiredScore=70 scoring untouched (ADR-0629) |
| `server/trading/signalScanner/promotionReadinessAdr0631.ts` | Read-only Shadow→Live promotion readiness diagnostic — pure function fusing existing forward-outcome evidence (Gate1ThresholdEvidenceSummary reviewReady/reviewBlockers + regimeAwareWindow.verdict) and CounterfactualOutcomeBoard into per-lever READY/NOT_READY verdicts (Lever A regime-aware threshold ADR-0546, Lever B condition-weight ADR-0624). Reuses existing reviewBlockers/verdict SSOT (invents no new threshold), zero new fetch, calls no register/unregister/ENV-write, executionImpact=NONE (ADR-0631) |
| `server/trading/signalScanner/consecutiveNetBuyLedgerAdr0614.ts` | Observation-only consecutive/cumulative investor net-buy time-series ledger — piggybacks already-fetched c.kisFlow (zero new fetch), per-stock rolling window, default OFF, NOT wired to Gate score (ADR-0614) |
| `server/screener/universeCompositionBiasObservationAdr0616.ts` | Observation-only per-scan universe composition bias aggregate — KOSPI/KOSDAQ share + leader/laggard RS distribution (reuses quote.return20d − kospi20dReturn, no 2nd RS formula) + market-cap tier via getStockByCode listedShares×price (null if missing, no fake cap). Zero new fetch, default OFF, NOT wired to universe discovery/Stage1 filter/sort/Gate score (ADR-0616) |
| `server/screener/leaderUniverseInjectionAdr0617.ts` | Leader-stock Stage1 preservation — carry expandedUniverse source tags (FOREIGN_NET_BUY/INST_NET_BUY/MARKET_CAP, reuses DynamicStock['source'], no 2nd enum) into CandidateStock + force-preserve leaders past the small-cap-biased top-N score cut via union (quality gates still enforced — score-rank cut only). Zero new fetch (reuses getExpandedUniverse cron data), flag LEADER_UNIVERSE_INJECTION_ENABLED default OFF byte-identical, per-scan observation ledger (ADR-0617) |
| `server/screener/universeDiscoveryAggressivenessAdr0622.ts` | Universe discovery aggressiveness levers — Stage1 top-N cut expansion (60→90, flag UNIVERSE_STAGE1_TOPN_EXPANSION_ENABLED) + RS percentile priority sort with laggard demotion (flag UNIVERSE_RS_PERCENTILE_RANK_ENABLED, reuses ADR-0616 RS = quote.return20d − kospi20dReturn + UNIVERSE_RS_LEADER_THRESHOLD, no 2nd RS formula) + flag-agnostic dry-run observation ledger. Zero new fetch, both flags default OFF byte-identical, discovery (Stage1) layer only — orthogonal to ADR-0617 leader union and Gate2 ADR-0621 (ADR-0622) |
| `server/trading/gates/unifiedExecutionContract.ts` | Unified execution-permission contract pins + PositionPolicyDecision SSOT — permission separated from count (ADR-0527) |
| `server/trading/symbolDataCollector.ts` | SourceSnapshot factory SSOT — orchestrate existing single-channel fetches into UnifiedSourceSnapshot, compute per-field freshness, no new fetch (ADR-0519/0556) |
| `server/trading/sourceSnapshot/unifiedSourceSnapshot.ts` | UnifiedSourceSnapshot container type + snapshotId — factory output SSOT, the only SourceSnapshot artifact (ADR-0519/0556) |
| `src/services/autoTrading/ssotPipeline.ts` | Gate/policy projection from snapshot perSymbol — pure projection layer, NOT a factory, never fetches providers (ADR-0556) |
| `src/components/market/MarketModeBanner.tsx` | Always-rendered top banner — MHS + Regime + VKOSPI + USD/KRW + allowed/forbidden trading policy (ADR-0018) |
| `src/components/common/DataQualityBadge.tsx` | Display computed/api/aiInferred count badge for stock card — heuristic fallback when server sourceTier metadata absent (ADR-0018) |
| `src/components/watchlist/GateStatusCard.tsx` | Compact read-only Gate 0/1/2/3 pass summary embedded in WatchlistCard (ADR-0018) |
| `src/types/ui.ts` | UI redesign P0-A shared types + REGIME_TRADING_POLICY SSOT for 6 regime levels (ADR-0018) |
| `src/utils/dataQualityClassifier.ts` | Compute DataQualityCount from StockRecommendation — heuristic fallback grouping 27 conditions into computed/api/aiInferred (ADR-0018) |
| `src/utils/regimeMapping.ts` | Map gate0Result.tradeRegime + bearRegimeResult into 6-level RegimeLevel for MarketModeBanner (ADR-0018) |
| `server/trading/exitEngine/index.ts` | Orchestrate exit evaluation — `_exitRunning` mutex + per-shadow loop + EXIT_RULES_IN_ORDER priority (ADR-0028) |
| `server/trading/exitEngine/types.ts` | ExitContext / ExitRuleResult / ExitRule signature SSOT (ADR-0028) |
| `server/trading/exitEngine/helpers/reserveSell.ts` | Record sell fill as SHADOW/PROVISIONAL/FAILED — `주문 접수 ≠ 체결` SSOT (ADR-0028) |
| `server/trading/exitEngine/helpers/rollbackFullClose.ts` | Capture and rollback shadow snapshot when full-close order fails (BUG #7, ADR-0028) |
| `server/trading/exitEngine/helpers/attribution.ts` | Emit partial attribution for SHADOW partial sell fills (PR-42 M1, ADR-0028) |
| `server/trading/exitEngine/helpers/rsiSeries.ts` | Wilder RSI series + bearish divergence detector pure functions (ADR-0028) |
| `server/trading/exitEngine/helpers/ma60.ts` | MA20/MA60 reversal judgement + KST business-day arithmetic + 120-day close fetcher (ADR-0028) |
| `server/trading/exitEngine/helpers/priceHistory.ts` | Yahoo symbol candidate generation + price/RSI history fetch helper (ADR-0028) |
| `server/trading/exitEngine/rules/*.ts` | One file per exit rule (16 rules) — byte-equivalent of original `_updateShadowResultsImpl` blocks (ADR-0028) |
| `server/screener/stockScreener.ts` | preScreenStocks (KIS 4-TR) + autoPopulateWatchlist (3-Preset) + getScreenerCache + barrel re-export (ADR-0029) |
| `server/screener/intradayWeakRotation.ts` | 신선 리더 admit 위한 MOMENTUM 약세 멤버 회전(evict) 정책 SSOT — admission-triggered, evict≠학습정지 (ADR-0628 D5) |
| `server/screener/stockUniverse.ts` | KOSPI/KOSDAQ 워치리스트 발굴 시드 종목 마스터 데이터 상수 (ADR-0029) |
| `server/screener/rejectionLog.ts` | 워치리스트 자동 충전 시 탈락 사유 메모리 캐시 SSOT (ADR-0029) |
| `server/screener/watchlistRejectionReport.ts` | 워치리스트 탈락 사유 텔레그램 일괄 리포트 송출 (ADR-0029) |
| `server/screener/adapters/yahooQuoteAdapter.ts` | Yahoo Finance OHLCV+지표 시세 페칭 어댑터 — Yahoo 단일 통로 (ADR-0029) |
| `server/screener/adapters/kisQuoteAdapter.ts` | KIS 시세 + 일봉 캔들 → YahooQuoteExtended 호환 어댑터 (ADR-0029) |
| `server/screener/adapters/krxScreenerAdapter.ts` | KRX 투자자별 매매 폴백 스크리너 어댑터 (ADR-0029) |
| `server/screener/adapters/_indicators.ts` | Yahoo·KIS 어댑터 공용 RSI MACD EMA 지표 순수 계산 헬퍼 (ADR-0029) |
| `server/utils/marketDayClassifier.ts` | KRX 영업일 7분기 분류 SSOT — 자기학습·스케줄러·매매 보수 모드 단일 컨텍스트 (ADR-0043) |
| `server/scheduler/scheduleGuard.ts` | cron 콜백 자동 가드 래퍼 — ScheduleClass 별 영업일/주말 차단 + 메트릭 기록 (ADR-0043) |
| `server/learning/learningDataValidator.ts` | 학습 입력 영업일 검증 — 비영업일 레코드 자동 필터링·거부 진단 헬퍼 (ADR-0043) |
| `server/trading/holidayResumePolicy.ts` | 연휴 복귀 첫 영업일 보수 매매 정책 SSOT — Kelly 축소 + Gate 상향 + 시초 진입 차단 (ADR-0044) |
| `server/trading/holidayResumeAlert.ts` | 연휴 복귀 보수 모드 텔레그램 알림 cron 함수 — 09:05 KST 평일 발송 (ADR-0044) |
| `server/persistence/krxHolidayRepo.ts` | KRX 휴장일 patch 영속 — 운영자 차년도 추가 휴장일 디스크 저장·idempotent (ADR-0045) |
| `server/trading/krxHolidayAudit.ts` | KRX 차년도 휴장일 등록 감사 — 매년 12/1 cron, 미달 시 CRITICAL 텔레그램 (ADR-0045) |
| `src/services/quant/f2wDriftDetector.ts` | F2W 가중치 σ 변화 감시 — drift 감지 시 LIVE 학습 일시정지 SSOT (ADR-0046) |
| `server/alerts/f2wDriftAlert.ts` | 클라이언트 F2W drift POST 진입점 — dispatchAlert(JOURNAL) + sendPrivateAlert 일괄 (ADR-0046) |
| `server/persistence/reflectionImpactRepo.ts` | 13개 reflection 모듈 일자별 meaningful boolean 영속 — 1년 ring buffer (ADR-0047) |
| `server/learning/reflectionImpactPolicy.ts` | reflection 모듈 status (normal/grace/silent/deprecated) 판정 SSOT — 임계 5%/1% (ADR-0047) |
| `server/learning/reflectionImpactRecorder.ts` | nightlyReflection report 로부터 13 모듈 meaningful 추론·일괄 영속 헬퍼 (ADR-0047) |
| `src/services/quant/learningCoverage.ts` | F2W 학습 (조건 × 레짐) 셀 카운트 SSOT — 30건 미만 셀 가중치 보정 차단 (ADR-0048) |
| `server/persistence/shadowTradeRepo.ts` (sector) | ServerShadowTrade 결정적 섹터 라벨 영속 SSOT — getSectorByCode 박제값 + lazy 백필 (ADR-0060) |
| `server/trading/buyPipeline.ts` (sector wiring) | buildBuyTrade 단일 SSOT — 4개 매수 경로 모두 getSectorByCode 자동 채움 (ADR-0060) |
| `src/utils/sectorStockMatch.ts` | UI 섹터 매칭 양방향 fallback — relatedSectors 우선, sector 단일 필드 fallback (ADR-0060) |
| `src/hooks/useAutoTradeContext.ts` | Map ClientMarketMode + KST time → AutoTradeContext 5-bucket SSOT (PRE_MARKET / LIVE_MARKET / POST_MARKET / OVERNIGHT / WEEKEND_HOLIDAY) — ADR-0049 |
| `src/components/autoTrading/AutoTradeContextSection.tsx` | Marker component — declares per-context priority/collapsed metadata as props, children pass through (ADR-0049) |
| `src/components/autoTrading/AutoTradeContextualLayout.tsx` | Stable-sort AutoTradeContextSection children by priorityByContext + render with collapse/hide policy (ADR-0049) |
| `server/health/survival.ts` | SurvivalSnapshot SSOT — 일일손실 + 섹터HHI + Kelly정합도 3 게이지 합성, 외부 호출 0건 (ADR-0050) |
| `server/routes/survivalRouter.ts` | GET /api/account/survival 엔드포인트 — collectSurvivalSnapshot read-only 노출 (ADR-0050) |
| `src/api/survivalClient.ts` | 클라이언트 동기 사본 + fetchAccountSurvival — 절대 규칙 #3 서버↔클라 직접 import 금지 (ADR-0050) |
| `src/components/autoTrading/AccountSurvivalGauge.tsx` | AutoTradePage 최상단 풀폭 위젯 — 3 게이지 카드 + tier 색상 + EMERGENCY 권고 박스 (ADR-0050) |
| `src/utils/invalidationConditions.ts` | 보유 포지션 4 카테고리 (손절가/손실/단계/목표) 무효화 조건 휴리스틱 평가 SSOT (ADR-0051) |
| `src/components/autoTrading/InvalidationMeter.tsx` | PositionLifecyclePanel 카드 인라인 미터 — 4 dot + tier 색상 + expand 4 조건 상세 (ADR-0051) |
| `server/routes/decisionInputsRouter.ts` | GET /api/decision/inputs — emergencyStop + pendingApprovals + macroSignals 합성 read-only (ADR-0052) |
| `src/api/decisionClient.ts` | DecisionInputs 타입 동기 사본 + fetchDecisionInputs (ADR-0052) |
| `src/utils/oneDecisionResolver.ts` | 6 case 우선순위 SSOT + VOID 4 조건 + computeVolatilityZScore 순수 함수 (ADR-0052) |
| `src/components/autoTrading/TodayOneDecisionCard.tsx` | AutoTradePage 최상단 단일 결정 카드 — 6 case + VOID 가운데 배치 (ADR-0052) |
| `src/api/learningClient.ts` | LearningStatusSnapshot 동기 사본 + fetchLearningStatus — /api/learning/status read-only (ADR-0053) |
| `src/components/autoTrading/NightlyReflectionCard.tsx` | Pro 모드 어젯밤 학습 카드 — verdict 이모지 + 카운트 + 편향 Top 3 + 누락 경고 (ADR-0053) |
| `server/learning/conditionSourceMap.ts` | 27 조건 COMPUTED/AI 분류 + classifyTier SSOT — 클라 evolutionEngine 동기 사본 (ADR-0054) |
| `src/api/concordanceClient.ts` | ConcordanceMatrix 동기 사본 + fetchAttributionConcordance (ADR-0054) |
| `src/components/analysis/ConcordanceMatrix.tsx` | RecommendationHistoryPage 5×5 합치도 매트릭스 — diagonal vs off-diagonal 메타 룰 (ADR-0054) |
| `src/utils/gateMiniIndicator.ts` | 4-Gate 미니 인디케이터 평가 SSOT — PR-A buildGateCardSummary 재활용, 4-tier 분류 (ADR-0055) |
| `src/components/watchlist/GateMiniIndicator.tsx` | WatchlistCard 헤더 우상단 4 dot 미니 인디케이터 — 즉시 인지용 (ADR-0055) |
| `server/utils/yahooProbeRetry.ts` | Yahoo 다중 호스트 재시도 + 다중 심볼 probe 헬퍼 SSOT — 미래 확장점 (ADR-0056) |
| `server/trading/fomcDayLiquidation.ts` | FOMC 발표 당일 14:30 KST 평일 보유 포지션 강제 전량 청산 + 사전 경보 3회 단일 진입점 (ADR-0061) |
| `server/trading/fomcCalendar.ts` (FomcDayLiquidationConfig) | FomcDayLiquidationConfig + getDefault + shouldExecuteLiquidationAt 5중 가드 SSOT (ADR-0061) |
| `src/services/stock/marketOverviewIndicators.ts` | Yahoo 가격성 실데이터 prefill SSOT (8 심볼 지수/FX/원자재) — AI hallucination 차단용 overlay 입력 (ADR-0064) |
| `server/persistence/marketIndicatorsSnapshotRepo.ts` | /api/market-indicators 11 필드 last-known-good disk 영속 — atomic write + stale fallback + X-Field-Stale 헤더 SSOT (ADR-0065) |
| `src/services/stock/marketOverviewCache.ts` | marketOverview AI raw 응답 SWR 캐시 SSOT — 60s fresh + 5min stale + 30min hard, inflight coalescing (ADR-0066) |
| `server/trading/macroStaleness.ts` | macroState 신선도 4-tier 분류 SSOT — FRESH/STALE_WARN/STALE_BLOCK/NO_DATA + 진입 차단 정책 (ADR-0068) |
| `src/components/common/MarketDataStaleBadge.tsx` | X-Field-Stale 헤더 → 시각 배지 — staleFields 0건 미렌더, ≥1 amber + 한국어 라벨 (ADR-0069) |
| `server/health/marketDataHealth.ts` | 시장 데이터 통합 품질 점수 SSOT — 5축 가중평균 0-100 + tier (HEALTHY/DEGRADED/CRITICAL) (ADR-0070) |
| `server/trading/crossSourceValidator.ts` | 다중 소스 교차 검증 SSOT — primary vs secondary 격차 임계 분기 6 tier (AGREED/WARN/CRITICAL/PRIMARY_ONLY/SECONDARY_ONLY/NO_DATA) (ADR-0071) |
| `server/persistence/tradeSignalStatusRepo.ts` | TradeSignalStatus 6단계 상태머신 영속 SSOT — AI_CANDIDATE → DATA_VERIFIED → RISK_APPROVED → USER_APPROVED → AUTO_TRADE_READY 또는 BLOCKED, atomic write + FIFO 1000 trim (ADR-0077) |
| `server/telegram/commands/system/signalStatus.cmd.ts` | `/signal_status [N]` 신호 상태 진단 텔레그램 명령 — TradeSignalRecord 최근 N건 (1~30) 표 형식 노출 (ADR-0077) |
| `server/trading/enemyAutoBlock.ts` | Enemy Checklist 자동 차단 정책 SSOT — creditRate ≥12% / individualDominance ≥88% 보수적 default + ENV 오버라이드 + DISABLED 회로, OR 결합 (ADR-0078) |
| `server/utils/yahooRangePolicy.ts` | Yahoo Finance `range` 파라미터 정책 SSOT — `1d/5d/1mo/3mo/6mo/1y` 6값 허용 + `2y/5y/10y/max/ytd` 자동 cap → `1y` + ENV `YAHOO_RANGE_CAP_DISABLED` 우회 (ADR-0082) |
| `server/persistence/walkForwardResultsRepo.ts` | Walk-Forward Framework Rolling window 결과 영속 SSOT — `data/walk-forward-results.json` atomic write + FIFO trim + 손상 JSON fallback. 기존 `WALK_FORWARD_STATE_FILE` (freeze) 와 책임 분리 (ADR-0083) |
| `server/learning/walkForwardFramework.ts` | Walk-Forward Framework — Rolling window IS/OOS 진단 + Regime 분리 + Alpha decay 추적. 기존 `walkForwardValidator.ts` (freeze 정책) 와 별개 진단 전용. Yahoo OHLCV fetch 0건 (영속 records 만 사용 — ADR-0082 정합) (ADR-0083) |
| `server/learning/conditionLifecyclePolicy.ts` | 27조건 lifecycle status SSOT — `attributionRepo` 위 read-only 분류 레이어. 결정 트리 4분기 (`normal`/`grace`/`silent`/`deprecated`) + activatedCount + activationRate + firstSeenAt + ageDays + byRegime. 별도 영속 미생성 (attributionRepo 영속 활용). ENV `CONDITION_LIFECYCLE_DISABLED=true` 우회 (ADR-0084) |
| `server/persistence/shadowWalkForwardResultsRepo.ts` | Shadow Walk-Forward 결과 영속 SSOT — `data/walk-forward-shadow-results.json` atomic write + FIFO trim. `walkForwardResultsRepo.ts` (LIVE) 와 책임 분리. windowId='shadow_*' prefix (ADR-0086) |
| `server/learning/shadowWalkForwardFramework.ts` | Shadow Walk-Forward Framework — Rejection + Twin 영속 데이터 IS/OOS 분할 + Decay 추적. adapter (`rejectionToWindowRecord` / `twinToWindowRecord`) 로 공통 형태 변환. LIVE `walkForwardFramework.ts` 본체 무수정. Yahoo OHLCV fetch 0건. ENV `SHADOW_WALK_FORWARD_DISABLED=true` 우회 (ADR-0086) |
| `server/learning/conditionAttributionShadow.ts` | Shadow → Condition Attribution 분류 SSOT — `RejectionShadowEntry.conditionScores` (옵셔널) + `currentReturnPct` 결합 분석. 27조건별 Over-Strict (≥+5% 종목 + score<6) vs Good Defense (≤-5% 종목 + score<6) 빈도 + ratio 분류 (over_strict / good_defense / insufficient / normal). ENV `SHADOW_CONDITION_ATTRIBUTION_DISABLED=true` 우회 (ADR-0087) |
| `server/trading/twoBarConfirmation.ts` | BEP 글라이드 단봉 노이즈 차단 정책 함수 SSOT — `evaluateTwoBarConfirmation` 결정 트리 6 분기 (CONFIRM_EXIT/WAIT/RESET/PASS) + 1차 터치 영속 (`shadow.bepGlideTouchAt`) + GRACE_DAYS=1 + ENV `BEP_TWO_BAR_CONFIRMATION_DISABLED` 우회 (ADR-0085 §트랙 1, 호출자 0건 — wiring 후속) |
| `server/trading/slotSizing.ts` | 슬롯 sizing 분할 분모 옵트인 정책 함수 SSOT — Default 단순 카운트 (현재 동작 byte-equivalent), `SLOT_CAPITAL_WEIGHTED_SIZING_ENABLED=true` 활성 시 PR-S1 자본 가중 적용. 분모 floor 1 보장. (ADR-0085 §트랙 2, 호출자 0건 — wiring 후속) |
| `src/config/uiLanguage.ts` | UI 카피 단일 출처 SSOT — nav/card/tier/regime/gate/empty/action 7 카테고리. tier 5-tier (VERIFIED/EXTERNAL/DELAYED/ESTIMATED/MANUAL) + regime 6 RegimeLevel (R1~R6) + gate 4 단계 + empty 4 sub-variant (Phase B IDontKnow 사전 등록). 금지 표현 정책 (절대/출처모호/감정/약속) 정의 (ADR-0094) |
| `src/hooks/useUILang.ts` | UI_LANG SSOT 게이트키퍼 훅 — 7 메서드 (nav/card/tier/regime/gate/empty/action) + raw 직접 접근. useMemo 메모이즈로 동일 reference 보장. 향후 KO/EN 토글은 본 훅 내부만 수정 → 50+ 컴포넌트 무수정 (ADR-0094) |
| `scripts/check_ui_language.js` | UI Language SSOT 정적 검증 게이트 — 4 카테고리 (ABSOLUTE/AMBIGUOUS_SOURCE/EMOTIONAL/PROMISE) 금지 표현 자동 차단. string/template literal 안에서만 매칭, 식별자/주석 제외. ENV 무관, validate:all + precommit 본체 통합 (ADR-0094) |
| `server/data/dataHoldRolePolicy.ts` | DATA_HOLD 역할별 fallback 정책 SSOT — BUY_CANDIDATE/WATCHLIST/HELD_POSITION 3-way 분기, HELD_POSITION blockSell=false 절대 규칙 (exit 경로 보존), ENV `DATA_HOLD_ROLE_POLICY_DISABLED` 우회 (ADR-0128) |
| `server/persistence/verificationQueueRepo.ts` | 검증 실패 영속 SSOT — `consecutiveFailures` 추적 + 24h backoff + 3회+ 시 manualReviewState='QUEUED' + atomic write tmp→rename + FIFO trim 500 + ENV `VERIFICATION_QUEUE_DISABLED` no-op (ADR-0128) |
| `server/data/dataVerificationBatch.ts` | 전일 장 마감 후 워치리스트 일괄 검증 SSOT — KST 평일 16:30 cron, ScheduleClass `'TRADING_DAY_ONLY'`, 실패 시 `applyDataQuarantine(role='WATCHLIST')` + 24h cooldown 텔레그램, ENV `DATA_VERIFICATION_BATCH_DISABLED` skip (ADR-0128) |
| `server/data/dataVerificationIncremental.ts` | 신규 매수 후보 증분 검증 SSOT — `verificationQueueRepo` 조회 (manual review/cooldown 우선) → KIS quote + `safePctChangeStrict` 평가 → 결과별 `resolveDataHoldAction('BUY_CANDIDATE')` 반환. NON-CRITICAL try/catch 격리 (ADR-0128) |
| `server/data/dartCorpEventClassifier.ts` | DART `report_nm` 한국어 키워드 → 5 corporate event 분류 SSOT (STOCK_SPLIT/MERGE/RIGHTS_OFFERING/NEW_SHARES/GENERAL) + 이벤트별 lookback days (90/60/30/14/7), ENV `DART_CORP_EVENT_LOOKBACK_DISABLED` 시 default 7일 (ADR-0128) |

---

## Boundary Rules

- **News-Lag Entry Window Observation boundary** (ADR-0615, 2026-06-16): `server/learning/newsLagEntryWindowObservationAdr0615.ts` 는 dead 상태이던 `newsLagBayesian.getOptimalEntryWindow` (lag posterior) 를 후보 선정 시점에 surface 하는 *관측 전용* SSOT — 각 후보 `sector` 의 활성 `NewsSupplyRecord`(미완료) × 학습 posterior 를 **sector 단위 조인**해 진입 윈도우 PRE/IN/PAST 분류·stamp. `loadNewsSupplyRecords`·`getOptimalEntryWindow` 기존 export 만 경유(newsLag/newsSupply 내부 store 직접 접근 금지). 분류 임계 = `getOptimalEntryWindow` 가 이미 노출하는 ci95(μ±1.96σ) — 두 번째 공식 0. `now` 주입(테스트 결정성)·graceful skip(n<3·손상 detectedAt·sector 미매칭, 불변식 #6). stamp 지점 `universeScanner.ts:696-719` Stage3 컨플루언스 루프(ADR-0614 append 인접·try/catch 격리 불변식 #1)·`CandidateStock.newsLagEntryWindow?` additive optional·ScanSummary 집계 1필드. 신규 fetch 0(로컬 영속 read only·KIS/KRX/Yahoo quota 0)·신규 ledger 0(입력 두 영속 소스 존재). ENV `NEWS_LAG_ENTRY_WINDOW_OBSERVATION_ENABLED` default OFF byte-equivalent. Gate score/주문/autoTradeEngine/kisClient/SourceSnapshot/entryTimingSignal/computeEtfSectorBoost 본문 0줄 — per-stock newsType 귀속·Gate 승격은 후속 ADR. 근거 = `docs/adr/0615-news-lag-entry-window-observation.md`. executionImpact=NONE.
- **Canonical Data SSOT 단방향 체인** (ADR-0525/0526/0527, 2026-05-25): 판단 결과 정본은 `SourceSnapshot → CandidateGateEvaluationView → ExecutionPermissionResolution → PositionPolicyDecision` 4종 단방향 체인이다 (①입력 → ②Gate판단 → ③실행허가 → ④포지션). **Telegram / Debug / Formatter 는 이 정본을 읽기만 한다** — 렌더 시점에 gate2/gate3/execution 을 재계산하거나 `entryFilterDecomposition.candidateTraces` 에서 pass/fail/topBlockReason 을 재추론하지 않는다. ② `candidateGateEvaluationView.ts` 가 per-candidate 판정 정본(formatter read 전용), ③ `executionPermissionResolver.ts` (P0 SSOT) + `unifiedExecutionContract.ts` 가 실행 허가 정본(이중 resolver 통합, permission boolean ≠ count), ④ `regimePositionPolicy.ts` + `PositionPolicyDecision` 이 포지션 정본(diagnosticVsLive 구분). Phase 0 `canonicalDebugRawView.ts` 는 persisted `gateLayerAudit.gate3Consolidated` / `gate1Survival` / `canonicalRuntimeResolution.sizing` 슬라이스를 scope 태그(`ALL_CANDIDATES` / `GATE3_TIMING_SAMPLE` / `PAPER_OBSERVATIONAL` 등)와 함께 순수 투영한다 (재계산 0). 단방향 가드: 정본 생성 후 formatter 의 legacy/raw read → lint/test fail. 9대 불변식 #3 (단일 SourceSnapshot) · #8 (실거래 차단 ≠ Shadow 차단) 정합. 생산자/스캔 로직(rich trace 정당 사용)은 가드 제외 — 소비자 분류는 `_workspace/2026-05-25_canonical-data-ssot/architect/entryFilterDecomposition-consumers.md`.
- **SourceSnapshot Factory Boundary & Contract** (ADR-0556, P2 선행 계약, 2026-06-03): SourceSnapshot factory 는 **기존 ADR-0519 `server/trading/symbolDataCollector.ts` 를 확장**한다 — 신규 `server/snapshot/sourceSnapshotFactory.ts` *신설 금지*(A=collector 정본·B=`ssotPipeline.ts` projection 에 이은 세 번째 아티팩트 = 두 번째 SSOT 신설, ADR-0555 §0 위반). `src/services/autoTrading/ssotPipeline.ts` 의 `UnifiedSourceSnapshot`/`FeatureSnapshot` 은 동명이인 — **factory 가 아니라 perSymbol→gate/policy 순수 *projection* 레이어**(책임 분리 명시). factory 의 신규 책임은 (1) 기존 단일통로 fetch 오케스트레이션 (2) raw→normalized 정규화 (3) **필드별 freshness/providerHealth 통합 산출** 3가지뿐 — **신규 fetch 0·per-scan KIS/KRX quota 순증 0**(불변식 9). freshness 는 **기존 enum 재사용**(`DataHealth`(`shadowCase.ts:29`) 공통 어휘 + 도메인별 `providerHealth`/`SymbolDataQuality`/`SymbolDartCadence` 유지·매핑) — 신규 enum 신설 금지. ADR-0011 경로 분리: factory 는 **자동매매/서버스캔 경로(L1/L2)만** 채우고 `aiUniverseService`(AI추천 L3/L4) import 금지·데이터 미혼합(불변식 7). provider 장애 시 **throw 금지** — 해당 필드만 `freshness=STALE|MISSING + providerIssue` 격리, marketSignal 비변환(불변식 1·2·4·5·6, `executionPermissionResolver.ts:208` 패턴). `quote` 만 필수(없으면 `SymbolDataQuality='MISSING'`), 나머지 optional·결측 시 snapshot 반환. 구현 = engine-dev P2 묶음0(skeleton)~5(byte-equivalent/NONE-간접); 묶음6(universeScanner Stage1 ranking)은 executionImpact *직접* → 별도 ADR + shadow A/B 분리. 입출력 11필드 계약표·근거 = `docs/adr/0556-sourcesnapshot-factory-boundary-and-contract.md`, 블루프린트 = `_workspace/2026-06-03_ssot-constitution/engine-dev/wiring-blueprint.md`. 본 ADR 문서 단계 executionImpact=NONE(코드 0줄).
- **Unified SourceSnapshot Consumer Threading** (ADR-0557, 정본 소비 배포 계약, 2026-06-03): factory 정본은 **스캔 경로**(`signalScanner/index.ts` 지역 변수 `unifiedSnapshot` 를 read-site 에 thread)·**비-스캔 경로**(telegram cmd/runner 진입부에서 `collectUnifiedSnapshot()` 1회 호출)의 *두 입구* 에서 **하나의 주입 형태** 로 소비자에 닿는다 — 채택 규약 = **C(소비자 optional `snapshot?`/`marketProgram?` 인자 + 호출자 주입) + B(비-스캔 경로 진입부 factory 호출)**. **A(retention store `getLatestUnifiedSnapshot` 신설) *기각*** — 세 번째 SourceSnapshot 아티팩트 = 두 번째 SSOT(ADR-0555 §0·불변식 #3 위반). **Consumer Contract(dead carry 방지):** 각 소비자는 (a) 정본 인자 present→정본 read(정본 일치 발현) / (b) absent→기존 provider/store fallback(flag OFF byte-equivalent) 표준 형태를 따르며, **factory 출력에 부착된 필드는 반드시 소비 계약을 동반**한다(생산만·소비 0 = dead carry 금지). 묶음4 가 부착한 `supply.marketProgram`(=`resolveMarketProgramFlow()` 정본, runner `:127` 가 직접 재호출하던 동일 함수)이 묶음3 에서 첫 소비 계약을 얻는다. 신규 타입 0(기존 `MarketProgramFlowResult` 재사용)·신규 store 0·신규 fetch 0(flag ON 시 호출수 byte-equivalent). throw 금지(불변식 #1)·providerIssue≠marketSignal carry 보존(불변식 #6)·L4 정본 미포함(불변식 #7). Roadmap: 본 ADR 위 묶음3(normalSupplyPreview, NONE)→묶음5(universeScanner Stage2, 간접·스캔 경로 지역 변수 thread)→묶음6(Stage1 ranking, **executionImpact 직접 → 별도 ADR + shadow A/B, 본 ADR 이 대체 안 함**). 근거·5대 판단기준 = `docs/adr/0557-unified-sourcesnapshot-consumer-threading.md`, 중단 진단 = `_workspace/2026-06-03_ssot-constitution/engine-dev/bundle3-v3b-stop-report.md`. 본 ADR 문서 단계 executionImpact=NONE(코드 0줄).
- **universeScanner lazy/budget fetch = 정당한 비-통합 경계** (ADR-0558, SSOT Single-Funnel 프로그램 마무리, 2026-06-03): universeScanner Stage1/2 의 lazy/budget fetch(KIS_LOAD_STATE budget max25 `universeScanner.ts:187` / 통과 후보만 fetch `:509` / 음수 changePercent SKIP 제외)는 **quota 절약을 위한 정당한 설계 분기** 다 — factory eager 전수수집(`collectUnifiedSnapshot`, `symbolDataCollector.ts:579`)으로 **통합하지 않는다.** 강제 통합 시 budget 이전 60개 eager fetch = quota 순증 + SKIP-제외 dedup 경계 파괴(불변식 #9 위반)이라 **비-통합이 헌법 정합**. supply(`fetchKisInvestorTradeByStockDaily`, `kisClient/query.ts:901`)는 Stage2·factory 가 *이미* 동일 단일함수 공유 = **단일 소유 충족**; quote 는 경로 이원(`enrichQuoteWithKisMTAS` / `fetchKisStockFullQuote`, eager 회피로 통합 보류·정당). **미래 dev 오수정 방지: "왜 universeScanner 는 factory 미사용?" 을 버그로 오인해 입력화로 고치지 말 것** — budget 우회·#9 회귀다. SSOT 가드 V1 allowlist = `LEGITIMATE_BUDGET_LAZY`(영구 허용, V5 `LEGITIMATE_DIAGNOSTIC` 선례 패턴). 로드맵 묶음5(부분)·묶음6 = '정당한 경계 — 비추진' 종결, `USE_UNIFIED_SOURCE_SNAPSHOT` 전역 활성화는 marketProgram 범위·별도 shadow A/B 잔존. 근거 = `docs/adr/0558-universescanner-lazy-budget-legitimate-non-funnel-boundary.md`, 중단 보고 = `_workspace/2026-06-03_ssot-constitution/engine-dev/bundle5-stage2-feasibility-halt.md`. executionImpact=NONE(코드 0줄·가드 주석 재분류).
- **Railway Decision Log Correlation Standard** (ADR-0528, Phase 3 of Canonical Data SSOT, 2026-05-25): Railway 핵심 결정 로그(autoTrade position policy / execution permission / Shadow lifecycle 5-event / gate 최종 verdict)는 `server/observability/decisionLogCorrelation.ts` 의 `DecisionLogCorrelation` 12 상관 필드를 `formatDecisionLog`/`logDecisionEvent` 헬퍼로 carry 한다 — `sourceSnapshotId`+`decisionStage`+`symbol` 은 필수, permission/verdict/positionIntent 는 단계별 가용성(optional). 출처: `finalVerdict` ← ② `CandidateGateEvaluationView.topBlockReason`, permission 3종 + `executionImpact` ← ③ `UnifiedExecutionPermissionResolution`, ids ← ① `SourceSnapshot`(ADR-0519)/Step27. **로그가 정본을 읽어 carry 할 뿐 재계산하지 않는다** (Canonical Data SSOT 단방향 체인 위에 얹힌 관측 레이어). Shadow 실제 체결 검증은 5-event 체인(`SHADOW_EXECUTION_START → SHADOW_ORDER_CREATED → SHADOW_PAPER_FILLED → SHADOW_POSITION_OPENED → LEDGER_RECORDED`, `SHADOW_LIFECYCLE_CHAIN` 상수)이 공통 `sourceSnapshotId` 로 묶일 때만 성립 — 단절 = 미체결. 정적 가드 `scripts/check_decision_log_correlation.js` 가 핵심 결정 로그(분류표 a)의 헬퍼 경유·필수 필드를 강제하고 부수 로그(분류표 b, retry/scheduler/provider health)는 파일 화이트리스트 + `/* DECISION-LOG-IGNORE: <사유> */` allowlist 로 오탐 방지. 9대 불변식 무영향 — 로그 출력 레이어 한정(결정 로직 0줄 변경, LIVE 매매 byte-equivalent). 분류표 = `_workspace/2026-05-25_canonical-data-ssot/architect/decision-log-points.md`, 가드 설계 = `.../decision-log-guard-design.md`.
- **DART Financials Canonical Inclusion into SourceSnapshot** (ADR-0529, Canonical Data SSOT 마지막 갭, 2026-05-25): supply·price·PER 정본 편입 이후 남은 마지막 갭(Gate2 DART 재무)을 `SymbolSnapshotData` 의 optional 슬롯 `dartFinancials?: SymbolDartFinancialsSlot | null` 로 정본 편입한다. 슬롯 payload 는 기존 `Gate2DartEvaluationFinancials`(= `DartFinancials | QmpDartFinancials`)를 **재사용**(별도 정본 타입 신설 0) + 정본 메타(`cadence: 'QUARTERLY_CACHED' | 'MISSING'` / `source` / `cacheHit` / `fetchedAt`). **cadence = cached-reference(분기) 채택** (per-scan 기각) — DART 는 L2 분기 공시라 intraday re-fetch 가 quota 낭비·신선도 이득 0. DART read 단일 통로 `getGate2DartFinancialsForEvaluation`(`gate2ExternalDataProvider.ts:746`)는 이미 **cache-first**(`gate2ExternalCache`, 500 cap, 7일 STALE) — collector 가 이 함수를 **cache-read 위임**으로 호출해 슬롯을 채우며 별도 fetch 로직 신설 0. quota 가드: cache hit 0회·miss ≤1회(기존과 동일, 신규 침범 0) + `try/catch` 격리(DART 실패 ≠ scan 중단, 불변식 #1) + 비영업일 skip + 분기 STALE. byte-equivalent: read site 4곳(`buyPipeline.ts:115` / `preBreakoutEntry.ts:275` / `kisIntradayCorrection.ts:66` / `universeScanner.ts:615`)이 슬롯 non-null 시 슬롯 read, 아니면 기존 함수 fallback — **슬롯·fallback 동일 함수 출처**라 DART 값·Gate2 판정·`gate2ConfluenceScore.ts:529` fundamental axis 불변. flag `USE_UNIFIED_SOURCE_SNAPSHOT` OFF 또는 cache miss(슬롯 null) 시 기존 독립 경로 100% 유지. **L1(KIS)·L2(DART) 혼재 컨테이너 — 슬롯 `cadence` 메타로 등급 분리 표기 필수**(L2 가 L1 intraday 신선도로 오인 금지, 불변식 #7 L4 격리 무관·DART=L2). 본 단계(타입 pin) LIVE Gate2 런타임 0 변경. engine-dev 단계별 계획·quota tradeoff·회귀 위험 = `_workspace/2026-05-25_canonical-data-ssot/architect/dart-canonical-inclusion-design.md`. PER dedup 선례 = `gate2ExternalDataProvider/perValuation.ts` `synthesizePerOutputFromSnapshotQuote`.
- **perSymbolEvaluation barrel + perSymbol/ 디렉토리 분해 SSOT** (ADR-0134 PR-Refactor-2): `server/trading/signalScanner/perSymbolEvaluation.ts` 는 분해 후 **얇은 barrel re-export 만 유지** (1617→30 LoC). 본체는 `signalScanner/perSymbol/` 디렉토리 5 파일로 분리: `types.ts` (Context+Mutables 4 인터페이스) / `helpers.ts` (getPrice + SymbolExitContext + getAdaptiveProfitTargets + FAILURE_BLOCK_THRESHOLD_PCT) / `buyListLoop.ts` (evaluateBuyList 메인 루프, 1190 LoC) / `intradayLoop.ts` (evaluateIntradayList 장중 루프, 247 LoC) / `index.ts` (barrel). 외부 importer (signalScanner.ts + 35 회귀 테스트) 경로 변경 0건 — barrel 호환 유지. ADR-0001 (signalScanner-decomposition) Phase B 완주. evaluateBuyList god function (cc=244) 본체 추가 분해는 후속 PR-Refactor-2-B (운영 데이터 누적 후).
- **gateEngine boundary**: Functions named `evaluateGate*`, `evaluateStock`, `computeSignalVerdict`, and related scoring helpers belong here. Do not add data-fetching or order logic.
- **bearEngine boundary**: Functions named `evaluateBear*`, `evaluateInverseGate*`, `evaluateMarketNeutral`, `evaluateIPS`, `evaluateFSS` belong here. Do not mix with bull-market Gate evaluation.
- **kisClient boundary**: All raw KIS REST calls (`kisGet`, `kisPost`, `getKisToken`) must go through this module. No other module may call the KIS API directly.
- **kisClient barrel + kisClient/ 디렉토리 분해 SSOT** (ADR-0135 PR-Refactor-3): `server/clients/kisClient.ts` 는 분해 후 **얇은 barrel re-export 만 유지** (1382→23 LoC). 본체는 `clients/kisClient/` 디렉토리 9 파일로 분리: `types.ts` (8 도메인 타입 SSOT) / `constants.ts` (KIS_BASE/TR_ID/HAS_REAL_DATA_CLIENT) / `auth.ts` (토큰 캐시 + refresh*Token + invalidateKisToken) / `resilience.ts` (회로차단 + ADR-0014 jitter + __testOnly + _alertUnsafeWriteFailure) / `overrides.ts` (KisClientOverrides + set/has/getOverrides) / `http.ts` (_rawKisGet/_rawKisPost + kisGet/kisPost + realDataKisGet) / `query.ts` (시세 5함수) / `holdings.ts` (잔고 3함수) / `orders.ts` (주문 6함수) / `index.ts` (barrel). 외부 importer (51 파일 + 회귀 테스트) 경로 변경 0건 — barrel 호환 유지. 의존 그래프 단방향 (constants ← auth ← resilience+overrides ← http ← {query,holdings,orders}). LIVE 매매 본체 byte-equivalent 보존. 절대 규칙 #2 (kisClient 단일 통로) 보존.
- **krxClient 분해 Phase 1+2 SSOT** (ADR-0502c, 2026-05-12): `server/clients/krxClient.ts` **2105 → 1073 LoC (-49.1%)**. 9 모듈 분해 — `clients/krxClient/{types, constants, cache, cooldown, timeWindow, dateUtils}.ts` (Phase 1, PR #915) + **`{csv, http, otpCsv}.ts` (Phase 2, 본 PR)**. ACMA 1500 한계 통과 → BASELINE_TECHNICAL_DEBT 정식 제거. 외부 importer (14 파일 + 회귀 3 테스트) 경로 변경 0건. 외부 export 28 시그니처 100% 보존, byte-equivalent 동작. 의존성 그래프 단방향 — types → constants → cache/cooldown/timeWindow → dateUtils → csv (types) → http (constants/cooldown/timeWindow/types + 외부 logger/marketClock/krxOpenApi) → otpCsv (http/csv/cooldown). ADR-0009 / 0251 / 0256 / 0259 / 0141 / 0445 정책 보존. http meta state (`_lastKrxPostMeta`) 단일 SSOT 격상 — krxClient.ts 의 `getLastKrxPostMeta` / `clearLastKrxPostMetaState` 위임. Phase 3 (parser + queries + facade) 후속 PR 의도된 점진 분해. 절대 규칙 #2 정합 (KRX 단일 통로).
- **stockService boundary**: All external data fetches for **auto-trading and server-side screener** (Yahoo, DART, Gemini, KIS via proxy, KRX via proxy) originate here. quantEngine must not perform network requests.
- **aiUniverseService boundary**: All external data fetches for **AI-recommendation universe discovery and enrichment** originate here. KIS/KRX direct calls are forbidden — use `googleSearchClient` + `naverFinanceClient` + `krxStockMasterRepo` (the master repo's once-a-day KRX download is the only allowed KRX touchpoint). Auto-trading paths must not import this module.
- **autoTradeEngine boundary**: This is the sole channel for real order execution on the server. Client-side modules must not place live orders when `AUTO_TRADE_ENABLED=true`.
- **multiSourceStockMaster boundary**: AI-recommendation universe must refresh the master via this orchestrator only. Direct calls to `refreshKrxStockMaster()` are forbidden outside the orchestrator and its tests. Shadow DB must only be updated by validated Tier 1 / Tier 2 payloads.
- **diagnostics boundary**: `server/health/diagnostics.ts` is the SSOT for system health snapshot collection. `/health` Telegram cmd and `/api/health/pipeline` HTTP route must import `collectHealthSnapshot()` — they may not duplicate data-gathering. External probes (Yahoo/DART HTTP) belong in `runExternalProbes()` — keep them out of the snapshot core to keep the function pure for testing.
- **MarketModeBanner vs MarketRegimeBanner boundary** (ADR-0018): `MarketModeBanner` is **always rendered** and exposes the trading policy box (`REGIME_TRADING_POLICY` SSOT). `MarketRegimeBanner` is the **alert layer** that only renders during non-BULL or VKOSPI/Inverse Gate alerts. Both stack in `MarketOverviewHeader`. Do not merge — responsibilities differ.
- **DataQualityBadge vs ConfidenceBadge boundary** (ADR-0018): `ConfidenceBadge` shows the **single price source** (REALTIME/YAHOO/AI/STALE). `DataQualityBadge` shows the **27+1 condition source mix count** (computed/api/aiInferred). Both coexist on stock cards — no merge.
- **GateStatusCard vs GateStatusWidget boundary** (ADR-0018): `GateStatusCard` is the **compact read-only** version embedded in WatchlistCard (no expand/interaction). `GateStatusWidget` is the **full expandable** version inside StockDetailModal. Do not unify with prop modes — interaction code in card would balloon LoC.
- **REGIME_TRADING_POLICY SSOT**: All future code that maps `RegimeLevel` to allowed/forbidden trading strategies must import from `src/types/ui.ts`. Do not duplicate the 6-level table elsewhere.
- **exitEngine boundary** (ADR-0028): `server/trading/exitEngine.ts` is a barrel re-export only. All logic lives under `server/trading/exitEngine/`. Adding a new exit rule = create `rules/<name>.ts` exporting an `async (ctx: ExitContext) => Promise<ExitRuleResult>` + add to `EXIT_RULES_IN_ORDER` in `index.ts`. Rule files may NOT import other rule files — shared logic goes through `helpers/*`. The `_exitRunning` mutex (PR-6 #12) lives in `index.ts` only.
- **marketDayClassifier boundary** (ADR-0043): `server/utils/marketDayClassifier.ts` 는 KRX 영업일 7분기(TRADING_DAY/WEEKEND/KRX_HOLIDAY/PRE_HOLIDAY/POST_HOLIDAY/LONG_HOLIDAY_*) 분류 + 다음/이전 영업일 산술 SSOT. `KRX_HOLIDAYS` Set 직접 import 금지 — `isKrxHoliday()` 또는 `getMarketDayContext()` 만 사용. 외부 의존(state/persistence)을 도입하지 않는 순수 모듈로 유지.
- **yahooRangePolicy boundary** (ADR-0082): `server/utils/yahooRangePolicy.ts` 는 Yahoo Finance `range` 파라미터 정책 SSOT. 자동 호출 경로(`yahooQuoteAdapter`/`marketDataRouter` 등) 는 `capYahooRange()` 통과 후 사용. `YAHOO_ALLOWED_RANGES` 6값 외 (`'2y'/'5y'/'10y'/'max'/'ytd'` 등) 직접 사용 금지 — `scripts/check_yahoo_range.js` 가 `validate:all` + `precommit` 본체에 통합되어 정적 차단. 화이트리스트는 정책 정의/UI 옵션/문서/테스트 한정. 사용자 명시 운영 정책 ("yahoo 2년치는 sanity 위반이 자주 발생하므로 금지").
- **walkForwardFramework boundary** (ADR-0083): `server/learning/walkForwardFramework.ts` 는 Rolling Window IS/OOS 진단 + Regime 분리 + Alpha decay 추적 *진단 전용* SSOT. LIVE 가중치 동결 정책에 영향 없음 (기존 `walkForwardValidator.ts` 본체 무수정). 영속은 `walkForwardResultsRepo.ts` 단일 통로 (`data/walk-forward-results.json`) — `WALK_FORWARD_STATE_FILE` (freeze) 와 책임 분리. Yahoo OHLCV fetch 0건 (영속 `recommendationTracker` records 만 시간 분할 — ADR-0082 정합). ENV 롤백 `WALK_FORWARD_FRAMEWORK_DISABLED=true`.
- **conditionLifecyclePolicy boundary** (ADR-0084): `server/learning/conditionLifecyclePolicy.ts` 는 27조건 lifecycle status 분류 SSOT. `attributionRepo` 영속 records (PR-19 ADR-0006) 위 *읽기 전용 분류 레이어* — 별도 영속 SSOT 신설 0건. 결정 트리 4분기 (`normal` / `grace` / `silent` / `deprecated`) 우선순위 SSOT 는 본 모듈만 보유. PR-Y2 `reflectionImpactPolicy.ts` 패턴 차용 — 13 모듈 vs 27 조건 동일 자연선택 사상. *측정 전용* — 실제 silent/deprecated 가드 wiring 은 데이터 누적 6개월 후 후속 PR. ENV 롤백 `CONDITION_LIFECYCLE_DISABLED=true`.
- **twoBarConfirmation boundary** (ADR-0085 §트랙 1): `server/trading/twoBarConfirmation.ts` 는 BEP 글라이드 단봉 노이즈 차단 정책 SSOT. `ServerShadowTrade.bepGlideTouchAt?` 옵셔널 영속 필드와 짝. ADR-0079 `classifyStopSource === 'BEP_PROTECTION'` 분기에서만 적용 — 일반 LOSS_STOP 즉시 청산 보존. 현재 호출자 0건 (정책 함수 + 영속 schema 만 — 실제 `exitEngine/rules/hardStopLoss.ts` wiring 은 후속 PR).
- **slotSizing boundary** (ADR-0085 §트랙 2): `server/trading/slotSizing.ts` 는 PR-S2 슬롯 sizing 분할 분모 옵트인 정책 SSOT. **default 비활성** — `SLOT_CAPITAL_WEIGHTED_SIZING_ENABLED=true` ENV 명시 활성화 시에만 PR-S1 자본 가중 적용. 현재 호출자 0건 (정책 함수만 — 실제 `perSymbolEvaluation.ts:902` wiring 은 운영 데이터 누적 후 후속 PR). LIVE 매매 신규 진입 포지션 크기 영향이라 안전 격리.
- **runnerPolicy boundary** (ADR-0606): `server/trading/exit/policies/runnerPolicy.ts` 는 "러너(추세추종) 모드" 승격 판정 *순수* 정책 SSOT — 단일 개념 "러너 = 트레일링 활성 종목 = 슬롯 카운트 제외". `evaluateRunnerPromotion`(마지막 LIMIT 트랜치 도달 OR returnPct ≥ 임계) / `resolveRunnerTrailPct`(넓은 트레일링, 항상 진입 trailPct 이상·타이트화 금지) / `shouldSkipTrancheForRunner`(잔량 전량 트랜치만 스킵, 초기 소량 LIMIT 유지) + ENV 헬퍼. provider/store/now 호출 0 (`regime/riskOnFastUpgrade.ts` 분리 선례). `ServerShadowTrade.isRunner?`/`runnerActivatedAt?`/`runnerTrailPct?`/`runnerPromotedReturnPct?`/`runnerPromotionReason?` 옵셔널 영속 필드 + 클라이언트 미러(`src/api/autoTradeClient.ts`)와 짝. **두 ENV 게이트 독립** (`RUNNER_MODE_ENABLED` 승격·트랜치 스킵·넓은 트레일링 / `RUNNER_SLOT_EXEMPTION_ENABLED` 슬롯 회계 제외) — 모두 **default OFF byte-equivalent**. 슬롯 제외는 `slotAccounting.computeSlotConsumption` eligible 필터 단일 지점에서만 적용(3소비처 loopInitializer/preflight/slotSizing 자연 전파). 현재 호출자 0건 (정책 함수 + 영속 schema 만 — 실제 `exitEngine/rules/{trancheTakeProfitLimit,trailingStop}.ts` + `slotAccounting.ts` wiring 은 Phase 1·2 engine-dev 인계). SHADOW 전용·LIVE 본체 무영향.
- **shadowWalkForwardFramework boundary** (ADR-0086): `server/learning/shadowWalkForwardFramework.ts` 는 Shadow 데이터(Rejection + Twin) IS/OOS Rolling window 진단 *전용* SSOT. LIVE `walkForwardFramework.ts` 본체 무수정. 영속은 `shadowWalkForwardResultsRepo.ts` 단일 통로 — windowId='shadow_*' prefix 로 LIVE 결과와 분리. Yahoo OHLCV fetch 0건 (영속 Rejection+Twin records 만 사용 — ADR-0082 정합). ENV `SHADOW_WALK_FORWARD_DISABLED=true` 우회.
- **conditionAttributionShadow boundary** (ADR-0087): `server/learning/conditionAttributionShadow.ts` 는 27조건 Over-Strict / Good Defense 분류 SSOT. `RejectionShadowEntry.conditionScores` 옵셔널 schema 활용 + **PR-F-2 호출자 wiring 정착** (`perSymbolEvaluation.ts` 거절 분기에서 `buildEntryConditionScores(stock.conditionKeys)` → `recordRejection(..., conditionScores)` 전달). 분석 *전용* — 분류 결과 기반 자동 임계 조정 금지 (운영자 검토 필수). ENV `SHADOW_CONDITION_ATTRIBUTION_DISABLED=true` 우회.
- **shadowLearningSummary boundary** (ADR-0089 PR-H): `server/alerts/shadowLearningSummary.ts` 는 일일 리포트 Shadow 학습 라인 빌더 SSOT. `summarizeRejectionShadow` + `compareTwinsVsReal` 결과를 1~2 줄로 압축. 데이터 부재 시 빈 문자열 반환 — 호출자(`reportGenerator.generateDailyReport`)가 `filter(Boolean)` 으로 자연 생략. LIVE 매매 본체 무영향.
- **cacheCoherenceAuditor boundary** (ADR-0090): `server/persistence/cacheCoherenceAuditor.ts` 는 79개 영속 파일 중 5종 cross-consistency invariant (I1 OCO ↔ Shadow / I2 Pending ↔ Shadow / I3 Attribution schema / I4 TradeSignal finalizedAt / I5 ConditionWeights 범위) 검증 *read-only* SSOT. 영속 파일 0줄 수정 + 외부 호출(KIS/KRX/Yahoo/Gemini) 0건. `runCoherenceAudit()` 진입점 + `audit*` 5종 순수 함수 (입력 주입형, 단위 테스트 용이). 진입 경로 2종: HTTP `GET /api/system/coherence-audit` (systemRouter) + Telegram `/coherence_audit [N=10]` (alias `/cohere`, SYS, riskLevel=0, ADMIN). 새 invariant 추가 시 `audit<NewName>` 순수 함수 추가 + `runCoherenceAudit` orchestrator 에 호출 1줄 + `CoherenceInvariantId` union 확장. ENV `CACHE_COHERENCE_AUDIT_DISABLED=true` 시 즉시 disabled=true + violations 빈 배열 반환 (긴급 우회). 자동 알림·자동 수정은 scope 밖 (Phase 2 후속 PR).
- **DeepAnalysisModal helpers boundary** (PR-Z 옵션 B): `src/components/analysis/DeepAnalysisModal/buildEvaluateInput.ts` (gateEngine 입력 객체 빌더 — 5개 sub-builder 분리), `MasterRadarChart.tsx` (27단계 레이더 차트 + `getRadarData` SSOT), `KeyChecklistOverview.tsx` (4그룹 핵심 체크리스트 그리드), `buildMockShadowSignal.ts` (QUANT 뷰 onShadowTrade 모의 시그널) — DeepAnalysisModal main 함수의 cc=95 god function 분해. main 은 *조립자(orchestrator)* 역할만 (cc=30). 신규 데이터 출처/카테고리 추가 시 helper 모듈에 추가 — main 본체 인라인 빌더 금지.
- **safePctChangeDetailed boundary** (ADR-0091 PR-Z4): `safePctChange` 와 별개의 SSOT — `{value, valid, reason}` 객체 반환. 기존 `safePctChange(): number | null` 51 호출 site 후방호환 보존. *신규 호출자만* detailed 사용 권장. 호출자 의무: (1) `valid=false` 시 Yahoo 등락률 *반드시* 폐기 (`?? 0` 패턴 금지), (2) KIS 등락률 우선 폴백, (3) KIS 도 없으면 0 + dataQuality='STALE_BASE' marker, (4) Gate/MOMENTUM 에서 해당 종목 감점 또는 universe 제외. 사용자 명시 정책 — sanity 실패 후에도 Yahoo base 를 계속 신뢰하는 silent degradation 영구 차단.
- **channelSellSignal remainingQtyAfter boundary** (ADR-0092 PR-Z5): `server/alerts/channelPipeline.ts` 의 매매 채널 청산 알림 빌더는 `remainingQtyAfter` 필드를 *fills SSOT 누적 반영된 정확한 잔량* 입력으로 사용한다. 9 호출자 (rrrCollapseExit / cascadeFinal / trancheTakeProfitLimit / trailingStop / ma60DeathForceExit / euphoriaPartialExit / legacyTakeProfit / hardStopLoss / bearishDivergenceExit) 모두 `reserveSell()` 결과의 `.remainingQty` 를 전달 의무화. 미전달 시 후방호환 폴백 (`originalQty - soldQty`) — *단일 회차 청산만 정확*. NaN/음수 입력 시 폴백 자동 작동. `remainingQtyAfter=0` + 부분청산 의도 시 "전량청산" 라벨 자동 격상 (마지막 회차 자연 전환). 사용자 보고 SK이노베이션 (4주 → 50% → 25% → 25%) "잔여 3주" 잘못 표기 영구 차단.
- **gating alert dedupeKey boundary** (ADR-0093 PR-Z6): VIX/FOMC 게이팅 차단 알림 (`signalScanner.ts` + `preflight.ts` 의 `[VIX 게이팅] 신규 진입 차단` + `[FOMC 게이팅] 신규 진입 차단` 4 site) 은 *반드시* `dedupeKey + cooldownMs: 12h` 사용. 패턴: VIX → `vix_gating_block:${KST_DATE}` (하루 1회), FOMC → `fomc_gating_block:${nextFomcDate ?? 'unknown'}` (FOMC 회차 1회, `fomc_relaxed_${date}` 와 정합). orchestrator_tick 매 1분 호출에도 채팅창 도배 차단. 신규 게이팅 알림 추가 시 동일 패턴 적용 의무 — `gatingAlertDedupe.test.ts` 정적 grep 회귀 가드 자동 fail.
- **krxHolidays boundary** (ADR-0045): `server/trading/krxHolidays.ts` 의 `KRX_HOLIDAYS` 는 `STATIC_HOLIDAYS` (정적 fallback) + `krxHolidayRepo` patch (영속) 합집합 ReadonlySet. 부팅 시 `reloadKrxHolidaySet()` 1회 호출 필수 — `maintenanceJobs.registerMaintenanceJobs()` 가 담당. 외부 호출자(`trancheExecutor` 포함) 는 인스턴스가 동일하므로 자동 반영. `STATIC_HOLIDAYS` 직접 import 금지 — `getStaticKrxHolidays()` view 사용.
- **Calendar(휴장일) SSOT 경계** (ADR-0559): `server/trading/krxHolidays.ts` = **휴장일 데이터 SSOT** (`KRX_HOLIDAYS` set, STATIC ∪ patch, 대체공휴일 로직, ADR-0548 KIS chk-holiday L1 sync) — 휴일 *데이터* 단일 출처. `server/calendar/krxTradingCalendar.ts` = **거래일 walk/staleness 헬퍼 레이어** — 휴일 *데이터* 는 krxHolidays 에 위임(자체 하드코딩 휴일셋 보유 금지). 두 곳이 export 하던 `isKrxHoliday` 중복은 `krxTradingCalendar.isKrxHoliday` 를 `krxHolidays.isKrxHoliday` re-export/위임으로 수렴 → divergence 구조적 소멸. LIVE 게이트(preflight `isKrxTradingDay`→marketSession→liveOrderAllowed)가 단일 데이터 SSOT 의 수혜를 받는다. 위임 교체는 시그니처 보존(소비처 무변경), 행동 변경은 **안전 방향(휴장일 추가 차단) only**.
- **R6 트리거 freshness SSOT 경계** (ADR-0592): `server/trading/kospiTriggerFreshness.ts` = R6 트리거 freshness 를 봉 거래일(KRX) 기준으로 평가하는 **순수 SSOT**(`resolveKospiTriggerFreshness`/`isTradeDateFreshnessEnabled`, provider/store 호출 0). 거래일 판정은 `krxTradingCalendar.isKrxTradingDay/toKstDateKey`(ADR-0559)에 위임 — 자체 휴일셋 0. `regimeBridge.base.ts triggerFreshness()` 가 flag(`R6_TRIGGER_TRADEDATE_FRESHNESS_ENABLED` default OFF) ON 시에만 위임, OFF 면 age-only `macroFreshnessFromUpdatedAt` byte-equivalent. 오늘 intraday KOSPI 수익률(결함 B)은 `marketDataRefresh/kospiIntradayRefresh.ts`(`applyKospiTriggerProvenance`)가 KIS 종합지수(0001, kisClient 단일 통로)로 공급 — Yahoo 금지(ADR-0561). 임계값(-5/+30/±3/+3%) 무변경 · 3 flag 전부 default OFF=baseline.
- **매수 게이트 단일화 경계 (D1, ADR-0630)**: shadow/counterfactual 매수 게이트는 **정본 raw 시장분류 하나** 만 읽고, 오버레이(R6-복구·risk·display)는 사이징(Kelly)·화면에만 쓰며 매수를 게이트하지 않는다. `signalScanner/preflight.ts:379` `learningRegime` 는 flag `BUY_GATE_CANONICAL_REGIME_ENABLED` default OFF(`=== 'true'`·SSOT `isBuyGateCanonicalRegimeEnabled`) ON 시 `toCanonicalRegimeLevel(regimeSnapshot.detectedRegime=rawRegime)` 로 파생(R5_STABILIZING 오버레이 우회·marketStateResolver:319 override 미경유)·결손 시 effectiveRegime 폴백. OFF→현행 `toCanonicalRegimeLevel(effectiveRegime)` byte-identical. **learningRegime 은 shadow lane(provisionalShadowLane:141·counterfactualShadowLearningLane:231 `=== 'R3_EARLY'` 게이트)+Router 입력 전용 carry — 라이브 `regime`(preflight:364-368)·Kelly·maxPositions·권한은 learningRegime 미소비→D1 라이브 byte-identical(불변식 #8 shadow 전용·D2 보다 안전).** shadow lane 게이트의 risk-on 밴드(R1/R2/R3) 확대는 **기각**(R1/R2 는 이미 라이브 isNormalRegime 경로+일반 shadow 표본 보유→중복, ADR-0426/0430 R3_EARLY-bound 보존). 진단(flag 무관 display-only): `persistScanResults:603/:649` noEligibleReason 에 `learning=<learningRegime>` 병기(라인 순증 0·1489줄/한계 1500 여유 11). ENV 1줄 롤백.
- **R6 복구 상태기계 exit SSOT 경계** (D2, ADR-0630): `server/trading/regimeBridge.base.ts` 의 `resolveRecoveredStateMachine` 가 **R6 복구 완료 시 다음 상태기계 등급(R3_NORMAL/R4_CAUTION/R5_STABILIZING) 결정 단일 지점**이다. D1 이 매수 경로를 raw 로 분리하므로 stuck 은 매수를 죽이지 않고 **riskOverride(Kelly ×0.50)·display 만** 오염 — D2 는 그 Kelly/display 정상화용(execution-adjacent, D1 과 독립 flag). exit 판정 분기를 호출부(:845) 외에 신설 금지. 기존 mhs 단일 경로(mhs≥70&raw강세→R3_NORMAL / mhs≥65→R4_CAUTION / else→R5_STABILIZING)는 **시간/tick 입력이 없어 mhs 40~64+raw R3_EARLY 모순 지대에서 R5_STABILIZING 무한 latch**(inRecoveryFlow :804 재진입 영구화)를 일으켜 effectiveRegime→riskOverride(Kelly ×0.50)·displayRegime·learningRegime(carry :379)을 R5 로 오염, shadow lane(:602/:648 R3_EARLY 관문) 영구 차단. 수리는 점진 복구(falling-knife 보호)를 죽이지 말고 **무한 latch 만 차단** — flag `R6_RECOVERY_STUCK_EXIT_ENABLED` default OFF(`=== 'true'`·SSOT `isR6RecoveryStuckExitEnabled` 본 모듈 co-locate) ON 시 raw 강세 `consecutiveHealthyRecoveryTicks ≥ R6_RECOVERY_STUCK_EXIT_MIN_HEALTHY_TICKS`(기본2)면 R3_NORMAL 탈출·첫 tick 은 R5 유지(초기 보호). OFF→mhs 단일 분기 byte-identical·ENV 1줄 롤백. 영속 `consecutiveHealthyRecoveryTicks?`(regimeTransitionStateRepo additive·legacy json 0 하위호환). ⚠️execution-adjacent(riskOverride·Kelly·display·LIVE 권한)→byte-equivalent+flag-gated(불변식 #8). `marketStateResolver.base.ts:319` override·`resolveRiskOverride`/`resolveDisplayRegime`·`classifyRegime` 자격 임계·`toCanonicalRegimeLevel` 매핑은 무접촉(exit 단일 지점만). 부수(flag 무관 display-only): `persistScanResults.ts:603/:649` noEligibleReason 가 LIVE-clamp `routerInput.regime`(R4_NEUTRAL) 대신 실제 게이트 값 `learningRegime` 표시(오진 제거).
- **SSOT Drift-Prevention Registry** (ADR-0560): `docs/SSOT_REGISTRY.md` 가 "동일 개념 다중 구현 drift" 방지 헌법의 데이터 SSOT (개념→owner `file:export`→guarded 심볼→LEGITIMATE 예외→근거 ADR). 정적 가드 `scripts/check_ssot_drift_registry.js`(후속 engine-dev) 가 본 레지스트리를 텍스트로 읽어, 등재 guarded 심볼이 owner + 등재 LEGITIMATE 예외 외 모듈에서 중복 export/정의되면 커밋타임 `EXIT 1`. 진짜 drift 만 차단·정당 분리(위임 re-export·projection 동명이인·divergence 경량 기준선)는 명시 허용(ADR-0558 계승). 신규 guarded 개념·LEGITIMATE 예외 추가는 ADR 동반 의무.
- **holidayResumePolicy boundary** (ADR-0044): `server/trading/holidayResumePolicy.ts` 는 연휴 복귀 첫 영업일 보수 매매 정책 SSOT. `BudgetPolicy`(ADR-0036) 와 별개 운영 — `kellyMultiplier` 는 BudgetPolicy 의 `fractionalKellyCap` 위에 곱해지는 추가 축소 계수, `gateScoreBoost` 는 `ENTRY_MIN_GATE_SCORE` 위에 더하는 추가 임계. 호출자가 명시적으로 `apply*` 헬퍼를 호출해야 효과 발동 (LIVE 매매 본체 0줄 변경 보장). 외부 의존성 marketDayClassifier 만 import — state/persistence/clients import 금지.
- **scheduleGuard boundary** (ADR-0043): `scheduledJob(cronExpr, ScheduleClass, jobName, fn)` 래퍼는 `cron.schedule` 의 단일 진입점. 신규 cron 등록 시 ScheduleClass 명시 필수 (TRADING_DAY_ONLY/WEEKEND_MAINTENANCE/MARKET_ADJACENT/ALWAYS_ON 4값). cron 표현식 `1-5`/`0-4` 평일 가드는 1차 방어선, ScheduleClass 가 KRX 공휴일을 평일에 차단하는 진짜 방어선.
- **stockScreener boundary** (ADR-0029): `server/screener/stockScreener.ts` is a hybrid (core preScreenStocks + autoPopulateWatchlist) + barrel re-export of 6 split modules. Adding a new data source = create `adapters/<name>QuoteAdapter.ts` + barrel re-export. Adapters MUST NOT import other adapters — shared math goes through `adapters/_indicators.ts`. `YahooQuoteExtended` type lives in `yahooQuoteAdapter.ts` and is re-exported. `lastRejectionLog` mutable state lives in `rejectionLog.ts` only — accessed via `getLastRejectionLog`/`setLastRejectionLog` SSOT.
- **alertRouter SSOT + 채널 ID boundary** (ADR-0037): `server/alerts/alertRouter.ts` 는 텔레그램 4채널 발송 단일 진입점이다. `dispatchAlert(category, message, options?)` 외 경로로 채널 송출 금지. `process.env.TELEGRAM_{TRADE,ANALYSIS,INFO,SYSTEM,PICK}_CHANNEL_ID` 직접 접근은 `alertRouter.ts`/`alertCategories.ts`/`telegramClient.ts (LEGACY)` 외에서 금지 — `npm run validate:channelBoundary` 가 자동 차단. 신규 코드는 `ChannelSemantic.{EXECUTION,SIGNAL,REGIME,JOURNAL}` 시멘틱 별칭 사용 권장. 진동 정책은 `VIBRATION_POLICY` 매트릭스 SSOT 만 참조 — 호출자가 `disableNotification` 명시 시 override 우선, 미지정 시 정책 자동 적용.
- **개인 회선(DM) vs 채널 분리** (ADR-0038): 잔고/자산/비상정지/손절 접근/KIS 오류/EgressGuard 차단 같은 민감 정보는 채널이 아니라 `sendPrivateAlert(message, opts?)` (개인 DM 전용 SSOT) 로만 발송한다. 채널 발송 (`dispatchAlert`/`channelBuySignalEmitted` 등 12종 함수) 메시지 본문에 잔고 키워드(`총자산`/`주문가능현금`/`잔여 현금`/`보유자산`/`평가손익` 등 8종) 가 string literal 또는 template literal 안에 들어가면 `npm run validate:sensitiveAlerts` 가 자동 차단. `console.*(...)` (Railway 로그) / `throw new Error(...)` (예외 메시지) / 인라인 `// safe-channel-keyword` 주석은 의도적 예외 처리. `sendTelegramBroadcast` 는 PR-X2 부터 `@deprecated` — 신규 코드는 `sendPrivateAlert` 또는 `dispatchAlert` 사용.
- **Telegram callsite 시멘틱 별칭 + sendPickChannelAlert 삭제** (ADR-0039): PR-X3 부터 11 callsite 모두 `ChannelSemantic.{EXECUTION,SIGNAL,REGIME,JOURNAL}` 별칭 또는 `sendPrivateAlert` 사용. `sendPickChannelAlert` 함수는 호출자 0건 마이그레이션 후 telegramClient.ts 에서 삭제 — `TELEGRAM_PICK_CHANNEL_ID` legacy fallback 은 `alertRouter.resolveAnalysisChannelId()` 만 처리. `ChannelBoundary` 화이트리스트는 3 파일(alertRouter / alertCategories / check_channel_boundary)로 축소. `stockPickReporter` 의 sendPickChannelAlert + dispatchAlert(ANALYSIS) 이중 발송 제거 — `dispatchAlert(SIGNAL)` 단일 발송으로 통합. 옵션 정리: `tier`/`category`/`disableChannelNotification` legacy 필드는 dispatchAlert 호출 시 제거 (VIBRATION_POLICY 가 자동 처리), `priority`/`dedupeKey` 만 유지.
- **CH3 REGIME 매크로 다이제스트 정기 발행** (ADR-0040): `server/alerts/macroDigestReport.ts` 가 1일 2회 매크로 다이제스트를 `dispatchAlert(ChannelSemantic.REGIME)` 으로 발송한다. PRE_OPEN KST 08:30 (UTC 23:30 일~목) + POST_CLOSE KST 16:00 (UTC 07:00 월~금). 데이터는 `macroStateRepo.loadMacroState()` 단일 SSOT 만 읽음 — 외부 호출 0건 (다른 cron 이 갱신한 macroState 재사용). **개별 종목 정보 절대 포함 금지** — CH3 정체성은 "시장 전체 상태만". 회귀 테스트가 6자리 코드 패턴 부재 + 잔고 키워드 8종 부재를 자동 검증. dedupeKey `macro_digest:{mode}:{KST 일자}` 로 재시작 시 이중 발송 차단.
- **CH4 JOURNAL 주간 자기비판 리포트** (ADR-0041): `server/alerts/weeklySelfCritiqueReport.ts` 가 일요일 19:00 KST (UTC 10:00 Sun) 에 `dispatchAlert(ChannelSemantic.JOURNAL)` 으로 발송한다. 데이터: `aggregateFillStats(trades, range)` 주간 fill 통계 + `getLearningHistory(7).escalatingBiases` 편향 (3일 연속 ≥ 0.5) + `summarizeStopPatterns(weeklyStops)` 손절 패턴 분포 + `buildStopPatternRecommendation` 자동 권고. **개별 종목 정보 절대 포함 금지** (CH4 메타 학습 정체성) — 회귀 테스트가 6자리 코드 패턴 부재 + 잔고 키워드 8종 부재 자동 검증. 자동 권고 휴리스틱은 결정적 (Gemini 호출 0) — 표본 ≥ 3건 + 비율 ≥ 40% 임계 통과 시 R5_CAUTION/R6_DEFENSE/ATR_HARD_STOP/CASCADE 패턴별 권고문 생성. dedupeKey `weekly_self_critique:{KST 일요일}` 로 이중 발송 차단.
- **/channel_test 4채널 헬스체크 + 손절 카운트다운 sendPrivateAlert** (ADR-0042): `server/telegram/commands/alert/channelTest.cmd.ts` 가 `runChannelHealthCheck()` (alertRouter SSOT) 를 호출해 4채널(EXECUTION/SIGNAL/REGIME/JOURNAL) 동시 발송 후 결과 집계 — `formatChannelHealthCheckResult()` 순수 함수가 정상/미설정/비활성/발송실패 4분기 처리 + 미설정 환경변수 누적 안내. `stopApproachAlert` 손절 접근 3단계 경보(-5%/-3%/-1%)는 `sendTelegramAlert` → `sendPrivateAlert` 시멘틱 정합화 — 사용자 패닉 매도 차단 위해 CH1 EXECUTION 채널이 아닌 개인 DM 만 발송, 실제 손절 발동 시 channelSellSignal 이 사후 보고 (CH1).
- **F2W Drift Detector boundary** (ADR-0046): `src/services/quant/f2wDriftDetector.ts` 는 자기학습 가중치 σ 변화 감시 SSOT. `feedbackLoopEngine.evaluateFeedbackLoop` 진입부 가드 외에서 `pauseF2W` / `clearF2WPause` 호출 금지 — 운영자 수동 해제는 별도 텔레그램 명령 (`/clear_f2w_pause` 후속 PR) 으로만. shadow=true 호출은 본 가드 우회 (ADR-0027 grace 보존). drift 감지 시 클라이언트가 `POST /api/learning/f2w-drift-alert` → `server/alerts/f2wDriftAlert.ts` 가 `dispatchAlert(ChannelSemantic.JOURNAL)` + `sendPrivateAlert` 일괄 발송. 클라이언트 측 모듈은 server/alerts import 금지 — fetch HTTP 경계만 사용. **개별 종목 정보 절대 포함 금지** (CH4 JOURNAL 정체성) — 회귀 테스트가 6자리 코드 패턴 부재 + 잔고 키워드 8종 부재 자동 검증. `LEARNING_F2W_DRIFT_DISABLED=true` 환경변수로 전체 회로 무력화 가능 — 사고 조사 시 임시 우회 경로.
- **Learning Coverage Heatmap boundary** (ADR-0048): `src/services/quant/learningCoverage.ts` 는 F2W (조건 × 레짐) 셀 카운트 SSOT — 27 × 7 = 189 셀. `feedbackLoopEngine.evaluateFeedbackLoop` 진입부 가드: 어떤 셀도 `COVERAGE_THRESHOLD=30` 미만이면 해당 조건 가중치 보정 스킵 (`coverageGated[]` 결과 노출). entryRegime 부재 v1 레코드는 `FALLBACK_REGIME='UNCERTAIN'` 그룹화. shadow=true 호출도 게이트 활성 (학습 무결성). 외부 의존성 0 — `regimeMemoryBank` 의 ALL_REGIMES 와 동일한 자체 사본 보유 (순환 import 차단). `LEARNING_COVERAGE_GATE_DISABLED=true` 환경변수 회로 무력화. 영속 데이터 신규 0건 — TradeRecord.entryRegime 만 사용 (메모리 only). 페르소나 "불확실성 높으면 관망" 의 학습 영역 적용.
- **Reflection Module Half-Life boundary** (ADR-0047): `server/persistence/reflectionImpactRepo.ts` 는 13개 reflection 모듈 영향률 영속 SSOT (1년 ring buffer). `server/learning/reflectionImpactPolicy.ts` 는 임계 5%/1% 정책 SSOT — `getModuleStatus()` 결정 트리: ENV 무력화 → grace period 30일 → 표본 < 20건 grace → 영향률 < 1% deprecated → < 5% silent → 그 외 normal. `reflectionImpactRecorder.ts` 가 nightlyReflectionEngine 의 report 완성 시점에 1회 호출되어 13개 모듈 meaningful 일괄 추론·영속 — 모듈 호출 직접 wrapping 금지 (광범위 변경 회피). 본 PR 은 *측정만* — 실제 silent/deprecated 가드 wiring (실행 스킵 / 출력 억제) 은 데이터 누적 후 후속 PR. 운영자 진단 endpoint `GET /api/learning/reflection-impact?days=180` — status/impactRate/runs 일괄 반환. `LEARNING_REFLECTION_HALFLIFE_DISABLED=true` 환경변수로 모든 모듈 'normal' 강제.
- **ServerShadowTrade.sector 박제 SSOT** (ADR-0060): `ServerShadowTrade.sector` 는 `getSectorByCode(stockCode)` 결과만 저장한다. 다른 source (사용자 입력 / AI 추론 / 외부 API / `relatedSectors` 다중 라벨) 금지 — server 측은 단일 결정적 라벨만 보존. wiring 지점은 `buildBuyTrade` (`server/trading/buyPipeline.ts`) 단일 SSOT 와 `shadowRouter.ts` 직접 리터럴 1건만. 기존 영속 레코드 마이그레이션은 `loadShadowTrades()` lazy 백필로 자동 처리 (KIS/KRX 호출 0). UI 측 `StockRecommendation.relatedSectors` 는 별개 축으로 공존.
- **섹터 게이트 fallback 패턴 통일** (ADR-0060): `sectorConcentrationGate` 와 `sectorPreGuardGate` 는 `stock.sector` 부재 시 `getSectorByCode(stock.code)` fallback 후 비교한다. `watchlist[].sector` 도 동일 fallback 적용 (대칭성 보장). 둘 다 부재 시 가드 pass — 진입 결정 로직 무변경, fallback 만 추가. 신규 섹터 가드 도입 시 본 패턴 차용 의무.
- **UI 섹터 매칭 양방향 fallback** (ADR-0060): `src/utils/sectorStockMatch.ts` `stockMatchesSector` 는 `relatedSectors` (AI 다중 라벨) 우선 매칭 → 없거나 불일치 시 `sector` 단일 필드 fallback. ShadowTrade-shaped 객체와 StockRecommendation 양쪽 매칭 보존. `filterStocksBySector` 는 generic 화 + code 기준 dedupe — `SectorStocksDrilldown` 이 watchlist/recommendations/active shadow trades 3 source 통합 시 중복 자연 제거.
- **AutoTradeContextualLayout boundary** (ADR-0049): `useAutoTradeContext()` 가 시각·MarketDataMode → 5 컨텍스트(PRE_MARKET / LIVE_MARKET / POST_MARKET / OVERNIGHT / WEEKEND_HOLIDAY) 매핑 SSOT. 각 자식 섹션은 `AutoTradeContextSection` 으로 wrap 되어 자기 `priorityByContext`/`collapsedByContext` 를 declare — 부모 `AutoTradeContextualLayout` 가 priority 오름차순 + 동률 originalIndex 안정 정렬 후 렌더. priority 9+ 는 hidden, collapsed=true 는 `<details>` 접힘. **기존 자식 컴포넌트 본체 무수정 원칙** — 신규 섹션 추가는 wrap 만으로. 비-AutoTradeContextSection children 은 정렬에서 제외 (별도 위치 배치 권장). Fragment 는 재귀 traverse 하여 조건부 렌더 (`{cond && <Section/>}`) 자연 동작.
- **AccountSurvivalGauge boundary** (ADR-0050): `server/health/survival.ts` 의 `collectSurvivalSnapshot()` 가 SurvivalSnapshot SSOT — 외부 호출 0건 (assessKillSwitch + evaluatePortfolioRisk + computeKellySurface + loadShadowTrades + loadMacroState 만 read). 신규 영속 SSOT 도입 금지 (peakEquity 영속 추적은 후속 PR). tier 임계값 (Daily Loss 50/25/0% buffer / Sector HHI 2500/4000 / Kelly ratio 1.0/1.5) 변경은 ADR-0050 §2.2 표 갱신 + 회귀 테스트 동시 수정 의무. UI 위젯 `AccountSurvivalGauge` 는 `src/api/survivalClient.ts` 단일 진입점 사용 — 절대 규칙 #3 (서버↔클라 직접 import 금지) 준수. AutoTradePage 통합은 PR-Z1 의 `AutoTradeContextSection` 으로 wrap, 모든 5 컨텍스트에서 priority=1.
- **InvalidationMeter boundary** (ADR-0051): `src/utils/invalidationConditions.ts` 가 4 카테고리 (STOP_LOSS_APPROACH / LOSS_THRESHOLD / STAGE_ESCALATION / TARGET_REACHED) 휴리스틱 평가 SSOT — `PositionItem` 의 *기존 필드* (stopLossPrice / pnlPct / stage / targetPrice1 / currentPrice) 만 사용. 외부 호출 0건. tier 임계 (0/1/≥2 metCount → OK/WARN/CRITICAL, evaluableCount=0 → NA) 변경은 ADR-0051 §2.3 표 갱신 + 회귀 테스트 동시 수정 의무. 매수 시점 무효화 조건의 영속 SSOT (ServerShadowTrade.invalidationConditions[]) 도입은 후속 PR — 본 PR 의 휴리스틱은 *근사값* 임을 명시. UI 위젯 `InvalidationMeter` 는 PositionLifecyclePanel 각 카드 내부에 임베드, AutoTradePage 본체 무수정.
- **TodayOneDecisionCard boundary** (ADR-0052): `src/utils/oneDecisionResolver.ts` 가 6 case 우선순위 SSOT — EMERGENCY_STOP > DAILY_LOSS_EMERGENCY > INVALIDATED_POSITIONS > ACCOUNT_CRITICAL > PENDING_APPROVALS > VOID > MONITORING. 위→아래 첫 매칭 단락. 우선순위 변경은 ADR-0052 §2.1 표 갱신 + 회귀 테스트 동시 수정 의무. VOID 4 조건(높은 변동성 / 활성 포지션 0 / 승인 대기 0 / 거시 리스크) 모두 AND — 거짓 양성 차단. 변동성 z-score 는 vixHistory 우선, vkospiDayChange fallback, 둘 다 부재 시 0 (보수적). PR-Z2 (SurvivalSnapshot) + PR-Z3 (InvalidationMeter) 자산 100% 재활용 — 신규 영속 SSOT 0개. UI 위젯은 `<TodayOneDecisionCard>` 단일 — AutoTradePage 의 AutoTradeContextSection 안정 정렬로 AccountSurvivalGauge 보다 먼저 렌더 (children 선언 순서 보존).
- **NightlyReflectionCard boundary** (ADR-0053): `src/components/autoTrading/NightlyReflectionCard.tsx` 는 Pro 모드 한정 — `useSettingsStore.autoTradeViewMode === 'pro'` 일 때만 AutoTradePage 에서 렌더. 컨텍스트별 priority: POST_MARKET=2 / OVERNIGHT=3 / WEEKEND_HOLIDAY=2 / PRE_MARKET=5 / LIVE_MARKET=7 (collapsed). 본 카드는 *읽기 전용* — `keyLessons`/`tomorrowAdjustments` 본문은 카운트만 노출, 정독은 텔레그램 `/learning_status` SSOT 단일 채널 유도. ADR-0007 의 manual approval 채널 정합 유지. 향후 매뉴얼 승인 인프라(experimentProposals.AWAIT_APPROVAL) 도입 시 본 카드 위에 승인/거부 버튼 추가하여 텔레그램 SSOT 와 통합.
- **ConcordanceMatrix boundary** (ADR-0054): `server/learning/conditionSourceMap.ts` 가 27 조건 분류(REAL_DATA 9 + AI 18) + classifyTier(8/6/4/2 임계값) SSOT — 클라 `src/services/quant/evolutionEngine.ts CONDITION_SOURCE_MAP` 동기 사본. 회귀 테스트가 양쪽 정합성 검증, 변경 시 동시 수정 의무. `/api/attribution/concordance` 는 `loadCurrentSchemaRecords()` 만 read (외부 호출 0건). 5×5=25 cell 보장. diagonal/off-diagonal 통계 합산. Bucket 임계값 변경은 ADR-0054 §2.1 표 갱신 + 회귀 테스트 동시 수정 의무. UI `ConcordanceMatrix` 는 RecommendationHistoryPage 임베드 — AutoTradePage 가 아님 (분석 페이지 분리).
- **pipelineDiagnosis Yahoo SSOT boundary** (ADR-0056): `server/trading/pipelineDiagnosis.ts` 는 Yahoo Finance 직접 fetch 금지 — `getYahooHealthSnapshot()` (`server/trading/marketDataRefresh.ts`) SSOT 만 read. 자체 fetch 추가는 24h 누적 통계 기반 판단을 우회해 단발성 503 알림 폭주를 다시 일으킨다. 알림 등급 분리: `issues`(OPERATIONAL CRITICAL — 즉시 텔레그램) / `warnings`(OPERATIONAL — 즉시 텔레그램) / `informational`(INFORMATIONAL — 일일 요약, 텔레그램 미발송). `informational` 필드는 옵셔널 후방호환. `server/utils/yahooProbeRetry.ts` (query1→query2 재시도 + 다중 심볼 헬퍼) 는 미래 확장점 — pipelineDiagnosis 본체에서는 미사용, 향후 수동 진단 명령·관측성 SSOT 에서 활용. cron `30 21 * * 0-4` (UTC = KST 06:30 KST) — NYSE 마감 직후 + 한국장 개장 2.5h 전 데이터 신선도/운영 관련성 동시 극대화.
- **marketIndicatorsSnapshot boundary** (ADR-0065): `server/persistence/marketIndicatorsSnapshotRepo.ts` 가 `/api/market-indicators` 11 필드 disk 영속 SSOT — `MARKET_INDICATOR_FIELDS` 상수 11 키가 응답 스키마와 1:1 정합 (회귀 테스트 강제). `mergeFreshIntoSnapshot` 가 NaN/Infinity/음수 가격 가드 후 성공 필드만 갱신, `fillMissingFromSnapshot` 가 null 필드를 last-known-good 으로 채우고 `staleFields[]` 반환. atomic write (tmp → rename) — 동시 갱신 race 시 이전 정상 본 보존. 손상 JSON 은 빈 객체 fallback 후 다음 정상 fetch 가 자동 복구. 라우터 핸들러 외에서 직접 호출 금지 — `/market-indicators` 엔드포인트가 단일 진입점. TTL 없음 (last-known-good 무한 보존), 운영자는 `X-Field-Stale` 응답 헤더로 stale 필드 인지.
- **marketOverview prefill overlay boundary** (ADR-0064): `src/services/stock/marketOverviewIndicators.ts` 가 가격성 데이터(지수/FX/원자재) 8 심볼 prefill SSOT — `INDEX_TARGETS` (5) + `FX_TARGETS` (2) + `COMMODITY_TARGETS` (2) `as const` 배열만 변경 가능. `getMarketOverview()` 가 AI 호출 전 `fetchPrefilledMarketData()` 호출 후 `normalizeMarketOverview(raw, prefillOverlay)` 에서 강제 덮어씀 — 정책: prefill 카테고리 ≥1 항목 → AI 응답 *완전히 무시* / 빈 배열 → AI fallback. AI 는 더 이상 indices/exchangeRates/commodities 를 추정하지 않는다 (프롬프트 명시 + overlay enforce). `interestRates`/`macroIndicators` (한국 3년물 / CPI / PCE / 실업률) 는 본 PR scope 외 — AI 위임 유지, 후속 PR 에서 ECOS/FRED 클라이언트 위임. `getMarketOverview` 호출자는 자동매매 경로 0건 (절대 규칙 #4 미위반). KOSPI/KOSDAQ 는 `/api/market-indicators` (intraday) 우선, USD/KRW 는 ECOS macroCached 우선.
- **marketOverview SWR cache boundary** (ADR-0066): `src/services/stock/marketOverviewCache.ts` 가 marketOverview AI raw 응답 SWR 캐시 SSOT — 단일 슬롯 메모리 캐시. `getMarketOverview` 외 호출자 금지 — 다른 모듈은 `getCachedAIResponse` (3층 캐시 SSOT) 사용. SWR 분기 4종 (EMPTY / FRESH 60s / STALE 5min / EXPIRED 30min) 임계값 변경은 ADR-0066 §2.1 표 갱신 + 회귀 테스트 동시 수정 의무. AI raw 응답만 캐시 — prefill 결과 (Yahoo 가격성 데이터) 는 normalize 시점에 매번 새로 적용 (ADR-0064 overlay 정책 보존). 백그라운드 refresh 실패 시 stale 캐시 보존 (사용자 영향 없음) + console.warn 로 운영자 인지. localStorage / 서버 Volume 영속 X — 새 탭 / 새로고침 시 첫 호출 항상 EMPTY 분기로 강제 fetch.
- **GateMiniIndicator boundary** (ADR-0055): `src/utils/gateMiniIndicator.ts` 가 4-Gate 미니 인디케이터 평가 SSOT — PR-A `buildGateCardSummary` 와 동일 SSOT(`GATE1_IDS`/`GATE2_IDS`/`GATE3_IDS`/`CONDITION_PASS_THRESHOLD`/`CONDITION_ID_TO_CHECKLIST_KEY`) 재활용. 4-tier classify(PASS≥0.5 / PARTIAL≥0.3 / FAIL>0 / NA totalCount=0). 외부 호출 0건. 본 PR 은 *클라이언트 휴리스틱*, 사용자 원안의 서버 SQLite 캐시 + circuit breaker 인프라는 stock.checklist 부재 케이스 발생 시 후속 PR 분리(ADR-0055 §6 명문화). UI: `GateMiniIndicator` 는 `WatchlistCard` 헤더 우측 컴팩트 4 dot — PR-A `GateStatusCard` (카드 본문 expand 가능 풀카드) 와 책임 분리: 미니=*즉시 인지*, 풀카드=*상세 검토*.
- **FOMC DAY 청산 단일 진입점** (ADR-0061): `server/trading/fomcDayLiquidation.ts` 의 `liquidateAllForFomc()` 외 경로로 FOMC DAY 강제 청산 금지. 다른 모듈은 import 만 가능 — direct fork (자체 placeKisSellOrder 루프 등) 차단. 사전 경보 (`runFomcDayMorningAlert` / `runFomcDayPreLiquidationAlert`) 도 본 모듈 단일 SSOT.
- **FOMC DAY reserveSell SSOT 의무** (ADR-0061): `liquidateAllForFomc` 는 `placeKisSellOrder` (kisClient 단일 통로, 절대 규칙 #2) + `reserveSell` (exitEngine.r6EmergencyExit 와 byte-equivalent 패턴, 절대 규칙 #4) 만 사용 — `kisGet`/`kisPost` 직접 호출 금지. fill 회계는 reserveSell 의 PROVISIONAL/CONFIRMED/FAILED 분기에 위임. LIVE PENDING 시 `addSellOrder()` 로 fillMonitor 폴링 등록. attribution 부착은 `buildExitAttribution('FOMC_DAY_LIQUIDATION', ['fomc_day_force_close'], regime)` (PR-S 패턴).
- **FOMC DAY 가드 자동 silent SSOT** (ADR-0061): 청산 cron 3건 (`fomc_day_morning_alert` 09:00 / `fomc_day_pre_liquidation_alert` 14:00 / `fomc_day_liquidation` 14:30 KST) 은 매일 평일(`1-5`) 등록되지만 `shouldExecuteLiquidationAt` 5중 가드 (DAY phase / config.enabled / AUTO_TRADE_ENABLED / !emergencyStop / KST 시각 boundary) 가 비-DAY 일과 SHADOW/disabled/비상정지 환경을 자동 silent 처리 — FOMC 캘린더 + 매매 활성 상태 단일 SSOT. 운영자 수동 트리거는 별도 텔레그램 명령 후속 PR 로 분리.
- **marketOverview ↔ 자동매매 boundary** (ADR-0067): `server/trading/`, `server/orchestrator/`, `server/scheduler/`, `server/services/aiUniverseService.ts` 에서 `marketOverview` / `marketOverviewCache` / `marketOverviewIndicators` / `useMarketData` / `useMarketStore` import 절대 금지 — `npm run validate:marketOverviewBoundary` 가 자동 차단 (precommit + validate:all 통합). 자동매매 경로는 *반드시* `macroState` (`server/persistence/macroStateRepo.ts`) SSOT 만 사용해야 한다 — AI hallucinate 가능성이 있는 marketOverview 데이터가 LIVE 주문 의사결정을 오염시키지 않도록 코드 레벨 강제. UI 영역 (`src/components/**`, `src/hooks/useMarketData.ts`, `src/stores/useMarketStore.ts`, `src/services/stock/*`) 는 정상 import 허용.
- **macroState staleness 차단 boundary** (ADR-0068): `server/trading/macroStaleness.ts` `evaluateMacroStaleness(macro?, now?)` SSOT — 자동매매 진입 게이트 (`runPreflight` Step 4.5) 가 24h+ STALE_BLOCK 또는 NO_DATA 시 `shouldAbort: true` + CRITICAL 텔레그램 알림 + shadow update + `abortReason: 'MACRO_STATE_STALE'`. STALE_WARN (8~24h) 은 진입 허용 + 운영자 경보. 임계값 변경은 ADR-0068 §2.1 표 갱신 + 회귀 테스트 동시 수정 의무. `MACRO_STALENESS_DISABLED=true` 긴급 운영 우회 — 권장 안 함, macroState cron 자체 복구 우선. boundary 비교는 `ageHoursRaw` (raw ms / 3_600_000) 사용 (round 된 `ageHours` 는 표시 전용).
- **다중 소스 교차 검증 boundary** (ADR-0071): `server/trading/crossSourceValidator.ts` `evaluateCrossSource(primary, secondary, label, thresholds?)` SSOT — 6 tier 분류 (AGREED/WARN/CRITICAL/PRIMARY_ONLY/SECONDARY_ONLY/NO_DATA). 기본 임계 warn 3% / critical 5%. CRITICAL 격차 시 secondary 우선 + diverged=true (CRITICAL 텔레그램 alert trigger). 변경 시 ADR-0071 §2.1 표 갱신 + 회귀 테스트 동시 수정 의무. 사용자 보고 (USD/KRW Yahoo 1380 vs ECOS 1474, 6.4% 격차) 직접 대응 — `marketDataRefresh.ts` 가 `fetchLatestUsdKrw()` (한국은행 ECOS 공식) 와 `KRW=X` (Yahoo) 두 소스 fetch 후 교차 검증. macroState 옵셔널 필드 3종 (`usdKrwSource` / `usdKrwDivergencePct` / `usdKrwDivergenceTier`) 영속. `/regime` 메시지의 `formatUsdKrwLine(macro)` 가 출처 + 격차 % 노출. ADR-0070 점수 6번째 axis (divergence) 추가 — CRITICAL -25 / WARN -10 / PRIMARY_ONLY · SECONDARY_ONLY -5.
- **MarketDataHealthScore SSOT** (ADR-0070): `server/health/marketDataHealth.ts` `computeMarketDataHealthScore(snapshot, staleFields, now?)` 가 5축 (macroState staleness + indicators staleFields + Yahoo + KIS + KRX) 가중평균 SSOT — 시작 100점에서 차감 합산 → floor 0. 점수 정책 변경은 ADR-0070 §2.2 표 갱신 + 회귀 테스트 동시 수정 의무. Tier 임계값 (HEALTHY ≥85 / DEGRADED ≥60 / CRITICAL <60) 도 동일. `loadMacroState()` 만 read — 외부 호출 0. `formatMarketDataHealthLine(score)` 가 /health 텔레그램 라인 포맷 (이모지 + 점수 + tier + 최상위 차감). 서버측 staleFields 는 marketIndicatorsSnapshotRepo 헤더 cache 부재로 본 PR 빈 배열 — 후속 PR 에서 lastStaleFields 영속 추가 후 정확 반영.
- **X-Field-Stale 헤더 → UI 배지 boundary** (ADR-0069): `src/services/stock/marketOverview.ts` `parseStaleFieldsHeader(header)` SSOT 가 콤마 구분 헤더를 string[] 로 정규화 (공백 trim + 빈 항목 제거). `fetchMarketIndicators` 응답에 `staleFields?: string[]` 옵셔널 필드 노출 — 기존 호출자 4건 (marketOverview/momentumRecommendations/batchIntel ×2) 무영향. `useMarketStore.staleFields` 영속 (partialize 제외 — 부팅 시 빈 배열, 첫 fetch 후 자동 갱신). `useMarketData` hook 이 5분 폴링으로 자동 갱신. UI 배지 `MarketDataStaleBadge` 는 staleFields.length === 0 시 미렌더 — 정상 상태 화면 깨끗. 라벨 매핑은 화이트리스트 (FIELD_LABELS), 미매핑은 raw 키 표시 (서버 신규 필드 즉시 노출).
- **TradeSignalStatus 상태머신 SSOT** (ADR-0077): `server/persistence/tradeSignalStatusRepo.ts` 가 AI 추천→자동매매 6단계 상태 영속 단일 진입점. 모든 mutating API (`recordAiCandidate` / `markDataVerified` / `markRiskApproved` / `markUserApproved` / `markAutoTradeReady` / `markBlocked`) 는 멱등 + atomic write + FIFO 1000 trim + canTransition 매트릭스 검증. **기존 게이트 본체 0줄 변경 원칙** — 4 wiring 위치(signalScanner addRecommendation 직후 / perSymbolEvaluation entryRevalidationStep 결과 분기 + chain fail / buyApproval callback / buyPipeline KIS 주문 직전) 모두 `try { ... } catch (e) { console.warn(...) }` 보호로 영속 실패가 매매 결정 차단 안 함. SHADOW 모드는 RISK_APPROVED → AUTO_TRADE_READY 직접 전이 허용 (USER_APPROVED 우회). BlockGate 12 분류 (DATA/GATE_0/GATE_1/GATE_2/GATE_3/RISK_BUDGET/SECTOR/PORTFOLIO/ENEMY/USER_REJECT/AUTO_TRADE_DISABLED/EMERGENCY_STOP/OTHER) — 신규 분류 추가 시 `ALL_BLOCK_GATES` SSOT + 회귀 테스트 동시 수정 의무. UI 카드 / Auto Approval 정책 / Enemy 자동 차단 wiring 은 후속 PR 분리 (운영 데이터 누적 후).
- **Enemy Auto-Block 정책 SSOT** (ADR-0078): `server/trading/enemyAutoBlock.ts` 가 `EnemyCheckResult` (creditRate + individualDominance) 자동 차단 평가 단일 진입점. `evaluateEnemyAutoBlock(enemy, thresholds?)` SSOT — creditRate ≥ 12% **또는** individualDominance ≥ 88% (OR 결합) 시 `shouldBlock=true`. `source === 'UNAVAILABLE'` 또는 `null` → `shouldBlock=false` (KIS 일시 장애가 매매 차단 안 함). NaN/Infinity 안전 fallback. ENV 오버라이드 4종 (`ENEMY_CREDIT_RATE_BLOCK` / `ENEMY_CREDIT_RATE_WARN` / `ENEMY_INDIVIDUAL_BLOCK` / `ENEMY_INDIVIDUAL_WARN`) + 비상 회로 차단 (`ENEMY_AUTO_BLOCK_DISABLED=true`). 임계 변경 시 ADR-0078 §2.1 표 갱신 + 회귀 테스트 동시 수정 의무. **buyPipeline.ts 단일 호출자** — `fetchEnemyCheckData` 결과 직후 1회 호출, BLOCK 시 `Promise.resolve('SKIP')` 반환 + `markBlocked('ENEMY')` (ADR-0077 wiring) + `REJECTED`. 사용자 audit 7 카테고리 (보호예수 / 대주주 / 공매도 / 목표가 / 실적 / 섹터 헤지) 데이터 소스 확장은 별도 ADR.
- **ATR-Buffered BEP Glide SSOT** (ADR-0079): `src/services/quant/dynamicStopEngine.ts` `bepGlideStopPrice(entryPrice, atr14?)` SSOT — BEP_TRIGGER (+5%) 분기에서 단일 점프(`Math.round(entryPrice)`) 대신 `entryPrice − 0.5 × atr14` 글라이드 적용. `atr14 ≤ 0 / NaN / Infinity / undefined` → `entryPrice` fallback (기존 동작 100% 보존). `BEP_GLIDE_DISABLED=true` env → 강제 점프 (긴급 롤백). Lock-in 분기(+10%, `entryPrice × 1.03`) 는 본 ADR scope 외 — 변경 0. 변경 시 ADR-0079 §결정 표 갱신 + 회귀 테스트 동시 수정 의무. 서버 측 `server/trading/exitEngine/rules/atrDynamicStop.ts` 가 본 모듈을 직접 import 하여 LIVE 자동매매 BEP 정책에 자동 반영 — 절대 규칙 #4 무위반 (별도 import path 0건).
- **preOrderGuard final gate SSOT** (ADR-0081): `server/trading/preOrderGuard.ts` `assertSafeOrder(ctx)` 가 LIVE 주문 직전 final 게이트 — 모든 진입점 (cron / 텔레그램 `/buy` / HTTP `POST /auto-trade/tranches/run` / 외부 `runAutoSignalScan({forceBuyCodes})`) 이 본 SSOT 통과 강제. 가드 우선순위: (0-A) emergencyStop active → fireKillSwitch('EMERGENCY_STOP_ACTIVE') / (0-B) `getDailyLossPct() >= DAILY_LOSS_LIMIT` (env default 5%) → fireKillSwitch('DAILY_LOSS_LIMIT_HIT') / (1) POSITION_EXPLOSION / (2) STOPLOSS_LOGIC_BROKEN / (3) ORDER_LOOP_SUSPECT. 신규 가드 2개가 *최우선* — emergencyStop 상태에서 잘못된 주문이 다른 가드 incident 까지 발생시키는 노이즈 차단. NaN/Infinity 입력 시 비교 false → 통과 (state 손상 시 잘못된 차단 시그널 차단). 임계 변경은 ADR-0081 §결정 표 갱신 + 회귀 테스트 동시 수정 의무. 우회 차단 검증은 회귀 테스트 8 케이스가 모든 분기 가드.
- **stopApproachAlert BEP Glide 라벨 SSOT** (ADR-0079 PR-Z7 H1 follow-up): `server/trading/exitEngine/rules/stopApproachAlert.ts` `classifyStopSource(hardStopLoss, shadowEntryPrice, tolerancePct?, entryATR14?)` SSOT — 청산선 4 분기 분류 우선순위: (1) shadowEntryPrice ≤ 0/NaN → LOSS_STOP (보수) / (2) `|diffPct| ≤ 0.5%` 정확 BEP → BEP_PROTECTION / (3) `diffPct > 0` (수익 영역) → PROFIT_LOCK_IN / (4) **NEW: entryATR14 양수 + `BEP_GLIDE_DISABLED` env 비활성 + hardStopLoss ≥ entryPrice − 0.5×ATR (글라이드 영역) → BEP_PROTECTION 흡수** / (5) 그 외 → LOSS_STOP. ADR-0079 PR-Z6 적용 후 ATR=1500 / entryPrice=10000 → trailingStopPrice=9250 시 -7.5% 차이 LOSS_STOP 오분류로 운영자 패닉 매도 유도하던 결함 차단. `entryATR14` 미전달 시 글라이드 분기 우회 (기존 호출자 무영향). `BEP_GLIDE_DISABLED=true` env 시 글라이드 영역도 LOSS_STOP (기존 동작 회귀). `BEP_GLIDE_ATR_RATIO=0.5` 는 dynamicStopEngine.ts 와 동일 SSOT 상수.
- **Capital-Weighted Slot Accounting SSOT** (ADR-0080): `server/trading/slotAccounting.ts` `computeSlotConsumption(shadows, effectiveMaxPositions)` SSOT — 슬롯 점유 = `getRemainingQty(shadow) / shadow.originalQuantity` (0~1 clamp). 70% 익절 잔존 30% 포지션 5개 = 1.5 슬롯 점유 (기존 5 카운트 대신). `originalQuantity ≤ 0` (레거시) 또는 `NaN/Infinity` → 1.0 보수적 fallback. INTRADAY/PRE_BREAKOUT `watchlistSource` 회계 제외 (기존 정책 보존). `SLOT_CAPITAL_WEIGHTED_DISABLED=true` env → `consumed = rawCount` 강제 (단순 카운트 동치, 긴급 롤백). **세 호출 site 동시 wiring** (`signalScanner.ts:430` LIVE 상위 게이트 + `signalScanner/preflight.ts:365` Phase B 후속 + `signalScanner/perSymbolEvaluation.ts:283-289` 루프 게이트, ADR-0080 PR-S1) — drift 차단. 단, **루프 내 sizing 분모** (`perSymbolEvaluation.ts:900-903 remainingSlots`) 는 본 ADR PR-S2 까지 `currentActive=rawCount` 유지 — 분할 비율 영향이라 shadow mode 1주 검증 후 분리. POSITION_FULL 트리거 분모(`effectiveMaxPositions`) 는 본 ADR scope 외 — 변경 0. 로그 형식 `${consumed.toFixed(2)}/${effectiveMaxPositions}` 로 변경(자본 가중 표기, raw= 카운트 동반 노출). drift 차단 회귀 테스트 `perSymbolLoopGateWiring.test.ts` 가 단순 카운트 회귀 패턴 자동 검출.
- **UI Language SSOT 게이트키퍼** (ADR-0094 Phase A): `src/config/uiLanguage.ts` 의 `UI_LANG` 객체가 UI 카피 단일 출처 — nav/card/tier/regime/gate/empty/action 7 카테고리. 컴포넌트는 `useUILang()` 훅 (`src/hooks/useUILang.ts`) 으로 한 줄 패턴 진입 (`const t = useUILang(); t.nav('DISCOVER')`). 직접 `import { UI_LANG }` 는 배열 매핑/Object.entries 케이스 한정. 향후 KO/EN 토글 / A/B 테스트 / 사용자별 라벨 커스터마이징은 본 훅 내부만 수정 → 50+ 컴포넌트 무수정. `tier` 5-tier (VERIFIED/EXTERNAL/DELAYED/ESTIMATED/MANUAL) 는 ADR-0028 PR-A 의 3-tier 후속 격상 포인트 — 본 PR 은 SSOT 만 도입, DataQualityBadge 5-tier 사다리는 후속 PR-Phase-A-2. `empty` 4 sub-variant 는 Phase B `IDontKnow` 컴포넌트 사전 등록.
- **UI Language 금지 표현 정책** (ADR-0094 Phase A): `scripts/check_ui_language.js` 정적 검증 — 4 카테고리 (ABSOLUTE/AMBIGUOUS_SOURCE/EMOTIONAL/PROMISE) 금지 표현이 src/ 의 string/template literal 안에 등장하면 빌드 실패. 매칭은 literal 안에서만 — 식별자/주석 제외 (false positive 차단). 화이트리스트 5 파일 (`src/config/uiLanguage.ts` + 3 회귀 테스트 + `scripts/check_ui_language.js` 자기 자신). ENV 무관 — 정적 검증 항상 실행. `validate:all` + `precommit` 본체 통합. 페르소나 철학 ("Data-driven, no emotion" / 확률 사고 / 출처 명시) 위반 표현 영구 차단. 신규 컴포넌트가 SSOT 우회로 인라인 한국어 라벨을 작성해도 본 검증이 차단 → SSOT 강제. 본 PR 은 SSOT 도입만, 기존 50+ 컴포넌트 마이그레이션은 후속 PR (`--changed` 모드로 신규 코드만 강제).
- **Phase A DoD 회귀 테스트 묶음** (ADR-0094 #11 아이디어): `src/__tests__/uiLanguagePhaseA-DoD.test.ts` 가 Phase A 종료 선언 회귀 가드. 9 DoD 항목 (7 카테고리 export / 훅 contract / 5-tier / 6-RegimeLevel / 4-Gate / 4 IDontKnow sub-variant / 검증 스크립트 베이스라인 / ADR 문서 존재 / 모든 값 비어있지 않음). Phase B/C/D 진입 전 본 묶음이 green 인지 CI 가 확인 — 미래 PR 에서 Phase A SSOT 가 깨지면 자동 fail. commandRegistry validate:* 패턴 정합 (경계가 깨지면 빌드 실패).
- **DataQualityBadge 5-tier 자동 사다리** (ADR-0095 Phase A-2): `ConditionSourceTier` union 이 기존 3값 (COMPUTED/API/AI_INFERRED) + 신규 2값 (DELAYED/MANUAL) — *후방호환 의무*. `DataQualityCount` 의 `delayed?` / `manual?` 옵셔널 필드 — 기존 50+ 호출자 무수정. `classifyDataQuality` 자동 사다리: `dataSourceType === 'STALE'` → `delayed +1` (이전엔 aiInferred 흡수). 휴리스틱 모드는 STALE 만 자동 분류, 메타 모드는 5-tier 전체 정확 분류. `DataQualityBadge` 가 `useUILang().tier(...)` 사용 — UI_LANG.tier SSOT (ADR-0094) 정합. compact 모드는 delayed/manual 카운트 0 시 자동 생략 (UI 간결성 보존). 백엔드 5-tier 메타 wiring (enrichment.ts 가 DELAYED/MANUAL 부여) 은 후속 PR. 종합 등급 (HIGH/MEDIUM/LOW) 는 현재 computed/total 비율 그대로 — 5-tier 가중 산출은 후속 PR (DataQualityRibbon 도입 시).
- **IDontKnow 4-variant compound 컴포넌트** (ADR-0096 Phase B): `src/components/common/IDontKnow.tsx` — *"모름"* 의 4 종류를 시각적으로 분리한 compound component (`<IDontKnow.Delayed/Insufficient/Stale/Conflicted />`). 각 variant 다른 아이콘 (Clock/BarChart3/Moon/AlertCircle) + 다른 색상 (slate/zinc/blue/amber) + 다른 권고 액션. 메시지 SSOT — `useUILang().empty(...)` (UI_LANG.empty 4 sub-variant, ADR-0094 정합). compact (배지) vs 배너 모드 분기. `data-idontknow-variant` 속성 (e2e 친화). 페르소나 *"Stop-loss is operating cost"* 의 거꾸로 적용 — 모름도 정보, 정보의 비용. 기존 `OffHoursBanner` (≈ STALE) / `MarketDataStaleBadge` (≈ DELAYED) / `RecommendationWarningsBanner` (≈ INSUFFICIENT) 흡수는 후속 PR (각 사용처 PR 별도, 회귀 위험 격리).
- **DataQualityRibbon 페이지 누적 신뢰도 띠** (ADR-0096 Phase B): `src/components/common/DataQualityRibbon.tsx` — 페이지 상단 얇은 가로 띠로 *모든 표시 필드의 5-tier 누적 분포* 를 ambient 노출. `computeRibbonRatios(counts)` 순수 함수 SSOT — 5 카테고리 합산 + grade 산출 (`estimatedPct ≥ 0.50` WARN / `≥ 0.30` OK / 그 외 GOOD). 5 segment 색상 (emerald/cyan/slate/amber/violet) + ARIA `role="img"` + aria-label 5-tier 정합. `data-quality-ribbon-grade` 속성 (e2e 친화). DataQualityBadge (개별 필드) 와 책임 분리 — Ribbon 은 *페이지 전체*, Badge 는 *개별 필드*. CSS 변수 안 건드림 (침습성 낮음 — 페이지 상단 띠 1줄만). 사용처 임베드 (MarketOverviewHeader / DiscoverWatchlistPage) 는 후속 PR — 본 컴포넌트는 도입만.
- **VerdictCard V-E-R compound + Migration Gate** (ADR-0097 Phase C #6): `src/components/common/VerdictCard.tsx` — Verdict-Evidence-Risk 3 슬롯 compound. `variant='default'` (기본) → 기존 카드와 동일 작동, `variant='verdict'` 명시 시에만 V-E-R 모드. 3 슬롯 children 모두 부재 시 자동 placeholder 다운그레이드 — 회귀 위험 0. **Migration Gate** 는 사용자 #6 의 점진 도입 안전망: 50+ 기존 카드 무영향, 신규/마이그레이션 카드만 V-E-R. 라벨 SSOT — `useUILang().card / .regime` (UI_LANG ADR-0094 정합). `data-vcard-variant` / `data-vcard-verdict` / `data-vcard-regime` / `data-vcard-expired` 속성 (e2e). WatchlistCard 마이그레이션은 후속 PR (각 컴포넌트별 별도 — 회귀 위험 격리).
- **VerdictCard.Risk Stop-loss First 디자인 SSOT** (ADR-0097 Phase C #7): `<VerdictCard.Risk>` 슬롯의 폰트 위계는 *Stop-loss First* 정책 SSOT — 손절가 `text-2xl font-black` (가장 큰) > invalidation `text-sm` (두 번째) > targetPrice/scenarios `text-xs opacity-70` (가장 작은). 사용자 무의식 *감내 손실* 우선 인식 → 규율 형성. 행동경제학 loss framing 을 시스템 가치 (손절 규율) 에 정확히 정렬. 폰트 위계 변경은 ADR-0097 §"Risk slot — Stop-loss First" 표 갱신 + 회귀 테스트 동시 수정 의무 — 욕망 키우는 카드 디자인 (수익률/목표가 큰 폰트) 영구 차단.
- **TimeBand 신뢰 유효시간 띠 SSOT** (ADR-0097 Phase C #8): `src/components/common/TimeBand.tsx` — 1-2px 두께 progress bar 로 verdict 신뢰 유효시간 시각화. `computeRemainingPct(createdAt, expiresAt, now)` 순수 함수 SSOT — 진행률 ∈ [0, 1] clamp. `classifyTimeBandGrade(pct)` 4 분기 SSOT (FRESH >0.5 / AGING >0.2 / STALE >0 / EXPIRED =0) — 색상 emerald/amber/red/zinc. 임계값 변경은 ADR-0097 §"TimeBand 4 grade" 표 갱신 + 회귀 테스트 동시 수정 의무. ADR-0019 RecommendationSnapshot lifecycle (`createdAt` + `expiresAt` 30일 만료) 결합 — 백엔드 SSOT 가 UI 띠에 그대로 반영. `onExpire` 콜백 1회 호출 (parent 가 verdict 라벨 "재검증 대기" 전환 트리거). VerdictCard 의 자연 부분 — 별도 위젯 아님 (CSS 1-2px 라인). ARIA `role="progressbar"` 접근성. wiring (snapshot.createdAt/expiresAt 주입) 은 후속 PR.
- **ConfluenceMeter 4축 + 축 결손 자동 표기** (ADR-0098 Phase D #9): `src/components/common/ConfluenceMeter.tsx` — Fundamental/Flow/Technical/Macro 4 축 합치도 시각화. `classifyAxisScore(score)` 3 분기 SSOT (STRONG ≥7 / NEUTRAL 5~7 / WEAK <5, NaN/Infinity 안전 fallback → WEAK). `shouldShowReason(score)` SSOT — score < 5 시만 결손 사유 노출 (STRONG/NEUTRAL 미노출 — 사용자 시각 부담 ↓). reason 부재 시 "데이터 미수집" placeholder. `data-confluence-axis-reason` 속성 (e2e — 결손 사유 영역만 정확 카운트). `classifyConfluence(axes)` 4 분기 종합 등급 SSOT (CONVICTION ≥3 / CONFIDENT 2 / MIXED 1 / DIVERGENT 0 STRONG 축). compact (4 막대만) vs full (4 라인 + progressbar) 모드 분기. UI_LANG.confluence 카테고리 추가는 후속 PR 분리. Blueprint 원칙 6번 *"모름을 표현할 수 있어야 한다"* — 시스템이 자기 결론의 한계 자발적 노출.
- **ConfluenceMeter 단방향 결합 SSOT** (ADR-0098 Phase D #10): `ConfluenceMeter` 는 `VerdictCard` 를 import 하지 않는다 — 외부 의존성 0 (props 만). `<VerdictCard.Evidence>` slot 안 *자식으로 박힘* (Evidence → ConfluenceMeter 단방향). 어디서든 단독 사용 가능 (Screener 헤더 KPI / Backtest 결과 카드 / DiscoverPage Top 등). 코드베이스 commandRegistry / SymbolMarketRegistry 같은 작은 SSOT 부품 패턴 정합 — *어디든 박힐 수 있는 작은 부품*. 회귀 테스트가 VerdictCard 외부 단독 렌더 시 vcard 마커 (data-vcard-*) 부재 자동 검증 — 단방향 보장. 향후 다른 페이지 (Screener / Backtest) 재사용 시 복제 0건.
- **UI Verbosity 토글 SSOT** (ADR-0099 Phase Verbose #12): `useSettingsStore.uiVerbosity: 'minimal' | 'balanced' | 'verbose'` 영속 + `useUIVerbosity()` 게이트키퍼 hook. 가시성 분기는 `shouldShowAtVerbosity(content, verbosity)` 순수 함수 SSOT — 11 컨텐츠 키 (verdict/gate-mini/idontknow/evidence/risk/time-band/data-quality/confluence/axis-reasons/data-quality-ribbon/gate-status) × 3 verbosity 매트릭스. minimal → Verdict + 미니 게이트만 / balanced (기본) → V-E-R + DataQuality + IDontKnow / verbose → ConfluenceMeter + 축 사유 + Ribbon + 풀 게이트. 매트릭스 변경은 ADR-0099 §"가시성 분기 SSOT 매트릭스" 표 갱신 + 회귀 테스트 동시 수정 의무. 컴포넌트 wiring (각 컴포넌트가 useUIVerbosity().shouldShow(키) 호출) 은 후속 PR 점진 마이그레이션. `UIVerbosityToggle` 컴포넌트는 Settings 모달 / 페이지 헤더 임베드용 (3-way segmented control + ARIA radiogroup). 사용자 12 아이디어 마지막 항목 — Phase A~D 누적 다수 컴포넌트의 *정보 과잉* 안전장치.
- **exitOutcomeClassifier SSOT — BREAKEVEN 분류** (ADR-0112): `server/trading/exitOutcomeClassifier.ts` 가 청산 결과 WIN/LOSS/BE 분류 단일 진실. `classifyExitOutcome(returnPct, exitRuleTag, stopLossExitType)` 우선순위 SSOT 9 분기 — TARGET_EXIT/EUPHORIA_PARTIAL → WIN / PROFIT_PROTECTION + -0.5%~+0.5% → BE / ENTRY_CIRCUIT_BREAKER + BE band → BE / +1.0% 이상 → WIN / BE band 휴리스틱 → BE / -0.5% 미만 → LOSS. `ServerShadowTrade.exitOutcome?: 'WIN' | 'LOSS' | 'BE'` 옵셔널 필드 — 후방호환 (미설정 = LOSS 간주). `countRecentConsecutiveLosses` 가 `status === 'HIT_STOP' && exitOutcome !== 'BE'` 조건만 카운트 — 1차 로그(2026-04-30) PROFIT_PROTECTION -0.18~-0.45% 청산 3건 서킷 무한 루프 영구 차단. `aggregateFillStats` 의 `winFills/lossFills/beFills` 3-way 분류 — Win Rate = WIN/(WIN+LOSS) + Scratch Rate = BE/total 별도. ENV `BE_CLASSIFICATION_DISABLED=true` 우회. 청산 규칙(hardStopLoss/cascadeFinal/ma60DeathForceExit/legacyTakeProfit/trailingStop) 5개가 `updateShadow` 호출 시 `exitOutcome` 동시 영속. 부분매도 규칙(r6EmergencyExit/cascadeHalf/entryCircuitBreaker)은 status 무변경 — fill-level 분류만 의미. 사용자 트레이딩 철학 *"본절은 방향 실패가 아니라 타이밍 실패"* 직접 반영.
- **DriftTier 4단계 분류 + Corporate Action Detector** (ADR-0113): `server/utils/safePctChange.ts` `classifyDriftTier(absDriftPct): 'NORMAL' | 'WARN' | 'INVALID' | 'CORPORATE_ACTION'` SSOT — 25%/60%/150% 4 임계. `safePctChangeDetailed` 가 mode/sanityBoundPct 미명시 + ENV 미우회 시 4단계 분기 적용 — NORMAL 정상 / WARN 진단 마커 (valid=true) / INVALID 60~150% KIS 폴백 / CORPORATE_ACTION >150% 워치리스트 격리. mode 명시 (yahooQuoteAdapter `DAILY`/`RECOMMENDATION_RETURN`) 또는 sanityBoundPct override 시 4단계 분기 우회 — 호출자 의도 보존. `server/trading/corporateActionDetector.ts` SSOT — drift 패턴 분류 (`SPLIT`/`MERGE`/`RIGHTS`/`UNKNOWN`). > 150% drift → SPLIT (분할/병합/감자 강제 의심), 50~150% + windowDays=1 → RIGHTS (권리락 추정). `applyEntryPriceDrift` union 'CORPORATE_ACTION' 추가 — drift > 150% 감지 시 호출자 (perSymbolEvaluation) 가 entryPrice 자동 재설정 + `corporateActionAdjusted` 마커 + HIGH priority 텔레그램 (24h dedupeKey). 1차 로그 098460 고영 +221% / 336260 두산테스나 +207% drift 워치리스트 entryPrice 박제 결함 영구 차단. ENV `DRIFT_TIERED_SANITY_DISABLED` / `CORPORATE_ACTION_DETECTOR_DISABLED` 우회.
- **Corporate Action Guard 일봉 연속성 검증** (ADR-0518): ADR-0113 magnitude-only(>80%) false positive 차단 — 피엠티(147760) 수 주 정상 급등 (+99.6%) 이 분할/병합/권리락으로 오판되어 universe 제외 + HIGH 텔레그램. ADR-0303 "KIS 일봉 자동 검증 후속 PR" 정식 구현. `server/trading/corporateActionDetector.ts` 순수 SSOT `verifyDailyBarContinuity(candles, { sinceDate, thresholdPct })` — candles old→new, addedAt(sinceDate) 이후 인접 |close[i]/close[i-1]-1|*100 최대값 > 35% → CONFIRMED(gapDate) / ≤35% → GENUINE_RALLY / <2 bars → UNVERIFIABLE. 물리적 판별 근거 — KRX 일일 ±30% 상한/하한 → 정상 매매로는 하루 ±30% 초과 불가, corporate action ex-date 기계 조정만 단일일 갭 ±30% 초과 (`KRX_DAILY_PRICE_LIMIT_PCT=30` + 5% 마진 = `CORPORATE_ACTION_GAP_THRESHOLD_PCT=35`). ⚠️ RAW 일봉 필요 — `server/clients/kisChartDataFetcher.ts` `fetchKisChartData`/`fetchKisDailyCandles` 에 `opts.rawPrices` 옵션 추가 (`true` → `FID_ORG_ADJ_PRC '1'` 원주가, 갭 보존 / 미전달·false → 기존 `'0'` 수정주가, 분할 갭 소급 제거 — 51 caller 무영향). wiring — `entryPriceDrift.ts checkEntryPriceDrift` 가 drift>80% 의심 시 주입형 default-raw fetcher 로 RAW 일봉 조회 → CONFIRMED 는 기존 universe 제외 + HIGH 텔레그램 + DART 검토 / GENUINE_RALLY 는 benign `'REMOVE'` 강등 (corp-action 텔레그램·DART 프레이밍 제거, `waitDriftRemove` 카운터) / UNVERIFIABLE 은 기존 보수 동작 유지 (무회귀). 동기 `applyEntryPriceDrift`(watchlistManager) 는 일봉 미조회 → 무변경. 타입 — `DailyBarVerdictStatus`('CONFIRMED'|'GENUINE_RALLY'|'UNVERIFIABLE') + `DailyBarLike`/`DailyBarVerdict` + `isDailyBarVerificationDisabled()` + `activeCorporateActionGapThresholdPct()`. executionImpact=NONE (GENUINE_RALLY 도 워치리스트 제거 → 신규 매수 경로 0 신설), entryPrice RAW immutable(ADR-0115/0116) 무변경. ENV `CORPORATE_ACTION_DAILYBAR_VERIFY_DISABLED=true`(ADR-0157 정확 비교) 1줄 즉시 ADR-0113/0115 magnitude-only 동작 복원 + `CORPORATE_ACTION_GAP_THRESHOLD_PCT` 임계 override.
- **Data Trust Layer 3-tier SSOT** (ADR-0114): `server/data/dataTrustLayer.ts` 가 데이터 출처 신뢰도 3-tier 정책 단일 진실. **TRUTH (tier 1)** — `KIS_REAL_QUOTE` / `KIS_DAILY` / `KRX_OPENAPI` / `DART_FILING` (reconciliation_anchor, LIVE_TRADING / POSITION_RECONCILE / CORPORATE_ACTION 허용). **INDICATOR (tier 2)** — `YAHOO_FINANCE` / `NAVER_MOBILE` / `NAVER_FINANCE` / `GOOGLE_SEARCH` (derivation_input, SCREENING / BACKTEST / CHART_UI 만). **INTERPRETATION (tier 3)** — `GEMINI_AI` (narrative_only, NARRATIVE / REFLECTION / PRE_MORTEM 만 — 가격/수치 결정 절대 불가). `assertDataTrustLayer(source, intendedUse)` 위반 시 throw. `getDataTrustTier` / `getDataTrustCategory` / `evaluateDataTrustViolation` 진단 헬퍼. ENV `DATA_TRUST_ENFORCEMENT_DISABLED=true` 우회 시 throw 대신 console.warn. `scripts/check_data_trust.js` 정적 검증 — AI numeric price 직접 사용 (`gemini.targetPrice` 등) + LIVE 매매 경로 INDICATOR 가격 사용 패턴 차단. `validate:dataTrust` + `validate:all` (12종) + `precommit` 통합. ADR-0011/0015/0064/0094/0095/0098 의 부분 정책을 단일 SSOT 로 격상 — 사용자 분석 §"Yahoo 신뢰도 계층화" 직접 반영.
- **매수 차단 사유 진단 인프라 + `/scan_blockers` 텔레그램 명령** (ADR-0118): 사용자 보고 *"FOMC 다음날인데 매수 0건"* 직접 대응. `server/trading/signalScanner/scanDiagnostics.ts` `ScanCounters` 8 카운터 확장 (waitDataHold/preBreakout/gateFail/sizingBlocked/driftRemove/driftCorpAction/volumeDrop/other) + `ScanSummary.waitDistribution` + `ScanSummary.macroGateState` 옵셔널 영속 (후방호환). `buildWaitDistribution(counters)` SSOT — counters → distribution 변환. `buildMacroGateState(input)` SSOT — emergencyStop / autoTradeEnabled / regime+regimeKelly / fomcPhase+fomcKelly+finalKelly / vixGatingActive / bearDefenseMode / mhsBelow30 / watchlistEmpty / sellOnlyMode 통합. `formatScanBlockersMessage(summary)` SSOT — 거시 게이트 표시 + 종목별 분포 + 진단 추정 (emergencyStop / autoTradeEnabled / sellOnlyMode / watchlistEmpty / R6_DEFENSE / bearDefenseMode / DATA_HOLD 30%+ / Gate 미달 50%+ / Pre-breakout 50%+ / candidates=0 / 명백한 차단 게이트 부재) 자동 분기. `perSymbolEvaluation` 6 분기 wiring (DATA_HOLD / pre-breakout / Gate 재검증 / sizingTier BLOCKED / drift REMOVE / CORPORATE_ACTION) — Gate 재검증 분기는 revalResult.waitReason='DATA_HOLD' 시 waitDataHold 우선 카운트 + 그 외 waitGateFail. `/scan_blockers` 텔레그램 명령 (alias `/blockers`, `/why_no_buy`) 신규 — `getLastScanSummary` + `formatScanBlockersMessage` 사용. 운영자가 매수 0 건 시 즉시 차단 게이트 식별 가능. `server/utils/safePctChange.ts` `safePctChangeStrict(params)` 신규 SSOT — *경고기* 가 아닌 *거래 차단 게이트*. 4 status (`OK` / `STALE_BASE_OR_SPLIT_ADJUSTMENT` / `ZERO_OR_INVALID_PRICE` / `SOURCE_UNTRUSTED`) + 3 차단 플래그(blockTrading / blockDriftUpdate / blockSizing) 동시 반환. `server/types/dataQuality.ts` `DataQualityInfo` + `WaitReason` union (DATA_HOLD / SIZING_BLOCKED 신규) + `shouldBlockTradingByDataQuality(dq)` SSOT — entryEngine / sizingTier / orchestrator 최종 결정 직전 의무 사용. **wiring 4종**: ① `evaluateEntryRevalidation` extensionPct safePctChangeStrict 승격 → sanity 위반 시 `{ ok: false, dataQuality, waitReason: 'DATA_HOLD' }` 반환 (반환 타입 `EntryRevalidationResult` 확장). ② `applyEntryPriceDrift` union 'DATA_HOLD' 추가 — 호출자(perSymbolEvaluation) 가 stock.isDataQuarantined=true + dataQuality 영속 + WAIT 메시지 + failCount 미증가. ③ `WatchlistEntry` 옵셔널 2 필드 (`isDataQuarantined?` + `dataQuality?`). ④ `sizingTierDecider` 입력에 `dataQuality?` + 진입부 BLOCKED 분기 (`shouldBlockTradingByDataQuality` 호출). ENV `DATA_QUALITY_STRICT_DISABLED=true` 우회 — 위반 시 ok=true + warn 만 (ADR-0113 동작 복원). 사용자 18단계 §6 "Drift Sanity Hard Filter" + §15 "DATA_HOLD 상태" 직접 반영. 1차 로그 098460 +221% 시뮬: extensionPct 차단 → `[AutoTrade] 리노공업(058470) → WAIT / DATA_HOLD / DATA_HOLD_STALE_BASE_OR_SPLIT_ADJUSTMENT`. 후속 PR (orchestrator/autoTradeEngine 최종 wiring + 텔레그램/UI dataQuality 노출 + dropFromOpenPct/openGapPct strict 승격).
- **entryPrice RAW/ADJUSTED 분리 + Gate3 ENV 완화 wiring** (ADR-0116): `server/data/priceAdjustment.ts` SSOT — `getAdjustedPrice(rawPrice, factor): number` 분할 보정 (factor=1 default identity) + `getEntryPriceRawForRecord(trade)` 우선순위 (entryPriceRaw → shadowEntryPrice → signalPrice → 0) + `getEntryPriceAdjusted(trade)` RAW 추출 + factor 적용 + `getEntryPriceForDisplay(trade)` UI/리포트용 (adjusted 우선). NaN/Infinity/0/음수 모두 안전 fallback. **Schema 확장 — `ServerShadowTrade.entryPriceRaw?` + `cumulativeAdjustmentFactor?` (1.0 default) 옵셔널 필드**. `WatchlistEntry` 동일 옵셔널. `buildBuyTrade` 가 `entryPriceRaw = shadowEntryPrice` + `cumulativeAdjustmentFactor=1.0` 자동 영속 — RAW 불변 원장. **Gate3 ENV 완화 wiring** (`server/trading/signalScanner/revalidationSteps/entryRevalidationStep.ts`) — ADR-0115 의 `EXECUTION_RELAXATION_ENABLED=true` 시 `minGate = Math.max(getMinGateScore(regime) - 1, 5)` 으로 Gate3 7→6 완화. default OFF — 운영자 명시 활성화 시에만. Math.max 5 보장으로 과도 완화 차단. 사용자 18단계 §1 "RAW PRICE 절대 수정 금지" + §3 "adjusted = raw / factor" + §12 "Gate3 7→6 완화" 직접 반영. 본 PR 시점: factor=1 default identity 라 모든 호출자에서 adjusted = raw 동등 — 후속 PR (P3 Corporate Action Ledger) 가 cumulativeFactor 갱신 시 분할 보정 자동 적용.
- **entryPrice immutable + 실행 레이어 완화 SSOT** (ADR-0115): `server/trading/signalScanner/failureClassifier.ts` 가 진입 실패 사유의 Critical/Non-Critical 분류 SSOT. **CRITICAL (failCount++)** — SPLIT_ANOMALY / KIS_MISMATCH / STALE_BASE / TRADING_HALT / CORPORATE_ACTION (종목 자체 데이터·상태 문제). **NON_CRITICAL (WAIT)** — PRE_BREAKOUT_MISS / VOLUME_DECREASE / GATE_REVALIDATION_FAIL / DRIFT_WARN / ENTRY_PRICE_DEVIATION (일시적 조건 미충족, 다음 사이클 재시도). `shouldIncrementFailCount(reason)` 가 default 정책 적용 (Non-critical 미증가) — ENV `PRE_BREAKOUT_FAILCOUNT_DISABLED=false` 명시 시 ADR-0113 레거시 복원. **RAW immutable 원칙** — `perSymbolEvaluation.ts` 의 'CORPORATE_ACTION' 분기 default 동작은 *entryPrice 보존 + universe 제외* (ADR-0113 의 자동 재설정 제거). ENV `ENTRY_PRICE_AUTO_CORRECT_DISABLED=false` 명시 시에만 ADR-0113 동작 복원. ENV 4종 (`ENTRY_PRICE_AUTO_CORRECT_DISABLED` / `PRE_BREAKOUT_FAILCOUNT_DISABLED` / `EXECUTION_RELAXATION_ENABLED` / `DRIFT_INVALID_UNIVERSE_EXCLUDE`) 으로 회귀 안전 격리. 사용자 18단계 설계안 §1/§5/§10/§11/§16 직접 반영 — "탐지 엔진 문제가 아니라 *실행 승인 엔진 과긴축*. RAW PRICE 절대 수정 금지." 후속 PR (RAW/ADJUSTED 분리 / Corporate Action Ledger / 상태머신 SCAN-WATCH-READY-EXECUTE-HOLD-INVALID 정식 도입).
- **Data verification timing + DATA_HOLD role policy** (ADR-0128): 검증 타이밍을 매수 critical path 에서 분리 + DATA_HOLD 역할별 fallback SSOT. **타이밍 분리** — `server/data/dataVerificationBatch.ts` 가 *주 검증* (KST 평일 16:30 cron, 워치리스트 일괄, ScheduleClass `'TRADING_DAY_ONLY'`) + `server/data/dataVerificationIncremental.ts` 가 *증분 검증* (신규 매수 후보 진입 시점, NON-CRITICAL try/catch 격리). ADR-0117 `safePctChangeStrict` 가 매수 critical path 안전망 유지. **역할별 fallback SSOT** — `server/data/dataHoldRolePolicy.ts` `resolveDataHoldAction(role: 'BUY_CANDIDATE' | 'WATCHLIST' | 'HELD_POSITION'): DataHoldAction` 매트릭스. **HELD_POSITION blockSell=false 절대 규칙** — 보유 종목 exit 경로 영원히 보존 (KIS 손절은 검증 실패와 무관하게 작동, 절대 규칙 #4 정합). BUY_CANDIDATE 자동 탈락 (당일) / WATCHLIST 알림 보류 + UI PENDING_VERIFICATION / HELD_POSITION 매수+알림 차단 + exit 유지. **3회 실패 manual review** — `server/persistence/verificationQueueRepo.ts` `consecutiveFailures ≥ 3` 시 `manualReviewState='QUEUED'` + 텔레그램 알림 + 운영자 검토 의무. atomic write tmp→rename + FIFO 500 trim + ENV `VERIFICATION_QUEUE_DISABLED=true` no-op. **DART 이벤트별 lookback 차등** — `server/data/dartCorpEventClassifier.ts` `report_nm` 한국어 키워드 → 5 type (STOCK_SPLIT 90일 / MERGE 60 / RIGHTS_OFFERING 30 / NEW_SHARES 14 / GENERAL 7). ENV `DART_CORP_EVENT_LOOKBACK_DISABLED=true` 시 default 7일. **ENV 5종** — `DATA_HOLD_ROLE_POLICY_DISABLED` (모든 역할 안전 default) / `VERIFICATION_QUEUE_DISABLED` / `DATA_VERIFICATION_BATCH_DISABLED` / `DART_CORP_EVENT_LOOKBACK_DISABLED` / `DATA_QUALITY_STRICT_DISABLED` (ADR-0117 기존). 사용자 18단계 §6 / §15 / §16 + 사용자 명시 6 정책 직접 반영. 후속 PR (signalScanner / autoPopulateWatchlist 본체 wiring + perSymbolEvaluation 의 DATA_HOLD 분기에서 `resolveDataHoldAction` 위임 + `/verification_queue` manual review 텔레그램 명령).
- **Shadow-Only Entry Liberalization (Gate1 점수 + Gate3 타이밍 이중 개방)** (ADR-0619): shadow paper-fill 진입이 live 와 이중 게이트(`entryEngine.ts:370` `quoteSignalType==='SKIP' || quoteGateScore<minGate`)를 공유해 상승장 shadow-fill 0 — 불변식 #8(실거래 차단↔shadow 차단 분리) 진입 경로 복원. **Gate1 점수 완화** = ADR-0608 seam(`resolveEntryMinGateScore`+`GATE1_REGIME_AWARE_SHADOW_ENTRY_ENABLED`+REGIME_AWARE_SHADOW floor-5 우회) **활성만 처방**(코드 변경 0·두 번째 임계식 금지). **Gate3 타이밍 완화** = `:370` 앞 절 `(signalType==='SKIP' && !shadowTimingBypass)` 재구조화, shadowTimingBypass = 신규 ENV `SHADOW_PREBREAKOUT_ENTRY_ENABLED`(=== 'true' default OFF) && `isShadow` && skipCause∈{BAND_MISS,MTAS_WEAK}(VIX/연속손절홀드/DATA_HOLD/SKIPPED_POLICY_BLOCK 절대 bypass 불가·UNKNOWN 보수 차단). skipCause 는 quantFilter signalType 산출 시점에 enum 동시 반환(details 파싱 금지). bypass 후에도 `score<minGate`·sanity·DATA_HOLD 전부 평가(두 완화 직교). live byte-identical = isShadow false∥ENV OFF → bypass 항상 false → `(SKIP&&!false)=SKIP` 연산 동치. 라벨 격리 `entryThresholdMode` 유니언에 `'PREBREAKOUT_SHADOW'` additive(학습 표본 층화·청산/사이징/LIVE 판정 0). 관측 ledger `shadowEntryLiberalizationLedgerAdr0619.ts`(ADR-0614 패턴·`SHADOW_ENTRY_LIBERALIZATION_LEDGER_FILE`·스캔당 liberalizedEntryCount/gate1·gate3LiberalizedCount/skipCauseDist). executionImpact OFF=NONE byte-identical(autoTradeEngine·kisClient·buyPipeline 실주문·SourceSnapshot·getMinGateScore live 분기 0줄)/ON=shadow paper-fill 확대(의도). 두 flag 독립·각 ENV 1줄 롤백. 불변식 #1/#2/#4/#7/#8 보존.
- **Shadow Learning Persistence (3 모듈 통합)** (ADR-0173): 사용자 §"Shadow 학습 지속성 1차 고도화 패치" — 매매 금지일·휴일·서버 장애·API 실패 시 신규 학습 샘플 생성 차단 결함을 차단하는 Phase 1 SSOT 인프라 (3 모듈 통합 + 4 Phase 시리즈 분리). 모든 모듈 Phase 1 *호출자 0건 dead code* — ENV default OFF + LIVE 매매 본체 0줄 변경. 4 Phase 시리즈: Phase 1 (본 PR, 3 SSOT 인프라) → Phase 2 (Safety Gate Attribution + Shadow vs Live Delta Report + future return resolve cron, P1) → Phase 3 (ReflectionInjectionBus + 5 early-return wiring + ENV 활성화, **LIVE 결합** P0) → Phase 4 (Learning Sanity Dashboard 11 지표 UI, P2). ADR-0162/0167 Phase 분리 패턴 차용.

- **MissedLearningQueue** (ADR-0173 §1): 휴일·서버 장애·배포·API 실패 시 스킵된 학습 작업을 다음 영업일에 안전하게 복구하는 SSOT. **신규 모듈** `server/learning/missedLearningQueue.ts` (`enqueueMissedLearningJob` + `replayMissedLearningJobs` + `dropStaleJobs`) + `server/persistence/missedLearningQueueRepo.ts` (영속 SSOT, atomic write tmp→rename + 손상 JSON 빈 배열 fallback + FIFO 1000 trim) + `data/missed-learning-queue.json` 영속 (`paths.ts` `MISSED_LEARNING_QUEUE_FILE`). **타입 SSOT** `MissedLearningJobName` 7 union (counterfactual_resolve / ledger_resolve / ghost_portfolio / nightly_reflection / daily_mini_backtest / shadow_live_delta_report / safety_gate_attribution) + `MissedLearningReason` 7 union (KRX_HOLIDAY / SERVER_DOWN / DEPLOYMENT / API_FAILURE / MARKET_DATA_MISSING / TIMEOUT / MANUAL_SKIP) + `ReplayPolicy` 4 union (SAFE_NEXT_TRADING_DAY / REPLAY_IMMEDIATELY / DROP_IF_STALE / MANUAL_REVIEW). **안전 invariant**: (1) **`idempotencyKey` 멱등성** — 동일 키 enqueue 중복 차단. (2) **14일 STALE 자동 DROP_IF_STALE** + replay 실패 시 `FAILED` + 재시도 횟수 제한 (default 3). (3) **KIS 주문 함수 import 절대 금지** — 학습 복구 전용, LIVE 주문 경로 결합 영구 차단 (정적 grep 가드). **ENV `MISSED_LEARNING_QUEUE_ENABLED`** default OFF — Phase 2 cron wiring 후 활성화. 진단 로그 3종 (enqueue / replay / drop).

- **ShadowLearningOnlyScan** (ADR-0173 §2): 매매 금지일 (FOMC/VIX/R0/R1/Liquidity/Manual/KRX_HOLIDAY_REPLAY/DataDegradation) 에도 *실제 주문 없이* Shadow 판단 샘플을 생성하는 SSOT. **신규 모듈** `server/trading/shadowLearningOnlyScan.ts` — `runShadowLearningOnlyScan(input)` 진입점 (Phase 1 호출자 0건 dead code) + `ShadowLearningOnlyScanReason` 8 union (FOMC_BLOCK / VIX_SPIKE / RISK_OFF_REGIME / R0_CRISIS / R1_DEFENSIVE / KRX_HOLIDAY_REPLAY / LIQUIDITY_BLOCK / MANUAL_BLOCK) + `ShadowLearningOnlySignal` 영속 schema 15+ 필드 (symbol / signalDate / blockedReason / wouldHaveBought / hypotheticalEntryPrice / hypotheticalStopLoss / hypotheticalTargetPrice / signalGrade / gateScore / regime / macroBlockReason / dataQualityStatus / futureReturn1d/3d/5d/20d / outcome) + `isShadowLearningOnBlockedDaysEnabled()` ENV 헬퍼. **안전 invariant 절대 규칙**: (1) **KIS 주문 함수 import 금지** — `placeKisMarketOrder` / `placeKisSellOrder` / `cancelKisOrder` / `placeKisStopLossOrder` / `placeKisTakeProfitOrder` / `tranchesRouter` 등 주문 경로 모두 import 0건 (정적 grep 가드 회귀 테스트로 강제). (2) **`allowRealOrder=false` 만 허용** — runtime throw + TypeScript literal type 2중 강제, `true` 또는 미전달 시 즉시 fail (LIVE 주문 경로 격리 invariant). (3) **`bypassMacroEntryBlock`** — 호출자가 boolean 명시 (`true` 매크로 우회 의도 명시 / `false` 평소 매크로 게이트 통과). (4) **`runAutoSignalScan` 직접 호출 금지** — 정적 grep 가드 (signalScanner → 본 모듈 → signalScanner 무한 루프 차단). (5) **데이터 품질 / 가격 sanity 우회 금지** — `safePctChangeStrict` (ADR-0117) + `getDataTrustTier` (ADR-0114) 통과 의무. (6) **학습 영속 SSOT 만 사용** — `counterfactualShadow.recordCounterfactual` / `rejectionShadowTracker.recordRejection` / `shadowTradeRepo` / MOMENTUM 학습 / `entryConditionScores` 5 채널 (Phase 2/3 wiring 시 호출). **ENV `SHADOW_LEARNING_ON_BLOCKED_DAYS_ENABLED`** default OFF (`=== 'true'` 정확 비교) — 운영자 명시 활성화 의무. ENV OFF 시 즉시 `{ skipped: true, reason: 'ENV_DISABLED' }` 반환 + 외부 호출 0건. signalScanner.ts / kisClient/** / orchestrator/** / autoTradeEngine 본체 0줄 변경.

- **LearningFreshnessGuard** (ADR-0173 §5): 학습 데이터 시간 감쇠 + regime 불일치 감쇠 SSOT. **신규 모듈** `server/learning/learningFreshnessGuard.ts` — `applyFreshnessDecay(lesson, currentRegime?, now?)` 순수 함수 + 임계 SSOT 3종 (`FRESHNESS_DECAY_14D_RATIO=0.3` / `FRESHNESS_EXPIRY_30D=0` / `REGIME_MISMATCH_RATIO=0.5`) + `isFreshnessGuardEnabled()` ENV 헬퍼. **결정 트리 우선순위**: (1) `ageDays > 30` → `weight=0` (만료, 절대 규칙) → (2) `ageDays > 14` → `weight × 0.3` (감쇠) → (3) `lesson.regime !== currentRegime` (둘 다 정의 시) → `weight × 0.5` (regime 불일치). 다중 조건 동시 충족 시 *곱셈 결합* (예: 14일 + regime 불일치 → 0.3 × 0.5 = 0.15). **적용 대상 5 lesson 타입** (Phase 3 ReflectionInjectionBus 결합) — `recentReflections` / `conditionLessons` / `biasHeatmap` / `counterfactualLessons` / `gateAttributionLessons`. **ENV `LEARNING_FRESHNESS_GUARD_ENABLED`** default OFF — Phase 3 wiring 후 활성화. **외부 의존성 0** — 순수 함수, fs/외부 API/zustand store 모두 미사용 (단위 테스트 격리 보장).

- **SafetyGateAttribution** (ADR-0174 §2.1): Phase 2a 영속 분석 SSOT — 7 게이트 (FOMC / VIX / R0_R1_REGIME / LIQUIDITY / DATA_SANITY / EXPOSURE_BUDGET / ENEMY_CHECKLIST) 의 사후 효과 계산. **신규 모듈** `server/learning/safetyGateAttribution.ts` — `computeSafetyGateAttribution(signals, options?)` 순수 함수 (Phase 1 `loadShadowLearningOnlySignals()` 영속 read-only) + `isSafetyGateAttributionEnabled()` ENV 헬퍼. **타입 SSOT** `GateName` 7 union + `GateAttributionResult` (gate / avoidedLoss / missedGain / netGateImpact / blockedWinnerCount / blockedLoserCount / gatePrecision / sampleSize). **게이트 매핑** Phase 1 `ShadowLearningOnlyScanReason` 8 union → 5 GateName (FOMC_BLOCK→FOMC / VIX_SPIKE→VIX / RISK_OFF_REGIME+R0_CRISIS+R1_DEFENSIVE→R0_R1_REGIME 통합 / LIQUIDITY_BLOCK→LIQUIDITY / MANUAL_BLOCK+KRX_HOLIDAY_REPLAY 제외). 잔여 2 게이트 (DATA_SANITY / EXPOSURE_BUDGET / ENEMY_CHECKLIST) 는 본 PR sampleSize=0 dead — Phase 3 ReflectionInjectionBus wiring 시 호출자가 추가 reason 컨텍스트 전달 후 활성화. **해석**: `netGateImpact > 0` → 게이트 손실 방지 / `< 0` → 과보호 가능성 / `blockedWinnerCount` 급증 → 게이트 완화 후보 / `blockedLoserCount` 급증 → 게이트 유지/강화. **안전 invariant 4종**: (1) **외부 의존성 0** — fs/외부 API/zustand 미사용 (호출자 read 결과만 입력). (2) **KIS 주문 함수 import 절대 금지** — 정적 grep 가드 (학습 분석 전용). (3) **호출자 0건 (Phase 2a dead code)** — Phase 4 Dashboard / Phase 2b cron read 에서 호출 예정. (4) **분석 결과 cache 영속 금지** — 순수 함수만, stale data 결함 차단. **ENV `SAFETY_GATE_ATTRIBUTION_ENABLED`** default OFF — Phase 4 Dashboard 또는 Phase 2b cron 활성화 후 운영자 명시 활성화 의무.

- **ShadowVsLiveDelta** (ADR-0174 §2.2): Phase 2a 영속 분석 SSOT — Shadow 가정 매수와 LIVE 실제 매수의 missedAlpha (= shadowReturn - liveReturn) 5 카테고리 분류. **신규 모듈** `server/learning/shadowVsLiveDelta.ts` — `computeShadowVsLiveDelta({shadowSignals, liveTrades, options?})` 순수 함수 (Phase 1 `ShadowLearningOnlySignal` + ServerShadowTrade 영속 read-only) + `isShadowVsLiveDeltaEnabled()` ENV 헬퍼. **타입 SSOT** `DeltaCategory` 5 union (SHADOW_BUY_LIVE_BLOCKED / LIVE_BUY_SHADOW_BETTER_SIZE / EXPOSURE_CAP_REDUCED / MACRO_GATE_BLOCKED / LIQUIDITY_GATE_BLOCKED) + `DeltaCategoryResult` (category / sampleSize / shadowReturnSum / liveReturnSum / missedAlpha / missedAlphaAvg). **5 카테고리 분류**: (1) SHADOW_BUY_LIVE_BLOCKED — `signal.wouldHaveBought=true` AND LIVE trade 부재 (KST 일자 매칭). (2) LIVE_BUY_SHADOW_BETTER_SIZE — Phase 1 `sizingEngineSnapshot` (ADR-0162) 비교 plumbing only (Phase 3 wiring 후 활성). (3) EXPOSURE_CAP_REDUCED — LIVE trade `cappedByBudget=true` (ADR-0166). (4) MACRO_GATE_BLOCKED — `signal.blockedReason ∈ {FOMC_BLOCK / VIX_SPIKE / RISK_OFF_REGIME / R0_CRISIS / R1_DEFENSIVE}`. (5) LIQUIDITY_GATE_BLOCKED — `signal.blockedReason === 'LIQUIDITY_BLOCK'`. **해석**: `missedAlpha > 0` → 보수적 차단으로 *돈을 못 번 경우* (기회 손실) / `< 0` → *살아남은 효과* (Shadow 가 샀더라면 손실). **안전 invariant 4종** SafetyGateAttribution 동일 — 외부 의존성 0 + KIS 주문 import 0건 + 호출자 0건 dead code + cache 영속 금지. **ENV `SHADOW_LIVE_DELTA_REPORT_ENABLED`** default OFF — Phase 4 Dashboard 또는 Phase 2b cron 활성화 후 운영자 명시 활성화 의무.

- **Unresolved Counterfactuals Card** (ADR-0182): Phase 4-B-2-b3 — Phase 4-B-2-b 시리즈 완주 (3/3). counterfactualShadow 학습 입력 진단 가시화. **신규 SSOT** `summarizeUnresolvedCounterfactuals(now?)` (`server/learning/counterfactualShadow.ts` +103 LoC, 기존 함수 시그니처 무수정 — *additive*) — `loadCounterfactuals()` 영속 read 후 9 필드 (totalCount / resolved30d/60d/90d / pending30d/60d/90d / awaitingHorizonCount / oldestSignalDate) 산출. **신규 endpoint** `GET /api/learning/counterfactual-unresolved-stats` (`learningRouter.ts` +14 LoC). **클라 SDK 확장** `learningDashboardClient.ts` +`fetchCounterfactualUnresolvedStats()` + `ClientCounterfactualUnresolvedStats` 동기 사본 (절대 규칙 #3 준수). **신규 카드** `UnresolvedCounterfactualsCard.tsx` — 4 metric grid (전체 / 30d 해결 / 30d 미해결 / 대기) + `classifyUnresolvedTone(pending30dCount, totalCount)` SSOT 헬퍼 5 분기 (totalCount=0 → zinc 데이터 없음 / pending30dCount ≥5 → rose cron 미실행 의심 / pending30dCount ≥1 → amber 경계 / totalCount ≥5 → emerald cron 정상 / 그 외 zinc 표본 부족) + horizon 별 진행률 (30d/60d/90d, `resolved/(resolved+pending)` %) + oldestSignalDate KST footer + totalCount=0 placeholder. **페이지 임베드** 6번째 카드 (`xl:grid-cols-3` 그대로 — 6 카드 = 2행 3 reflow). **Phase 4-B-2-b 분할 정책** — b1 (Rejected Winners, PR #540) / b2 (Stale Reflections, PR #541) / b3 (본 PR, Unresolved Counterfactuals) — *시리즈 완주*. **안전 invariant 5종**: (1) **LIVE 매매 본체 0줄 변경**. (2) **KIS 주문 함수 import 0건** (정적 grep 가드). (3) **기존 endpoint + 카드 무수정** — ADR-0177/0178/0179/0180/0181 모두 git diff 무변경. (4) **read-only GET** — 신규 endpoint POST/PUT/DELETE 0건 + 카드 trigger 버튼 0건. (5) **서버↔클라 직접 import 금지** — 클라 SDK 타입 동기 사본. **잘못된 해결 방법 영구 차단 5종** (ADR-0182 §4) — `counterfactualShadow` 본체 변경 (기존 함수 시그니처 무수정, *additive* 만 허용) / `getCounterfactualStats(horizon)` 결과 재사용 (분포 분석 ≠ 미해결 진단) / 카드에 manual resolve 트리거 버튼 / ENV gate 추가 / Phase 3 의존 지표 통합. **운영 효과** — 운영자 사이드바 1 클릭으로 *unresolved counterfactuals* (학습 입력 진단) 즉시 확인 + `pending30dCount ≥ 5` rose tone alert 로 counterfactual_resolve cron 미실행 또는 가격 fetcher 실패 운영자 인지 트리거 + `oldestSignalDate` 표시로 영속 누적 진행도 가시화 + Phase 4-B-2-b 시리즈 완주 (3/3) → Phase 4-B-2-c 진입 가능 (reflection injection rate / learning freshness score, Phase 3 wiring 후).

- **Stale Reflections Card** (ADR-0181): Phase 4-B-2-b2 — 사용자 §7 잔여 5 지표 중 2번째 (reflectionImpactPolicy silent/deprecated 자연선택). **신규 endpoint 0건** — ADR-0084 기존 `/api/learning/reflection-impact?days=N` 재사용. **클라 SDK 확장** `learningDashboardClient.ts` +`fetchReflectionImpact()` + `ClientReflectionImpactSummary` / `ClientReflectionModuleReport` / `ClientReflectionModuleStatus` 4 union 동기 사본 (절대 규칙 #3 준수). **신규 카드** `StaleReflectionsCard.tsx` — 4 status 카운트 grid (normal ✅ / grace ⏳ / silent ⚠️ / deprecated ❌) + `classifyStaleTone(staleCount, graceCount, total)` SSOT 헬퍼 4 분기 (total=0 zinc 데이터 없음 / 모든 모듈 grace zinc 표본 부족 / silent+deprecated ≥3 rose 자연선택 활발 / ≥1 amber 경계 / 0 emerald 모든 모듈 정상) + Top 5 모듈 (silent/deprecated 우선, 그 외 impactRate 오름차순) 노출 + 윈도우 N일 footer. **페이지 임베드** 5번째 카드 (`xl:grid-cols-3` 그대로 — 5 카드 = 1행 3 + 2행 2 reflow). **Phase 4-B-2-b 분할 정책** — b1 (Rejected Winners, PR #540 머지) / b2 (본 PR, Stale Reflections) / b3 (Unresolved Counterfactuals, 별도 PR). **안전 invariant 5종**: (1) **LIVE 매매 본체 0줄 변경**. (2) **서버 코드 0줄 변경** — UI-only PR, ADR-0084 endpoint 재사용. (3) **기존 endpoint + 카드 무수정** — ADR-0084/0177/0178/0179/0180 모두 git diff 무변경. (4) **서버↔클라 직접 import 금지** — 클라 SDK 타입 동기 사본. (5) **read-only** — 카드에 모듈 강제 deprecation 트리거 버튼 0건. **잘못된 해결 방법 영구 차단 5종** (ADR-0181 §4) — Phase 4-B-2-b3 통합 / 신규 endpoint 추가 / `reflectionImpactPolicy` 본체 변경 / silent/deprecated 자동 wiring 차단 (ADR-0084 §"잠재 영구화 정책") / 카드 trigger 버튼 추가. **운영 효과** — 운영자 사이드바 1 클릭으로 *stale reflections* (silent + deprecated 카운트) 즉시 확인 + 13 모듈 중 ≥3건 silent/deprecated 시 rose tone alert + Phase 4-B-2 시리즈 진행도 2/3 → 3/3 (b3 만 잔여).

- **Rejected Winners Card** (ADR-0180): Phase 4-B-2-b1 — 사용자 §7 잔여 6 지표 중 1번째 (rejection shadow tracker false negative rate). **신규 endpoint** `GET /api/learning/rejection-shadow-stats` (`learningRouter.ts` +12 LoC) — `summarizeRejectionShadow()` (`server/learning/rejectionShadowTracker.ts` SSOT, ADR-0173 Phase 1 영속) 호출 + `RejectionShadowSummary` 그대로 (totalCount / closedCount / activeCount / avgClosedReturnPct / medianClosedReturnPct / falseNegativeRate / quartiles / reliable). 기존 `/api/learning/rejection-shadow` 와 분리 — entries 제외, summary 전용. **클라 SDK 확장** `learningDashboardClient.ts` +`fetchRejectionShadowStats()` + `ClientRejectionShadowSummary` 동기 사본. **신규 카드** `RejectedWinnersCard.tsx` — 4 metric grid (closedCount / winnerCount=round(falseNegativeRate × closedCount) / falseNegativeRate (%) / reliable ✓/✗) + tone 분기 (`reliable=false` zinc 표본 부족 / `falseNegativeRate ≥ 0.3` rose 게이트 완화 후보 / `< 0.1` emerald 게이트 정합 / 그 외 amber 경계) + closedCount=0 placeholder + activeCount > 0 시 "추적 중 N건" 라벨. **페이지 임베드** 4번째 카드 (`xl:grid-cols-3` 그대로 — 4 카드 = 1행 3 + 2행 1 reflow). **Phase 4-B-2-b 분할 정책** — b1 (본 PR, Rejected Winners) / b2 (Stale Reflections, 별도 PR) / b3 (Unresolved Counterfactuals, 별도 PR). **안전 invariant 5종**: (1) **LIVE 매매 본체 0줄 변경**. (2) **KIS 주문 함수 import 0건** (learningRouter / 신규 카드 정적 grep 가드). (3) **기존 endpoint + 카드 무수정** — ADR-0177/0178/0179 모두 git diff 무변경. (4) **read-only GET** — 신규 endpoint POST/PUT/DELETE 0건 + 카드 trigger 버튼 0건. (5) **서버↔클라 직접 import 금지** — 클라 SDK 타입 동기 사본. **운영 효과** — 운영자 사이드바 1 클릭으로 *rejected winners* (거짓 부정 — 게이트가 너무 엄격해서 놓친 종목) 즉시 확인 + `falseNegativeRate ≥ 0.3` rose tone alert 로 게이트 완화 후보 운영자 인지.

- **MissedLearningQueue Stats Card** (ADR-0179): Phase 4-B-2-a — 사용자 §7 잔여 9 지표 시작 MVP. **신규 endpoint** `GET /api/learning/missed-learning-queue-stats` (`learningRouter.ts` +33 LoC) — `loadMissedLearningQueue()` 영속 read + 4 status (PENDING/REPLAYED/FAILED/DROPPED) 카운트 집계 + total + lastEnqueuedAt (skippedAt 최대). ENV gate 부재 — Phase 1 영속 read-only (빈 큐 시 모든 카운트 0). **클라 SDK 확장** `learningDashboardClient.ts` +`fetchMissedLearningQueueStats()` + `ClientMissedLearningQueueStats` 동기 사본. **신규 카드** `MissedLearningQueueStatsCard.tsx` — 4 status grid (StatCell 4종) + tone 분기 (`pending > 0` amber alert / `failed > 0` rose alert / `replayed` emerald / `dropped` zinc) + lastEnqueuedAt KST 시각 + total === 0 placeholder. **페이지 grid 격상** `lg:grid-cols-2` → `xl:grid-cols-3` (3 카드 동시 표시). **Phase 4-B-2 분할 정책** — 4-B-2-a (본 PR, MissedLearningQueue stats) / 4-B-2-b (Counterfactual + Rejection + Reflection 통계, 별도 PR) / 4-B-2-c (Phase 3 결합 — reflection injection rate + learning freshness score, Phase 3 wiring 후). **안전 invariant 5종**: (1) **LIVE 매매 본체 0줄 변경**. (2) **KIS 주문 함수 import 0건** (정적 grep 가드). (3) **기존 endpoint + 카드 무수정** — ADR-0177 endpoint 2종 + ADR-0178 카드 2종 모두 git diff 무변경. (4) **read-only GET** — 신규 endpoint POST/PUT/DELETE 0건 + 카드에 trigger 버튼 0건. (5) **서버↔클라 직접 import 금지** — 클라 SDK 타입 동기 사본.

- **Learning Sanity Dashboard UI** (ADR-0178): Phase 4-B-1 — ADR-0177 endpoint 의 첫 UI 호출자. **클라 SDK** `src/api/learningDashboardClient.ts` 신규 — 서버 타입 동기 사본 (절대 규칙 #3 준수, `ClientGateAttributionResult` / `ClientDeltaCategoryResult` / `ClientGateName` 7 union / `ClientDeltaCategory` 5 union) + `fetchSafetyGateAttribution(opts?)` / `fetchShadowVsLiveDelta(opts?)` 2 함수 + `ALL_GATE_NAMES` / `ALL_DELTA_CATEGORIES` SSOT export. **2 핵심 카드 컴포넌트** — `src/components/learning/SafetyGateAttributionCard.tsx` (7 GateName grid + netGateImpact tone 분기 emerald/rose/zinc + 표본 0건 placeholder) + `src/components/learning/ShadowVsLiveDeltaCard.tsx` (5 DeltaCategory grid + missedAlpha tone 분기 emerald/rose/zinc + placeholder). **신규 페이지** `src/pages/LearningSanityDashboardPage.tsx` — TanStack Query useQuery 2건 (60s staleTime + refetchInterval, retry 2) + 2 카드 grid 임베드. **View 등록 4 위치** — `useSettingsStore.View` +`'LEARNING_SANITY'` / `viewRegistry.VIEW_LABELS.LEARNING_SANITY = 'Learning Sanity'` / `navigation` 인텔리전스 그룹 + Activity 아이콘 / `PageRouter` view 분기 추가. **안전 invariant 5종**: (1) **LIVE 매매 본체 0줄 변경**. (2) **서버 코드 0줄 변경** — UI-only PR. (3) **서버↔클라 직접 import 금지** — 타입 동기 사본. (4) **기존 페이지 무수정** — ShadowLearningPage / RecommendationHistoryPage 등 git diff 무변경. (5) **데이터 0건 placeholder** — endpoint 빈 배열 응답 시 카드 placeholder 표시. **Phase 4-B-2 후속 PR** — 사용자 §7 잔여 9 지표 (skipped jobs / replayed / failed replay / unresolved counterfactuals / stale reflections / rejected winners / gate opportunity cost / reflection injection rate / learning freshness score). **운영 효과** — 운영자 사이드바 *인텔리전스 → Learning Sanity* 클릭 1회로 2 핵심 지표 즉시 확인 + 데이터 누적 시 자연 가시화.

- **Learning Sanity Dashboard HTTP endpoint** (ADR-0177): Phase 4-A — ADR-0174 Phase 2a 분석 SSOT (`computeSafetyGateAttribution` + `computeShadowVsLiveDelta`) 의 *호출자 0건 dead code* 첫 활성화. `learningRouter.ts` 에 신규 endpoint 2종 추가 — `GET /api/learning/safety-gate-attribution?days=N&horizon=H` (`loadShadowLearningOnlySignals()` + `computeSafetyGateAttribution(signals, options)` 호출, 응답 `GateAttributionResult[]` 7 GateName entry) + `GET /api/learning/shadow-vs-live-delta?days=N&horizon=H` (`loadShadowLearningOnlySignals()` + `loadShadowTrades()` + `computeShadowVsLiveDelta({shadowSignals, liveTrades, options})` 호출, 응답 `DeltaCategoryResult[]` 5 DeltaCategory entry). 쿼리 파라미터 `days` (1~365 default 90) + `horizon` (1/3/5/20 default 5). 기존 8 endpoint (`/status` / `/history` / `/reflection-impact` / `/walk-forward` / `/condition-lifecycle` / `/rejection-shadow` / `/twin-portfolio` / `/condition-attribution-shadow` / `/shadow-walk-forward`) 패턴 정합. **안전 invariant 5종**: (1) **LIVE 매매 본체 0줄 변경** — signalScanner/entryEngine/exitEngine/kisClient/orchestrator/autoTradeEngine 무수정. (2) **KIS 주문 함수 import 0건** (learningRouter 정적 grep 가드). (3) **기존 라우터 무수정** — 기존 8 endpoint 모두 git diff 무변경. (4) **read-only** — GET 메서드만, 영속 write 0건. (5) **ENV default OFF** — Phase 2a SSOT 내부 ENV gate (`SAFETY_GATE_ATTRIBUTION_ENABLED` / `SHADOW_LIVE_DELTA_REPORT_ENABLED`) 자동 적용, ENV OFF 시 응답 빈 배열 `[]`. **Phase 4-B 후속 PR** — UI 컴포넌트 11 지표 (LearningSanityDashboard.tsx + SafetyGateAttributionCard / ShadowVsLiveDeltaCard) + 클라 SDK (`src/api/learningDashboardClient.ts`). **운영 효과** — 운영자 SHADOW 검증 시 `curl /api/learning/safety-gate-attribution` 등 read-only 직접 확인 + Phase 4-B UI 의 데이터 입력 인프라 마련.

- **MissedLearningQueue cron wiring** (ADR-0176): Phase 2b-2 — ADR-0173 Phase 1 의 MissedLearningQueue SSOT (호출자 0건 dead code) 첫 wiring. **scheduleGuard.ts hook** — `ScheduleGuardOptions.enqueueOnSkip?: { replayPolicy?: ReplayPolicy }` 옵셔널 인자 추가. `scheduledJob` 의 `if (decision.skip)` 분기 안 (edge-trigger log + metric 직후) 에 hook 호출 — `options.enqueueOnSkip && isMissedLearningQueueEnabled()` 통과 시에만 `enqueueMissedLearningJob({ jobName, reason, skippedAt, replayPolicy, idempotencyKey: jobName + ':' + YYYYMMDD })` 호출. **5 학습 cron wiring** — `learningJobs.ts` 의 counterfactual_resolve / ledger_resolve / ghost_portfolio / nightly_reflection / daily_mini_backtest 5 호출에 `enqueueOnSkip: {}` 옵션 전달 (default replayPolicy='SAFE_NEXT_TRADING_DAY'). 잔여 2 학습 작업 (shadow_live_delta_report / safety_gate_attribution) cron 자체 부재 — Phase 4 Dashboard 후속 PR scope. **신규 replay cron** — `'30 0 * * 1-5'` (UTC 평일 00:30 = **KST 평일 09:30** 장 시작 30분 전) + ScheduleClass='TRADING_DAY_ONLY' (KRX 휴장일 자동 silent skip) + jobName='missed_learning_replay' + ENV gate 안에서 `replayMissedLearningJobs({tradingDate, maxJobsPerRun: 10})` 호출 (jobName → 실제 함수 매핑은 ADR-0173 §1 plumbing only 그대로, Phase 3 후속 PR 에서 dispatcher 추가). **안전 invariant 7종**: (1) **LIVE 매매 본체 0줄 변경** — signalScanner/entryEngine/exitEngine/kisClient/orchestrator/autoTradeEngine 무수정. (2) **KIS 주문 함수 import 0건** (정적 grep 가드). (3) **기존 cron 동작 100% 보존** — scheduleGuard 옵션 미전달 79+ cron 모두 무영향 (옵셔널 인자 패턴). (4) **ENV default OFF** — `MISSED_LEARNING_QUEUE_ENABLED` 미설정 시 hook + replay cron 모두 no-op. (5) **화이트리스트 5 학습 cron** — 다른 cron 에 `enqueueOnSkip` 옵션 전달 0건 (정적 grep 가드). (6) **enqueue throw catch** — scheduleGuard hook 의 try/catch (cron 흐름 차단 안 함). (7) **호출자 1건 (replay cron 만)** — `replayMissedLearningJobs` 호출 정확 1건. **Phase 3/4 wiring 정책** — Phase 3: `replayMissedLearningJobs` jobName 매핑 dispatcher / Phase 4: shadow_live_delta_report + safety_gate_attribution cron 등록.

- **FutureReturnResolver cron** (ADR-0175): Phase 2b-1 영속 갱신 cron — Phase 1 `ShadowLearningOnlySignal` 의 `futureReturn1d/3d/5d/20d` + `outcome` 필드 갱신. **신규 모듈** `server/learning/futureReturnResolver.ts` — `resolveFutureReturns(input?)` async 진입점 + `priceFetcher` 의존성 주입 (default 미정의 — Phase 2b-2/3 wiring 결정 위임, 미전달 시 모든 signal `errors++` + console.warn 1회 안전 default, KIS quota 0 보장) + `isFutureReturnResolverEnabled()` ENV 헬퍼. **결정 트리**: closed signal (outcome ∈ {WIN/LOSS/BE}) skip / signalDate + horizon 영업일 도달 시 horizon 별 갱신 / 20d 도달 + 모든 horizon 갱신 완료 시 outcome 분류 (ADR-0112 임계 정합 — `WIN > +1.0% / LOSS < -0.5% / BE 그 외`). **cron 등록** `server/scheduler/learningJobs.ts` `30 7 * * 1-5` (UTC 평일 07:30 = **KST 평일 16:30** 장 마감 30분 후) + ScheduleClass='TRADING_DAY_ONLY' (KRX 휴장일 자동 silent skip, ADR-0045 정합). **안전 invariant 6종**: (1) **LIVE 매매 본체 0줄 변경** — signalScanner/entryEngine/exitEngine/kisClient/orchestrator/autoTradeEngine 무수정. (2) **KIS 주문 함수 import 0건** (정적 grep 가드 — 학습 영속 갱신 전용). (3) **KIS 호출은 read-only 만** — `fetchKisCurrentPrice` 등 (절대 규칙 #2 정합). (4) **ENV default OFF** — `FUTURE_RETURN_RESOLVER_ENABLED` 미설정 시 cron 진입부 즉시 return + 외부 호출 0건. (5) **호출자 1건 (cron 만)** — 다른 호출자 추가 후속 PR. (6) **closed signal 재계산 금지** — outcome 결정된 signal 재방문 차단 (불변성). **호출자 wiring 후 효과** — Phase 1 데이터 0건 라 현재 호출 0건, Phase 3 wiring 후 데이터 누적 시작 시 cron 1회당 signal × horizon 회 KIS 호출 (100 signal 시 최대 400회, 일일 한도 충분).

- **Cumulative reflection context for self-learning** (ADR-0130): 자기학습 시스템의 *stateless* 결함을 차단하는 입력 채널 SSOT. **MainReflectionInputs 옵셔널 확장** — `recentReflections?: RecentReflectionContext[]` (직전 7일 verdict + tomorrowAdjustments + biasTop3 + narrativeFirstSentence) + `activeFailurePatterns?: ActiveFailurePatternSummary[]` (TTL 180일 활성 패턴 Top 5, exitDate 내림차순). `mainReflection.formatNarrativeInput` 이 narrative 끝부분에 3 섹션 추가 — `📅 직전 N일 자기반성 누적 컨텍스트` + `❓ 어제 너는 다음과 같이 조정한다고 했다 — 적용되었는가` + `🚨 누적 실패 패턴 N건 active`. **`nightlyReflectionEngine`** 이 mainInput 빌드 시 `buildRecentReflectionContexts(date, 7)` + `buildActiveFailurePatternSummaries(5)` 헬퍼 호출 (영속 read 만, 외부 호출 0건). **/learning_loop_health 진단 명령** — `server/learning/learningLoopHealth.ts` 7 지표 (narrativeSimilarity7d Jaccard / biasScoreVariance7d / failurePatternIngestion / reflectionImpactPhase 1=true 2=false 영구 / tomorrowVsNarrativeOverlap7d / signalScannerWeightLastUpdate=false 영구 / silentVerdictRatio7d) + 종합 verdict 4분기 (HEALTHY/DEGRADED/STATELESS/INSUFFICIENT_DATA). **ENV 우회 2종** — `LEARNING_RECENT_REFLECTIONS_DISABLED` / `LEARNING_FAILURE_PATTERN_INPUT_DISABLED` (default false=정책 적용). **본 PR 입력 채널만** — Gemini 가 매일 백지 상태에서 오늘만 보던 stateless 결함 차단. 행동 변경 채널 (가중치/Kelly/Gate 임계 자동 조정) 은 후속 PR-Fix-3/4 (biasHeatmap stateful + reflectionImpactRecorder Phase 2 wiring). 사용자 4/30 진단 (스크린샷 narrative ≈ tomorrow's learning point + 7일 RECENCY 1.00 / HERDING 0.99~1.00) 직접 반영.
- **recommendation lane-split boundary** (ADR-0549 PR-2): `server/trading/recommendation/gate1LaneResult.ts` (`buildGate1LaneResult`) + `recommendationScore.ts` (`scoreRecommendationCandidate`) 는 자동매매 트리(signalScanner/gates/buyPipeline)와 물리적으로 분리된 *read-only 추천/관측* read-model — `executionImpact:'NONE'` · `recommendationOnly:true` · `buySignal:false` 리터럴 고정, `scripts/check_rally_lens_isolation.js` 가 주문/shadow-buy/paper 생성 import 정적 차단. lane 은 `LIVE_HARD_PASS`(`liveHardPass === (gate1Passed && minSignalScorePassed)` 미러식, `gate1DryRunObservationLedgerAdr0476.ts:500` 동일 식 — `LIVE_HARD_PASS_BYTE_IDENTICAL`, Gate1 평가/임계 재계산 0줄) / `SEARCH_RECOMMENDATION_PASS`(finalScore=`CandidateSnapshot.gateScore` read-only degrade-safe OR) / `INDEX_RALLY_WATCH_PASS`(`MarketRallyLens.enabled` 게이트, providerIssue→비전환 `RALLY_UNKNOWN_PROVIDER_ISSUE` 불변식#6). RecommendationScore 는 GateScore 와 별도 척도 — gateScore 입력 참조만, 역류 0 (`RECOMMENDATION_SCORE_SEPARATED_FROM_GATE_SCORE`). ENV `MARKET_RALLY_LENS_ENABLED` OFF default → `enabled:false` byte-equivalent.

---

## Planned Decompositions (advisory)

The following files currently exceed `scripts/check_complexity.js` thresholds. Their
planned decomposition is documented in `docs/adr/`. Until migration completes, the
current file continues to own the listed responsibility above.

| File | Lines | ADR | Planned sub-modules |
|------|------:|-----|---------------------|
| `server/trading/signalScanner.ts` | 1,820 | [ADR-0001](./docs/adr/0001-signalScanner-decomposition.md) | `signalScanner/{index,preflight,candidateSelect,perSymbolEvaluation,approvalQueue,scanDiagnostics}` |
| `server/telegram/webhookHandler.ts` | 1,700 | TBD (P1) | — |
| `server/screener/stockScreener.ts` | 1,571 | TBD (P1) | — |
| ~~`server/trading/exitEngine.ts`~~ | 18 (barrel) | [ADR-0028](./docs/adr/0028-exitEngine-decomposition.md) ✅ | `exitEngine/{index,types,helpers/*,rules/*}` 분해 완료 (PR-53) |

When implementing a decomposition, follow `.claude/skills/server-refactor-orchestrator/SKILL.md` 6-Phase flow and update this table.
